//! Auto-generated API clients for gcp/discoveryengine.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/discoveryengine`

#![cfg(feature = "gcp/discoveryengine")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/projects/{projectsId}:provision
/// Provisions the project resource. During the process, related systems will get prepared and initialized. Caller must read the [Terms for data use](<https://cloud.google.`com/retail/data-use-terms`>), and optionally specify in request to provide consent to that service terms.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_provision_execute()` to send, or `discoveryengine_projects_provision` for simplest API.

pub fn discoveryengine_projects_provision_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ProvisionProjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}:provision",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:provision
/// Provisions the project resource. During the process, related systems will get prepared and initialized. Caller must read the [Terms for data use](<https://cloud.google.`com/retail/data-use-terms`>), and optionally specify in request to provide consent to that service terms.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_provision_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_provision_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_provision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:provision
/// Provisions the project resource. During the process, related systems will get prepared and initialized. Caller must read the [Terms for data use](<https://cloud.google.`com/retail/data-use-terms`>), and optionally specify in request to provide consent to that service terms.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_provision_builder()` + `discoveryengine_projects_provision_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_provision(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ProvisionProjectRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_provision_builder(client, name, body)?;
    discoveryengine_projects_provision_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclConfig
/// Gets the AclConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_get_acl_config_execute()` to send, or `discoveryengine_projects_locations_get_acl_config` for simplest API.

pub fn discoveryengine_projects_locations_get_acl_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/aclConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclConfig
/// Gets the AclConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_get_acl_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_get_acl_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_get_acl_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AclConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AclConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclConfig
/// Gets the AclConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_get_acl_config_builder()` + `discoveryengine_projects_locations_get_acl_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_get_acl_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AclConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_get_acl_config_builder(client, name)?;
    discoveryengine_projects_locations_get_acl_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfig
/// Gets the CmekConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_get_cmek_config_execute()` to send, or `discoveryengine_projects_locations_get_cmek_config` for simplest API.

pub fn discoveryengine_projects_locations_get_cmek_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/cmekConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfig
/// Gets the CmekConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_get_cmek_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_get_cmek_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_get_cmek_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CmekConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1CmekConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfig
/// Gets the CmekConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_get_cmek_config_builder()` + `discoveryengine_projects_locations_get_cmek_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_get_cmek_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CmekConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_get_cmek_config_builder(client, name)?;
    discoveryengine_projects_locations_get_cmek_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:setUpDataConnector
/// Creates a Collection and sets up the DataConnector for it. To stop a DataConnector after setup, use the CollectionService.DeleteCollection method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_set_up_data_connector_execute()` to send, or `discoveryengine_projects_locations_set_up_data_connector` for simplest API.

pub fn discoveryengine_projects_locations_set_up_data_connector_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1SetUpDataConnectorRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}:setUpDataConnector",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:setUpDataConnector
/// Creates a Collection and sets up the DataConnector for it. To stop a DataConnector after setup, use the CollectionService.DeleteCollection method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_set_up_data_connector_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_set_up_data_connector_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_set_up_data_connector_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:setUpDataConnector
/// Creates a Collection and sets up the DataConnector for it. To stop a DataConnector after setup, use the CollectionService.DeleteCollection method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_set_up_data_connector_builder()` + `discoveryengine_projects_locations_set_up_data_connector_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_set_up_data_connector(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1SetUpDataConnectorRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_set_up_data_connector_builder(client, parent, body)?;
    discoveryengine_projects_locations_set_up_data_connector_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:setUpDataConnectorV2
/// Creates a Collection and sets up the DataConnector for it. To stop a DataConnector after setup, use the CollectionService.DeleteCollection method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_set_up_data_connector_v2_execute()` to send, or `discoveryengine_projects_locations_set_up_data_connector_v2` for simplest API.

pub fn discoveryengine_projects_locations_set_up_data_connector_v2_builder(
    client: &SimpleHttpClient,
    parent: &str,
    collectionDisplayName: Option<&str>,
    collectionId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataConnector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}:setUpDataConnectorV2",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = collectionDisplayName {
        query_parts.push(format!("collectionDisplayName={}", val));
    }
    if let Some(val) = collectionId {
        query_parts.push(format!("collectionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:setUpDataConnectorV2
/// Creates a Collection and sets up the DataConnector for it. To stop a DataConnector after setup, use the CollectionService.DeleteCollection method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_set_up_data_connector_v2_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_set_up_data_connector_v2_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_set_up_data_connector_v2_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:setUpDataConnectorV2
/// Creates a Collection and sets up the DataConnector for it. To stop a DataConnector after setup, use the CollectionService.DeleteCollection method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_set_up_data_connector_v2_builder()` + `discoveryengine_projects_locations_set_up_data_connector_v2_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_set_up_data_connector_v2(
    client: &SimpleHttpClient,
    parent: &str,
    collectionDisplayName: Option<&str>,
    collectionId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataConnector,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_set_up_data_connector_v2_builder(
        client,
        parent,
        collectionDisplayName,
        collectionId,
        body,
    )?;
    discoveryengine_projects_locations_set_up_data_connector_v2_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclConfig
/// Default ACL configuration for use in a location of a customer's project. Updates will only reflect to new data stores. Existing data stores will still use the old value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_update_acl_config_execute()` to send, or `discoveryengine_projects_locations_update_acl_config` for simplest API.

pub fn discoveryengine_projects_locations_update_acl_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1AclConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/aclConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclConfig
/// Default ACL configuration for use in a location of a customer's project. Updates will only reflect to new data stores. Existing data stores will still use the old value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_update_acl_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_update_acl_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_update_acl_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AclConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AclConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/aclConfig
/// Default ACL configuration for use in a location of a customer's project. Updates will only reflect to new data stores. Existing data stores will still use the old value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_update_acl_config_builder()` + `discoveryengine_projects_locations_update_acl_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_update_acl_config(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1AclConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AclConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_update_acl_config_builder(client, name, body)?;
    discoveryengine_projects_locations_update_acl_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfig
/// Provisions a CMEK key for use in a location of a customer's project. This method will also conduct location validation on the provided `cmekConfig` to make sure the key is valid and can be used in the selected location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_update_cmek_config_execute()` to send, or `discoveryengine_projects_locations_update_cmek_config` for simplest API.

pub fn discoveryengine_projects_locations_update_cmek_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    setDefault: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1CmekConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/cmekConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = setDefault {
        query_parts.push(format!("setDefault={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfig
/// Provisions a CMEK key for use in a location of a customer's project. This method will also conduct location validation on the provided `cmekConfig` to make sure the key is valid and can be used in the selected location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_update_cmek_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_update_cmek_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_update_cmek_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfig
/// Provisions a CMEK key for use in a location of a customer's project. This method will also conduct location validation on the provided `cmekConfig` to make sure the key is valid and can be used in the selected location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_update_cmek_config_builder()` + `discoveryengine_projects_locations_update_cmek_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_update_cmek_config(
    client: &SimpleHttpClient,
    name: &str,
    setDefault: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1CmekConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_update_cmek_config_builder(
        client, name, setDefault, body,
    )?;
    discoveryengine_projects_locations_update_cmek_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// De-provisions a CmekConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_cmek_configs_delete_execute()` to send, or `discoveryengine_projects_locations_cmek_configs_delete` for simplest API.

pub fn discoveryengine_projects_locations_cmek_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/cmekConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// De-provisions a CmekConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_cmek_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// De-provisions a CmekConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_delete_builder()` + `discoveryengine_projects_locations_cmek_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_cmek_configs_delete_builder(client, name)?;
    discoveryengine_projects_locations_cmek_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// Gets the CmekConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_cmek_configs_get_execute()` to send, or `discoveryengine_projects_locations_cmek_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_cmek_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/cmekConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// Gets the CmekConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_cmek_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CmekConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1CmekConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// Gets the CmekConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_get_builder()` + `discoveryengine_projects_locations_cmek_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CmekConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_cmek_configs_get_builder(client, name)?;
    discoveryengine_projects_locations_cmek_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs
/// Lists all the CmekConfigs with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_cmek_configs_list_execute()` to send, or `discoveryengine_projects_locations_cmek_configs_list` for simplest API.

pub fn discoveryengine_projects_locations_cmek_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/cmekConfigs",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs
/// Lists all the CmekConfigs with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_cmek_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListCmekConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListCmekConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs
/// Lists all the CmekConfigs with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_list_builder()` + `discoveryengine_projects_locations_cmek_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListCmekConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_cmek_configs_list_builder(client, parent)?;
    discoveryengine_projects_locations_cmek_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// Provisions a CMEK key for use in a location of a customer's project. This method will also conduct location validation on the provided `cmekConfig` to make sure the key is valid and can be used in the selected location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_cmek_configs_patch_execute()` to send, or `discoveryengine_projects_locations_cmek_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_cmek_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    setDefault: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1CmekConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/cmekConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = setDefault {
        query_parts.push(format!("setDefault={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// Provisions a CMEK key for use in a location of a customer's project. This method will also conduct location validation on the provided `cmekConfig` to make sure the key is valid and can be used in the selected location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_cmek_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/cmekConfigs/{cmekConfigsId}
/// Provisions a CMEK key for use in a location of a customer's project. This method will also conduct location validation on the provided `cmekConfig` to make sure the key is valid and can be used in the selected location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_cmek_configs_patch_builder()` + `discoveryengine_projects_locations_cmek_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_cmek_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    setDefault: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1CmekConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_cmek_configs_patch_builder(
        client, name, setDefault, body,
    )?;
    discoveryengine_projects_locations_cmek_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}
/// Deletes a Collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_delete_execute()` to send, or `discoveryengine_projects_locations_collections_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}
/// Deletes a Collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}
/// Deletes a Collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_delete_builder()` + `discoveryengine_projects_locations_collections_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_delete_builder(client, name)?;
    discoveryengine_projects_locations_collections_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector
/// Gets the DataConnector. DataConnector is a singleton resource for each Collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_get_data_connector_execute()` to send, or `discoveryengine_projects_locations_collections_get_data_connector` for simplest API.

pub fn discoveryengine_projects_locations_collections_get_data_connector_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataConnector",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector
/// Gets the DataConnector. DataConnector is a singleton resource for each Collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_get_data_connector_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_get_data_connector_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_get_data_connector_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataConnector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1DataConnector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector
/// Gets the DataConnector. DataConnector is a singleton resource for each Collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_get_data_connector_builder()` + `discoveryengine_projects_locations_collections_get_data_connector_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_get_data_connector(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataConnector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_get_data_connector_builder(client, name)?;
    discoveryengine_projects_locations_collections_get_data_connector_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector
/// Updates a DataConnector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_update_data_connector_execute()` to send, or `discoveryengine_projects_locations_collections_update_data_connector` for simplest API.

pub fn discoveryengine_projects_locations_collections_update_data_connector_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataConnector,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataConnector",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector
/// Updates a DataConnector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_update_data_connector_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_update_data_connector_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_update_data_connector_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataConnector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1DataConnector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector
/// Updates a DataConnector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_update_data_connector_builder()` + `discoveryengine_projects_locations_collections_update_data_connector_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_update_data_connector(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataConnector,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataConnector>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_update_data_connector_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_collections_update_data_connector_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_connector_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_connector_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_connector_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataConnector/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_connector_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_connector_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_connector_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_connector_operations_get_builder()` + `discoveryengine_projects_locations_collections_data_connector_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_connector_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_connector_operations_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_connector_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_connector_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_connector_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_connector_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataConnector/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_connector_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_connector_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_connector_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataConnector/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_connector_operations_list_builder()` + `discoveryengine_projects_locations_collections_data_connector_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_connector_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_connector_operations_list_builder(
            client,
            name,
            filter,
            pageSize,
            pageToken,
            returnPartialSuccess,
        )?;
    discoveryengine_projects_locations_collections_data_connector_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}:completeQuery
/// Completes the specified user input with keyword suggestions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_complete_query_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_complete_query` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_complete_query_builder(
    client: &SimpleHttpClient,
    dataStore: &str,
    includeTailSuggestions: Option<bool>,
    query: Option<&str>,
    queryModel: Option<&str>,
    userPseudoId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}:completeQuery",
        dataStore,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeTailSuggestions {
        query_parts.push(format!("includeTailSuggestions={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = queryModel {
        query_parts.push(format!("queryModel={}", val));
    }
    if let Some(val) = userPseudoId {
        query_parts.push(format!("userPseudoId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}:completeQuery
/// Completes the specified user input with keyword suggestions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_complete_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_complete_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_complete_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CompleteQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1CompleteQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}:completeQuery
/// Completes the specified user input with keyword suggestions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_complete_query_builder()` + `discoveryengine_projects_locations_collections_data_stores_complete_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_complete_query(
    client: &SimpleHttpClient,
    dataStore: &str,
    includeTailSuggestions: Option<bool>,
    query: Option<&str>,
    queryModel: Option<&str>,
    userPseudoId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CompleteQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_complete_query_builder(
            client,
            dataStore,
            includeTailSuggestions,
            query,
            queryModel,
            userPseudoId,
        )?;
    discoveryengine_projects_locations_collections_data_stores_complete_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores
/// Creates a DataStore. DataStore is for storing Documents. To serve these documents for Search, or Recommendation use case, an Engine needs to be created separately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    cmekConfigName: Option<&str>,
    createAdvancedSiteSearch: Option<bool>,
    dataStoreId: Option<&str>,
    disableCmek: Option<bool>,
    skipDefaultSchemaCreation: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cmekConfigName {
        query_parts.push(format!("cmekConfigName={}", val));
    }
    if let Some(val) = createAdvancedSiteSearch {
        query_parts.push(format!("createAdvancedSiteSearch={}", val));
    }
    if let Some(val) = dataStoreId {
        query_parts.push(format!("dataStoreId={}", val));
    }
    if let Some(val) = disableCmek {
        query_parts.push(format!("disableCmek={}", val));
    }
    if let Some(val) = skipDefaultSchemaCreation {
        query_parts.push(format!("skipDefaultSchemaCreation={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores
/// Creates a DataStore. DataStore is for storing Documents. To serve these documents for Search, or Recommendation use case, an Engine needs to be created separately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores
/// Creates a DataStore. DataStore is for storing Documents. To serve these documents for Search, or Recommendation use case, an Engine needs to be created separately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_create(
    client: &SimpleHttpClient,
    parent: &str,
    cmekConfigName: Option<&str>,
    createAdvancedSiteSearch: Option<bool>,
    dataStoreId: Option<&str>,
    disableCmek: Option<bool>,
    skipDefaultSchemaCreation: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_create_builder(
        client,
        parent,
        cmekConfigName,
        createAdvancedSiteSearch,
        dataStoreId,
        disableCmek,
        skipDefaultSchemaCreation,
        body,
    )?;
    discoveryengine_projects_locations_collections_data_stores_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Deletes a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Deletes a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Deletes a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_delete_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Gets a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Gets a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1DataStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Gets a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine
/// Gets the SiteSearchEngine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_get_site_search_engine` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine
/// Gets the SiteSearchEngine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SiteSearchEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SiteSearchEngine =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine
/// Gets the SiteSearchEngine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_builder()` + `discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_get_site_search_engine(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SiteSearchEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_get_site_search_engine_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores
/// Lists all the DataStores associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores
/// Lists all the DataStores associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDataStoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListDataStoresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores
/// Lists all the DataStores associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDataStoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_data_stores_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Updates a DataStore
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Updates a DataStore
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1DataStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}
/// Updates a DataStore
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_collections_data_stores_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}:trainCustomModel
/// Trains a custom model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_train_custom_model_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_train_custom_model` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_train_custom_model_builder(
    client: &SimpleHttpClient,
    dataStore: &str,
    body: &GoogleCloudDiscoveryengineV1TrainCustomModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}:trainCustomModel",
        dataStore,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}:trainCustomModel
/// Trains a custom model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_train_custom_model_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_train_custom_model_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_train_custom_model_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}:trainCustomModel
/// Trains a custom model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_train_custom_model_builder()` + `discoveryengine_projects_locations_collections_data_stores_train_custom_model_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_train_custom_model(
    client: &SimpleHttpClient,
    dataStore: &str,
    body: &GoogleCloudDiscoveryengineV1TrainCustomModelRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_train_custom_model_builder(
            client, dataStore, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_train_custom_model_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/batchGetDocumentsMetadata
/// Gets index freshness metadata for Documents. Supported for website search only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_builder(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_fhirMatcher_fhirResources: Option<&str>,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/batchGetDocumentsMetadata",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = matcher_fhirMatcher_fhirResources {
        query_parts.push(format!("matcher_fhirMatcher_fhirResources={}", val));
    }
    if let Some(val) = matcher_urisMatcher_uris {
        query_parts.push(format!("matcher_urisMatcher_uris={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/batchGetDocumentsMetadata
/// Gets index freshness metadata for Documents. Supported for website search only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1BatchGetDocumentsMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1BatchGetDocumentsMetadataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/batchGetDocumentsMetadata
/// Gets index freshness metadata for Documents. Supported for website search only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_fhirMatcher_fhirResources: Option<&str>,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1BatchGetDocumentsMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_builder(client, parent, matcher_fhirMatcher_fhirResources, matcher_urisMatcher_uris)?;
    discoveryengine_projects_locations_collections_data_stores_branches_batch_get_documents_metadata_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Creates a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_documents_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_documents_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    documentId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/documents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = documentId {
        query_parts.push(format!("documentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Creates a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_documents_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Creates a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_documents_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_create(
    client: &SimpleHttpClient,
    parent: &str,
    documentId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_documents_create_builder(client, parent, documentId, body)?;
    discoveryengine_projects_locations_collections_data_stores_branches_documents_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Deletes a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_documents_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/documents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Deletes a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Deletes a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_branches_documents_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Gets a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_documents_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_documents_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/documents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Gets a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_documents_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Gets a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_documents_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_branches_documents_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_branches_documents_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:import
/// Bulk import of multiple Documents. Request processing may be synchronous. Non-existing items are created. Note: It is possible for a subset of the Documents to be successfully updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_documents_import_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_documents_import` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportDocumentsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/documents:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:import
/// Bulk import of multiple Documents. Request processing may be synchronous. Non-existing items are created. Note: It is possible for a subset of the Documents to be successfully updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_documents_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:import
/// Bulk import of multiple Documents. Request processing may be synchronous. Non-existing items are created. Note: It is possible for a subset of the Documents to be successfully updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_import_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_documents_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportDocumentsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_documents_import_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_branches_documents_import_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Gets a list of Documents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_documents_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_documents_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/documents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Gets a list of Documents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_documents_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDocumentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListDocumentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Gets a list of Documents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_documents_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDocumentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_branches_documents_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    discoveryengine_projects_locations_collections_data_stores_branches_documents_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Updates a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_documents_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/documents/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Updates a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Updates a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_builder(client, name, allowMissing, updateMask, body)?;
    discoveryengine_projects_locations_collections_data_stores_branches_documents_patch_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:purge
/// Permanently deletes all selected Documents in a branch. This process is asynchronous. Depending on the number of Documents to be deleted, this operation can take hours to complete. Before the delete operation completes, some Documents might still be returned by DocumentService.GetDocument or DocumentService.ListDocuments. To get a list of the Documents to be deleted, set PurgeDocumentsRequest.force to `false`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_documents_purge` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeDocumentsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/documents:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:purge
/// Permanently deletes all selected Documents in a branch. This process is asynchronous. Depending on the number of Documents to be deleted, this operation can take hours to complete. Before the delete operation completes, some Documents might still be returned by DocumentService.GetDocument or DocumentService.ListDocuments. To get a list of the Documents to be deleted, set PurgeDocumentsRequest.force to `false`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:purge
/// Permanently deletes all selected Documents in a branch. This process is asynchronous. Depending on the number of Documents to be deleted, this operation can take hours to complete. Before the delete operation completes, some Documents might still be returned by DocumentService.GetDocument or DocumentService.ListDocuments. To get a list of the Documents to be deleted, set PurgeDocumentsRequest.force to `false`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_documents_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeDocumentsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_branches_documents_purge_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_builder(client, name, body)?;
    discoveryengine_projects_locations_collections_data_stores_branches_operations_cancel_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_operations_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_branches_operations_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_branches_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_branches_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_branches_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/branches/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_branches_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_branches_operations_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_branches_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_branches_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_branches_operations_list_builder(client, name, filter, pageSize, pageToken, returnPartialSuccess)?;
    discoveryengine_projects_locations_collections_data_stores_branches_operations_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_builder(
    client: &SimpleHttpClient,
    completionConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AdvancedCompleteQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/completionConfig:completeQuery",
        completionConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_builder()` + `discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query(
    client: &SimpleHttpClient,
    completionConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AdvancedCompleteQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_builder(client, completionConfig, body)?;
    discoveryengine_projects_locations_collections_data_stores_completion_config_complete_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionSuggestions:import
/// Imports CompletionSuggestions for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportCompletionSuggestionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/completionSuggestions:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionSuggestions:import
/// Imports CompletionSuggestions for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionSuggestions:import
/// Imports CompletionSuggestions for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_builder()` + `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportCompletionSuggestionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_completion_suggestions_import_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionSuggestions:purge
/// Permanently deletes all CompletionSuggestions for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeCompletionSuggestionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/completionSuggestions:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionSuggestions:purge
/// Permanently deletes all CompletionSuggestions for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/completionSuggestions:purge
/// Permanently deletes all CompletionSuggestions for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_builder()` + `discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeCompletionSuggestionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_completion_suggestions_purge_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_controls_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_controls_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    controlId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = controlId {
        query_parts.push(format!("controlId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_controls_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_controls_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_create(
    client: &SimpleHttpClient,
    parent: &str,
    controlId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_controls_create_builder(
            client, parent, controlId, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_controls_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_controls_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_controls_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/controls/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_controls_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_controls_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_controls_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_controls_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Gets a Control.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_controls_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_controls_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/controls/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Gets a Control.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_controls_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Gets a Control.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_controls_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_controls_get_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_data_stores_controls_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_controls_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_controls_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_controls_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListControlsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_controls_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_controls_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_data_stores_controls_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_controls_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_controls_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/controls/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_controls_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_controls_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_controls_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_controls_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_controls_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_controls_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_conversations_converse_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_conversations_converse` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_converse_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ConverseConversationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/conversations/{}:converse",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_converse_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_conversations_converse_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_converse_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ConverseConversationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ConverseConversationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_converse_builder()` + `discoveryengine_projects_locations_collections_data_stores_conversations_converse_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_converse(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ConverseConversationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ConverseConversationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_conversations_converse_builder(
            client, name, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_conversations_converse_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_conversations_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_conversations_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/conversations",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_conversations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_conversations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_conversations_create_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_conversations_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_conversations_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_conversations_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/conversations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_conversations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_conversations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_conversations_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_conversations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_conversations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_conversations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/conversations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_conversations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_conversations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_conversations_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_conversations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_conversations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_conversations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/conversations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_conversations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListConversationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_conversations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_conversations_list_builder(
            client, parent, filter, orderBy, pageSize, pageToken,
        )?;
    discoveryengine_projects_locations_collections_data_stores_conversations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_conversations_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_conversations_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_conversations_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_conversations_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_conversations_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_conversations_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_conversations_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_conversations_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/customModels
/// Gets a list of all the custom models.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_custom_models_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_custom_models_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_custom_models_list_builder(
    client: &SimpleHttpClient,
    dataStore: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/customModels",
        dataStore,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/customModels
/// Gets a list of all the custom models.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_custom_models_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_custom_models_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_custom_models_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListCustomModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListCustomModelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/customModels
/// Gets a list of all the custom models.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_custom_models_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_custom_models_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_custom_models_list(
    client: &SimpleHttpClient,
    dataStore: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListCustomModelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_custom_models_list_builder(
            client, dataStore,
        )?;
    discoveryengine_projects_locations_collections_data_stores_custom_models_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_models_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_models_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_models_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/models/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_models_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_models_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_models_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_models_operations_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_models_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_models_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_models_operations_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_models_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_models_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_models_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_models_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/models/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_models_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_models_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_models_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_models_operations_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_models_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_models_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_models_operations_list_builder(
            client,
            name,
            filter,
            pageSize,
            pageToken,
            returnPartialSuccess,
        )?;
    discoveryengine_projects_locations_collections_data_stores_models_operations_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_operations_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_operations_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_operations_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_operations_list_builder(
            client,
            name,
            filter,
            pageSize,
            pageToken,
            returnPartialSuccess,
        )?;
    discoveryengine_projects_locations_collections_data_stores_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas
/// Creates a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_schemas_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_schemas_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    schemaId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = schemaId {
        query_parts.push(format!("schemaId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas
/// Creates a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_schemas_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas
/// Creates a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_schemas_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_create(
    client: &SimpleHttpClient,
    parent: &str,
    schemaId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_schemas_create_builder(
            client, parent, schemaId, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_schemas_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Deletes a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_schemas_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_schemas_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/schemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Deletes a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_schemas_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Deletes a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_schemas_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_schemas_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_schemas_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Gets a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_schemas_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_schemas_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/schemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Gets a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_schemas_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Schema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Schema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Gets a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_schemas_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Schema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_schemas_get_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_data_stores_schemas_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas
/// Gets a list of Schemas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_schemas_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_schemas_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas
/// Gets a list of Schemas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_schemas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas
/// Gets a list of Schemas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_schemas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_schemas_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_data_stores_schemas_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Updates a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_schemas_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_schemas_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/schemas/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Updates a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_schemas_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Updates a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_schemas_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_schemas_patch_builder(
        client,
        name,
        allowMissing,
        body,
    )?;
    discoveryengine_projects_locations_collections_data_stores_schemas_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_schemas_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/schemas/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_schemas_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_schemas_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/schemas/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/schemas/{schemasId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_schemas_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_builder(
            client,
            name,
            filter,
            pageSize,
            pageToken,
            returnPartialSuccess,
        )?;
    discoveryengine_projects_locations_collections_data_stores_schemas_operations_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_answer` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}:answer",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AnswerQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_answer(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_answer_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    servingConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = servingConfigId {
        query_parts.push(format!("servingConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    servingConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_serving_configs_create_builder(
            client,
            parent,
            servingConfigId,
            body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_serving_configs_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListServingConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListServingConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListServingConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_serving_configs_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_patch_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RecommendRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}:recommend",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RecommendResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1RecommendResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RecommendRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RecommendResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_builder(client, servingConfig, body)?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_recommend_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_search` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_search_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}:search",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_search(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_serving_configs_search_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_search_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}:searchLite",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_builder(client, servingConfig, body)?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_search_lite_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/servingConfigs/{}:streamAnswer",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AnswerQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_builder()` + `discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_builder(client, servingConfig, body)?;
    discoveryengine_projects_locations_collections_data_stores_serving_configs_stream_answer_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_sessions_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_sessions_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/sessions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_sessions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_sessions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_sessions_create_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_sessions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_sessions_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_sessions_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/sessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_sessions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_sessions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_sessions_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_sessions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_sessions_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_sessions_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    includeAnswerDetails: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeAnswerDetails {
        query_parts.push(format!("includeAnswerDetails={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_sessions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_sessions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_get(
    client: &SimpleHttpClient,
    name: &str,
    includeAnswerDetails: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_sessions_get_builder(
        client,
        name,
        includeAnswerDetails,
    )?;
    discoveryengine_projects_locations_collections_data_stores_sessions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_sessions_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_sessions_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/sessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_sessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListSessionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_sessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_sessions_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_data_stores_sessions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_sessions_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_sessions_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_sessions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_sessions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_sessions_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_sessions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_sessions_answers_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/sessions/{}/answers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Answer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Answer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_sessions_answers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Answer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_data_stores_sessions_answers_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:batchVerifyTargetSites
/// Verify target sites' ownership and validity. This API sends all the target sites under site search engine for verification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchVerifyTargetSitesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine:batchVerifyTargetSites",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:batchVerifyTargetSites
/// Verify target sites' ownership and validity. This API sends all the target sites under site search engine for verification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:batchVerifyTargetSites
/// Verify target sites' ownership and validity. This API sends all the target sites under site search engine for verification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchVerifyTargetSitesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_batch_verify_target_sites_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:disableAdvancedSiteSearch
/// Downgrade from advanced site search to basic site search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_builder(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1DisableAdvancedSiteSearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine:disableAdvancedSiteSearch",
        siteSearchEngine,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:disableAdvancedSiteSearch
/// Downgrade from advanced site search to basic site search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:disableAdvancedSiteSearch
/// Downgrade from advanced site search to basic site search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1DisableAdvancedSiteSearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_builder(client, siteSearchEngine, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_disable_advanced_site_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:enableAdvancedSiteSearch
/// Upgrade from basic site search to advanced site search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_builder(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1EnableAdvancedSiteSearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine:enableAdvancedSiteSearch",
        siteSearchEngine,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:enableAdvancedSiteSearch
/// Upgrade from basic site search to advanced site search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:enableAdvancedSiteSearch
/// Upgrade from basic site search to advanced site search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1EnableAdvancedSiteSearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_builder(client, siteSearchEngine, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_enable_advanced_site_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:fetchDomainVerificationStatus
/// Returns list of target sites with its domain verification status. This method can only be called under data store with BASIC_SITE_SEARCH state at the moment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_builder(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine:fetchDomainVerificationStatus",
        siteSearchEngine,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:fetchDomainVerificationStatus
/// Returns list of target sites with its domain verification status. This method can only be called under data store with BASIC_SITE_SEARCH state at the moment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1FetchDomainVerificationStatusResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1FetchDomainVerificationStatusResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:fetchDomainVerificationStatus
/// Returns list of target sites with its domain verification status. This method can only be called under data store with BASIC_SITE_SEARCH state at the moment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1FetchDomainVerificationStatusResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_builder(client, siteSearchEngine, pageSize, pageToken)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_fetch_domain_verification_status_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:recrawlUris
/// Request on-demand recrawl for a list of URIs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_builder(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1RecrawlUrisRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine:recrawlUris",
        siteSearchEngine,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:recrawlUris
/// Request on-demand recrawl for a list of URIs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine:recrawlUris
/// Request on-demand recrawl for a list of URIs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1RecrawlUrisRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_builder(client, siteSearchEngine, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_recrawl_uris_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_builder(client, name, filter, pageSize, pageToken, returnPartialSuccess)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps
/// Creates a Sitemap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Sitemap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/sitemaps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps
/// Creates a Sitemap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps
/// Creates a Sitemap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Sitemap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps/{sitemapsId}
/// Deletes a Sitemap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/sitemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps/{sitemapsId}
/// Deletes a Sitemap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps/{sitemapsId}
/// Deletes a Sitemap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps:fetch
/// Fetch Sitemaps in a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_builder(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/sitemaps:fetch",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = matcher_urisMatcher_uris {
        query_parts.push(format!("matcher_urisMatcher_uris={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps:fetch
/// Fetch Sitemaps in a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1FetchSitemapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1FetchSitemapsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps:fetch
/// Fetch Sitemaps in a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1FetchSitemapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_builder(client, parent, matcher_urisMatcher_uris)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_sitemaps_fetch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites:batchCreate
/// Creates TargetSite in a batch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchCreateTargetSitesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites:batchCreate
/// Creates TargetSite in a batch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites:batchCreate
/// Creates TargetSite in a batch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchCreateTargetSitesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Creates a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Creates a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Creates a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Deletes a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Deletes a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Deletes a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Gets a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Gets a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1TargetSite>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1TargetSite = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Gets a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1TargetSite>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Gets a list of TargetSites.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Gets a list of TargetSites.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListTargetSitesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListTargetSitesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Gets a list of TargetSites.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListTargetSitesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_builder(client, parent, pageSize, pageToken)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Updates a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Updates a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Updates a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_builder(client, name, body)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/siteSearchEngine/targetSites/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_builder()` + `discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_builder(client, name, filter, pageSize, pageToken, returnPartialSuccess)?;
    discoveryengine_projects_locations_collections_data_stores_site_search_engine_target_sites_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:import
/// Imports all SuggestionDenyListEntry for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportSuggestionDenyListEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/suggestionDenyListEntries:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:import
/// Imports all SuggestionDenyListEntry for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:import
/// Imports all SuggestionDenyListEntry for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_builder()` + `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportSuggestionDenyListEntriesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:purge
/// Permanently deletes all SuggestionDenyListEntry for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeSuggestionDenyListEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/suggestionDenyListEntries:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:purge
/// Permanently deletes all SuggestionDenyListEntry for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:purge
/// Permanently deletes all SuggestionDenyListEntry for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_builder()` + `discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeSuggestionDenyListEntriesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_builder(client, parent, body)?;
    discoveryengine_projects_locations_collections_data_stores_suggestion_deny_list_entries_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_user_events_collect_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_user_events_collect` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_collect_builder(
    client: &SimpleHttpClient,
    parent: &str,
    ets: Option<&str>,
    uri: Option<&str>,
    userEvent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/userEvents:collect",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ets {
        query_parts.push(format!("ets={}", val));
    }
    if let Some(val) = uri {
        query_parts.push(format!("uri={}", val));
    }
    if let Some(val) = userEvent {
        query_parts.push(format!("userEvent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_collect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_user_events_collect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_collect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_collect_builder()` + `discoveryengine_projects_locations_collections_data_stores_user_events_collect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_collect(
    client: &SimpleHttpClient,
    parent: &str,
    ets: Option<&str>,
    uri: Option<&str>,
    userEvent: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_user_events_collect_builder(
            client, parent, ets, uri, userEvent,
        )?;
    discoveryengine_projects_locations_collections_data_stores_user_events_collect_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_user_events_import_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_user_events_import` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportUserEventsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/userEvents:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_user_events_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_import_builder()` + `discoveryengine_projects_locations_collections_data_stores_user_events_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportUserEventsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_user_events_import_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_user_events_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_user_events_purge_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_user_events_purge` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeUserEventsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/userEvents:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_user_events_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_purge_builder()` + `discoveryengine_projects_locations_collections_data_stores_user_events_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeUserEventsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_user_events_purge_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_user_events_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:write
/// Writes a single user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_user_events_write_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_user_events_write` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_write_builder(
    client: &SimpleHttpClient,
    parent: &str,
    writeAsync: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1UserEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/userEvents:write",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = writeAsync {
        query_parts.push(format!("writeAsync={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:write
/// Writes a single user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_write_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_user_events_write_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1UserEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/userEvents:write
/// Writes a single user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_user_events_write_builder()` + `discoveryengine_projects_locations_collections_data_stores_user_events_write_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_user_events_write(
    client: &SimpleHttpClient,
    parent: &str,
    writeAsync: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1UserEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_user_events_write_builder(
            client, parent, writeAsync, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_user_events_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_widget_configs_get_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_widget_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_widget_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    acceptCache: Option<bool>,
    getWidgetConfigRequestOption_turnOffCollectionComponents: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/widgetConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = acceptCache {
        query_parts.push(format!("acceptCache={}", val));
    }
    if let Some(val) = getWidgetConfigRequestOption_turnOffCollectionComponents {
        query_parts.push(format!(
            "getWidgetConfigRequestOption_turnOffCollectionComponents={}",
            val
        ));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_widget_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_widget_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_widget_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1WidgetConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_widget_configs_get_builder()` + `discoveryengine_projects_locations_collections_data_stores_widget_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_widget_configs_get(
    client: &SimpleHttpClient,
    name: &str,
    acceptCache: Option<bool>,
    getWidgetConfigRequestOption_turnOffCollectionComponents: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_widget_configs_get_builder(
            client,
            name,
            acceptCache,
            getWidgetConfigRequestOption_turnOffCollectionComponents,
        )?;
    discoveryengine_projects_locations_collections_data_stores_widget_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_execute()` to send, or `discoveryengine_projects_locations_collections_data_stores_widget_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1WidgetConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/dataStores/{}/widgetConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1WidgetConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_builder()` + `discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_data_stores_widget_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1WidgetConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_data_stores_widget_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines
/// Creates an Engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_create_execute()` to send, or `discoveryengine_projects_locations_collections_engines_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    engineId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Engine,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = engineId {
        query_parts.push(format!("engineId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines
/// Creates an Engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines
/// Creates an Engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_create_builder()` + `discoveryengine_projects_locations_collections_engines_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_create(
    client: &SimpleHttpClient,
    parent: &str,
    engineId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Engine,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_create_builder(
        client, parent, engineId, body,
    )?;
    discoveryengine_projects_locations_collections_engines_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Deletes an Engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_delete_execute()` to send, or `discoveryengine_projects_locations_collections_engines_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Deletes an Engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Deletes an Engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_delete_builder()` + `discoveryengine_projects_locations_collections_engines_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_delete_builder(client, name)?;
    discoveryengine_projects_locations_collections_engines_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Gets an Engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Gets an Engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Engine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Engine = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Gets an Engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_get_builder()` + `discoveryengine_projects_locations_collections_engines_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Engine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_engines_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}:getIamPolicy
/// Gets the IAM access control policy for an Engine. A NOT_FOUND error is returned if the resource does not exist. An empty policy is returned if the resource exists but does not have a policy set on it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_get_iam_policy_execute()` to send, or `discoveryengine_projects_locations_collections_engines_get_iam_policy` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}:getIamPolicy
/// Gets the IAM access control policy for an Engine. A NOT_FOUND error is returned if the resource does not exist. An empty policy is returned if the resource exists but does not have a policy set on it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}:getIamPolicy
/// Gets the IAM access control policy for an Engine. A NOT_FOUND error is returned if the resource does not exist. An empty policy is returned if the resource exists but does not have a policy set on it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_get_iam_policy_builder()` + `discoveryengine_projects_locations_collections_engines_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    discoveryengine_projects_locations_collections_engines_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines
/// Lists all the Engines associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines
/// Lists all the Engines associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListEnginesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines
/// Lists all the Engines associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_list_builder()` + `discoveryengine_projects_locations_collections_engines_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListEnginesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_engines_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Updates an Engine
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_patch_execute()` to send, or `discoveryengine_projects_locations_collections_engines_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Engine,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Updates an Engine
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Engine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Engine = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}
/// Updates an Engine
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_patch_builder()` + `discoveryengine_projects_locations_collections_engines_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Engine,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Engine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_collections_engines_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}:setIamPolicy
/// Sets the IAM access control policy for an Engine. A NOT_FOUND error is returned if the resource does not exist. **Important:** When setting a policy directly on an Engine resource, the only recommended roles in the bindings are: `roles/discoveryengine`.user and `roles/discoveryengine`.`agentspaceUser`. Attempting to grant any other role will result in a warning in logging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_set_iam_policy_execute()` to send, or `discoveryengine_projects_locations_collections_engines_set_iam_policy` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}:setIamPolicy
/// Sets the IAM access control policy for an Engine. A NOT_FOUND error is returned if the resource does not exist. **Important:** When setting a policy directly on an Engine resource, the only recommended roles in the bindings are: `roles/discoveryengine`.user and `roles/discoveryengine`.`agentspaceUser`. Attempting to grant any other role will result in a warning in logging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}:setIamPolicy
/// Sets the IAM access control policy for an Engine. A NOT_FOUND error is returned if the resource does not exist. **Important:** When setting a policy directly on an Engine resource, the only recommended roles in the bindings are: `roles/discoveryengine`.user and `roles/discoveryengine`.`agentspaceUser`. Attempting to grant any other role will result in a warning in logging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_set_iam_policy_builder()` + `discoveryengine_projects_locations_collections_engines_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_set_iam_policy_builder(
        client, resource, body,
    )?;
    discoveryengine_projects_locations_collections_engines_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants
/// Creates an Assistant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_create_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    assistantId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Assistant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assistantId {
        query_parts.push(format!("assistantId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants
/// Creates an Assistant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Assistant>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Assistant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants
/// Creates an Assistant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_create_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_create(
    client: &SimpleHttpClient,
    parent: &str,
    assistantId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Assistant,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Assistant>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_create_builder(
        client,
        parent,
        assistantId,
        body,
    )?;
    discoveryengine_projects_locations_collections_engines_assistants_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Deletes an Assistant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_delete_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Deletes an Assistant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Deletes an Assistant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_delete_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_delete_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_engines_assistants_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Gets an Assistant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Gets an Assistant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Assistant>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Assistant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Gets an Assistant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_get_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Assistant>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_get_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_engines_assistants_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants
/// Lists all Assistants under an Engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants
/// Lists all Assistants under an Engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListAssistantsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListAssistantsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants
/// Lists all Assistants under an Engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_list_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListAssistantsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_engines_assistants_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Updates an Assistant
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_patch_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Assistant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Updates an Assistant
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Assistant>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Assistant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}
/// Updates an Assistant
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_patch_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Assistant,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Assistant>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_collections_engines_assistants_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}:streamAssist
/// Assists the user with a query in a streaming fashion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_stream_assist_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_stream_assist` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_stream_assist_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1StreamAssistRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}:streamAssist",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}:streamAssist
/// Assists the user with a query in a streaming fashion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_stream_assist_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_stream_assist_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_stream_assist_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1StreamAssistResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1StreamAssistResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}:streamAssist
/// Assists the user with a query in a streaming fashion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_stream_assist_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_stream_assist_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_stream_assist(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1StreamAssistRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1StreamAssistResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_assistants_stream_assist_builder(
            client, name, body,
        )?;
    discoveryengine_projects_locations_collections_engines_assistants_stream_assist_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/card
/// GetAgentCard returns the agent card for the agent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_builder(
    client: &SimpleHttpClient,
    tenant: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/card",
        tenant,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/card
/// GetAgentCard returns the agent card for the agent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1AgentCard>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1AgentCard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/card
/// GetAgentCard returns the agent card for the agent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card(
    client: &SimpleHttpClient,
    tenant: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1AgentCard>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_builder(client, tenant)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_get_card_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/message:send
/// Send a message to the agent. This is a blocking call that will return the task once it is completed, or a LRO if requested.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    body: &A2aV1SendMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/message:send",
        tenant,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/message:send
/// Send a message to the agent. This is a blocking call that will return the task once it is completed, or a LRO if requested.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1SendMessageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1SendMessageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/message:send
/// Send a message to the agent. This is a blocking call that will return the task once it is completed, or a LRO if requested.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send(
    client: &SimpleHttpClient,
    tenant: &str,
    body: &A2aV1SendMessageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1SendMessageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_builder(client, tenant, body)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_send_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/message:stream
/// SendStreamingMessage is a streaming call that will return a stream of task update events until the Task is in an interrupted or terminal state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    body: &A2aV1SendMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/message:stream",
        tenant,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/message:stream
/// SendStreamingMessage is a streaming call that will return a stream of task update events until the Task is in an interrupted or terminal state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1StreamResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/message:stream
/// SendStreamingMessage is a streaming call that will return a stream of task update events until the Task is in an interrupted or terminal state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream(
    client: &SimpleHttpClient,
    tenant: &str,
    body: &A2aV1SendMessageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_builder(client, tenant, body)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_message_stream_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:cancel
/// Cancel a task from the agent. If supported one should expect no more task updates for the task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
    body: &A2aV1CancelTaskRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/tasks/{}:cancel",
        tenant,
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:cancel
/// Cancel a task from the agent. If supported one should expect no more task updates for the task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:cancel
/// Cancel a task from the agent. If supported one should expect no more task updates for the task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
    body: &A2aV1CancelTaskRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_builder(client, tenant, name, body)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}
/// Get the current state of a task from the agent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
    historyLength: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/tasks/{}",
        tenant,
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = historyLength {
        query_parts.push(format!("historyLength={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}
/// Get the current state of a task from the agent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1Task = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}
/// Get the current state of a task from the agent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
    historyLength: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1Task>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_builder(client, tenant, name, historyLength)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:subscribe
/// TaskSubscription is a streaming call that will return a stream of task update events. This attaches the stream to an existing in process task. If the task is complete the stream will return the completed task (like GetTask) and close the stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/tasks/{}:subscribe",
        tenant,
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:subscribe
/// TaskSubscription is a streaming call that will return a stream of task update events. This attaches the stream to an existing in process task. If the task is complete the stream will return the completed task (like GetTask) and close the stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1StreamResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:subscribe
/// TaskSubscription is a streaming call that will return a stream of task update events. This attaches the stream to an existing in process task. If the task is complete the stream will return the completed task (like GetTask) and close the stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<A2aV1StreamResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_builder(client, tenant, name)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs
/// Set a push notification config for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    parent: &str,
    configId: Option<&str>,
    body: &A2aV1TaskPushNotificationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/tasks/{}/pushNotificationConfigs",
        tenant,
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = configId {
        query_parts.push(format!("configId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs
/// Set a push notification config for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<A2aV1TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1TaskPushNotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs
/// Set a push notification config for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create(
    client: &SimpleHttpClient,
    tenant: &str,
    parent: &str,
    configId: Option<&str>,
    body: &A2aV1TaskPushNotificationConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<A2aV1TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_builder(client, tenant, parent, configId, body)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs/{pushNotificationConfigsId}
/// Delete a push notification config for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/tasks/{}/pushNotificationConfigs/{}",
        tenant,
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs/{pushNotificationConfigsId}
/// Delete a push notification config for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs/{pushNotificationConfigsId}
/// Delete a push notification config for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_builder(client, tenant, name)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs/{pushNotificationConfigsId}
/// Get a push notification config for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/tasks/{}/pushNotificationConfigs/{}",
        tenant,
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs/{pushNotificationConfigsId}
/// Get a push notification config for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<A2aV1TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1TaskPushNotificationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs/{pushNotificationConfigsId}
/// Get a push notification config for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get(
    client: &SimpleHttpClient,
    tenant: &str,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<A2aV1TaskPushNotificationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_builder(client, tenant, name)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs
/// Get a list of push notifications configured for a task.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/a2a/v1/tasks/{}/pushNotificationConfigs",
        tenant,
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs
/// Get a list of push notifications configured for a task.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<A2aV1ListTaskPushNotificationConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: A2aV1ListTaskPushNotificationConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}/pushNotificationConfigs
/// Get a list of push notifications configured for a task.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list(
    client: &SimpleHttpClient,
    tenant: &str,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<A2aV1ListTaskPushNotificationConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_builder(client, tenant, parent, pageSize, pageToken)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_push_notification_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/assistants/{}/agents/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_builder()` + `discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_engines_assistants_agents_operations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_completion_config_complete_query_execute()` to send, or `discoveryengine_projects_locations_collections_engines_completion_config_complete_query` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_completion_config_complete_query_builder(
    client: &SimpleHttpClient,
    completionConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AdvancedCompleteQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/completionConfig:completeQuery",
        completionConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_completion_config_complete_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_completion_config_complete_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_completion_config_complete_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_completion_config_complete_query_builder()` + `discoveryengine_projects_locations_collections_engines_completion_config_complete_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_completion_config_complete_query(
    client: &SimpleHttpClient,
    completionConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AdvancedCompleteQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_completion_config_complete_query_builder(client, completionConfig, body)?;
    discoveryengine_projects_locations_collections_engines_completion_config_complete_query_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_controls_create_execute()` to send, or `discoveryengine_projects_locations_collections_engines_controls_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_controls_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    controlId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = controlId {
        query_parts.push(format!("controlId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_controls_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_create_builder()` + `discoveryengine_projects_locations_collections_engines_controls_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_create(
    client: &SimpleHttpClient,
    parent: &str,
    controlId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_controls_create_builder(
        client, parent, controlId, body,
    )?;
    discoveryengine_projects_locations_collections_engines_controls_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_controls_delete_execute()` to send, or `discoveryengine_projects_locations_collections_engines_controls_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_controls_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/controls/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_controls_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_delete_builder()` + `discoveryengine_projects_locations_collections_engines_controls_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_controls_delete_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_engines_controls_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Gets a Control.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_controls_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_controls_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_controls_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/controls/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Gets a Control.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_controls_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Gets a Control.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_get_builder()` + `discoveryengine_projects_locations_collections_engines_controls_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_controls_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_engines_controls_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_controls_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_controls_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_controls_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_controls_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListControlsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_list_builder()` + `discoveryengine_projects_locations_collections_engines_controls_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_controls_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_engines_controls_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_controls_patch_execute()` to send, or `discoveryengine_projects_locations_collections_engines_controls_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_controls_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/controls/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_controls_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_controls_patch_builder()` + `discoveryengine_projects_locations_collections_engines_controls_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_controls_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_controls_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_collections_engines_controls_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_conversations_converse_execute()` to send, or `discoveryengine_projects_locations_collections_engines_conversations_converse` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_conversations_converse_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ConverseConversationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/conversations/{}:converse",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_converse_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_conversations_converse_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_converse_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ConverseConversationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ConverseConversationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_converse_builder()` + `discoveryengine_projects_locations_collections_engines_conversations_converse_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_converse(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ConverseConversationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ConverseConversationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_conversations_converse_builder(
            client, name, body,
        )?;
    discoveryengine_projects_locations_collections_engines_conversations_converse_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_conversations_create_execute()` to send, or `discoveryengine_projects_locations_collections_engines_conversations_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_conversations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/conversations",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_conversations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_create_builder()` + `discoveryengine_projects_locations_collections_engines_conversations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_conversations_create_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_collections_engines_conversations_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_conversations_delete_execute()` to send, or `discoveryengine_projects_locations_collections_engines_conversations_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_conversations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/conversations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_conversations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_delete_builder()` + `discoveryengine_projects_locations_collections_engines_conversations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_conversations_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_engines_conversations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_conversations_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_conversations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_conversations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/conversations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_conversations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_get_builder()` + `discoveryengine_projects_locations_collections_engines_conversations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_conversations_get_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_engines_conversations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_conversations_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_conversations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_conversations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/conversations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_conversations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListConversationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_list_builder()` + `discoveryengine_projects_locations_collections_engines_conversations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_conversations_list_builder(
            client, parent, filter, orderBy, pageSize, pageToken,
        )?;
    discoveryengine_projects_locations_collections_engines_conversations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_conversations_patch_execute()` to send, or `discoveryengine_projects_locations_collections_engines_conversations_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_conversations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_conversations_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_conversations_patch_builder()` + `discoveryengine_projects_locations_collections_engines_conversations_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_conversations_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_conversations_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_engines_conversations_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_operations_cancel_execute()` to send, or `discoveryengine_projects_locations_collections_engines_operations_cancel` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_operations_cancel_builder()` + `discoveryengine_projects_locations_collections_engines_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_operations_cancel_builder(
        client, name, body,
    )?;
    discoveryengine_projects_locations_collections_engines_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_operations_get_builder()` + `discoveryengine_projects_locations_collections_engines_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_operations_get_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_engines_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_operations_list_builder()` + `discoveryengine_projects_locations_collections_engines_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    discoveryengine_projects_locations_collections_engines_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_answer_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_answer` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_answer_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}:answer",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_answer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_answer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_answer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AnswerQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_answer_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_answer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_answer(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_answer_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_answer_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_create_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    servingConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = servingConfigId {
        query_parts.push(format!("servingConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_create_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    servingConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_create_builder(
            client,
            parent,
            servingConfigId,
            body,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_delete_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_delete_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_get_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListServingConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListServingConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_list_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListServingConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_patch_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_patch_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_recommend_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_recommend` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_recommend_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RecommendRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}:recommend",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_recommend_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_recommend_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_recommend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RecommendResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1RecommendResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_recommend_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_recommend_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_recommend(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RecommendRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RecommendResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_recommend_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_recommend_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_search_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_search` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_search_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}:search",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_search_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_search(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_search_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_search_lite` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}:searchLite",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_search_lite(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_collections_engines_serving_configs_search_lite_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_execute()` to send, or `discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/servingConfigs/{}:streamAnswer",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AnswerQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_builder()` + `discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_builder(client, servingConfig, body)?;
    discoveryengine_projects_locations_collections_engines_serving_configs_stream_answer_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_sessions_create_execute()` to send, or `discoveryengine_projects_locations_collections_engines_sessions_create` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/sessions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_sessions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_create_builder()` + `discoveryengine_projects_locations_collections_engines_sessions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_sessions_create_builder(
        client, parent, body,
    )?;
    discoveryengine_projects_locations_collections_engines_sessions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_sessions_delete_execute()` to send, or `discoveryengine_projects_locations_collections_engines_sessions_delete` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_sessions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/sessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_sessions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_delete_builder()` + `discoveryengine_projects_locations_collections_engines_sessions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_sessions_delete_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_collections_engines_sessions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_sessions_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_sessions_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    includeAnswerDetails: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeAnswerDetails {
        query_parts.push(format!("includeAnswerDetails={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_sessions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_get_builder()` + `discoveryengine_projects_locations_collections_engines_sessions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_get(
    client: &SimpleHttpClient,
    name: &str,
    includeAnswerDetails: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_sessions_get_builder(
        client,
        name,
        includeAnswerDetails,
    )?;
    discoveryengine_projects_locations_collections_engines_sessions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_sessions_list_execute()` to send, or `discoveryengine_projects_locations_collections_engines_sessions_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/sessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_sessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListSessionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_list_builder()` + `discoveryengine_projects_locations_collections_engines_sessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_sessions_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_collections_engines_sessions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_sessions_patch_execute()` to send, or `discoveryengine_projects_locations_collections_engines_sessions_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_sessions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_sessions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_patch_builder()` + `discoveryengine_projects_locations_collections_engines_sessions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_engines_sessions_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_collections_engines_sessions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_sessions_answers_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_sessions_answers_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_sessions_answers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/sessions/{}/answers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_answers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_sessions_answers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_answers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Answer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Answer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_sessions_answers_get_builder()` + `discoveryengine_projects_locations_collections_engines_sessions_answers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_sessions_answers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Answer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_sessions_answers_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_collections_engines_sessions_answers_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_widget_configs_get_execute()` to send, or `discoveryengine_projects_locations_collections_engines_widget_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_widget_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    acceptCache: Option<bool>,
    getWidgetConfigRequestOption_turnOffCollectionComponents: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/widgetConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = acceptCache {
        query_parts.push(format!("acceptCache={}", val));
    }
    if let Some(val) = getWidgetConfigRequestOption_turnOffCollectionComponents {
        query_parts.push(format!(
            "getWidgetConfigRequestOption_turnOffCollectionComponents={}",
            val
        ));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_widget_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_widget_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_widget_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1WidgetConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_widget_configs_get_builder()` + `discoveryengine_projects_locations_collections_engines_widget_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_widget_configs_get(
    client: &SimpleHttpClient,
    name: &str,
    acceptCache: Option<bool>,
    getWidgetConfigRequestOption_turnOffCollectionComponents: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_widget_configs_get_builder(
            client,
            name,
            acceptCache,
            getWidgetConfigRequestOption_turnOffCollectionComponents,
        )?;
    discoveryengine_projects_locations_collections_engines_widget_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_engines_widget_configs_patch_execute()` to send, or `discoveryengine_projects_locations_collections_engines_widget_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_collections_engines_widget_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1WidgetConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/engines/{}/widgetConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_engines_widget_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_engines_widget_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_widget_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1WidgetConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_engines_widget_configs_patch_builder()` + `discoveryengine_projects_locations_collections_engines_widget_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_engines_widget_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1WidgetConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_engines_widget_configs_patch_builder(
            client, name, updateMask, body,
        )?;
    discoveryengine_projects_locations_collections_engines_widget_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_operations_get_execute()` to send, or `discoveryengine_projects_locations_collections_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_collections_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_operations_get_builder()` + `discoveryengine_projects_locations_collections_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_collections_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_collections_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_collections_operations_list_execute()` to send, or `discoveryengine_projects_locations_collections_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_collections_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/collections/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_collections_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_collections_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_collections_operations_list_builder()` + `discoveryengine_projects_locations_collections_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_collections_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_collections_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    discoveryengine_projects_locations_collections_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}:completeQuery
/// Completes the specified user input with keyword suggestions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_complete_query_execute()` to send, or `discoveryengine_projects_locations_data_stores_complete_query` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_complete_query_builder(
    client: &SimpleHttpClient,
    dataStore: &str,
    includeTailSuggestions: Option<bool>,
    query: Option<&str>,
    queryModel: Option<&str>,
    userPseudoId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}:completeQuery",
        dataStore,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeTailSuggestions {
        query_parts.push(format!("includeTailSuggestions={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = queryModel {
        query_parts.push(format!("queryModel={}", val));
    }
    if let Some(val) = userPseudoId {
        query_parts.push(format!("userPseudoId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}:completeQuery
/// Completes the specified user input with keyword suggestions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_complete_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_complete_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_complete_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CompleteQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1CompleteQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}:completeQuery
/// Completes the specified user input with keyword suggestions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_complete_query_builder()` + `discoveryengine_projects_locations_data_stores_complete_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_complete_query(
    client: &SimpleHttpClient,
    dataStore: &str,
    includeTailSuggestions: Option<bool>,
    query: Option<&str>,
    queryModel: Option<&str>,
    userPseudoId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CompleteQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_complete_query_builder(
        client,
        dataStore,
        includeTailSuggestions,
        query,
        queryModel,
        userPseudoId,
    )?;
    discoveryengine_projects_locations_data_stores_complete_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores
/// Creates a DataStore. DataStore is for storing Documents. To serve these documents for Search, or Recommendation use case, an Engine needs to be created separately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    cmekConfigName: Option<&str>,
    createAdvancedSiteSearch: Option<bool>,
    dataStoreId: Option<&str>,
    disableCmek: Option<bool>,
    skipDefaultSchemaCreation: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cmekConfigName {
        query_parts.push(format!("cmekConfigName={}", val));
    }
    if let Some(val) = createAdvancedSiteSearch {
        query_parts.push(format!("createAdvancedSiteSearch={}", val));
    }
    if let Some(val) = dataStoreId {
        query_parts.push(format!("dataStoreId={}", val));
    }
    if let Some(val) = disableCmek {
        query_parts.push(format!("disableCmek={}", val));
    }
    if let Some(val) = skipDefaultSchemaCreation {
        query_parts.push(format!("skipDefaultSchemaCreation={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores
/// Creates a DataStore. DataStore is for storing Documents. To serve these documents for Search, or Recommendation use case, an Engine needs to be created separately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores
/// Creates a DataStore. DataStore is for storing Documents. To serve these documents for Search, or Recommendation use case, an Engine needs to be created separately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_create_builder()` + `discoveryengine_projects_locations_data_stores_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_create(
    client: &SimpleHttpClient,
    parent: &str,
    cmekConfigName: Option<&str>,
    createAdvancedSiteSearch: Option<bool>,
    dataStoreId: Option<&str>,
    disableCmek: Option<bool>,
    skipDefaultSchemaCreation: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_create_builder(
        client,
        parent,
        cmekConfigName,
        createAdvancedSiteSearch,
        dataStoreId,
        disableCmek,
        skipDefaultSchemaCreation,
        body,
    )?;
    discoveryengine_projects_locations_data_stores_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Deletes a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Deletes a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Deletes a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_delete_builder()` + `discoveryengine_projects_locations_data_stores_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_delete_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Gets a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Gets a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1DataStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Gets a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_get_builder()` + `discoveryengine_projects_locations_data_stores_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine
/// Gets the SiteSearchEngine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_get_site_search_engine_execute()` to send, or `discoveryengine_projects_locations_data_stores_get_site_search_engine` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_get_site_search_engine_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine
/// Gets the SiteSearchEngine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_get_site_search_engine_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_get_site_search_engine_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_get_site_search_engine_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SiteSearchEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SiteSearchEngine =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine
/// Gets the SiteSearchEngine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_get_site_search_engine_builder()` + `discoveryengine_projects_locations_data_stores_get_site_search_engine_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_get_site_search_engine(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SiteSearchEngine>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_get_site_search_engine_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_data_stores_get_site_search_engine_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores
/// Lists all the DataStores associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores
/// Lists all the DataStores associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDataStoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListDataStoresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores
/// Lists all the DataStores associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_list_builder()` + `discoveryengine_projects_locations_data_stores_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDataStoresResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_data_stores_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Updates a DataStore
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Updates a DataStore
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1DataStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}
/// Updates a DataStore
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_patch_builder()` + `discoveryengine_projects_locations_data_stores_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1DataStore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1DataStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_data_stores_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/batchGetDocumentsMetadata
/// Gets index freshness metadata for Documents. Supported for website search only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_builder(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_fhirMatcher_fhirResources: Option<&str>,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/batchGetDocumentsMetadata",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = matcher_fhirMatcher_fhirResources {
        query_parts.push(format!("matcher_fhirMatcher_fhirResources={}", val));
    }
    if let Some(val) = matcher_urisMatcher_uris {
        query_parts.push(format!("matcher_urisMatcher_uris={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/batchGetDocumentsMetadata
/// Gets index freshness metadata for Documents. Supported for website search only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1BatchGetDocumentsMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1BatchGetDocumentsMetadataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/batchGetDocumentsMetadata
/// Gets index freshness metadata for Documents. Supported for website search only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_builder()` + `discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_fhirMatcher_fhirResources: Option<&str>,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1BatchGetDocumentsMetadataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_builder(client, parent, matcher_fhirMatcher_fhirResources, matcher_urisMatcher_uris)?;
    discoveryengine_projects_locations_data_stores_branches_batch_get_documents_metadata_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Creates a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_documents_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_documents_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    documentId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/documents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = documentId {
        query_parts.push(format!("documentId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Creates a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_documents_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Creates a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_create_builder()` + `discoveryengine_projects_locations_data_stores_branches_documents_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_create(
    client: &SimpleHttpClient,
    parent: &str,
    documentId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_documents_create_builder(
        client, parent, documentId, body,
    )?;
    discoveryengine_projects_locations_data_stores_branches_documents_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Deletes a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_documents_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_documents_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/documents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Deletes a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_documents_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Deletes a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_delete_builder()` + `discoveryengine_projects_locations_data_stores_branches_documents_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_documents_delete_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_data_stores_branches_documents_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Gets a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_documents_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_documents_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/documents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Gets a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_documents_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Gets a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_get_builder()` + `discoveryengine_projects_locations_data_stores_branches_documents_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_documents_get_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_data_stores_branches_documents_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:import
/// Bulk import of multiple Documents. Request processing may be synchronous. Non-existing items are created. Note: It is possible for a subset of the Documents to be successfully updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_documents_import_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_documents_import` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportDocumentsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/documents:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:import
/// Bulk import of multiple Documents. Request processing may be synchronous. Non-existing items are created. Note: It is possible for a subset of the Documents to be successfully updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_documents_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:import
/// Bulk import of multiple Documents. Request processing may be synchronous. Non-existing items are created. Note: It is possible for a subset of the Documents to be successfully updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_import_builder()` + `discoveryengine_projects_locations_data_stores_branches_documents_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportDocumentsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_documents_import_builder(
        client, parent, body,
    )?;
    discoveryengine_projects_locations_data_stores_branches_documents_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Gets a list of Documents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_documents_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_documents_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/documents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Gets a list of Documents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_documents_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDocumentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListDocumentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents
/// Gets a list of Documents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_list_builder()` + `discoveryengine_projects_locations_data_stores_branches_documents_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListDocumentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_documents_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_data_stores_branches_documents_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Updates a Document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_documents_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_documents_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/documents/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Updates a Document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_documents_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents/{documentsId}
/// Updates a Document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_patch_builder()` + `discoveryengine_projects_locations_data_stores_branches_documents_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Document,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Document>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_documents_patch_builder(
        client,
        name,
        allowMissing,
        updateMask,
        body,
    )?;
    discoveryengine_projects_locations_data_stores_branches_documents_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:purge
/// Permanently deletes all selected Documents in a branch. This process is asynchronous. Depending on the number of Documents to be deleted, this operation can take hours to complete. Before the delete operation completes, some Documents might still be returned by DocumentService.GetDocument or DocumentService.ListDocuments. To get a list of the Documents to be deleted, set PurgeDocumentsRequest.force to `false`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_documents_purge_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_documents_purge` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeDocumentsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/documents:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:purge
/// Permanently deletes all selected Documents in a branch. This process is asynchronous. Depending on the number of Documents to be deleted, this operation can take hours to complete. Before the delete operation completes, some Documents might still be returned by DocumentService.GetDocument or DocumentService.ListDocuments. To get a list of the Documents to be deleted, set PurgeDocumentsRequest.force to `false`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_documents_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/documents:purge
/// Permanently deletes all selected Documents in a branch. This process is asynchronous. Depending on the number of Documents to be deleted, this operation can take hours to complete. Before the delete operation completes, some Documents might still be returned by DocumentService.GetDocument or DocumentService.ListDocuments. To get a list of the Documents to be deleted, set PurgeDocumentsRequest.force to `false`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_documents_purge_builder()` + `discoveryengine_projects_locations_data_stores_branches_documents_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_documents_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeDocumentsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_documents_purge_builder(
        client, parent, body,
    )?;
    discoveryengine_projects_locations_data_stores_branches_documents_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_operations_cancel_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_operations_cancel` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_operations_cancel_builder()` + `discoveryengine_projects_locations_data_stores_branches_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_branches_operations_cancel_builder(
            client, name, body,
        )?;
    discoveryengine_projects_locations_data_stores_branches_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_operations_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_operations_get_builder()` + `discoveryengine_projects_locations_data_stores_branches_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_operations_get_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_data_stores_branches_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_branches_operations_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_branches_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/branches/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_branches_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/branches/{branchesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_branches_operations_list_builder()` + `discoveryengine_projects_locations_data_stores_branches_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_branches_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_branches_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    discoveryengine_projects_locations_data_stores_branches_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_completion_config_complete_query_execute()` to send, or `discoveryengine_projects_locations_data_stores_completion_config_complete_query` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_completion_config_complete_query_builder(
    client: &SimpleHttpClient,
    completionConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AdvancedCompleteQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/completionConfig:completeQuery",
        completionConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_completion_config_complete_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_completion_config_complete_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_completion_config_complete_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionConfig:completeQuery
/// Completes the user input with advanced keyword suggestions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_completion_config_complete_query_builder()` + `discoveryengine_projects_locations_data_stores_completion_config_complete_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_completion_config_complete_query(
    client: &SimpleHttpClient,
    completionConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AdvancedCompleteQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1AdvancedCompleteQueryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_completion_config_complete_query_builder(
            client,
            completionConfig,
            body,
        )?;
    discoveryengine_projects_locations_data_stores_completion_config_complete_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionSuggestions:import
/// Imports CompletionSuggestions for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_completion_suggestions_import_execute()` to send, or `discoveryengine_projects_locations_data_stores_completion_suggestions_import` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_completion_suggestions_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportCompletionSuggestionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/completionSuggestions:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionSuggestions:import
/// Imports CompletionSuggestions for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_completion_suggestions_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_completion_suggestions_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_completion_suggestions_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionSuggestions:import
/// Imports CompletionSuggestions for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_completion_suggestions_import_builder()` + `discoveryengine_projects_locations_data_stores_completion_suggestions_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_completion_suggestions_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportCompletionSuggestionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_completion_suggestions_import_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_data_stores_completion_suggestions_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionSuggestions:purge
/// Permanently deletes all CompletionSuggestions for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_completion_suggestions_purge_execute()` to send, or `discoveryengine_projects_locations_data_stores_completion_suggestions_purge` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_completion_suggestions_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeCompletionSuggestionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/completionSuggestions:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionSuggestions:purge
/// Permanently deletes all CompletionSuggestions for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_completion_suggestions_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_completion_suggestions_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_completion_suggestions_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/completionSuggestions:purge
/// Permanently deletes all CompletionSuggestions for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_completion_suggestions_purge_builder()` + `discoveryengine_projects_locations_data_stores_completion_suggestions_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_completion_suggestions_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeCompletionSuggestionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_completion_suggestions_purge_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_data_stores_completion_suggestions_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_controls_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_controls_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_controls_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    controlId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = controlId {
        query_parts.push(format!("controlId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_controls_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls
/// Creates a Control. By default 1000 controls are allowed for a data store. A request can be submitted to adjust this limit. If the Control to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_create_builder()` + `discoveryengine_projects_locations_data_stores_controls_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_create(
    client: &SimpleHttpClient,
    parent: &str,
    controlId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_controls_create_builder(
        client, parent, controlId, body,
    )?;
    discoveryengine_projects_locations_data_stores_controls_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_controls_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_controls_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_controls_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/controls/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_controls_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Deletes a Control. If the Control to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_delete_builder()` + `discoveryengine_projects_locations_data_stores_controls_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_controls_delete_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_controls_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Gets a Control.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_controls_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_controls_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_controls_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/controls/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Gets a Control.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_controls_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Gets a Control.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_get_builder()` + `discoveryengine_projects_locations_data_stores_controls_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_controls_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_controls_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_controls_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_controls_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_controls_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/controls",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_controls_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListControlsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls
/// Lists all Controls by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_list_builder()` + `discoveryengine_projects_locations_data_stores_controls_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListControlsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_controls_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_data_stores_controls_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_controls_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_controls_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_controls_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/controls/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_controls_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Control = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/controls/{controlsId}
/// Updates a Control. Control action type cannot be changed. If the Control to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_controls_patch_builder()` + `discoveryengine_projects_locations_data_stores_controls_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_controls_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Control,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Control>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_controls_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_data_stores_controls_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_conversations_converse_execute()` to send, or `discoveryengine_projects_locations_data_stores_conversations_converse` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_conversations_converse_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ConverseConversationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/conversations/{}:converse",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_converse_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_conversations_converse_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_converse_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ConverseConversationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ConverseConversationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}:converse
/// Converses a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_converse_builder()` + `discoveryengine_projects_locations_data_stores_conversations_converse_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_converse(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1ConverseConversationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ConverseConversationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_conversations_converse_builder(
        client, name, body,
    )?;
    discoveryengine_projects_locations_data_stores_conversations_converse_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_conversations_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_conversations_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_conversations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/conversations",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_conversations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations
/// Creates a Conversation. If the Conversation to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_create_builder()` + `discoveryengine_projects_locations_data_stores_conversations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_conversations_create_builder(
        client, parent, body,
    )?;
    discoveryengine_projects_locations_data_stores_conversations_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_conversations_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_conversations_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_conversations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/conversations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_conversations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Deletes a Conversation. If the Conversation to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_delete_builder()` + `discoveryengine_projects_locations_data_stores_conversations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_conversations_delete_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_conversations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_conversations_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_conversations_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_conversations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/conversations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_conversations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Gets a Conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_get_builder()` + `discoveryengine_projects_locations_data_stores_conversations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_conversations_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_conversations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_conversations_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_conversations_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_conversations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/conversations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_conversations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListConversationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations
/// Lists all Conversations by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_list_builder()` + `discoveryengine_projects_locations_data_stores_conversations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_conversations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_data_stores_conversations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_conversations_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_conversations_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_conversations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_conversations_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Conversation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/conversations/{conversationsId}
/// Updates a Conversation. Conversation action type cannot be changed. If the Conversation to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_conversations_patch_builder()` + `discoveryengine_projects_locations_data_stores_conversations_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_conversations_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Conversation,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_conversations_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_data_stores_conversations_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_models_operations_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_models_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_models_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/models/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_models_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_models_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_models_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/models/{modelsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_models_operations_get_builder()` + `discoveryengine_projects_locations_data_stores_models_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_models_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_models_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_models_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_models_operations_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_models_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_models_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/models/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_models_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_models_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_models_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/models/{modelsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_models_operations_list_builder()` + `discoveryengine_projects_locations_data_stores_models_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_models_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_models_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    discoveryengine_projects_locations_data_stores_models_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_operations_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_operations_get_builder()` + `discoveryengine_projects_locations_data_stores_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_operations_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_operations_list_builder()` + `discoveryengine_projects_locations_data_stores_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    discoveryengine_projects_locations_data_stores_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas
/// Creates a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_schemas_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_schemas_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_schemas_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    schemaId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = schemaId {
        query_parts.push(format!("schemaId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas
/// Creates a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_schemas_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas
/// Creates a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_create_builder()` + `discoveryengine_projects_locations_data_stores_schemas_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_create(
    client: &SimpleHttpClient,
    parent: &str,
    schemaId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_schemas_create_builder(
        client, parent, schemaId, body,
    )?;
    discoveryengine_projects_locations_data_stores_schemas_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Deletes a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_schemas_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_schemas_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_schemas_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/schemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Deletes a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_schemas_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Deletes a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_delete_builder()` + `discoveryengine_projects_locations_data_stores_schemas_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_schemas_delete_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_schemas_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Gets a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_schemas_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_schemas_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_schemas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/schemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Gets a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_schemas_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Schema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Schema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Gets a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_get_builder()` + `discoveryengine_projects_locations_data_stores_schemas_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Schema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_schemas_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_schemas_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas
/// Gets a list of Schemas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_schemas_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_schemas_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas
/// Gets a list of Schemas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_schemas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas
/// Gets a list of Schemas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_list_builder()` + `discoveryengine_projects_locations_data_stores_schemas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_schemas_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_data_stores_schemas_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Updates a Schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_schemas_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_schemas_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_schemas_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/schemas/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Updates a Schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_schemas_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/schemas/{schemasId}
/// Updates a Schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_schemas_patch_builder()` + `discoveryengine_projects_locations_data_stores_schemas_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_schemas_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1Schema,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_schemas_patch_builder(
        client,
        name,
        allowMissing,
        body,
    )?;
    discoveryengine_projects_locations_data_stores_schemas_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_answer_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_answer` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_answer_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}:answer",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_answer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_answer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_answer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AnswerQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:answer
/// Answer query method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_answer_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_answer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_answer(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_serving_configs_answer_builder(
        client,
        servingConfig,
        body,
    )?;
    discoveryengine_projects_locations_data_stores_serving_configs_answer_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    servingConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = servingConfigId {
        query_parts.push(format!("servingConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs
/// Creates a ServingConfig. Note: The Google Cloud console works only with the default serving config. Additional ServingConfigs can be created and managed only via the API. A maximum of 100 ServingConfigs are allowed in an Engine, otherwise a RESOURCE_EXHAUSTED error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_create_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    servingConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_serving_configs_create_builder(
        client,
        parent,
        servingConfigId,
        body,
    )?;
    discoveryengine_projects_locations_data_stores_serving_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Deletes a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_delete_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_serving_configs_delete_builder(
        client, name,
    )?;
    discoveryengine_projects_locations_data_stores_serving_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Gets a ServingConfig. Returns a NotFound error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_get_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_serving_configs_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_serving_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListServingConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListServingConfigsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs
/// Lists all ServingConfigs linked to this `dataStore`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_list_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListServingConfigsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_serving_configs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_data_stores_serving_configs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ServingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}
/// Updates a ServingConfig. Returns a NOT_FOUND error if the ServingConfig does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_patch_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1ServingConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ServingConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_serving_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_data_stores_serving_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_recommend_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_recommend` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_recommend_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RecommendRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}:recommend",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_recommend_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_recommend_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_recommend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RecommendResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1RecommendResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:recommend
/// Makes a recommendation, which requires a contextual user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_recommend_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_recommend_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_recommend(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RecommendRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RecommendResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_serving_configs_recommend_builder(
        client,
        servingConfig,
        body,
    )?;
    discoveryengine_projects_locations_data_stores_serving_configs_recommend_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_search_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_search` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_search_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}:search",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:search
/// Performs a search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_search_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_search(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_serving_configs_search_builder(
        client,
        servingConfig,
        body,
    )?;
    discoveryengine_projects_locations_data_stores_serving_configs_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_search_lite_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_search_lite` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_search_lite_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}:searchLite",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_search_lite_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_search_lite_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_search_lite_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1SearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:searchLite
/// Performs a search. Similar to the SearchService.Search method, but a lite version that allows API key for authentication, where OAuth and IAM checks are not required. Only public website search is supported by this method. If data stores and engines not associated with public website search are specified, a FAILED_PRECONDITION error is returned. This method can be used for easy onboarding without having to implement an authentication backend. However, it is strongly recommended to use SearchService.Search instead with required OAuth and IAM checks to provide better data security.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_search_lite_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_search_lite_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_search_lite(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1SearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1SearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_serving_configs_search_lite_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_data_stores_serving_configs_search_lite_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_execute()` to send, or `discoveryengine_projects_locations_data_stores_serving_configs_stream_answer` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_builder(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/servingConfigs/{}:streamAnswer",
        servingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1AnswerQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/servingConfigs/{servingConfigsId}:streamAnswer
/// Answer query method (streaming). It takes one AnswerQueryRequest and returns multiple AnswerQueryResponse messages in a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_builder()` + `discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_serving_configs_stream_answer(
    client: &SimpleHttpClient,
    servingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1AnswerQueryRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1AnswerQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_builder(
            client,
            servingConfig,
            body,
        )?;
    discoveryengine_projects_locations_data_stores_serving_configs_stream_answer_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_sessions_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_sessions_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/sessions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_sessions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions
/// Creates a Session. If the Session to create already exists, an ALREADY_EXISTS error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_create_builder()` + `discoveryengine_projects_locations_data_stores_sessions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_sessions_create_builder(
        client, parent, body,
    )?;
    discoveryengine_projects_locations_data_stores_sessions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_sessions_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_sessions_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_sessions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/sessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_sessions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Deletes a Session. If the Session to delete does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_delete_builder()` + `discoveryengine_projects_locations_data_stores_sessions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_sessions_delete_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_sessions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_sessions_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_sessions_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    includeAnswerDetails: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeAnswerDetails {
        query_parts.push(format!("includeAnswerDetails={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_sessions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Gets a Session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_get_builder()` + `discoveryengine_projects_locations_data_stores_sessions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_get(
    client: &SimpleHttpClient,
    name: &str,
    includeAnswerDetails: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_sessions_get_builder(
        client,
        name,
        includeAnswerDetails,
    )?;
    discoveryengine_projects_locations_data_stores_sessions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_sessions_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_sessions_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/sessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_sessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListSessionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions
/// Lists all Sessions by their parent DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_list_builder()` + `discoveryengine_projects_locations_data_stores_sessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListSessionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_sessions_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_data_stores_sessions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_sessions_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_sessions_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_sessions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_sessions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}
/// Updates a Session. Session action type cannot be changed. If the Session to update does not exist, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_patch_builder()` + `discoveryengine_projects_locations_data_stores_sessions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1Session,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Session>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_sessions_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_data_stores_sessions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_sessions_answers_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_sessions_answers_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_sessions_answers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/sessions/{}/answers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_answers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_sessions_answers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_answers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Answer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1Answer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/sessions/{sessionsId}/answers/{answersId}
/// Gets a Answer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_sessions_answers_get_builder()` + `discoveryengine_projects_locations_data_stores_sessions_answers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_sessions_answers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1Answer>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_sessions_answers_get_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_sessions_answers_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:disableAdvancedSiteSearch
/// Downgrade from advanced site search to basic site search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_builder(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1DisableAdvancedSiteSearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine:disableAdvancedSiteSearch",
        siteSearchEngine,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:disableAdvancedSiteSearch
/// Downgrade from advanced site search to basic site search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:disableAdvancedSiteSearch
/// Downgrade from advanced site search to basic site search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1DisableAdvancedSiteSearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_builder(client, siteSearchEngine, body)?;
    discoveryengine_projects_locations_data_stores_site_search_engine_disable_advanced_site_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:enableAdvancedSiteSearch
/// Upgrade from basic site search to advanced site search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_builder(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1EnableAdvancedSiteSearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine:enableAdvancedSiteSearch",
        siteSearchEngine,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:enableAdvancedSiteSearch
/// Upgrade from basic site search to advanced site search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:enableAdvancedSiteSearch
/// Upgrade from basic site search to advanced site search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1EnableAdvancedSiteSearchRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_builder(client, siteSearchEngine, body)?;
    discoveryengine_projects_locations_data_stores_site_search_engine_enable_advanced_site_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:recrawlUris
/// Request on-demand recrawl for a list of URIs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_builder(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1RecrawlUrisRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine:recrawlUris",
        siteSearchEngine,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:recrawlUris
/// Request on-demand recrawl for a list of URIs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine:recrawlUris
/// Request on-demand recrawl for a list of URIs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris(
    client: &SimpleHttpClient,
    siteSearchEngine: &str,
    body: &GoogleCloudDiscoveryengineV1RecrawlUrisRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_builder(
            client,
            siteSearchEngine,
            body,
        )?;
    discoveryengine_projects_locations_data_stores_site_search_engine_recrawl_uris_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps
/// Creates a Sitemap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Sitemap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/sitemaps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps
/// Creates a Sitemap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps
/// Creates a Sitemap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1Sitemap,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps/{sitemapsId}
/// Deletes a Sitemap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/sitemaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps/{sitemapsId}
/// Deletes a Sitemap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps/{sitemapsId}
/// Deletes a Sitemap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps:fetch
/// Fetch Sitemaps in a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_builder(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/sitemaps:fetch",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = matcher_urisMatcher_uris {
        query_parts.push(format!("matcher_urisMatcher_uris={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps:fetch
/// Fetch Sitemaps in a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1FetchSitemapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1FetchSitemapsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/sitemaps:fetch
/// Fetch Sitemaps in a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch(
    client: &SimpleHttpClient,
    parent: &str,
    matcher_urisMatcher_uris: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1FetchSitemapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_builder(
            client,
            parent,
            matcher_urisMatcher_uris,
        )?;
    discoveryengine_projects_locations_data_stores_site_search_engine_sitemaps_fetch_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites:batchCreate
/// Creates TargetSite in a batch.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchCreateTargetSitesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/targetSites:batchCreate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites:batchCreate
/// Creates TargetSite in a batch.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites:batchCreate
/// Creates TargetSite in a batch.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchCreateTargetSitesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_builder(client, parent, body)?;
    discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_batch_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Creates a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/targetSites",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Creates a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Creates a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_builder(client, parent, body)?;
    discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Deletes a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/targetSites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Deletes a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Deletes a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_builder(client, name)?;
    discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Gets a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/targetSites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Gets a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1TargetSite>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1TargetSite = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Gets a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1TargetSite>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Gets a list of TargetSites.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/targetSites",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Gets a list of TargetSites.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListTargetSitesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListTargetSitesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites
/// Gets a list of TargetSites.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListTargetSitesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_builder(client, parent, pageSize, pageToken)?;
    discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Updates a TargetSite.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/siteSearchEngine/targetSites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Updates a TargetSite.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/siteSearchEngine/targetSites/{targetSitesId}
/// Updates a TargetSite.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_builder()` + `discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDiscoveryengineV1TargetSite,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_builder(client, name, body)?;
    discoveryengine_projects_locations_data_stores_site_search_engine_target_sites_patch_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:import
/// Imports all SuggestionDenyListEntry for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_execute()` to send, or `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportSuggestionDenyListEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/suggestionDenyListEntries:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:import
/// Imports all SuggestionDenyListEntry for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:import
/// Imports all SuggestionDenyListEntry for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_builder()` + `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportSuggestionDenyListEntriesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_import_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:purge
/// Permanently deletes all SuggestionDenyListEntry for a DataStore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_execute()` to send, or `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeSuggestionDenyListEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/suggestionDenyListEntries:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:purge
/// Permanently deletes all SuggestionDenyListEntry for a DataStore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/suggestionDenyListEntries:purge
/// Permanently deletes all SuggestionDenyListEntry for a DataStore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_builder()` + `discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeSuggestionDenyListEntriesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_data_stores_suggestion_deny_list_entries_purge_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_user_events_collect_execute()` to send, or `discoveryengine_projects_locations_data_stores_user_events_collect` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_user_events_collect_builder(
    client: &SimpleHttpClient,
    parent: &str,
    ets: Option<&str>,
    uri: Option<&str>,
    userEvent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/userEvents:collect",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ets {
        query_parts.push(format!("ets={}", val));
    }
    if let Some(val) = uri {
        query_parts.push(format!("uri={}", val));
    }
    if let Some(val) = userEvent {
        query_parts.push(format!("userEvent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_collect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_user_events_collect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_collect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_collect_builder()` + `discoveryengine_projects_locations_data_stores_user_events_collect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_collect(
    client: &SimpleHttpClient,
    parent: &str,
    ets: Option<&str>,
    uri: Option<&str>,
    userEvent: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_user_events_collect_builder(
        client, parent, ets, uri, userEvent,
    )?;
    discoveryengine_projects_locations_data_stores_user_events_collect_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_user_events_import_execute()` to send, or `discoveryengine_projects_locations_data_stores_user_events_import` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_user_events_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportUserEventsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/userEvents:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_user_events_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_import_builder()` + `discoveryengine_projects_locations_data_stores_user_events_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportUserEventsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_user_events_import_builder(
        client, parent, body,
    )?;
    discoveryengine_projects_locations_data_stores_user_events_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_user_events_purge_execute()` to send, or `discoveryengine_projects_locations_data_stores_user_events_purge` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_user_events_purge_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeUserEventsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/userEvents:purge",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_purge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_user_events_purge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_purge_builder()` + `discoveryengine_projects_locations_data_stores_user_events_purge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_purge(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeUserEventsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_user_events_purge_builder(
        client, parent, body,
    )?;
    discoveryengine_projects_locations_data_stores_user_events_purge_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:write
/// Writes a single user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_user_events_write_execute()` to send, or `discoveryengine_projects_locations_data_stores_user_events_write` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_user_events_write_builder(
    client: &SimpleHttpClient,
    parent: &str,
    writeAsync: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1UserEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/userEvents:write",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = writeAsync {
        query_parts.push(format!("writeAsync={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:write
/// Writes a single user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_write_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_user_events_write_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1UserEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/userEvents:write
/// Writes a single user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_user_events_write_builder()` + `discoveryengine_projects_locations_data_stores_user_events_write_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_user_events_write(
    client: &SimpleHttpClient,
    parent: &str,
    writeAsync: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1UserEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_user_events_write_builder(
        client, parent, writeAsync, body,
    )?;
    discoveryengine_projects_locations_data_stores_user_events_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_widget_configs_get_execute()` to send, or `discoveryengine_projects_locations_data_stores_widget_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_widget_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    acceptCache: Option<bool>,
    getWidgetConfigRequestOption_turnOffCollectionComponents: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/widgetConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = acceptCache {
        query_parts.push(format!("acceptCache={}", val));
    }
    if let Some(val) = getWidgetConfigRequestOption_turnOffCollectionComponents {
        query_parts.push(format!(
            "getWidgetConfigRequestOption_turnOffCollectionComponents={}",
            val
        ));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_widget_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_widget_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_widget_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1WidgetConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Gets a WidgetConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_widget_configs_get_builder()` + `discoveryengine_projects_locations_data_stores_widget_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_widget_configs_get(
    client: &SimpleHttpClient,
    name: &str,
    acceptCache: Option<bool>,
    getWidgetConfigRequestOption_turnOffCollectionComponents: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_widget_configs_get_builder(
        client,
        name,
        acceptCache,
        getWidgetConfigRequestOption_turnOffCollectionComponents,
    )?;
    discoveryengine_projects_locations_data_stores_widget_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_data_stores_widget_configs_patch_execute()` to send, or `discoveryengine_projects_locations_data_stores_widget_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_data_stores_widget_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1WidgetConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/dataStores/{}/widgetConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_data_stores_widget_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_data_stores_widget_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_widget_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1WidgetConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataStores/{dataStoresId}/widgetConfigs/{widgetConfigsId}
/// Update a WidgetConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_data_stores_widget_configs_patch_builder()` + `discoveryengine_projects_locations_data_stores_widget_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_data_stores_widget_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1WidgetConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1WidgetConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_data_stores_widget_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_data_stores_widget_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groundingConfigs/{groundingConfigsId}:check
/// Performs a grounding check.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_grounding_configs_check_execute()` to send, or `discoveryengine_projects_locations_grounding_configs_check` for simplest API.

pub fn discoveryengine_projects_locations_grounding_configs_check_builder(
    client: &SimpleHttpClient,
    groundingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1CheckGroundingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/groundingConfigs/{}:check",
        groundingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groundingConfigs/{groundingConfigsId}:check
/// Performs a grounding check.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_grounding_configs_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_grounding_configs_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_grounding_configs_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CheckGroundingResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1CheckGroundingResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/groundingConfigs/{groundingConfigsId}:check
/// Performs a grounding check.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_grounding_configs_check_builder()` + `discoveryengine_projects_locations_grounding_configs_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_grounding_configs_check(
    client: &SimpleHttpClient,
    groundingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1CheckGroundingRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1CheckGroundingResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_grounding_configs_check_builder(
        client,
        groundingConfig,
        body,
    )?;
    discoveryengine_projects_locations_grounding_configs_check_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores
/// Creates a new Identity Mapping Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_create_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_create` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    cmekConfigName: Option<&str>,
    disableCmek: Option<bool>,
    identityMappingStoreId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1IdentityMappingStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cmekConfigName {
        query_parts.push(format!("cmekConfigName={}", val));
    }
    if let Some(val) = disableCmek {
        query_parts.push(format!("disableCmek={}", val));
    }
    if let Some(val) = identityMappingStoreId {
        query_parts.push(format!("identityMappingStoreId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores
/// Creates a new Identity Mapping Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1IdentityMappingStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1IdentityMappingStore =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores
/// Creates a new Identity Mapping Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_create_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_create(
    client: &SimpleHttpClient,
    parent: &str,
    cmekConfigName: Option<&str>,
    disableCmek: Option<bool>,
    identityMappingStoreId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1IdentityMappingStore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1IdentityMappingStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_identity_mapping_stores_create_builder(
        client,
        parent,
        cmekConfigName,
        disableCmek,
        identityMappingStoreId,
        body,
    )?;
    discoveryengine_projects_locations_identity_mapping_stores_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}
/// Deletes the Identity Mapping Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_delete_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_delete` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}
/// Deletes the Identity Mapping Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}
/// Deletes the Identity Mapping Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_delete_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_identity_mapping_stores_delete_builder(client, name)?;
    discoveryengine_projects_locations_identity_mapping_stores_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}
/// Gets the Identity Mapping Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_get_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_get` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}
/// Gets the Identity Mapping Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1IdentityMappingStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1IdentityMappingStore =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}
/// Gets the Identity Mapping Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_get_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1IdentityMappingStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_identity_mapping_stores_get_builder(client, name)?;
    discoveryengine_projects_locations_identity_mapping_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:importIdentityMappings
/// Imports a list of Identity Mapping Entries to an Identity Mapping Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_builder(
    client: &SimpleHttpClient,
    identityMappingStore: &str,
    body: &GoogleCloudDiscoveryengineV1ImportIdentityMappingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores/{}:importIdentityMappings",
        identityMappingStore,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:importIdentityMappings
/// Imports a list of Identity Mapping Entries to an Identity Mapping Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:importIdentityMappings
/// Imports a list of Identity Mapping Entries to an Identity Mapping Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings(
    client: &SimpleHttpClient,
    identityMappingStore: &str,
    body: &GoogleCloudDiscoveryengineV1ImportIdentityMappingsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_builder(client, identityMappingStore, body)?;
    discoveryengine_projects_locations_identity_mapping_stores_import_identity_mappings_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores
/// Lists all Identity Mapping Stores.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_list_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_list` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores
/// Lists all Identity Mapping Stores.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListIdentityMappingStoresResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListIdentityMappingStoresResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores
/// Lists all Identity Mapping Stores.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_list_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListIdentityMappingStoresResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_identity_mapping_stores_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_identity_mapping_stores_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:listIdentityMappings
/// Lists Identity Mappings in an Identity Mapping Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_builder(
    client: &SimpleHttpClient,
    identityMappingStore: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores/{}:listIdentityMappings",
        identityMappingStore,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:listIdentityMappings
/// Lists Identity Mappings in an Identity Mapping Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListIdentityMappingsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListIdentityMappingsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:listIdentityMappings
/// Lists Identity Mappings in an Identity Mapping Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings(
    client: &SimpleHttpClient,
    identityMappingStore: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListIdentityMappingsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_builder(
            client,
            identityMappingStore,
            pageSize,
            pageToken,
        )?;
    discoveryengine_projects_locations_identity_mapping_stores_list_identity_mappings_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:purgeIdentityMappings
/// Purges specified or all Identity Mapping Entries from an Identity Mapping Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_builder(
    client: &SimpleHttpClient,
    identityMappingStore: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeIdentityMappingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores/{}:purgeIdentityMappings",
        identityMappingStore,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:purgeIdentityMappings
/// Purges specified or all Identity Mapping Entries from an Identity Mapping Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}:purgeIdentityMappings
/// Purges specified or all Identity Mapping Entries from an Identity Mapping Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings(
    client: &SimpleHttpClient,
    identityMappingStore: &str,
    body: &GoogleCloudDiscoveryengineV1PurgeIdentityMappingsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_builder(
            client,
            identityMappingStore,
            body,
        )?;
    discoveryengine_projects_locations_identity_mapping_stores_purge_identity_mappings_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_operations_get_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_operations_get_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_identity_mapping_stores_operations_get_builder(
            client, name,
        )?;
    discoveryengine_projects_locations_identity_mapping_stores_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_identity_mapping_stores_operations_list_execute()` to send, or `discoveryengine_projects_locations_identity_mapping_stores_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_identity_mapping_stores_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/identityMappingStores/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_identity_mapping_stores_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/identityMappingStores/{identityMappingStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_identity_mapping_stores_operations_list_builder()` + `discoveryengine_projects_locations_identity_mapping_stores_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_identity_mapping_stores_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_identity_mapping_stores_operations_list_builder(
            client,
            name,
            filter,
            pageSize,
            pageToken,
            returnPartialSuccess,
        )?;
    discoveryengine_projects_locations_identity_mapping_stores_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs
/// Creates a LicenseConfig This method should only be used for creating NotebookLm licenses or Gemini Enterprise free trial licenses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_license_configs_create_execute()` to send, or `discoveryengine_projects_locations_license_configs_create` for simplest API.

pub fn discoveryengine_projects_locations_license_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    licenseConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1LicenseConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/licenseConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = licenseConfigId {
        query_parts.push(format!("licenseConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs
/// Creates a LicenseConfig This method should only be used for creating NotebookLm licenses or Gemini Enterprise free trial licenses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_license_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_license_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_license_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1LicenseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1LicenseConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs
/// Creates a LicenseConfig This method should only be used for creating NotebookLm licenses or Gemini Enterprise free trial licenses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_license_configs_create_builder()` + `discoveryengine_projects_locations_license_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_license_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    licenseConfigId: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1LicenseConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1LicenseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_license_configs_create_builder(
        client,
        parent,
        licenseConfigId,
        body,
    )?;
    discoveryengine_projects_locations_license_configs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs/{licenseConfigsId}
/// Gets a LicenseConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_license_configs_get_execute()` to send, or `discoveryengine_projects_locations_license_configs_get` for simplest API.

pub fn discoveryengine_projects_locations_license_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/licenseConfigs/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs/{licenseConfigsId}
/// Gets a LicenseConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_license_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_license_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_license_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1LicenseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1LicenseConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs/{licenseConfigsId}
/// Gets a LicenseConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_license_configs_get_builder()` + `discoveryengine_projects_locations_license_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_license_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1LicenseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_license_configs_get_builder(client, name)?;
    discoveryengine_projects_locations_license_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs/{licenseConfigsId}
/// Updates the LicenseConfig
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_license_configs_patch_execute()` to send, or `discoveryengine_projects_locations_license_configs_patch` for simplest API.

pub fn discoveryengine_projects_locations_license_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1LicenseConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/licenseConfigs/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs/{licenseConfigsId}
/// Updates the LicenseConfig
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_license_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_license_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_license_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1LicenseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1LicenseConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/licenseConfigs/{licenseConfigsId}
/// Updates the LicenseConfig
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_license_configs_patch_builder()` + `discoveryengine_projects_locations_license_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_license_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1LicenseConfig,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1LicenseConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_license_configs_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_license_configs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_operations_get_execute()` to send, or `discoveryengine_projects_locations_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_operations_get_builder()` + `discoveryengine_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_operations_list_execute()` to send, or `discoveryengine_projects_locations_operations_list` for simplest API.

pub fn discoveryengine_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_operations_list_builder()` + `discoveryengine_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    discoveryengine_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/podcasts/{podcastsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_podcasts_operations_get_execute()` to send, or `discoveryengine_projects_locations_podcasts_operations_get` for simplest API.

pub fn discoveryengine_projects_locations_podcasts_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/podcasts/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/podcasts/{podcastsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_podcasts_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_podcasts_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_podcasts_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/podcasts/{podcastsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_podcasts_operations_get_builder()` + `discoveryengine_projects_locations_podcasts_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_podcasts_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_podcasts_operations_get_builder(client, name)?;
    discoveryengine_projects_locations_podcasts_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/rankingConfigs/{rankingConfigsId}:rank
/// Ranks a list of text records based on the given input query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_ranking_configs_rank_execute()` to send, or `discoveryengine_projects_locations_ranking_configs_rank` for simplest API.

pub fn discoveryengine_projects_locations_ranking_configs_rank_builder(
    client: &SimpleHttpClient,
    rankingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RankRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/rankingConfigs/{}:rank",
        rankingConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/rankingConfigs/{rankingConfigsId}:rank
/// Ranks a list of text records based on the given input query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_ranking_configs_rank_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_ranking_configs_rank_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_ranking_configs_rank_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RankResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1RankResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/rankingConfigs/{rankingConfigsId}:rank
/// Ranks a list of text records based on the given input query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_ranking_configs_rank_builder()` + `discoveryengine_projects_locations_ranking_configs_rank_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_ranking_configs_rank(
    client: &SimpleHttpClient,
    rankingConfig: &str,
    body: &GoogleCloudDiscoveryengineV1RankRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1RankResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_ranking_configs_rank_builder(
        client,
        rankingConfig,
        body,
    )?;
    discoveryengine_projects_locations_ranking_configs_rank_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_events_collect_execute()` to send, or `discoveryengine_projects_locations_user_events_collect` for simplest API.

pub fn discoveryengine_projects_locations_user_events_collect_builder(
    client: &SimpleHttpClient,
    parent: &str,
    ets: Option<&str>,
    uri: Option<&str>,
    userEvent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userEvents:collect",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ets {
        query_parts.push(format!("ets={}", val));
    }
    if let Some(val) = uri {
        query_parts.push(format!("uri={}", val));
    }
    if let Some(val) = userEvent {
        query_parts.push(format!("userEvent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_events_collect_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_events_collect_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_events_collect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a third-party domain. This method is used only by the Discovery Engine API JavaScript pixel and Google Tag Manager. Users should not call this method directly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_events_collect_builder()` + `discoveryengine_projects_locations_user_events_collect_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_events_collect(
    client: &SimpleHttpClient,
    parent: &str,
    ets: Option<&str>,
    uri: Option<&str>,
    userEvent: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_user_events_collect_builder(
        client, parent, ets, uri, userEvent,
    )?;
    discoveryengine_projects_locations_user_events_collect_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_events_import_execute()` to send, or `discoveryengine_projects_locations_user_events_import` for simplest API.

pub fn discoveryengine_projects_locations_user_events_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportUserEventsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userEvents:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_events_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_events_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_events_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:import
/// Bulk import of user events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_events_import_builder()` + `discoveryengine_projects_locations_user_events_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_events_import(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1ImportUserEventsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_user_events_import_builder(client, parent, body)?;
    discoveryengine_projects_locations_user_events_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:write
/// Writes a single user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_events_write_execute()` to send, or `discoveryengine_projects_locations_user_events_write` for simplest API.

pub fn discoveryengine_projects_locations_user_events_write_builder(
    client: &SimpleHttpClient,
    parent: &str,
    writeAsync: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1UserEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userEvents:write",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = writeAsync {
        query_parts.push(format!("writeAsync={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:write
/// Writes a single user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_events_write_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_events_write_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_events_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1UserEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userEvents:write
/// Writes a single user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_events_write_builder()` + `discoveryengine_projects_locations_user_events_write_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_events_write(
    client: &SimpleHttpClient,
    parent: &str,
    writeAsync: Option<bool>,
    body: &GoogleCloudDiscoveryengineV1UserEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_user_events_write_builder(
        client, parent, writeAsync, body,
    )?;
    discoveryengine_projects_locations_user_events_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}:batchUpdateUserLicenses
/// Updates the User License. This method is used for batch `assign/unassign` licenses to users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_stores_batch_update_user_licenses_execute()` to send, or `discoveryengine_projects_locations_user_stores_batch_update_user_licenses` for simplest API.

pub fn discoveryengine_projects_locations_user_stores_batch_update_user_licenses_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchUpdateUserLicensesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userStores/{}:batchUpdateUserLicenses",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}:batchUpdateUserLicenses
/// Updates the User License. This method is used for batch `assign/unassign` licenses to users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_stores_batch_update_user_licenses_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_stores_batch_update_user_licenses_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_batch_update_user_licenses_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}:batchUpdateUserLicenses
/// Updates the User License. This method is used for batch `assign/unassign` licenses to users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_stores_batch_update_user_licenses_builder()` + `discoveryengine_projects_locations_user_stores_batch_update_user_licenses_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_batch_update_user_licenses(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDiscoveryengineV1BatchUpdateUserLicensesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_user_stores_batch_update_user_licenses_builder(
            client, parent, body,
        )?;
    discoveryengine_projects_locations_user_stores_batch_update_user_licenses_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}
/// Gets the User Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_stores_get_execute()` to send, or `discoveryengine_projects_locations_user_stores_get` for simplest API.

pub fn discoveryengine_projects_locations_user_stores_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userStores/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}
/// Gets the User Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_stores_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_stores_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1UserStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}
/// Gets the User Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_stores_get_builder()` + `discoveryengine_projects_locations_user_stores_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_user_stores_get_builder(client, name)?;
    discoveryengine_projects_locations_user_stores_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}
/// Updates the User Store.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_stores_patch_execute()` to send, or `discoveryengine_projects_locations_user_stores_patch` for simplest API.

pub fn discoveryengine_projects_locations_user_stores_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1UserStore,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userStores/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}
/// Updates the User Store.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_stores_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_stores_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1UserStore = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}
/// Updates the User Store.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_stores_patch_builder()` + `discoveryengine_projects_locations_user_stores_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDiscoveryengineV1UserStore,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1UserStore>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_user_stores_patch_builder(
        client, name, updateMask, body,
    )?;
    discoveryengine_projects_locations_user_stores_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}/licenseConfigsUsageStats
/// Lists all the LicenseConfigUsageStatss associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_execute()` to send, or `discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list` for simplest API.

pub fn discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userStores/{}/licenseConfigsUsageStats",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}/licenseConfigsUsageStats
/// Lists all the LicenseConfigUsageStatss associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListLicenseConfigsUsageStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListLicenseConfigsUsageStatsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}/licenseConfigsUsageStats
/// Lists all the LicenseConfigUsageStatss associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_builder()` + `discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDiscoveryengineV1ListLicenseConfigsUsageStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_builder(
            client, parent,
        )?;
    discoveryengine_projects_locations_user_stores_license_configs_usage_stats_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}/userLicenses
/// Lists the User Licenses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_locations_user_stores_user_licenses_list_execute()` to send, or `discoveryengine_projects_locations_user_stores_user_licenses_list` for simplest API.

pub fn discoveryengine_projects_locations_user_stores_user_licenses_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/locations/{}/userStores/{}/userLicenses",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}/userLicenses
/// Lists the User Licenses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_locations_user_stores_user_licenses_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_locations_user_stores_user_licenses_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_user_licenses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListUserLicensesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDiscoveryengineV1ListUserLicensesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/userStores/{userStoresId}/userLicenses
/// Lists the User Licenses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_locations_user_stores_user_licenses_list_builder()` + `discoveryengine_projects_locations_user_stores_user_licenses_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_locations_user_stores_user_licenses_list(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDiscoveryengineV1ListUserLicensesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_locations_user_stores_user_licenses_list_builder(
        client, parent, orderBy, pageSize, pageToken,
    )?;
    discoveryengine_projects_locations_user_stores_user_licenses_list_execute(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_operations_cancel_execute()` to send, or `discoveryengine_projects_operations_cancel` for simplest API.

pub fn discoveryengine_projects_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_operations_cancel_builder()` + `discoveryengine_projects_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_operations_cancel_builder(client, name, body)?;
    discoveryengine_projects_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_operations_get_execute()` to send, or `discoveryengine_projects_operations_get` for simplest API.

pub fn discoveryengine_projects_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_operations_get_builder()` + `discoveryengine_projects_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_operations_get_builder(client, name)?;
    discoveryengine_projects_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discoveryengine_projects_operations_list_execute()` to send, or `discoveryengine_projects_operations_list` for simplest API.

pub fn discoveryengine_projects_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discoveryengine_projects_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discoveryengine_projects_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discoveryengine_projects_operations_list_builder()` + `discoveryengine_projects_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discoveryengine_projects_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = discoveryengine_projects_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    discoveryengine_projects_operations_list_execute(builder)
}
