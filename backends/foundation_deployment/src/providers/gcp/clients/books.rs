//! Auto-generated API clients for Google Cloud Platform - books.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET books/v1/users/{userId}/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_bookshelves_get_execute()` to send, or `books_bookshelves_get` for simplest API.

pub fn books_bookshelves_get_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    userId: &str,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/users/{}/bookshelves/{}",
        shelf, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_bookshelves_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_bookshelves_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelf = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_bookshelves_get_builder()` + `books_bookshelves_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_get(
    client: &SimpleHttpClient,
    shelf: &str,
    userId: &str,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_bookshelves_get_builder(client, shelf, userId, source)?;
    books_bookshelves_get_execute(builder)
}

/// GET books/v1/users/{userId}/bookshelves
/// Retrieves a list of public bookshelves for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_bookshelves_list_execute()` to send, or `books_bookshelves_list` for simplest API.

pub fn books_bookshelves_list_builder(
    client: &SimpleHttpClient,
    userId: &str,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/users/{}/bookshelves",
        userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/users/{userId}/bookshelves
/// Retrieves a list of public bookshelves for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_bookshelves_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_bookshelves_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelves = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/users/{userId}/bookshelves
/// Retrieves a list of public bookshelves for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_bookshelves_list_builder()` + `books_bookshelves_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_list(
    client: &SimpleHttpClient,
    userId: &str,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_bookshelves_list_builder(client, userId, source)?;
    books_bookshelves_list_execute(builder)
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
/// Retrieves volumes in a specific bookshelf for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_bookshelves_volumes_list_execute()` to send, or `books_bookshelves_volumes_list` for simplest API.

pub fn books_bookshelves_volumes_list_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    userId: &str,
    maxResults: Option<i32>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/users/{}/bookshelves/{}/volumes",
        shelf, userId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showPreorders",
            showPreorders
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
/// Retrieves volumes in a specific bookshelf for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_bookshelves_volumes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_bookshelves_volumes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
/// Retrieves volumes in a specific bookshelf for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_bookshelves_volumes_list_builder()` + `books_bookshelves_volumes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_volumes_list(
    client: &SimpleHttpClient,
    shelf: &str,
    userId: &str,
    maxResults: Option<i32>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_bookshelves_volumes_list_builder(
        client,
        shelf,
        userId,
        maxResults,
        showPreorders,
        source,
        startIndex,
    )?;
    books_bookshelves_volumes_list_execute(builder)
}

/// GET books/v1/cloudloading/addBook
/// Add a user-upload volume and triggers processing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_cloudloading_add_book_execute()` to send, or `books_cloudloading_add_book` for simplest API.

pub fn books_cloudloading_add_book_builder(
    client: &SimpleHttpClient,
    drive_document_id: Option<&str>,
    mime_type: Option<&str>,
    name: Option<&str>,
    upload_client_token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/cloudloading/addBook",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "drive_document_id",
            drive_document_id
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mime_type",
            mime_type
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "upload_client_token",
            upload_client_token
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/cloudloading/addBook
/// Add a user-upload volume and triggers processing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_cloudloading_add_book_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_cloudloading_add_book_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_add_book_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BooksCloudloadingResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/cloudloading/addBook
/// Add a user-upload volume and triggers processing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_cloudloading_add_book_builder()` + `books_cloudloading_add_book_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_add_book(
    client: &SimpleHttpClient,
    drive_document_id: Option<&str>,
    mime_type: Option<&str>,
    name: Option<&str>,
    upload_client_token: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_cloudloading_add_book_builder(
        client,
        drive_document_id,
        mime_type,
        name,
        upload_client_token,
    )?;
    books_cloudloading_add_book_execute(builder)
}

/// GET books/v1/cloudloading/deleteBook
/// Remove the book and its contents
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_cloudloading_delete_book_execute()` to send, or `books_cloudloading_delete_book` for simplest API.

pub fn books_cloudloading_delete_book_builder(
    client: &SimpleHttpClient,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/cloudloading/deleteBook",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/cloudloading/deleteBook
/// Remove the book and its contents
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_cloudloading_delete_book_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_cloudloading_delete_book_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_delete_book_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/cloudloading/deleteBook
/// Remove the book and its contents
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_cloudloading_delete_book_builder()` + `books_cloudloading_delete_book_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_delete_book(
    client: &SimpleHttpClient,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_cloudloading_delete_book_builder(client, volumeId)?;
    books_cloudloading_delete_book_execute(builder)
}

/// GET books/v1/cloudloading/updateBook
/// Updates a user-upload volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_cloudloading_update_book_execute()` to send, or `books_cloudloading_update_book` for simplest API.

pub fn books_cloudloading_update_book_builder(
    client: &SimpleHttpClient,
    body: &BooksCloudloadingResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/cloudloading/updateBook",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET books/v1/cloudloading/updateBook
/// Updates a user-upload volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_cloudloading_update_book_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_cloudloading_update_book_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_update_book_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BooksCloudloadingResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/cloudloading/updateBook
/// Updates a user-upload volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_cloudloading_update_book_builder()` + `books_cloudloading_update_book_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_update_book(
    client: &SimpleHttpClient,
    body: &BooksCloudloadingResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_cloudloading_update_book_builder(client, body)?;
    books_cloudloading_update_book_execute(builder)
}

/// GET books/v1/dictionary/listOfflineMetadata
/// Returns a list of offline dictionary metadata available
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_dictionary_list_offline_metadata_execute()` to send, or `books_dictionary_list_offline_metadata` for simplest API.

pub fn books_dictionary_list_offline_metadata_builder(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/dictionary/listOfflineMetadata",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cpksver",
            cpksver
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/dictionary/listOfflineMetadata
/// Returns a list of offline dictionary metadata available
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_dictionary_list_offline_metadata_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_dictionary_list_offline_metadata_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_dictionary_list_offline_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metadata>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Metadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/dictionary/listOfflineMetadata
/// Returns a list of offline dictionary metadata available
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_dictionary_list_offline_metadata_builder()` + `books_dictionary_list_offline_metadata_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_dictionary_list_offline_metadata(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metadata>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_dictionary_list_offline_metadata_builder(client, cpksver)?;
    books_dictionary_list_offline_metadata_execute(builder)
}

/// GET books/v1/familysharing/getFamilyInfo
/// Gets information regarding the family that the user is part of.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_familysharing_get_family_info_execute()` to send, or `books_familysharing_get_family_info` for simplest API.

pub fn books_familysharing_get_family_info_builder(
    client: &SimpleHttpClient,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/familysharing/getFamilyInfo",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/familysharing/getFamilyInfo
/// Gets information regarding the family that the user is part of.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_familysharing_get_family_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_familysharing_get_family_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_get_family_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FamilyInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FamilyInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/familysharing/getFamilyInfo
/// Gets information regarding the family that the user is part of.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_familysharing_get_family_info_builder()` + `books_familysharing_get_family_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_get_family_info(
    client: &SimpleHttpClient,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FamilyInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_familysharing_get_family_info_builder(client, source)?;
    books_familysharing_get_family_info_execute(builder)
}

/// GET books/v1/familysharing/share
/// Initiates sharing of the content with the user's family. Empty response indicates success.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_familysharing_share_execute()` to send, or `books_familysharing_share` for simplest API.

pub fn books_familysharing_share_builder(
    client: &SimpleHttpClient,
    docId: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/familysharing/share",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "docId",
            docId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/familysharing/share
/// Initiates sharing of the content with the user's family. Empty response indicates success.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_familysharing_share_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_familysharing_share_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_share_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/familysharing/share
/// Initiates sharing of the content with the user's family. Empty response indicates success.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_familysharing_share_builder()` + `books_familysharing_share_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_share(
    client: &SimpleHttpClient,
    docId: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_familysharing_share_builder(client, docId, source, volumeId)?;
    books_familysharing_share_execute(builder)
}

/// GET books/v1/familysharing/unshare
/// Initiates revoking content that has already been shared with the user's family. Empty response indicates success.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_familysharing_unshare_execute()` to send, or `books_familysharing_unshare` for simplest API.

pub fn books_familysharing_unshare_builder(
    client: &SimpleHttpClient,
    docId: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/familysharing/unshare",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "docId",
            docId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/familysharing/unshare
/// Initiates revoking content that has already been shared with the user's family. Empty response indicates success.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_familysharing_unshare_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_familysharing_unshare_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_unshare_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/familysharing/unshare
/// Initiates revoking content that has already been shared with the user's family. Empty response indicates success.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_familysharing_unshare_builder()` + `books_familysharing_unshare_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_unshare(
    client: &SimpleHttpClient,
    docId: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_familysharing_unshare_builder(client, docId, source, volumeId)?;
    books_familysharing_unshare_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
/// Gets the layer summary for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_get_execute()` to send, or `books_layers_get` for simplest API.

pub fn books_layers_get_builder(
    client: &SimpleHttpClient,
    summaryId: &str,
    volumeId: &str,
    contentVersion: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/volumes/{}/layersummary/{}",
        summaryId, volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
/// Gets the layer summary for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_layers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_layers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Layersummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
/// Gets the layer summary for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_get_builder()` + `books_layers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_get(
    client: &SimpleHttpClient,
    summaryId: &str,
    volumeId: &str,
    contentVersion: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_get_builder(client, summaryId, volumeId, contentVersion, source)?;
    books_layers_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layersummary
/// List the layer summaries for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_list_execute()` to send, or `books_layers_list` for simplest API.

pub fn books_layers_list_builder(
    client: &SimpleHttpClient,
    volumeId: &str,
    contentVersion: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/volumes/{}/layersummary",
        volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}/layersummary
/// List the layer summaries for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_layers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_layers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummaries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Layersummaries = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}/layersummary
/// List the layer summaries for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_list_builder()` + `books_layers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_list(
    client: &SimpleHttpClient,
    volumeId: &str,
    contentVersion: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummaries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_list_builder(
        client,
        volumeId,
        contentVersion,
        maxResults,
        pageToken,
        source,
    )?;
    books_layers_list_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
/// Gets the annotation data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_annotation_data_get_execute()` to send, or `books_layers_annotation_data_get` for simplest API.

pub fn books_layers_annotation_data_get_builder(
    client: &SimpleHttpClient,
    annotationDataId: &str,
    layerId: &str,
    volumeId: &str,
    allowWebDefinitions: Option<bool>,
    contentVersion: Option<&str>,
    h: Option<i32>,
    locale: Option<&str>,
    scale: Option<i32>,
    source: Option<&str>,
    w: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/volumes/{}/layers/{}/data/{}",
        annotationDataId, layerId, volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowWebDefinitions",
            allowWebDefinitions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "h",
            h.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "scale",
            scale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "w",
            w.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
/// Gets the annotation data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_layers_annotation_data_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_layers_annotation_data_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DictionaryAnnotationdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DictionaryAnnotationdata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
/// Gets the annotation data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_annotation_data_get_builder()` + `books_layers_annotation_data_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_get(
    client: &SimpleHttpClient,
    annotationDataId: &str,
    layerId: &str,
    volumeId: &str,
    allowWebDefinitions: Option<bool>,
    contentVersion: Option<&str>,
    h: Option<i32>,
    locale: Option<&str>,
    scale: Option<i32>,
    source: Option<&str>,
    w: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DictionaryAnnotationdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_annotation_data_get_builder(
        client,
        annotationDataId,
        layerId,
        volumeId,
        allowWebDefinitions,
        contentVersion,
        h,
        locale,
        scale,
        source,
        w,
    )?;
    books_layers_annotation_data_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
/// Gets the annotation data for a volume and layer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_annotation_data_list_execute()` to send, or `books_layers_annotation_data_list` for simplest API.

pub fn books_layers_annotation_data_list_builder(
    client: &SimpleHttpClient,
    layerId: &str,
    volumeId: &str,
    annotationDataId: Option<&str>,
    contentVersion: Option<&str>,
    h: Option<i32>,
    locale: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    scale: Option<i32>,
    source: Option<&str>,
    updatedMax: Option<&str>,
    updatedMin: Option<&str>,
    w: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/volumes/{}/layers/{}/data",
        layerId, volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "annotationDataId",
            annotationDataId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "h",
            h.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "scale",
            scale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMax",
            updatedMax
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMin",
            updatedMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "w",
            w.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
/// Gets the annotation data for a volume and layer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_layers_annotation_data_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_layers_annotation_data_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotationsdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotationsdata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
/// Gets the annotation data for a volume and layer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_annotation_data_list_builder()` + `books_layers_annotation_data_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_list(
    client: &SimpleHttpClient,
    layerId: &str,
    volumeId: &str,
    annotationDataId: Option<&str>,
    contentVersion: Option<&str>,
    h: Option<i32>,
    locale: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    scale: Option<i32>,
    source: Option<&str>,
    updatedMax: Option<&str>,
    updatedMin: Option<&str>,
    w: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotationsdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_annotation_data_list_builder(
        client,
        layerId,
        volumeId,
        annotationDataId,
        contentVersion,
        h,
        locale,
        maxResults,
        pageToken,
        scale,
        source,
        updatedMax,
        updatedMin,
        w,
    )?;
    books_layers_annotation_data_list_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
/// Gets the volume annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_volume_annotations_get_execute()` to send, or `books_layers_volume_annotations_get` for simplest API.

pub fn books_layers_volume_annotations_get_builder(
    client: &SimpleHttpClient,
    annotationId: &str,
    layerId: &str,
    volumeId: &str,
    locale: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/volumes/{}/layers/{}/annotations/{}",
        annotationId, layerId, volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
/// Gets the volume annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_layers_volume_annotations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_layers_volume_annotations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumeannotation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
/// Gets the volume annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_volume_annotations_get_builder()` + `books_layers_volume_annotations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_get(
    client: &SimpleHttpClient,
    annotationId: &str,
    layerId: &str,
    volumeId: &str,
    locale: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_volume_annotations_get_builder(
        client,
        annotationId,
        layerId,
        volumeId,
        locale,
        source,
    )?;
    books_layers_volume_annotations_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}
/// Gets the volume annotations for a volume and layer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_volume_annotations_list_execute()` to send, or `books_layers_volume_annotations_list` for simplest API.

pub fn books_layers_volume_annotations_list_builder(
    client: &SimpleHttpClient,
    layerId: &str,
    volumeId: &str,
    contentVersion: Option<&str>,
    endOffset: Option<&str>,
    endPosition: Option<&str>,
    locale: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    source: Option<&str>,
    startOffset: Option<&str>,
    startPosition: Option<&str>,
    updatedMax: Option<&str>,
    updatedMin: Option<&str>,
    volumeAnnotationsVersion: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/volumes/{}/layers/{}",
        layerId, volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "endOffset",
            endOffset
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "endPosition",
            endPosition
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startOffset",
            startOffset
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startPosition",
            startPosition
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMax",
            updatedMax
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMin",
            updatedMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeAnnotationsVersion",
            volumeAnnotationsVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}
/// Gets the volume annotations for a volume and layer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_layers_volume_annotations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_layers_volume_annotations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotations>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumeannotations = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}
/// Gets the volume annotations for a volume and layer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_volume_annotations_list_builder()` + `books_layers_volume_annotations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_list(
    client: &SimpleHttpClient,
    layerId: &str,
    volumeId: &str,
    contentVersion: Option<&str>,
    endOffset: Option<&str>,
    endPosition: Option<&str>,
    locale: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    source: Option<&str>,
    startOffset: Option<&str>,
    startPosition: Option<&str>,
    updatedMax: Option<&str>,
    updatedMin: Option<&str>,
    volumeAnnotationsVersion: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotations>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_volume_annotations_list_builder(
        client,
        layerId,
        volumeId,
        contentVersion,
        endOffset,
        endPosition,
        locale,
        maxResults,
        pageToken,
        showDeleted,
        source,
        startOffset,
        startPosition,
        updatedMax,
        updatedMin,
        volumeAnnotationsVersion,
    )?;
    books_layers_volume_annotations_list_execute(builder)
}

/// GET books/v1/myconfig/getUserSettings
/// Gets the current settings for the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_get_user_settings_execute()` to send, or `books_myconfig_get_user_settings` for simplest API.

pub fn books_myconfig_get_user_settings_builder(
    client: &SimpleHttpClient,
    country: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/myconfig/getUserSettings",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "country",
            country
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/myconfig/getUserSettings
/// Gets the current settings for the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_myconfig_get_user_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_myconfig_get_user_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_get_user_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Usersettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/myconfig/getUserSettings
/// Gets the current settings for the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_get_user_settings_builder()` + `books_myconfig_get_user_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_get_user_settings(
    client: &SimpleHttpClient,
    country: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_myconfig_get_user_settings_builder(client, country)?;
    books_myconfig_get_user_settings_execute(builder)
}

/// GET books/v1/myconfig/releaseDownloadAccess
/// Release downloaded content access restriction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_release_download_access_execute()` to send, or `books_myconfig_release_download_access` for simplest API.

pub fn books_myconfig_release_download_access_builder(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
    locale: Option<&str>,
    source: Option<&str>,
    volumeIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/myconfig/releaseDownloadAccess",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cpksver",
            cpksver
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeIds",
            volumeIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/myconfig/releaseDownloadAccess
/// Release downloaded content access restriction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_myconfig_release_download_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_myconfig_release_download_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_release_download_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccesses>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DownloadAccesses = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/myconfig/releaseDownloadAccess
/// Release downloaded content access restriction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_release_download_access_builder()` + `books_myconfig_release_download_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_release_download_access(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
    locale: Option<&str>,
    source: Option<&str>,
    volumeIds: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccesses>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        books_myconfig_release_download_access_builder(client, cpksver, locale, source, volumeIds)?;
    books_myconfig_release_download_access_execute(builder)
}

/// GET books/v1/myconfig/requestAccess
/// Request concurrent and download access restrictions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_request_access_execute()` to send, or `books_myconfig_request_access` for simplest API.

pub fn books_myconfig_request_access_builder(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
    licenseTypes: Option<&str>,
    locale: Option<&str>,
    nonce: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/myconfig/requestAccess",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cpksver",
            cpksver
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "licenseTypes",
            licenseTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "nonce",
            nonce
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/myconfig/requestAccess
/// Request concurrent and download access restrictions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_myconfig_request_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_myconfig_request_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_request_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RequestAccessData>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RequestAccessData = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/myconfig/requestAccess
/// Request concurrent and download access restrictions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_request_access_builder()` + `books_myconfig_request_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_request_access(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
    licenseTypes: Option<&str>,
    locale: Option<&str>,
    nonce: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RequestAccessData>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_myconfig_request_access_builder(
        client,
        cpksver,
        licenseTypes,
        locale,
        nonce,
        source,
        volumeId,
    )?;
    books_myconfig_request_access_execute(builder)
}

/// GET books/v1/myconfig/syncVolumeLicenses
/// Request downloaded content access for specified volumes on the My eBooks shelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_sync_volume_licenses_execute()` to send, or `books_myconfig_sync_volume_licenses` for simplest API.

pub fn books_myconfig_sync_volume_licenses_builder(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
    features: Option<&str>,
    includeNonComicsSeries: Option<bool>,
    locale: Option<&str>,
    nonce: Option<&str>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    volumeIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/myconfig/syncVolumeLicenses",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cpksver",
            cpksver
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "features",
            features
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeNonComicsSeries",
            includeNonComicsSeries
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "nonce",
            nonce
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showPreorders",
            showPreorders
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeIds",
            volumeIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/myconfig/syncVolumeLicenses
/// Request downloaded content access for specified volumes on the My eBooks shelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_myconfig_sync_volume_licenses_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_myconfig_sync_volume_licenses_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_sync_volume_licenses_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/myconfig/syncVolumeLicenses
/// Request downloaded content access for specified volumes on the My eBooks shelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_sync_volume_licenses_builder()` + `books_myconfig_sync_volume_licenses_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_sync_volume_licenses(
    client: &SimpleHttpClient,
    cpksver: Option<&str>,
    features: Option<&str>,
    includeNonComicsSeries: Option<bool>,
    locale: Option<&str>,
    nonce: Option<&str>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    volumeIds: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_myconfig_sync_volume_licenses_builder(
        client,
        cpksver,
        features,
        includeNonComicsSeries,
        locale,
        nonce,
        showPreorders,
        source,
        volumeIds,
    )?;
    books_myconfig_sync_volume_licenses_execute(builder)
}

/// GET books/v1/myconfig/updateUserSettings
/// Sets the settings for the user. If a sub-object is specified, it will overwrite the existing sub-object stored in the server. Unspecified sub-objects will retain the existing value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_update_user_settings_execute()` to send, or `books_myconfig_update_user_settings` for simplest API.

pub fn books_myconfig_update_user_settings_builder(
    client: &SimpleHttpClient,
    body: &Usersettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/myconfig/updateUserSettings",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET books/v1/myconfig/updateUserSettings
/// Sets the settings for the user. If a sub-object is specified, it will overwrite the existing sub-object stored in the server. Unspecified sub-objects will retain the existing value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_myconfig_update_user_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_myconfig_update_user_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_update_user_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Usersettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/myconfig/updateUserSettings
/// Sets the settings for the user. If a sub-object is specified, it will overwrite the existing sub-object stored in the server. Unspecified sub-objects will retain the existing value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_update_user_settings_builder()` + `books_myconfig_update_user_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_update_user_settings(
    client: &SimpleHttpClient,
    body: &Usersettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_myconfig_update_user_settings_builder(client, body)?;
    books_myconfig_update_user_settings_execute(builder)
}

/// GET books/v1/mylibrary/annotations/{annotationId}
/// Deletes an annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_delete_execute()` to send, or `books_mylibrary_annotations_delete` for simplest API.

pub fn books_mylibrary_annotations_delete_builder(
    client: &SimpleHttpClient,
    annotationId: &str,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/annotations/{}",
        annotationId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/annotations/{annotationId}
/// Deletes an annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_annotations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_annotations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/annotations/{annotationId}
/// Deletes an annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_delete_builder()` + `books_mylibrary_annotations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_delete(
    client: &SimpleHttpClient,
    annotationId: &str,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_delete_builder(client, annotationId, source)?;
    books_mylibrary_annotations_delete_execute(builder)
}

/// GET books/v1/mylibrary/annotations
/// Inserts a new annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_insert_execute()` to send, or `books_mylibrary_annotations_insert` for simplest API.

pub fn books_mylibrary_annotations_insert_builder(
    client: &SimpleHttpClient,
    annotationId: Option<&str>,
    country: Option<&str>,
    showOnlySummaryInResponse: Option<bool>,
    source: Option<&str>,
    body: &Annotation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/mylibrary/annotations",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "annotationId",
            annotationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "country",
            country
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showOnlySummaryInResponse",
            showOnlySummaryInResponse
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET books/v1/mylibrary/annotations
/// Inserts a new annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_annotations_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_annotations_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/annotations
/// Inserts a new annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_insert_builder()` + `books_mylibrary_annotations_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_insert(
    client: &SimpleHttpClient,
    annotationId: Option<&str>,
    country: Option<&str>,
    showOnlySummaryInResponse: Option<bool>,
    source: Option<&str>,
    body: &Annotation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_insert_builder(
        client,
        annotationId,
        country,
        showOnlySummaryInResponse,
        source,
        body,
    )?;
    books_mylibrary_annotations_insert_execute(builder)
}

/// GET books/v1/mylibrary/annotations
/// Retrieves a list of annotations, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_list_execute()` to send, or `books_mylibrary_annotations_list` for simplest API.

pub fn books_mylibrary_annotations_list_builder(
    client: &SimpleHttpClient,
    contentVersion: Option<&str>,
    layerId: Option<&str>,
    layerIds: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    source: Option<&str>,
    updatedMax: Option<&str>,
    updatedMin: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/mylibrary/annotations",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "layerId",
            layerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "layerIds",
            layerIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showDeleted",
            showDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMax",
            updatedMax
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatedMin",
            updatedMin
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/annotations
/// Retrieves a list of annotations, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_annotations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_annotations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotations>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotations = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/annotations
/// Retrieves a list of annotations, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_list_builder()` + `books_mylibrary_annotations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_list(
    client: &SimpleHttpClient,
    contentVersion: Option<&str>,
    layerId: Option<&str>,
    layerIds: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
    source: Option<&str>,
    updatedMax: Option<&str>,
    updatedMin: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotations>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_list_builder(
        client,
        contentVersion,
        layerId,
        layerIds,
        maxResults,
        pageToken,
        showDeleted,
        source,
        updatedMax,
        updatedMin,
        volumeId,
    )?;
    books_mylibrary_annotations_list_execute(builder)
}

/// GET books/v1/mylibrary/annotations/summary
/// Gets the summary of specified layers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_summary_execute()` to send, or `books_mylibrary_annotations_summary` for simplest API.

pub fn books_mylibrary_annotations_summary_builder(
    client: &SimpleHttpClient,
    layerIds: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/mylibrary/annotations/summary",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "layerIds",
            layerIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/annotations/summary
/// Gets the summary of specified layers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_annotations_summary_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_annotations_summary_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnnotationsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnnotationsSummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/annotations/summary
/// Gets the summary of specified layers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_summary_builder()` + `books_mylibrary_annotations_summary_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_summary(
    client: &SimpleHttpClient,
    layerIds: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnnotationsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_summary_builder(client, layerIds, source, volumeId)?;
    books_mylibrary_annotations_summary_execute(builder)
}

/// GET books/v1/mylibrary/annotations/{annotationId}
/// Updates an existing annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_update_execute()` to send, or `books_mylibrary_annotations_update` for simplest API.

pub fn books_mylibrary_annotations_update_builder(
    client: &SimpleHttpClient,
    annotationId: &str,
    source: Option<&str>,
    body: &Annotation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/annotations/{}",
        annotationId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET books/v1/mylibrary/annotations/{annotationId}
/// Updates an existing annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_annotations_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_annotations_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/annotations/{annotationId}
/// Updates an existing annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_update_builder()` + `books_mylibrary_annotations_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_update(
    client: &SimpleHttpClient,
    annotationId: &str,
    source: Option<&str>,
    body: &Annotation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_update_builder(client, annotationId, source, body)?;
    books_mylibrary_annotations_update_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/addVolume
/// Adds a volume to a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_add_volume_execute()` to send, or `books_mylibrary_bookshelves_add_volume` for simplest API.

pub fn books_mylibrary_bookshelves_add_volume_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    reason: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/bookshelves/{}/addVolume",
        shelf,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "reason",
            reason
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/addVolume
/// Adds a volume to a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_bookshelves_add_volume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_bookshelves_add_volume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_add_volume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/addVolume
/// Adds a volume to a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_add_volume_builder()` + `books_mylibrary_bookshelves_add_volume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_add_volume(
    client: &SimpleHttpClient,
    shelf: &str,
    reason: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_mylibrary_bookshelves_add_volume_builder(client, shelf, reason, source, volumeId)?;
    books_mylibrary_bookshelves_add_volume_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
/// Clears all volumes from a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_clear_volumes_execute()` to send, or `books_mylibrary_bookshelves_clear_volumes` for simplest API.

pub fn books_mylibrary_bookshelves_clear_volumes_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/bookshelves/{}/clearVolumes",
        shelf,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
/// Clears all volumes from a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_bookshelves_clear_volumes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_bookshelves_clear_volumes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_clear_volumes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
/// Clears all volumes from a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_clear_volumes_builder()` + `books_mylibrary_bookshelves_clear_volumes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_clear_volumes(
    client: &SimpleHttpClient,
    shelf: &str,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_clear_volumes_builder(client, shelf, source)?;
    books_mylibrary_bookshelves_clear_volumes_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf belonging to the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_get_execute()` to send, or `books_mylibrary_bookshelves_get` for simplest API.

pub fn books_mylibrary_bookshelves_get_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/bookshelves/{}",
        shelf,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf belonging to the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_bookshelves_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_bookshelves_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelf = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf belonging to the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_get_builder()` + `books_mylibrary_bookshelves_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_get(
    client: &SimpleHttpClient,
    shelf: &str,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_get_builder(client, shelf, source)?;
    books_mylibrary_bookshelves_get_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves
/// Retrieves a list of bookshelves belonging to the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_list_execute()` to send, or `books_mylibrary_bookshelves_list` for simplest API.

pub fn books_mylibrary_bookshelves_list_builder(
    client: &SimpleHttpClient,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/mylibrary/bookshelves",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/bookshelves
/// Retrieves a list of bookshelves belonging to the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_bookshelves_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_bookshelves_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelves = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/bookshelves
/// Retrieves a list of bookshelves belonging to the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_list_builder()` + `books_mylibrary_bookshelves_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_list(
    client: &SimpleHttpClient,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_list_builder(client, source)?;
    books_mylibrary_bookshelves_list_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/moveVolume
/// Moves a volume within a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_move_volume_execute()` to send, or `books_mylibrary_bookshelves_move_volume` for simplest API.

pub fn books_mylibrary_bookshelves_move_volume_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    source: Option<&str>,
    volumeId: Option<&str>,
    volumePosition: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/bookshelves/{}/moveVolume",
        shelf,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumePosition",
            volumePosition
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/moveVolume
/// Moves a volume within a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_bookshelves_move_volume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_bookshelves_move_volume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_move_volume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/moveVolume
/// Moves a volume within a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_move_volume_builder()` + `books_mylibrary_bookshelves_move_volume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_move_volume(
    client: &SimpleHttpClient,
    shelf: &str,
    source: Option<&str>,
    volumeId: Option<&str>,
    volumePosition: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_move_volume_builder(
        client,
        shelf,
        source,
        volumeId,
        volumePosition,
    )?;
    books_mylibrary_bookshelves_move_volume_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/removeVolume
/// Removes a volume from a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_remove_volume_execute()` to send, or `books_mylibrary_bookshelves_remove_volume` for simplest API.

pub fn books_mylibrary_bookshelves_remove_volume_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    reason: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/bookshelves/{}/removeVolume",
        shelf,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "reason",
            reason
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/removeVolume
/// Removes a volume from a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_bookshelves_remove_volume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_bookshelves_remove_volume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_remove_volume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/removeVolume
/// Removes a volume from a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_remove_volume_builder()` + `books_mylibrary_bookshelves_remove_volume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_remove_volume(
    client: &SimpleHttpClient,
    shelf: &str,
    reason: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_mylibrary_bookshelves_remove_volume_builder(client, shelf, reason, source, volumeId)?;
    books_mylibrary_bookshelves_remove_volume_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
/// Gets volume information for volumes on a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_volumes_list_execute()` to send, or `books_mylibrary_bookshelves_volumes_list` for simplest API.

pub fn books_mylibrary_bookshelves_volumes_list_builder(
    client: &SimpleHttpClient,
    shelf: &str,
    country: Option<&str>,
    maxResults: Option<i32>,
    projection: Option<&str>,
    q: Option<&str>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/bookshelves/{}/volumes",
        shelf,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "country",
            country
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projection",
            projection
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showPreorders",
            showPreorders
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
/// Gets volume information for volumes on a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_bookshelves_volumes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_bookshelves_volumes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
/// Gets volume information for volumes on a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_volumes_list_builder()` + `books_mylibrary_bookshelves_volumes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_volumes_list(
    client: &SimpleHttpClient,
    shelf: &str,
    country: Option<&str>,
    maxResults: Option<i32>,
    projection: Option<&str>,
    q: Option<&str>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_volumes_list_builder(
        client,
        shelf,
        country,
        maxResults,
        projection,
        q,
        showPreorders,
        source,
        startIndex,
    )?;
    books_mylibrary_bookshelves_volumes_list_execute(builder)
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}
/// Retrieves my reading position information for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_readingpositions_get_execute()` to send, or `books_mylibrary_readingpositions_get` for simplest API.

pub fn books_mylibrary_readingpositions_get_builder(
    client: &SimpleHttpClient,
    volumeId: &str,
    contentVersion: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/readingpositions/{}",
        volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}
/// Retrieves my reading position information for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_readingpositions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_readingpositions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadingPosition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReadingPosition = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}
/// Retrieves my reading position information for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_readingpositions_get_builder()` + `books_mylibrary_readingpositions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_get(
    client: &SimpleHttpClient,
    volumeId: &str,
    contentVersion: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadingPosition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        books_mylibrary_readingpositions_get_builder(client, volumeId, contentVersion, source)?;
    books_mylibrary_readingpositions_get_execute(builder)
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}/setPosition
/// Sets my reading position information for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_readingpositions_set_position_execute()` to send, or `books_mylibrary_readingpositions_set_position` for simplest API.

pub fn books_mylibrary_readingpositions_set_position_builder(
    client: &SimpleHttpClient,
    volumeId: &str,
    action: Option<&str>,
    contentVersion: Option<&str>,
    deviceCookie: Option<&str>,
    position: Option<&str>,
    source: Option<&str>,
    timestamp: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/mylibrary/readingpositions/{}/setPosition",
        volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "action",
            action
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "contentVersion",
            contentVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "deviceCookie",
            deviceCookie
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "position",
            position
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "timestamp",
            timestamp
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}/setPosition
/// Sets my reading position information for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_mylibrary_readingpositions_set_position_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_mylibrary_readingpositions_set_position_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_set_position_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}/setPosition
/// Sets my reading position information for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_readingpositions_set_position_builder()` + `books_mylibrary_readingpositions_set_position_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_set_position(
    client: &SimpleHttpClient,
    volumeId: &str,
    action: Option<&str>,
    contentVersion: Option<&str>,
    deviceCookie: Option<&str>,
    position: Option<&str>,
    source: Option<&str>,
    timestamp: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_readingpositions_set_position_builder(
        client,
        volumeId,
        action,
        contentVersion,
        deviceCookie,
        position,
        source,
        timestamp,
    )?;
    books_mylibrary_readingpositions_set_position_execute(builder)
}

/// GET books/v1/notification/get
/// Returns notification details for a given notification id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_notification_get_execute()` to send, or `books_notification_get` for simplest API.

pub fn books_notification_get_builder(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    notification_id: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/notification/get",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "notification_id",
            notification_id
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/notification/get
/// Returns notification details for a given notification id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_notification_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_notification_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_notification_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Notification>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Notification = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/notification/get
/// Returns notification details for a given notification id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_notification_get_builder()` + `books_notification_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_notification_get(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    notification_id: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Notification>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_notification_get_builder(client, locale, notification_id, source)?;
    books_notification_get_execute(builder)
}

/// GET books/v1/onboarding/listCategories
/// List categories for onboarding experience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_onboarding_list_categories_execute()` to send, or `books_onboarding_list_categories` for simplest API.

pub fn books_onboarding_list_categories_builder(
    client: &SimpleHttpClient,
    locale: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/onboarding/listCategories",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/onboarding/listCategories
/// List categories for onboarding experience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_onboarding_list_categories_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_onboarding_list_categories_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_categories_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Category>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Category = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/onboarding/listCategories
/// List categories for onboarding experience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_onboarding_list_categories_builder()` + `books_onboarding_list_categories_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_categories(
    client: &SimpleHttpClient,
    locale: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Category>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_onboarding_list_categories_builder(client, locale)?;
    books_onboarding_list_categories_execute(builder)
}

/// GET books/v1/onboarding/listCategoryVolumes
/// List available volumes under categories for onboarding experience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_onboarding_list_category_volumes_execute()` to send, or `books_onboarding_list_category_volumes` for simplest API.

pub fn books_onboarding_list_category_volumes_builder(
    client: &SimpleHttpClient,
    categoryId: Option<&str>,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/onboarding/listCategoryVolumes",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "categoryId",
            categoryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAllowedMaturityRating",
            maxAllowedMaturityRating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/onboarding/listCategoryVolumes
/// List available volumes under categories for onboarding experience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_onboarding_list_category_volumes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_onboarding_list_category_volumes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_category_volumes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume2>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/onboarding/listCategoryVolumes
/// List available volumes under categories for onboarding experience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_onboarding_list_category_volumes_builder()` + `books_onboarding_list_category_volumes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_category_volumes(
    client: &SimpleHttpClient,
    categoryId: Option<&str>,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume2>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_onboarding_list_category_volumes_builder(
        client,
        categoryId,
        locale,
        maxAllowedMaturityRating,
        pageSize,
        pageToken,
    )?;
    books_onboarding_list_category_volumes_execute(builder)
}

/// GET books/v1/personalizedstream/get
/// Returns a stream of personalized book clusters
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_personalizedstream_get_execute()` to send, or `books_personalizedstream_get` for simplest API.

pub fn books_personalizedstream_get_builder(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/personalizedstream/get",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAllowedMaturityRating",
            maxAllowedMaturityRating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/personalizedstream/get
/// Returns a stream of personalized book clusters
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_personalizedstream_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_personalizedstream_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_personalizedstream_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discoveryclusters>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Discoveryclusters = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/personalizedstream/get
/// Returns a stream of personalized book clusters
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_personalizedstream_get_builder()` + `books_personalizedstream_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_personalizedstream_get(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discoveryclusters>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        books_personalizedstream_get_builder(client, locale, maxAllowedMaturityRating, source)?;
    books_personalizedstream_get_execute(builder)
}

/// GET books/v1/promooffer/accept
/// Accepts the promo offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_promooffer_accept_execute()` to send, or `books_promooffer_accept` for simplest API.

pub fn books_promooffer_accept_builder(
    client: &SimpleHttpClient,
    androidId: Option<&str>,
    device: Option<&str>,
    manufacturer: Option<&str>,
    model: Option<&str>,
    offerId: Option<&str>,
    product: Option<&str>,
    serial: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/promooffer/accept",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "androidId",
            androidId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "device",
            device
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "manufacturer",
            manufacturer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "model",
            model
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "offerId",
            offerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "product",
            product
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serial",
            serial
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/promooffer/accept
/// Accepts the promo offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_promooffer_accept_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_promooffer_accept_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/promooffer/accept
/// Accepts the promo offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_promooffer_accept_builder()` + `books_promooffer_accept_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_accept(
    client: &SimpleHttpClient,
    androidId: Option<&str>,
    device: Option<&str>,
    manufacturer: Option<&str>,
    model: Option<&str>,
    offerId: Option<&str>,
    product: Option<&str>,
    serial: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_promooffer_accept_builder(
        client,
        androidId,
        device,
        manufacturer,
        model,
        offerId,
        product,
        serial,
        volumeId,
    )?;
    books_promooffer_accept_execute(builder)
}

/// GET books/v1/promooffer/dismiss
/// Marks the promo offer as dismissed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_promooffer_dismiss_execute()` to send, or `books_promooffer_dismiss` for simplest API.

pub fn books_promooffer_dismiss_builder(
    client: &SimpleHttpClient,
    androidId: Option<&str>,
    device: Option<&str>,
    manufacturer: Option<&str>,
    model: Option<&str>,
    offerId: Option<&str>,
    product: Option<&str>,
    serial: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/promooffer/dismiss",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "androidId",
            androidId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "device",
            device
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "manufacturer",
            manufacturer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "model",
            model
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "offerId",
            offerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "product",
            product
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serial",
            serial
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/promooffer/dismiss
/// Marks the promo offer as dismissed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_promooffer_dismiss_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_promooffer_dismiss_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_dismiss_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/promooffer/dismiss
/// Marks the promo offer as dismissed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_promooffer_dismiss_builder()` + `books_promooffer_dismiss_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_dismiss(
    client: &SimpleHttpClient,
    androidId: Option<&str>,
    device: Option<&str>,
    manufacturer: Option<&str>,
    model: Option<&str>,
    offerId: Option<&str>,
    product: Option<&str>,
    serial: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_promooffer_dismiss_builder(
        client,
        androidId,
        device,
        manufacturer,
        model,
        offerId,
        product,
        serial,
    )?;
    books_promooffer_dismiss_execute(builder)
}

/// GET books/v1/promooffer/get
/// Returns a list of promo offers available to the user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_promooffer_get_execute()` to send, or `books_promooffer_get` for simplest API.

pub fn books_promooffer_get_builder(
    client: &SimpleHttpClient,
    androidId: Option<&str>,
    device: Option<&str>,
    manufacturer: Option<&str>,
    model: Option<&str>,
    product: Option<&str>,
    serial: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/promooffer/get",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "androidId",
            androidId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "device",
            device
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "manufacturer",
            manufacturer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "model",
            model
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "product",
            product
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "serial",
            serial
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/promooffer/get
/// Returns a list of promo offers available to the user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_promooffer_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_promooffer_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Offers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Offers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/promooffer/get
/// Returns a list of promo offers available to the user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_promooffer_get_builder()` + `books_promooffer_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_get(
    client: &SimpleHttpClient,
    androidId: Option<&str>,
    device: Option<&str>,
    manufacturer: Option<&str>,
    model: Option<&str>,
    product: Option<&str>,
    serial: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Offers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_promooffer_get_builder(
        client,
        androidId,
        device,
        manufacturer,
        model,
        product,
        serial,
    )?;
    books_promooffer_get_execute(builder)
}

/// GET books/v1/series/get
/// Returns Series metadata for the given series ids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_series_get_execute()` to send, or `books_series_get` for simplest API.

pub fn books_series_get_builder(
    client: &SimpleHttpClient,
    series_id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/series/get",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "series_id",
            series_id
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/series/get
/// Returns Series metadata for the given series ids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_series_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_series_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Series>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Series = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/series/get
/// Returns Series metadata for the given series ids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_series_get_builder()` + `books_series_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_get(
    client: &SimpleHttpClient,
    series_id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Series>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_series_get_builder(client, series_id)?;
    books_series_get_execute(builder)
}

/// GET books/v1/series/membership/get
/// Returns Series membership data given the series id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_series_membership_get_execute()` to send, or `books_series_membership_get` for simplest API.

pub fn books_series_membership_get_builder(
    client: &SimpleHttpClient,
    page_size: Option<i32>,
    page_token: Option<&str>,
    series_id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/series/membership/get",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page_size",
            page_size
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page_token",
            page_token
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "series_id",
            series_id
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/series/membership/get
/// Returns Series membership data given the series id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_series_membership_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_series_membership_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_membership_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Seriesmembership>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Seriesmembership = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/series/membership/get
/// Returns Series membership data given the series id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_series_membership_get_builder()` + `books_series_membership_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_membership_get(
    client: &SimpleHttpClient,
    page_size: Option<i32>,
    page_token: Option<&str>,
    series_id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Seriesmembership>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_series_membership_get_builder(client, page_size, page_token, series_id)?;
    books_series_membership_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}
/// Gets volume information for a single volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_get_execute()` to send, or `books_volumes_get` for simplest API.

pub fn books_volumes_get_builder(
    client: &SimpleHttpClient,
    volumeId: &str,
    country: Option<&str>,
    includeNonComicsSeries: Option<bool>,
    partner: Option<&str>,
    projection: Option<&str>,
    source: Option<&str>,
    user_library_consistent_read: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/volumes/{}", volumeId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "country",
            country
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeNonComicsSeries",
            includeNonComicsSeries
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "partner",
            partner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projection",
            projection
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "user_library_consistent_read",
            user_library_consistent_read
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}
/// Gets volume information for a single volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_volumes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_volumes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}
/// Gets volume information for a single volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_get_builder()` + `books_volumes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_get(
    client: &SimpleHttpClient,
    volumeId: &str,
    country: Option<&str>,
    includeNonComicsSeries: Option<bool>,
    partner: Option<&str>,
    projection: Option<&str>,
    source: Option<&str>,
    user_library_consistent_read: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_get_builder(
        client,
        volumeId,
        country,
        includeNonComicsSeries,
        partner,
        projection,
        source,
        user_library_consistent_read,
    )?;
    books_volumes_get_execute(builder)
}

/// GET books/v1/volumes
/// Performs a book search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_list_execute()` to send, or `books_volumes_list` for simplest API.

pub fn books_volumes_list_builder(
    client: &SimpleHttpClient,
    download: Option<&str>,
    filter: Option<&str>,
    langRestrict: Option<&str>,
    libraryRestrict: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    partner: Option<&str>,
    printType: Option<&str>,
    projection: Option<&str>,
    q: Option<&str>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/volumes",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "download",
            download
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "langRestrict",
            langRestrict
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "libraryRestrict",
            libraryRestrict
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAllowedMaturityRating",
            maxAllowedMaturityRating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "partner",
            partner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "printType",
            printType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "projection",
            projection
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showPreorders",
            showPreorders
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes
/// Performs a book search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_volumes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_volumes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes
/// Performs a book search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_list_builder()` + `books_volumes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_list(
    client: &SimpleHttpClient,
    download: Option<&str>,
    filter: Option<&str>,
    langRestrict: Option<&str>,
    libraryRestrict: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    maxResults: Option<i32>,
    orderBy: Option<&str>,
    partner: Option<&str>,
    printType: Option<&str>,
    projection: Option<&str>,
    q: Option<&str>,
    showPreorders: Option<bool>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_list_builder(
        client,
        download,
        filter,
        langRestrict,
        libraryRestrict,
        maxAllowedMaturityRating,
        maxResults,
        orderBy,
        partner,
        printType,
        projection,
        q,
        showPreorders,
        source,
        startIndex,
    )?;
    books_volumes_list_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/associated
/// Return a list of associated books.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_associated_list_execute()` to send, or `books_volumes_associated_list` for simplest API.

pub fn books_volumes_associated_list_builder(
    client: &SimpleHttpClient,
    volumeId: &str,
    association: Option<&str>,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.combooks/v1/volumes/{}/associated",
        volumeId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "association",
            association
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAllowedMaturityRating",
            maxAllowedMaturityRating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/{volumeId}/associated
/// Return a list of associated books.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_volumes_associated_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_volumes_associated_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_associated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/{volumeId}/associated
/// Return a list of associated books.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_associated_list_builder()` + `books_volumes_associated_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_associated_list(
    client: &SimpleHttpClient,
    volumeId: &str,
    association: Option<&str>,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_associated_list_builder(
        client,
        volumeId,
        association,
        locale,
        maxAllowedMaturityRating,
        source,
    )?;
    books_volumes_associated_list_execute(builder)
}

/// GET books/v1/volumes/mybooks
/// Return a list of books in My Library.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_mybooks_list_execute()` to send, or `books_volumes_mybooks_list` for simplest API.

pub fn books_volumes_mybooks_list_builder(
    client: &SimpleHttpClient,
    acquireMethod: Option<&str>,
    country: Option<&str>,
    locale: Option<&str>,
    maxResults: Option<i32>,
    processingState: Option<&str>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/volumes/mybooks",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "acquireMethod",
            acquireMethod
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "country",
            country
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "processingState",
            processingState
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/mybooks
/// Return a list of books in My Library.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_volumes_mybooks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_volumes_mybooks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_mybooks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/mybooks
/// Return a list of books in My Library.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_mybooks_list_builder()` + `books_volumes_mybooks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_mybooks_list(
    client: &SimpleHttpClient,
    acquireMethod: Option<&str>,
    country: Option<&str>,
    locale: Option<&str>,
    maxResults: Option<i32>,
    processingState: Option<&str>,
    source: Option<&str>,
    startIndex: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_mybooks_list_builder(
        client,
        acquireMethod,
        country,
        locale,
        maxResults,
        processingState,
        source,
        startIndex,
    )?;
    books_volumes_mybooks_list_execute(builder)
}

/// GET books/v1/volumes/recommended
/// Return a list of recommended books for the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_recommended_list_execute()` to send, or `books_volumes_recommended_list` for simplest API.

pub fn books_volumes_recommended_list_builder(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    source: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/volumes/recommended",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAllowedMaturityRating",
            maxAllowedMaturityRating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/recommended
/// Return a list of recommended books for the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_volumes_recommended_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_volumes_recommended_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/recommended
/// Return a list of recommended books for the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_recommended_list_builder()` + `books_volumes_recommended_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_list(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    maxAllowedMaturityRating: Option<&str>,
    source: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_volumes_recommended_list_builder(client, locale, maxAllowedMaturityRating, source)?;
    books_volumes_recommended_list_execute(builder)
}

/// GET books/v1/volumes/recommended/rate
/// Rate a recommended book for the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_recommended_rate_execute()` to send, or `books_volumes_recommended_rate` for simplest API.

pub fn books_volumes_recommended_rate_builder(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    rating: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/volumes/recommended/rate",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rating",
            rating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/recommended/rate
/// Rate a recommended book for the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_volumes_recommended_rate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_volumes_recommended_rate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_rate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BooksVolumesRecommendedRateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BooksVolumesRecommendedRateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/recommended/rate
/// Rate a recommended book for the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_recommended_rate_builder()` + `books_volumes_recommended_rate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_rate(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    rating: Option<&str>,
    source: Option<&str>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BooksVolumesRecommendedRateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = books_volumes_recommended_rate_builder(client, locale, rating, source, volumeId)?;
    books_volumes_recommended_rate_execute(builder)
}

/// GET books/v1/volumes/useruploaded
/// Return a list of books uploaded by the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_useruploaded_list_execute()` to send, or `books_volumes_useruploaded_list` for simplest API.

pub fn books_volumes_useruploaded_list_builder(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    maxResults: Option<i32>,
    processingState: Option<&str>,
    source: Option<&str>,
    startIndex: Option<i32>,
    volumeId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.combooks/v1/volumes/useruploaded",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "processingState",
            processingState
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "source",
            source
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "volumeId",
            volumeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET books/v1/volumes/useruploaded
/// Return a list of books uploaded by the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `books_volumes_useruploaded_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from books_volumes_useruploaded_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_useruploaded_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET books/v1/volumes/useruploaded
/// Return a list of books uploaded by the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_useruploaded_list_builder()` + `books_volumes_useruploaded_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_useruploaded_list(
    client: &SimpleHttpClient,
    locale: Option<&str>,
    maxResults: Option<i32>,
    processingState: Option<&str>,
    source: Option<&str>,
    startIndex: Option<i32>,
    volumeId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_useruploaded_list_builder(
        client,
        locale,
        maxResults,
        processingState,
        source,
        startIndex,
        volumeId,
    )?;
    books_volumes_useruploaded_list_execute(builder)
}
