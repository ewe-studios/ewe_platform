//! Auto-generated API clients for gcp/books.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET books/v1/users/{userId}/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_bookshelves_get_execute()` to send, or `books_bookshelves_get` for simplest API.

pub fn books_bookshelves_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    shelf: &String,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/users/{}/bookshelves/{}",
        userId, shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_bookshelves_get_execute()` or `books_bookshelves_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_bookshelves_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelf>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelf = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_bookshelves_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_bookshelves_get_task()`.
/// For the simplest API, use `books_bookshelves_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_bookshelves_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_bookshelves_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_bookshelves_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_bookshelves_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksBookshelvesGetArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_bookshelves_get_builder()` + `books_bookshelves_get_execute()`.
/// For task-level control, use `books_bookshelves_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_get(
    client: &SimpleHttpClient,
    args: &BooksBookshelvesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_bookshelves_get_builder(client, &args.userId, &args.shelf, &args.source)?;
    books_bookshelves_get_execute(builder)
}

/// GET books/v1/users/{userId}/bookshelves
/// Retrieves a list of public bookshelves for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_bookshelves_list_execute()` to send, or `books_bookshelves_list` for simplest API.

pub fn books_bookshelves_list_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/users/{}/bookshelves",
        userId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/users/{userId}/bookshelves
/// Retrieves a list of public bookshelves for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_bookshelves_list_execute()` or `books_bookshelves_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_bookshelves_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelves>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelves = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/users/{userId}/bookshelves
/// Retrieves a list of public bookshelves for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_bookshelves_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_bookshelves_list_task()`.
/// For the simplest API, use `books_bookshelves_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_bookshelves_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_bookshelves_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_bookshelves_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_bookshelves_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksBookshelvesListArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/users/{userId}/bookshelves
/// Retrieves a list of public bookshelves for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_bookshelves_list_builder()` + `books_bookshelves_list_execute()`.
/// For task-level control, use `books_bookshelves_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_list(
    client: &SimpleHttpClient,
    args: &BooksBookshelvesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_bookshelves_list_builder(client, &args.userId, &args.source)?;
    books_bookshelves_list_execute(builder)
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
/// Retrieves volumes in a specific bookshelf for the specified user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_bookshelves_volumes_list_execute()` to send, or `books_bookshelves_volumes_list` for simplest API.

pub fn books_bookshelves_volumes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    shelf: &String,
    maxResults: &Option<Option<String>>,
    showPreorders: &Option<Option<String>>,
    source: &Option<Option<String>>,
    startIndex: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/users/{}/bookshelves/{}/volumes",
        userId, shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = showPreorders.as_ref() {
        query_parts.push(format!("showPreorders={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = startIndex.as_ref() {
        query_parts.push(format!("startIndex={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
/// Retrieves volumes in a specific bookshelf for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_bookshelves_volumes_list_execute()` or `books_bookshelves_volumes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_bookshelves_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_volumes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
/// Retrieves volumes in a specific bookshelf for the specified user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_bookshelves_volumes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_bookshelves_volumes_list_task()`.
/// For the simplest API, use `books_bookshelves_volumes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_bookshelves_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_bookshelves_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_bookshelves_volumes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_bookshelves_volumes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksBookshelvesVolumesListArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: showPreorders
    pub showPreorders: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: startIndex
    pub startIndex: Option<Option<String>>,
}

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
/// Retrieves volumes in a specific bookshelf for the specified user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_bookshelves_volumes_list_builder()` + `books_bookshelves_volumes_list_execute()`.
/// For task-level control, use `books_bookshelves_volumes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_bookshelves_volumes_list(
    client: &SimpleHttpClient,
    args: &BooksBookshelvesVolumesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_bookshelves_volumes_list_builder(
        client,
        &args.userId,
        &args.shelf,
        &args.maxResults,
        &args.showPreorders,
        &args.source,
        &args.startIndex,
    )?;
    books_bookshelves_volumes_list_execute(builder)
}

/// POST books/v1/cloudloading/addBook
/// Add a user-upload volume and triggers processing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_cloudloading_add_book_execute()` to send, or `books_cloudloading_add_book` for simplest API.

pub fn books_cloudloading_add_book_builder<R>(
    client: &SimpleHttpClient<R>,
    drive_document_id: &Option<Option<String>>,
    mime_type: &Option<Option<String>>,
    name: &Option<Option<String>>,
    upload_client_token: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/cloudloading/addBook",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = drive_document_id.as_ref() {
        query_parts.push(format!("drive_document_id={}", val));
    }
    if let Some(val) = mime_type.as_ref() {
        query_parts.push(format!("mime_type={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = upload_client_token.as_ref() {
        query_parts.push(format!("upload_client_token={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/cloudloading/addBook
/// Add a user-upload volume and triggers processing.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_cloudloading_add_book_execute()` or `books_cloudloading_add_book`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_cloudloading_add_book_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_add_book_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BooksCloudloadingResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BooksCloudloadingResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/cloudloading/addBook
/// Add a user-upload volume and triggers processing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_cloudloading_add_book_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_cloudloading_add_book_task()`.
/// For the simplest API, use `books_cloudloading_add_book()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_cloudloading_add_book_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_cloudloading_add_book_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_cloudloading_add_book_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_cloudloading_add_book`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksCloudloadingAddBookArgs {
    /// Query parameter: drive_document_id
    pub drive_document_id: Option<Option<String>>,
    /// Query parameter: mime_type
    pub mime_type: Option<Option<String>>,
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: upload_client_token
    pub upload_client_token: Option<Option<String>>,
}

/// POST books/v1/cloudloading/addBook
/// Add a user-upload volume and triggers processing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_cloudloading_add_book_builder()` + `books_cloudloading_add_book_execute()`.
/// For task-level control, use `books_cloudloading_add_book_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_add_book(
    client: &SimpleHttpClient,
    args: &BooksCloudloadingAddBookArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_cloudloading_add_book_builder(
        client,
        &args.drive_document_id,
        &args.mime_type,
        &args.name,
        &args.upload_client_token,
    )?;
    books_cloudloading_add_book_execute(builder)
}

/// POST books/v1/cloudloading/deleteBook
/// Remove the book and its contents
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_cloudloading_delete_book_execute()` to send, or `books_cloudloading_delete_book` for simplest API.

pub fn books_cloudloading_delete_book_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/cloudloading/deleteBook",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/cloudloading/deleteBook
/// Remove the book and its contents
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_cloudloading_delete_book_execute()` or `books_cloudloading_delete_book`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_cloudloading_delete_book_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_delete_book_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/cloudloading/deleteBook
/// Remove the book and its contents
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_cloudloading_delete_book_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_cloudloading_delete_book_task()`.
/// For the simplest API, use `books_cloudloading_delete_book()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_cloudloading_delete_book_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_cloudloading_delete_book_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_cloudloading_delete_book_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_cloudloading_delete_book`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksCloudloadingDeleteBookArgs {
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/cloudloading/deleteBook
/// Remove the book and its contents
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_cloudloading_delete_book_builder()` + `books_cloudloading_delete_book_execute()`.
/// For task-level control, use `books_cloudloading_delete_book_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_delete_book(
    client: &SimpleHttpClient,
    args: &BooksCloudloadingDeleteBookArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_cloudloading_delete_book_builder(client, &args.volumeId)?;
    books_cloudloading_delete_book_execute(builder)
}

/// POST books/v1/cloudloading/updateBook
/// Updates a user-upload volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_cloudloading_update_book_execute()` to send, or `books_cloudloading_update_book` for simplest API.

pub fn books_cloudloading_update_book_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/cloudloading/updateBook",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/cloudloading/updateBook
/// Updates a user-upload volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_cloudloading_update_book_execute()` or `books_cloudloading_update_book`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_cloudloading_update_book_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_update_book_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BooksCloudloadingResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BooksCloudloadingResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/cloudloading/updateBook
/// Updates a user-upload volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_cloudloading_update_book_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_cloudloading_update_book_task()`.
/// For the simplest API, use `books_cloudloading_update_book()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_cloudloading_update_book_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_cloudloading_update_book_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_cloudloading_update_book_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST books/v1/cloudloading/updateBook
/// Updates a user-upload volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_cloudloading_update_book_builder()` + `books_cloudloading_update_book_execute()`.
/// For task-level control, use `books_cloudloading_update_book_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_cloudloading_update_book(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BooksCloudloadingResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_cloudloading_update_book_builder(client)?;
    books_cloudloading_update_book_execute(builder)
}

/// GET books/v1/dictionary/listOfflineMetadata
/// Returns a list of offline dictionary metadata available
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_dictionary_list_offline_metadata_execute()` to send, or `books_dictionary_list_offline_metadata` for simplest API.

pub fn books_dictionary_list_offline_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    cpksver: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/dictionary/listOfflineMetadata",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cpksver.as_ref() {
        query_parts.push(format!("cpksver={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/dictionary/listOfflineMetadata
/// Returns a list of offline dictionary metadata available
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_dictionary_list_offline_metadata_execute()` or `books_dictionary_list_offline_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_dictionary_list_offline_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_dictionary_list_offline_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Metadata>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Metadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/dictionary/listOfflineMetadata
/// Returns a list of offline dictionary metadata available
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_dictionary_list_offline_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_dictionary_list_offline_metadata_task()`.
/// For the simplest API, use `books_dictionary_list_offline_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_dictionary_list_offline_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_dictionary_list_offline_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metadata>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_dictionary_list_offline_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_dictionary_list_offline_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksDictionaryListOfflineMetadataArgs {
    /// Query parameter: cpksver
    pub cpksver: Option<Option<String>>,
}

/// GET books/v1/dictionary/listOfflineMetadata
/// Returns a list of offline dictionary metadata available
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_dictionary_list_offline_metadata_builder()` + `books_dictionary_list_offline_metadata_execute()`.
/// For task-level control, use `books_dictionary_list_offline_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_dictionary_list_offline_metadata(
    client: &SimpleHttpClient,
    args: &BooksDictionaryListOfflineMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Metadata>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_dictionary_list_offline_metadata_builder(client, &args.cpksver)?;
    books_dictionary_list_offline_metadata_execute(builder)
}

/// GET books/v1/familysharing/getFamilyInfo
/// Gets information regarding the family that the user is part of.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_familysharing_get_family_info_execute()` to send, or `books_familysharing_get_family_info` for simplest API.

pub fn books_familysharing_get_family_info_builder<R>(
    client: &SimpleHttpClient<R>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/familysharing/getFamilyInfo",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/familysharing/getFamilyInfo
/// Gets information regarding the family that the user is part of.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_familysharing_get_family_info_execute()` or `books_familysharing_get_family_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_familysharing_get_family_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_get_family_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FamilyInfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FamilyInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/familysharing/getFamilyInfo
/// Gets information regarding the family that the user is part of.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_familysharing_get_family_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_familysharing_get_family_info_task()`.
/// For the simplest API, use `books_familysharing_get_family_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_familysharing_get_family_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_familysharing_get_family_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FamilyInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_familysharing_get_family_info_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_familysharing_get_family_info`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksFamilysharingGetFamilyInfoArgs {
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/familysharing/getFamilyInfo
/// Gets information regarding the family that the user is part of.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_familysharing_get_family_info_builder()` + `books_familysharing_get_family_info_execute()`.
/// For task-level control, use `books_familysharing_get_family_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_get_family_info(
    client: &SimpleHttpClient,
    args: &BooksFamilysharingGetFamilyInfoArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FamilyInfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_familysharing_get_family_info_builder(client, &args.source)?;
    books_familysharing_get_family_info_execute(builder)
}

/// POST books/v1/familysharing/share
/// Initiates sharing of the content with the user''s family. Empty response indicates success.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_familysharing_share_execute()` to send, or `books_familysharing_share` for simplest API.

pub fn books_familysharing_share_builder<R>(
    client: &SimpleHttpClient<R>,
    docId: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/familysharing/share",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = docId.as_ref() {
        query_parts.push(format!("docId={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/familysharing/share
/// Initiates sharing of the content with the user''s family. Empty response indicates success.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_familysharing_share_execute()` or `books_familysharing_share`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_familysharing_share_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_share_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/familysharing/share
/// Initiates sharing of the content with the user''s family. Empty response indicates success.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_familysharing_share_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_familysharing_share_task()`.
/// For the simplest API, use `books_familysharing_share()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_familysharing_share_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_familysharing_share_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_familysharing_share_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_familysharing_share`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksFamilysharingShareArgs {
    /// Query parameter: docId
    pub docId: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/familysharing/share
/// Initiates sharing of the content with the user''s family. Empty response indicates success.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_familysharing_share_builder()` + `books_familysharing_share_execute()`.
/// For task-level control, use `books_familysharing_share_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_share(
    client: &SimpleHttpClient,
    args: &BooksFamilysharingShareArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_familysharing_share_builder(client, &args.docId, &args.source, &args.volumeId)?;
    books_familysharing_share_execute(builder)
}

/// POST books/v1/familysharing/unshare
/// Initiates revoking content that has already been shared with the user''s family. Empty response indicates success.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_familysharing_unshare_execute()` to send, or `books_familysharing_unshare` for simplest API.

pub fn books_familysharing_unshare_builder<R>(
    client: &SimpleHttpClient<R>,
    docId: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/familysharing/unshare",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = docId.as_ref() {
        query_parts.push(format!("docId={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/familysharing/unshare
/// Initiates revoking content that has already been shared with the user''s family. Empty response indicates success.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_familysharing_unshare_execute()` or `books_familysharing_unshare`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_familysharing_unshare_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_unshare_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/familysharing/unshare
/// Initiates revoking content that has already been shared with the user''s family. Empty response indicates success.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_familysharing_unshare_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_familysharing_unshare_task()`.
/// For the simplest API, use `books_familysharing_unshare()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_familysharing_unshare_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_familysharing_unshare_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_familysharing_unshare_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_familysharing_unshare`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksFamilysharingUnshareArgs {
    /// Query parameter: docId
    pub docId: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/familysharing/unshare
/// Initiates revoking content that has already been shared with the user''s family. Empty response indicates success.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_familysharing_unshare_builder()` + `books_familysharing_unshare_execute()`.
/// For task-level control, use `books_familysharing_unshare_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_familysharing_unshare(
    client: &SimpleHttpClient,
    args: &BooksFamilysharingUnshareArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_familysharing_unshare_builder(client, &args.docId, &args.source, &args.volumeId)?;
    books_familysharing_unshare_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
/// Gets the layer summary for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_get_execute()` to send, or `books_layers_get` for simplest API.

pub fn books_layers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    summaryId: &String,
    contentVersion: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layersummary/{}",
        volumeId, summaryId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
/// Gets the layer summary for a volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_layers_get_execute()` or `books_layers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Layersummary>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Layersummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
/// Gets the layer summary for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_layers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_layers_get_task()`.
/// For the simplest API, use `books_layers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_layers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_layers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_layers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersGetArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Path parameter: summaryId
    pub summaryId: String,
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
/// Gets the layer summary for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_get_builder()` + `books_layers_get_execute()`.
/// For task-level control, use `books_layers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_get(
    client: &SimpleHttpClient,
    args: &BooksLayersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_get_builder(
        client,
        &args.volumeId,
        &args.summaryId,
        &args.contentVersion,
        &args.source,
    )?;
    books_layers_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layersummary
/// List the layer summaries for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_list_execute()` to send, or `books_layers_list` for simplest API.

pub fn books_layers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    contentVersion: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layersummary",
        volumeId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}/layersummary
/// List the layer summaries for a volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_layers_list_execute()` or `books_layers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Layersummaries>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Layersummaries = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}/layersummary
/// List the layer summaries for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_layers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_layers_list_task()`.
/// For the simplest API, use `books_layers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_layers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummaries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_layers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_layers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersListArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}/layersummary
/// List the layer summaries for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_list_builder()` + `books_layers_list_execute()`.
/// For task-level control, use `books_layers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_list(
    client: &SimpleHttpClient,
    args: &BooksLayersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Layersummaries>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_list_builder(
        client,
        &args.volumeId,
        &args.contentVersion,
        &args.maxResults,
        &args.pageToken,
        &args.source,
    )?;
    books_layers_list_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
/// Gets the annotation data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_annotation_data_get_execute()` to send, or `books_layers_annotation_data_get` for simplest API.

pub fn books_layers_annotation_data_get_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    layerId: &String,
    annotationDataId: &String,
    allowWebDefinitions: &Option<Option<String>>,
    contentVersion: &Option<Option<String>>,
    h: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    scale: &Option<Option<String>>,
    source: &Option<Option<String>>,
    w: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}/data/{}",
        volumeId, layerId, annotationDataId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowWebDefinitions.as_ref() {
        query_parts.push(format!("allowWebDefinitions={}", val));
    }
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = h.as_ref() {
        query_parts.push(format!("h={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = scale.as_ref() {
        query_parts.push(format!("scale={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = w.as_ref() {
        query_parts.push(format!("w={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
/// Gets the annotation data.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_layers_annotation_data_get_execute()` or `books_layers_annotation_data_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_annotation_data_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DictionaryAnnotationdata>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DictionaryAnnotationdata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
/// Gets the annotation data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_layers_annotation_data_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_layers_annotation_data_get_task()`.
/// For the simplest API, use `books_layers_annotation_data_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_annotation_data_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_layers_annotation_data_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DictionaryAnnotationdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_layers_annotation_data_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_layers_annotation_data_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersAnnotationDataGetArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Path parameter: layerId
    pub layerId: String,
    /// Path parameter: annotationDataId
    pub annotationDataId: String,
    /// Query parameter: allowWebDefinitions
    pub allowWebDefinitions: Option<Option<String>>,
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: h
    pub h: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: scale
    pub scale: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: w
    pub w: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
/// Gets the annotation data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_annotation_data_get_builder()` + `books_layers_annotation_data_get_execute()`.
/// For task-level control, use `books_layers_annotation_data_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_get(
    client: &SimpleHttpClient,
    args: &BooksLayersAnnotationDataGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DictionaryAnnotationdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_annotation_data_get_builder(
        client,
        &args.volumeId,
        &args.layerId,
        &args.annotationDataId,
        &args.allowWebDefinitions,
        &args.contentVersion,
        &args.h,
        &args.locale,
        &args.scale,
        &args.source,
        &args.w,
    )?;
    books_layers_annotation_data_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
/// Gets the annotation data for a volume and layer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_annotation_data_list_execute()` to send, or `books_layers_annotation_data_list` for simplest API.

pub fn books_layers_annotation_data_list_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    layerId: &String,
    annotationDataId: &Option<Option<String>>,
    contentVersion: &Option<Option<String>>,
    h: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    scale: &Option<Option<String>>,
    source: &Option<Option<String>>,
    updatedMax: &Option<Option<String>>,
    updatedMin: &Option<Option<String>>,
    w: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}/data",
        volumeId, layerId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = annotationDataId.as_ref() {
        query_parts.push(format!("annotationDataId={}", val));
    }
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = h.as_ref() {
        query_parts.push(format!("h={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = scale.as_ref() {
        query_parts.push(format!("scale={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = updatedMax.as_ref() {
        query_parts.push(format!("updatedMax={}", val));
    }
    if let Some(val) = updatedMin.as_ref() {
        query_parts.push(format!("updatedMin={}", val));
    }
    if let Some(val) = w.as_ref() {
        query_parts.push(format!("w={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
/// Gets the annotation data for a volume and layer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_layers_annotation_data_list_execute()` or `books_layers_annotation_data_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_annotation_data_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotationsdata>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotationsdata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
/// Gets the annotation data for a volume and layer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_layers_annotation_data_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_layers_annotation_data_list_task()`.
/// For the simplest API, use `books_layers_annotation_data_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_annotation_data_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_layers_annotation_data_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotationsdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_layers_annotation_data_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_layers_annotation_data_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersAnnotationDataListArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Path parameter: layerId
    pub layerId: String,
    /// Query parameter: annotationDataId
    pub annotationDataId: Option<Option<String>>,
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: h
    pub h: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: scale
    pub scale: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: updatedMax
    pub updatedMax: Option<Option<String>>,
    /// Query parameter: updatedMin
    pub updatedMin: Option<Option<String>>,
    /// Query parameter: w
    pub w: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
/// Gets the annotation data for a volume and layer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_annotation_data_list_builder()` + `books_layers_annotation_data_list_execute()`.
/// For task-level control, use `books_layers_annotation_data_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_annotation_data_list(
    client: &SimpleHttpClient,
    args: &BooksLayersAnnotationDataListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotationsdata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_annotation_data_list_builder(
        client,
        &args.volumeId,
        &args.layerId,
        &args.annotationDataId,
        &args.contentVersion,
        &args.h,
        &args.locale,
        &args.maxResults,
        &args.pageToken,
        &args.scale,
        &args.source,
        &args.updatedMax,
        &args.updatedMin,
        &args.w,
    )?;
    books_layers_annotation_data_list_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
/// Gets the volume annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_volume_annotations_get_execute()` to send, or `books_layers_volume_annotations_get` for simplest API.

pub fn books_layers_volume_annotations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    layerId: &String,
    annotationId: &String,
    locale: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}/annotations/{}",
        volumeId, layerId, annotationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
/// Gets the volume annotation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_layers_volume_annotations_get_execute()` or `books_layers_volume_annotations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_volume_annotations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumeannotation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumeannotation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
/// Gets the volume annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_layers_volume_annotations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_layers_volume_annotations_get_task()`.
/// For the simplest API, use `books_layers_volume_annotations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_volume_annotations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_layers_volume_annotations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_layers_volume_annotations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_layers_volume_annotations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersVolumeAnnotationsGetArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Path parameter: layerId
    pub layerId: String,
    /// Path parameter: annotationId
    pub annotationId: String,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
/// Gets the volume annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_volume_annotations_get_builder()` + `books_layers_volume_annotations_get_execute()`.
/// For task-level control, use `books_layers_volume_annotations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_get(
    client: &SimpleHttpClient,
    args: &BooksLayersVolumeAnnotationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_volume_annotations_get_builder(
        client,
        &args.volumeId,
        &args.layerId,
        &args.annotationId,
        &args.locale,
        &args.source,
    )?;
    books_layers_volume_annotations_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}
/// Gets the volume annotations for a volume and layer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_layers_volume_annotations_list_execute()` to send, or `books_layers_volume_annotations_list` for simplest API.

pub fn books_layers_volume_annotations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    layerId: &String,
    contentVersion: &Option<Option<String>>,
    endOffset: &Option<Option<String>>,
    endPosition: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    showDeleted: &Option<Option<String>>,
    source: &Option<Option<String>>,
    startOffset: &Option<Option<String>>,
    startPosition: &Option<Option<String>>,
    updatedMax: &Option<Option<String>>,
    updatedMin: &Option<Option<String>>,
    volumeAnnotationsVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}",
        volumeId, layerId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = endOffset.as_ref() {
        query_parts.push(format!("endOffset={}", val));
    }
    if let Some(val) = endPosition.as_ref() {
        query_parts.push(format!("endPosition={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = startOffset.as_ref() {
        query_parts.push(format!("startOffset={}", val));
    }
    if let Some(val) = startPosition.as_ref() {
        query_parts.push(format!("startPosition={}", val));
    }
    if let Some(val) = updatedMax.as_ref() {
        query_parts.push(format!("updatedMax={}", val));
    }
    if let Some(val) = updatedMin.as_ref() {
        query_parts.push(format!("updatedMin={}", val));
    }
    if let Some(val) = volumeAnnotationsVersion.as_ref() {
        query_parts.push(format!("volumeAnnotationsVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}
/// Gets the volume annotations for a volume and layer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_layers_volume_annotations_list_execute()` or `books_layers_volume_annotations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_volume_annotations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumeannotations>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumeannotations = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}
/// Gets the volume annotations for a volume and layer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_layers_volume_annotations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_layers_volume_annotations_list_task()`.
/// For the simplest API, use `books_layers_volume_annotations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_layers_volume_annotations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_layers_volume_annotations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotations>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_layers_volume_annotations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_layers_volume_annotations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersVolumeAnnotationsListArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Path parameter: layerId
    pub layerId: String,
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: endOffset
    pub endOffset: Option<Option<String>>,
    /// Query parameter: endPosition
    pub endPosition: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: startOffset
    pub startOffset: Option<Option<String>>,
    /// Query parameter: startPosition
    pub startPosition: Option<Option<String>>,
    /// Query parameter: updatedMax
    pub updatedMax: Option<Option<String>>,
    /// Query parameter: updatedMin
    pub updatedMin: Option<Option<String>>,
    /// Query parameter: volumeAnnotationsVersion
    pub volumeAnnotationsVersion: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}/layers/{layerId}
/// Gets the volume annotations for a volume and layer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_layers_volume_annotations_list_builder()` + `books_layers_volume_annotations_list_execute()`.
/// For task-level control, use `books_layers_volume_annotations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_layers_volume_annotations_list(
    client: &SimpleHttpClient,
    args: &BooksLayersVolumeAnnotationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumeannotations>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_layers_volume_annotations_list_builder(
        client,
        &args.volumeId,
        &args.layerId,
        &args.contentVersion,
        &args.endOffset,
        &args.endPosition,
        &args.locale,
        &args.maxResults,
        &args.pageToken,
        &args.showDeleted,
        &args.source,
        &args.startOffset,
        &args.startPosition,
        &args.updatedMax,
        &args.updatedMin,
        &args.volumeAnnotationsVersion,
    )?;
    books_layers_volume_annotations_list_execute(builder)
}

/// GET books/v1/myconfig/getUserSettings
/// Gets the current settings for the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_get_user_settings_execute()` to send, or `books_myconfig_get_user_settings` for simplest API.

pub fn books_myconfig_get_user_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    country: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/myconfig/getUserSettings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = country.as_ref() {
        query_parts.push(format!("country={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/myconfig/getUserSettings
/// Gets the current settings for the user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_myconfig_get_user_settings_execute()` or `books_myconfig_get_user_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_get_user_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_get_user_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Usersettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Usersettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/myconfig/getUserSettings
/// Gets the current settings for the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_myconfig_get_user_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_myconfig_get_user_settings_task()`.
/// For the simplest API, use `books_myconfig_get_user_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_get_user_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_myconfig_get_user_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_myconfig_get_user_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_myconfig_get_user_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMyconfigGetUserSettingsArgs {
    /// Query parameter: country
    pub country: Option<Option<String>>,
}

/// GET books/v1/myconfig/getUserSettings
/// Gets the current settings for the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_get_user_settings_builder()` + `books_myconfig_get_user_settings_execute()`.
/// For task-level control, use `books_myconfig_get_user_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_get_user_settings(
    client: &SimpleHttpClient,
    args: &BooksMyconfigGetUserSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_myconfig_get_user_settings_builder(client, &args.country)?;
    books_myconfig_get_user_settings_execute(builder)
}

/// POST books/v1/myconfig/releaseDownloadAccess
/// Release downloaded content access restriction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_release_download_access_execute()` to send, or `books_myconfig_release_download_access` for simplest API.

pub fn books_myconfig_release_download_access_builder<R>(
    client: &SimpleHttpClient<R>,
    cpksver: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeIds: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/myconfig/releaseDownloadAccess",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cpksver.as_ref() {
        query_parts.push(format!("cpksver={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeIds.as_ref() {
        query_parts.push(format!("volumeIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/myconfig/releaseDownloadAccess
/// Release downloaded content access restriction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_myconfig_release_download_access_execute()` or `books_myconfig_release_download_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_release_download_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_release_download_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DownloadAccesses>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DownloadAccesses = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/myconfig/releaseDownloadAccess
/// Release downloaded content access restriction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_myconfig_release_download_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_myconfig_release_download_access_task()`.
/// For the simplest API, use `books_myconfig_release_download_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_release_download_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_myconfig_release_download_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccesses>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_myconfig_release_download_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_myconfig_release_download_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMyconfigReleaseDownloadAccessArgs {
    /// Query parameter: cpksver
    pub cpksver: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeIds
    pub volumeIds: Option<Option<String>>,
}

/// POST books/v1/myconfig/releaseDownloadAccess
/// Release downloaded content access restriction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_release_download_access_builder()` + `books_myconfig_release_download_access_execute()`.
/// For task-level control, use `books_myconfig_release_download_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_release_download_access(
    client: &SimpleHttpClient,
    args: &BooksMyconfigReleaseDownloadAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DownloadAccesses>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_myconfig_release_download_access_builder(
        client,
        &args.cpksver,
        &args.locale,
        &args.source,
        &args.volumeIds,
    )?;
    books_myconfig_release_download_access_execute(builder)
}

/// POST books/v1/myconfig/requestAccess
/// Request concurrent and download access restrictions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_request_access_execute()` to send, or `books_myconfig_request_access` for simplest API.

pub fn books_myconfig_request_access_builder<R>(
    client: &SimpleHttpClient<R>,
    cpksver: &Option<Option<String>>,
    licenseTypes: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    nonce: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/myconfig/requestAccess",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cpksver.as_ref() {
        query_parts.push(format!("cpksver={}", val));
    }
    if let Some(val) = licenseTypes.as_ref() {
        query_parts.push(format!("licenseTypes={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = nonce.as_ref() {
        query_parts.push(format!("nonce={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/myconfig/requestAccess
/// Request concurrent and download access restrictions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_myconfig_request_access_execute()` or `books_myconfig_request_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_request_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_request_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RequestAccessData>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RequestAccessData = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/myconfig/requestAccess
/// Request concurrent and download access restrictions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_myconfig_request_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_myconfig_request_access_task()`.
/// For the simplest API, use `books_myconfig_request_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_request_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_myconfig_request_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RequestAccessData>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_myconfig_request_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_myconfig_request_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMyconfigRequestAccessArgs {
    /// Query parameter: cpksver
    pub cpksver: Option<Option<String>>,
    /// Query parameter: licenseTypes
    pub licenseTypes: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: nonce
    pub nonce: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/myconfig/requestAccess
/// Request concurrent and download access restrictions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_request_access_builder()` + `books_myconfig_request_access_execute()`.
/// For task-level control, use `books_myconfig_request_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_request_access(
    client: &SimpleHttpClient,
    args: &BooksMyconfigRequestAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RequestAccessData>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_myconfig_request_access_builder(
        client,
        &args.cpksver,
        &args.licenseTypes,
        &args.locale,
        &args.nonce,
        &args.source,
        &args.volumeId,
    )?;
    books_myconfig_request_access_execute(builder)
}

/// POST books/v1/myconfig/syncVolumeLicenses
/// Request downloaded content access for specified volumes on the My eBooks shelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_sync_volume_licenses_execute()` to send, or `books_myconfig_sync_volume_licenses` for simplest API.

pub fn books_myconfig_sync_volume_licenses_builder<R>(
    client: &SimpleHttpClient<R>,
    cpksver: &Option<Option<String>>,
    features: &Option<Option<String>>,
    includeNonComicsSeries: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    nonce: &Option<Option<String>>,
    showPreorders: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeIds: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/myconfig/syncVolumeLicenses",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = cpksver.as_ref() {
        query_parts.push(format!("cpksver={}", val));
    }
    if let Some(val) = features.as_ref() {
        query_parts.push(format!("features={}", val));
    }
    if let Some(val) = includeNonComicsSeries.as_ref() {
        query_parts.push(format!("includeNonComicsSeries={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = nonce.as_ref() {
        query_parts.push(format!("nonce={}", val));
    }
    if let Some(val) = showPreorders.as_ref() {
        query_parts.push(format!("showPreorders={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeIds.as_ref() {
        query_parts.push(format!("volumeIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/myconfig/syncVolumeLicenses
/// Request downloaded content access for specified volumes on the My eBooks shelf.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_myconfig_sync_volume_licenses_execute()` or `books_myconfig_sync_volume_licenses`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_sync_volume_licenses_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_sync_volume_licenses_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/myconfig/syncVolumeLicenses
/// Request downloaded content access for specified volumes on the My eBooks shelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_myconfig_sync_volume_licenses_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_myconfig_sync_volume_licenses_task()`.
/// For the simplest API, use `books_myconfig_sync_volume_licenses()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_sync_volume_licenses_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_myconfig_sync_volume_licenses_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_myconfig_sync_volume_licenses_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_myconfig_sync_volume_licenses`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMyconfigSyncVolumeLicensesArgs {
    /// Query parameter: cpksver
    pub cpksver: Option<Option<String>>,
    /// Query parameter: features
    pub features: Option<Option<String>>,
    /// Query parameter: includeNonComicsSeries
    pub includeNonComicsSeries: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: nonce
    pub nonce: Option<Option<String>>,
    /// Query parameter: showPreorders
    pub showPreorders: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeIds
    pub volumeIds: Option<Option<String>>,
}

/// POST books/v1/myconfig/syncVolumeLicenses
/// Request downloaded content access for specified volumes on the My eBooks shelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_sync_volume_licenses_builder()` + `books_myconfig_sync_volume_licenses_execute()`.
/// For task-level control, use `books_myconfig_sync_volume_licenses_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_sync_volume_licenses(
    client: &SimpleHttpClient,
    args: &BooksMyconfigSyncVolumeLicensesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_myconfig_sync_volume_licenses_builder(
        client,
        &args.cpksver,
        &args.features,
        &args.includeNonComicsSeries,
        &args.locale,
        &args.nonce,
        &args.showPreorders,
        &args.source,
        &args.volumeIds,
    )?;
    books_myconfig_sync_volume_licenses_execute(builder)
}

/// POST books/v1/myconfig/updateUserSettings
/// Sets the settings for the user. If a sub-object is specified, it will overwrite the existing sub-object stored in the server. Unspecified sub-objects will retain the existing value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_myconfig_update_user_settings_execute()` to send, or `books_myconfig_update_user_settings` for simplest API.

pub fn books_myconfig_update_user_settings_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/myconfig/updateUserSettings",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/myconfig/updateUserSettings
/// Sets the settings for the user. If a sub-object is specified, it will overwrite the existing sub-object stored in the server. Unspecified sub-objects will retain the existing value.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_myconfig_update_user_settings_execute()` or `books_myconfig_update_user_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_update_user_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_update_user_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Usersettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Usersettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/myconfig/updateUserSettings
/// Sets the settings for the user. If a sub-object is specified, it will overwrite the existing sub-object stored in the server. Unspecified sub-objects will retain the existing value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_myconfig_update_user_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_myconfig_update_user_settings_task()`.
/// For the simplest API, use `books_myconfig_update_user_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_myconfig_update_user_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_myconfig_update_user_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_myconfig_update_user_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST books/v1/myconfig/updateUserSettings
/// Sets the settings for the user. If a sub-object is specified, it will overwrite the existing sub-object stored in the server. Unspecified sub-objects will retain the existing value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_myconfig_update_user_settings_builder()` + `books_myconfig_update_user_settings_execute()`.
/// For task-level control, use `books_myconfig_update_user_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_myconfig_update_user_settings(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Usersettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_myconfig_update_user_settings_builder(client)?;
    books_myconfig_update_user_settings_execute(builder)
}

/// DELETE books/v1/mylibrary/annotations/{annotationId}
/// Deletes an annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_delete_execute()` to send, or `books_mylibrary_annotations_delete` for simplest API.

pub fn books_mylibrary_annotations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    annotationId: &String,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/annotations/{}",
        annotationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE books/v1/mylibrary/annotations/{annotationId}
/// Deletes an annotation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_annotations_delete_execute()` or `books_mylibrary_annotations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE books/v1/mylibrary/annotations/{annotationId}
/// Deletes an annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_annotations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_annotations_delete_task()`.
/// For the simplest API, use `books_mylibrary_annotations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_annotations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_annotations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_annotations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsDeleteArgs {
    /// Path parameter: annotationId
    pub annotationId: String,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// DELETE books/v1/mylibrary/annotations/{annotationId}
/// Deletes an annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_delete_builder()` + `books_mylibrary_annotations_delete_execute()`.
/// For task-level control, use `books_mylibrary_annotations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_delete(
    client: &SimpleHttpClient,
    args: &BooksMylibraryAnnotationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_mylibrary_annotations_delete_builder(client, &args.annotationId, &args.source)?;
    books_mylibrary_annotations_delete_execute(builder)
}

/// POST books/v1/mylibrary/annotations
/// Inserts a new annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_insert_execute()` to send, or `books_mylibrary_annotations_insert` for simplest API.

pub fn books_mylibrary_annotations_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    annotationId: &Option<Option<String>>,
    country: &Option<Option<String>>,
    showOnlySummaryInResponse: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/annotations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = annotationId.as_ref() {
        query_parts.push(format!("annotationId={}", val));
    }
    if let Some(val) = country.as_ref() {
        query_parts.push(format!("country={}", val));
    }
    if let Some(val) = showOnlySummaryInResponse.as_ref() {
        query_parts.push(format!("showOnlySummaryInResponse={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/mylibrary/annotations
/// Inserts a new annotation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_annotations_insert_execute()` or `books_mylibrary_annotations_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/mylibrary/annotations
/// Inserts a new annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_annotations_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_annotations_insert_task()`.
/// For the simplest API, use `books_mylibrary_annotations_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_annotations_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_annotations_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_annotations_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsInsertArgs {
    /// Query parameter: annotationId
    pub annotationId: Option<Option<String>>,
    /// Query parameter: country
    pub country: Option<Option<String>>,
    /// Query parameter: showOnlySummaryInResponse
    pub showOnlySummaryInResponse: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// POST books/v1/mylibrary/annotations
/// Inserts a new annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_insert_builder()` + `books_mylibrary_annotations_insert_execute()`.
/// For task-level control, use `books_mylibrary_annotations_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_insert(
    client: &SimpleHttpClient,
    args: &BooksMylibraryAnnotationsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_insert_builder(
        client,
        &args.annotationId,
        &args.country,
        &args.showOnlySummaryInResponse,
        &args.source,
    )?;
    books_mylibrary_annotations_insert_execute(builder)
}

/// GET books/v1/mylibrary/annotations
/// Retrieves a list of annotations, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_list_execute()` to send, or `books_mylibrary_annotations_list` for simplest API.

pub fn books_mylibrary_annotations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    contentVersion: &Option<Option<String>>,
    layerId: &Option<Option<String>>,
    layerIds: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    showDeleted: &Option<Option<String>>,
    source: &Option<Option<String>>,
    updatedMax: &Option<Option<String>>,
    updatedMin: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/annotations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = layerId.as_ref() {
        query_parts.push(format!("layerId={}", val));
    }
    if let Some(val) = layerIds.as_ref() {
        query_parts.push(format!("layerIds={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = updatedMax.as_ref() {
        query_parts.push(format!("updatedMax={}", val));
    }
    if let Some(val) = updatedMin.as_ref() {
        query_parts.push(format!("updatedMin={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/mylibrary/annotations
/// Retrieves a list of annotations, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_annotations_list_execute()` or `books_mylibrary_annotations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotations>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotations = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/mylibrary/annotations
/// Retrieves a list of annotations, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_annotations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_annotations_list_task()`.
/// For the simplest API, use `books_mylibrary_annotations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_annotations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotations>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_annotations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_annotations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsListArgs {
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: layerId
    pub layerId: Option<Option<String>>,
    /// Query parameter: layerIds
    pub layerIds: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: updatedMax
    pub updatedMax: Option<Option<String>>,
    /// Query parameter: updatedMin
    pub updatedMin: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// GET books/v1/mylibrary/annotations
/// Retrieves a list of annotations, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_list_builder()` + `books_mylibrary_annotations_list_execute()`.
/// For task-level control, use `books_mylibrary_annotations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_list(
    client: &SimpleHttpClient,
    args: &BooksMylibraryAnnotationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotations>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_list_builder(
        client,
        &args.contentVersion,
        &args.layerId,
        &args.layerIds,
        &args.maxResults,
        &args.pageToken,
        &args.showDeleted,
        &args.source,
        &args.updatedMax,
        &args.updatedMin,
        &args.volumeId,
    )?;
    books_mylibrary_annotations_list_execute(builder)
}

/// POST books/v1/mylibrary/annotations/summary
/// Gets the summary of specified layers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_summary_execute()` to send, or `books_mylibrary_annotations_summary` for simplest API.

pub fn books_mylibrary_annotations_summary_builder<R>(
    client: &SimpleHttpClient<R>,
    layerIds: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/mylibrary/annotations/summary",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = layerIds.as_ref() {
        query_parts.push(format!("layerIds={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/mylibrary/annotations/summary
/// Gets the summary of specified layers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_annotations_summary_execute()` or `books_mylibrary_annotations_summary`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_summary_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnnotationsSummary>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnnotationsSummary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/mylibrary/annotations/summary
/// Gets the summary of specified layers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_annotations_summary_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_annotations_summary_task()`.
/// For the simplest API, use `books_mylibrary_annotations_summary()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_annotations_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnnotationsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_mylibrary_annotations_summary_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_annotations_summary`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsSummaryArgs {
    /// Query parameter: layerIds
    pub layerIds: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/mylibrary/annotations/summary
/// Gets the summary of specified layers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_summary_builder()` + `books_mylibrary_annotations_summary_execute()`.
/// For task-level control, use `books_mylibrary_annotations_summary_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_summary(
    client: &SimpleHttpClient,
    args: &BooksMylibraryAnnotationsSummaryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnnotationsSummary>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_mylibrary_annotations_summary_builder(
        client,
        &args.layerIds,
        &args.source,
        &args.volumeId,
    )?;
    books_mylibrary_annotations_summary_execute(builder)
}

/// PUT books/v1/mylibrary/annotations/{annotationId}
/// Updates an existing annotation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_annotations_update_execute()` to send, or `books_mylibrary_annotations_update` for simplest API.

pub fn books_mylibrary_annotations_update_builder<R>(
    client: &SimpleHttpClient<R>,
    annotationId: &String,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/annotations/{}",
        annotationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT books/v1/mylibrary/annotations/{annotationId}
/// Updates an existing annotation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_annotations_update_execute()` or `books_mylibrary_annotations_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Annotation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT books/v1/mylibrary/annotations/{annotationId}
/// Updates an existing annotation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_annotations_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_annotations_update_task()`.
/// For the simplest API, use `books_mylibrary_annotations_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_annotations_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_annotations_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_annotations_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_annotations_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsUpdateArgs {
    /// Path parameter: annotationId
    pub annotationId: String,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// PUT books/v1/mylibrary/annotations/{annotationId}
/// Updates an existing annotation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_annotations_update_builder()` + `books_mylibrary_annotations_update_execute()`.
/// For task-level control, use `books_mylibrary_annotations_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_annotations_update(
    client: &SimpleHttpClient,
    args: &BooksMylibraryAnnotationsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Annotation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_mylibrary_annotations_update_builder(client, &args.annotationId, &args.source)?;
    books_mylibrary_annotations_update_execute(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/addVolume
/// Adds a volume to a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_add_volume_execute()` to send, or `books_mylibrary_bookshelves_add_volume` for simplest API.

pub fn books_mylibrary_bookshelves_add_volume_builder<R>(
    client: &SimpleHttpClient<R>,
    shelf: &String,
    reason: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/addVolume",
        shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reason.as_ref() {
        query_parts.push(format!("reason={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/addVolume
/// Adds a volume to a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_bookshelves_add_volume_execute()` or `books_mylibrary_bookshelves_add_volume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_add_volume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_add_volume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/addVolume
/// Adds a volume to a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_bookshelves_add_volume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_bookshelves_add_volume_task()`.
/// For the simplest API, use `books_mylibrary_bookshelves_add_volume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_add_volume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_bookshelves_add_volume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_bookshelves_add_volume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_bookshelves_add_volume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesAddVolumeArgs {
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: reason
    pub reason: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/addVolume
/// Adds a volume to a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_add_volume_builder()` + `books_mylibrary_bookshelves_add_volume_execute()`.
/// For task-level control, use `books_mylibrary_bookshelves_add_volume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_add_volume(
    client: &SimpleHttpClient,
    args: &BooksMylibraryBookshelvesAddVolumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_add_volume_builder(
        client,
        &args.shelf,
        &args.reason,
        &args.source,
        &args.volumeId,
    )?;
    books_mylibrary_bookshelves_add_volume_execute(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
/// Clears all volumes from a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_clear_volumes_execute()` to send, or `books_mylibrary_bookshelves_clear_volumes` for simplest API.

pub fn books_mylibrary_bookshelves_clear_volumes_builder<R>(
    client: &SimpleHttpClient<R>,
    shelf: &String,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/clearVolumes",
        shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
/// Clears all volumes from a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_bookshelves_clear_volumes_execute()` or `books_mylibrary_bookshelves_clear_volumes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_clear_volumes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_clear_volumes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
/// Clears all volumes from a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_bookshelves_clear_volumes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_bookshelves_clear_volumes_task()`.
/// For the simplest API, use `books_mylibrary_bookshelves_clear_volumes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_clear_volumes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_bookshelves_clear_volumes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_bookshelves_clear_volumes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_bookshelves_clear_volumes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesClearVolumesArgs {
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
/// Clears all volumes from a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_clear_volumes_builder()` + `books_mylibrary_bookshelves_clear_volumes_execute()`.
/// For task-level control, use `books_mylibrary_bookshelves_clear_volumes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_clear_volumes(
    client: &SimpleHttpClient,
    args: &BooksMylibraryBookshelvesClearVolumesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        books_mylibrary_bookshelves_clear_volumes_builder(client, &args.shelf, &args.source)?;
    books_mylibrary_bookshelves_clear_volumes_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf belonging to the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_get_execute()` to send, or `books_mylibrary_bookshelves_get` for simplest API.

pub fn books_mylibrary_bookshelves_get_builder<R>(
    client: &SimpleHttpClient<R>,
    shelf: &String,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}",
        shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf belonging to the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_bookshelves_get_execute()` or `books_mylibrary_bookshelves_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelf>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelf = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf belonging to the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_bookshelves_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_bookshelves_get_task()`.
/// For the simplest API, use `books_mylibrary_bookshelves_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_bookshelves_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_bookshelves_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_bookshelves_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesGetArgs {
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/mylibrary/bookshelves/{shelf}
/// Retrieves metadata for a specific bookshelf belonging to the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_get_builder()` + `books_mylibrary_bookshelves_get_execute()`.
/// For task-level control, use `books_mylibrary_bookshelves_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_get(
    client: &SimpleHttpClient,
    args: &BooksMylibraryBookshelvesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelf>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_get_builder(client, &args.shelf, &args.source)?;
    books_mylibrary_bookshelves_get_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves
/// Retrieves a list of bookshelves belonging to the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_list_execute()` to send, or `books_mylibrary_bookshelves_list` for simplest API.

pub fn books_mylibrary_bookshelves_list_builder<R>(
    client: &SimpleHttpClient<R>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/bookshelves",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/mylibrary/bookshelves
/// Retrieves a list of bookshelves belonging to the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_bookshelves_list_execute()` or `books_mylibrary_bookshelves_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelves>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bookshelves = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/mylibrary/bookshelves
/// Retrieves a list of bookshelves belonging to the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_bookshelves_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_bookshelves_list_task()`.
/// For the simplest API, use `books_mylibrary_bookshelves_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_bookshelves_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_bookshelves_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_bookshelves_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesListArgs {
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/mylibrary/bookshelves
/// Retrieves a list of bookshelves belonging to the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_list_builder()` + `books_mylibrary_bookshelves_list_execute()`.
/// For task-level control, use `books_mylibrary_bookshelves_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_list(
    client: &SimpleHttpClient,
    args: &BooksMylibraryBookshelvesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bookshelves>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_list_builder(client, &args.source)?;
    books_mylibrary_bookshelves_list_execute(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/moveVolume
/// Moves a volume within a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_move_volume_execute()` to send, or `books_mylibrary_bookshelves_move_volume` for simplest API.

pub fn books_mylibrary_bookshelves_move_volume_builder<R>(
    client: &SimpleHttpClient<R>,
    shelf: &String,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
    volumePosition: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/moveVolume",
        shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }
    if let Some(val) = volumePosition.as_ref() {
        query_parts.push(format!("volumePosition={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/moveVolume
/// Moves a volume within a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_bookshelves_move_volume_execute()` or `books_mylibrary_bookshelves_move_volume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_move_volume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_move_volume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/moveVolume
/// Moves a volume within a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_bookshelves_move_volume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_bookshelves_move_volume_task()`.
/// For the simplest API, use `books_mylibrary_bookshelves_move_volume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_move_volume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_bookshelves_move_volume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_bookshelves_move_volume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_bookshelves_move_volume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesMoveVolumeArgs {
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
    /// Query parameter: volumePosition
    pub volumePosition: Option<Option<String>>,
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/moveVolume
/// Moves a volume within a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_move_volume_builder()` + `books_mylibrary_bookshelves_move_volume_execute()`.
/// For task-level control, use `books_mylibrary_bookshelves_move_volume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_move_volume(
    client: &SimpleHttpClient,
    args: &BooksMylibraryBookshelvesMoveVolumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_move_volume_builder(
        client,
        &args.shelf,
        &args.source,
        &args.volumeId,
        &args.volumePosition,
    )?;
    books_mylibrary_bookshelves_move_volume_execute(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/removeVolume
/// Removes a volume from a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_remove_volume_execute()` to send, or `books_mylibrary_bookshelves_remove_volume` for simplest API.

pub fn books_mylibrary_bookshelves_remove_volume_builder<R>(
    client: &SimpleHttpClient<R>,
    shelf: &String,
    reason: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/removeVolume",
        shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reason.as_ref() {
        query_parts.push(format!("reason={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/removeVolume
/// Removes a volume from a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_bookshelves_remove_volume_execute()` or `books_mylibrary_bookshelves_remove_volume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_remove_volume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_remove_volume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/removeVolume
/// Removes a volume from a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_bookshelves_remove_volume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_bookshelves_remove_volume_task()`.
/// For the simplest API, use `books_mylibrary_bookshelves_remove_volume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_remove_volume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_bookshelves_remove_volume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_bookshelves_remove_volume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_bookshelves_remove_volume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesRemoveVolumeArgs {
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: reason
    pub reason: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/mylibrary/bookshelves/{shelf}/removeVolume
/// Removes a volume from a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_remove_volume_builder()` + `books_mylibrary_bookshelves_remove_volume_execute()`.
/// For task-level control, use `books_mylibrary_bookshelves_remove_volume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_remove_volume(
    client: &SimpleHttpClient,
    args: &BooksMylibraryBookshelvesRemoveVolumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_remove_volume_builder(
        client,
        &args.shelf,
        &args.reason,
        &args.source,
        &args.volumeId,
    )?;
    books_mylibrary_bookshelves_remove_volume_execute(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
/// Gets volume information for volumes on a bookshelf.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_bookshelves_volumes_list_execute()` to send, or `books_mylibrary_bookshelves_volumes_list` for simplest API.

pub fn books_mylibrary_bookshelves_volumes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    shelf: &String,
    country: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    projection: &Option<Option<String>>,
    q: &Option<Option<String>>,
    showPreorders: &Option<Option<String>>,
    source: &Option<Option<String>>,
    startIndex: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/volumes",
        shelf,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = country.as_ref() {
        query_parts.push(format!("country={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = projection.as_ref() {
        query_parts.push(format!("projection={}", val));
    }
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = showPreorders.as_ref() {
        query_parts.push(format!("showPreorders={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = startIndex.as_ref() {
        query_parts.push(format!("startIndex={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
/// Gets volume information for volumes on a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_bookshelves_volumes_list_execute()` or `books_mylibrary_bookshelves_volumes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_volumes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
/// Gets volume information for volumes on a bookshelf.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_bookshelves_volumes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_bookshelves_volumes_list_task()`.
/// For the simplest API, use `books_mylibrary_bookshelves_volumes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_bookshelves_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_bookshelves_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_bookshelves_volumes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_bookshelves_volumes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesVolumesListArgs {
    /// Path parameter: shelf
    pub shelf: String,
    /// Query parameter: country
    pub country: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: projection
    pub projection: Option<Option<String>>,
    /// Query parameter: q
    pub q: Option<Option<String>>,
    /// Query parameter: showPreorders
    pub showPreorders: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: startIndex
    pub startIndex: Option<Option<String>>,
}

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
/// Gets volume information for volumes on a bookshelf.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_bookshelves_volumes_list_builder()` + `books_mylibrary_bookshelves_volumes_list_execute()`.
/// For task-level control, use `books_mylibrary_bookshelves_volumes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_bookshelves_volumes_list(
    client: &SimpleHttpClient,
    args: &BooksMylibraryBookshelvesVolumesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_bookshelves_volumes_list_builder(
        client,
        &args.shelf,
        &args.country,
        &args.maxResults,
        &args.projection,
        &args.q,
        &args.showPreorders,
        &args.source,
        &args.startIndex,
    )?;
    books_mylibrary_bookshelves_volumes_list_execute(builder)
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}
/// Retrieves my reading position information for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_readingpositions_get_execute()` to send, or `books_mylibrary_readingpositions_get` for simplest API.

pub fn books_mylibrary_readingpositions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    contentVersion: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/readingpositions/{}",
        volumeId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}
/// Retrieves my reading position information for a volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_readingpositions_get_execute()` or `books_mylibrary_readingpositions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_readingpositions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReadingPosition>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReadingPosition = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}
/// Retrieves my reading position information for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_readingpositions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_readingpositions_get_task()`.
/// For the simplest API, use `books_mylibrary_readingpositions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_readingpositions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_readingpositions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadingPosition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_mylibrary_readingpositions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_readingpositions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryReadingpositionsGetArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/mylibrary/readingpositions/{volumeId}
/// Retrieves my reading position information for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_readingpositions_get_builder()` + `books_mylibrary_readingpositions_get_execute()`.
/// For task-level control, use `books_mylibrary_readingpositions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_get(
    client: &SimpleHttpClient,
    args: &BooksMylibraryReadingpositionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReadingPosition>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_mylibrary_readingpositions_get_builder(
        client,
        &args.volumeId,
        &args.contentVersion,
        &args.source,
    )?;
    books_mylibrary_readingpositions_get_execute(builder)
}

/// POST books/v1/mylibrary/readingpositions/{volumeId}/setPosition
/// Sets my reading position information for a volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_mylibrary_readingpositions_set_position_execute()` to send, or `books_mylibrary_readingpositions_set_position` for simplest API.

pub fn books_mylibrary_readingpositions_set_position_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    action: &Option<Option<String>>,
    contentVersion: &Option<Option<String>>,
    deviceCookie: &Option<Option<String>>,
    position: &Option<Option<String>>,
    source: &Option<Option<String>>,
    timestamp: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/readingpositions/{}/setPosition",
        volumeId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = action.as_ref() {
        query_parts.push(format!("action={}", val));
    }
    if let Some(val) = contentVersion.as_ref() {
        query_parts.push(format!("contentVersion={}", val));
    }
    if let Some(val) = deviceCookie.as_ref() {
        query_parts.push(format!("deviceCookie={}", val));
    }
    if let Some(val) = position.as_ref() {
        query_parts.push(format!("position={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = timestamp.as_ref() {
        query_parts.push(format!("timestamp={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/mylibrary/readingpositions/{volumeId}/setPosition
/// Sets my reading position information for a volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_mylibrary_readingpositions_set_position_execute()` or `books_mylibrary_readingpositions_set_position`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_readingpositions_set_position_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_set_position_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/mylibrary/readingpositions/{volumeId}/setPosition
/// Sets my reading position information for a volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_mylibrary_readingpositions_set_position_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_mylibrary_readingpositions_set_position_task()`.
/// For the simplest API, use `books_mylibrary_readingpositions_set_position()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_mylibrary_readingpositions_set_position_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_mylibrary_readingpositions_set_position_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_mylibrary_readingpositions_set_position_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_mylibrary_readingpositions_set_position`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryReadingpositionsSetPositionArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Query parameter: action
    pub action: Option<Option<String>>,
    /// Query parameter: contentVersion
    pub contentVersion: Option<Option<String>>,
    /// Query parameter: deviceCookie
    pub deviceCookie: Option<Option<String>>,
    /// Query parameter: position
    pub position: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: timestamp
    pub timestamp: Option<Option<String>>,
}

/// POST books/v1/mylibrary/readingpositions/{volumeId}/setPosition
/// Sets my reading position information for a volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_mylibrary_readingpositions_set_position_builder()` + `books_mylibrary_readingpositions_set_position_execute()`.
/// For task-level control, use `books_mylibrary_readingpositions_set_position_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_mylibrary_readingpositions_set_position(
    client: &SimpleHttpClient,
    args: &BooksMylibraryReadingpositionsSetPositionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_mylibrary_readingpositions_set_position_builder(
        client,
        &args.volumeId,
        &args.action,
        &args.contentVersion,
        &args.deviceCookie,
        &args.position,
        &args.source,
        &args.timestamp,
    )?;
    books_mylibrary_readingpositions_set_position_execute(builder)
}

/// GET books/v1/notification/get
/// Returns notification details for a given notification id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_notification_get_execute()` to send, or `books_notification_get` for simplest API.

pub fn books_notification_get_builder<R>(
    client: &SimpleHttpClient<R>,
    locale: &Option<Option<String>>,
    notification_id: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/notification/get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = notification_id.as_ref() {
        query_parts.push(format!("notification_id={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/notification/get
/// Returns notification details for a given notification id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_notification_get_execute()` or `books_notification_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_notification_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_notification_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Notification>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Notification = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/notification/get
/// Returns notification details for a given notification id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_notification_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_notification_get_task()`.
/// For the simplest API, use `books_notification_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_notification_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_notification_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Notification>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_notification_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_notification_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksNotificationGetArgs {
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: notification_id
    pub notification_id: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/notification/get
/// Returns notification details for a given notification id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_notification_get_builder()` + `books_notification_get_execute()`.
/// For task-level control, use `books_notification_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_notification_get(
    client: &SimpleHttpClient,
    args: &BooksNotificationGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Notification>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        books_notification_get_builder(client, &args.locale, &args.notification_id, &args.source)?;
    books_notification_get_execute(builder)
}

/// GET books/v1/onboarding/listCategories
/// List categories for onboarding experience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_onboarding_list_categories_execute()` to send, or `books_onboarding_list_categories` for simplest API.

pub fn books_onboarding_list_categories_builder<R>(
    client: &SimpleHttpClient<R>,
    locale: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/onboarding/listCategories",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/onboarding/listCategories
/// List categories for onboarding experience.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_onboarding_list_categories_execute()` or `books_onboarding_list_categories`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_onboarding_list_categories_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_categories_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Category>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Category = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/onboarding/listCategories
/// List categories for onboarding experience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_onboarding_list_categories_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_onboarding_list_categories_task()`.
/// For the simplest API, use `books_onboarding_list_categories()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_onboarding_list_categories_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_onboarding_list_categories_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Category>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_onboarding_list_categories_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_onboarding_list_categories`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksOnboardingListCategoriesArgs {
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
}

/// GET books/v1/onboarding/listCategories
/// List categories for onboarding experience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_onboarding_list_categories_builder()` + `books_onboarding_list_categories_execute()`.
/// For task-level control, use `books_onboarding_list_categories_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_categories(
    client: &SimpleHttpClient,
    args: &BooksOnboardingListCategoriesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Category>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_onboarding_list_categories_builder(client, &args.locale)?;
    books_onboarding_list_categories_execute(builder)
}

/// GET books/v1/onboarding/listCategoryVolumes
/// List available volumes under categories for onboarding experience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_onboarding_list_category_volumes_execute()` to send, or `books_onboarding_list_category_volumes` for simplest API.

pub fn books_onboarding_list_category_volumes_builder<R>(
    client: &SimpleHttpClient<R>,
    categoryId: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    maxAllowedMaturityRating: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/onboarding/listCategoryVolumes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = categoryId.as_ref() {
        query_parts.push(format!("categoryId={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxAllowedMaturityRating.as_ref() {
        query_parts.push(format!("maxAllowedMaturityRating={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/onboarding/listCategoryVolumes
/// List available volumes under categories for onboarding experience.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_onboarding_list_category_volumes_execute()` or `books_onboarding_list_category_volumes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_onboarding_list_category_volumes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_category_volumes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume2>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/onboarding/listCategoryVolumes
/// List available volumes under categories for onboarding experience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_onboarding_list_category_volumes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_onboarding_list_category_volumes_task()`.
/// For the simplest API, use `books_onboarding_list_category_volumes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_onboarding_list_category_volumes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_onboarding_list_category_volumes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume2>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_onboarding_list_category_volumes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_onboarding_list_category_volumes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksOnboardingListCategoryVolumesArgs {
    /// Query parameter: categoryId
    pub categoryId: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxAllowedMaturityRating
    pub maxAllowedMaturityRating: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET books/v1/onboarding/listCategoryVolumes
/// List available volumes under categories for onboarding experience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_onboarding_list_category_volumes_builder()` + `books_onboarding_list_category_volumes_execute()`.
/// For task-level control, use `books_onboarding_list_category_volumes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_onboarding_list_category_volumes(
    client: &SimpleHttpClient,
    args: &BooksOnboardingListCategoryVolumesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume2>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_onboarding_list_category_volumes_builder(
        client,
        &args.categoryId,
        &args.locale,
        &args.maxAllowedMaturityRating,
        &args.pageSize,
        &args.pageToken,
    )?;
    books_onboarding_list_category_volumes_execute(builder)
}

/// GET books/v1/personalizedstream/get
/// Returns a stream of personalized book clusters
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_personalizedstream_get_execute()` to send, or `books_personalizedstream_get` for simplest API.

pub fn books_personalizedstream_get_builder<R>(
    client: &SimpleHttpClient<R>,
    locale: &Option<Option<String>>,
    maxAllowedMaturityRating: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/personalizedstream/get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxAllowedMaturityRating.as_ref() {
        query_parts.push(format!("maxAllowedMaturityRating={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/personalizedstream/get
/// Returns a stream of personalized book clusters
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_personalizedstream_get_execute()` or `books_personalizedstream_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_personalizedstream_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_personalizedstream_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Discoveryclusters>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Discoveryclusters = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/personalizedstream/get
/// Returns a stream of personalized book clusters
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_personalizedstream_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_personalizedstream_get_task()`.
/// For the simplest API, use `books_personalizedstream_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_personalizedstream_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_personalizedstream_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discoveryclusters>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_personalizedstream_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_personalizedstream_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksPersonalizedstreamGetArgs {
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxAllowedMaturityRating
    pub maxAllowedMaturityRating: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/personalizedstream/get
/// Returns a stream of personalized book clusters
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_personalizedstream_get_builder()` + `books_personalizedstream_get_execute()`.
/// For task-level control, use `books_personalizedstream_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_personalizedstream_get(
    client: &SimpleHttpClient,
    args: &BooksPersonalizedstreamGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Discoveryclusters>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_personalizedstream_get_builder(
        client,
        &args.locale,
        &args.maxAllowedMaturityRating,
        &args.source,
    )?;
    books_personalizedstream_get_execute(builder)
}

/// POST books/v1/promooffer/accept
/// Accepts the promo offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_promooffer_accept_execute()` to send, or `books_promooffer_accept` for simplest API.

pub fn books_promooffer_accept_builder<R>(
    client: &SimpleHttpClient<R>,
    androidId: &Option<Option<String>>,
    device: &Option<Option<String>>,
    manufacturer: &Option<Option<String>>,
    model: &Option<Option<String>>,
    offerId: &Option<Option<String>>,
    product: &Option<Option<String>>,
    serial: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/promooffer/accept",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = androidId.as_ref() {
        query_parts.push(format!("androidId={}", val));
    }
    if let Some(val) = device.as_ref() {
        query_parts.push(format!("device={}", val));
    }
    if let Some(val) = manufacturer.as_ref() {
        query_parts.push(format!("manufacturer={}", val));
    }
    if let Some(val) = model.as_ref() {
        query_parts.push(format!("model={}", val));
    }
    if let Some(val) = offerId.as_ref() {
        query_parts.push(format!("offerId={}", val));
    }
    if let Some(val) = product.as_ref() {
        query_parts.push(format!("product={}", val));
    }
    if let Some(val) = serial.as_ref() {
        query_parts.push(format!("serial={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/promooffer/accept
/// Accepts the promo offer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_promooffer_accept_execute()` or `books_promooffer_accept`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_promooffer_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_accept_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/promooffer/accept
/// Accepts the promo offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_promooffer_accept_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_promooffer_accept_task()`.
/// For the simplest API, use `books_promooffer_accept()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_promooffer_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_promooffer_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_promooffer_accept_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_promooffer_accept`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksPromoofferAcceptArgs {
    /// Query parameter: androidId
    pub androidId: Option<Option<String>>,
    /// Query parameter: device
    pub device: Option<Option<String>>,
    /// Query parameter: manufacturer
    pub manufacturer: Option<Option<String>>,
    /// Query parameter: model
    pub model: Option<Option<String>>,
    /// Query parameter: offerId
    pub offerId: Option<Option<String>>,
    /// Query parameter: product
    pub product: Option<Option<String>>,
    /// Query parameter: serial
    pub serial: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/promooffer/accept
/// Accepts the promo offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_promooffer_accept_builder()` + `books_promooffer_accept_execute()`.
/// For task-level control, use `books_promooffer_accept_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_accept(
    client: &SimpleHttpClient,
    args: &BooksPromoofferAcceptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_promooffer_accept_builder(
        client,
        &args.androidId,
        &args.device,
        &args.manufacturer,
        &args.model,
        &args.offerId,
        &args.product,
        &args.serial,
        &args.volumeId,
    )?;
    books_promooffer_accept_execute(builder)
}

/// POST books/v1/promooffer/dismiss
/// Marks the promo offer as dismissed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_promooffer_dismiss_execute()` to send, or `books_promooffer_dismiss` for simplest API.

pub fn books_promooffer_dismiss_builder<R>(
    client: &SimpleHttpClient<R>,
    androidId: &Option<Option<String>>,
    device: &Option<Option<String>>,
    manufacturer: &Option<Option<String>>,
    model: &Option<Option<String>>,
    offerId: &Option<Option<String>>,
    product: &Option<Option<String>>,
    serial: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/promooffer/dismiss",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = androidId.as_ref() {
        query_parts.push(format!("androidId={}", val));
    }
    if let Some(val) = device.as_ref() {
        query_parts.push(format!("device={}", val));
    }
    if let Some(val) = manufacturer.as_ref() {
        query_parts.push(format!("manufacturer={}", val));
    }
    if let Some(val) = model.as_ref() {
        query_parts.push(format!("model={}", val));
    }
    if let Some(val) = offerId.as_ref() {
        query_parts.push(format!("offerId={}", val));
    }
    if let Some(val) = product.as_ref() {
        query_parts.push(format!("product={}", val));
    }
    if let Some(val) = serial.as_ref() {
        query_parts.push(format!("serial={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/promooffer/dismiss
/// Marks the promo offer as dismissed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_promooffer_dismiss_execute()` or `books_promooffer_dismiss`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_promooffer_dismiss_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_dismiss_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/promooffer/dismiss
/// Marks the promo offer as dismissed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_promooffer_dismiss_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_promooffer_dismiss_task()`.
/// For the simplest API, use `books_promooffer_dismiss()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_promooffer_dismiss_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_promooffer_dismiss_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_promooffer_dismiss_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_promooffer_dismiss`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksPromoofferDismissArgs {
    /// Query parameter: androidId
    pub androidId: Option<Option<String>>,
    /// Query parameter: device
    pub device: Option<Option<String>>,
    /// Query parameter: manufacturer
    pub manufacturer: Option<Option<String>>,
    /// Query parameter: model
    pub model: Option<Option<String>>,
    /// Query parameter: offerId
    pub offerId: Option<Option<String>>,
    /// Query parameter: product
    pub product: Option<Option<String>>,
    /// Query parameter: serial
    pub serial: Option<Option<String>>,
}

/// POST books/v1/promooffer/dismiss
/// Marks the promo offer as dismissed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_promooffer_dismiss_builder()` + `books_promooffer_dismiss_execute()`.
/// For task-level control, use `books_promooffer_dismiss_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_dismiss(
    client: &SimpleHttpClient,
    args: &BooksPromoofferDismissArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_promooffer_dismiss_builder(
        client,
        &args.androidId,
        &args.device,
        &args.manufacturer,
        &args.model,
        &args.offerId,
        &args.product,
        &args.serial,
    )?;
    books_promooffer_dismiss_execute(builder)
}

/// GET books/v1/promooffer/get
/// Returns a list of promo offers available to the user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_promooffer_get_execute()` to send, or `books_promooffer_get` for simplest API.

pub fn books_promooffer_get_builder<R>(
    client: &SimpleHttpClient<R>,
    androidId: &Option<Option<String>>,
    device: &Option<Option<String>>,
    manufacturer: &Option<Option<String>>,
    model: &Option<Option<String>>,
    product: &Option<Option<String>>,
    serial: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/promooffer/get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = androidId.as_ref() {
        query_parts.push(format!("androidId={}", val));
    }
    if let Some(val) = device.as_ref() {
        query_parts.push(format!("device={}", val));
    }
    if let Some(val) = manufacturer.as_ref() {
        query_parts.push(format!("manufacturer={}", val));
    }
    if let Some(val) = model.as_ref() {
        query_parts.push(format!("model={}", val));
    }
    if let Some(val) = product.as_ref() {
        query_parts.push(format!("product={}", val));
    }
    if let Some(val) = serial.as_ref() {
        query_parts.push(format!("serial={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/promooffer/get
/// Returns a list of promo offers available to the user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_promooffer_get_execute()` or `books_promooffer_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_promooffer_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Offers>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Offers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/promooffer/get
/// Returns a list of promo offers available to the user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_promooffer_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_promooffer_get_task()`.
/// For the simplest API, use `books_promooffer_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_promooffer_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_promooffer_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Offers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_promooffer_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_promooffer_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksPromoofferGetArgs {
    /// Query parameter: androidId
    pub androidId: Option<Option<String>>,
    /// Query parameter: device
    pub device: Option<Option<String>>,
    /// Query parameter: manufacturer
    pub manufacturer: Option<Option<String>>,
    /// Query parameter: model
    pub model: Option<Option<String>>,
    /// Query parameter: product
    pub product: Option<Option<String>>,
    /// Query parameter: serial
    pub serial: Option<Option<String>>,
}

/// GET books/v1/promooffer/get
/// Returns a list of promo offers available to the user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_promooffer_get_builder()` + `books_promooffer_get_execute()`.
/// For task-level control, use `books_promooffer_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_promooffer_get(
    client: &SimpleHttpClient,
    args: &BooksPromoofferGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Offers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_promooffer_get_builder(
        client,
        &args.androidId,
        &args.device,
        &args.manufacturer,
        &args.model,
        &args.product,
        &args.serial,
    )?;
    books_promooffer_get_execute(builder)
}

/// GET books/v1/series/get
/// Returns Series metadata for the given series ids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_series_get_execute()` to send, or `books_series_get` for simplest API.

pub fn books_series_get_builder<R>(
    client: &SimpleHttpClient<R>,
    series_id: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/series/get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = series_id.as_ref() {
        query_parts.push(format!("series_id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/series/get
/// Returns Series metadata for the given series ids.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_series_get_execute()` or `books_series_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_series_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Series>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Series = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/series/get
/// Returns Series metadata for the given series ids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_series_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_series_get_task()`.
/// For the simplest API, use `books_series_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_series_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_series_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Series>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_series_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_series_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksSeriesGetArgs {
    /// Query parameter: series_id
    pub series_id: Option<Option<String>>,
}

/// GET books/v1/series/get
/// Returns Series metadata for the given series ids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_series_get_builder()` + `books_series_get_execute()`.
/// For task-level control, use `books_series_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_get(
    client: &SimpleHttpClient,
    args: &BooksSeriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Series>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_series_get_builder(client, &args.series_id)?;
    books_series_get_execute(builder)
}

/// GET books/v1/series/membership/get
/// Returns Series membership data given the series id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_series_membership_get_execute()` to send, or `books_series_membership_get` for simplest API.

pub fn books_series_membership_get_builder<R>(
    client: &SimpleHttpClient<R>,
    page_size: &Option<Option<String>>,
    page_token: &Option<Option<String>>,
    series_id: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/series/membership/get",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page_size.as_ref() {
        query_parts.push(format!("page_size={}", val));
    }
    if let Some(val) = page_token.as_ref() {
        query_parts.push(format!("page_token={}", val));
    }
    if let Some(val) = series_id.as_ref() {
        query_parts.push(format!("series_id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/series/membership/get
/// Returns Series membership data given the series id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_series_membership_get_execute()` or `books_series_membership_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_series_membership_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_membership_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Seriesmembership>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Seriesmembership = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/series/membership/get
/// Returns Series membership data given the series id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_series_membership_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_series_membership_get_task()`.
/// For the simplest API, use `books_series_membership_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_series_membership_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_series_membership_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Seriesmembership>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = books_series_membership_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_series_membership_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksSeriesMembershipGetArgs {
    /// Query parameter: page_size
    pub page_size: Option<Option<String>>,
    /// Query parameter: page_token
    pub page_token: Option<Option<String>>,
    /// Query parameter: series_id
    pub series_id: Option<Option<String>>,
}

/// GET books/v1/series/membership/get
/// Returns Series membership data given the series id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_series_membership_get_builder()` + `books_series_membership_get_execute()`.
/// For task-level control, use `books_series_membership_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_series_membership_get(
    client: &SimpleHttpClient,
    args: &BooksSeriesMembershipGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Seriesmembership>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = books_series_membership_get_builder(
        client,
        &args.page_size,
        &args.page_token,
        &args.series_id,
    )?;
    books_series_membership_get_execute(builder)
}

/// GET books/v1/volumes/{volumeId}
/// Gets volume information for a single volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_get_execute()` to send, or `books_volumes_get` for simplest API.

pub fn books_volumes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    country: &Option<Option<String>>,
    includeNonComicsSeries: &Option<Option<String>>,
    partner: &Option<Option<String>>,
    projection: &Option<Option<String>>,
    source: &Option<Option<String>>,
    user_library_consistent_read: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/{}", volumeId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = country.as_ref() {
        query_parts.push(format!("country={}", val));
    }
    if let Some(val) = includeNonComicsSeries.as_ref() {
        query_parts.push(format!("includeNonComicsSeries={}", val));
    }
    if let Some(val) = partner.as_ref() {
        query_parts.push(format!("partner={}", val));
    }
    if let Some(val) = projection.as_ref() {
        query_parts.push(format!("projection={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = user_library_consistent_read.as_ref() {
        query_parts.push(format!("user_library_consistent_read={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}
/// Gets volume information for a single volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_volumes_get_execute()` or `books_volumes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}
/// Gets volume information for a single volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_volumes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_volumes_get_task()`.
/// For the simplest API, use `books_volumes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_volumes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_volumes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_volumes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksVolumesGetArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Query parameter: country
    pub country: Option<Option<String>>,
    /// Query parameter: includeNonComicsSeries
    pub includeNonComicsSeries: Option<Option<String>>,
    /// Query parameter: partner
    pub partner: Option<Option<String>>,
    /// Query parameter: projection
    pub projection: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: user_library_consistent_read
    pub user_library_consistent_read: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}
/// Gets volume information for a single volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_get_builder()` + `books_volumes_get_execute()`.
/// For task-level control, use `books_volumes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_get(
    client: &SimpleHttpClient,
    args: &BooksVolumesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_get_builder(
        client,
        &args.volumeId,
        &args.country,
        &args.includeNonComicsSeries,
        &args.partner,
        &args.projection,
        &args.source,
        &args.user_library_consistent_read,
    )?;
    books_volumes_get_execute(builder)
}

/// GET books/v1/volumes
/// Performs a book search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_list_execute()` to send, or `books_volumes_list` for simplest API.

pub fn books_volumes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    download: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    langRestrict: &Option<Option<String>>,
    libraryRestrict: &Option<Option<String>>,
    maxAllowedMaturityRating: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    partner: &Option<Option<String>>,
    printType: &Option<Option<String>>,
    projection: &Option<Option<String>>,
    q: &Option<Option<String>>,
    showPreorders: &Option<Option<String>>,
    source: &Option<Option<String>>,
    startIndex: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = download.as_ref() {
        query_parts.push(format!("download={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = langRestrict.as_ref() {
        query_parts.push(format!("langRestrict={}", val));
    }
    if let Some(val) = libraryRestrict.as_ref() {
        query_parts.push(format!("libraryRestrict={}", val));
    }
    if let Some(val) = maxAllowedMaturityRating.as_ref() {
        query_parts.push(format!("maxAllowedMaturityRating={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = partner.as_ref() {
        query_parts.push(format!("partner={}", val));
    }
    if let Some(val) = printType.as_ref() {
        query_parts.push(format!("printType={}", val));
    }
    if let Some(val) = projection.as_ref() {
        query_parts.push(format!("projection={}", val));
    }
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = showPreorders.as_ref() {
        query_parts.push(format!("showPreorders={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = startIndex.as_ref() {
        query_parts.push(format!("startIndex={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes
/// Performs a book search.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_volumes_list_execute()` or `books_volumes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes
/// Performs a book search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_volumes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_volumes_list_task()`.
/// For the simplest API, use `books_volumes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_volumes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_volumes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksVolumesListArgs {
    /// Query parameter: download
    pub download: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: langRestrict
    pub langRestrict: Option<Option<String>>,
    /// Query parameter: libraryRestrict
    pub libraryRestrict: Option<Option<String>>,
    /// Query parameter: maxAllowedMaturityRating
    pub maxAllowedMaturityRating: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: partner
    pub partner: Option<Option<String>>,
    /// Query parameter: printType
    pub printType: Option<Option<String>>,
    /// Query parameter: projection
    pub projection: Option<Option<String>>,
    /// Query parameter: q
    pub q: Option<Option<String>>,
    /// Query parameter: showPreorders
    pub showPreorders: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: startIndex
    pub startIndex: Option<Option<String>>,
}

/// GET books/v1/volumes
/// Performs a book search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_list_builder()` + `books_volumes_list_execute()`.
/// For task-level control, use `books_volumes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_list(
    client: &SimpleHttpClient,
    args: &BooksVolumesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_list_builder(
        client,
        &args.download,
        &args.filter,
        &args.langRestrict,
        &args.libraryRestrict,
        &args.maxAllowedMaturityRating,
        &args.maxResults,
        &args.orderBy,
        &args.partner,
        &args.printType,
        &args.projection,
        &args.q,
        &args.showPreorders,
        &args.source,
        &args.startIndex,
    )?;
    books_volumes_list_execute(builder)
}

/// GET books/v1/volumes/{volumeId}/associated
/// Return a list of associated books.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_associated_list_execute()` to send, or `books_volumes_associated_list` for simplest API.

pub fn books_volumes_associated_list_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeId: &String,
    association: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    maxAllowedMaturityRating: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/associated",
        volumeId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = association.as_ref() {
        query_parts.push(format!("association={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxAllowedMaturityRating.as_ref() {
        query_parts.push(format!("maxAllowedMaturityRating={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/{volumeId}/associated
/// Return a list of associated books.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_volumes_associated_list_execute()` or `books_volumes_associated_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_associated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_associated_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/{volumeId}/associated
/// Return a list of associated books.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_volumes_associated_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_volumes_associated_list_task()`.
/// For the simplest API, use `books_volumes_associated_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_associated_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_volumes_associated_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_volumes_associated_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_volumes_associated_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksVolumesAssociatedListArgs {
    /// Path parameter: volumeId
    pub volumeId: String,
    /// Query parameter: association
    pub association: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxAllowedMaturityRating
    pub maxAllowedMaturityRating: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/volumes/{volumeId}/associated
/// Return a list of associated books.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_associated_list_builder()` + `books_volumes_associated_list_execute()`.
/// For task-level control, use `books_volumes_associated_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_associated_list(
    client: &SimpleHttpClient,
    args: &BooksVolumesAssociatedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_associated_list_builder(
        client,
        &args.volumeId,
        &args.association,
        &args.locale,
        &args.maxAllowedMaturityRating,
        &args.source,
    )?;
    books_volumes_associated_list_execute(builder)
}

/// GET books/v1/volumes/mybooks
/// Return a list of books in My Library.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_mybooks_list_execute()` to send, or `books_volumes_mybooks_list` for simplest API.

pub fn books_volumes_mybooks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    acquireMethod: &Option<Option<String>>,
    country: &Option<Option<String>>,
    locale: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    processingState: &Option<Option<String>>,
    source: &Option<Option<String>>,
    startIndex: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/mybooks",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = acquireMethod.as_ref() {
        query_parts.push(format!("acquireMethod={}", val));
    }
    if let Some(val) = country.as_ref() {
        query_parts.push(format!("country={}", val));
    }
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = processingState.as_ref() {
        query_parts.push(format!("processingState={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = startIndex.as_ref() {
        query_parts.push(format!("startIndex={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/mybooks
/// Return a list of books in My Library.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_volumes_mybooks_list_execute()` or `books_volumes_mybooks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_mybooks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_mybooks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/mybooks
/// Return a list of books in My Library.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_volumes_mybooks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_volumes_mybooks_list_task()`.
/// For the simplest API, use `books_volumes_mybooks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_mybooks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_volumes_mybooks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_volumes_mybooks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_volumes_mybooks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksVolumesMybooksListArgs {
    /// Query parameter: acquireMethod
    pub acquireMethod: Option<Option<String>>,
    /// Query parameter: country
    pub country: Option<Option<String>>,
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: processingState
    pub processingState: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: startIndex
    pub startIndex: Option<Option<String>>,
}

/// GET books/v1/volumes/mybooks
/// Return a list of books in My Library.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_mybooks_list_builder()` + `books_volumes_mybooks_list_execute()`.
/// For task-level control, use `books_volumes_mybooks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_mybooks_list(
    client: &SimpleHttpClient,
    args: &BooksVolumesMybooksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_mybooks_list_builder(
        client,
        &args.acquireMethod,
        &args.country,
        &args.locale,
        &args.maxResults,
        &args.processingState,
        &args.source,
        &args.startIndex,
    )?;
    books_volumes_mybooks_list_execute(builder)
}

/// GET books/v1/volumes/recommended
/// Return a list of recommended books for the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_recommended_list_execute()` to send, or `books_volumes_recommended_list` for simplest API.

pub fn books_volumes_recommended_list_builder<R>(
    client: &SimpleHttpClient<R>,
    locale: &Option<Option<String>>,
    maxAllowedMaturityRating: &Option<Option<String>>,
    source: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/recommended",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxAllowedMaturityRating.as_ref() {
        query_parts.push(format!("maxAllowedMaturityRating={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/recommended
/// Return a list of recommended books for the current user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_volumes_recommended_list_execute()` or `books_volumes_recommended_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_recommended_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/recommended
/// Return a list of recommended books for the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_volumes_recommended_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_volumes_recommended_list_task()`.
/// For the simplest API, use `books_volumes_recommended_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_recommended_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_volumes_recommended_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_volumes_recommended_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_volumes_recommended_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksVolumesRecommendedListArgs {
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxAllowedMaturityRating
    pub maxAllowedMaturityRating: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
}

/// GET books/v1/volumes/recommended
/// Return a list of recommended books for the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_recommended_list_builder()` + `books_volumes_recommended_list_execute()`.
/// For task-level control, use `books_volumes_recommended_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_list(
    client: &SimpleHttpClient,
    args: &BooksVolumesRecommendedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_recommended_list_builder(
        client,
        &args.locale,
        &args.maxAllowedMaturityRating,
        &args.source,
    )?;
    books_volumes_recommended_list_execute(builder)
}

/// POST books/v1/volumes/recommended/rate
/// Rate a recommended book for the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_recommended_rate_execute()` to send, or `books_volumes_recommended_rate` for simplest API.

pub fn books_volumes_recommended_rate_builder<R>(
    client: &SimpleHttpClient<R>,
    locale: &Option<Option<String>>,
    rating: &Option<Option<String>>,
    source: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/recommended/rate",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = rating.as_ref() {
        query_parts.push(format!("rating={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST books/v1/volumes/recommended/rate
/// Rate a recommended book for the current user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_volumes_recommended_rate_execute()` or `books_volumes_recommended_rate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_recommended_rate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_rate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BooksVolumesRecommendedRateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BooksVolumesRecommendedRateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST books/v1/volumes/recommended/rate
/// Rate a recommended book for the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_volumes_recommended_rate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_volumes_recommended_rate_task()`.
/// For the simplest API, use `books_volumes_recommended_rate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_recommended_rate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_volumes_recommended_rate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BooksVolumesRecommendedRateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = books_volumes_recommended_rate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_volumes_recommended_rate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksVolumesRecommendedRateArgs {
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: rating
    pub rating: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// POST books/v1/volumes/recommended/rate
/// Rate a recommended book for the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_recommended_rate_builder()` + `books_volumes_recommended_rate_execute()`.
/// For task-level control, use `books_volumes_recommended_rate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_recommended_rate(
    client: &SimpleHttpClient,
    args: &BooksVolumesRecommendedRateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BooksVolumesRecommendedRateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = books_volumes_recommended_rate_builder(
        client,
        &args.locale,
        &args.rating,
        &args.source,
        &args.volumeId,
    )?;
    books_volumes_recommended_rate_execute(builder)
}

/// GET books/v1/volumes/useruploaded
/// Return a list of books uploaded by the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `books_volumes_useruploaded_list_execute()` to send, or `books_volumes_useruploaded_list` for simplest API.

pub fn books_volumes_useruploaded_list_builder<R>(
    client: &SimpleHttpClient<R>,
    locale: &Option<Option<String>>,
    maxResults: &Option<Option<String>>,
    processingState: &Option<Option<String>>,
    source: &Option<Option<String>>,
    startIndex: &Option<Option<String>>,
    volumeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/useruploaded",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = locale.as_ref() {
        query_parts.push(format!("locale={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = processingState.as_ref() {
        query_parts.push(format!("processingState={}", val));
    }
    if let Some(val) = source.as_ref() {
        query_parts.push(format!("source={}", val));
    }
    if let Some(val) = startIndex.as_ref() {
        query_parts.push(format!("startIndex={}", val));
    }
    if let Some(val) = volumeId.as_ref() {
        query_parts.push(format!("volumeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET books/v1/volumes/useruploaded
/// Return a list of books uploaded by the current user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `books_volumes_useruploaded_list_execute()` or `books_volumes_useruploaded_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_useruploaded_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_useruploaded_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volumes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET books/v1/volumes/useruploaded
/// Return a list of books uploaded by the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `books_volumes_useruploaded_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `books_volumes_useruploaded_list_task()`.
/// For the simplest API, use `books_volumes_useruploaded_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `books_volumes_useruploaded_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn books_volumes_useruploaded_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = books_volumes_useruploaded_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`books_volumes_useruploaded_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksVolumesUseruploadedListArgs {
    /// Query parameter: locale
    pub locale: Option<Option<String>>,
    /// Query parameter: maxResults
    pub maxResults: Option<Option<String>>,
    /// Query parameter: processingState
    pub processingState: Option<Option<String>>,
    /// Query parameter: source
    pub source: Option<Option<String>>,
    /// Query parameter: startIndex
    pub startIndex: Option<Option<String>>,
    /// Query parameter: volumeId
    pub volumeId: Option<Option<String>>,
}

/// GET books/v1/volumes/useruploaded
/// Return a list of books uploaded by the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `books_volumes_useruploaded_list_builder()` + `books_volumes_useruploaded_list_execute()`.
/// For task-level control, use `books_volumes_useruploaded_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn books_volumes_useruploaded_list(
    client: &SimpleHttpClient,
    args: &BooksVolumesUseruploadedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volumes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = books_volumes_useruploaded_list_builder(
        client,
        &args.locale,
        &args.maxResults,
        &args.processingState,
        &args.source,
        &args.startIndex,
        &args.volumeId,
    )?;
    books_volumes_useruploaded_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Bookshelf
// =============================================================================

/// ResourceIdentifier implementation for Bookshelf with BooksBookshelvesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksBookshelvesGetArgs> for Bookshelf {
    fn generate_resource_id(&self, input: &BooksBookshelvesGetArgs) -> String {
        format!("gcp::books::Bookshelf/{}/{}", input.userId, input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Bookshelf"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Bookshelves
// =============================================================================

/// ResourceIdentifier implementation for Bookshelves with BooksBookshelvesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksBookshelvesListArgs> for Bookshelves {
    fn generate_resource_id(&self, input: &BooksBookshelvesListArgs) -> String {
        format!("gcp::books::Bookshelves/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Bookshelves"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksBookshelvesVolumesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksBookshelvesVolumesListArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksBookshelvesVolumesListArgs) -> String {
        format!("gcp::books::Volumes/{}/{}", input.userId, input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BooksCloudloadingResource
// =============================================================================

/// ResourceIdentifier implementation for BooksCloudloadingResource with BooksCloudloadingAddBookArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksCloudloadingAddBookArgs> for BooksCloudloadingResource {
    fn generate_resource_id(&self, input: &BooksCloudloadingAddBookArgs) -> String {
        "gcp::books::BooksCloudloadingResource".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::BooksCloudloadingResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksCloudloadingDeleteBookArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksCloudloadingDeleteBookArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksCloudloadingDeleteBookArgs) -> String {
        "gcp::books::Empty".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BooksCloudloadingResource
// =============================================================================

/// ResourceIdentifier implementation for BooksCloudloadingResource with BooksCloudloadingUpdateBookArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksCloudloadingUpdateBookArgs> for BooksCloudloadingResource {
    fn generate_resource_id(&self, input: &BooksCloudloadingUpdateBookArgs) -> String {
        "gcp::books::BooksCloudloadingResource".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::BooksCloudloadingResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Metadata
// =============================================================================

/// ResourceIdentifier implementation for Metadata with BooksDictionaryListOfflineMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksDictionaryListOfflineMetadataArgs> for Metadata {
    fn generate_resource_id(&self, input: &BooksDictionaryListOfflineMetadataArgs) -> String {
        "gcp::books::Metadata".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Metadata"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FamilyInfo
// =============================================================================

/// ResourceIdentifier implementation for FamilyInfo with BooksFamilysharingGetFamilyInfoArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksFamilysharingGetFamilyInfoArgs> for FamilyInfo {
    fn generate_resource_id(&self, input: &BooksFamilysharingGetFamilyInfoArgs) -> String {
        "gcp::books::FamilyInfo".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::FamilyInfo"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksFamilysharingShareArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksFamilysharingShareArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksFamilysharingShareArgs) -> String {
        "gcp::books::Empty".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksFamilysharingUnshareArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksFamilysharingUnshareArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksFamilysharingUnshareArgs) -> String {
        "gcp::books::Empty".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Layersummary
// =============================================================================

/// ResourceIdentifier implementation for Layersummary with BooksLayersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksLayersGetArgs> for Layersummary {
    fn generate_resource_id(&self, input: &BooksLayersGetArgs) -> String {
        format!(
            "gcp::books::Layersummary/{}/{}",
            input.volumeId, input.summaryId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Layersummary"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Layersummaries
// =============================================================================

/// ResourceIdentifier implementation for Layersummaries with BooksLayersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksLayersListArgs> for Layersummaries {
    fn generate_resource_id(&self, input: &BooksLayersListArgs) -> String {
        format!("gcp::books::Layersummaries/{}", input.volumeId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Layersummaries"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DictionaryAnnotationdata
// =============================================================================

/// ResourceIdentifier implementation for DictionaryAnnotationdata with BooksLayersAnnotationDataGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksLayersAnnotationDataGetArgs> for DictionaryAnnotationdata {
    fn generate_resource_id(&self, input: &BooksLayersAnnotationDataGetArgs) -> String {
        format!(
            "gcp::books::DictionaryAnnotationdata/{}/{}/{}",
            input.volumeId, input.layerId, input.annotationDataId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::DictionaryAnnotationdata"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Annotationsdata
// =============================================================================

/// ResourceIdentifier implementation for Annotationsdata with BooksLayersAnnotationDataListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksLayersAnnotationDataListArgs> for Annotationsdata {
    fn generate_resource_id(&self, input: &BooksLayersAnnotationDataListArgs) -> String {
        format!(
            "gcp::books::Annotationsdata/{}/{}",
            input.volumeId, input.layerId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Annotationsdata"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumeannotation
// =============================================================================

/// ResourceIdentifier implementation for Volumeannotation with BooksLayersVolumeAnnotationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksLayersVolumeAnnotationsGetArgs> for Volumeannotation {
    fn generate_resource_id(&self, input: &BooksLayersVolumeAnnotationsGetArgs) -> String {
        format!(
            "gcp::books::Volumeannotation/{}/{}/{}",
            input.volumeId, input.layerId, input.annotationId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumeannotation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumeannotations
// =============================================================================

/// ResourceIdentifier implementation for Volumeannotations with BooksLayersVolumeAnnotationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksLayersVolumeAnnotationsListArgs> for Volumeannotations {
    fn generate_resource_id(&self, input: &BooksLayersVolumeAnnotationsListArgs) -> String {
        format!(
            "gcp::books::Volumeannotations/{}/{}",
            input.volumeId, input.layerId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumeannotations"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Usersettings
// =============================================================================

/// ResourceIdentifier implementation for Usersettings with BooksMyconfigGetUserSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMyconfigGetUserSettingsArgs> for Usersettings {
    fn generate_resource_id(&self, input: &BooksMyconfigGetUserSettingsArgs) -> String {
        "gcp::books::Usersettings".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Usersettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DownloadAccesses
// =============================================================================

/// ResourceIdentifier implementation for DownloadAccesses with BooksMyconfigReleaseDownloadAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMyconfigReleaseDownloadAccessArgs> for DownloadAccesses {
    fn generate_resource_id(&self, input: &BooksMyconfigReleaseDownloadAccessArgs) -> String {
        "gcp::books::DownloadAccesses".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::DownloadAccesses"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RequestAccessData
// =============================================================================

/// ResourceIdentifier implementation for RequestAccessData with BooksMyconfigRequestAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMyconfigRequestAccessArgs> for RequestAccessData {
    fn generate_resource_id(&self, input: &BooksMyconfigRequestAccessArgs) -> String {
        "gcp::books::RequestAccessData".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::RequestAccessData"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksMyconfigSyncVolumeLicensesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMyconfigSyncVolumeLicensesArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksMyconfigSyncVolumeLicensesArgs) -> String {
        "gcp::books::Volumes".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Usersettings
// =============================================================================

/// ResourceIdentifier implementation for Usersettings with BooksMyconfigUpdateUserSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMyconfigUpdateUserSettingsArgs> for Usersettings {
    fn generate_resource_id(&self, input: &BooksMyconfigUpdateUserSettingsArgs) -> String {
        "gcp::books::Usersettings".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Usersettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksMylibraryAnnotationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryAnnotationsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksMylibraryAnnotationsDeleteArgs) -> String {
        format!("gcp::books::Empty/{}", input.annotationId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Annotation
// =============================================================================

/// ResourceIdentifier implementation for Annotation with BooksMylibraryAnnotationsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryAnnotationsInsertArgs> for Annotation {
    fn generate_resource_id(&self, input: &BooksMylibraryAnnotationsInsertArgs) -> String {
        "gcp::books::Annotation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Annotation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Annotations
// =============================================================================

/// ResourceIdentifier implementation for Annotations with BooksMylibraryAnnotationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryAnnotationsListArgs> for Annotations {
    fn generate_resource_id(&self, input: &BooksMylibraryAnnotationsListArgs) -> String {
        "gcp::books::Annotations".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Annotations"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AnnotationsSummary
// =============================================================================

/// ResourceIdentifier implementation for AnnotationsSummary with BooksMylibraryAnnotationsSummaryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryAnnotationsSummaryArgs> for AnnotationsSummary {
    fn generate_resource_id(&self, input: &BooksMylibraryAnnotationsSummaryArgs) -> String {
        "gcp::books::AnnotationsSummary".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::AnnotationsSummary"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Annotation
// =============================================================================

/// ResourceIdentifier implementation for Annotation with BooksMylibraryAnnotationsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryAnnotationsUpdateArgs> for Annotation {
    fn generate_resource_id(&self, input: &BooksMylibraryAnnotationsUpdateArgs) -> String {
        format!("gcp::books::Annotation/{}", input.annotationId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Annotation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksMylibraryBookshelvesAddVolumeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryBookshelvesAddVolumeArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksMylibraryBookshelvesAddVolumeArgs) -> String {
        format!("gcp::books::Empty/{}", input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksMylibraryBookshelvesClearVolumesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryBookshelvesClearVolumesArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksMylibraryBookshelvesClearVolumesArgs) -> String {
        format!("gcp::books::Empty/{}", input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Bookshelf
// =============================================================================

/// ResourceIdentifier implementation for Bookshelf with BooksMylibraryBookshelvesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryBookshelvesGetArgs> for Bookshelf {
    fn generate_resource_id(&self, input: &BooksMylibraryBookshelvesGetArgs) -> String {
        format!("gcp::books::Bookshelf/{}", input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Bookshelf"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Bookshelves
// =============================================================================

/// ResourceIdentifier implementation for Bookshelves with BooksMylibraryBookshelvesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryBookshelvesListArgs> for Bookshelves {
    fn generate_resource_id(&self, input: &BooksMylibraryBookshelvesListArgs) -> String {
        "gcp::books::Bookshelves".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Bookshelves"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksMylibraryBookshelvesMoveVolumeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryBookshelvesMoveVolumeArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksMylibraryBookshelvesMoveVolumeArgs) -> String {
        format!("gcp::books::Empty/{}", input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksMylibraryBookshelvesRemoveVolumeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryBookshelvesRemoveVolumeArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksMylibraryBookshelvesRemoveVolumeArgs) -> String {
        format!("gcp::books::Empty/{}", input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksMylibraryBookshelvesVolumesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryBookshelvesVolumesListArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksMylibraryBookshelvesVolumesListArgs) -> String {
        format!("gcp::books::Volumes/{}", input.shelf)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReadingPosition
// =============================================================================

/// ResourceIdentifier implementation for ReadingPosition with BooksMylibraryReadingpositionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryReadingpositionsGetArgs> for ReadingPosition {
    fn generate_resource_id(&self, input: &BooksMylibraryReadingpositionsGetArgs) -> String {
        format!("gcp::books::ReadingPosition/{}", input.volumeId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::ReadingPosition"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksMylibraryReadingpositionsSetPositionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksMylibraryReadingpositionsSetPositionArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &BooksMylibraryReadingpositionsSetPositionArgs,
    ) -> String {
        format!("gcp::books::Empty/{}", input.volumeId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Notification
// =============================================================================

/// ResourceIdentifier implementation for Notification with BooksNotificationGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksNotificationGetArgs> for Notification {
    fn generate_resource_id(&self, input: &BooksNotificationGetArgs) -> String {
        "gcp::books::Notification".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Notification"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Category
// =============================================================================

/// ResourceIdentifier implementation for Category with BooksOnboardingListCategoriesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksOnboardingListCategoriesArgs> for Category {
    fn generate_resource_id(&self, input: &BooksOnboardingListCategoriesArgs) -> String {
        "gcp::books::Category".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Category"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volume2
// =============================================================================

/// ResourceIdentifier implementation for Volume2 with BooksOnboardingListCategoryVolumesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksOnboardingListCategoryVolumesArgs> for Volume2 {
    fn generate_resource_id(&self, input: &BooksOnboardingListCategoryVolumesArgs) -> String {
        "gcp::books::Volume2".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volume2"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Discoveryclusters
// =============================================================================

/// ResourceIdentifier implementation for Discoveryclusters with BooksPersonalizedstreamGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksPersonalizedstreamGetArgs> for Discoveryclusters {
    fn generate_resource_id(&self, input: &BooksPersonalizedstreamGetArgs) -> String {
        "gcp::books::Discoveryclusters".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Discoveryclusters"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksPromoofferAcceptArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksPromoofferAcceptArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksPromoofferAcceptArgs) -> String {
        "gcp::books::Empty".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BooksPromoofferDismissArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksPromoofferDismissArgs> for Empty {
    fn generate_resource_id(&self, input: &BooksPromoofferDismissArgs) -> String {
        "gcp::books::Empty".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Offers
// =============================================================================

/// ResourceIdentifier implementation for Offers with BooksPromoofferGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksPromoofferGetArgs> for Offers {
    fn generate_resource_id(&self, input: &BooksPromoofferGetArgs) -> String {
        "gcp::books::Offers".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Offers"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Series
// =============================================================================

/// ResourceIdentifier implementation for Series with BooksSeriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksSeriesGetArgs> for Series {
    fn generate_resource_id(&self, input: &BooksSeriesGetArgs) -> String {
        "gcp::books::Series".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Series"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Seriesmembership
// =============================================================================

/// ResourceIdentifier implementation for Seriesmembership with BooksSeriesMembershipGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksSeriesMembershipGetArgs> for Seriesmembership {
    fn generate_resource_id(&self, input: &BooksSeriesMembershipGetArgs) -> String {
        "gcp::books::Seriesmembership".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Seriesmembership"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volume
// =============================================================================

/// ResourceIdentifier implementation for Volume with BooksVolumesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksVolumesGetArgs> for Volume {
    fn generate_resource_id(&self, input: &BooksVolumesGetArgs) -> String {
        format!("gcp::books::Volume/{}", input.volumeId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volume"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksVolumesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksVolumesListArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksVolumesListArgs) -> String {
        "gcp::books::Volumes".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksVolumesAssociatedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksVolumesAssociatedListArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksVolumesAssociatedListArgs) -> String {
        format!("gcp::books::Volumes/{}", input.volumeId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksVolumesMybooksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksVolumesMybooksListArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksVolumesMybooksListArgs) -> String {
        "gcp::books::Volumes".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksVolumesRecommendedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksVolumesRecommendedListArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksVolumesRecommendedListArgs) -> String {
        "gcp::books::Volumes".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BooksVolumesRecommendedRateResponse
// =============================================================================

/// ResourceIdentifier implementation for BooksVolumesRecommendedRateResponse with BooksVolumesRecommendedRateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksVolumesRecommendedRateArgs> for BooksVolumesRecommendedRateResponse {
    fn generate_resource_id(&self, input: &BooksVolumesRecommendedRateArgs) -> String {
        "gcp::books::BooksVolumesRecommendedRateResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::BooksVolumesRecommendedRateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volumes
// =============================================================================

/// ResourceIdentifier implementation for Volumes with BooksVolumesUseruploadedListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BooksVolumesUseruploadedListArgs> for Volumes {
    fn generate_resource_id(&self, input: &BooksVolumesUseruploadedListArgs) -> String {
        "gcp::books::Volumes".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::books::Volumes"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
