//! Auto-generated API clients for gcp/playgrouping.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}:verify
/// Verify an API token by asserting the app and persona it belongs to. The verification is a protection against client-side attacks and will fail if the contents of the token don't match the provided values. A token must be verified before it can be used to manipulate user tags.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playgrouping_apps_tokens_verify_execute()` to send, or `playgrouping_apps_tokens_verify` for simplest API.

pub fn playgrouping_apps_tokens_verify_builder(
    client: &SimpleHttpClient,
    appPackage: &String,
    token: &String,
    body: &VerifyTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://playgrouping.googleapis.com/v1alpha1/apps/{}/tokens/{}:verify",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}:verify
/// Verify an API token by asserting the app and persona it belongs to. The verification is a protection against client-side attacks and will fail if the contents of the token don't match the provided values. A token must be verified before it can be used to manipulate user tags.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playgrouping_apps_tokens_verify_execute()` or `playgrouping_apps_tokens_verify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playgrouping_apps_tokens_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_verify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}:verify
/// Verify an API token by asserting the app and persona it belongs to. The verification is a protection against client-side attacks and will fail if the contents of the token don't match the provided values. A token must be verified before it can be used to manipulate user tags.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playgrouping_apps_tokens_verify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playgrouping_apps_tokens_verify_task()`.
/// For the simplest API, use `playgrouping_apps_tokens_verify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playgrouping_apps_tokens_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playgrouping_apps_tokens_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = playgrouping_apps_tokens_verify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playgrouping_apps_tokens_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaygroupingAppsTokensVerifyArgs {
    /// Path parameter: appPackage
    pub appPackage: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: VerifyTokenRequest,
}

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}:verify
/// Verify an API token by asserting the app and persona it belongs to. The verification is a protection against client-side attacks and will fail if the contents of the token don't match the provided values. A token must be verified before it can be used to manipulate user tags.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playgrouping_apps_tokens_verify_builder()` + `playgrouping_apps_tokens_verify_execute()`.
/// For task-level control, use `playgrouping_apps_tokens_verify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_verify(
    client: &SimpleHttpClient,
    args: &PlaygroupingAppsTokensVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        playgrouping_apps_tokens_verify_builder(client, &args.appPackage, &args.token, &args.body)?;
    playgrouping_apps_tokens_verify_execute(builder)
}

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}/tags:createOrUpdate
/// Create or update tags for the user and app that are represented by the given token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playgrouping_apps_tokens_tags_create_or_update_execute()` to send, or `playgrouping_apps_tokens_tags_create_or_update` for simplest API.

pub fn playgrouping_apps_tokens_tags_create_or_update_builder(
    client: &SimpleHttpClient,
    appPackage: &String,
    token: &String,
    body: &CreateOrUpdateTagsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://playgrouping.googleapis.com/v1alpha1/apps/{}/tokens/{}/tags:createOrUpdate",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}/tags:createOrUpdate
/// Create or update tags for the user and app that are represented by the given token.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playgrouping_apps_tokens_tags_create_or_update_execute()` or `playgrouping_apps_tokens_tags_create_or_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playgrouping_apps_tokens_tags_create_or_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_tags_create_or_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateOrUpdateTagsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateOrUpdateTagsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}/tags:createOrUpdate
/// Create or update tags for the user and app that are represented by the given token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playgrouping_apps_tokens_tags_create_or_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playgrouping_apps_tokens_tags_create_or_update_task()`.
/// For the simplest API, use `playgrouping_apps_tokens_tags_create_or_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playgrouping_apps_tokens_tags_create_or_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playgrouping_apps_tokens_tags_create_or_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateOrUpdateTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playgrouping_apps_tokens_tags_create_or_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playgrouping_apps_tokens_tags_create_or_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlaygroupingAppsTokensTagsCreateOrUpdateArgs {
    /// Path parameter: appPackage
    pub appPackage: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: CreateOrUpdateTagsRequest,
}

/// GET v1alpha1/apps/{appsId}/tokens/{tokensId}/tags:createOrUpdate
/// Create or update tags for the user and app that are represented by the given token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playgrouping_apps_tokens_tags_create_or_update_builder()` + `playgrouping_apps_tokens_tags_create_or_update_execute()`.
/// For task-level control, use `playgrouping_apps_tokens_tags_create_or_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_tags_create_or_update(
    client: &SimpleHttpClient,
    args: &PlaygroupingAppsTokensTagsCreateOrUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateOrUpdateTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = playgrouping_apps_tokens_tags_create_or_update_builder(
        client,
        &args.appPackage,
        &args.token,
        &args.body,
    )?;
    playgrouping_apps_tokens_tags_create_or_update_execute(builder)
}
