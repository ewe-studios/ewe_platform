//! Auto-generated API clients for gcp/playgrouping.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1alpha1/{+appPackage}/{+token}:verify
/// Verify an API token by asserting the app and persona it belongs to. The verification is a protection against client-side attacks and will fail if the contents of the token don't match the provided values. A token must be verified before it can be used to manipulate user tags.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playgrouping_apps_tokens_verify_execute()` to send, or `playgrouping_apps_tokens_verify` for simplest API.

pub fn playgrouping_apps_tokens_verify_builder(
    client: &SimpleHttpClient,
    appPackage: &str,
    token: &str,
    body: &VerifyTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1alpha1/{+appPackage}/{+token}:verify",
        appPackage, token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1alpha1/{+appPackage}/{+token}:verify
/// Verify an API token by asserting the app and persona it belongs to. The verification is a protection against client-side attacks and will fail if the contents of the token don't match the provided values. A token must be verified before it can be used to manipulate user tags.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playgrouping_apps_tokens_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playgrouping_apps_tokens_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VerifyTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/{+appPackage}/{+token}:verify
/// Verify an API token by asserting the app and persona it belongs to. The verification is a protection against client-side attacks and will fail if the contents of the token don't match the provided values. A token must be verified before it can be used to manipulate user tags.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playgrouping_apps_tokens_verify_builder()` + `playgrouping_apps_tokens_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_verify(
    client: &SimpleHttpClient,
    appPackage: &str,
    token: &str,
    body: &VerifyTokenRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VerifyTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = playgrouping_apps_tokens_verify_builder(client, appPackage, token, body)?;
    playgrouping_apps_tokens_verify_execute(builder)
}

/// GET v1alpha1/{+appPackage}/{+token}/tags:createOrUpdate
/// Create or update tags for the user and app that are represented by the given token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playgrouping_apps_tokens_tags_create_or_update_execute()` to send, or `playgrouping_apps_tokens_tags_create_or_update` for simplest API.

pub fn playgrouping_apps_tokens_tags_create_or_update_builder(
    client: &SimpleHttpClient,
    appPackage: &str,
    token: &str,
    body: &CreateOrUpdateTagsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1alpha1/{+appPackage}/{+token}/tags:createOrUpdate",
        appPackage, token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1alpha1/{+appPackage}/{+token}/tags:createOrUpdate
/// Create or update tags for the user and app that are represented by the given token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playgrouping_apps_tokens_tags_create_or_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playgrouping_apps_tokens_tags_create_or_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_tags_create_or_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateOrUpdateTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateOrUpdateTagsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/{+appPackage}/{+token}/tags:createOrUpdate
/// Create or update tags for the user and app that are represented by the given token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playgrouping_apps_tokens_tags_create_or_update_builder()` + `playgrouping_apps_tokens_tags_create_or_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playgrouping_apps_tokens_tags_create_or_update(
    client: &SimpleHttpClient,
    appPackage: &str,
    token: &str,
    body: &CreateOrUpdateTagsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CreateOrUpdateTagsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playgrouping_apps_tokens_tags_create_or_update_builder(client, appPackage, token, body)?;
    playgrouping_apps_tokens_tags_create_or_update_execute(builder)
}
