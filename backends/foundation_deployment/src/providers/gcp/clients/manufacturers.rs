//! Auto-generated API clients for gcp/manufacturers.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accounts/{accountsId}/products/{productsId}
/// Deletes the product from a Manufacturer Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `manufacturers_accounts_products_delete_execute()` to send, or `manufacturers_accounts_products_delete` for simplest API.

pub fn manufacturers_accounts_products_delete_builder(
    client: &SimpleHttpClient,
    parent: &String,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://manufacturers.googleapis.com/v1/accounts/{}/products/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/products/{productsId}
/// Deletes the product from a Manufacturer Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `manufacturers_accounts_products_delete_execute()` or `manufacturers_accounts_products_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `manufacturers_accounts_products_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn manufacturers_accounts_products_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/products/{productsId}
/// Deletes the product from a Manufacturer Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `manufacturers_accounts_products_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `manufacturers_accounts_products_delete_task()`.
/// For the simplest API, use `manufacturers_accounts_products_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `manufacturers_accounts_products_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn manufacturers_accounts_products_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = manufacturers_accounts_products_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`manufacturers_accounts_products_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManufacturersAccountsProductsDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: name
    pub name: String,
}

/// GET v1/accounts/{accountsId}/products/{productsId}
/// Deletes the product from a Manufacturer Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `manufacturers_accounts_products_delete_builder()` + `manufacturers_accounts_products_delete_execute()`.
/// For task-level control, use `manufacturers_accounts_products_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn manufacturers_accounts_products_delete(
    client: &SimpleHttpClient,
    args: &ManufacturersAccountsProductsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = manufacturers_accounts_products_delete_builder(client, &args.parent, &args.name)?;
    manufacturers_accounts_products_delete_execute(builder)
}

/// GET v1/accounts/{accountsId}/products
/// Lists all the products in a Manufacturer Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `manufacturers_accounts_products_list_execute()` to send, or `manufacturers_accounts_products_list` for simplest API.

pub fn manufacturers_accounts_products_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    include: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://manufacturers.googleapis.com/v1/accounts/{}/products",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = include.as_ref() {
        query_parts.push(format!("include={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/products
/// Lists all the products in a Manufacturer Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `manufacturers_accounts_products_list_execute()` or `manufacturers_accounts_products_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `manufacturers_accounts_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn manufacturers_accounts_products_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/products
/// Lists all the products in a Manufacturer Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `manufacturers_accounts_products_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `manufacturers_accounts_products_list_task()`.
/// For the simplest API, use `manufacturers_accounts_products_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `manufacturers_accounts_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn manufacturers_accounts_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = manufacturers_accounts_products_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`manufacturers_accounts_products_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManufacturersAccountsProductsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: include
    pub include: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/accounts/{accountsId}/products
/// Lists all the products in a Manufacturer Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `manufacturers_accounts_products_list_builder()` + `manufacturers_accounts_products_list_execute()`.
/// For task-level control, use `manufacturers_accounts_products_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn manufacturers_accounts_products_list(
    client: &SimpleHttpClient,
    args: &ManufacturersAccountsProductsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = manufacturers_accounts_products_list_builder(
        client,
        &args.parent,
        &args.include,
        &args.pageSize,
        &args.pageToken,
    )?;
    manufacturers_accounts_products_list_execute(builder)
}
