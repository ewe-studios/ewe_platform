//! Auto-generated API clients for Google Cloud Platform - binaryauthorization.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// A policy specifies the attestors that must attest to a container image, before the project is allowed to deploy that image. There is at most one policy per project. All image admission requests are permitted if a project has no policy. Gets the policy for this project. Returns a default policy if the project does not have one.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_get_policy_execute()` to send, or `binaryauthorization_projects_get_policy` for simplest API.

pub fn binaryauthorization_projects_get_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// A policy specifies the attestors that must attest to a container image, before the project is allowed to deploy that image. There is at most one policy per project. All image admission requests are permitted if a project has no policy. Gets the policy for this project. Returns a default policy if the project does not have one.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_get_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_get_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_get_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// A policy specifies the attestors that must attest to a container image, before the project is allowed to deploy that image. There is at most one policy per project. All image admission requests are permitted if a project has no policy. Gets the policy for this project. Returns a default policy if the project does not have one.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_get_policy_builder()` + `binaryauthorization_projects_get_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_get_policy(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_get_policy_builder(client, name)?;
    binaryauthorization_projects_get_policy_execute(builder)
}

/// GET v1/{+name}
/// Creates or updates a project's policy, and returns a copy of the new policy. A policy is always updated as a whole, to avoid race conditions with concurrent policy enforcement (or management!) requests. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_update_policy_execute()` to send, or `binaryauthorization_projects_update_policy` for simplest API.

pub fn binaryauthorization_projects_update_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Creates or updates a project's policy, and returns a copy of the new policy. A policy is always updated as a whole, to avoid race conditions with concurrent policy enforcement (or management!) requests. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_update_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_update_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_update_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Creates or updates a project's policy, and returns a copy of the new policy. A policy is always updated as a whole, to avoid race conditions with concurrent policy enforcement (or management!) requests. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_update_policy_builder()` + `binaryauthorization_projects_update_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_update_policy(
    client: &SimpleHttpClient,
    name: &str,
    body: &Policy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_update_policy_builder(client, name, body)?;
    binaryauthorization_projects_update_policy_execute(builder)
}

/// GET v1/{+parent}/attestors
/// Creates an attestor, and returns a copy of the new attestor. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the attestor already exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_create_execute()` to send, or `binaryauthorization_projects_attestors_create` for simplest API.

pub fn binaryauthorization_projects_attestors_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    attestorId: Option<&str>,
    body: &Attestor,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/attestors", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "attestorId",
            attestorId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/attestors
/// Creates an attestor, and returns a copy of the new attestor. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the attestor already exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attestor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/attestors
/// Creates an attestor, and returns a copy of the new attestor. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the attestor already exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_create_builder()` + `binaryauthorization_projects_attestors_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_create(
    client: &SimpleHttpClient,
    parent: &str,
    attestorId: Option<&str>,
    body: &Attestor,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        binaryauthorization_projects_attestors_create_builder(client, parent, attestorId, body)?;
    binaryauthorization_projects_attestors_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_delete_execute()` to send, or `binaryauthorization_projects_attestors_delete` for simplest API.

pub fn binaryauthorization_projects_attestors_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_delete_builder()` + `binaryauthorization_projects_attestors_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_attestors_delete_builder(client, name)?;
    binaryauthorization_projects_attestors_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_get_execute()` to send, or `binaryauthorization_projects_attestors_get` for simplest API.

pub fn binaryauthorization_projects_attestors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attestor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_get_builder()` + `binaryauthorization_projects_attestors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_attestors_get_builder(client, name)?;
    binaryauthorization_projects_attestors_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_get_iam_policy_execute()` to send, or `binaryauthorization_projects_attestors_get_iam_policy` for simplest API.

pub fn binaryauthorization_projects_attestors_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "options_requestedPolicyVersion",
            options_requestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IamPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_get_iam_policy_builder()` + `binaryauthorization_projects_attestors_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_attestors_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    binaryauthorization_projects_attestors_get_iam_policy_execute(builder)
}

/// GET v1/{+parent}/attestors
/// Lists attestors. Returns INVALID_ARGUMENT if the project does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_list_execute()` to send, or `binaryauthorization_projects_attestors_list` for simplest API.

pub fn binaryauthorization_projects_attestors_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/attestors", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/attestors
/// Lists attestors. Returns INVALID_ARGUMENT if the project does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttestorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttestorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/attestors
/// Lists attestors. Returns INVALID_ARGUMENT if the project does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_list_builder()` + `binaryauthorization_projects_attestors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAttestorsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        binaryauthorization_projects_attestors_list_builder(client, parent, pageSize, pageToken)?;
    binaryauthorization_projects_attestors_list_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_set_iam_policy_execute()` to send, or `binaryauthorization_projects_attestors_set_iam_policy` for simplest API.

pub fn binaryauthorization_projects_attestors_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IamPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_set_iam_policy_builder()` + `binaryauthorization_projects_attestors_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        binaryauthorization_projects_attestors_set_iam_policy_builder(client, resource, body)?;
    binaryauthorization_projects_attestors_set_iam_policy_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_test_iam_permissions_execute()` to send, or `binaryauthorization_projects_attestors_test_iam_permissions` for simplest API.

pub fn binaryauthorization_projects_attestors_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_test_iam_permissions_builder()` + `binaryauthorization_projects_attestors_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_attestors_test_iam_permissions_builder(
        client, resource, body,
    )?;
    binaryauthorization_projects_attestors_test_iam_permissions_execute(builder)
}

/// GET v1/{+name}
/// Updates an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_update_execute()` to send, or `binaryauthorization_projects_attestors_update` for simplest API.

pub fn binaryauthorization_projects_attestors_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &Attestor,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attestor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an attestor. Returns NOT_FOUND if the attestor does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_update_builder()` + `binaryauthorization_projects_attestors_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &Attestor,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_attestors_update_builder(client, name, body)?;
    binaryauthorization_projects_attestors_update_execute(builder)
}

/// GET v1/{+attestor}:validateAttestationOccurrence
/// Returns whether the given Attestation for the given image URI was signed by the given Attestor
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_validate_attestation_occurrence_execute()` to send, or `binaryauthorization_projects_attestors_validate_attestation_occurrence` for simplest API.

pub fn binaryauthorization_projects_attestors_validate_attestation_occurrence_builder(
    client: &SimpleHttpClient,
    attestor: &str,
    body: &ValidateAttestationOccurrenceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+attestor}:validateAttestationOccurrence",
        attestor,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+attestor}:validateAttestationOccurrence
/// Returns whether the given Attestation for the given image URI was signed by the given Attestor
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_attestors_validate_attestation_occurrence_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_attestors_validate_attestation_occurrence_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_validate_attestation_occurrence_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateAttestationOccurrenceResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateAttestationOccurrenceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+attestor}:validateAttestationOccurrence
/// Returns whether the given Attestation for the given image URI was signed by the given Attestor
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_validate_attestation_occurrence_builder()` + `binaryauthorization_projects_attestors_validate_attestation_occurrence_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_validate_attestation_occurrence(
    client: &SimpleHttpClient,
    attestor: &str,
    body: &ValidateAttestationOccurrenceRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateAttestationOccurrenceResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_attestors_validate_attestation_occurrence_builder(
        client, attestor, body,
    )?;
    binaryauthorization_projects_attestors_validate_attestation_occurrence_execute(builder)
}

/// GET v1/{+name}:evaluate
/// Evaluates a Kubernetes object versus a GKE platform policy. Returns NOT_FOUND if the policy doesn't exist, INVALID_ARGUMENT if the policy or request is malformed and PERMISSION_DENIED if the client does not have sufficient permissions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_platforms_gke_policies_evaluate_execute()` to send, or `binaryauthorization_projects_platforms_gke_policies_evaluate` for simplest API.

pub fn binaryauthorization_projects_platforms_gke_policies_evaluate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EvaluateGkePolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:evaluate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:evaluate
/// Evaluates a Kubernetes object versus a GKE platform policy. Returns NOT_FOUND if the policy doesn't exist, INVALID_ARGUMENT if the policy or request is malformed and PERMISSION_DENIED if the client does not have sufficient permissions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_platforms_gke_policies_evaluate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_platforms_gke_policies_evaluate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_gke_policies_evaluate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EvaluateGkePolicyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EvaluateGkePolicyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:evaluate
/// Evaluates a Kubernetes object versus a GKE platform policy. Returns NOT_FOUND if the policy doesn't exist, INVALID_ARGUMENT if the policy or request is malformed and PERMISSION_DENIED if the client does not have sufficient permissions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_platforms_gke_policies_evaluate_builder()` + `binaryauthorization_projects_platforms_gke_policies_evaluate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_gke_policies_evaluate(
    client: &SimpleHttpClient,
    name: &str,
    body: &EvaluateGkePolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EvaluateGkePolicyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        binaryauthorization_projects_platforms_gke_policies_evaluate_builder(client, name, body)?;
    binaryauthorization_projects_platforms_gke_policies_evaluate_execute(builder)
}

/// GET v1/{+parent}/policies
/// Creates a platform policy, and returns a copy of it. Returns NOT_FOUND if the project or platform doesn't exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the policy already exists, and INVALID_ARGUMENT if the policy contains a platform-specific policy that does not match the platform value specified in the URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_platforms_policies_create_execute()` to send, or `binaryauthorization_projects_platforms_policies_create` for simplest API.

pub fn binaryauthorization_projects_platforms_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    policyId: Option<&str>,
    body: &PlatformPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/policies", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "policyId",
            policyId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/policies
/// Creates a platform policy, and returns a copy of it. Returns NOT_FOUND if the project or platform doesn't exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the policy already exists, and INVALID_ARGUMENT if the policy contains a platform-specific policy that does not match the platform value specified in the URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_platforms_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_platforms_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlatformPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/policies
/// Creates a platform policy, and returns a copy of it. Returns NOT_FOUND if the project or platform doesn't exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the policy already exists, and INVALID_ARGUMENT if the policy contains a platform-specific policy that does not match the platform value specified in the URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_platforms_policies_create_builder()` + `binaryauthorization_projects_platforms_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_create(
    client: &SimpleHttpClient,
    parent: &str,
    policyId: Option<&str>,
    body: &PlatformPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_platforms_policies_create_builder(
        client, parent, policyId, body,
    )?;
    binaryauthorization_projects_platforms_policies_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_platforms_policies_delete_execute()` to send, or `binaryauthorization_projects_platforms_policies_delete` for simplest API.

pub fn binaryauthorization_projects_platforms_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_platforms_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_platforms_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_platforms_policies_delete_builder()` + `binaryauthorization_projects_platforms_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        binaryauthorization_projects_platforms_policies_delete_builder(client, name, etag)?;
    binaryauthorization_projects_platforms_policies_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_platforms_policies_get_execute()` to send, or `binaryauthorization_projects_platforms_policies_get` for simplest API.

pub fn binaryauthorization_projects_platforms_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_platforms_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_platforms_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlatformPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_platforms_policies_get_builder()` + `binaryauthorization_projects_platforms_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_platforms_policies_get_builder(client, name)?;
    binaryauthorization_projects_platforms_policies_get_execute(builder)
}

/// GET v1/{+parent}/policies
/// Lists platform policies owned by a project in the specified platform. Returns INVALID_ARGUMENT if the project or the platform doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_platforms_policies_list_execute()` to send, or `binaryauthorization_projects_platforms_policies_list` for simplest API.

pub fn binaryauthorization_projects_platforms_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/policies", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/policies
/// Lists platform policies owned by a project in the specified platform. Returns INVALID_ARGUMENT if the project or the platform doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_platforms_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_platforms_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlatformPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPlatformPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/policies
/// Lists platform policies owned by a project in the specified platform. Returns INVALID_ARGUMENT if the project or the platform doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_platforms_policies_list_builder()` + `binaryauthorization_projects_platforms_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPlatformPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_platforms_policies_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    binaryauthorization_projects_platforms_policies_list_execute(builder)
}

/// GET v1/{+name}
/// Replaces a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_platforms_policies_replace_platform_policy_execute()` to send, or `binaryauthorization_projects_platforms_policies_replace_platform_policy` for simplest API.

pub fn binaryauthorization_projects_platforms_policies_replace_platform_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &PlatformPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Replaces a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_platforms_policies_replace_platform_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_platforms_policies_replace_platform_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_replace_platform_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlatformPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Replaces a platform policy. Returns NOT_FOUND if the policy doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_platforms_policies_replace_platform_policy_builder()` + `binaryauthorization_projects_platforms_policies_replace_platform_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_platforms_policies_replace_platform_policy(
    client: &SimpleHttpClient,
    name: &str,
    body: &PlatformPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlatformPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_platforms_policies_replace_platform_policy_builder(
        client, name, body,
    )?;
    binaryauthorization_projects_platforms_policies_replace_platform_policy_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_policy_get_iam_policy_execute()` to send, or `binaryauthorization_projects_policy_get_iam_policy` for simplest API.

pub fn binaryauthorization_projects_policy_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "options_requestedPolicyVersion",
            options_requestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_policy_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_policy_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IamPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_policy_get_iam_policy_builder()` + `binaryauthorization_projects_policy_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_policy_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    binaryauthorization_projects_policy_get_iam_policy_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_policy_set_iam_policy_execute()` to send, or `binaryauthorization_projects_policy_set_iam_policy` for simplest API.

pub fn binaryauthorization_projects_policy_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_policy_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_policy_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IamPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_policy_set_iam_policy_builder()` + `binaryauthorization_projects_policy_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        binaryauthorization_projects_policy_set_iam_policy_builder(client, resource, body)?;
    binaryauthorization_projects_policy_set_iam_policy_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_policy_test_iam_permissions_execute()` to send, or `binaryauthorization_projects_policy_test_iam_permissions` for simplest API.

pub fn binaryauthorization_projects_policy_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_projects_policy_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_projects_policy_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_policy_test_iam_permissions_builder()` + `binaryauthorization_projects_policy_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        binaryauthorization_projects_policy_test_iam_permissions_builder(client, resource, body)?;
    binaryauthorization_projects_policy_test_iam_permissions_execute(builder)
}

/// GET v1/{+name}
/// Gets the current system policy in the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_systempolicy_get_policy_execute()` to send, or `binaryauthorization_systempolicy_get_policy` for simplest API.

pub fn binaryauthorization_systempolicy_get_policy_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the current system policy in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `binaryauthorization_systempolicy_get_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from binaryauthorization_systempolicy_get_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_systempolicy_get_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the current system policy in the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_systempolicy_get_policy_builder()` + `binaryauthorization_systempolicy_get_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_systempolicy_get_policy(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_systempolicy_get_policy_builder(client, name)?;
    binaryauthorization_systempolicy_get_policy_execute(builder)
}
