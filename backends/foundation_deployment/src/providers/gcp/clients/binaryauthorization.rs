//! Auto-generated API clients for gcp/binaryauthorization.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/policy
/// A policy specifies the attestors that must attest to a container image, before the project is allowed to deploy that image. There is at most one policy per project. All image admission requests are permitted if a project has no policy. Gets the policy for this project. Returns a default policy if the project does not have one.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_get_policy_execute()` to send, or `binaryauthorization_projects_get_policy` for simplest API.

pub fn binaryauthorization_projects_get_policy_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://binaryauthorization.googleapis.com/v1/projects/{}/policy",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/policy
/// A policy specifies the attestors that must attest to a container image, before the project is allowed to deploy that image. There is at most one policy per project. All image admission requests are permitted if a project has no policy. Gets the policy for this project. Returns a default policy if the project does not have one.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `binaryauthorization_projects_get_policy_execute()` or `binaryauthorization_projects_get_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_get_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_get_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/policy
/// A policy specifies the attestors that must attest to a container image, before the project is allowed to deploy that image. There is at most one policy per project. All image admission requests are permitted if a project has no policy. Gets the policy for this project. Returns a default policy if the project does not have one.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `binaryauthorization_projects_get_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `binaryauthorization_projects_get_policy_task()`.
/// For the simplest API, use `binaryauthorization_projects_get_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_get_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn binaryauthorization_projects_get_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = binaryauthorization_projects_get_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`binaryauthorization_projects_get_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsGetPolicyArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/policy
/// A policy specifies the attestors that must attest to a container image, before the project is allowed to deploy that image. There is at most one policy per project. All image admission requests are permitted if a project has no policy. Gets the policy for this project. Returns a default policy if the project does not have one.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_get_policy_builder()` + `binaryauthorization_projects_get_policy_execute()`.
/// For task-level control, use `binaryauthorization_projects_get_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_get_policy(
    client: &SimpleHttpClient,
    args: &BinaryauthorizationProjectsGetPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_get_policy_builder(client, args.name.clone())?;
    binaryauthorization_projects_get_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/attestors
/// Creates an attestor, and returns a copy of the new attestor. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the attestor already exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_attestors_create_execute()` to send, or `binaryauthorization_projects_attestors_create` for simplest API.

pub fn binaryauthorization_projects_attestors_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    attestorId: Option<String>,
    body: &Attestor,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://binaryauthorization.googleapis.com/v1/projects/{}/attestors",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = attestorId {
        query_parts.push(format!("attestorId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/attestors
/// Creates an attestor, and returns a copy of the new attestor. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the attestor already exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `binaryauthorization_projects_attestors_create_execute()` or `binaryauthorization_projects_attestors_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_attestors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Attestor>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attestor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/attestors
/// Creates an attestor, and returns a copy of the new attestor. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the attestor already exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `binaryauthorization_projects_attestors_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `binaryauthorization_projects_attestors_create_task()`.
/// For the simplest API, use `binaryauthorization_projects_attestors_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_attestors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn binaryauthorization_projects_attestors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = binaryauthorization_projects_attestors_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`binaryauthorization_projects_attestors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsAttestorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: attestorId
    pub attestorId: Option<String>,
    /// Request body.
    pub body: Attestor,
}

/// GET v1/projects/{projectsId}/attestors
/// Creates an attestor, and returns a copy of the new attestor. Returns NOT_FOUND if the project does not exist, INVALID_ARGUMENT if the request is malformed, ALREADY_EXISTS if the attestor already exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_attestors_create_builder()` + `binaryauthorization_projects_attestors_create_execute()`.
/// For task-level control, use `binaryauthorization_projects_attestors_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_attestors_create(
    client: &SimpleHttpClient,
    args: &BinaryauthorizationProjectsAttestorsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attestor>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_attestors_create_builder(
        client,
        args.parent.clone(),
        args.attestorId.clone(),
        &args.body,
    )?;
    binaryauthorization_projects_attestors_create_execute(builder)
}

/// GET v1/projects/{projectsId}/policy:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_policy_get_iam_policy_execute()` to send, or `binaryauthorization_projects_policy_get_iam_policy` for simplest API.

pub fn binaryauthorization_projects_policy_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: String,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://binaryauthorization.googleapis.com/v1/projects/{}/policy:getIamPolicy",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/policy:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `binaryauthorization_projects_policy_get_iam_policy_execute()` or `binaryauthorization_projects_policy_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_policy_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IamPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IamPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/policy:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `binaryauthorization_projects_policy_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `binaryauthorization_projects_policy_get_iam_policy_task()`.
/// For the simplest API, use `binaryauthorization_projects_policy_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_policy_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn binaryauthorization_projects_policy_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = binaryauthorization_projects_policy_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`binaryauthorization_projects_policy_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPolicyGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/policy:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_policy_get_iam_policy_builder()` + `binaryauthorization_projects_policy_get_iam_policy_execute()`.
/// For task-level control, use `binaryauthorization_projects_policy_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BinaryauthorizationProjectsPolicyGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_policy_get_iam_policy_builder(
        client,
        args.resource.clone(),
        args.options_requestedPolicyVersion.clone(),
    )?;
    binaryauthorization_projects_policy_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/policy:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_policy_set_iam_policy_execute()` to send, or `binaryauthorization_projects_policy_set_iam_policy` for simplest API.

pub fn binaryauthorization_projects_policy_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: String,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://binaryauthorization.googleapis.com/v1/projects/{}/policy:setIamPolicy",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/policy:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `binaryauthorization_projects_policy_set_iam_policy_execute()` or `binaryauthorization_projects_policy_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_policy_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IamPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IamPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/policy:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `binaryauthorization_projects_policy_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `binaryauthorization_projects_policy_set_iam_policy_task()`.
/// For the simplest API, use `binaryauthorization_projects_policy_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_policy_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn binaryauthorization_projects_policy_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = binaryauthorization_projects_policy_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`binaryauthorization_projects_policy_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPolicySetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/policy:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_policy_set_iam_policy_builder()` + `binaryauthorization_projects_policy_set_iam_policy_execute()`.
/// For task-level control, use `binaryauthorization_projects_policy_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BinaryauthorizationProjectsPolicySetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IamPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_policy_set_iam_policy_builder(
        client,
        args.resource.clone(),
        &args.body,
    )?;
    binaryauthorization_projects_policy_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/policy:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_projects_policy_test_iam_permissions_execute()` to send, or `binaryauthorization_projects_policy_test_iam_permissions` for simplest API.

pub fn binaryauthorization_projects_policy_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: String,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://binaryauthorization.googleapis.com/v1/projects/{}/policy:testIamPermissions",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/policy:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `binaryauthorization_projects_policy_test_iam_permissions_execute()` or `binaryauthorization_projects_policy_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_policy_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/policy:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `binaryauthorization_projects_policy_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `binaryauthorization_projects_policy_test_iam_permissions_task()`.
/// For the simplest API, use `binaryauthorization_projects_policy_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_projects_policy_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn binaryauthorization_projects_policy_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = binaryauthorization_projects_policy_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`binaryauthorization_projects_policy_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPolicyTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/policy:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_projects_policy_test_iam_permissions_builder()` + `binaryauthorization_projects_policy_test_iam_permissions_execute()`.
/// For task-level control, use `binaryauthorization_projects_policy_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_projects_policy_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BinaryauthorizationProjectsPolicyTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = binaryauthorization_projects_policy_test_iam_permissions_builder(
        client,
        args.resource.clone(),
        &args.body,
    )?;
    binaryauthorization_projects_policy_test_iam_permissions_execute(builder)
}

/// GET v1/locations/{locationsId}/policy
/// Gets the current system policy in the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `binaryauthorization_systempolicy_get_policy_execute()` to send, or `binaryauthorization_systempolicy_get_policy` for simplest API.

pub fn binaryauthorization_systempolicy_get_policy_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://binaryauthorization.googleapis.com/v1/locations/{}/policy",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/policy
/// Gets the current system policy in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `binaryauthorization_systempolicy_get_policy_execute()` or `binaryauthorization_systempolicy_get_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_systempolicy_get_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_systempolicy_get_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/policy
/// Gets the current system policy in the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `binaryauthorization_systempolicy_get_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `binaryauthorization_systempolicy_get_policy_task()`.
/// For the simplest API, use `binaryauthorization_systempolicy_get_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `binaryauthorization_systempolicy_get_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn binaryauthorization_systempolicy_get_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = binaryauthorization_systempolicy_get_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`binaryauthorization_systempolicy_get_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationSystempolicyGetPolicyArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/policy
/// Gets the current system policy in the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `binaryauthorization_systempolicy_get_policy_builder()` + `binaryauthorization_systempolicy_get_policy_execute()`.
/// For task-level control, use `binaryauthorization_systempolicy_get_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn binaryauthorization_systempolicy_get_policy(
    client: &SimpleHttpClient,
    args: &BinaryauthorizationSystempolicyGetPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = binaryauthorization_systempolicy_get_policy_builder(client, args.name.clone())?;
    binaryauthorization_systempolicy_get_policy_execute(builder)
}
