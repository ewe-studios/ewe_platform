//! Auto-generated API clients for gcp/cloudprofiler.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/projects/{projectsId}/profiles
/// CreateProfile creates a new profile resource in the online mode. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._ The server ensures that the new profiles are created at a constant rate per deployment, so the creation request may hang for some time until the next profile session is available. The request may fail with ABORTED error if the creation is not available within ~1m, the response will indicate the duration of the backoff the client should take before attempting creating a profile again. The backoff duration is returned in google.rpc.RetryInfo extension on the response status. To a `gRPC` client, the extension will be return as a binary-serialized proto in the trailing metadata item named "google.rpc.retryinfo-bin".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudprofiler_projects_profiles_create_execute()` to send, or `cloudprofiler_projects_profiles_create` for simplest API.

pub fn cloudprofiler_projects_profiles_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudprofiler.googleapis.com/v2/projects/{}/profiles",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/profiles
/// CreateProfile creates a new profile resource in the online mode. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._ The server ensures that the new profiles are created at a constant rate per deployment, so the creation request may hang for some time until the next profile session is available. The request may fail with ABORTED error if the creation is not available within ~1m, the response will indicate the duration of the backoff the client should take before attempting creating a profile again. The backoff duration is returned in google.rpc.RetryInfo extension on the response status. To a `gRPC` client, the extension will be return as a binary-serialized proto in the trailing metadata item named "google.rpc.retryinfo-bin".
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudprofiler_projects_profiles_create_execute()` or `cloudprofiler_projects_profiles_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/profiles
/// CreateProfile creates a new profile resource in the online mode. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._ The server ensures that the new profiles are created at a constant rate per deployment, so the creation request may hang for some time until the next profile session is available. The request may fail with ABORTED error if the creation is not available within ~1m, the response will indicate the duration of the backoff the client should take before attempting creating a profile again. The backoff duration is returned in google.rpc.RetryInfo extension on the response status. To a `gRPC` client, the extension will be return as a binary-serialized proto in the trailing metadata item named "google.rpc.retryinfo-bin".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudprofiler_projects_profiles_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudprofiler_projects_profiles_create_task()`.
/// For the simplest API, use `cloudprofiler_projects_profiles_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudprofiler_projects_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudprofiler_projects_profiles_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudprofiler_projects_profiles_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudprofilerProjectsProfilesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/profiles
/// CreateProfile creates a new profile resource in the online mode. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._ The server ensures that the new profiles are created at a constant rate per deployment, so the creation request may hang for some time until the next profile session is available. The request may fail with ABORTED error if the creation is not available within ~1m, the response will indicate the duration of the backoff the client should take before attempting creating a profile again. The backoff duration is returned in google.rpc.RetryInfo extension on the response status. To a `gRPC` client, the extension will be return as a binary-serialized proto in the trailing metadata item named "google.rpc.retryinfo-bin".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudprofiler_projects_profiles_create_builder()` + `cloudprofiler_projects_profiles_create_execute()`.
/// For task-level control, use `cloudprofiler_projects_profiles_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_create(
    client: &SimpleHttpClient,
    args: &CloudprofilerProjectsProfilesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudprofiler_projects_profiles_create_builder(client, &args.parent)?;
    cloudprofiler_projects_profiles_create_execute(builder)
}

/// POST v2/projects/{projectsId}/profiles:createOffline
/// CreateOfflineProfile creates a new profile resource in the offline mode. The client provides the profile to create along with the profile bytes, the server records it. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudprofiler_projects_profiles_create_offline_execute()` to send, or `cloudprofiler_projects_profiles_create_offline` for simplest API.

pub fn cloudprofiler_projects_profiles_create_offline_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudprofiler.googleapis.com/v2/projects/{}/profiles:createOffline",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/profiles:createOffline
/// CreateOfflineProfile creates a new profile resource in the offline mode. The client provides the profile to create along with the profile bytes, the server records it. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudprofiler_projects_profiles_create_offline_execute()` or `cloudprofiler_projects_profiles_create_offline`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_create_offline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_create_offline_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/profiles:createOffline
/// CreateOfflineProfile creates a new profile resource in the offline mode. The client provides the profile to create along with the profile bytes, the server records it. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudprofiler_projects_profiles_create_offline_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudprofiler_projects_profiles_create_offline_task()`.
/// For the simplest API, use `cloudprofiler_projects_profiles_create_offline()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_create_offline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudprofiler_projects_profiles_create_offline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudprofiler_projects_profiles_create_offline_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudprofiler_projects_profiles_create_offline`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudprofilerProjectsProfilesCreateOfflineArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/profiles:createOffline
/// CreateOfflineProfile creates a new profile resource in the offline mode. The client provides the profile to create along with the profile bytes, the server records it. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudprofiler_projects_profiles_create_offline_builder()` + `cloudprofiler_projects_profiles_create_offline_execute()`.
/// For task-level control, use `cloudprofiler_projects_profiles_create_offline_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_create_offline(
    client: &SimpleHttpClient,
    args: &CloudprofilerProjectsProfilesCreateOfflineArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudprofiler_projects_profiles_create_offline_builder(client, &args.parent)?;
    cloudprofiler_projects_profiles_create_offline_execute(builder)
}

/// GET v2/projects/{projectsId}/profiles
/// Lists profiles which have been collected so far and for which the caller has permission to view.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudprofiler_projects_profiles_list_execute()` to send, or `cloudprofiler_projects_profiles_list` for simplest API.

pub fn cloudprofiler_projects_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudprofiler.googleapis.com/v2/projects/{}/profiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/profiles
/// Lists profiles which have been collected so far and for which the caller has permission to view.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudprofiler_projects_profiles_list_execute()` or `cloudprofiler_projects_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/profiles
/// Lists profiles which have been collected so far and for which the caller has permission to view.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudprofiler_projects_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudprofiler_projects_profiles_list_task()`.
/// For the simplest API, use `cloudprofiler_projects_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudprofiler_projects_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProfilesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudprofiler_projects_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudprofiler_projects_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudprofilerProjectsProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/profiles
/// Lists profiles which have been collected so far and for which the caller has permission to view.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudprofiler_projects_profiles_list_builder()` + `cloudprofiler_projects_profiles_list_execute()`.
/// For task-level control, use `cloudprofiler_projects_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_list(
    client: &SimpleHttpClient,
    args: &CloudprofilerProjectsProfilesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProfilesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudprofiler_projects_profiles_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudprofiler_projects_profiles_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/profiles/{profilesId}
/// UpdateProfile updates the profile bytes and labels on the profile resource created in the online mode. Updating the bytes for profiles created in the offline mode is currently not supported: the profile content must be provided at the time of the profile creation. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudprofiler_projects_profiles_patch_execute()` to send, or `cloudprofiler_projects_profiles_patch` for simplest API.

pub fn cloudprofiler_projects_profiles_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudprofiler.googleapis.com/v2/projects/{}/profiles/{profilesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/profiles/{profilesId}
/// UpdateProfile updates the profile bytes and labels on the profile resource created in the online mode. Updating the bytes for profiles created in the offline mode is currently not supported: the profile content must be provided at the time of the profile creation. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudprofiler_projects_profiles_patch_execute()` or `cloudprofiler_projects_profiles_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Profile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/profiles/{profilesId}
/// UpdateProfile updates the profile bytes and labels on the profile resource created in the online mode. Updating the bytes for profiles created in the offline mode is currently not supported: the profile content must be provided at the time of the profile creation. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudprofiler_projects_profiles_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudprofiler_projects_profiles_patch_task()`.
/// For the simplest API, use `cloudprofiler_projects_profiles_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudprofiler_projects_profiles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudprofiler_projects_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudprofiler_projects_profiles_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudprofiler_projects_profiles_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudprofilerProjectsProfilesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/profiles/{profilesId}
/// UpdateProfile updates the profile bytes and labels on the profile resource created in the online mode. Updating the bytes for profiles created in the offline mode is currently not supported: the profile content must be provided at the time of the profile creation. _Direct use of this API is discouraged, please use a [supported profiler agent](<https://cloud.google.`com/profiler/docs/about-profiler`#profiling_agent>) instead for profile collection._
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudprofiler_projects_profiles_patch_builder()` + `cloudprofiler_projects_profiles_patch_execute()`.
/// For task-level control, use `cloudprofiler_projects_profiles_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudprofiler_projects_profiles_patch(
    client: &SimpleHttpClient,
    args: &CloudprofilerProjectsProfilesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Profile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudprofiler_projects_profiles_patch_builder(client, &args.name, &args.updateMask)?;
    cloudprofiler_projects_profiles_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Profile
// =============================================================================

/// ResourceIdentifier implementation for Profile with CloudprofilerProjectsProfilesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudprofilerProjectsProfilesCreateArgs> for Profile {
    fn generate_resource_id(&self, input: &CloudprofilerProjectsProfilesCreateArgs) -> String {
        format!("gcp::cloudprofiler::Profile/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudprofiler::Profile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Profile
// =============================================================================

/// ResourceIdentifier implementation for Profile with CloudprofilerProjectsProfilesCreateOfflineArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudprofilerProjectsProfilesCreateOfflineArgs> for Profile {
    fn generate_resource_id(
        &self,
        input: &CloudprofilerProjectsProfilesCreateOfflineArgs,
    ) -> String {
        format!("gcp::cloudprofiler::Profile/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudprofiler::Profile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListProfilesResponse with CloudprofilerProjectsProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudprofilerProjectsProfilesListArgs> for ListProfilesResponse {
    fn generate_resource_id(&self, input: &CloudprofilerProjectsProfilesListArgs) -> String {
        format!("gcp::cloudprofiler::ListProfilesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudprofiler::ListProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Profile
// =============================================================================

/// ResourceIdentifier implementation for Profile with CloudprofilerProjectsProfilesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudprofilerProjectsProfilesPatchArgs> for Profile {
    fn generate_resource_id(&self, input: &CloudprofilerProjectsProfilesPatchArgs) -> String {
        format!("gcp::cloudprofiler::Profile/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudprofiler::Profile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
