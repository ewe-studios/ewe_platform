//! Auto-generated API clients for gcp/classroom.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/courses
/// Creates a course. The user specified in `ownerId` is the owner of the created course and added as a teacher. A non-admin requesting user can only create a course with themselves as the owner. Domain admins can create courses owned by any user within their domain. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create courses or for access errors. * NOT_FOUND if the primary teacher is not a valid user. * FAILED_PRECONDITION if the course owner's account is disabled or for the following request errors: * UserCannotOwnCourse * UserGroupsMembershipLimitReached * CourseTitleCannotContainUrl * ALREADY_EXISTS if an alias was specified in the id and already exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_create_execute()` to send, or `classroom_courses_create` for simplest API.

pub fn classroom_courses_create_builder(
    client: &SimpleHttpClient,
    body: &Course,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://classroom.googleapis.com/v1/courses",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses
/// Creates a course. The user specified in `ownerId` is the owner of the created course and added as a teacher. A non-admin requesting user can only create a course with themselves as the owner. Domain admins can create courses owned by any user within their domain. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create courses or for access errors. * NOT_FOUND if the primary teacher is not a valid user. * FAILED_PRECONDITION if the course owner's account is disabled or for the following request errors: * UserCannotOwnCourse * UserGroupsMembershipLimitReached * CourseTitleCannotContainUrl * ALREADY_EXISTS if an alias was specified in the id and already exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_create_execute()` or `classroom_courses_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Course>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Course = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses
/// Creates a course. The user specified in `ownerId` is the owner of the created course and added as a teacher. A non-admin requesting user can only create a course with themselves as the owner. Domain admins can create courses owned by any user within their domain. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create courses or for access errors. * NOT_FOUND if the primary teacher is not a valid user. * FAILED_PRECONDITION if the course owner's account is disabled or for the following request errors: * UserCannotOwnCourse * UserGroupsMembershipLimitReached * CourseTitleCannotContainUrl * ALREADY_EXISTS if an alias was specified in the id and already exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_create_task()`.
/// For the simplest API, use `classroom_courses_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Course>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCreateArgs {
    /// Request body.
    pub body: Course,
}

/// GET v1/courses
/// Creates a course. The user specified in `ownerId` is the owner of the created course and added as a teacher. A non-admin requesting user can only create a course with themselves as the owner. Domain admins can create courses owned by any user within their domain. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create courses or for access errors. * NOT_FOUND if the primary teacher is not a valid user. * FAILED_PRECONDITION if the course owner's account is disabled or for the following request errors: * UserCannotOwnCourse * UserGroupsMembershipLimitReached * CourseTitleCannotContainUrl * ALREADY_EXISTS if an alias was specified in the id and already exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_create_builder()` + `classroom_courses_create_execute()`.
/// For task-level control, use `classroom_courses_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Course>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_create_builder(client, &args.body)?;
    classroom_courses_create_execute(builder)
}

/// GET v1/courses/{id}
/// Deletes a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested course or for access errors. * NOT_FOUND if no course exists with the requested ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_delete_execute()` to send, or `classroom_courses_delete` for simplest API.

pub fn classroom_courses_delete_builder(
    client: &SimpleHttpClient,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://classroom.googleapis.com/v1/courses/{}", id,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{id}
/// Deletes a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested course or for access errors. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_delete_execute()` or `classroom_courses_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{id}
/// Deletes a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested course or for access errors. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_delete_task()`.
/// For the simplest API, use `classroom_courses_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesDeleteArgs {
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{id}
/// Deletes a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested course or for access errors. * NOT_FOUND if no course exists with the requested ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_delete_builder()` + `classroom_courses_delete_execute()`.
/// For task-level control, use `classroom_courses_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_delete_builder(client, &args.id)?;
    classroom_courses_delete_execute(builder)
}

/// GET v1/courses/{courseId}/gradingPeriodSettings
/// Returns the grading period settings in a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to access the grading period settings in the requested course or for access errors. * NOT_FOUND if the requested course does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_get_grading_period_settings_execute()` to send, or `classroom_courses_get_grading_period_settings` for simplest API.

pub fn classroom_courses_get_grading_period_settings_builder(
    client: &SimpleHttpClient,
    courseId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/gradingPeriodSettings",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/gradingPeriodSettings
/// Returns the grading period settings in a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to access the grading period settings in the requested course or for access errors. * NOT_FOUND if the requested course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_get_grading_period_settings_execute()` or `classroom_courses_get_grading_period_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_get_grading_period_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_get_grading_period_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GradingPeriodSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GradingPeriodSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/gradingPeriodSettings
/// Returns the grading period settings in a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to access the grading period settings in the requested course or for access errors. * NOT_FOUND if the requested course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_get_grading_period_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_get_grading_period_settings_task()`.
/// For the simplest API, use `classroom_courses_get_grading_period_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_get_grading_period_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_get_grading_period_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GradingPeriodSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_get_grading_period_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_get_grading_period_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesGetGradingPeriodSettingsArgs {
    /// Path parameter: courseId
    pub courseId: String,
}

/// GET v1/courses/{courseId}/gradingPeriodSettings
/// Returns the grading period settings in a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to access the grading period settings in the requested course or for access errors. * NOT_FOUND if the requested course does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_get_grading_period_settings_builder()` + `classroom_courses_get_grading_period_settings_execute()`.
/// For task-level control, use `classroom_courses_get_grading_period_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_get_grading_period_settings(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesGetGradingPeriodSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GradingPeriodSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_get_grading_period_settings_builder(client, &args.courseId)?;
    classroom_courses_get_grading_period_settings_execute(builder)
}

/// GET v1/courses/{courseId}/aliases
/// Creates an alias for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the alias or for access errors. * NOT_FOUND if the course does not exist. * ALREADY_EXISTS if the alias already exists. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to access a domain-scoped alias).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_aliases_create_execute()` to send, or `classroom_courses_aliases_create` for simplest API.

pub fn classroom_courses_aliases_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    body: &CourseAlias,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/aliases",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/aliases
/// Creates an alias for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the alias or for access errors. * NOT_FOUND if the course does not exist. * ALREADY_EXISTS if the alias already exists. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to access a domain-scoped alias).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_aliases_create_execute()` or `classroom_courses_aliases_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_aliases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_aliases_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CourseAlias>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CourseAlias = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/aliases
/// Creates an alias for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the alias or for access errors. * NOT_FOUND if the course does not exist. * ALREADY_EXISTS if the alias already exists. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to access a domain-scoped alias).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_aliases_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_aliases_create_task()`.
/// For the simplest API, use `classroom_courses_aliases_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_aliases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_aliases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseAlias>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_aliases_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_aliases_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAliasesCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Request body.
    pub body: CourseAlias,
}

/// GET v1/courses/{courseId}/aliases
/// Creates an alias for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the alias or for access errors. * NOT_FOUND if the course does not exist. * ALREADY_EXISTS if the alias already exists. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to access a domain-scoped alias).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_aliases_create_builder()` + `classroom_courses_aliases_create_execute()`.
/// For task-level control, use `classroom_courses_aliases_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_aliases_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAliasesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseAlias>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_aliases_create_builder(client, &args.courseId, &args.body)?;
    classroom_courses_aliases_create_execute(builder)
}

/// GET v1/courses/{courseId}/aliases/{alias}
/// Deletes an alias of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to remove the alias or for access errors. * NOT_FOUND if the alias does not exist. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to delete a domain-scoped alias).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_aliases_delete_execute()` to send, or `classroom_courses_aliases_delete` for simplest API.

pub fn classroom_courses_aliases_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    alias: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/aliases/{}",
        courseId, alias,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/aliases/{alias}
/// Deletes an alias of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to remove the alias or for access errors. * NOT_FOUND if the alias does not exist. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to delete a domain-scoped alias).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_aliases_delete_execute()` or `classroom_courses_aliases_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_aliases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_aliases_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/aliases/{alias}
/// Deletes an alias of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to remove the alias or for access errors. * NOT_FOUND if the alias does not exist. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to delete a domain-scoped alias).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_aliases_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_aliases_delete_task()`.
/// For the simplest API, use `classroom_courses_aliases_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_aliases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_aliases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_aliases_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_aliases_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAliasesDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: alias
    pub alias: String,
}

/// GET v1/courses/{courseId}/aliases/{alias}
/// Deletes an alias of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to remove the alias or for access errors. * NOT_FOUND if the alias does not exist. * FAILED_PRECONDITION if the alias requested does not make sense for the requesting user or course (for example, if a user not in a domain attempts to delete a domain-scoped alias).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_aliases_delete_builder()` + `classroom_courses_aliases_delete_execute()`.
/// For task-level control, use `classroom_courses_aliases_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_aliases_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAliasesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_aliases_delete_builder(client, &args.courseId, &args.alias)?;
    classroom_courses_aliases_delete_execute(builder)
}

/// GET v1/courses/{courseId}/announcements
/// Creates an announcement. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create announcements in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_announcements_create_execute()` to send, or `classroom_courses_announcements_create` for simplest API.

pub fn classroom_courses_announcements_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    body: &Announcement,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/announcements",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/announcements
/// Creates an announcement. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create announcements in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_announcements_create_execute()` or `classroom_courses_announcements_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Announcement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Announcement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/announcements
/// Creates an announcement. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create announcements in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_announcements_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_announcements_create_task()`.
/// For the simplest API, use `classroom_courses_announcements_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_announcements_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Announcement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_announcements_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_announcements_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAnnouncementsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Request body.
    pub body: Announcement,
}

/// GET v1/courses/{courseId}/announcements
/// Creates an announcement. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create announcements in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_announcements_create_builder()` + `classroom_courses_announcements_create_execute()`.
/// For task-level control, use `classroom_courses_announcements_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAnnouncementsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Announcement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        classroom_courses_announcements_create_builder(client, &args.courseId, &args.body)?;
    classroom_courses_announcements_create_execute(builder)
}

/// GET v1/courses/{courseId}/announcements/{id}
/// Deletes an announcement. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding announcement item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding announcement, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested announcement has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_announcements_delete_execute()` to send, or `classroom_courses_announcements_delete` for simplest API.

pub fn classroom_courses_announcements_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/announcements/{}",
        courseId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/announcements/{id}
/// Deletes an announcement. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding announcement item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding announcement, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested announcement has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_announcements_delete_execute()` or `classroom_courses_announcements_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/announcements/{id}
/// Deletes an announcement. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding announcement item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding announcement, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested announcement has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_announcements_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_announcements_delete_task()`.
/// For the simplest API, use `classroom_courses_announcements_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_announcements_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_announcements_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_announcements_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAnnouncementsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{courseId}/announcements/{id}
/// Deletes an announcement. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding announcement item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding announcement, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested announcement has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_announcements_delete_builder()` + `classroom_courses_announcements_delete_execute()`.
/// For task-level control, use `classroom_courses_announcements_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAnnouncementsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_announcements_delete_builder(client, &args.courseId, &args.id)?;
    classroom_courses_announcements_delete_execute(builder)
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_announcements_get_add_on_context_execute()` to send, or `classroom_courses_announcements_get_add_on_context` for simplest API.

pub fn classroom_courses_announcements_get_add_on_context_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    addOnToken: &Option<String>,
    attachmentId: &Option<String>,
    postId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/announcements/{}/addOnContext",
        courseId, itemId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = attachmentId.as_ref() {
        query_parts.push(format!("attachmentId={}", val));
    }
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_announcements_get_add_on_context_execute()` or `classroom_courses_announcements_get_add_on_context`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_get_add_on_context_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnContext>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnContext = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_announcements_get_add_on_context_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_announcements_get_add_on_context_task()`.
/// For the simplest API, use `classroom_courses_announcements_get_add_on_context()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_announcements_get_add_on_context_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_announcements_get_add_on_context_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_announcements_get_add_on_context`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAnnouncementsGetAddOnContextArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: attachmentId
    pub attachmentId: Option<String>,
    /// Query parameter: postId
    pub postId: Option<String>,
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_announcements_get_add_on_context_builder()` + `classroom_courses_announcements_get_add_on_context_execute()`.
/// For task-level control, use `classroom_courses_announcements_get_add_on_context_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_get_add_on_context(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAnnouncementsGetAddOnContextArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_announcements_get_add_on_context_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.addOnToken,
        &args.attachmentId,
        &args.postId,
    )?;
    classroom_courses_announcements_get_add_on_context_execute(builder)
}

/// GET v1/courses/{courseId}/announcements/{id}:modifyAssignees
/// Modifies assignee mode and options of an announcement. Only a teacher of the course that contains the announcement may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_announcements_modify_assignees_execute()` to send, or `classroom_courses_announcements_modify_assignees` for simplest API.

pub fn classroom_courses_announcements_modify_assignees_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    id: &String,
    body: &ModifyAnnouncementAssigneesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/announcements/{}:modifyAssignees",
        courseId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/announcements/{id}:modifyAssignees
/// Modifies assignee mode and options of an announcement. Only a teacher of the course that contains the announcement may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_announcements_modify_assignees_execute()` or `classroom_courses_announcements_modify_assignees`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_modify_assignees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_modify_assignees_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Announcement>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Announcement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/announcements/{id}:modifyAssignees
/// Modifies assignee mode and options of an announcement. Only a teacher of the course that contains the announcement may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_announcements_modify_assignees_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_announcements_modify_assignees_task()`.
/// For the simplest API, use `classroom_courses_announcements_modify_assignees()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_modify_assignees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_announcements_modify_assignees_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Announcement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_announcements_modify_assignees_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_announcements_modify_assignees`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAnnouncementsModifyAssigneesArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: id
    pub id: String,
    /// Request body.
    pub body: ModifyAnnouncementAssigneesRequest,
}

/// GET v1/courses/{courseId}/announcements/{id}:modifyAssignees
/// Modifies assignee mode and options of an announcement. Only a teacher of the course that contains the announcement may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_announcements_modify_assignees_builder()` + `classroom_courses_announcements_modify_assignees_execute()`.
/// For task-level control, use `classroom_courses_announcements_modify_assignees_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_modify_assignees(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAnnouncementsModifyAssigneesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Announcement>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_announcements_modify_assignees_builder(
        client,
        &args.courseId,
        &args.id,
        &args.body,
    )?;
    classroom_courses_announcements_modify_assignees_execute(builder)
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_announcements_add_on_attachments_create_execute()` to send, or `classroom_courses_announcements_add_on_attachments_create` for simplest API.

pub fn classroom_courses_announcements_add_on_attachments_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    addOnToken: &Option<String>,
    postId: &Option<String>,
    body: &AddOnAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/announcements/{}/addOnAttachments",
        courseId, itemId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_announcements_add_on_attachments_create_execute()` or `classroom_courses_announcements_add_on_attachments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_add_on_attachments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnAttachment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_announcements_add_on_attachments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_announcements_add_on_attachments_create_task()`.
/// For the simplest API, use `classroom_courses_announcements_add_on_attachments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_announcements_add_on_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_announcements_add_on_attachments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_announcements_add_on_attachments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAnnouncementsAddOnAttachmentsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: postId
    pub postId: Option<String>,
    /// Request body.
    pub body: AddOnAttachment,
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_announcements_add_on_attachments_create_builder()` + `classroom_courses_announcements_add_on_attachments_create_execute()`.
/// For task-level control, use `classroom_courses_announcements_add_on_attachments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_add_on_attachments_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAnnouncementsAddOnAttachmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_announcements_add_on_attachments_create_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.addOnToken,
        &args.postId,
        &args.body,
    )?;
    classroom_courses_announcements_add_on_attachments_create_execute(builder)
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_announcements_add_on_attachments_delete_execute()` to send, or `classroom_courses_announcements_add_on_attachments_delete` for simplest API.

pub fn classroom_courses_announcements_add_on_attachments_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    attachmentId: &String,
    postId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/announcements/{}/addOnAttachments/{}",
        courseId, itemId, attachmentId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_announcements_add_on_attachments_delete_execute()` or `classroom_courses_announcements_add_on_attachments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_add_on_attachments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_announcements_add_on_attachments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_announcements_add_on_attachments_delete_task()`.
/// For the simplest API, use `classroom_courses_announcements_add_on_attachments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_announcements_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_announcements_add_on_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_announcements_add_on_attachments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_announcements_add_on_attachments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesAnnouncementsAddOnAttachmentsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Path parameter: attachmentId
    pub attachmentId: String,
    /// Query parameter: postId
    pub postId: Option<String>,
}

/// GET v1/courses/{courseId}/announcements/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_announcements_add_on_attachments_delete_builder()` + `classroom_courses_announcements_add_on_attachments_delete_execute()`.
/// For task-level control, use `classroom_courses_announcements_add_on_attachments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_announcements_add_on_attachments_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesAnnouncementsAddOnAttachmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_announcements_add_on_attachments_delete_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.attachmentId,
        &args.postId,
    )?;
    classroom_courses_announcements_add_on_attachments_delete_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork
/// Creates course work. The resulting course work (and corresponding student submissions) are associated with the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to make the request. Classroom API requests to modify course work and student submissions must be made with an OAuth client ID from the associated Developer Console project. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_create_execute()` to send, or `classroom_courses_course_work_create` for simplest API.

pub fn classroom_courses_course_work_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    body: &CourseWork,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork
/// Creates course work. The resulting course work (and corresponding student submissions) are associated with the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to make the request. Classroom API requests to modify course work and student submissions must be made with an OAuth client ID from the associated Developer Console project. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_create_execute()` or `classroom_courses_course_work_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CourseWork>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CourseWork = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork
/// Creates course work. The resulting course work (and corresponding student submissions) are associated with the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to make the request. Classroom API requests to modify course work and student submissions must be made with an OAuth client ID from the associated Developer Console project. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_create_task()`.
/// For the simplest API, use `classroom_courses_course_work_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseWork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Request body.
    pub body: CourseWork,
}

/// GET v1/courses/{courseId}/courseWork
/// Creates course work. The resulting course work (and corresponding student submissions) are associated with the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to make the request. Classroom API requests to modify course work and student submissions must be made with an OAuth client ID from the associated Developer Console project. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_create_builder()` + `classroom_courses_course_work_create_execute()`.
/// For task-level control, use `classroom_courses_course_work_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseWork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_create_builder(client, &args.courseId, &args.body)?;
    classroom_courses_course_work_create_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{id}
/// Deletes a course work. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_delete_execute()` to send, or `classroom_courses_course_work_delete` for simplest API.

pub fn classroom_courses_course_work_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}",
        courseId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWork/{id}
/// Deletes a course work. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_delete_execute()` or `classroom_courses_course_work_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{id}
/// Deletes a course work. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_delete_task()`.
/// For the simplest API, use `classroom_courses_course_work_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{courseId}/courseWork/{id}
/// Deletes a course work. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_delete_builder()` + `classroom_courses_course_work_delete_execute()`.
/// For task-level control, use `classroom_courses_course_work_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_delete_builder(client, &args.courseId, &args.id)?;
    classroom_courses_course_work_delete_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_get_add_on_context_execute()` to send, or `classroom_courses_course_work_get_add_on_context` for simplest API.

pub fn classroom_courses_course_work_get_add_on_context_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    addOnToken: &Option<String>,
    attachmentId: &Option<String>,
    postId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/addOnContext",
        courseId, itemId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = attachmentId.as_ref() {
        query_parts.push(format!("attachmentId={}", val));
    }
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_get_add_on_context_execute()` or `classroom_courses_course_work_get_add_on_context`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_get_add_on_context_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnContext>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnContext = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_get_add_on_context_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_get_add_on_context_task()`.
/// For the simplest API, use `classroom_courses_course_work_get_add_on_context()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_get_add_on_context_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_get_add_on_context_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_get_add_on_context`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkGetAddOnContextArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: attachmentId
    pub attachmentId: Option<String>,
    /// Query parameter: postId
    pub postId: Option<String>,
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_get_add_on_context_builder()` + `classroom_courses_course_work_get_add_on_context_execute()`.
/// For task-level control, use `classroom_courses_course_work_get_add_on_context_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_get_add_on_context(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkGetAddOnContextArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_get_add_on_context_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.addOnToken,
        &args.attachmentId,
        &args.postId,
    )?;
    classroom_courses_course_work_get_add_on_context_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{id}:modifyAssignees
/// Modifies assignee mode and options of a coursework. Only a teacher of the course that contains the coursework may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_modify_assignees_execute()` to send, or `classroom_courses_course_work_modify_assignees` for simplest API.

pub fn classroom_courses_course_work_modify_assignees_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    id: &String,
    body: &ModifyCourseWorkAssigneesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}:modifyAssignees",
        courseId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{id}:modifyAssignees
/// Modifies assignee mode and options of a coursework. Only a teacher of the course that contains the coursework may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_modify_assignees_execute()` or `classroom_courses_course_work_modify_assignees`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_modify_assignees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_modify_assignees_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CourseWork>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CourseWork = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{id}:modifyAssignees
/// Modifies assignee mode and options of a coursework. Only a teacher of the course that contains the coursework may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_modify_assignees_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_modify_assignees_task()`.
/// For the simplest API, use `classroom_courses_course_work_modify_assignees()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_modify_assignees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_modify_assignees_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseWork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_modify_assignees_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_modify_assignees`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkModifyAssigneesArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: id
    pub id: String,
    /// Request body.
    pub body: ModifyCourseWorkAssigneesRequest,
}

/// GET v1/courses/{courseId}/courseWork/{id}:modifyAssignees
/// Modifies assignee mode and options of a coursework. Only a teacher of the course that contains the coursework may call this method. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course or course work does not exist. * FAILED_PRECONDITION for the following request error: * EmptyAssignees
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_modify_assignees_builder()` + `classroom_courses_course_work_modify_assignees_execute()`.
/// For task-level control, use `classroom_courses_course_work_modify_assignees_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_modify_assignees(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkModifyAssigneesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseWork>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_modify_assignees_builder(
        client,
        &args.courseId,
        &args.id,
        &args.body,
    )?;
    classroom_courses_course_work_modify_assignees_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubric
/// Updates a rubric. See google.classroom.v1.Rubric for details of which fields can be updated. Rubric update capabilities are [limited](/`classroom/rubrics/limitations`) once grading has started. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding course work, if the user isn't permitted to make the requested modification to the rubric, or for access errors. This error code is also returned if grading has already started on the rubric. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course, course work, or rubric doesn't exist or if the user doesn't have access to the corresponding course work. * INTERNAL if grading has already started on the rubric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_update_rubric_execute()` to send, or `classroom_courses_course_work_update_rubric` for simplest API.

pub fn classroom_courses_course_work_update_rubric_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    id: &Option<String>,
    updateMask: &Option<String>,
    body: &Rubric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/rubric",
        courseId, courseWorkId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id.as_ref() {
        query_parts.push(format!("id={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubric
/// Updates a rubric. See google.classroom.v1.Rubric for details of which fields can be updated. Rubric update capabilities are [limited](/`classroom/rubrics/limitations`) once grading has started. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding course work, if the user isn't permitted to make the requested modification to the rubric, or for access errors. This error code is also returned if grading has already started on the rubric. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course, course work, or rubric doesn't exist or if the user doesn't have access to the corresponding course work. * INTERNAL if grading has already started on the rubric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_update_rubric_execute()` or `classroom_courses_course_work_update_rubric`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_update_rubric_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_update_rubric_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Rubric>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Rubric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubric
/// Updates a rubric. See google.classroom.v1.Rubric for details of which fields can be updated. Rubric update capabilities are [limited](/`classroom/rubrics/limitations`) once grading has started. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding course work, if the user isn't permitted to make the requested modification to the rubric, or for access errors. This error code is also returned if grading has already started on the rubric. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course, course work, or rubric doesn't exist or if the user doesn't have access to the corresponding course work. * INTERNAL if grading has already started on the rubric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_update_rubric_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_update_rubric_task()`.
/// For the simplest API, use `classroom_courses_course_work_update_rubric()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_update_rubric_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_update_rubric_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rubric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_update_rubric_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_update_rubric`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkUpdateRubricArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Query parameter: id
    pub id: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Rubric,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubric
/// Updates a rubric. See google.classroom.v1.Rubric for details of which fields can be updated. Rubric update capabilities are [limited](/`classroom/rubrics/limitations`) once grading has started. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding course work, if the user isn't permitted to make the requested modification to the rubric, or for access errors. This error code is also returned if grading has already started on the rubric. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course, course work, or rubric doesn't exist or if the user doesn't have access to the corresponding course work. * INTERNAL if grading has already started on the rubric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_update_rubric_builder()` + `classroom_courses_course_work_update_rubric_execute()`.
/// For task-level control, use `classroom_courses_course_work_update_rubric_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_update_rubric(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkUpdateRubricArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rubric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_update_rubric_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.id,
        &args.updateMask,
        &args.body,
    )?;
    classroom_courses_course_work_update_rubric_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_add_on_attachments_create_execute()` to send, or `classroom_courses_course_work_add_on_attachments_create` for simplest API.

pub fn classroom_courses_course_work_add_on_attachments_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    addOnToken: &Option<String>,
    postId: &Option<String>,
    body: &AddOnAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/addOnAttachments",
        courseId, itemId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_add_on_attachments_create_execute()` or `classroom_courses_course_work_add_on_attachments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_add_on_attachments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnAttachment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_add_on_attachments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_add_on_attachments_create_task()`.
/// For the simplest API, use `classroom_courses_course_work_add_on_attachments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_add_on_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_add_on_attachments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_add_on_attachments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkAddOnAttachmentsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: postId
    pub postId: Option<String>,
    /// Request body.
    pub body: AddOnAttachment,
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_add_on_attachments_create_builder()` + `classroom_courses_course_work_add_on_attachments_create_execute()`.
/// For task-level control, use `classroom_courses_course_work_add_on_attachments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_add_on_attachments_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkAddOnAttachmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_add_on_attachments_create_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.addOnToken,
        &args.postId,
        &args.body,
    )?;
    classroom_courses_course_work_add_on_attachments_create_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_add_on_attachments_delete_execute()` to send, or `classroom_courses_course_work_add_on_attachments_delete` for simplest API.

pub fn classroom_courses_course_work_add_on_attachments_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    attachmentId: &String,
    postId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/addOnAttachments/{}",
        courseId, itemId, attachmentId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_add_on_attachments_delete_execute()` or `classroom_courses_course_work_add_on_attachments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_add_on_attachments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_add_on_attachments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_add_on_attachments_delete_task()`.
/// For the simplest API, use `classroom_courses_course_work_add_on_attachments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_add_on_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_add_on_attachments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_add_on_attachments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkAddOnAttachmentsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Path parameter: attachmentId
    pub attachmentId: String,
    /// Query parameter: postId
    pub postId: Option<String>,
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_add_on_attachments_delete_builder()` + `classroom_courses_course_work_add_on_attachments_delete_execute()`.
/// For task-level control, use `classroom_courses_course_work_add_on_attachments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_add_on_attachments_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkAddOnAttachmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_add_on_attachments_delete_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.attachmentId,
        &args.postId,
    )?;
    classroom_courses_course_work_add_on_attachments_delete_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_add_on_attachments_student_submissions_get_execute()` to send, or `classroom_courses_course_work_add_on_attachments_student_submissions_get` for simplest API.

pub fn classroom_courses_course_work_add_on_attachments_student_submissions_get_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    attachmentId: &String,
    submissionId: &String,
    postId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/addOnAttachments/{}/studentSubmissions/{}",
        courseId,
        itemId,
        attachmentId,
        submissionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_add_on_attachments_student_submissions_get_execute()` or `classroom_courses_course_work_add_on_attachments_student_submissions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_add_on_attachments_student_submissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_add_on_attachments_student_submissions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnAttachmentStudentSubmission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnAttachmentStudentSubmission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_add_on_attachments_student_submissions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_add_on_attachments_student_submissions_get_task()`.
/// For the simplest API, use `classroom_courses_course_work_add_on_attachments_student_submissions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_add_on_attachments_student_submissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_add_on_attachments_student_submissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AddOnAttachmentStudentSubmission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        classroom_courses_course_work_add_on_attachments_student_submissions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_add_on_attachments_student_submissions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkAddOnAttachmentsStudentSubmissionsGetArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Path parameter: attachmentId
    pub attachmentId: String,
    /// Path parameter: submissionId
    pub submissionId: String,
    /// Query parameter: postId
    pub postId: Option<String>,
}

/// GET v1/courses/{courseId}/courseWork/{itemId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_add_on_attachments_student_submissions_get_builder()` + `classroom_courses_course_work_add_on_attachments_student_submissions_get_execute()`.
/// For task-level control, use `classroom_courses_course_work_add_on_attachments_student_submissions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_add_on_attachments_student_submissions_get(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkAddOnAttachmentsStudentSubmissionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AddOnAttachmentStudentSubmission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_add_on_attachments_student_submissions_get_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.attachmentId,
        &args.submissionId,
        &args.postId,
    )?;
    classroom_courses_course_work_add_on_attachments_student_submissions_get_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics
/// Creates a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). For further details, see [Rubrics structure and known limitations](/`classroom/rubrics/limitations`). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to create rubrics for course work in the requested course. * INTERNAL if the request has insufficient OAuth scopes. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course or course work don't exist or the user doesn't have access to the course or course work. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_rubrics_create_execute()` to send, or `classroom_courses_course_work_rubrics_create` for simplest API.

pub fn classroom_courses_course_work_rubrics_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    body: &Rubric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/rubrics",
        courseId, courseWorkId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics
/// Creates a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). For further details, see [Rubrics structure and known limitations](/`classroom/rubrics/limitations`). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to create rubrics for course work in the requested course. * INTERNAL if the request has insufficient OAuth scopes. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course or course work don't exist or the user doesn't have access to the course or course work. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_rubrics_create_execute()` or `classroom_courses_course_work_rubrics_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_rubrics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_rubrics_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Rubric>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Rubric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics
/// Creates a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). For further details, see [Rubrics structure and known limitations](/`classroom/rubrics/limitations`). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to create rubrics for course work in the requested course. * INTERNAL if the request has insufficient OAuth scopes. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course or course work don't exist or the user doesn't have access to the course or course work. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_rubrics_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_rubrics_create_task()`.
/// For the simplest API, use `classroom_courses_course_work_rubrics_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_rubrics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_rubrics_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rubric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_rubrics_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_rubrics_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkRubricsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Request body.
    pub body: Rubric,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics
/// Creates a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). For further details, see [Rubrics structure and known limitations](/`classroom/rubrics/limitations`). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the parent course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user isn't permitted to create rubrics for course work in the requested course. * INTERNAL if the request has insufficient OAuth scopes. * INVALID_ARGUMENT if the request is malformed and for the following request error: * RubricCriteriaInvalidFormat * NOT_FOUND if the requested course or course work don't exist or the user doesn't have access to the course or course work. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_rubrics_create_builder()` + `classroom_courses_course_work_rubrics_create_execute()`.
/// For task-level control, use `classroom_courses_course_work_rubrics_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_rubrics_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkRubricsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rubric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_rubrics_create_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.body,
    )?;
    classroom_courses_course_work_rubrics_create_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics/{id}
/// Deletes a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding rubric. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding rubric, or if the requesting user isn't permitted to delete the requested rubric. * NOT_FOUND if no rubric exists with the requested ID or the user does not have access to the course, course work, or rubric. * INVALID_ARGUMENT if grading has already started on the rubric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_rubrics_delete_execute()` to send, or `classroom_courses_course_work_rubrics_delete` for simplest API.

pub fn classroom_courses_course_work_rubrics_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/rubrics/{}",
        courseId, courseWorkId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics/{id}
/// Deletes a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding rubric. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding rubric, or if the requesting user isn't permitted to delete the requested rubric. * NOT_FOUND if no rubric exists with the requested ID or the user does not have access to the course, course work, or rubric. * INVALID_ARGUMENT if grading has already started on the rubric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_rubrics_delete_execute()` or `classroom_courses_course_work_rubrics_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_rubrics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_rubrics_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics/{id}
/// Deletes a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding rubric. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding rubric, or if the requesting user isn't permitted to delete the requested rubric. * NOT_FOUND if no rubric exists with the requested ID or the user does not have access to the course, course work, or rubric. * INVALID_ARGUMENT if grading has already started on the rubric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_rubrics_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_rubrics_delete_task()`.
/// For the simplest API, use `classroom_courses_course_work_rubrics_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_rubrics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_rubrics_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_rubrics_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_rubrics_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkRubricsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/rubrics/{id}
/// Deletes a rubric. The requesting user and course owner must have rubrics creation capabilities. For details, see [licensing requirements](<https://developers.google.`com/workspace/classroom/rubrics/limitations`#license-requirements>). This request must be made by the Google Cloud console of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding rubric. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project didn't create the corresponding rubric, or if the requesting user isn't permitted to delete the requested rubric. * NOT_FOUND if no rubric exists with the requested ID or the user does not have access to the course, course work, or rubric. * INVALID_ARGUMENT if grading has already started on the rubric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_rubrics_delete_builder()` + `classroom_courses_course_work_rubrics_delete_execute()`.
/// For task-level control, use `classroom_courses_course_work_rubrics_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_rubrics_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkRubricsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_rubrics_delete_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.id,
    )?;
    classroom_courses_course_work_rubrics_delete_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}
/// Returns a student submission. * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, course work, or student submission or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_student_submissions_get_execute()` to send, or `classroom_courses_course_work_student_submissions_get` for simplest API.

pub fn classroom_courses_course_work_student_submissions_get_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/studentSubmissions/{}",
        courseId, courseWorkId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}
/// Returns a student submission. * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, course work, or student submission or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_student_submissions_get_execute()` or `classroom_courses_course_work_student_submissions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudentSubmission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudentSubmission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}
/// Returns a student submission. * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, course work, or student submission or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_student_submissions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_student_submissions_get_task()`.
/// For the simplest API, use `classroom_courses_course_work_student_submissions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_student_submissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentSubmission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_student_submissions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_student_submissions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkStudentSubmissionsGetArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}
/// Returns a student submission. * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, course work, or student submission or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_student_submissions_get_builder()` + `classroom_courses_course_work_student_submissions_get_execute()`.
/// For task-level control, use `classroom_courses_course_work_student_submissions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_get(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkStudentSubmissionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentSubmission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_student_submissions_get_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.id,
    )?;
    classroom_courses_course_work_student_submissions_get_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions
/// Returns a list of student submissions that the requester is permitted to view, factoring in the OAuth scopes of the request. A hyphen (-) may be specified as the course_work_id to include student submissions for multiple course work items. Course students may only view their own work. Course teachers and domain administrators may view all student submissions. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_student_submissions_list_execute()` to send, or `classroom_courses_course_work_student_submissions_list` for simplest API.

pub fn classroom_courses_course_work_student_submissions_list_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    late: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    states: &Option<String>,
    userId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/studentSubmissions",
        courseId, courseWorkId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = late.as_ref() {
        query_parts.push(format!("late={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = states.as_ref() {
        query_parts.push(format!("states={}", val));
    }
    if let Some(val) = userId.as_ref() {
        query_parts.push(format!("userId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions
/// Returns a list of student submissions that the requester is permitted to view, factoring in the OAuth scopes of the request. A hyphen (-) may be specified as the course_work_id to include student submissions for multiple course work items. Course students may only view their own work. Course teachers and domain administrators may view all student submissions. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_student_submissions_list_execute()` or `classroom_courses_course_work_student_submissions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListStudentSubmissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListStudentSubmissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions
/// Returns a list of student submissions that the requester is permitted to view, factoring in the OAuth scopes of the request. A hyphen (-) may be specified as the course_work_id to include student submissions for multiple course work items. Course students may only view their own work. Course teachers and domain administrators may view all student submissions. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_student_submissions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_student_submissions_list_task()`.
/// For the simplest API, use `classroom_courses_course_work_student_submissions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_student_submissions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListStudentSubmissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_student_submissions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_student_submissions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkStudentSubmissionsListArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Query parameter: late
    pub late: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: states
    pub states: Option<String>,
    /// Query parameter: userId
    pub userId: Option<String>,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions
/// Returns a list of student submissions that the requester is permitted to view, factoring in the OAuth scopes of the request. A hyphen (-) may be specified as the course_work_id to include student submissions for multiple course work items. Course students may only view their own work. Course teachers and domain administrators may view all student submissions. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_student_submissions_list_builder()` + `classroom_courses_course_work_student_submissions_list_execute()`.
/// For task-level control, use `classroom_courses_course_work_student_submissions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_list(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkStudentSubmissionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListStudentSubmissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_student_submissions_list_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.late,
        &args.pageSize,
        &args.pageToken,
        &args.states,
        &args.userId,
    )?;
    classroom_courses_course_work_student_submissions_list_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:modifyAttachments
/// Modifies attachments of student submission. Attachments may only be added to student submissions belonging to course work objects with a `workType` of ASSIGNMENT. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, if the user is not permitted to modify attachments on the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_student_submissions_modify_attachments_execute()` to send, or `classroom_courses_course_work_student_submissions_modify_attachments` for simplest API.

pub fn classroom_courses_course_work_student_submissions_modify_attachments_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    id: &String,
    body: &ModifyAttachmentsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/studentSubmissions/{}:modifyAttachments",
        courseId,
        courseWorkId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:modifyAttachments
/// Modifies attachments of student submission. Attachments may only be added to student submissions belonging to course work objects with a `workType` of ASSIGNMENT. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, if the user is not permitted to modify attachments on the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_student_submissions_modify_attachments_execute()` or `classroom_courses_course_work_student_submissions_modify_attachments`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_modify_attachments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_modify_attachments_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudentSubmission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudentSubmission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:modifyAttachments
/// Modifies attachments of student submission. Attachments may only be added to student submissions belonging to course work objects with a `workType` of ASSIGNMENT. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, if the user is not permitted to modify attachments on the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_student_submissions_modify_attachments_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_student_submissions_modify_attachments_task()`.
/// For the simplest API, use `classroom_courses_course_work_student_submissions_modify_attachments()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_modify_attachments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_student_submissions_modify_attachments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentSubmission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_student_submissions_modify_attachments_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_student_submissions_modify_attachments`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkStudentSubmissionsModifyAttachmentsArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Path parameter: id
    pub id: String,
    /// Request body.
    pub body: ModifyAttachmentsRequest,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:modifyAttachments
/// Modifies attachments of student submission. Attachments may only be added to student submissions belonging to course work objects with a `workType` of ASSIGNMENT. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, if the user is not permitted to modify attachments on the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_student_submissions_modify_attachments_builder()` + `classroom_courses_course_work_student_submissions_modify_attachments_execute()`.
/// For task-level control, use `classroom_courses_course_work_student_submissions_modify_attachments_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_modify_attachments(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkStudentSubmissionsModifyAttachmentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentSubmission>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_student_submissions_modify_attachments_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.id,
        &args.body,
    )?;
    classroom_courses_course_work_student_submissions_modify_attachments_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:reclaim
/// Reclaims a student submission on behalf of the student that owns it. Reclaiming a student submission transfers ownership of attached Drive files to the student and updates the submission state. Only the student that owns the requested student submission may call this method, and only for a student submission that has been turned in. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, unsubmit the requested student submission, or for access errors. * FAILED_PRECONDITION if the student submission has not been turned in. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_student_submissions_reclaim_execute()` to send, or `classroom_courses_course_work_student_submissions_reclaim` for simplest API.

pub fn classroom_courses_course_work_student_submissions_reclaim_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    id: &String,
    body: &ReclaimStudentSubmissionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/studentSubmissions/{}:reclaim",
        courseId,
        courseWorkId,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:reclaim
/// Reclaims a student submission on behalf of the student that owns it. Reclaiming a student submission transfers ownership of attached Drive files to the student and updates the submission state. Only the student that owns the requested student submission may call this method, and only for a student submission that has been turned in. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, unsubmit the requested student submission, or for access errors. * FAILED_PRECONDITION if the student submission has not been turned in. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_student_submissions_reclaim_execute()` or `classroom_courses_course_work_student_submissions_reclaim`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_reclaim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_reclaim_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:reclaim
/// Reclaims a student submission on behalf of the student that owns it. Reclaiming a student submission transfers ownership of attached Drive files to the student and updates the submission state. Only the student that owns the requested student submission may call this method, and only for a student submission that has been turned in. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, unsubmit the requested student submission, or for access errors. * FAILED_PRECONDITION if the student submission has not been turned in. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_student_submissions_reclaim_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_student_submissions_reclaim_task()`.
/// For the simplest API, use `classroom_courses_course_work_student_submissions_reclaim()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_reclaim_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_student_submissions_reclaim_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_student_submissions_reclaim_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_student_submissions_reclaim`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkStudentSubmissionsReclaimArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Path parameter: id
    pub id: String,
    /// Request body.
    pub body: ReclaimStudentSubmissionRequest,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:reclaim
/// Reclaims a student submission on behalf of the student that owns it. Reclaiming a student submission transfers ownership of attached Drive files to the student and updates the submission state. Only the student that owns the requested student submission may call this method, and only for a student submission that has been turned in. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, unsubmit the requested student submission, or for access errors. * FAILED_PRECONDITION if the student submission has not been turned in. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_student_submissions_reclaim_builder()` + `classroom_courses_course_work_student_submissions_reclaim_execute()`.
/// For task-level control, use `classroom_courses_course_work_student_submissions_reclaim_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_reclaim(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkStudentSubmissionsReclaimArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_student_submissions_reclaim_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.id,
        &args.body,
    )?;
    classroom_courses_course_work_student_submissions_reclaim_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:return
/// Returns a student submission. Returning a student submission transfers ownership of attached Drive files to the student and may also update the submission state. Unlike the Classroom application, returning a student submission does not set `assignedGrade` to the `draftGrade` value. Only a teacher of the course that contains the requested student submission may call this method. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, return the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_student_submissions_return_execute()` to send, or `classroom_courses_course_work_student_submissions_return` for simplest API.

pub fn classroom_courses_course_work_student_submissions_return_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    id: &String,
    body: &ReturnStudentSubmissionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/studentSubmissions/{}:return",
        courseId, courseWorkId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:return
/// Returns a student submission. Returning a student submission transfers ownership of attached Drive files to the student and may also update the submission state. Unlike the Classroom application, returning a student submission does not set `assignedGrade` to the `draftGrade` value. Only a teacher of the course that contains the requested student submission may call this method. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, return the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_student_submissions_return_execute()` or `classroom_courses_course_work_student_submissions_return`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_return_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_return_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:return
/// Returns a student submission. Returning a student submission transfers ownership of attached Drive files to the student and may also update the submission state. Unlike the Classroom application, returning a student submission does not set `assignedGrade` to the `draftGrade` value. Only a teacher of the course that contains the requested student submission may call this method. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, return the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_student_submissions_return_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_student_submissions_return_task()`.
/// For the simplest API, use `classroom_courses_course_work_student_submissions_return()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_return_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_student_submissions_return_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_student_submissions_return_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_student_submissions_return`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkStudentSubmissionsReturnArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Path parameter: id
    pub id: String,
    /// Request body.
    pub body: ReturnStudentSubmissionRequest,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:return
/// Returns a student submission. Returning a student submission transfers ownership of attached Drive files to the student and may also update the submission state. Unlike the Classroom application, returning a student submission does not set `assignedGrade` to the `draftGrade` value. Only a teacher of the course that contains the requested student submission may call this method. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, return the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_student_submissions_return_builder()` + `classroom_courses_course_work_student_submissions_return_execute()`.
/// For task-level control, use `classroom_courses_course_work_student_submissions_return_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_return(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkStudentSubmissionsReturnArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_student_submissions_return_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.id,
        &args.body,
    )?;
    classroom_courses_course_work_student_submissions_return_execute(builder)
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:turnIn
/// Turns in a student submission. Turning in a student submission transfers ownership of attached Drive files to the teacher and may also update the submission state. This may only be called by the student that owns the specified student submission. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, turn in the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_student_submissions_turn_in_execute()` to send, or `classroom_courses_course_work_student_submissions_turn_in` for simplest API.

pub fn classroom_courses_course_work_student_submissions_turn_in_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    courseWorkId: &String,
    id: &String,
    body: &TurnInStudentSubmissionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWork/{}/studentSubmissions/{}:turnIn",
        courseId, courseWorkId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:turnIn
/// Turns in a student submission. Turning in a student submission transfers ownership of attached Drive files to the teacher and may also update the submission state. This may only be called by the student that owns the specified student submission. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, turn in the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_student_submissions_turn_in_execute()` or `classroom_courses_course_work_student_submissions_turn_in`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_turn_in_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_turn_in_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:turnIn
/// Turns in a student submission. Turning in a student submission transfers ownership of attached Drive files to the teacher and may also update the submission state. This may only be called by the student that owns the specified student submission. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, turn in the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_student_submissions_turn_in_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_student_submissions_turn_in_task()`.
/// For the simplest API, use `classroom_courses_course_work_student_submissions_turn_in()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_student_submissions_turn_in_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_student_submissions_turn_in_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_student_submissions_turn_in_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_student_submissions_turn_in`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkStudentSubmissionsTurnInArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: courseWorkId
    pub courseWorkId: String,
    /// Path parameter: id
    pub id: String,
    /// Request body.
    pub body: TurnInStudentSubmissionRequest,
}

/// GET v1/courses/{courseId}/courseWork/{courseWorkId}/studentSubmissions/{id}:turnIn
/// Turns in a student submission. Turning in a student submission transfers ownership of attached Drive files to the teacher and may also update the submission state. This may only be called by the student that owns the specified student submission. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work item. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course or course work, turn in the requested student submission, or for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if the requested course, course work, or student submission does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_student_submissions_turn_in_builder()` + `classroom_courses_course_work_student_submissions_turn_in_execute()`.
/// For task-level control, use `classroom_courses_course_work_student_submissions_turn_in_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_student_submissions_turn_in(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkStudentSubmissionsTurnInArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_student_submissions_turn_in_builder(
        client,
        &args.courseId,
        &args.courseWorkId,
        &args.id,
        &args.body,
    )?;
    classroom_courses_course_work_student_submissions_turn_in_execute(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials
/// Creates a course work material. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work material in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed or if more than 20 * materials are provided. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_materials_create_execute()` to send, or `classroom_courses_course_work_materials_create` for simplest API.

pub fn classroom_courses_course_work_materials_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    body: &CourseWorkMaterial,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWorkMaterials",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWorkMaterials
/// Creates a course work material. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work material in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed or if more than 20 * materials are provided. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_materials_create_execute()` or `classroom_courses_course_work_materials_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CourseWorkMaterial>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CourseWorkMaterial = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWorkMaterials
/// Creates a course work material. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work material in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed or if more than 20 * materials are provided. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_materials_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_materials_create_task()`.
/// For the simplest API, use `classroom_courses_course_work_materials_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_materials_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseWorkMaterial>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_materials_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_materials_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkMaterialsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Request body.
    pub body: CourseWorkMaterial,
}

/// GET v1/courses/{courseId}/courseWorkMaterials
/// Creates a course work material. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create course work material in the requested course, share a Drive attachment, or for access errors. * INVALID_ARGUMENT if the request is malformed or if more than 20 * materials are provided. * NOT_FOUND if the requested course does not exist. * FAILED_PRECONDITION for the following request error: * AttachmentNotVisible
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_materials_create_builder()` + `classroom_courses_course_work_materials_create_execute()`.
/// For task-level control, use `classroom_courses_course_work_materials_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkMaterialsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CourseWorkMaterial>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        classroom_courses_course_work_materials_create_builder(client, &args.courseId, &args.body)?;
    classroom_courses_course_work_materials_create_execute(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{id}
/// Deletes a course work material. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work material item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work material, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work material has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_materials_delete_execute()` to send, or `classroom_courses_course_work_materials_delete` for simplest API.

pub fn classroom_courses_course_work_materials_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWorkMaterials/{}",
        courseId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{id}
/// Deletes a course work material. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work material item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work material, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work material has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_materials_delete_execute()` or `classroom_courses_course_work_materials_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{id}
/// Deletes a course work material. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work material item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work material, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work material has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_materials_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_materials_delete_task()`.
/// For the simplest API, use `classroom_courses_course_work_materials_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_materials_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_materials_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_materials_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkMaterialsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{id}
/// Deletes a course work material. This request must be made by the Developer Console project of the [OAuth client ID](<https://support.google.`com/cloud/answer/6158849`>) used to create the corresponding course work material item. This method returns the following error codes: * PERMISSION_DENIED if the requesting developer project did not create the corresponding course work material, if the requesting user is not permitted to delete the requested course or for access errors. * FAILED_PRECONDITION if the requested course work material has already been deleted. * NOT_FOUND if no course exists with the requested ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_materials_delete_builder()` + `classroom_courses_course_work_materials_delete_execute()`.
/// For task-level control, use `classroom_courses_course_work_materials_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkMaterialsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        classroom_courses_course_work_materials_delete_builder(client, &args.courseId, &args.id)?;
    classroom_courses_course_work_materials_delete_execute(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_materials_get_add_on_context_execute()` to send, or `classroom_courses_course_work_materials_get_add_on_context` for simplest API.

pub fn classroom_courses_course_work_materials_get_add_on_context_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    addOnToken: &Option<String>,
    attachmentId: &Option<String>,
    postId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWorkMaterials/{}/addOnContext",
        courseId, itemId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = attachmentId.as_ref() {
        query_parts.push(format!("attachmentId={}", val));
    }
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_materials_get_add_on_context_execute()` or `classroom_courses_course_work_materials_get_add_on_context`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_get_add_on_context_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnContext>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnContext = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_materials_get_add_on_context_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_materials_get_add_on_context_task()`.
/// For the simplest API, use `classroom_courses_course_work_materials_get_add_on_context()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_materials_get_add_on_context_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_materials_get_add_on_context_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_materials_get_add_on_context`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkMaterialsGetAddOnContextArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: attachmentId
    pub attachmentId: Option<String>,
    /// Query parameter: postId
    pub postId: Option<String>,
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_materials_get_add_on_context_builder()` + `classroom_courses_course_work_materials_get_add_on_context_execute()`.
/// For task-level control, use `classroom_courses_course_work_materials_get_add_on_context_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_get_add_on_context(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkMaterialsGetAddOnContextArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_materials_get_add_on_context_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.addOnToken,
        &args.attachmentId,
        &args.postId,
    )?;
    classroom_courses_course_work_materials_get_add_on_context_execute(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_materials_add_on_attachments_create_execute()` to send, or `classroom_courses_course_work_materials_add_on_attachments_create` for simplest API.

pub fn classroom_courses_course_work_materials_add_on_attachments_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    addOnToken: &Option<String>,
    postId: &Option<String>,
    body: &AddOnAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWorkMaterials/{}/addOnAttachments",
        courseId, itemId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_materials_add_on_attachments_create_execute()` or `classroom_courses_course_work_materials_add_on_attachments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_add_on_attachments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnAttachment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_materials_add_on_attachments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_materials_add_on_attachments_create_task()`.
/// For the simplest API, use `classroom_courses_course_work_materials_add_on_attachments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_materials_add_on_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_materials_add_on_attachments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_materials_add_on_attachments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkMaterialsAddOnAttachmentsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: postId
    pub postId: Option<String>,
    /// Request body.
    pub body: AddOnAttachment,
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_materials_add_on_attachments_create_builder()` + `classroom_courses_course_work_materials_add_on_attachments_create_execute()`.
/// For task-level control, use `classroom_courses_course_work_materials_add_on_attachments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_add_on_attachments_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkMaterialsAddOnAttachmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_materials_add_on_attachments_create_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.addOnToken,
        &args.postId,
        &args.body,
    )?;
    classroom_courses_course_work_materials_add_on_attachments_create_execute(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_course_work_materials_add_on_attachments_delete_execute()` to send, or `classroom_courses_course_work_materials_add_on_attachments_delete` for simplest API.

pub fn classroom_courses_course_work_materials_add_on_attachments_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    itemId: &String,
    attachmentId: &String,
    postId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/courseWorkMaterials/{}/addOnAttachments/{}",
        courseId, itemId, attachmentId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = postId.as_ref() {
        query_parts.push(format!("postId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_course_work_materials_add_on_attachments_delete_execute()` or `classroom_courses_course_work_materials_add_on_attachments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_add_on_attachments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_course_work_materials_add_on_attachments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_course_work_materials_add_on_attachments_delete_task()`.
/// For the simplest API, use `classroom_courses_course_work_materials_add_on_attachments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_course_work_materials_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_course_work_materials_add_on_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_course_work_materials_add_on_attachments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_course_work_materials_add_on_attachments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesCourseWorkMaterialsAddOnAttachmentsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: itemId
    pub itemId: String,
    /// Path parameter: attachmentId
    pub attachmentId: String,
    /// Query parameter: postId
    pub postId: Option<String>,
}

/// GET v1/courses/{courseId}/courseWorkMaterials/{itemId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_course_work_materials_add_on_attachments_delete_builder()` + `classroom_courses_course_work_materials_add_on_attachments_delete_execute()`.
/// For task-level control, use `classroom_courses_course_work_materials_add_on_attachments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_course_work_materials_add_on_attachments_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesCourseWorkMaterialsAddOnAttachmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_course_work_materials_add_on_attachments_delete_builder(
        client,
        &args.courseId,
        &args.itemId,
        &args.attachmentId,
        &args.postId,
    )?;
    classroom_courses_course_work_materials_add_on_attachments_delete_execute(builder)
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_posts_get_add_on_context_execute()` to send, or `classroom_courses_posts_get_add_on_context` for simplest API.

pub fn classroom_courses_posts_get_add_on_context_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    postId: &String,
    addOnToken: &Option<String>,
    attachmentId: &Option<String>,
    itemId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/posts/{}/addOnContext",
        courseId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = attachmentId.as_ref() {
        query_parts.push(format!("attachmentId={}", val));
    }
    if let Some(val) = itemId.as_ref() {
        query_parts.push(format!("itemId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_posts_get_add_on_context_execute()` or `classroom_courses_posts_get_add_on_context`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_get_add_on_context_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnContext>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnContext = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_posts_get_add_on_context_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_posts_get_add_on_context_task()`.
/// For the simplest API, use `classroom_courses_posts_get_add_on_context()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_get_add_on_context_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_posts_get_add_on_context_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_posts_get_add_on_context_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_posts_get_add_on_context`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesPostsGetAddOnContextArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: attachmentId
    pub attachmentId: Option<String>,
    /// Query parameter: itemId
    pub itemId: Option<String>,
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnContext
/// Gets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](<https://developers.google.`com/workspace/classroom/add-ons/get-started/iframes/iframes-overview`>). This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_posts_get_add_on_context_builder()` + `classroom_courses_posts_get_add_on_context_execute()`.
/// For task-level control, use `classroom_courses_posts_get_add_on_context_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_get_add_on_context(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesPostsGetAddOnContextArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnContext>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_posts_get_add_on_context_builder(
        client,
        &args.courseId,
        &args.postId,
        &args.addOnToken,
        &args.attachmentId,
        &args.itemId,
    )?;
    classroom_courses_posts_get_add_on_context_execute(builder)
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_posts_add_on_attachments_create_execute()` to send, or `classroom_courses_posts_add_on_attachments_create` for simplest API.

pub fn classroom_courses_posts_add_on_attachments_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    postId: &String,
    addOnToken: &Option<String>,
    itemId: &Option<String>,
    body: &AddOnAttachment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/posts/{}/addOnAttachments",
        courseId, postId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addOnToken.as_ref() {
        query_parts.push(format!("addOnToken={}", val));
    }
    if let Some(val) = itemId.as_ref() {
        query_parts.push(format!("itemId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_posts_add_on_attachments_create_execute()` or `classroom_courses_posts_add_on_attachments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_add_on_attachments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnAttachment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnAttachment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_posts_add_on_attachments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_posts_add_on_attachments_create_task()`.
/// For the simplest API, use `classroom_courses_posts_add_on_attachments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_add_on_attachments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_posts_add_on_attachments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_posts_add_on_attachments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_posts_add_on_attachments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesPostsAddOnAttachmentsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Query parameter: addOnToken
    pub addOnToken: Option<String>,
    /// Query parameter: itemId
    pub itemId: Option<String>,
    /// Request body.
    pub body: AddOnAttachment,
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments
/// Creates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_posts_add_on_attachments_create_builder()` + `classroom_courses_posts_add_on_attachments_create_execute()`.
/// For task-level control, use `classroom_courses_posts_add_on_attachments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_add_on_attachments_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesPostsAddOnAttachmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddOnAttachment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_posts_add_on_attachments_create_builder(
        client,
        &args.courseId,
        &args.postId,
        &args.addOnToken,
        &args.itemId,
        &args.body,
    )?;
    classroom_courses_posts_add_on_attachments_create_execute(builder)
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_posts_add_on_attachments_delete_execute()` to send, or `classroom_courses_posts_add_on_attachments_delete` for simplest API.

pub fn classroom_courses_posts_add_on_attachments_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    postId: &String,
    attachmentId: &String,
    itemId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/posts/{}/addOnAttachments/{}",
        courseId, postId, attachmentId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = itemId.as_ref() {
        query_parts.push(format!("itemId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_posts_add_on_attachments_delete_execute()` or `classroom_courses_posts_add_on_attachments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_add_on_attachments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_posts_add_on_attachments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_posts_add_on_attachments_delete_task()`.
/// For the simplest API, use `classroom_courses_posts_add_on_attachments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_add_on_attachments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_posts_add_on_attachments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_posts_add_on_attachments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_posts_add_on_attachments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesPostsAddOnAttachmentsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Path parameter: attachmentId
    pub attachmentId: String,
    /// Query parameter: itemId
    pub itemId: Option<String>,
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}
/// Deletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_posts_add_on_attachments_delete_builder()` + `classroom_courses_posts_add_on_attachments_delete_execute()`.
/// For task-level control, use `classroom_courses_posts_add_on_attachments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_add_on_attachments_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesPostsAddOnAttachmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_posts_add_on_attachments_delete_builder(
        client,
        &args.courseId,
        &args.postId,
        &args.attachmentId,
        &args.itemId,
    )?;
    classroom_courses_posts_add_on_attachments_delete_execute(builder)
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_posts_add_on_attachments_student_submissions_get_execute()` to send, or `classroom_courses_posts_add_on_attachments_student_submissions_get` for simplest API.

pub fn classroom_courses_posts_add_on_attachments_student_submissions_get_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    postId: &String,
    attachmentId: &String,
    submissionId: &String,
    itemId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/posts/{}/addOnAttachments/{}/studentSubmissions/{}",
        courseId,
        postId,
        attachmentId,
        submissionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = itemId.as_ref() {
        query_parts.push(format!("itemId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_posts_add_on_attachments_student_submissions_get_execute()` or `classroom_courses_posts_add_on_attachments_student_submissions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_add_on_attachments_student_submissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_add_on_attachments_student_submissions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddOnAttachmentStudentSubmission>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddOnAttachmentStudentSubmission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_posts_add_on_attachments_student_submissions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_posts_add_on_attachments_student_submissions_get_task()`.
/// For the simplest API, use `classroom_courses_posts_add_on_attachments_student_submissions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_posts_add_on_attachments_student_submissions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_posts_add_on_attachments_student_submissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AddOnAttachmentStudentSubmission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_posts_add_on_attachments_student_submissions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_posts_add_on_attachments_student_submissions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesPostsAddOnAttachmentsStudentSubmissionsGetArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: postId
    pub postId: String,
    /// Path parameter: attachmentId
    pub attachmentId: String,
    /// Path parameter: submissionId
    pub submissionId: String,
    /// Query parameter: itemId
    pub itemId: Option<String>,
}

/// GET v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}
/// Returns a student submission for an add-on attachment. This method returns the following error codes: * PERMISSION_DENIED for access errors. * INVALID_ARGUMENT if the request is malformed. * NOT_FOUND if one of the identified resources does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_posts_add_on_attachments_student_submissions_get_builder()` + `classroom_courses_posts_add_on_attachments_student_submissions_get_execute()`.
/// For task-level control, use `classroom_courses_posts_add_on_attachments_student_submissions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_posts_add_on_attachments_student_submissions_get(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesPostsAddOnAttachmentsStudentSubmissionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AddOnAttachmentStudentSubmission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_posts_add_on_attachments_student_submissions_get_builder(
        client,
        &args.courseId,
        &args.postId,
        &args.attachmentId,
        &args.submissionId,
        &args.itemId,
    )?;
    classroom_courses_posts_add_on_attachments_student_submissions_get_execute(builder)
}

/// GET v1/courses/{courseId}/studentGroups
/// Creates a student group for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or for access errors. * NOT_FOUND if the course does not exist or the requesting user doesn't have access to the course. * FAILED_PRECONDITION if creating the student group would exceed the maximum number of student groups per course.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_student_groups_create_execute()` to send, or `classroom_courses_student_groups_create` for simplest API.

pub fn classroom_courses_student_groups_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    body: &StudentGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/studentGroups",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/studentGroups
/// Creates a student group for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or for access errors. * NOT_FOUND if the course does not exist or the requesting user doesn't have access to the course. * FAILED_PRECONDITION if creating the student group would exceed the maximum number of student groups per course.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_student_groups_create_execute()` or `classroom_courses_student_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudentGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudentGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/studentGroups
/// Creates a student group for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or for access errors. * NOT_FOUND if the course does not exist or the requesting user doesn't have access to the course. * FAILED_PRECONDITION if creating the student group would exceed the maximum number of student groups per course.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_student_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_student_groups_create_task()`.
/// For the simplest API, use `classroom_courses_student_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_student_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_student_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_student_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesStudentGroupsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Request body.
    pub body: StudentGroup,
}

/// GET v1/courses/{courseId}/studentGroups
/// Creates a student group for a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or for access errors. * NOT_FOUND if the course does not exist or the requesting user doesn't have access to the course. * FAILED_PRECONDITION if creating the student group would exceed the maximum number of student groups per course.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_student_groups_create_builder()` + `classroom_courses_student_groups_create_execute()`.
/// For task-level control, use `classroom_courses_student_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesStudentGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        classroom_courses_student_groups_create_builder(client, &args.courseId, &args.body)?;
    classroom_courses_student_groups_create_execute(builder)
}

/// GET v1/courses/{courseId}/studentGroups/{id}
/// Deletes a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group or for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_student_groups_delete_execute()` to send, or `classroom_courses_student_groups_delete` for simplest API.

pub fn classroom_courses_student_groups_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/studentGroups/{}",
        courseId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/studentGroups/{id}
/// Deletes a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group or for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_student_groups_delete_execute()` or `classroom_courses_student_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/studentGroups/{id}
/// Deletes a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group or for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_student_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_student_groups_delete_task()`.
/// For the simplest API, use `classroom_courses_student_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_student_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_student_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_student_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesStudentGroupsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{courseId}/studentGroups/{id}
/// Deletes a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group or for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_student_groups_delete_builder()` + `classroom_courses_student_groups_delete_execute()`.
/// For task-level control, use `classroom_courses_student_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesStudentGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        classroom_courses_student_groups_delete_builder(client, &args.courseId, &args.id)?;
    classroom_courses_student_groups_delete_execute(builder)
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers
/// Creates a student group member for a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or member for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group. * ALREADY_EXISTS if the student group member already exists. * FAILED_PRECONDITION if attempting to add a member to a student group that has reached its member limit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_student_groups_student_group_members_create_execute()` to send, or `classroom_courses_student_groups_student_group_members_create` for simplest API.

pub fn classroom_courses_student_groups_student_group_members_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    studentGroupId: &String,
    body: &StudentGroupMember,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/studentGroups/{}/studentGroupMembers",
        courseId, studentGroupId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers
/// Creates a student group member for a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or member for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group. * ALREADY_EXISTS if the student group member already exists. * FAILED_PRECONDITION if attempting to add a member to a student group that has reached its member limit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_student_groups_student_group_members_create_execute()` or `classroom_courses_student_groups_student_group_members_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_student_group_members_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_student_group_members_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StudentGroupMember>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StudentGroupMember = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers
/// Creates a student group member for a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or member for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group. * ALREADY_EXISTS if the student group member already exists. * FAILED_PRECONDITION if attempting to add a member to a student group that has reached its member limit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_student_groups_student_group_members_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_student_groups_student_group_members_create_task()`.
/// For the simplest API, use `classroom_courses_student_groups_student_group_members_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_student_group_members_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_student_groups_student_group_members_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentGroupMember>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_courses_student_groups_student_group_members_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_student_groups_student_group_members_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesStudentGroupsStudentGroupMembersCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: studentGroupId
    pub studentGroupId: String,
    /// Request body.
    pub body: StudentGroupMember,
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers
/// Creates a student group member for a student group. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create the student group or member for access errors. * NOT_FOUND if the student group does not exist or the user does not have access to the student group. * ALREADY_EXISTS if the student group member already exists. * FAILED_PRECONDITION if attempting to add a member to a student group that has reached its member limit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_student_groups_student_group_members_create_builder()` + `classroom_courses_student_groups_student_group_members_create_execute()`.
/// For task-level control, use `classroom_courses_student_groups_student_group_members_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_student_group_members_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesStudentGroupsStudentGroupMembersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StudentGroupMember>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_courses_student_groups_student_group_members_create_builder(
        client,
        &args.courseId,
        &args.studentGroupId,
        &args.body,
    )?;
    classroom_courses_student_groups_student_group_members_create_execute(builder)
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers/{userId}
/// Deletes a student group member. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group member or for access errors. * NOT_FOUND if the student group member does not exist or the user does not have access to the student group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_student_groups_student_group_members_delete_execute()` to send, or `classroom_courses_student_groups_student_group_members_delete` for simplest API.

pub fn classroom_courses_student_groups_student_group_members_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    studentGroupId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/studentGroups/{}/studentGroupMembers/{}",
        courseId, studentGroupId, userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers/{userId}
/// Deletes a student group member. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group member or for access errors. * NOT_FOUND if the student group member does not exist or the user does not have access to the student group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_student_groups_student_group_members_delete_execute()` or `classroom_courses_student_groups_student_group_members_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_student_group_members_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_student_group_members_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers/{userId}
/// Deletes a student group member. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group member or for access errors. * NOT_FOUND if the student group member does not exist or the user does not have access to the student group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_student_groups_student_group_members_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_student_groups_student_group_members_delete_task()`.
/// For the simplest API, use `classroom_courses_student_groups_student_group_members_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_student_groups_student_group_members_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_student_groups_student_group_members_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_student_groups_student_group_members_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_student_groups_student_group_members_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesStudentGroupsStudentGroupMembersDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: studentGroupId
    pub studentGroupId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET v1/courses/{courseId}/studentGroups/{studentGroupId}/studentGroupMembers/{userId}
/// Deletes a student group member. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested student group member or for access errors. * NOT_FOUND if the student group member does not exist or the user does not have access to the student group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_student_groups_student_group_members_delete_builder()` + `classroom_courses_student_groups_student_group_members_delete_execute()`.
/// For task-level control, use `classroom_courses_student_groups_student_group_members_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_student_groups_student_group_members_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesStudentGroupsStudentGroupMembersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_student_groups_student_group_members_delete_builder(
        client,
        &args.courseId,
        &args.studentGroupId,
        &args.userId,
    )?;
    classroom_courses_student_groups_student_group_members_delete_execute(builder)
}

/// GET v1/courses/{courseId}/students
/// Adds a user as a student of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as students to courses within their domain. Students are permitted to add themselves to a course using an enrollment code. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create students in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a student or teacher in the course.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_students_create_execute()` to send, or `classroom_courses_students_create` for simplest API.

pub fn classroom_courses_students_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    enrollmentCode: &Option<String>,
    body: &Student,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/students",
        courseId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = enrollmentCode.as_ref() {
        query_parts.push(format!("enrollmentCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/students
/// Adds a user as a student of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as students to courses within their domain. Students are permitted to add themselves to a course using an enrollment code. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create students in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a student or teacher in the course.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_students_create_execute()` or `classroom_courses_students_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_students_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_students_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Student>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Student = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/students
/// Adds a user as a student of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as students to courses within their domain. Students are permitted to add themselves to a course using an enrollment code. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create students in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a student or teacher in the course.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_students_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_students_create_task()`.
/// For the simplest API, use `classroom_courses_students_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_students_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_students_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Student>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_students_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_students_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesStudentsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Query parameter: enrollmentCode
    pub enrollmentCode: Option<String>,
    /// Request body.
    pub body: Student,
}

/// GET v1/courses/{courseId}/students
/// Adds a user as a student of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as students to courses within their domain. Students are permitted to add themselves to a course using an enrollment code. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create students in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a student or teacher in the course.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_students_create_builder()` + `classroom_courses_students_create_execute()`.
/// For task-level control, use `classroom_courses_students_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_students_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesStudentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Student>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_students_create_builder(
        client,
        &args.courseId,
        &args.enrollmentCode,
        &args.body,
    )?;
    classroom_courses_students_create_execute(builder)
}

/// GET v1/courses/{courseId}/students/{userId}
/// Deletes a student of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete students of this course or for access errors. * NOT_FOUND if no student of this course has the requested ID or if the course does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_students_delete_execute()` to send, or `classroom_courses_students_delete` for simplest API.

pub fn classroom_courses_students_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/students/{}",
        courseId, userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/students/{userId}
/// Deletes a student of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete students of this course or for access errors. * NOT_FOUND if no student of this course has the requested ID or if the course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_students_delete_execute()` or `classroom_courses_students_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_students_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_students_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/students/{userId}
/// Deletes a student of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete students of this course or for access errors. * NOT_FOUND if no student of this course has the requested ID or if the course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_students_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_students_delete_task()`.
/// For the simplest API, use `classroom_courses_students_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_students_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_students_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_students_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_students_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesStudentsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET v1/courses/{courseId}/students/{userId}
/// Deletes a student of a course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete students of this course or for access errors. * NOT_FOUND if no student of this course has the requested ID or if the course does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_students_delete_builder()` + `classroom_courses_students_delete_execute()`.
/// For task-level control, use `classroom_courses_students_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_students_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesStudentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_students_delete_builder(client, &args.courseId, &args.userId)?;
    classroom_courses_students_delete_execute(builder)
}

/// GET v1/courses/{courseId}/teachers
/// Creates a teacher of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as teachers to courses within their domain. Non-admin users should send an Invitation instead. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create teachers in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a teacher or student in the course.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_teachers_create_execute()` to send, or `classroom_courses_teachers_create` for simplest API.

pub fn classroom_courses_teachers_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    body: &Teacher,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/teachers",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/teachers
/// Creates a teacher of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as teachers to courses within their domain. Non-admin users should send an Invitation instead. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create teachers in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a teacher or student in the course.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_teachers_create_execute()` or `classroom_courses_teachers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_teachers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_teachers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Teacher>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Teacher = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/teachers
/// Creates a teacher of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as teachers to courses within their domain. Non-admin users should send an Invitation instead. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create teachers in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a teacher or student in the course.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_teachers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_teachers_create_task()`.
/// For the simplest API, use `classroom_courses_teachers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_teachers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_teachers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Teacher>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_teachers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_teachers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesTeachersCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Request body.
    pub body: Teacher,
}

/// GET v1/courses/{courseId}/teachers
/// Creates a teacher of a course. Domain administrators are permitted to [directly add](<https://developers.google.`com/workspace/classroom/guides/manage-users`>) users within their domain as teachers to courses within their domain. Non-admin users should send an Invitation instead. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create teachers in this course or for access errors. * NOT_FOUND if the requested course ID does not exist. * FAILED_PRECONDITION if the requested user's account is disabled, for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * InactiveCourseOwner * ALREADY_EXISTS if the user is already a teacher or student in the course.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_teachers_create_builder()` + `classroom_courses_teachers_create_execute()`.
/// For task-level control, use `classroom_courses_teachers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_teachers_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesTeachersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Teacher>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_teachers_create_builder(client, &args.courseId, &args.body)?;
    classroom_courses_teachers_create_execute(builder)
}

/// GET v1/courses/{courseId}/teachers/{userId}
/// Removes the specified teacher from the specified course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete teachers of this course or for access errors. * NOT_FOUND if no teacher of this course has the requested ID or if the course does not exist. * FAILED_PRECONDITION if the requested ID belongs to the primary teacher of this course. * FAILED_PRECONDITION if the requested ID belongs to the owner of the course Drive folder. * FAILED_PRECONDITION if the course no longer has an active owner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_teachers_delete_execute()` to send, or `classroom_courses_teachers_delete` for simplest API.

pub fn classroom_courses_teachers_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/teachers/{}",
        courseId, userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/teachers/{userId}
/// Removes the specified teacher from the specified course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete teachers of this course or for access errors. * NOT_FOUND if no teacher of this course has the requested ID or if the course does not exist. * FAILED_PRECONDITION if the requested ID belongs to the primary teacher of this course. * FAILED_PRECONDITION if the requested ID belongs to the owner of the course Drive folder. * FAILED_PRECONDITION if the course no longer has an active owner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_teachers_delete_execute()` or `classroom_courses_teachers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_teachers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_teachers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/teachers/{userId}
/// Removes the specified teacher from the specified course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete teachers of this course or for access errors. * NOT_FOUND if no teacher of this course has the requested ID or if the course does not exist. * FAILED_PRECONDITION if the requested ID belongs to the primary teacher of this course. * FAILED_PRECONDITION if the requested ID belongs to the owner of the course Drive folder. * FAILED_PRECONDITION if the course no longer has an active owner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_teachers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_teachers_delete_task()`.
/// For the simplest API, use `classroom_courses_teachers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_teachers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_teachers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_teachers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_teachers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesTeachersDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET v1/courses/{courseId}/teachers/{userId}
/// Removes the specified teacher from the specified course. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete teachers of this course or for access errors. * NOT_FOUND if no teacher of this course has the requested ID or if the course does not exist. * FAILED_PRECONDITION if the requested ID belongs to the primary teacher of this course. * FAILED_PRECONDITION if the requested ID belongs to the owner of the course Drive folder. * FAILED_PRECONDITION if the course no longer has an active owner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_teachers_delete_builder()` + `classroom_courses_teachers_delete_execute()`.
/// For task-level control, use `classroom_courses_teachers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_teachers_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesTeachersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_teachers_delete_builder(client, &args.courseId, &args.userId)?;
    classroom_courses_teachers_delete_execute(builder)
}

/// GET v1/courses/{courseId}/topics
/// Creates a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create a topic in the requested course, or for access errors. * INVALID_ARGUMENT if the request is malformed. * ALREADY_EXISTS if there exists a topic in the course with the same name. * FAILED_PRECONDITION for the following request error: * CourseTopicLimitReached * NOT_FOUND if the requested course does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_topics_create_execute()` to send, or `classroom_courses_topics_create` for simplest API.

pub fn classroom_courses_topics_create_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    body: &Topic,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/topics",
        courseId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/courses/{courseId}/topics
/// Creates a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create a topic in the requested course, or for access errors. * INVALID_ARGUMENT if the request is malformed. * ALREADY_EXISTS if there exists a topic in the course with the same name. * FAILED_PRECONDITION for the following request error: * CourseTopicLimitReached * NOT_FOUND if the requested course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_topics_create_execute()` or `classroom_courses_topics_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_topics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_topics_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topic>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topic = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/topics
/// Creates a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create a topic in the requested course, or for access errors. * INVALID_ARGUMENT if the request is malformed. * ALREADY_EXISTS if there exists a topic in the course with the same name. * FAILED_PRECONDITION for the following request error: * CourseTopicLimitReached * NOT_FOUND if the requested course does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_topics_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_topics_create_task()`.
/// For the simplest API, use `classroom_courses_topics_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_topics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_topics_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_topics_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_topics_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesTopicsCreateArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Request body.
    pub body: Topic,
}

/// GET v1/courses/{courseId}/topics
/// Creates a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access the requested course, create a topic in the requested course, or for access errors. * INVALID_ARGUMENT if the request is malformed. * ALREADY_EXISTS if there exists a topic in the course with the same name. * FAILED_PRECONDITION for the following request error: * CourseTopicLimitReached * NOT_FOUND if the requested course does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_topics_create_builder()` + `classroom_courses_topics_create_execute()`.
/// For task-level control, use `classroom_courses_topics_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_topics_create(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesTopicsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_topics_create_builder(client, &args.courseId, &args.body)?;
    classroom_courses_topics_create_execute(builder)
}

/// GET v1/courses/{courseId}/topics/{id}
/// Deletes a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not allowed to delete the requested topic or for access errors. * FAILED_PRECONDITION if the requested topic has already been deleted. * NOT_FOUND if no course or topic exists with the requested ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_courses_topics_delete_execute()` to send, or `classroom_courses_topics_delete` for simplest API.

pub fn classroom_courses_topics_delete_builder(
    client: &SimpleHttpClient,
    courseId: &String,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/courses/{}/topics/{}",
        courseId, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/courses/{courseId}/topics/{id}
/// Deletes a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not allowed to delete the requested topic or for access errors. * FAILED_PRECONDITION if the requested topic has already been deleted. * NOT_FOUND if no course or topic exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_courses_topics_delete_execute()` or `classroom_courses_topics_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_topics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_topics_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/courses/{courseId}/topics/{id}
/// Deletes a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not allowed to delete the requested topic or for access errors. * FAILED_PRECONDITION if the requested topic has already been deleted. * NOT_FOUND if no course or topic exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_courses_topics_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_courses_topics_delete_task()`.
/// For the simplest API, use `classroom_courses_topics_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_courses_topics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_courses_topics_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_courses_topics_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_courses_topics_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomCoursesTopicsDeleteArgs {
    /// Path parameter: courseId
    pub courseId: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/courses/{courseId}/topics/{id}
/// Deletes a topic. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not allowed to delete the requested topic or for access errors. * FAILED_PRECONDITION if the requested topic has already been deleted. * NOT_FOUND if no course or topic exists with the requested ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_courses_topics_delete_builder()` + `classroom_courses_topics_delete_execute()`.
/// For task-level control, use `classroom_courses_topics_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_courses_topics_delete(
    client: &SimpleHttpClient,
    args: &ClassroomCoursesTopicsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_courses_topics_delete_builder(client, &args.courseId, &args.id)?;
    classroom_courses_topics_delete_execute(builder)
}

/// GET v1/invitations/{id}:accept
/// Accepts an invitation, removing it and adding the invited user to the teachers or students (as appropriate) of the specified course. Only the invited user may accept an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to accept the requested invitation or for access errors. * FAILED_PRECONDITION for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * NOT_FOUND if no invitation exists with the requested ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_invitations_accept_execute()` to send, or `classroom_invitations_accept` for simplest API.

pub fn classroom_invitations_accept_builder(
    client: &SimpleHttpClient,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/invitations/{}:accept",
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/invitations/{id}:accept
/// Accepts an invitation, removing it and adding the invited user to the teachers or students (as appropriate) of the specified course. Only the invited user may accept an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to accept the requested invitation or for access errors. * FAILED_PRECONDITION for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * NOT_FOUND if no invitation exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_invitations_accept_execute()` or `classroom_invitations_accept`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_invitations_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_invitations_accept_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/invitations/{id}:accept
/// Accepts an invitation, removing it and adding the invited user to the teachers or students (as appropriate) of the specified course. Only the invited user may accept an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to accept the requested invitation or for access errors. * FAILED_PRECONDITION for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * NOT_FOUND if no invitation exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_invitations_accept_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_invitations_accept_task()`.
/// For the simplest API, use `classroom_invitations_accept()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_invitations_accept_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_invitations_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_invitations_accept_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_invitations_accept`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomInvitationsAcceptArgs {
    /// Path parameter: id
    pub id: String,
}

/// GET v1/invitations/{id}:accept
/// Accepts an invitation, removing it and adding the invited user to the teachers or students (as appropriate) of the specified course. Only the invited user may accept an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to accept the requested invitation or for access errors. * FAILED_PRECONDITION for the following request errors: * CourseMemberLimitReached * CourseNotModifiable * CourseTeacherLimitReached * UserGroupsMembershipLimitReached * NOT_FOUND if no invitation exists with the requested ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_invitations_accept_builder()` + `classroom_invitations_accept_execute()`.
/// For task-level control, use `classroom_invitations_accept_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_invitations_accept(
    client: &SimpleHttpClient,
    args: &ClassroomInvitationsAcceptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_invitations_accept_builder(client, &args.id)?;
    classroom_invitations_accept_execute(builder)
}

/// GET v1/invitations
/// Creates an invitation. Only one invitation for a user and course may exist at a time. Delete and re-create an invitation to make changes. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create invitations for this course or for access errors. * NOT_FOUND if the course or the user does not exist. * FAILED_PRECONDITION: * if the requested user's account is disabled. * if the user already has this role or a role with greater permissions. * for the following request errors: * IneligibleOwner * ALREADY_EXISTS if an invitation for the specified user and course already exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_invitations_create_execute()` to send, or `classroom_invitations_create` for simplest API.

pub fn classroom_invitations_create_builder(
    client: &SimpleHttpClient,
    body: &Invitation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://classroom.googleapis.com/v1/invitations",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/invitations
/// Creates an invitation. Only one invitation for a user and course may exist at a time. Delete and re-create an invitation to make changes. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create invitations for this course or for access errors. * NOT_FOUND if the course or the user does not exist. * FAILED_PRECONDITION: * if the requested user's account is disabled. * if the user already has this role or a role with greater permissions. * for the following request errors: * IneligibleOwner * ALREADY_EXISTS if an invitation for the specified user and course already exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_invitations_create_execute()` or `classroom_invitations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_invitations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_invitations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Invitation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Invitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/invitations
/// Creates an invitation. Only one invitation for a user and course may exist at a time. Delete and re-create an invitation to make changes. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create invitations for this course or for access errors. * NOT_FOUND if the course or the user does not exist. * FAILED_PRECONDITION: * if the requested user's account is disabled. * if the user already has this role or a role with greater permissions. * for the following request errors: * IneligibleOwner * ALREADY_EXISTS if an invitation for the specified user and course already exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_invitations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_invitations_create_task()`.
/// For the simplest API, use `classroom_invitations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_invitations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_invitations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invitation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_invitations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_invitations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomInvitationsCreateArgs {
    /// Request body.
    pub body: Invitation,
}

/// GET v1/invitations
/// Creates an invitation. Only one invitation for a user and course may exist at a time. Delete and re-create an invitation to make changes. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to create invitations for this course or for access errors. * NOT_FOUND if the course or the user does not exist. * FAILED_PRECONDITION: * if the requested user's account is disabled. * if the user already has this role or a role with greater permissions. * for the following request errors: * IneligibleOwner * ALREADY_EXISTS if an invitation for the specified user and course already exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_invitations_create_builder()` + `classroom_invitations_create_execute()`.
/// For task-level control, use `classroom_invitations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_invitations_create(
    client: &SimpleHttpClient,
    args: &ClassroomInvitationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Invitation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_invitations_create_builder(client, &args.body)?;
    classroom_invitations_create_execute(builder)
}

/// GET v1/invitations/{id}
/// Deletes an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested invitation or for access errors. * NOT_FOUND if no invitation exists with the requested ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_invitations_delete_execute()` to send, or `classroom_invitations_delete` for simplest API.

pub fn classroom_invitations_delete_builder(
    client: &SimpleHttpClient,
    id: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://classroom.googleapis.com/v1/invitations/{}", id,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/invitations/{id}
/// Deletes an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested invitation or for access errors. * NOT_FOUND if no invitation exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_invitations_delete_execute()` or `classroom_invitations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_invitations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_invitations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/invitations/{id}
/// Deletes an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested invitation or for access errors. * NOT_FOUND if no invitation exists with the requested ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_invitations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_invitations_delete_task()`.
/// For the simplest API, use `classroom_invitations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_invitations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_invitations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_invitations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_invitations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomInvitationsDeleteArgs {
    /// Path parameter: id
    pub id: String,
}

/// GET v1/invitations/{id}
/// Deletes an invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to delete the requested invitation or for access errors. * NOT_FOUND if no invitation exists with the requested ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_invitations_delete_builder()` + `classroom_invitations_delete_execute()`.
/// For task-level control, use `classroom_invitations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_invitations_delete(
    client: &SimpleHttpClient,
    args: &ClassroomInvitationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_invitations_delete_builder(client, &args.id)?;
    classroom_invitations_delete_execute(builder)
}

/// GET v1/registrations
/// Creates a Registration, causing Classroom to start sending notifications from the provided feed to the destination provided in `cloudPubSubTopic`. Returns the created Registration. Currently, this will be the same as the argument, but with server-assigned fields such as expiry_time and id filled in. Note that any value specified for the expiry_time or id fields will be ignored. While Classroom may validate the `cloudPubSubTopic` and return errors on a best effort basis, it is the caller's responsibility to ensure that it exists and that Classroom has permission to publish to it. This method may return the following error codes: * PERMISSION_DENIED if: * the authenticated user does not have permission to receive notifications from the requested field; or * the current user has not granted access to the current Cloud project with the appropriate scope for the requested feed. Note that domain-wide delegation of authority is not currently supported for this purpose. If the request has the appropriate scope, but no grant exists, a Request Errors is returned. * another access error is encountered. * INVALID_ARGUMENT if: * no `cloudPubsubTopic` is specified, or the specified `cloudPubsubTopic` is not valid; or * no feed is specified, or the specified feed is not valid. * NOT_FOUND if: * the specified feed cannot be located, or the requesting user does not have permission to determine whether or not it exists; or * the specified `cloudPubsubTopic` cannot be located, or Classroom has not been granted permission to publish to it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_registrations_create_execute()` to send, or `classroom_registrations_create` for simplest API.

pub fn classroom_registrations_create_builder(
    client: &SimpleHttpClient,
    body: &Registration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://classroom.googleapis.com/v1/registrations",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/registrations
/// Creates a Registration, causing Classroom to start sending notifications from the provided feed to the destination provided in `cloudPubSubTopic`. Returns the created Registration. Currently, this will be the same as the argument, but with server-assigned fields such as expiry_time and id filled in. Note that any value specified for the expiry_time or id fields will be ignored. While Classroom may validate the `cloudPubSubTopic` and return errors on a best effort basis, it is the caller's responsibility to ensure that it exists and that Classroom has permission to publish to it. This method may return the following error codes: * PERMISSION_DENIED if: * the authenticated user does not have permission to receive notifications from the requested field; or * the current user has not granted access to the current Cloud project with the appropriate scope for the requested feed. Note that domain-wide delegation of authority is not currently supported for this purpose. If the request has the appropriate scope, but no grant exists, a Request Errors is returned. * another access error is encountered. * INVALID_ARGUMENT if: * no `cloudPubsubTopic` is specified, or the specified `cloudPubsubTopic` is not valid; or * no feed is specified, or the specified feed is not valid. * NOT_FOUND if: * the specified feed cannot be located, or the requesting user does not have permission to determine whether or not it exists; or * the specified `cloudPubsubTopic` cannot be located, or Classroom has not been granted permission to publish to it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_registrations_create_execute()` or `classroom_registrations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_registrations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_registrations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Registration>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Registration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/registrations
/// Creates a Registration, causing Classroom to start sending notifications from the provided feed to the destination provided in `cloudPubSubTopic`. Returns the created Registration. Currently, this will be the same as the argument, but with server-assigned fields such as expiry_time and id filled in. Note that any value specified for the expiry_time or id fields will be ignored. While Classroom may validate the `cloudPubSubTopic` and return errors on a best effort basis, it is the caller's responsibility to ensure that it exists and that Classroom has permission to publish to it. This method may return the following error codes: * PERMISSION_DENIED if: * the authenticated user does not have permission to receive notifications from the requested field; or * the current user has not granted access to the current Cloud project with the appropriate scope for the requested feed. Note that domain-wide delegation of authority is not currently supported for this purpose. If the request has the appropriate scope, but no grant exists, a Request Errors is returned. * another access error is encountered. * INVALID_ARGUMENT if: * no `cloudPubsubTopic` is specified, or the specified `cloudPubsubTopic` is not valid; or * no feed is specified, or the specified feed is not valid. * NOT_FOUND if: * the specified feed cannot be located, or the requesting user does not have permission to determine whether or not it exists; or * the specified `cloudPubsubTopic` cannot be located, or Classroom has not been granted permission to publish to it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_registrations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_registrations_create_task()`.
/// For the simplest API, use `classroom_registrations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_registrations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_registrations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Registration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_registrations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_registrations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomRegistrationsCreateArgs {
    /// Request body.
    pub body: Registration,
}

/// GET v1/registrations
/// Creates a Registration, causing Classroom to start sending notifications from the provided feed to the destination provided in `cloudPubSubTopic`. Returns the created Registration. Currently, this will be the same as the argument, but with server-assigned fields such as expiry_time and id filled in. Note that any value specified for the expiry_time or id fields will be ignored. While Classroom may validate the `cloudPubSubTopic` and return errors on a best effort basis, it is the caller's responsibility to ensure that it exists and that Classroom has permission to publish to it. This method may return the following error codes: * PERMISSION_DENIED if: * the authenticated user does not have permission to receive notifications from the requested field; or * the current user has not granted access to the current Cloud project with the appropriate scope for the requested feed. Note that domain-wide delegation of authority is not currently supported for this purpose. If the request has the appropriate scope, but no grant exists, a Request Errors is returned. * another access error is encountered. * INVALID_ARGUMENT if: * no `cloudPubsubTopic` is specified, or the specified `cloudPubsubTopic` is not valid; or * no feed is specified, or the specified feed is not valid. * NOT_FOUND if: * the specified feed cannot be located, or the requesting user does not have permission to determine whether or not it exists; or * the specified `cloudPubsubTopic` cannot be located, or Classroom has not been granted permission to publish to it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_registrations_create_builder()` + `classroom_registrations_create_execute()`.
/// For task-level control, use `classroom_registrations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_registrations_create(
    client: &SimpleHttpClient,
    args: &ClassroomRegistrationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Registration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_registrations_create_builder(client, &args.body)?;
    classroom_registrations_create_execute(builder)
}

/// GET v1/registrations/{registrationId}
/// Deletes a Registration, causing Classroom to stop sending notifications for that Registration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_registrations_delete_execute()` to send, or `classroom_registrations_delete` for simplest API.

pub fn classroom_registrations_delete_builder(
    client: &SimpleHttpClient,
    registrationId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/registrations/{}",
        registrationId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/registrations/{registrationId}
/// Deletes a Registration, causing Classroom to stop sending notifications for that Registration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_registrations_delete_execute()` or `classroom_registrations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_registrations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_registrations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/registrations/{registrationId}
/// Deletes a Registration, causing Classroom to stop sending notifications for that Registration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_registrations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_registrations_delete_task()`.
/// For the simplest API, use `classroom_registrations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_registrations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_registrations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_registrations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_registrations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomRegistrationsDeleteArgs {
    /// Path parameter: registrationId
    pub registrationId: String,
}

/// GET v1/registrations/{registrationId}
/// Deletes a Registration, causing Classroom to stop sending notifications for that Registration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_registrations_delete_builder()` + `classroom_registrations_delete_execute()`.
/// For task-level control, use `classroom_registrations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_registrations_delete(
    client: &SimpleHttpClient,
    args: &ClassroomRegistrationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_registrations_delete_builder(client, &args.registrationId)?;
    classroom_registrations_delete_execute(builder)
}

/// GET v1/userProfiles/{userId}
/// Returns a user profile. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access this user profile, if no profile exists with the requested ID, or for access errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_user_profiles_get_execute()` to send, or `classroom_user_profiles_get` for simplest API.

pub fn classroom_user_profiles_get_builder(
    client: &SimpleHttpClient,
    userId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/userProfiles/{}",
        userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/userProfiles/{userId}
/// Returns a user profile. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access this user profile, if no profile exists with the requested ID, or for access errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_user_profiles_get_execute()` or `classroom_user_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/userProfiles/{userId}
/// Returns a user profile. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access this user profile, if no profile exists with the requested ID, or for access errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_user_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_user_profiles_get_task()`.
/// For the simplest API, use `classroom_user_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_user_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserProfile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_user_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_user_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomUserProfilesGetArgs {
    /// Path parameter: userId
    pub userId: String,
}

/// GET v1/userProfiles/{userId}
/// Returns a user profile. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to access this user profile, if no profile exists with the requested ID, or for access errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_user_profiles_get_builder()` + `classroom_user_profiles_get_execute()`.
/// For task-level control, use `classroom_user_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_get(
    client: &SimpleHttpClient,
    args: &ClassroomUserProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserProfile>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_user_profiles_get_builder(client, &args.userId)?;
    classroom_user_profiles_get_execute(builder)
}

/// GET v1/userProfiles/{studentId}/guardianInvitations
/// Creates a guardian invitation, and sends an email to the guardian asking them to confirm that they are the student's guardian. Once the guardian accepts the invitation, their state will change to COMPLETED and they will start receiving guardian notifications. A Guardian resource will also be created to represent the active guardian. The request object must have the student_id and invited_email_address fields set. Failing to set these fields, or setting any other fields in the request, will result in an error. This method returns the following error codes: * PERMISSION_DENIED if the current user does not have permission to manage guardians, if the guardian in question has already rejected too many requests for that student, if guardians are not enabled for the domain in question, or for other access errors. * RESOURCE_EXHAUSTED if the student or guardian has exceeded the guardian link limit. * INVALID_ARGUMENT if the guardian email address is not valid (for example, if it is too long), or if the format of the student ID provided cannot be recognized (it is not an email address, nor a user_id from this API). This error will also be returned if read-only fields are set, or if the state field is set to to a value other than `PENDING`. * NOT_FOUND if the student ID provided is a valid student ID, but Classroom has no record of that student. * ALREADY_EXISTS if there is already a pending guardian invitation for the student and invited_email_address provided, or if the provided invited_email_address matches the Google account of an existing Guardian for this user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_user_profiles_guardian_invitations_create_execute()` to send, or `classroom_user_profiles_guardian_invitations_create` for simplest API.

pub fn classroom_user_profiles_guardian_invitations_create_builder(
    client: &SimpleHttpClient,
    studentId: &String,
    body: &GuardianInvitation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/userProfiles/{}/guardianInvitations",
        studentId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/userProfiles/{studentId}/guardianInvitations
/// Creates a guardian invitation, and sends an email to the guardian asking them to confirm that they are the student's guardian. Once the guardian accepts the invitation, their state will change to COMPLETED and they will start receiving guardian notifications. A Guardian resource will also be created to represent the active guardian. The request object must have the student_id and invited_email_address fields set. Failing to set these fields, or setting any other fields in the request, will result in an error. This method returns the following error codes: * PERMISSION_DENIED if the current user does not have permission to manage guardians, if the guardian in question has already rejected too many requests for that student, if guardians are not enabled for the domain in question, or for other access errors. * RESOURCE_EXHAUSTED if the student or guardian has exceeded the guardian link limit. * INVALID_ARGUMENT if the guardian email address is not valid (for example, if it is too long), or if the format of the student ID provided cannot be recognized (it is not an email address, nor a user_id from this API). This error will also be returned if read-only fields are set, or if the state field is set to to a value other than `PENDING`. * NOT_FOUND if the student ID provided is a valid student ID, but Classroom has no record of that student. * ALREADY_EXISTS if there is already a pending guardian invitation for the student and invited_email_address provided, or if the provided invited_email_address matches the Google account of an existing Guardian for this user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_user_profiles_guardian_invitations_create_execute()` or `classroom_user_profiles_guardian_invitations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardian_invitations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardian_invitations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GuardianInvitation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GuardianInvitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/userProfiles/{studentId}/guardianInvitations
/// Creates a guardian invitation, and sends an email to the guardian asking them to confirm that they are the student's guardian. Once the guardian accepts the invitation, their state will change to COMPLETED and they will start receiving guardian notifications. A Guardian resource will also be created to represent the active guardian. The request object must have the student_id and invited_email_address fields set. Failing to set these fields, or setting any other fields in the request, will result in an error. This method returns the following error codes: * PERMISSION_DENIED if the current user does not have permission to manage guardians, if the guardian in question has already rejected too many requests for that student, if guardians are not enabled for the domain in question, or for other access errors. * RESOURCE_EXHAUSTED if the student or guardian has exceeded the guardian link limit. * INVALID_ARGUMENT if the guardian email address is not valid (for example, if it is too long), or if the format of the student ID provided cannot be recognized (it is not an email address, nor a user_id from this API). This error will also be returned if read-only fields are set, or if the state field is set to to a value other than `PENDING`. * NOT_FOUND if the student ID provided is a valid student ID, but Classroom has no record of that student. * ALREADY_EXISTS if there is already a pending guardian invitation for the student and invited_email_address provided, or if the provided invited_email_address matches the Google account of an existing Guardian for this user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_user_profiles_guardian_invitations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_user_profiles_guardian_invitations_create_task()`.
/// For the simplest API, use `classroom_user_profiles_guardian_invitations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardian_invitations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_user_profiles_guardian_invitations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuardianInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_user_profiles_guardian_invitations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_user_profiles_guardian_invitations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomUserProfilesGuardianInvitationsCreateArgs {
    /// Path parameter: studentId
    pub studentId: String,
    /// Request body.
    pub body: GuardianInvitation,
}

/// GET v1/userProfiles/{studentId}/guardianInvitations
/// Creates a guardian invitation, and sends an email to the guardian asking them to confirm that they are the student's guardian. Once the guardian accepts the invitation, their state will change to COMPLETED and they will start receiving guardian notifications. A Guardian resource will also be created to represent the active guardian. The request object must have the student_id and invited_email_address fields set. Failing to set these fields, or setting any other fields in the request, will result in an error. This method returns the following error codes: * PERMISSION_DENIED if the current user does not have permission to manage guardians, if the guardian in question has already rejected too many requests for that student, if guardians are not enabled for the domain in question, or for other access errors. * RESOURCE_EXHAUSTED if the student or guardian has exceeded the guardian link limit. * INVALID_ARGUMENT if the guardian email address is not valid (for example, if it is too long), or if the format of the student ID provided cannot be recognized (it is not an email address, nor a user_id from this API). This error will also be returned if read-only fields are set, or if the state field is set to to a value other than `PENDING`. * NOT_FOUND if the student ID provided is a valid student ID, but Classroom has no record of that student. * ALREADY_EXISTS if there is already a pending guardian invitation for the student and invited_email_address provided, or if the provided invited_email_address matches the Google account of an existing Guardian for this user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_user_profiles_guardian_invitations_create_builder()` + `classroom_user_profiles_guardian_invitations_create_execute()`.
/// For task-level control, use `classroom_user_profiles_guardian_invitations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardian_invitations_create(
    client: &SimpleHttpClient,
    args: &ClassroomUserProfilesGuardianInvitationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuardianInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_user_profiles_guardian_invitations_create_builder(
        client,
        &args.studentId,
        &args.body,
    )?;
    classroom_user_profiles_guardian_invitations_create_execute(builder)
}

/// GET v1/userProfiles/{studentId}/guardianInvitations/{invitationId}
/// Returns a specific guardian invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to view guardian invitations for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). * NOT_FOUND if Classroom cannot find any record of the given student or invitation_id. May also be returned if the student exists, but the requesting user does not have access to see that student.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_user_profiles_guardian_invitations_get_execute()` to send, or `classroom_user_profiles_guardian_invitations_get` for simplest API.

pub fn classroom_user_profiles_guardian_invitations_get_builder(
    client: &SimpleHttpClient,
    studentId: &String,
    invitationId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/userProfiles/{}/guardianInvitations/{}",
        studentId, invitationId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/userProfiles/{studentId}/guardianInvitations/{invitationId}
/// Returns a specific guardian invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to view guardian invitations for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). * NOT_FOUND if Classroom cannot find any record of the given student or invitation_id. May also be returned if the student exists, but the requesting user does not have access to see that student.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_user_profiles_guardian_invitations_get_execute()` or `classroom_user_profiles_guardian_invitations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardian_invitations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardian_invitations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GuardianInvitation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GuardianInvitation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/userProfiles/{studentId}/guardianInvitations/{invitationId}
/// Returns a specific guardian invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to view guardian invitations for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). * NOT_FOUND if Classroom cannot find any record of the given student or invitation_id. May also be returned if the student exists, but the requesting user does not have access to see that student.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_user_profiles_guardian_invitations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_user_profiles_guardian_invitations_get_task()`.
/// For the simplest API, use `classroom_user_profiles_guardian_invitations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardian_invitations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_user_profiles_guardian_invitations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuardianInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_user_profiles_guardian_invitations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_user_profiles_guardian_invitations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomUserProfilesGuardianInvitationsGetArgs {
    /// Path parameter: studentId
    pub studentId: String,
    /// Path parameter: invitationId
    pub invitationId: String,
}

/// GET v1/userProfiles/{studentId}/guardianInvitations/{invitationId}
/// Returns a specific guardian invitation. This method returns the following error codes: * PERMISSION_DENIED if the requesting user is not permitted to view guardian invitations for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). * NOT_FOUND if Classroom cannot find any record of the given student or invitation_id. May also be returned if the student exists, but the requesting user does not have access to see that student.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_user_profiles_guardian_invitations_get_builder()` + `classroom_user_profiles_guardian_invitations_get_execute()`.
/// For task-level control, use `classroom_user_profiles_guardian_invitations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardian_invitations_get(
    client: &SimpleHttpClient,
    args: &ClassroomUserProfilesGuardianInvitationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuardianInvitation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_user_profiles_guardian_invitations_get_builder(
        client,
        &args.studentId,
        &args.invitationId,
    )?;
    classroom_user_profiles_guardian_invitations_get_execute(builder)
}

/// GET v1/userProfiles/{studentId}/guardians/{guardianId}
/// Deletes a guardian. The guardian will no longer receive guardian notifications and the guardian will no longer be accessible via the API. This method returns the following error codes: * PERMISSION_DENIED if no user that matches the provided student_id is visible to the requesting user, if the requesting user is not permitted to manage guardians for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API). * NOT_FOUND if the requesting user is permitted to modify guardians for the requested student_id, but no Guardian record exists for that student with the provided guardian_id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_user_profiles_guardians_delete_execute()` to send, or `classroom_user_profiles_guardians_delete` for simplest API.

pub fn classroom_user_profiles_guardians_delete_builder(
    client: &SimpleHttpClient,
    studentId: &String,
    guardianId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/userProfiles/{}/guardians/{}",
        studentId, guardianId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/userProfiles/{studentId}/guardians/{guardianId}
/// Deletes a guardian. The guardian will no longer receive guardian notifications and the guardian will no longer be accessible via the API. This method returns the following error codes: * PERMISSION_DENIED if no user that matches the provided student_id is visible to the requesting user, if the requesting user is not permitted to manage guardians for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API). * NOT_FOUND if the requesting user is permitted to modify guardians for the requested student_id, but no Guardian record exists for that student with the provided guardian_id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_user_profiles_guardians_delete_execute()` or `classroom_user_profiles_guardians_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardians_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardians_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/userProfiles/{studentId}/guardians/{guardianId}
/// Deletes a guardian. The guardian will no longer receive guardian notifications and the guardian will no longer be accessible via the API. This method returns the following error codes: * PERMISSION_DENIED if no user that matches the provided student_id is visible to the requesting user, if the requesting user is not permitted to manage guardians for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API). * NOT_FOUND if the requesting user is permitted to modify guardians for the requested student_id, but no Guardian record exists for that student with the provided guardian_id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_user_profiles_guardians_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_user_profiles_guardians_delete_task()`.
/// For the simplest API, use `classroom_user_profiles_guardians_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardians_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_user_profiles_guardians_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = classroom_user_profiles_guardians_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_user_profiles_guardians_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomUserProfilesGuardiansDeleteArgs {
    /// Path parameter: studentId
    pub studentId: String,
    /// Path parameter: guardianId
    pub guardianId: String,
}

/// GET v1/userProfiles/{studentId}/guardians/{guardianId}
/// Deletes a guardian. The guardian will no longer receive guardian notifications and the guardian will no longer be accessible via the API. This method returns the following error codes: * PERMISSION_DENIED if no user that matches the provided student_id is visible to the requesting user, if the requesting user is not permitted to manage guardians for the student identified by the student_id, if guardians are not enabled for the domain in question, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API). * NOT_FOUND if the requesting user is permitted to modify guardians for the requested student_id, but no Guardian record exists for that student with the provided guardian_id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_user_profiles_guardians_delete_builder()` + `classroom_user_profiles_guardians_delete_execute()`.
/// For task-level control, use `classroom_user_profiles_guardians_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardians_delete(
    client: &SimpleHttpClient,
    args: &ClassroomUserProfilesGuardiansDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = classroom_user_profiles_guardians_delete_builder(
        client,
        &args.studentId,
        &args.guardianId,
    )?;
    classroom_user_profiles_guardians_delete_execute(builder)
}

/// GET v1/userProfiles/{studentId}/guardians
/// Returns a list of guardians that the requesting user is permitted to view, restricted to those that match the request. To list guardians for any student that the requesting user may view guardians for, use the literal character - for the student ID. This method returns the following error codes: * PERMISSION_DENIED if a student_id is specified, and the requesting user is not permitted to view guardian information for that student, if "-" is specified as the student_id and the user is not a domain administrator, if guardians are not enabled for the domain in question, if the invited_email_address filter is set by a user who is not a domain administrator, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). May also be returned if an invalid page_token is provided. * NOT_FOUND if a student_id is specified, and its format can be recognized, but Classroom has no record of that student.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `classroom_user_profiles_guardians_list_execute()` to send, or `classroom_user_profiles_guardians_list` for simplest API.

pub fn classroom_user_profiles_guardians_list_builder(
    client: &SimpleHttpClient,
    studentId: &String,
    invitedEmailAddress: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://classroom.googleapis.com/v1/userProfiles/{}/guardians",
        studentId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = invitedEmailAddress.as_ref() {
        query_parts.push(format!("invitedEmailAddress={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/userProfiles/{studentId}/guardians
/// Returns a list of guardians that the requesting user is permitted to view, restricted to those that match the request. To list guardians for any student that the requesting user may view guardians for, use the literal character - for the student ID. This method returns the following error codes: * PERMISSION_DENIED if a student_id is specified, and the requesting user is not permitted to view guardian information for that student, if "-" is specified as the student_id and the user is not a domain administrator, if guardians are not enabled for the domain in question, if the invited_email_address filter is set by a user who is not a domain administrator, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). May also be returned if an invalid page_token is provided. * NOT_FOUND if a student_id is specified, and its format can be recognized, but Classroom has no record of that student.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `classroom_user_profiles_guardians_list_execute()` or `classroom_user_profiles_guardians_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardians_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardians_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGuardiansResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGuardiansResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/userProfiles/{studentId}/guardians
/// Returns a list of guardians that the requesting user is permitted to view, restricted to those that match the request. To list guardians for any student that the requesting user may view guardians for, use the literal character - for the student ID. This method returns the following error codes: * PERMISSION_DENIED if a student_id is specified, and the requesting user is not permitted to view guardian information for that student, if "-" is specified as the student_id and the user is not a domain administrator, if guardians are not enabled for the domain in question, if the invited_email_address filter is set by a user who is not a domain administrator, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). May also be returned if an invalid page_token is provided. * NOT_FOUND if a student_id is specified, and its format can be recognized, but Classroom has no record of that student.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `classroom_user_profiles_guardians_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `classroom_user_profiles_guardians_list_task()`.
/// For the simplest API, use `classroom_user_profiles_guardians_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `classroom_user_profiles_guardians_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn classroom_user_profiles_guardians_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGuardiansResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = classroom_user_profiles_guardians_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`classroom_user_profiles_guardians_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ClassroomUserProfilesGuardiansListArgs {
    /// Path parameter: studentId
    pub studentId: String,
    /// Query parameter: invitedEmailAddress
    pub invitedEmailAddress: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/userProfiles/{studentId}/guardians
/// Returns a list of guardians that the requesting user is permitted to view, restricted to those that match the request. To list guardians for any student that the requesting user may view guardians for, use the literal character - for the student ID. This method returns the following error codes: * PERMISSION_DENIED if a student_id is specified, and the requesting user is not permitted to view guardian information for that student, if "-" is specified as the student_id and the user is not a domain administrator, if guardians are not enabled for the domain in question, if the invited_email_address filter is set by a user who is not a domain administrator, or for other access errors. * INVALID_ARGUMENT if a student_id is specified, but its format cannot be recognized (it is not an email address, nor a student_id from the API, nor the literal string me). May also be returned if an invalid page_token is provided. * NOT_FOUND if a student_id is specified, and its format can be recognized, but Classroom has no record of that student.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `classroom_user_profiles_guardians_list_builder()` + `classroom_user_profiles_guardians_list_execute()`.
/// For task-level control, use `classroom_user_profiles_guardians_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn classroom_user_profiles_guardians_list(
    client: &SimpleHttpClient,
    args: &ClassroomUserProfilesGuardiansListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGuardiansResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = classroom_user_profiles_guardians_list_builder(
        client,
        &args.studentId,
        &args.invitedEmailAddress,
        &args.pageSize,
        &args.pageToken,
    )?;
    classroom_user_profiles_guardians_list_execute(builder)
}
