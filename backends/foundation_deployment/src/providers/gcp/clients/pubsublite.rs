//! Auto-generated API clients for gcp/pubsublite.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_operations_cancel_execute()` to send, or `pubsublite_admin_projects_locations_operations_cancel` for simplest API.

pub fn pubsublite_admin_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_operations_cancel_execute()` or `pubsublite_admin_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_operations_cancel_builder()` + `pubsublite_admin_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        pubsublite_admin_projects_locations_operations_cancel_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_operations_delete_execute()` to send, or `pubsublite_admin_projects_locations_operations_delete` for simplest API.

pub fn pubsublite_admin_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_operations_delete_execute()` or `pubsublite_admin_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_delete_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_operations_delete_builder()` + `pubsublite_admin_projects_locations_operations_delete_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        pubsublite_admin_projects_locations_operations_delete_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_operations_delete_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_operations_get_execute()` to send, or `pubsublite_admin_projects_locations_operations_get` for simplest API.

pub fn pubsublite_admin_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_operations_get_execute()` or `pubsublite_admin_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_get_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_operations_get_builder()` + `pubsublite_admin_projects_locations_operations_get_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_operations_get_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_operations_get_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_operations_list_execute()` to send, or `pubsublite_admin_projects_locations_operations_list` for simplest API.

pub fn pubsublite_admin_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_operations_list_execute()` or `pubsublite_admin_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_list_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_operations_list_builder()` + `pubsublite_admin_projects_locations_operations_list_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    pubsublite_admin_projects_locations_operations_list_execute(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_reservations_create_execute()` to send, or `pubsublite_admin_projects_locations_reservations_create` for simplest API.

pub fn pubsublite_admin_projects_locations_reservations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    reservationId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/reservations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = reservationId.as_ref() {
        query_parts.push(format!("reservationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_reservations_create_execute()` or `pubsublite_admin_projects_locations_reservations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_reservations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_create_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_reservations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_reservations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_reservations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_reservations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsReservationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: reservationId
    pub reservationId: Option<String>,
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Creates a new reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_reservations_create_builder()` + `pubsublite_admin_projects_locations_reservations_create_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_create(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsReservationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_reservations_create_builder(
        client,
        &args.parent,
        &args.reservationId,
    )?;
    pubsublite_admin_projects_locations_reservations_create_execute(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes the specified reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_reservations_delete_execute()` to send, or `pubsublite_admin_projects_locations_reservations_delete` for simplest API.

pub fn pubsublite_admin_projects_locations_reservations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/reservations/{reservationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_reservations_delete_execute()` or `pubsublite_admin_projects_locations_reservations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_reservations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_delete_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_reservations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_reservations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_reservations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_reservations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsReservationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Deletes the specified reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_reservations_delete_builder()` + `pubsublite_admin_projects_locations_reservations_delete_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_delete(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsReservationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        pubsublite_admin_projects_locations_reservations_delete_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_reservations_delete_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns the reservation configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_reservations_get_execute()` to send, or `pubsublite_admin_projects_locations_reservations_get` for simplest API.

pub fn pubsublite_admin_projects_locations_reservations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/reservations/{reservationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns the reservation configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_reservations_get_execute()` or `pubsublite_admin_projects_locations_reservations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns the reservation configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_reservations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_get_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_reservations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_reservations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_reservations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_reservations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsReservationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Returns the reservation configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_reservations_get_builder()` + `pubsublite_admin_projects_locations_reservations_get_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_get(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsReservationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_reservations_get_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_reservations_get_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Returns the list of reservations for the given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_reservations_list_execute()` to send, or `pubsublite_admin_projects_locations_reservations_list` for simplest API.

pub fn pubsublite_admin_projects_locations_reservations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/reservations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Returns the list of reservations for the given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_reservations_list_execute()` or `pubsublite_admin_projects_locations_reservations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReservationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReservationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Returns the list of reservations for the given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_reservations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_list_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_reservations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_reservations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReservationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_reservations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_reservations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsReservationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations
/// Returns the list of reservations for the given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_reservations_list_builder()` + `pubsublite_admin_projects_locations_reservations_list_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_list(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsReservationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReservationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_reservations_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsublite_admin_projects_locations_reservations_list_execute(builder)
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates properties of the specified reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_reservations_patch_execute()` to send, or `pubsublite_admin_projects_locations_reservations_patch` for simplest API.

pub fn pubsublite_admin_projects_locations_reservations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/reservations/{reservationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates properties of the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_reservations_patch_execute()` or `pubsublite_admin_projects_locations_reservations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reservation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates properties of the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_reservations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_patch_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_reservations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_reservations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_reservations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_reservations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsReservationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}
/// Updates properties of the specified reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_reservations_patch_builder()` + `pubsublite_admin_projects_locations_reservations_patch_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_patch(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsReservationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reservation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_reservations_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    pubsublite_admin_projects_locations_reservations_patch_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/topics
/// Lists the topics attached to the specified reservation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_reservations_topics_list_execute()` to send, or `pubsublite_admin_projects_locations_reservations_topics_list` for simplest API.

pub fn pubsublite_admin_projects_locations_reservations_topics_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/reservations/{reservationsId}/topics",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/topics
/// Lists the topics attached to the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_reservations_topics_list_execute()` or `pubsublite_admin_projects_locations_reservations_topics_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_topics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_topics_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReservationTopicsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReservationTopicsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/topics
/// Lists the topics attached to the specified reservation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_reservations_topics_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_topics_list_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_reservations_topics_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_reservations_topics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_reservations_topics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReservationTopicsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_reservations_topics_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_reservations_topics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsReservationsTopicsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/reservations/{reservationsId}/topics
/// Lists the topics attached to the specified reservation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_reservations_topics_list_builder()` + `pubsublite_admin_projects_locations_reservations_topics_list_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_reservations_topics_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_reservations_topics_list(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsReservationsTopicsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReservationTopicsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_reservations_topics_list_builder(
        client,
        &args.name,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsublite_admin_projects_locations_reservations_topics_list_execute(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Creates a new subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_subscriptions_create_execute()` to send, or `pubsublite_admin_projects_locations_subscriptions_create` for simplest API.

pub fn pubsublite_admin_projects_locations_subscriptions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    skipBacklog: &Option<String>,
    subscriptionId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/subscriptions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = skipBacklog.as_ref() {
        query_parts.push(format!("skipBacklog={}", val));
    }
    if let Some(val) = subscriptionId.as_ref() {
        query_parts.push(format!("subscriptionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Creates a new subscription.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_subscriptions_create_execute()` or `pubsublite_admin_projects_locations_subscriptions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Creates a new subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_subscriptions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_create_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_subscriptions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_subscriptions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_subscriptions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_subscriptions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsSubscriptionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: skipBacklog
    pub skipBacklog: Option<String>,
    /// Query parameter: subscriptionId
    pub subscriptionId: Option<String>,
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Creates a new subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_subscriptions_create_builder()` + `pubsublite_admin_projects_locations_subscriptions_create_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_create(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsSubscriptionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_subscriptions_create_builder(
        client,
        &args.parent,
        &args.skipBacklog,
        &args.subscriptionId,
    )?;
    pubsublite_admin_projects_locations_subscriptions_create_execute(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Deletes the specified subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_subscriptions_delete_execute()` to send, or `pubsublite_admin_projects_locations_subscriptions_delete` for simplest API.

pub fn pubsublite_admin_projects_locations_subscriptions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/subscriptions/{subscriptionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Deletes the specified subscription.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_subscriptions_delete_execute()` or `pubsublite_admin_projects_locations_subscriptions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Deletes the specified subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_subscriptions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_delete_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_subscriptions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_subscriptions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_subscriptions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_subscriptions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsSubscriptionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Deletes the specified subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_subscriptions_delete_builder()` + `pubsublite_admin_projects_locations_subscriptions_delete_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_delete(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsSubscriptionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        pubsublite_admin_projects_locations_subscriptions_delete_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_subscriptions_delete_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Returns the subscription configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_subscriptions_get_execute()` to send, or `pubsublite_admin_projects_locations_subscriptions_get` for simplest API.

pub fn pubsublite_admin_projects_locations_subscriptions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/subscriptions/{subscriptionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Returns the subscription configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_subscriptions_get_execute()` or `pubsublite_admin_projects_locations_subscriptions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Returns the subscription configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_subscriptions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_get_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_subscriptions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_subscriptions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_subscriptions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsSubscriptionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Returns the subscription configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_subscriptions_get_builder()` + `pubsublite_admin_projects_locations_subscriptions_get_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_get(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsSubscriptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        pubsublite_admin_projects_locations_subscriptions_get_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_subscriptions_get_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Returns the list of subscriptions for the given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_subscriptions_list_execute()` to send, or `pubsublite_admin_projects_locations_subscriptions_list` for simplest API.

pub fn pubsublite_admin_projects_locations_subscriptions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/subscriptions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Returns the list of subscriptions for the given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_subscriptions_list_execute()` or `pubsublite_admin_projects_locations_subscriptions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Returns the list of subscriptions for the given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_subscriptions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_list_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_subscriptions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_subscriptions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_subscriptions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsSubscriptionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions
/// Returns the list of subscriptions for the given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_subscriptions_list_builder()` + `pubsublite_admin_projects_locations_subscriptions_list_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_list(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsSubscriptionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_subscriptions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsublite_admin_projects_locations_subscriptions_list_execute(builder)
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Updates properties of the specified subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_subscriptions_patch_execute()` to send, or `pubsublite_admin_projects_locations_subscriptions_patch` for simplest API.

pub fn pubsublite_admin_projects_locations_subscriptions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/subscriptions/{subscriptionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Updates properties of the specified subscription.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_subscriptions_patch_execute()` or `pubsublite_admin_projects_locations_subscriptions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Updates properties of the specified subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_subscriptions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_patch_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_subscriptions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_subscriptions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_subscriptions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_subscriptions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsSubscriptionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}
/// Updates properties of the specified subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_subscriptions_patch_builder()` + `pubsublite_admin_projects_locations_subscriptions_patch_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_patch(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsSubscriptionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_subscriptions_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    pubsublite_admin_projects_locations_subscriptions_patch_execute(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:seek
/// Performs an out-of-band seek for a subscription to a specified target, which may be timestamps or named positions within the message backlog. Seek translates these targets to cursors for each partition and orchestrates subscribers to start consuming messages from these seek cursors. If an operation is returned, the seek has been registered and subscribers will eventually receive messages from the seek cursors (i.e. eventual consistency), as long as they are using a minimum supported client library version and not a system that tracks cursors independently of P`ub/Sub` Lite (e.g. Apache Beam, Dataflow, Spark). The seek operation will fail for unsupported clients. If clients would like to know when subscribers react to the seek (or not), they can poll the operation. The seek operation will succeed and complete once subscribers are ready to receive messages from the seek cursors for all partitions of the topic. This means that the seek operation will not complete until all subscribers come online. If the previous seek operation has not yet completed, it will be aborted and the new invocation of seek will supersede it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_subscriptions_seek_execute()` to send, or `pubsublite_admin_projects_locations_subscriptions_seek` for simplest API.

pub fn pubsublite_admin_projects_locations_subscriptions_seek_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/subscriptions/{subscriptionsId}:seek",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:seek
/// Performs an out-of-band seek for a subscription to a specified target, which may be timestamps or named positions within the message backlog. Seek translates these targets to cursors for each partition and orchestrates subscribers to start consuming messages from these seek cursors. If an operation is returned, the seek has been registered and subscribers will eventually receive messages from the seek cursors (i.e. eventual consistency), as long as they are using a minimum supported client library version and not a system that tracks cursors independently of P`ub/Sub` Lite (e.g. Apache Beam, Dataflow, Spark). The seek operation will fail for unsupported clients. If clients would like to know when subscribers react to the seek (or not), they can poll the operation. The seek operation will succeed and complete once subscribers are ready to receive messages from the seek cursors for all partitions of the topic. This means that the seek operation will not complete until all subscribers come online. If the previous seek operation has not yet completed, it will be aborted and the new invocation of seek will supersede it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_subscriptions_seek_execute()` or `pubsublite_admin_projects_locations_subscriptions_seek`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_seek_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_seek_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:seek
/// Performs an out-of-band seek for a subscription to a specified target, which may be timestamps or named positions within the message backlog. Seek translates these targets to cursors for each partition and orchestrates subscribers to start consuming messages from these seek cursors. If an operation is returned, the seek has been registered and subscribers will eventually receive messages from the seek cursors (i.e. eventual consistency), as long as they are using a minimum supported client library version and not a system that tracks cursors independently of P`ub/Sub` Lite (e.g. Apache Beam, Dataflow, Spark). The seek operation will fail for unsupported clients. If clients would like to know when subscribers react to the seek (or not), they can poll the operation. The seek operation will succeed and complete once subscribers are ready to receive messages from the seek cursors for all partitions of the topic. This means that the seek operation will not complete until all subscribers come online. If the previous seek operation has not yet completed, it will be aborted and the new invocation of seek will supersede it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_subscriptions_seek_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_seek_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_subscriptions_seek()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_subscriptions_seek_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_subscriptions_seek_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_subscriptions_seek_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_subscriptions_seek`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsSubscriptionsSeekArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:seek
/// Performs an out-of-band seek for a subscription to a specified target, which may be timestamps or named positions within the message backlog. Seek translates these targets to cursors for each partition and orchestrates subscribers to start consuming messages from these seek cursors. If an operation is returned, the seek has been registered and subscribers will eventually receive messages from the seek cursors (i.e. eventual consistency), as long as they are using a minimum supported client library version and not a system that tracks cursors independently of P`ub/Sub` Lite (e.g. Apache Beam, Dataflow, Spark). The seek operation will fail for unsupported clients. If clients would like to know when subscribers react to the seek (or not), they can poll the operation. The seek operation will succeed and complete once subscribers are ready to receive messages from the seek cursors for all partitions of the topic. This means that the seek operation will not complete until all subscribers come online. If the previous seek operation has not yet completed, it will be aborted and the new invocation of seek will supersede it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_subscriptions_seek_builder()` + `pubsublite_admin_projects_locations_subscriptions_seek_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_subscriptions_seek_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_subscriptions_seek(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsSubscriptionsSeekArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        pubsublite_admin_projects_locations_subscriptions_seek_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_subscriptions_seek_execute(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Creates a new topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_topics_create_execute()` to send, or `pubsublite_admin_projects_locations_topics_create` for simplest API.

pub fn pubsublite_admin_projects_locations_topics_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    topicId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/topics",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = topicId.as_ref() {
        query_parts.push(format!("topicId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Creates a new topic.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_topics_create_execute()` or `pubsublite_admin_projects_locations_topics_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topic>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topic = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Creates a new topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_topics_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_create_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_topics_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_topics_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_topics_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_topics_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsTopicsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: topicId
    pub topicId: Option<String>,
}

/// POST v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Creates a new topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_topics_create_builder()` + `pubsublite_admin_projects_locations_topics_create_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_create(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsTopicsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_topics_create_builder(
        client,
        &args.parent,
        &args.topicId,
    )?;
    pubsublite_admin_projects_locations_topics_create_execute(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Deletes the specified topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_topics_delete_execute()` to send, or `pubsublite_admin_projects_locations_topics_delete` for simplest API.

pub fn pubsublite_admin_projects_locations_topics_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/topics/{topicsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Deletes the specified topic.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_topics_delete_execute()` or `pubsublite_admin_projects_locations_topics_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Deletes the specified topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_topics_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_delete_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_topics_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_topics_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_topics_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_topics_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsTopicsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Deletes the specified topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_topics_delete_builder()` + `pubsublite_admin_projects_locations_topics_delete_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_delete(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsTopicsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_topics_delete_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_topics_delete_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Returns the topic configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_topics_get_execute()` to send, or `pubsublite_admin_projects_locations_topics_get` for simplest API.

pub fn pubsublite_admin_projects_locations_topics_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/topics/{topicsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Returns the topic configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_topics_get_execute()` or `pubsublite_admin_projects_locations_topics_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topic>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topic = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Returns the topic configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_topics_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_get_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_topics_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_topics_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_topics_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_topics_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsTopicsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Returns the topic configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_topics_get_builder()` + `pubsublite_admin_projects_locations_topics_get_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_get(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsTopicsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_topics_get_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_topics_get_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/partitions
/// Returns the partition information for the requested topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_topics_get_partitions_execute()` to send, or `pubsublite_admin_projects_locations_topics_get_partitions` for simplest API.

pub fn pubsublite_admin_projects_locations_topics_get_partitions_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/topics/{topicsId}/partitions",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/partitions
/// Returns the partition information for the requested topic.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_topics_get_partitions_execute()` or `pubsublite_admin_projects_locations_topics_get_partitions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_get_partitions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_get_partitions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TopicPartitions>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TopicPartitions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/partitions
/// Returns the partition information for the requested topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_topics_get_partitions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_get_partitions_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_topics_get_partitions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_get_partitions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_topics_get_partitions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TopicPartitions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_topics_get_partitions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_topics_get_partitions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsTopicsGetPartitionsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/partitions
/// Returns the partition information for the requested topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_topics_get_partitions_builder()` + `pubsublite_admin_projects_locations_topics_get_partitions_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_get_partitions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_get_partitions(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsTopicsGetPartitionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TopicPartitions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        pubsublite_admin_projects_locations_topics_get_partitions_builder(client, &args.name)?;
    pubsublite_admin_projects_locations_topics_get_partitions_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Returns the list of topics for the given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_topics_list_execute()` to send, or `pubsublite_admin_projects_locations_topics_list` for simplest API.

pub fn pubsublite_admin_projects_locations_topics_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/topics",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Returns the list of topics for the given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_topics_list_execute()` or `pubsublite_admin_projects_locations_topics_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTopicsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTopicsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Returns the list of topics for the given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_topics_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_list_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_topics_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_topics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTopicsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_topics_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_topics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsTopicsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics
/// Returns the list of topics for the given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_topics_list_builder()` + `pubsublite_admin_projects_locations_topics_list_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_list(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsTopicsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTopicsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_topics_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsublite_admin_projects_locations_topics_list_execute(builder)
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Updates properties of the specified topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_topics_patch_execute()` to send, or `pubsublite_admin_projects_locations_topics_patch` for simplest API.

pub fn pubsublite_admin_projects_locations_topics_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/topics/{topicsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Updates properties of the specified topic.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_topics_patch_execute()` or `pubsublite_admin_projects_locations_topics_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Topic>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Topic = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Updates properties of the specified topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_topics_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_patch_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_topics_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_topics_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_topics_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_topics_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsTopicsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}
/// Updates properties of the specified topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_topics_patch_builder()` + `pubsublite_admin_projects_locations_topics_patch_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_patch(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsTopicsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Topic>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_topics_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    pubsublite_admin_projects_locations_topics_patch_execute(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/subscriptions
/// Lists the subscriptions attached to the specified topic.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_admin_projects_locations_topics_subscriptions_list_execute()` to send, or `pubsublite_admin_projects_locations_topics_subscriptions_list` for simplest API.

pub fn pubsublite_admin_projects_locations_topics_subscriptions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/admin/projects/{}/locations/{locationsId}/topics/{topicsId}/subscriptions",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/subscriptions
/// Lists the subscriptions attached to the specified topic.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_admin_projects_locations_topics_subscriptions_list_execute()` or `pubsublite_admin_projects_locations_topics_subscriptions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_subscriptions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTopicSubscriptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTopicSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/subscriptions
/// Lists the subscriptions attached to the specified topic.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_admin_projects_locations_topics_subscriptions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_subscriptions_list_task()`.
/// For the simplest API, use `pubsublite_admin_projects_locations_topics_subscriptions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_admin_projects_locations_topics_subscriptions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_admin_projects_locations_topics_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTopicSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_admin_projects_locations_topics_subscriptions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_admin_projects_locations_topics_subscriptions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteAdminProjectsLocationsTopicsSubscriptionsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/admin/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}/subscriptions
/// Lists the subscriptions attached to the specified topic.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_admin_projects_locations_topics_subscriptions_list_builder()` + `pubsublite_admin_projects_locations_topics_subscriptions_list_execute()`.
/// For task-level control, use `pubsublite_admin_projects_locations_topics_subscriptions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_admin_projects_locations_topics_subscriptions_list(
    client: &SimpleHttpClient,
    args: &PubsubliteAdminProjectsLocationsTopicsSubscriptionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTopicSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_admin_projects_locations_topics_subscriptions_list_builder(
        client,
        &args.name,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsublite_admin_projects_locations_topics_subscriptions_list_execute(builder)
}

/// POST v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:commitCursor
/// Updates the committed cursor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_execute()` to send, or `pubsublite_cursor_projects_locations_subscriptions_commit_cursor` for simplest API.

pub fn pubsublite_cursor_projects_locations_subscriptions_commit_cursor_builder<R>(
    client: &SimpleHttpClient<R>,
    subscription: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/cursor/projects/{}/locations/{locationsId}/subscriptions/{subscriptionsId}:commitCursor",
        subscription,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:commitCursor
/// Updates the committed cursor.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_execute()` or `pubsublite_cursor_projects_locations_subscriptions_commit_cursor`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_cursor_projects_locations_subscriptions_commit_cursor_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitCursorResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitCursorResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:commitCursor
/// Updates the committed cursor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_task()`.
/// For the simplest API, use `pubsublite_cursor_projects_locations_subscriptions_commit_cursor()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_cursor_projects_locations_subscriptions_commit_cursor_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitCursorResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_cursor_projects_locations_subscriptions_commit_cursor_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_cursor_projects_locations_subscriptions_commit_cursor`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteCursorProjectsLocationsSubscriptionsCommitCursorArgs {
    /// Path parameter: subscription
    pub subscription: String,
}

/// POST v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}:commitCursor
/// Updates the committed cursor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_builder()` + `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_execute()`.
/// For task-level control, use `pubsublite_cursor_projects_locations_subscriptions_commit_cursor_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_cursor_projects_locations_subscriptions_commit_cursor(
    client: &SimpleHttpClient,
    args: &PubsubliteCursorProjectsLocationsSubscriptionsCommitCursorArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitCursorResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_cursor_projects_locations_subscriptions_commit_cursor_builder(
        client,
        &args.subscription,
    )?;
    pubsublite_cursor_projects_locations_subscriptions_commit_cursor_execute(builder)
}

/// GET v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}/cursors
/// Returns all committed cursor information for a subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_cursor_projects_locations_subscriptions_cursors_list_execute()` to send, or `pubsublite_cursor_projects_locations_subscriptions_cursors_list` for simplest API.

pub fn pubsublite_cursor_projects_locations_subscriptions_cursors_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/cursor/projects/{}/locations/{locationsId}/subscriptions/{subscriptionsId}/cursors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}/cursors
/// Returns all committed cursor information for a subscription.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_cursor_projects_locations_subscriptions_cursors_list_execute()` or `pubsublite_cursor_projects_locations_subscriptions_cursors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_cursor_projects_locations_subscriptions_cursors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_cursor_projects_locations_subscriptions_cursors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPartitionCursorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPartitionCursorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}/cursors
/// Returns all committed cursor information for a subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_cursor_projects_locations_subscriptions_cursors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_cursor_projects_locations_subscriptions_cursors_list_task()`.
/// For the simplest API, use `pubsublite_cursor_projects_locations_subscriptions_cursors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_cursor_projects_locations_subscriptions_cursors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_cursor_projects_locations_subscriptions_cursors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPartitionCursorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_cursor_projects_locations_subscriptions_cursors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_cursor_projects_locations_subscriptions_cursors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteCursorProjectsLocationsSubscriptionsCursorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/cursor/projects/{projectsId}/locations/{locationsId}/subscriptions/{subscriptionsId}/cursors
/// Returns all committed cursor information for a subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_cursor_projects_locations_subscriptions_cursors_list_builder()` + `pubsublite_cursor_projects_locations_subscriptions_cursors_list_execute()`.
/// For task-level control, use `pubsublite_cursor_projects_locations_subscriptions_cursors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_cursor_projects_locations_subscriptions_cursors_list(
    client: &SimpleHttpClient,
    args: &PubsubliteCursorProjectsLocationsSubscriptionsCursorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPartitionCursorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_cursor_projects_locations_subscriptions_cursors_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    pubsublite_cursor_projects_locations_subscriptions_cursors_list_execute(builder)
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeHeadCursor
/// Compute the head cursor for the partition. The head cursor''s offset is guaranteed to be less than or equal to all messages which have not yet been acknowledged as published, and greater than the offset of any message whose publish has already been acknowledged. It is zero if there have never been messages in the partition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_execute()` to send, or `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor` for simplest API.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_builder<R>(
    client: &SimpleHttpClient<R>,
    topic: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/topicStats/projects/{}/locations/{locationsId}/topics/{topicsId}:computeHeadCursor",
        topic,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeHeadCursor
/// Compute the head cursor for the partition. The head cursor''s offset is guaranteed to be less than or equal to all messages which have not yet been acknowledged as published, and greater than the offset of any message whose publish has already been acknowledged. It is zero if there have never been messages in the partition.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_execute()` or `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ComputeHeadCursorResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ComputeHeadCursorResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeHeadCursor
/// Compute the head cursor for the partition. The head cursor''s offset is guaranteed to be less than or equal to all messages which have not yet been acknowledged as published, and greater than the offset of any message whose publish has already been acknowledged. It is zero if there have never been messages in the partition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_task()`.
/// For the simplest API, use `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ComputeHeadCursorResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_topic_stats_projects_locations_topics_compute_head_cursor`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteTopicStatsProjectsLocationsTopicsComputeHeadCursorArgs {
    /// Path parameter: topic
    pub topic: String,
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeHeadCursor
/// Compute the head cursor for the partition. The head cursor''s offset is guaranteed to be less than or equal to all messages which have not yet been acknowledged as published, and greater than the offset of any message whose publish has already been acknowledged. It is zero if there have never been messages in the partition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_builder()` + `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_execute()`.
/// For task-level control, use `pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_head_cursor(
    client: &SimpleHttpClient,
    args: &PubsubliteTopicStatsProjectsLocationsTopicsComputeHeadCursorArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ComputeHeadCursorResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_builder(
        client,
        &args.topic,
    )?;
    pubsublite_topic_stats_projects_locations_topics_compute_head_cursor_execute(builder)
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeMessageStats
/// Compute statistics about a range of messages in a given topic and partition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_execute()` to send, or `pubsublite_topic_stats_projects_locations_topics_compute_message_stats` for simplest API.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_message_stats_builder<R>(
    client: &SimpleHttpClient<R>,
    topic: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/topicStats/projects/{}/locations/{locationsId}/topics/{topicsId}:computeMessageStats",
        topic,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeMessageStats
/// Compute statistics about a range of messages in a given topic and partition.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_execute()` or `pubsublite_topic_stats_projects_locations_topics_compute_message_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_message_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ComputeMessageStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ComputeMessageStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeMessageStats
/// Compute statistics about a range of messages in a given topic and partition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_task()`.
/// For the simplest API, use `pubsublite_topic_stats_projects_locations_topics_compute_message_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_message_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ComputeMessageStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        pubsublite_topic_stats_projects_locations_topics_compute_message_stats_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_topic_stats_projects_locations_topics_compute_message_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteTopicStatsProjectsLocationsTopicsComputeMessageStatsArgs {
    /// Path parameter: topic
    pub topic: String,
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeMessageStats
/// Compute statistics about a range of messages in a given topic and partition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_builder()` + `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_execute()`.
/// For task-level control, use `pubsublite_topic_stats_projects_locations_topics_compute_message_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_message_stats(
    client: &SimpleHttpClient,
    args: &PubsubliteTopicStatsProjectsLocationsTopicsComputeMessageStatsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ComputeMessageStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_topic_stats_projects_locations_topics_compute_message_stats_builder(
        client,
        &args.topic,
    )?;
    pubsublite_topic_stats_projects_locations_topics_compute_message_stats_execute(builder)
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeTimeCursor
/// Compute the corresponding cursor for a publish or event time in a topic partition.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_execute()` to send, or `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor` for simplest API.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_builder<R>(
    client: &SimpleHttpClient<R>,
    topic: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://pubsublite.googleapis.com/v1/topicStats/projects/{}/locations/{locationsId}/topics/{topicsId}:computeTimeCursor",
        topic,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeTimeCursor
/// Compute the corresponding cursor for a publish or event time in a topic partition.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_execute()` or `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ComputeTimeCursorResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ComputeTimeCursorResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeTimeCursor
/// Compute the corresponding cursor for a publish or event time in a topic partition.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_task()`.
/// For the simplest API, use `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ComputeTimeCursorResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`pubsublite_topic_stats_projects_locations_topics_compute_time_cursor`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PubsubliteTopicStatsProjectsLocationsTopicsComputeTimeCursorArgs {
    /// Path parameter: topic
    pub topic: String,
}

/// POST v1/topicStats/projects/{projectsId}/locations/{locationsId}/topics/{topicsId}:computeTimeCursor
/// Compute the corresponding cursor for a publish or event time in a topic partition.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_builder()` + `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_execute()`.
/// For task-level control, use `pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn pubsublite_topic_stats_projects_locations_topics_compute_time_cursor(
    client: &SimpleHttpClient,
    args: &PubsubliteTopicStatsProjectsLocationsTopicsComputeTimeCursorArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ComputeTimeCursorResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_builder(
        client,
        &args.topic,
    )?;
    pubsublite_topic_stats_projects_locations_topics_compute_time_cursor_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PubsubliteAdminProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::pubsublite::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PubsubliteAdminProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::pubsublite::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PubsubliteAdminProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::pubsublite::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with PubsubliteAdminProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsOperationsListArgs,
    ) -> String {
        format!("gcp::pubsublite::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with PubsubliteAdminProjectsLocationsReservationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsReservationsCreateArgs> for Reservation {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsReservationsCreateArgs,
    ) -> String {
        format!("gcp::pubsublite::Reservation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PubsubliteAdminProjectsLocationsReservationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsReservationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsReservationsDeleteArgs,
    ) -> String {
        format!("gcp::pubsublite::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with PubsubliteAdminProjectsLocationsReservationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsReservationsGetArgs> for Reservation {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsReservationsGetArgs,
    ) -> String {
        format!("gcp::pubsublite::Reservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReservationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReservationsResponse with PubsubliteAdminProjectsLocationsReservationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsReservationsListArgs>
    for ListReservationsResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsReservationsListArgs,
    ) -> String {
        format!("gcp::pubsublite::ListReservationsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ListReservationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Reservation
// =============================================================================

/// ResourceIdentifier implementation for Reservation with PubsubliteAdminProjectsLocationsReservationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsReservationsPatchArgs> for Reservation {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsReservationsPatchArgs,
    ) -> String {
        format!("gcp::pubsublite::Reservation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Reservation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReservationTopicsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReservationTopicsResponse with PubsubliteAdminProjectsLocationsReservationsTopicsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsReservationsTopicsListArgs>
    for ListReservationTopicsResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsReservationsTopicsListArgs,
    ) -> String {
        format!(
            "gcp::pubsublite::ListReservationTopicsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ListReservationTopicsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with PubsubliteAdminProjectsLocationsSubscriptionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsSubscriptionsCreateArgs> for Subscription {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsSubscriptionsCreateArgs,
    ) -> String {
        format!("gcp::pubsublite::Subscription/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PubsubliteAdminProjectsLocationsSubscriptionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsSubscriptionsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsSubscriptionsDeleteArgs,
    ) -> String {
        format!("gcp::pubsublite::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with PubsubliteAdminProjectsLocationsSubscriptionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsSubscriptionsGetArgs> for Subscription {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsSubscriptionsGetArgs,
    ) -> String {
        format!("gcp::pubsublite::Subscription/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSubscriptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSubscriptionsResponse with PubsubliteAdminProjectsLocationsSubscriptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsSubscriptionsListArgs>
    for ListSubscriptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsSubscriptionsListArgs,
    ) -> String {
        format!(
            "gcp::pubsublite::ListSubscriptionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ListSubscriptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Subscription
// =============================================================================

/// ResourceIdentifier implementation for Subscription with PubsubliteAdminProjectsLocationsSubscriptionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsSubscriptionsPatchArgs> for Subscription {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsSubscriptionsPatchArgs,
    ) -> String {
        format!("gcp::pubsublite::Subscription/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Subscription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PubsubliteAdminProjectsLocationsSubscriptionsSeekArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsSubscriptionsSeekArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsSubscriptionsSeekArgs,
    ) -> String {
        format!("gcp::pubsublite::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Topic
// =============================================================================

/// ResourceIdentifier implementation for Topic with PubsubliteAdminProjectsLocationsTopicsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsTopicsCreateArgs> for Topic {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsTopicsCreateArgs,
    ) -> String {
        format!("gcp::pubsublite::Topic/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Topic"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PubsubliteAdminProjectsLocationsTopicsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsTopicsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsTopicsDeleteArgs,
    ) -> String {
        format!("gcp::pubsublite::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Topic
// =============================================================================

/// ResourceIdentifier implementation for Topic with PubsubliteAdminProjectsLocationsTopicsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsTopicsGetArgs> for Topic {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsTopicsGetArgs,
    ) -> String {
        format!("gcp::pubsublite::Topic/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Topic"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TopicPartitions
// =============================================================================

/// ResourceIdentifier implementation for TopicPartitions with PubsubliteAdminProjectsLocationsTopicsGetPartitionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsTopicsGetPartitionsArgs>
    for TopicPartitions
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsTopicsGetPartitionsArgs,
    ) -> String {
        format!("gcp::pubsublite::TopicPartitions/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::TopicPartitions"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTopicsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTopicsResponse with PubsubliteAdminProjectsLocationsTopicsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsTopicsListArgs> for ListTopicsResponse {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsTopicsListArgs,
    ) -> String {
        format!("gcp::pubsublite::ListTopicsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ListTopicsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Topic
// =============================================================================

/// ResourceIdentifier implementation for Topic with PubsubliteAdminProjectsLocationsTopicsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsTopicsPatchArgs> for Topic {
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsTopicsPatchArgs,
    ) -> String {
        format!("gcp::pubsublite::Topic/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::Topic"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTopicSubscriptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTopicSubscriptionsResponse with PubsubliteAdminProjectsLocationsTopicsSubscriptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteAdminProjectsLocationsTopicsSubscriptionsListArgs>
    for ListTopicSubscriptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteAdminProjectsLocationsTopicsSubscriptionsListArgs,
    ) -> String {
        format!(
            "gcp::pubsublite::ListTopicSubscriptionsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ListTopicSubscriptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommitCursorResponse
// =============================================================================

/// ResourceIdentifier implementation for CommitCursorResponse with PubsubliteCursorProjectsLocationsSubscriptionsCommitCursorArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteCursorProjectsLocationsSubscriptionsCommitCursorArgs>
    for CommitCursorResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteCursorProjectsLocationsSubscriptionsCommitCursorArgs,
    ) -> String {
        format!(
            "gcp::pubsublite::CommitCursorResponse/{}",
            input.subscription
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::CommitCursorResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListPartitionCursorsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPartitionCursorsResponse with PubsubliteCursorProjectsLocationsSubscriptionsCursorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteCursorProjectsLocationsSubscriptionsCursorsListArgs>
    for ListPartitionCursorsResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteCursorProjectsLocationsSubscriptionsCursorsListArgs,
    ) -> String {
        format!(
            "gcp::pubsublite::ListPartitionCursorsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ListPartitionCursorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ComputeHeadCursorResponse
// =============================================================================

/// ResourceIdentifier implementation for ComputeHeadCursorResponse with PubsubliteTopicStatsProjectsLocationsTopicsComputeHeadCursorArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteTopicStatsProjectsLocationsTopicsComputeHeadCursorArgs>
    for ComputeHeadCursorResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteTopicStatsProjectsLocationsTopicsComputeHeadCursorArgs,
    ) -> String {
        format!("gcp::pubsublite::ComputeHeadCursorResponse/{}", input.topic)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ComputeHeadCursorResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ComputeMessageStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for ComputeMessageStatsResponse with PubsubliteTopicStatsProjectsLocationsTopicsComputeMessageStatsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteTopicStatsProjectsLocationsTopicsComputeMessageStatsArgs>
    for ComputeMessageStatsResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteTopicStatsProjectsLocationsTopicsComputeMessageStatsArgs,
    ) -> String {
        format!(
            "gcp::pubsublite::ComputeMessageStatsResponse/{}",
            input.topic
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ComputeMessageStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ComputeTimeCursorResponse
// =============================================================================

/// ResourceIdentifier implementation for ComputeTimeCursorResponse with PubsubliteTopicStatsProjectsLocationsTopicsComputeTimeCursorArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PubsubliteTopicStatsProjectsLocationsTopicsComputeTimeCursorArgs>
    for ComputeTimeCursorResponse
{
    fn generate_resource_id(
        &self,
        input: &PubsubliteTopicStatsProjectsLocationsTopicsComputeTimeCursorArgs,
    ) -> String {
        format!("gcp::pubsublite::ComputeTimeCursorResponse/{}", input.topic)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::pubsublite::ComputeTimeCursorResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
