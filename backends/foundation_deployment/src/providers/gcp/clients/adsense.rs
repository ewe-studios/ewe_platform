//! Auto-generated API clients for gcp/adsense.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/accounts/{accountsId}
/// Gets information about the selected AdSense account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_get_execute()` to send, or `adsense_accounts_get` for simplest API.

pub fn adsense_accounts_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}
/// Gets information about the selected AdSense account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_get_execute()` or `adsense_accounts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}
/// Gets information about the selected AdSense account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_get_task()`.
/// For the simplest API, use `adsense_accounts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsense_accounts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/accounts/{accountsId}
/// Gets information about the selected AdSense account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_get_builder()` + `adsense_accounts_get_execute()`.
/// For task-level control, use `adsense_accounts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_get(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adsense_accounts_get_builder(client, &args.name)?;
    adsense_accounts_get_execute(builder)
}

/// GET v2/accounts/{accountsId}/adBlockingRecoveryTag
/// Gets the ad blocking recovery tag of an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_get_ad_blocking_recovery_tag_execute()` to send, or `adsense_accounts_get_ad_blocking_recovery_tag` for simplest API.

pub fn adsense_accounts_get_ad_blocking_recovery_tag_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://adsense.googleapis.com/v2/accounts/{}/adBlockingRecoveryTag",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/adBlockingRecoveryTag
/// Gets the ad blocking recovery tag of an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_get_ad_blocking_recovery_tag_execute()` or `adsense_accounts_get_ad_blocking_recovery_tag`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_get_ad_blocking_recovery_tag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_get_ad_blocking_recovery_tag_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdBlockingRecoveryTag>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdBlockingRecoveryTag = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/adBlockingRecoveryTag
/// Gets the ad blocking recovery tag of an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_get_ad_blocking_recovery_tag_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_get_ad_blocking_recovery_tag_task()`.
/// For the simplest API, use `adsense_accounts_get_ad_blocking_recovery_tag()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_get_ad_blocking_recovery_tag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_get_ad_blocking_recovery_tag_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdBlockingRecoveryTag>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_get_ad_blocking_recovery_tag_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_get_ad_blocking_recovery_tag`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsGetAdBlockingRecoveryTagArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/accounts/{accountsId}/adBlockingRecoveryTag
/// Gets the ad blocking recovery tag of an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_get_ad_blocking_recovery_tag_builder()` + `adsense_accounts_get_ad_blocking_recovery_tag_execute()`.
/// For task-level control, use `adsense_accounts_get_ad_blocking_recovery_tag_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_get_ad_blocking_recovery_tag(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsGetAdBlockingRecoveryTagArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdBlockingRecoveryTag>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_get_ad_blocking_recovery_tag_builder(client, &args.name)?;
    adsense_accounts_get_ad_blocking_recovery_tag_execute(builder)
}

/// GET v2/accounts
/// Lists all accounts available to this user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_list_execute()` to send, or `adsense_accounts_list` for simplest API.

pub fn adsense_accounts_list_builder(
    client: &SimpleHttpClient,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts
/// Lists all accounts available to this user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_list_execute()` or `adsense_accounts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts
/// Lists all accounts available to this user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_list_task()`.
/// For the simplest API, use `adsense_accounts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/accounts
/// Lists all accounts available to this user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_list_builder()` + `adsense_accounts_list_execute()`.
/// For task-level control, use `adsense_accounts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_list(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_list_builder(client, &args.pageSize, &args.pageToken)?;
    adsense_accounts_list_execute(builder)
}

/// GET v2/accounts/{accountsId}:listChildAccounts
/// Lists all accounts directly managed by the given AdSense account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_list_child_accounts_execute()` to send, or `adsense_accounts_list_child_accounts` for simplest API.

pub fn adsense_accounts_list_child_accounts_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}:listChildAccounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}:listChildAccounts
/// Lists all accounts directly managed by the given AdSense account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_list_child_accounts_execute()` or `adsense_accounts_list_child_accounts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_list_child_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_list_child_accounts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListChildAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListChildAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}:listChildAccounts
/// Lists all accounts directly managed by the given AdSense account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_list_child_accounts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_list_child_accounts_task()`.
/// For the simplest API, use `adsense_accounts_list_child_accounts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_list_child_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_list_child_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChildAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_list_child_accounts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_list_child_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsListChildAccountsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/accounts/{accountsId}:listChildAccounts
/// Lists all accounts directly managed by the given AdSense account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_list_child_accounts_builder()` + `adsense_accounts_list_child_accounts_execute()`.
/// For task-level control, use `adsense_accounts_list_child_accounts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_list_child_accounts(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsListChildAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChildAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_list_child_accounts_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    adsense_accounts_list_child_accounts_execute(builder)
}

/// GET v2/accounts/{accountsId}/adclients
/// Lists all the ad clients available in an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_adclients_list_execute()` to send, or `adsense_accounts_adclients_list` for simplest API.

pub fn adsense_accounts_adclients_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}/adclients",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/adclients
/// Lists all the ad clients available in an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_adclients_list_execute()` or `adsense_accounts_adclients_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_adclients_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_adclients_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdClientsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdClientsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/adclients
/// Lists all the ad clients available in an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_adclients_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_adclients_list_task()`.
/// For the simplest API, use `adsense_accounts_adclients_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_adclients_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_adclients_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_adclients_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_adclients_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsAdclientsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/accounts/{accountsId}/adclients
/// Lists all the ad clients available in an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_adclients_list_builder()` + `adsense_accounts_adclients_list_execute()`.
/// For task-level control, use `adsense_accounts_adclients_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_adclients_list(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsAdclientsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdClientsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_adclients_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    adsense_accounts_adclients_list_execute(builder)
}

/// GET v2/accounts/{accountsId}/alerts
/// Lists all the alerts available in an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_alerts_list_execute()` to send, or `adsense_accounts_alerts_list` for simplest API.

pub fn adsense_accounts_alerts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}/alerts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/alerts
/// Lists all the alerts available in an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_alerts_list_execute()` or `adsense_accounts_alerts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_alerts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAlertsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/alerts
/// Lists all the alerts available in an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_alerts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_alerts_list_task()`.
/// For the simplest API, use `adsense_accounts_alerts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_alerts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_alerts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_alerts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsAlertsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v2/accounts/{accountsId}/alerts
/// Lists all the alerts available in an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_alerts_list_builder()` + `adsense_accounts_alerts_list_execute()`.
/// For task-level control, use `adsense_accounts_alerts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_alerts_list(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsAlertsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_alerts_list_builder(client, &args.parent, &args.languageCode)?;
    adsense_accounts_alerts_list_execute(builder)
}

/// GET v2/accounts/{accountsId}/payments
/// Lists all the payments available for an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_payments_list_execute()` to send, or `adsense_accounts_payments_list` for simplest API.

pub fn adsense_accounts_payments_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}/payments",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/payments
/// Lists all the payments available for an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_payments_list_execute()` or `adsense_accounts_payments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_payments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_payments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPaymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPaymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/payments
/// Lists all the payments available for an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_payments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_payments_list_task()`.
/// For the simplest API, use `adsense_accounts_payments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_payments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_payments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPaymentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_payments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_payments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsPaymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v2/accounts/{accountsId}/payments
/// Lists all the payments available for an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_payments_list_builder()` + `adsense_accounts_payments_list_execute()`.
/// For task-level control, use `adsense_accounts_payments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_payments_list(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsPaymentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPaymentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_payments_list_builder(client, &args.parent)?;
    adsense_accounts_payments_list_execute(builder)
}

/// GET v2/accounts/{accountsId}/policyIssues
/// Lists all the policy issues where the specified account is involved, both directly and through any AFP child accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_policy_issues_list_execute()` to send, or `adsense_accounts_policy_issues_list` for simplest API.

pub fn adsense_accounts_policy_issues_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}/policyIssues",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/policyIssues
/// Lists all the policy issues where the specified account is involved, both directly and through any AFP child accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_policy_issues_list_execute()` or `adsense_accounts_policy_issues_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_policy_issues_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_policy_issues_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPolicyIssuesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPolicyIssuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/policyIssues
/// Lists all the policy issues where the specified account is involved, both directly and through any AFP child accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_policy_issues_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_policy_issues_list_task()`.
/// For the simplest API, use `adsense_accounts_policy_issues_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_policy_issues_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_policy_issues_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPolicyIssuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_policy_issues_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_policy_issues_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsPolicyIssuesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/accounts/{accountsId}/policyIssues
/// Lists all the policy issues where the specified account is involved, both directly and through any AFP child accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_policy_issues_list_builder()` + `adsense_accounts_policy_issues_list_execute()`.
/// For task-level control, use `adsense_accounts_policy_issues_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_policy_issues_list(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsPolicyIssuesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPolicyIssuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_policy_issues_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    adsense_accounts_policy_issues_list_execute(builder)
}

/// GET v2/accounts/{accountsId}/reports:generate
/// Generates an ad hoc report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_reports_generate_execute()` to send, or `adsense_accounts_reports_generate` for simplest API.

pub fn adsense_accounts_reports_generate_builder(
    client: &SimpleHttpClient,
    account: &String,
    currencyCode: &Option<String>,
    dateRange: &Option<String>,
    dimensions: &Option<String>,
    endDate_day: &Option<i32>,
    endDate_month: &Option<i32>,
    endDate_year: &Option<i32>,
    filters: &Option<String>,
    languageCode: &Option<String>,
    limit: &Option<i32>,
    metrics: &Option<String>,
    orderBy: &Option<String>,
    reportingTimeZone: &Option<String>,
    startDate_day: &Option<i32>,
    startDate_month: &Option<i32>,
    startDate_year: &Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}/reports:generate",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = currencyCode.as_ref() {
        query_parts.push(format!("currencyCode={}", val));
    }
    if let Some(val) = dateRange.as_ref() {
        query_parts.push(format!("dateRange={}", val));
    }
    if let Some(val) = dimensions.as_ref() {
        query_parts.push(format!("dimensions={}", val));
    }
    if let Some(val) = endDate_day.as_ref() {
        query_parts.push(format!("endDate.day={}", val));
    }
    if let Some(val) = endDate_month.as_ref() {
        query_parts.push(format!("endDate.month={}", val));
    }
    if let Some(val) = endDate_year.as_ref() {
        query_parts.push(format!("endDate.year={}", val));
    }
    if let Some(val) = filters.as_ref() {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = metrics.as_ref() {
        query_parts.push(format!("metrics={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = reportingTimeZone.as_ref() {
        query_parts.push(format!("reportingTimeZone={}", val));
    }
    if let Some(val) = startDate_day.as_ref() {
        query_parts.push(format!("startDate.day={}", val));
    }
    if let Some(val) = startDate_month.as_ref() {
        query_parts.push(format!("startDate.month={}", val));
    }
    if let Some(val) = startDate_year.as_ref() {
        query_parts.push(format!("startDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/reports:generate
/// Generates an ad hoc report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_reports_generate_execute()` or `adsense_accounts_reports_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_reports_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_reports_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportResult = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/reports:generate
/// Generates an ad hoc report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_reports_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_reports_generate_task()`.
/// For the simplest API, use `adsense_accounts_reports_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_reports_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_reports_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportResult>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_reports_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_reports_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsReportsGenerateArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: currencyCode
    pub currencyCode: Option<String>,
    /// Query parameter: dateRange
    pub dateRange: Option<String>,
    /// Query parameter: dimensions
    pub dimensions: Option<String>,
    /// Query parameter: endDate_day
    pub endDate_day: Option<i32>,
    /// Query parameter: endDate_month
    pub endDate_month: Option<i32>,
    /// Query parameter: endDate_year
    pub endDate_year: Option<i32>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: metrics
    pub metrics: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: reportingTimeZone
    pub reportingTimeZone: Option<String>,
    /// Query parameter: startDate_day
    pub startDate_day: Option<i32>,
    /// Query parameter: startDate_month
    pub startDate_month: Option<i32>,
    /// Query parameter: startDate_year
    pub startDate_year: Option<i32>,
}

/// GET v2/accounts/{accountsId}/reports:generate
/// Generates an ad hoc report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_reports_generate_builder()` + `adsense_accounts_reports_generate_execute()`.
/// For task-level control, use `adsense_accounts_reports_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_reports_generate(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsReportsGenerateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReportResult>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_reports_generate_builder(
        client,
        &args.account,
        &args.currencyCode,
        &args.dateRange,
        &args.dimensions,
        &args.endDate_day,
        &args.endDate_month,
        &args.endDate_year,
        &args.filters,
        &args.languageCode,
        &args.limit,
        &args.metrics,
        &args.orderBy,
        &args.reportingTimeZone,
        &args.startDate_day,
        &args.startDate_month,
        &args.startDate_year,
    )?;
    adsense_accounts_reports_generate_execute(builder)
}

/// GET v2/accounts/{accountsId}/reports:generateCsv
/// Generates a csv formatted ad hoc report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_reports_generate_csv_execute()` to send, or `adsense_accounts_reports_generate_csv` for simplest API.

pub fn adsense_accounts_reports_generate_csv_builder(
    client: &SimpleHttpClient,
    account: &String,
    currencyCode: &Option<String>,
    dateRange: &Option<String>,
    dimensions: &Option<String>,
    endDate_day: &Option<i32>,
    endDate_month: &Option<i32>,
    endDate_year: &Option<i32>,
    filters: &Option<String>,
    languageCode: &Option<String>,
    limit: &Option<i32>,
    metrics: &Option<String>,
    orderBy: &Option<String>,
    reportingTimeZone: &Option<String>,
    startDate_day: &Option<i32>,
    startDate_month: &Option<i32>,
    startDate_year: &Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://adsense.googleapis.com/v2/accounts/{}/reports:generateCsv",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = currencyCode.as_ref() {
        query_parts.push(format!("currencyCode={}", val));
    }
    if let Some(val) = dateRange.as_ref() {
        query_parts.push(format!("dateRange={}", val));
    }
    if let Some(val) = dimensions.as_ref() {
        query_parts.push(format!("dimensions={}", val));
    }
    if let Some(val) = endDate_day.as_ref() {
        query_parts.push(format!("endDate.day={}", val));
    }
    if let Some(val) = endDate_month.as_ref() {
        query_parts.push(format!("endDate.month={}", val));
    }
    if let Some(val) = endDate_year.as_ref() {
        query_parts.push(format!("endDate.year={}", val));
    }
    if let Some(val) = filters.as_ref() {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = metrics.as_ref() {
        query_parts.push(format!("metrics={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = reportingTimeZone.as_ref() {
        query_parts.push(format!("reportingTimeZone={}", val));
    }
    if let Some(val) = startDate_day.as_ref() {
        query_parts.push(format!("startDate.day={}", val));
    }
    if let Some(val) = startDate_month.as_ref() {
        query_parts.push(format!("startDate.month={}", val));
    }
    if let Some(val) = startDate_year.as_ref() {
        query_parts.push(format!("startDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/reports:generateCsv
/// Generates a csv formatted ad hoc report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_reports_generate_csv_execute()` or `adsense_accounts_reports_generate_csv`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_reports_generate_csv_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_reports_generate_csv_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/reports:generateCsv
/// Generates a csv formatted ad hoc report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_reports_generate_csv_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_reports_generate_csv_task()`.
/// For the simplest API, use `adsense_accounts_reports_generate_csv()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_reports_generate_csv_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_reports_generate_csv_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = adsense_accounts_reports_generate_csv_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_reports_generate_csv`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsReportsGenerateCsvArgs {
    /// Path parameter: account
    pub account: String,
    /// Query parameter: currencyCode
    pub currencyCode: Option<String>,
    /// Query parameter: dateRange
    pub dateRange: Option<String>,
    /// Query parameter: dimensions
    pub dimensions: Option<String>,
    /// Query parameter: endDate_day
    pub endDate_day: Option<i32>,
    /// Query parameter: endDate_month
    pub endDate_month: Option<i32>,
    /// Query parameter: endDate_year
    pub endDate_year: Option<i32>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: limit
    pub limit: Option<i32>,
    /// Query parameter: metrics
    pub metrics: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: reportingTimeZone
    pub reportingTimeZone: Option<String>,
    /// Query parameter: startDate_day
    pub startDate_day: Option<i32>,
    /// Query parameter: startDate_month
    pub startDate_month: Option<i32>,
    /// Query parameter: startDate_year
    pub startDate_year: Option<i32>,
}

/// GET v2/accounts/{accountsId}/reports:generateCsv
/// Generates a csv formatted ad hoc report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_reports_generate_csv_builder()` + `adsense_accounts_reports_generate_csv_execute()`.
/// For task-level control, use `adsense_accounts_reports_generate_csv_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_reports_generate_csv(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsReportsGenerateCsvArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = adsense_accounts_reports_generate_csv_builder(
        client,
        &args.account,
        &args.currencyCode,
        &args.dateRange,
        &args.dimensions,
        &args.endDate_day,
        &args.endDate_month,
        &args.endDate_year,
        &args.filters,
        &args.languageCode,
        &args.limit,
        &args.metrics,
        &args.orderBy,
        &args.reportingTimeZone,
        &args.startDate_day,
        &args.startDate_month,
        &args.startDate_year,
    )?;
    adsense_accounts_reports_generate_csv_execute(builder)
}

/// GET v2/accounts/{accountsId}/reports/saved
/// Lists saved reports.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_reports_saved_list_execute()` to send, or `adsense_accounts_reports_saved_list` for simplest API.

pub fn adsense_accounts_reports_saved_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}/reports/saved",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/reports/saved
/// Lists saved reports.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_reports_saved_list_execute()` or `adsense_accounts_reports_saved_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_reports_saved_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_reports_saved_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSavedReportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/reports/saved
/// Lists saved reports.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_reports_saved_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_reports_saved_list_task()`.
/// For the simplest API, use `adsense_accounts_reports_saved_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_reports_saved_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_reports_saved_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_reports_saved_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_reports_saved_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsReportsSavedListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/accounts/{accountsId}/reports/saved
/// Lists saved reports.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_reports_saved_list_builder()` + `adsense_accounts_reports_saved_list_execute()`.
/// For task-level control, use `adsense_accounts_reports_saved_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_reports_saved_list(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsReportsSavedListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = adsense_accounts_reports_saved_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    adsense_accounts_reports_saved_list_execute(builder)
}

/// GET v2/accounts/{accountsId}/sites
/// Lists all the sites available in an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `adsense_accounts_sites_list_execute()` to send, or `adsense_accounts_sites_list` for simplest API.

pub fn adsense_accounts_sites_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://adsense.googleapis.com/v2/accounts/{}/sites",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/accounts/{accountsId}/sites
/// Lists all the sites available in an account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `adsense_accounts_sites_list_execute()` or `adsense_accounts_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/accounts/{accountsId}/sites
/// Lists all the sites available in an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `adsense_accounts_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `adsense_accounts_sites_list_task()`.
/// For the simplest API, use `adsense_accounts_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `adsense_accounts_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn adsense_accounts_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = adsense_accounts_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`adsense_accounts_sites_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdsenseAccountsSitesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/accounts/{accountsId}/sites
/// Lists all the sites available in an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `adsense_accounts_sites_list_builder()` + `adsense_accounts_sites_list_execute()`.
/// For task-level control, use `adsense_accounts_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn adsense_accounts_sites_list(
    client: &SimpleHttpClient,
    args: &AdsenseAccountsSitesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        adsense_accounts_sites_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    adsense_accounts_sites_list_execute(builder)
}
