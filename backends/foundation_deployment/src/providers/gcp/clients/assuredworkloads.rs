//! Auto-generated API clients for gcp/assuredworkloads.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_operations_get_execute()` to send, or `assuredworkloads_organizations_locations_operations_get` for simplest API.

pub fn assuredworkloads_organizations_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_operations_get_execute()` or `assuredworkloads_organizations_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_operations_get_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_operations_get_builder()` + `assuredworkloads_organizations_locations_operations_get_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_operations_get(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        assuredworkloads_organizations_locations_operations_get_builder(client, &args.name)?;
    assuredworkloads_organizations_locations_operations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_operations_list_execute()` to send, or `assuredworkloads_organizations_locations_operations_list` for simplest API.

pub fn assuredworkloads_organizations_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_operations_list_execute()` or `assuredworkloads_organizations_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_operations_list_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_operations_list_builder()` + `assuredworkloads_organizations_locations_operations_list_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_operations_list(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    assuredworkloads_organizations_locations_operations_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:analyzeWorkloadMove
/// Analyzes a hypothetical move of a source resource to a target workload to surface compliance risks. The analysis is best effort and is not guaranteed to be exhaustive.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_analyze_workload_move_execute()` to send, or `assuredworkloads_organizations_locations_workloads_analyze_workload_move` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_analyze_workload_move_builder<R>(
    client: &SimpleHttpClient<R>,
    target: &String,
    assetTypes: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    project: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}:analyzeWorkloadMove",
        target,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assetTypes.as_ref() {
        query_parts.push(format!("assetTypes={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = project.as_ref() {
        query_parts.push(format!("project={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:analyzeWorkloadMove
/// Analyzes a hypothetical move of a source resource to a target workload to surface compliance risks. The analysis is best effort and is not guaranteed to be exhaustive.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_analyze_workload_move_execute()` or `assuredworkloads_organizations_locations_workloads_analyze_workload_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_analyze_workload_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_analyze_workload_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:analyzeWorkloadMove
/// Analyzes a hypothetical move of a source resource to a target workload to surface compliance risks. The analysis is best effort and is not guaranteed to be exhaustive.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_analyze_workload_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_analyze_workload_move_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_analyze_workload_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_analyze_workload_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_analyze_workload_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        assuredworkloads_organizations_locations_workloads_analyze_workload_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_analyze_workload_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsAnalyzeWorkloadMoveArgs {
    /// Path parameter: target
    pub target: String,
    /// Query parameter: assetTypes
    pub assetTypes: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: project
    pub project: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:analyzeWorkloadMove
/// Analyzes a hypothetical move of a source resource to a target workload to surface compliance risks. The analysis is best effort and is not guaranteed to be exhaustive.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_analyze_workload_move_builder()` + `assuredworkloads_organizations_locations_workloads_analyze_workload_move_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_analyze_workload_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_analyze_workload_move(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsAnalyzeWorkloadMoveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_analyze_workload_move_builder(
        client,
        &args.target,
        &args.assetTypes,
        &args.pageSize,
        &args.pageToken,
        &args.project,
    )?;
    assuredworkloads_organizations_locations_workloads_analyze_workload_move_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Creates Assured Workload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_create_execute()` to send, or `assuredworkloads_organizations_locations_workloads_create` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    externalId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = externalId.as_ref() {
        query_parts.push(format!("externalId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Creates Assured Workload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_create_execute()` or `assuredworkloads_organizations_locations_workloads_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Creates Assured Workload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_create_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: externalId
    pub externalId: Option<Option<String>>,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Creates Assured Workload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_create_builder()` + `assuredworkloads_organizations_locations_workloads_create_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_create(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_create_builder(
        client,
        &args.parent,
        &args.externalId,
    )?;
    assuredworkloads_organizations_locations_workloads_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Deletes the workload. Make sure that workload''s direct children are already in a deleted state, otherwise the request will fail with a FAILED_PRECONDITION error. In addition to assuredworkloads.workload.delete permission, the user should also have orgpolicy.policy.set permission on the deleted folder to remove Assured Workloads OrgPolicies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_delete_execute()` to send, or `assuredworkloads_organizations_locations_workloads_delete` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Deletes the workload. Make sure that workload''s direct children are already in a deleted state, otherwise the request will fail with a FAILED_PRECONDITION error. In addition to assuredworkloads.workload.delete permission, the user should also have orgpolicy.policy.set permission on the deleted folder to remove Assured Workloads OrgPolicies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_delete_execute()` or `assuredworkloads_organizations_locations_workloads_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Deletes the workload. Make sure that workload''s direct children are already in a deleted state, otherwise the request will fail with a FAILED_PRECONDITION error. In addition to assuredworkloads.workload.delete permission, the user should also have orgpolicy.policy.set permission on the deleted folder to remove Assured Workloads OrgPolicies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_delete_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Deletes the workload. Make sure that workload''s direct children are already in a deleted state, otherwise the request will fail with a FAILED_PRECONDITION error. In addition to assuredworkloads.workload.delete permission, the user should also have orgpolicy.policy.set permission on the deleted folder to remove Assured Workloads OrgPolicies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_delete_builder()` + `assuredworkloads_organizations_locations_workloads_delete_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_delete(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_delete_builder(
        client, &args.name, &args.etag,
    )?;
    assuredworkloads_organizations_locations_workloads_delete_execute(builder)
}

/// PUT v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableComplianceUpdates
/// This endpoint enables Assured Workloads service to offer compliance updates for the folder based assured workload. It sets up an Assured Workloads Service Agent, having permissions to read compliance controls (for example: Org Policies) applied on the workload. The caller must have resourcemanager.folders.`getIamPolicy` and resourcemanager.folders.`setIamPolicy` permissions on the assured workload folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_execute()` to send, or `assuredworkloads_organizations_locations_workloads_enable_compliance_updates` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_enable_compliance_updates_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}:enableComplianceUpdates",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableComplianceUpdates
/// This endpoint enables Assured Workloads service to offer compliance updates for the folder based assured workload. It sets up an Assured Workloads Service Agent, having permissions to read compliance controls (for example: Org Policies) applied on the workload. The caller must have resourcemanager.folders.`getIamPolicy` and resourcemanager.folders.`setIamPolicy` permissions on the assured workload folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_execute()` or `assuredworkloads_organizations_locations_workloads_enable_compliance_updates`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_enable_compliance_updates_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableComplianceUpdates
/// This endpoint enables Assured Workloads service to offer compliance updates for the folder based assured workload. It sets up an Assured Workloads Service Agent, having permissions to read compliance controls (for example: Org Policies) applied on the workload. The caller must have resourcemanager.folders.`getIamPolicy` and resourcemanager.folders.`setIamPolicy` permissions on the assured workload folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_enable_compliance_updates()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_enable_compliance_updates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        assuredworkloads_organizations_locations_workloads_enable_compliance_updates_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_enable_compliance_updates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsEnableComplianceUpdatesArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableComplianceUpdates
/// This endpoint enables Assured Workloads service to offer compliance updates for the folder based assured workload. It sets up an Assured Workloads Service Agent, having permissions to read compliance controls (for example: Org Policies) applied on the workload. The caller must have resourcemanager.folders.`getIamPolicy` and resourcemanager.folders.`setIamPolicy` permissions on the assured workload folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_builder()` + `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_enable_compliance_updates_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_enable_compliance_updates(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsEnableComplianceUpdatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        assuredworkloads_organizations_locations_workloads_enable_compliance_updates_builder(
            client, &args.name,
        )?;
    assuredworkloads_organizations_locations_workloads_enable_compliance_updates_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableResourceMonitoring
/// Enable resource violation monitoring for a workload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_execute()` to send, or `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}:enableResourceMonitoring",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableResourceMonitoring
/// Enable resource violation monitoring for a workload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_execute()` or `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableResourceMonitoring
/// Enable resource violation monitoring for a workload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_enable_resource_monitoring`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsEnableResourceMonitoringArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:enableResourceMonitoring
/// Enable resource violation monitoring for a workload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_builder()` + `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_enable_resource_monitoring(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsEnableResourceMonitoringArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_builder(
            client, &args.name,
        )?;
    assuredworkloads_organizations_locations_workloads_enable_resource_monitoring_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Gets Assured Workload associated with a CRM Node
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_get_execute()` to send, or `assuredworkloads_organizations_locations_workloads_get` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Gets Assured Workload associated with a CRM Node
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_get_execute()` or `assuredworkloads_organizations_locations_workloads_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1Workload = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Gets Assured Workload associated with a CRM Node
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_get_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Gets Assured Workload associated with a CRM Node
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_get_builder()` + `assuredworkloads_organizations_locations_workloads_get_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_get(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        assuredworkloads_organizations_locations_workloads_get_builder(client, &args.name)?;
    assuredworkloads_organizations_locations_workloads_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Lists Assured Workloads under a CRM Node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_list_execute()` to send, or `assuredworkloads_organizations_locations_workloads_list` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Lists Assured Workloads under a CRM Node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_list_execute()` or `assuredworkloads_organizations_locations_workloads_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1ListWorkloadsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1ListWorkloadsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Lists Assured Workloads under a CRM Node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_list_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1ListWorkloadsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads
/// Lists Assured Workloads under a CRM Node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_list_builder()` + `assuredworkloads_organizations_locations_workloads_list_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_list(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1ListWorkloadsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    assuredworkloads_organizations_locations_workloads_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:mutatePartnerPermissions
/// Update the permissions settings for an existing partner workload. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_execute()` to send, or `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}:mutatePartnerPermissions",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:mutatePartnerPermissions
/// Update the permissions settings for an existing partner workload. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_execute()` or `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1Workload = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:mutatePartnerPermissions
/// Update the permissions settings for an existing partner workload. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_mutate_partner_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsMutatePartnerPermissionsArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:mutatePartnerPermissions
/// Update the permissions settings for an existing partner workload. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_builder()` + `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_mutate_partner_permissions(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsMutatePartnerPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_builder(
            client, &args.name,
        )?;
    assuredworkloads_organizations_locations_workloads_mutate_partner_permissions_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Updates an existing workload. Currently allows updating of workload display_name and labels. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_patch_execute()` to send, or `assuredworkloads_organizations_locations_workloads_patch` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Updates an existing workload. Currently allows updating of workload display_name and labels. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_patch_execute()` or `assuredworkloads_organizations_locations_workloads_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1Workload = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Updates an existing workload. Currently allows updating of workload display_name and labels. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_patch_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}
/// Updates an existing workload. Currently allows updating of workload display_name and labels. For force updates don''t set etag field in the Workload. Only one update operation per workload can be in progress.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_patch_builder()` + `assuredworkloads_organizations_locations_workloads_patch_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_patch(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Workload>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    assuredworkloads_organizations_locations_workloads_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:restrictAllowedResources
/// Restrict the list of resources allowed in the Workload environment. The current list of allowed products can be found at <https://cloud.google.`com/assured-workloads/docs/supported-products`> In addition to assuredworkloads.workload.update permission, the user should also have orgpolicy.policy.set permission on the folder resource to use this functionality.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_execute()` to send, or `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}:restrictAllowedResources",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:restrictAllowedResources
/// Restrict the list of resources allowed in the Workload environment. The current list of allowed products can be found at <https://cloud.google.`com/assured-workloads/docs/supported-products`> In addition to assuredworkloads.workload.update permission, the user should also have orgpolicy.policy.set permission on the folder resource to use this functionality.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_execute()` or `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:restrictAllowedResources
/// Restrict the list of resources allowed in the Workload environment. The current list of allowed products can be found at <https://cloud.google.`com/assured-workloads/docs/supported-products`> In addition to assuredworkloads.workload.update permission, the user should also have orgpolicy.policy.set permission on the folder resource to use this functionality.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_restrict_allowed_resources`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsRestrictAllowedResourcesArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}:restrictAllowedResources
/// Restrict the list of resources allowed in the Workload environment. The current list of allowed products can be found at <https://cloud.google.`com/assured-workloads/docs/supported-products`> In addition to assuredworkloads.workload.update permission, the user should also have orgpolicy.policy.set permission on the folder resource to use this functionality.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_builder()` + `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_restrict_allowed_resources(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsRestrictAllowedResourcesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_builder(
            client, &args.name,
        )?;
    assuredworkloads_organizations_locations_workloads_restrict_allowed_resources_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates/{updatesId}:apply
/// This endpoint creates a new operation to apply the given update.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_updates_apply_execute()` to send, or `assuredworkloads_organizations_locations_workloads_updates_apply` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_updates_apply_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}/updates/{updatesId}:apply",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates/{updatesId}:apply
/// This endpoint creates a new operation to apply the given update.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_updates_apply_execute()` or `assuredworkloads_organizations_locations_workloads_updates_apply`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_updates_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_updates_apply_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates/{updatesId}:apply
/// This endpoint creates a new operation to apply the given update.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_updates_apply_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_updates_apply_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_updates_apply()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_updates_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_updates_apply_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_updates_apply_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_updates_apply`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesApplyArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates/{updatesId}:apply
/// This endpoint creates a new operation to apply the given update.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_updates_apply_builder()` + `assuredworkloads_organizations_locations_workloads_updates_apply_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_updates_apply_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_updates_apply(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesApplyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_updates_apply_builder(
        client, &args.name,
    )?;
    assuredworkloads_organizations_locations_workloads_updates_apply_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates
/// This endpoint lists all updates for the given workload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_updates_list_execute()` to send, or `assuredworkloads_organizations_locations_workloads_updates_list` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_updates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}/updates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates
/// This endpoint lists all updates for the given workload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_updates_list_execute()` or `assuredworkloads_organizations_locations_workloads_updates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_updates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_updates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates
/// This endpoint lists all updates for the given workload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_updates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_updates_list_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_updates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_updates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_updates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_updates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_updates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/updates
/// This endpoint lists all updates for the given workload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_updates_list_builder()` + `assuredworkloads_organizations_locations_workloads_updates_list_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_updates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_updates_list(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_updates_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    assuredworkloads_organizations_locations_workloads_updates_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}:acknowledge
/// Acknowledges an existing violation. By acknowledging a violation, users acknowledge the existence of a compliance violation in their workload and decide to ignore it due to a valid business justification. Acknowledgement is a permanent operation and it cannot be reverted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_violations_acknowledge_execute()` to send, or `assuredworkloads_organizations_locations_workloads_violations_acknowledge` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_violations_acknowledge_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}:acknowledge",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}:acknowledge
/// Acknowledges an existing violation. By acknowledging a violation, users acknowledge the existence of a compliance violation in their workload and decide to ignore it due to a valid business justification. Acknowledgement is a permanent operation and it cannot be reverted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_violations_acknowledge_execute()` or `assuredworkloads_organizations_locations_workloads_violations_acknowledge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_violations_acknowledge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_violations_acknowledge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}:acknowledge
/// Acknowledges an existing violation. By acknowledging a violation, users acknowledge the existence of a compliance violation in their workload and decide to ignore it due to a valid business justification. Acknowledgement is a permanent operation and it cannot be reverted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_violations_acknowledge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_violations_acknowledge_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_violations_acknowledge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_violations_acknowledge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_violations_acknowledge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        assuredworkloads_organizations_locations_workloads_violations_acknowledge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_violations_acknowledge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsViolationsAcknowledgeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}:acknowledge
/// Acknowledges an existing violation. By acknowledging a violation, users acknowledge the existence of a compliance violation in their workload and decide to ignore it due to a valid business justification. Acknowledgement is a permanent operation and it cannot be reverted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_violations_acknowledge_builder()` + `assuredworkloads_organizations_locations_workloads_violations_acknowledge_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_violations_acknowledge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_violations_acknowledge(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsViolationsAcknowledgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        assuredworkloads_organizations_locations_workloads_violations_acknowledge_builder(
            client, &args.name,
        )?;
    assuredworkloads_organizations_locations_workloads_violations_acknowledge_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}
/// Retrieves Assured Workload Violation based on ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_violations_get_execute()` to send, or `assuredworkloads_organizations_locations_workloads_violations_get` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_violations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}
/// Retrieves Assured Workload Violation based on ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_violations_get_execute()` or `assuredworkloads_organizations_locations_workloads_violations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_violations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_violations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Violation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1Violation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}
/// Retrieves Assured Workload Violation based on ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_violations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_violations_get_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_violations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_violations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_violations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Violation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_violations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_violations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsViolationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations/{violationsId}
/// Retrieves Assured Workload Violation based on ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_violations_get_builder()` + `assuredworkloads_organizations_locations_workloads_violations_get_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_violations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_violations_get(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsViolationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1Violation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_violations_get_builder(
        client, &args.name,
    )?;
    assuredworkloads_organizations_locations_workloads_violations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations
/// Lists the Violations in the AssuredWorkload Environment. Callers may also choose to read across multiple Workloads as per [AIP-159](<https://google.aip.`dev/159`>) by using ''-'' (the hyphen or dash character) as a wildcard character instead of workload-id in the parent. Format `organizations/{org_id}/locations/{location}/workloads/-`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `assuredworkloads_organizations_locations_workloads_violations_list_execute()` to send, or `assuredworkloads_organizations_locations_workloads_violations_list` for simplest API.

pub fn assuredworkloads_organizations_locations_workloads_violations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    interval_endTime: &Option<Option<String>>,
    interval_startTime: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://assuredworkloads.googleapis.com/v1/organizations/{}/locations/{locationsId}/workloads/{workloadsId}/violations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime.as_ref() {
        query_parts.push(format!("interval.endTime={}", val));
    }
    if let Some(val) = interval_startTime.as_ref() {
        query_parts.push(format!("interval.startTime={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations
/// Lists the Violations in the AssuredWorkload Environment. Callers may also choose to read across multiple Workloads as per [AIP-159](<https://google.aip.`dev/159`>) by using ''-'' (the hyphen or dash character) as a wildcard character instead of workload-id in the parent. Format `organizations/{org_id}/locations/{location}/workloads/-`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `assuredworkloads_organizations_locations_workloads_violations_list_execute()` or `assuredworkloads_organizations_locations_workloads_violations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_violations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_violations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAssuredworkloadsV1ListViolationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAssuredworkloadsV1ListViolationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations
/// Lists the Violations in the AssuredWorkload Environment. Callers may also choose to read across multiple Workloads as per [AIP-159](<https://google.aip.`dev/159`>) by using ''-'' (the hyphen or dash character) as a wildcard character instead of workload-id in the parent. Format `organizations/{org_id}/locations/{location}/workloads/-`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `assuredworkloads_organizations_locations_workloads_violations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_violations_list_task()`.
/// For the simplest API, use `assuredworkloads_organizations_locations_workloads_violations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `assuredworkloads_organizations_locations_workloads_violations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn assuredworkloads_organizations_locations_workloads_violations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1ListViolationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = assuredworkloads_organizations_locations_workloads_violations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`assuredworkloads_organizations_locations_workloads_violations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AssuredworkloadsOrganizationsLocationsWorkloadsViolationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: interval_endTime
    pub interval_endTime: Option<Option<String>>,
    /// Query parameter: interval_startTime
    pub interval_startTime: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/workloads/{workloadsId}/violations
/// Lists the Violations in the AssuredWorkload Environment. Callers may also choose to read across multiple Workloads as per [AIP-159](<https://google.aip.`dev/159`>) by using ''-'' (the hyphen or dash character) as a wildcard character instead of workload-id in the parent. Format `organizations/{org_id}/locations/{location}/workloads/-`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `assuredworkloads_organizations_locations_workloads_violations_list_builder()` + `assuredworkloads_organizations_locations_workloads_violations_list_execute()`.
/// For task-level control, use `assuredworkloads_organizations_locations_workloads_violations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn assuredworkloads_organizations_locations_workloads_violations_list(
    client: &SimpleHttpClient,
    args: &AssuredworkloadsOrganizationsLocationsWorkloadsViolationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAssuredworkloadsV1ListViolationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = assuredworkloads_organizations_locations_workloads_violations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.interval_endTime,
        &args.interval_startTime,
        &args.pageSize,
        &args.pageToken,
    )?;
    assuredworkloads_organizations_locations_workloads_violations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with AssuredworkloadsOrganizationsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsOperationsGetArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with AssuredworkloadsOrganizationsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse with AssuredworkloadsOrganizationsLocationsWorkloadsAnalyzeWorkloadMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsAnalyzeWorkloadMoveArgs>
    for GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsAnalyzeWorkloadMoveArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse/{}",
            input.target
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1AnalyzeWorkloadMoveResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with AssuredworkloadsOrganizationsLocationsWorkloadsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsCreateArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with AssuredworkloadsOrganizationsLocationsWorkloadsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsDeleteArgs,
    ) -> String {
        format!("gcp::assuredworkloads::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse with AssuredworkloadsOrganizationsLocationsWorkloadsEnableComplianceUpdatesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsEnableComplianceUpdatesArgs>
    for GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsEnableComplianceUpdatesArgs,
    ) -> String {
        format!("gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1EnableComplianceUpdatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse with AssuredworkloadsOrganizationsLocationsWorkloadsEnableResourceMonitoringArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsEnableResourceMonitoringArgs>
    for GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsEnableResourceMonitoringArgs,
    ) -> String {
        format!("gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1EnableResourceMonitoringResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Workload
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Workload with AssuredworkloadsOrganizationsLocationsWorkloadsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsGetArgs>
    for GoogleCloudAssuredworkloadsV1Workload
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsGetArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Workload/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Workload"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1ListWorkloadsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1ListWorkloadsResponse with AssuredworkloadsOrganizationsLocationsWorkloadsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsListArgs>
    for GoogleCloudAssuredworkloadsV1ListWorkloadsResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsListArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1ListWorkloadsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1ListWorkloadsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Workload
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Workload with AssuredworkloadsOrganizationsLocationsWorkloadsMutatePartnerPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsMutatePartnerPermissionsArgs>
    for GoogleCloudAssuredworkloadsV1Workload
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsMutatePartnerPermissionsArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Workload/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Workload"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Workload
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Workload with AssuredworkloadsOrganizationsLocationsWorkloadsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsPatchArgs>
    for GoogleCloudAssuredworkloadsV1Workload
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsPatchArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Workload/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Workload"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse with AssuredworkloadsOrganizationsLocationsWorkloadsRestrictAllowedResourcesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsRestrictAllowedResourcesArgs>
    for GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsRestrictAllowedResourcesArgs,
    ) -> String {
        format!("gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1RestrictAllowedResourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesApplyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesApplyArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesApplyArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse with AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesListArgs>
    for GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsUpdatesListArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1ListWorkloadUpdatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse with AssuredworkloadsOrganizationsLocationsWorkloadsViolationsAcknowledgeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsViolationsAcknowledgeArgs>
    for GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsViolationsAcknowledgeArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1AcknowledgeViolationResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Violation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1Violation with AssuredworkloadsOrganizationsLocationsWorkloadsViolationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsViolationsGetArgs>
    for GoogleCloudAssuredworkloadsV1Violation
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsViolationsGetArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Violation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1Violation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1ListViolationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAssuredworkloadsV1ListViolationsResponse with AssuredworkloadsOrganizationsLocationsWorkloadsViolationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AssuredworkloadsOrganizationsLocationsWorkloadsViolationsListArgs>
    for GoogleCloudAssuredworkloadsV1ListViolationsResponse
{
    fn generate_resource_id(
        &self,
        input: &AssuredworkloadsOrganizationsLocationsWorkloadsViolationsListArgs,
    ) -> String {
        format!(
            "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1ListViolationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::assuredworkloads::GoogleCloudAssuredworkloadsV1ListViolationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
