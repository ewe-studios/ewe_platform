//! Auto-generated API clients for gcp/storagetransfer.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/storagetransfer`

#![cfg(feature = "gcp/storagetransfer")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/googleServiceAccounts/{projectId}
/// Returns the Google service account that is used by Storage Transfer Service to access buckets in the project where transfers run or in other projects. Each Google service account is associated with one Google Cloud project. Users should add this service account to the Google Cloud Storage bucket ACLs to grant access to Storage Transfer Service. This service account is created and owned by Storage Transfer Service and can only be used by Storage Transfer Service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_google_service_accounts_get_execute()` to send, or `storagetransfer_google_service_accounts_get` for simplest API.

pub fn storagetransfer_google_service_accounts_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/googleServiceAccounts/{}",
        projectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/googleServiceAccounts/{projectId}
/// Returns the Google service account that is used by Storage Transfer Service to access buckets in the project where transfers run or in other projects. Each Google service account is associated with one Google Cloud project. Users should add this service account to the Google Cloud Storage bucket ACLs to grant access to Storage Transfer Service. This service account is created and owned by Storage Transfer Service and can only be used by Storage Transfer Service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_google_service_accounts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_google_service_accounts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_google_service_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleServiceAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleServiceAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/googleServiceAccounts/{projectId}
/// Returns the Google service account that is used by Storage Transfer Service to access buckets in the project where transfers run or in other projects. Each Google service account is associated with one Google Cloud project. Users should add this service account to the Google Cloud Storage bucket ACLs to grant access to Storage Transfer Service. This service account is created and owned by Storage Transfer Service and can only be used by Storage Transfer Service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_google_service_accounts_get_builder()` + `storagetransfer_google_service_accounts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_google_service_accounts_get(
    client: &SimpleHttpClient,
    projectId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleServiceAccount>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = storagetransfer_google_service_accounts_get_builder(client, projectId)?;
    storagetransfer_google_service_accounts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/agentPools
/// Creates an agent pool resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_projects_agent_pools_create_execute()` to send, or `storagetransfer_projects_agent_pools_create` for simplest API.

pub fn storagetransfer_projects_agent_pools_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    agentPoolId: Option<&str>,
    body: &AgentPool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/projects/{}/agentPools",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = agentPoolId {
        query_parts.push(format!("agentPoolId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/agentPools
/// Creates an agent pool resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_projects_agent_pools_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_projects_agent_pools_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AgentPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AgentPool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/agentPools
/// Creates an agent pool resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_projects_agent_pools_create_builder()` + `storagetransfer_projects_agent_pools_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_create(
    client: &SimpleHttpClient,
    projectId: &str,
    agentPoolId: Option<&str>,
    body: &AgentPool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AgentPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        storagetransfer_projects_agent_pools_create_builder(client, projectId, agentPoolId, body)?;
    storagetransfer_projects_agent_pools_create_execute(builder)
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Deletes an agent pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_projects_agent_pools_delete_execute()` to send, or `storagetransfer_projects_agent_pools_delete` for simplest API.

pub fn storagetransfer_projects_agent_pools_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/projects/{}/agentPools/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Deletes an agent pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_projects_agent_pools_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_projects_agent_pools_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Deletes an agent pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_projects_agent_pools_delete_builder()` + `storagetransfer_projects_agent_pools_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_projects_agent_pools_delete_builder(client, name)?;
    storagetransfer_projects_agent_pools_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Gets an agent pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_projects_agent_pools_get_execute()` to send, or `storagetransfer_projects_agent_pools_get` for simplest API.

pub fn storagetransfer_projects_agent_pools_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/projects/{}/agentPools/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Gets an agent pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_projects_agent_pools_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_projects_agent_pools_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AgentPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AgentPool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Gets an agent pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_projects_agent_pools_get_builder()` + `storagetransfer_projects_agent_pools_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AgentPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_projects_agent_pools_get_builder(client, name)?;
    storagetransfer_projects_agent_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/agentPools
/// Lists agent pools.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_projects_agent_pools_list_execute()` to send, or `storagetransfer_projects_agent_pools_list` for simplest API.

pub fn storagetransfer_projects_agent_pools_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/projects/{}/agentPools",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/agentPools
/// Lists agent pools.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_projects_agent_pools_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_projects_agent_pools_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAgentPoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAgentPoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/agentPools
/// Lists agent pools.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_projects_agent_pools_list_builder()` + `storagetransfer_projects_agent_pools_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_list(
    client: &SimpleHttpClient,
    projectId: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAgentPoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = storagetransfer_projects_agent_pools_list_builder(
        client, projectId, filter, pageSize, pageToken,
    )?;
    storagetransfer_projects_agent_pools_list_execute(builder)
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Updates an existing agent pool resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_projects_agent_pools_patch_execute()` to send, or `storagetransfer_projects_agent_pools_patch` for simplest API.

pub fn storagetransfer_projects_agent_pools_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &AgentPool,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/projects/{}/agentPools/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Updates an existing agent pool resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_projects_agent_pools_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_projects_agent_pools_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AgentPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AgentPool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/agentPools/{agentPoolsId}
/// Updates an existing agent pool resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_projects_agent_pools_patch_builder()` + `storagetransfer_projects_agent_pools_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_projects_agent_pools_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &AgentPool,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AgentPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        storagetransfer_projects_agent_pools_patch_builder(client, name, updateMask, body)?;
    storagetransfer_projects_agent_pools_patch_execute(builder)
}

/// GET v1/transferJobs
/// Creates a transfer job that runs periodically.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_jobs_create_execute()` to send, or `storagetransfer_transfer_jobs_create` for simplest API.

pub fn storagetransfer_transfer_jobs_create_builder(
    client: &SimpleHttpClient,
    body: &TransferJob,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://storagetransfer.googleapis.com/v1/transferJobs",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs
/// Creates a transfer job that runs periodically.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs
/// Creates a transfer job that runs periodically.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_jobs_create_builder()` + `storagetransfer_transfer_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_create(
    client: &SimpleHttpClient,
    body: &TransferJob,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_jobs_create_builder(client, body)?;
    storagetransfer_transfer_jobs_create_execute(builder)
}

/// GET v1/transferJobs/{transferJobsId}
/// Deletes a transfer job. Deleting a transfer job sets its status to DELETED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_jobs_delete_execute()` to send, or `storagetransfer_transfer_jobs_delete` for simplest API.

pub fn storagetransfer_transfer_jobs_delete_builder(
    client: &SimpleHttpClient,
    jobName: &str,
    projectId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferJobs/{}",
        jobName, projectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/transferJobs/{transferJobsId}
/// Deletes a transfer job. Deleting a transfer job sets its status to DELETED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_jobs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_jobs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs/{transferJobsId}
/// Deletes a transfer job. Deleting a transfer job sets its status to DELETED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_jobs_delete_builder()` + `storagetransfer_transfer_jobs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_delete(
    client: &SimpleHttpClient,
    jobName: &str,
    projectId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_jobs_delete_builder(client, jobName, projectId)?;
    storagetransfer_transfer_jobs_delete_execute(builder)
}

/// GET v1/transferJobs/{transferJobsId}
/// Gets a transfer job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_jobs_get_execute()` to send, or `storagetransfer_transfer_jobs_get` for simplest API.

pub fn storagetransfer_transfer_jobs_get_builder(
    client: &SimpleHttpClient,
    jobName: &str,
    projectId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferJobs/{}",
        jobName, projectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/transferJobs/{transferJobsId}
/// Gets a transfer job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs/{transferJobsId}
/// Gets a transfer job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_jobs_get_builder()` + `storagetransfer_transfer_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_get(
    client: &SimpleHttpClient,
    jobName: &str,
    projectId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_jobs_get_builder(client, jobName, projectId)?;
    storagetransfer_transfer_jobs_get_execute(builder)
}

/// GET v1/transferJobs
/// Lists transfer jobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_jobs_list_execute()` to send, or `storagetransfer_transfer_jobs_list` for simplest API.

pub fn storagetransfer_transfer_jobs_list_builder(
    client: &SimpleHttpClient,
    filter: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferJobs",
        filter,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/transferJobs
/// Lists transfer jobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransferJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs
/// Lists transfer jobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_jobs_list_builder()` + `storagetransfer_transfer_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_list(
    client: &SimpleHttpClient,
    filter: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransferJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_jobs_list_builder(client, filter, pageSize, pageToken)?;
    storagetransfer_transfer_jobs_list_execute(builder)
}

/// GET v1/transferJobs/{transferJobsId}
/// Updates a transfer job. Updating a job's transfer spec does not affect transfer operations that are running already. **Note:** The job's status field can be modified using this RPC (for example, to set a job's status to DELETED, DISABLED, or ENABLED).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_jobs_patch_execute()` to send, or `storagetransfer_transfer_jobs_patch` for simplest API.

pub fn storagetransfer_transfer_jobs_patch_builder(
    client: &SimpleHttpClient,
    jobName: &str,
    body: &UpdateTransferJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferJobs/{}",
        jobName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs/{transferJobsId}
/// Updates a transfer job. Updating a job's transfer spec does not affect transfer operations that are running already. **Note:** The job's status field can be modified using this RPC (for example, to set a job's status to DELETED, DISABLED, or ENABLED).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_jobs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_jobs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TransferJob = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs/{transferJobsId}
/// Updates a transfer job. Updating a job's transfer spec does not affect transfer operations that are running already. **Note:** The job's status field can be modified using this RPC (for example, to set a job's status to DELETED, DISABLED, or ENABLED).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_jobs_patch_builder()` + `storagetransfer_transfer_jobs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_patch(
    client: &SimpleHttpClient,
    jobName: &str,
    body: &UpdateTransferJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TransferJob>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_jobs_patch_builder(client, jobName, body)?;
    storagetransfer_transfer_jobs_patch_execute(builder)
}

/// GET v1/transferJobs/{transferJobsId}:run
/// Starts a new operation for the specified transfer job. A TransferJob has a maximum of one active TransferOperation. If this method is called while a TransferOperation is active, an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_jobs_run_execute()` to send, or `storagetransfer_transfer_jobs_run` for simplest API.

pub fn storagetransfer_transfer_jobs_run_builder(
    client: &SimpleHttpClient,
    jobName: &str,
    body: &RunTransferJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferJobs/{}:run",
        jobName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs/{transferJobsId}:run
/// Starts a new operation for the specified transfer job. A TransferJob has a maximum of one active TransferOperation. If this method is called while a TransferOperation is active, an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_jobs_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_jobs_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferJobs/{transferJobsId}:run
/// Starts a new operation for the specified transfer job. A TransferJob has a maximum of one active TransferOperation. If this method is called while a TransferOperation is active, an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_jobs_run_builder()` + `storagetransfer_transfer_jobs_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_jobs_run(
    client: &SimpleHttpClient,
    jobName: &str,
    body: &RunTransferJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_jobs_run_builder(client, jobName, body)?;
    storagetransfer_transfer_jobs_run_execute(builder)
}

/// GET v1/transferOperations/{transferOperationsId}:cancel
/// Cancels a transfer. Use the `transferOperations`.get method to check if the cancellation succeeded or if the operation completed despite the cancel request. When you cancel an operation, the currently running transfer is interrupted. For recurring transfer jobs, the next instance of the transfer job will still run. For example, if your job is configured to run every day at 1pm and you cancel Monday's operation at 1:05pm, Monday's transfer will stop. However, a transfer job will still be attempted on Tuesday. This applies only to currently running operations. If an operation is not currently running, cancel does nothing. *Caution:* Canceling a transfer job can leave your data in an unknown state. We recommend that you restore the state at both the destination and the source after the cancel request completes so that your data is in a consistent state. When you cancel a job, the next job computes a delta of files and may repair any inconsistent state. For instance, if you run a job every day, and today's job found 10 new files and transferred five files before you canceled the job, tomorrow's transfer operation will compute a new delta with the five files that were not copied today plus any new files discovered tomorrow.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_operations_cancel_execute()` to send, or `storagetransfer_transfer_operations_cancel` for simplest API.

pub fn storagetransfer_transfer_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferOperations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations/{transferOperationsId}:cancel
/// Cancels a transfer. Use the `transferOperations`.get method to check if the cancellation succeeded or if the operation completed despite the cancel request. When you cancel an operation, the currently running transfer is interrupted. For recurring transfer jobs, the next instance of the transfer job will still run. For example, if your job is configured to run every day at 1pm and you cancel Monday's operation at 1:05pm, Monday's transfer will stop. However, a transfer job will still be attempted on Tuesday. This applies only to currently running operations. If an operation is not currently running, cancel does nothing. *Caution:* Canceling a transfer job can leave your data in an unknown state. We recommend that you restore the state at both the destination and the source after the cancel request completes so that your data is in a consistent state. When you cancel a job, the next job computes a delta of files and may repair any inconsistent state. For instance, if you run a job every day, and today's job found 10 new files and transferred five files before you canceled the job, tomorrow's transfer operation will compute a new delta with the five files that were not copied today plus any new files discovered tomorrow.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations/{transferOperationsId}:cancel
/// Cancels a transfer. Use the `transferOperations`.get method to check if the cancellation succeeded or if the operation completed despite the cancel request. When you cancel an operation, the currently running transfer is interrupted. For recurring transfer jobs, the next instance of the transfer job will still run. For example, if your job is configured to run every day at 1pm and you cancel Monday's operation at 1:05pm, Monday's transfer will stop. However, a transfer job will still be attempted on Tuesday. This applies only to currently running operations. If an operation is not currently running, cancel does nothing. *Caution:* Canceling a transfer job can leave your data in an unknown state. We recommend that you restore the state at both the destination and the source after the cancel request completes so that your data is in a consistent state. When you cancel a job, the next job computes a delta of files and may repair any inconsistent state. For instance, if you run a job every day, and today's job found 10 new files and transferred five files before you canceled the job, tomorrow's transfer operation will compute a new delta with the five files that were not copied today plus any new files discovered tomorrow.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_operations_cancel_builder()` + `storagetransfer_transfer_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_operations_cancel_builder(client, name, body)?;
    storagetransfer_transfer_operations_cancel_execute(builder)
}

/// GET v1/transferOperations/{transferOperationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_operations_get_execute()` to send, or `storagetransfer_transfer_operations_get` for simplest API.

pub fn storagetransfer_transfer_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferOperations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/transferOperations/{transferOperationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations/{transferOperationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_operations_get_builder()` + `storagetransfer_transfer_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_operations_get_builder(client, name)?;
    storagetransfer_transfer_operations_get_execute(builder)
}

/// GET v1/transferOperations
/// Lists transfer operations. Operations are ordered by their creation time in reverse chronological order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_operations_list_execute()` to send, or `storagetransfer_transfer_operations_list` for simplest API.

pub fn storagetransfer_transfer_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferOperations",
        name, filter,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/transferOperations
/// Lists transfer operations. Operations are ordered by their creation time in reverse chronological order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations
/// Lists transfer operations. Operations are ordered by their creation time in reverse chronological order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_operations_list_builder()` + `storagetransfer_transfer_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    storagetransfer_transfer_operations_list_execute(builder)
}

/// GET v1/transferOperations/{transferOperationsId}:pause
/// Pauses a transfer operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_operations_pause_execute()` to send, or `storagetransfer_transfer_operations_pause` for simplest API.

pub fn storagetransfer_transfer_operations_pause_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &PauseTransferOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferOperations/{}:pause",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations/{transferOperationsId}:pause
/// Pauses a transfer operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_operations_pause_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_operations_pause_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_pause_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations/{transferOperationsId}:pause
/// Pauses a transfer operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_operations_pause_builder()` + `storagetransfer_transfer_operations_pause_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_pause(
    client: &SimpleHttpClient,
    name: &str,
    body: &PauseTransferOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_operations_pause_builder(client, name, body)?;
    storagetransfer_transfer_operations_pause_execute(builder)
}

/// GET v1/transferOperations/{transferOperationsId}:resume
/// Resumes a transfer operation that is paused.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `storagetransfer_transfer_operations_resume_execute()` to send, or `storagetransfer_transfer_operations_resume` for simplest API.

pub fn storagetransfer_transfer_operations_resume_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResumeTransferOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://storagetransfer.googleapis.com/v1/transferOperations/{}:resume",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations/{transferOperationsId}:resume
/// Resumes a transfer operation that is paused.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `storagetransfer_transfer_operations_resume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from storagetransfer_transfer_operations_resume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/transferOperations/{transferOperationsId}:resume
/// Resumes a transfer operation that is paused.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `storagetransfer_transfer_operations_resume_builder()` + `storagetransfer_transfer_operations_resume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn storagetransfer_transfer_operations_resume(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResumeTransferOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = storagetransfer_transfer_operations_resume_builder(client, name, body)?;
    storagetransfer_transfer_operations_resume_execute(builder)
}
