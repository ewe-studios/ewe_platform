//! Auto-generated API clients for gcp/mybusinesslodging.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/mybusinesslodging`

#![cfg(feature = "gcp/mybusinesslodging")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/locations/{locationsId}/lodging
/// Returns the Lodging of a specific location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinesslodging_locations_get_lodging_execute()` to send, or `mybusinesslodging_locations_get_lodging` for simplest API.

pub fn mybusinesslodging_locations_get_lodging_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinesslodging.googleapis.com/v1/locations/{}/lodging",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/lodging
/// Returns the Lodging of a specific location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinesslodging_locations_get_lodging_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinesslodging_locations_get_lodging_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinesslodging_locations_get_lodging_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lodging>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Lodging = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinesslodging_locations_get_lodging`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinesslodgingLocationsGetLodgingArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/locations/{locationsId}/lodging
/// Returns the Lodging of a specific location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinesslodging_locations_get_lodging_builder()` + `mybusinesslodging_locations_get_lodging_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinesslodging_locations_get_lodging(
    client: &SimpleHttpClient,
    args: &MybusinesslodgingLocationsGetLodgingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lodging>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinesslodging_locations_get_lodging_builder(
        client,
        &args.name,
        args.readMask.as_deref(),
    )?;
    mybusinesslodging_locations_get_lodging_execute(builder)
}

/// GET v1/locations/{locationsId}/lodging
/// Updates the Lodging of a specific location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinesslodging_locations_update_lodging_execute()` to send, or `mybusinesslodging_locations_update_lodging` for simplest API.

pub fn mybusinesslodging_locations_update_lodging_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Lodging,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinesslodging.googleapis.com/v1/locations/{}/lodging",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/lodging
/// Updates the Lodging of a specific location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinesslodging_locations_update_lodging_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinesslodging_locations_update_lodging_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinesslodging_locations_update_lodging_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lodging>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Lodging = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinesslodging_locations_update_lodging`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinesslodgingLocationsUpdateLodgingArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Lodging,
}

/// GET v1/locations/{locationsId}/lodging
/// Updates the Lodging of a specific location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinesslodging_locations_update_lodging_builder()` + `mybusinesslodging_locations_update_lodging_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinesslodging_locations_update_lodging(
    client: &SimpleHttpClient,
    args: &MybusinesslodgingLocationsUpdateLodgingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lodging>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinesslodging_locations_update_lodging_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    mybusinesslodging_locations_update_lodging_execute(builder)
}

/// GET v1/locations/{locationsId}/lodging:getGoogleUpdated
/// Returns the Google updated Lodging of a specific location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinesslodging_locations_lodging_get_google_updated_execute()` to send, or `mybusinesslodging_locations_lodging_get_google_updated` for simplest API.

pub fn mybusinesslodging_locations_lodging_get_google_updated_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinesslodging.googleapis.com/v1/locations/{}/lodging:getGoogleUpdated",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/lodging:getGoogleUpdated
/// Returns the Google updated Lodging of a specific location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinesslodging_locations_lodging_get_google_updated_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinesslodging_locations_lodging_get_google_updated_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinesslodging_locations_lodging_get_google_updated_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetGoogleUpdatedLodgingResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetGoogleUpdatedLodgingResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinesslodging_locations_lodging_get_google_updated`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinesslodgingLocationsLodgingGetGoogleUpdatedArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/locations/{locationsId}/lodging:getGoogleUpdated
/// Returns the Google updated Lodging of a specific location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinesslodging_locations_lodging_get_google_updated_builder()` + `mybusinesslodging_locations_lodging_get_google_updated_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinesslodging_locations_lodging_get_google_updated(
    client: &SimpleHttpClient,
    args: &MybusinesslodgingLocationsLodgingGetGoogleUpdatedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetGoogleUpdatedLodgingResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinesslodging_locations_lodging_get_google_updated_builder(
        client,
        &args.name,
        args.readMask.as_deref(),
    )?;
    mybusinesslodging_locations_lodging_get_google_updated_execute(builder)
}
