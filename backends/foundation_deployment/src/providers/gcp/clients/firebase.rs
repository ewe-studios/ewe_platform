//! Auto-generated API clients for gcp/firebase.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta1/availableProjects
/// Lists each [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>) that can have Firebase resources added and Firebase services enabled. A Project will only be listed if: - The caller has sufficient [Google IAM](<https://cloud.google.`com/iam`>) permissions to call AddFirebase. - The Project is not already a FirebaseProject. - The Project is not in an Organization which has policies that prevent Firebase resources from being added.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_available_projects_list_execute()` to send, or `firebase_available_projects_list` for simplest API.

pub fn firebase_available_projects_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://firebase.googleapis.com/v1beta1/availableProjects",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/availableProjects
/// Lists each [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>) that can have Firebase resources added and Firebase services enabled. A Project will only be listed if: - The caller has sufficient [Google IAM](<https://cloud.google.`com/iam`>) permissions to call AddFirebase. - The Project is not already a FirebaseProject. - The Project is not in an Organization which has policies that prevent Firebase resources from being added.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_available_projects_list_execute()` or `firebase_available_projects_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_available_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_available_projects_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAvailableProjectsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAvailableProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/availableProjects
/// Lists each [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>) that can have Firebase resources added and Firebase services enabled. A Project will only be listed if: - The caller has sufficient [Google IAM](<https://cloud.google.`com/iam`>) permissions to call AddFirebase. - The Project is not already a FirebaseProject. - The Project is not in an Organization which has policies that prevent Firebase resources from being added.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_available_projects_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_available_projects_list_task()`.
/// For the simplest API, use `firebase_available_projects_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_available_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_available_projects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAvailableProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebase_available_projects_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_available_projects_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseAvailableProjectsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/availableProjects
/// Lists each [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>) that can have Firebase resources added and Firebase services enabled. A Project will only be listed if: - The caller has sufficient [Google IAM](<https://cloud.google.`com/iam`>) permissions to call AddFirebase. - The Project is not already a FirebaseProject. - The Project is not in an Organization which has policies that prevent Firebase resources from being added.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_available_projects_list_builder()` + `firebase_available_projects_list_execute()`.
/// For task-level control, use `firebase_available_projects_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_available_projects_list(
    client: &SimpleHttpClient,
    args: &FirebaseAvailableProjectsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAvailableProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebase_available_projects_list_builder(client, &args.pageSize, &args.pageToken)?;
    firebase_available_projects_list_execute(builder)
}

/// GET v1beta1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_operations_get_execute()` to send, or `firebase_operations_get` for simplest API.

pub fn firebase_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_operations_get_execute()` or `firebase_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_operations_get_task()`.
/// For the simplest API, use `firebase_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_operations_get_builder()` + `firebase_operations_get_execute()`.
/// For task-level control, use `firebase_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_operations_get(
    client: &SimpleHttpClient,
    args: &FirebaseOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_operations_get_builder(client, &args.name)?;
    firebase_operations_get_execute(builder)
}

/// POST v1beta1/projects/{projectsId}:addFirebase
/// Adds Firebase resources and enables Firebase services in the specified existing [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>). Since a FirebaseProject is actually also a Google Cloud Project, a FirebaseProject has the same underlying Google Cloud identifiers (`projectNumber` and `projectId`). This allows for easy interop with Google APIs. The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to a FirebaseProject; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. This method does not modify any billing account information on the underlying Google Cloud Project. To call AddFirebase, a project member or service account must have the following permissions (the IAM roles of Editor and Owner contain these permissions): firebase.projects.update, resourcemanager.projects.get, serviceusage.services.enable, and serviceusage.services.get.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_add_firebase_execute()` to send, or `firebase_projects_add_firebase` for simplest API.

pub fn firebase_projects_add_firebase_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}:addFirebase",
        project,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}:addFirebase
/// Adds Firebase resources and enables Firebase services in the specified existing [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>). Since a FirebaseProject is actually also a Google Cloud Project, a FirebaseProject has the same underlying Google Cloud identifiers (`projectNumber` and `projectId`). This allows for easy interop with Google APIs. The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to a FirebaseProject; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. This method does not modify any billing account information on the underlying Google Cloud Project. To call AddFirebase, a project member or service account must have the following permissions (the IAM roles of Editor and Owner contain these permissions): firebase.projects.update, resourcemanager.projects.get, serviceusage.services.enable, and serviceusage.services.get.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_add_firebase_execute()` or `firebase_projects_add_firebase`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_add_firebase_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_add_firebase_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}:addFirebase
/// Adds Firebase resources and enables Firebase services in the specified existing [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>). Since a FirebaseProject is actually also a Google Cloud Project, a FirebaseProject has the same underlying Google Cloud identifiers (`projectNumber` and `projectId`). This allows for easy interop with Google APIs. The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to a FirebaseProject; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. This method does not modify any billing account information on the underlying Google Cloud Project. To call AddFirebase, a project member or service account must have the following permissions (the IAM roles of Editor and Owner contain these permissions): firebase.projects.update, resourcemanager.projects.get, serviceusage.services.enable, and serviceusage.services.get.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_add_firebase_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_add_firebase_task()`.
/// For the simplest API, use `firebase_projects_add_firebase()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_add_firebase_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_add_firebase_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_add_firebase_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_add_firebase`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAddFirebaseArgs {
    /// Path parameter: project
    pub project: String,
}

/// POST v1beta1/projects/{projectsId}:addFirebase
/// Adds Firebase resources and enables Firebase services in the specified existing [Google Cloud Project](<https://cloud.google.`com/resource-manager/reference/rest/v1/projects`>). Since a FirebaseProject is actually also a Google Cloud Project, a FirebaseProject has the same underlying Google Cloud identifiers (`projectNumber` and `projectId`). This allows for easy interop with Google APIs. The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to a FirebaseProject; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. This method does not modify any billing account information on the underlying Google Cloud Project. To call AddFirebase, a project member or service account must have the following permissions (the IAM roles of Editor and Owner contain these permissions): firebase.projects.update, resourcemanager.projects.get, serviceusage.services.enable, and serviceusage.services.get.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_add_firebase_builder()` + `firebase_projects_add_firebase_execute()`.
/// For task-level control, use `firebase_projects_add_firebase_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_add_firebase(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAddFirebaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_add_firebase_builder(client, &args.project)?;
    firebase_projects_add_firebase_execute(builder)
}

/// POST v1beta1/projects/{projectsId}:addGoogleAnalytics
/// Links the specified FirebaseProject with an existing [Google Analytics account](<http://www.google.`com/analytics/`>). Using this call, you can either: - Specify an `analyticsAccountId` to provision a new Google Analytics property within the specified account and associate the new property with the FirebaseProject. - Specify an existing `analyticsPropertyId` to associate the property with the FirebaseProject. Note that when you call AddGoogleAnalytics: 1. The first check determines if any existing data streams in the Google Analytics property correspond to any existing Firebase Apps in the FirebaseProject (based on the `packageName` or `bundleId` associated with the data stream). Then, as applicable, the data streams and apps are linked. Note that this auto-linking only applies to AndroidApps and IosApps. 2. If no corresponding data streams are found for the Firebase Apps, new data streams are provisioned in the Google Analytics property for each of the Firebase Apps. Note that a new data stream is always provisioned for a Web App even if it was previously associated with a data stream in the Analytics property. Learn more about the hierarchy and structure of Google Analytics accounts in the [Analytics documentation](<https://support.google.`com/analytics/answer/9303323`>). The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to an AnalyticsDetails; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. To call AddGoogleAnalytics, a project member must be an Owner for the existing FirebaseProject and have the [Edit permission](<https://support.google.`com/analytics/answer/2884495`>) for the Google Analytics account. If the FirebaseProject already has Google Analytics enabled, and you call AddGoogleAnalytics using an `analyticsPropertyId` that''s different from the currently associated property, then the call will fail. Analytics may have already been enabled in the Firebase console or by specifying `timeZone` and `regionCode` in the call to [AddFirebase](../../v1beta1/`projects/`addFirebase``).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_add_google_analytics_execute()` to send, or `firebase_projects_add_google_analytics` for simplest API.

pub fn firebase_projects_add_google_analytics_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}:addGoogleAnalytics",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}:addGoogleAnalytics
/// Links the specified FirebaseProject with an existing [Google Analytics account](<http://www.google.`com/analytics/`>). Using this call, you can either: - Specify an `analyticsAccountId` to provision a new Google Analytics property within the specified account and associate the new property with the FirebaseProject. - Specify an existing `analyticsPropertyId` to associate the property with the FirebaseProject. Note that when you call AddGoogleAnalytics: 1. The first check determines if any existing data streams in the Google Analytics property correspond to any existing Firebase Apps in the FirebaseProject (based on the `packageName` or `bundleId` associated with the data stream). Then, as applicable, the data streams and apps are linked. Note that this auto-linking only applies to AndroidApps and IosApps. 2. If no corresponding data streams are found for the Firebase Apps, new data streams are provisioned in the Google Analytics property for each of the Firebase Apps. Note that a new data stream is always provisioned for a Web App even if it was previously associated with a data stream in the Analytics property. Learn more about the hierarchy and structure of Google Analytics accounts in the [Analytics documentation](<https://support.google.`com/analytics/answer/9303323`>). The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to an AnalyticsDetails; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. To call AddGoogleAnalytics, a project member must be an Owner for the existing FirebaseProject and have the [Edit permission](<https://support.google.`com/analytics/answer/2884495`>) for the Google Analytics account. If the FirebaseProject already has Google Analytics enabled, and you call AddGoogleAnalytics using an `analyticsPropertyId` that''s different from the currently associated property, then the call will fail. Analytics may have already been enabled in the Firebase console or by specifying `timeZone` and `regionCode` in the call to [AddFirebase](../../v1beta1/`projects/`addFirebase``).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_add_google_analytics_execute()` or `firebase_projects_add_google_analytics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_add_google_analytics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_add_google_analytics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}:addGoogleAnalytics
/// Links the specified FirebaseProject with an existing [Google Analytics account](<http://www.google.`com/analytics/`>). Using this call, you can either: - Specify an `analyticsAccountId` to provision a new Google Analytics property within the specified account and associate the new property with the FirebaseProject. - Specify an existing `analyticsPropertyId` to associate the property with the FirebaseProject. Note that when you call AddGoogleAnalytics: 1. The first check determines if any existing data streams in the Google Analytics property correspond to any existing Firebase Apps in the FirebaseProject (based on the `packageName` or `bundleId` associated with the data stream). Then, as applicable, the data streams and apps are linked. Note that this auto-linking only applies to AndroidApps and IosApps. 2. If no corresponding data streams are found for the Firebase Apps, new data streams are provisioned in the Google Analytics property for each of the Firebase Apps. Note that a new data stream is always provisioned for a Web App even if it was previously associated with a data stream in the Analytics property. Learn more about the hierarchy and structure of Google Analytics accounts in the [Analytics documentation](<https://support.google.`com/analytics/answer/9303323`>). The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to an AnalyticsDetails; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. To call AddGoogleAnalytics, a project member must be an Owner for the existing FirebaseProject and have the [Edit permission](<https://support.google.`com/analytics/answer/2884495`>) for the Google Analytics account. If the FirebaseProject already has Google Analytics enabled, and you call AddGoogleAnalytics using an `analyticsPropertyId` that''s different from the currently associated property, then the call will fail. Analytics may have already been enabled in the Firebase console or by specifying `timeZone` and `regionCode` in the call to [AddFirebase](../../v1beta1/`projects/`addFirebase``).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_add_google_analytics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_add_google_analytics_task()`.
/// For the simplest API, use `firebase_projects_add_google_analytics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_add_google_analytics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_add_google_analytics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_add_google_analytics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_add_google_analytics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAddGoogleAnalyticsArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}:addGoogleAnalytics
/// Links the specified FirebaseProject with an existing [Google Analytics account](<http://www.google.`com/analytics/`>). Using this call, you can either: - Specify an `analyticsAccountId` to provision a new Google Analytics property within the specified account and associate the new property with the FirebaseProject. - Specify an existing `analyticsPropertyId` to associate the property with the FirebaseProject. Note that when you call AddGoogleAnalytics: 1. The first check determines if any existing data streams in the Google Analytics property correspond to any existing Firebase Apps in the FirebaseProject (based on the `packageName` or `bundleId` associated with the data stream). Then, as applicable, the data streams and apps are linked. Note that this auto-linking only applies to AndroidApps and IosApps. 2. If no corresponding data streams are found for the Firebase Apps, new data streams are provisioned in the Google Analytics property for each of the Firebase Apps. Note that a new data stream is always provisioned for a Web App even if it was previously associated with a data stream in the Analytics property. Learn more about the hierarchy and structure of Google Analytics accounts in the [Analytics documentation](<https://support.google.`com/analytics/answer/9303323`>). The result of this call is an [Operation](../../v1beta1/operations). Poll the Operation to track the provisioning process by calling GetOperation until [done](../../v1beta1/operations#Operation.FIELDS.done) is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) is set to an AnalyticsDetails; if the Operation failed, its [error](../../v1beta1/operations#Operation.FIELDS.error) is set to a google.rpc.Status. To call AddGoogleAnalytics, a project member must be an Owner for the existing FirebaseProject and have the [Edit permission](<https://support.google.`com/analytics/answer/2884495`>) for the Google Analytics account. If the FirebaseProject already has Google Analytics enabled, and you call AddGoogleAnalytics using an `analyticsPropertyId` that''s different from the currently associated property, then the call will fail. Analytics may have already been enabled in the Firebase console or by specifying `timeZone` and `regionCode` in the call to [AddFirebase](../../v1beta1/`projects/`addFirebase``).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_add_google_analytics_builder()` + `firebase_projects_add_google_analytics_execute()`.
/// For task-level control, use `firebase_projects_add_google_analytics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_add_google_analytics(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAddGoogleAnalyticsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_add_google_analytics_builder(client, &args.parent)?;
    firebase_projects_add_google_analytics_execute(builder)
}

/// GET v1beta1/projects/{projectsId}
/// Gets the specified FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_get_execute()` to send, or `firebase_projects_get` for simplest API.

pub fn firebase_projects_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://firebase.googleapis.com/v1beta1/projects/{}", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}
/// Gets the specified FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_get_execute()` or `firebase_projects_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirebaseProject>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirebaseProject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}
/// Gets the specified FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_get_task()`.
/// For the simplest API, use `firebase_projects_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirebaseProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}
/// Gets the specified FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_get_builder()` + `firebase_projects_get_execute()`.
/// For task-level control, use `firebase_projects_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_get(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirebaseProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_get_builder(client, &args.name)?;
    firebase_projects_get_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/adminSdkConfig
/// Gets the configuration artifact associated with the specified FirebaseProject, which can be used by servers to simplify initialization. Typically, this configuration is used with the Firebase Admin SDK [`initializeApp`](<https://firebase.google.`com/docs/admin/setup`#initialize_the_sdk>) command.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_get_admin_sdk_config_execute()` to send, or `firebase_projects_get_admin_sdk_config` for simplest API.

pub fn firebase_projects_get_admin_sdk_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/adminSdkConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/adminSdkConfig
/// Gets the configuration artifact associated with the specified FirebaseProject, which can be used by servers to simplify initialization. Typically, this configuration is used with the Firebase Admin SDK [`initializeApp`](<https://firebase.google.`com/docs/admin/setup`#initialize_the_sdk>) command.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_get_admin_sdk_config_execute()` or `firebase_projects_get_admin_sdk_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_get_admin_sdk_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_get_admin_sdk_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdminSdkConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdminSdkConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/adminSdkConfig
/// Gets the configuration artifact associated with the specified FirebaseProject, which can be used by servers to simplify initialization. Typically, this configuration is used with the Firebase Admin SDK [`initializeApp`](<https://firebase.google.`com/docs/admin/setup`#initialize_the_sdk>) command.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_get_admin_sdk_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_get_admin_sdk_config_task()`.
/// For the simplest API, use `firebase_projects_get_admin_sdk_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_get_admin_sdk_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_get_admin_sdk_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdminSdkConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_get_admin_sdk_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_get_admin_sdk_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsGetAdminSdkConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/adminSdkConfig
/// Gets the configuration artifact associated with the specified FirebaseProject, which can be used by servers to simplify initialization. Typically, this configuration is used with the Firebase Admin SDK [`initializeApp`](<https://firebase.google.`com/docs/admin/setup`#initialize_the_sdk>) command.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_get_admin_sdk_config_builder()` + `firebase_projects_get_admin_sdk_config_execute()`.
/// For task-level control, use `firebase_projects_get_admin_sdk_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_get_admin_sdk_config(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsGetAdminSdkConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdminSdkConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_get_admin_sdk_config_builder(client, &args.name)?;
    firebase_projects_get_admin_sdk_config_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/analyticsDetails
/// Gets the Google Analytics details currently associated with the specified FirebaseProject. If the FirebaseProject is not yet linked to Google Analytics, then the response to GetAnalyticsDetails is NOT_FOUND.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_get_analytics_details_execute()` to send, or `firebase_projects_get_analytics_details` for simplest API.

pub fn firebase_projects_get_analytics_details_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/analyticsDetails",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/analyticsDetails
/// Gets the Google Analytics details currently associated with the specified FirebaseProject. If the FirebaseProject is not yet linked to Google Analytics, then the response to GetAnalyticsDetails is NOT_FOUND.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_get_analytics_details_execute()` or `firebase_projects_get_analytics_details`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_get_analytics_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_get_analytics_details_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnalyticsDetails>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyticsDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/analyticsDetails
/// Gets the Google Analytics details currently associated with the specified FirebaseProject. If the FirebaseProject is not yet linked to Google Analytics, then the response to GetAnalyticsDetails is NOT_FOUND.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_get_analytics_details_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_get_analytics_details_task()`.
/// For the simplest API, use `firebase_projects_get_analytics_details()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_get_analytics_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_get_analytics_details_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyticsDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_get_analytics_details_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_get_analytics_details`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsGetAnalyticsDetailsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/analyticsDetails
/// Gets the Google Analytics details currently associated with the specified FirebaseProject. If the FirebaseProject is not yet linked to Google Analytics, then the response to GetAnalyticsDetails is NOT_FOUND.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_get_analytics_details_builder()` + `firebase_projects_get_analytics_details_execute()`.
/// For task-level control, use `firebase_projects_get_analytics_details_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_get_analytics_details(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsGetAnalyticsDetailsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyticsDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_get_analytics_details_builder(client, &args.name)?;
    firebase_projects_get_analytics_details_execute(builder)
}

/// GET v1beta1/projects
/// Lists each FirebaseProject accessible to the caller. The elements are returned in no particular order, but they will be a consistent view of the Projects when additional requests are made with a `pageToken`. This method is eventually consistent with Project mutations, which means newly provisioned Projects and recent modifications to existing Projects might not be reflected in the set of Projects. The list will include only `ACTIVE` Projects. Use GetFirebaseProject for consistent reads as well as for additional Project details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_list_execute()` to send, or `firebase_projects_list` for simplest API.

pub fn firebase_projects_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    showDeleted: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://firebase.googleapis.com/v1beta1/projects",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects
/// Lists each FirebaseProject accessible to the caller. The elements are returned in no particular order, but they will be a consistent view of the Projects when additional requests are made with a `pageToken`. This method is eventually consistent with Project mutations, which means newly provisioned Projects and recent modifications to existing Projects might not be reflected in the set of Projects. The list will include only `ACTIVE` Projects. Use GetFirebaseProject for consistent reads as well as for additional Project details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_list_execute()` or `firebase_projects_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFirebaseProjectsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFirebaseProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects
/// Lists each FirebaseProject accessible to the caller. The elements are returned in no particular order, but they will be a consistent view of the Projects when additional requests are made with a `pageToken`. This method is eventually consistent with Project mutations, which means newly provisioned Projects and recent modifications to existing Projects might not be reflected in the set of Projects. The list will include only `ACTIVE` Projects. Use GetFirebaseProject for consistent reads as well as for additional Project details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_list_task()`.
/// For the simplest API, use `firebase_projects_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirebaseProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<String>,
}

/// GET v1beta1/projects
/// Lists each FirebaseProject accessible to the caller. The elements are returned in no particular order, but they will be a consistent view of the Projects when additional requests are made with a `pageToken`. This method is eventually consistent with Project mutations, which means newly provisioned Projects and recent modifications to existing Projects might not be reflected in the set of Projects. The list will include only `ACTIVE` Projects. Use GetFirebaseProject for consistent reads as well as for additional Project details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_list_builder()` + `firebase_projects_list_execute()`.
/// For task-level control, use `firebase_projects_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_list(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirebaseProjectsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebase_projects_list_builder(client, &args.pageSize, &args.pageToken, &args.showDeleted)?;
    firebase_projects_list_execute(builder)
}

/// PATCH v1beta1/projects/{projectsId}
/// Updates the attributes of the specified FirebaseProject. All [query parameters](#query-parameters) are required.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_patch_execute()` to send, or `firebase_projects_patch` for simplest API.

pub fn firebase_projects_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://firebase.googleapis.com/v1beta1/projects/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1beta1/projects/{projectsId}
/// Updates the attributes of the specified FirebaseProject. All [query parameters](#query-parameters) are required.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_patch_execute()` or `firebase_projects_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirebaseProject>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirebaseProject = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1beta1/projects/{projectsId}
/// Updates the attributes of the specified FirebaseProject. All [query parameters](#query-parameters) are required.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_patch_task()`.
/// For the simplest API, use `firebase_projects_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirebaseProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1beta1/projects/{projectsId}
/// Updates the attributes of the specified FirebaseProject. All [query parameters](#query-parameters) are required.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_patch_builder()` + `firebase_projects_patch_execute()`.
/// For task-level control, use `firebase_projects_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_patch(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirebaseProject>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_patch_builder(client, &args.name, &args.updateMask)?;
    firebase_projects_patch_execute(builder)
}

/// POST v1beta1/projects/{projectsId}:removeAnalytics
/// Unlinks the specified FirebaseProject from its Google Analytics account. This call removes the association of the specified FirebaseProject with its current Google Analytics property. However, this call does not delete the Google Analytics resources, such as the Google Analytics property or any data streams. These resources may be re-associated later to the FirebaseProject by calling [AddGoogleAnalytics](../../v1beta1/`projects/`addGoogleAnalytics``) and specifying the same `analyticsPropertyId`. For Android Apps and iOS Apps, this call re-links data streams with their corresponding apps. However, for Web Apps, this call provisions a *new* data stream for each Web App. To call RemoveAnalytics, a project member must be an Owner for the FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_remove_analytics_execute()` to send, or `firebase_projects_remove_analytics` for simplest API.

pub fn firebase_projects_remove_analytics_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}:removeAnalytics",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}:removeAnalytics
/// Unlinks the specified FirebaseProject from its Google Analytics account. This call removes the association of the specified FirebaseProject with its current Google Analytics property. However, this call does not delete the Google Analytics resources, such as the Google Analytics property or any data streams. These resources may be re-associated later to the FirebaseProject by calling [AddGoogleAnalytics](../../v1beta1/`projects/`addGoogleAnalytics``) and specifying the same `analyticsPropertyId`. For Android Apps and iOS Apps, this call re-links data streams with their corresponding apps. However, for Web Apps, this call provisions a *new* data stream for each Web App. To call RemoveAnalytics, a project member must be an Owner for the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_remove_analytics_execute()` or `firebase_projects_remove_analytics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_remove_analytics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_remove_analytics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}:removeAnalytics
/// Unlinks the specified FirebaseProject from its Google Analytics account. This call removes the association of the specified FirebaseProject with its current Google Analytics property. However, this call does not delete the Google Analytics resources, such as the Google Analytics property or any data streams. These resources may be re-associated later to the FirebaseProject by calling [AddGoogleAnalytics](../../v1beta1/`projects/`addGoogleAnalytics``) and specifying the same `analyticsPropertyId`. For Android Apps and iOS Apps, this call re-links data streams with their corresponding apps. However, for Web Apps, this call provisions a *new* data stream for each Web App. To call RemoveAnalytics, a project member must be an Owner for the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_remove_analytics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_remove_analytics_task()`.
/// For the simplest API, use `firebase_projects_remove_analytics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_remove_analytics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_remove_analytics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_remove_analytics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_remove_analytics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsRemoveAnalyticsArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}:removeAnalytics
/// Unlinks the specified FirebaseProject from its Google Analytics account. This call removes the association of the specified FirebaseProject with its current Google Analytics property. However, this call does not delete the Google Analytics resources, such as the Google Analytics property or any data streams. These resources may be re-associated later to the FirebaseProject by calling [AddGoogleAnalytics](../../v1beta1/`projects/`addGoogleAnalytics``) and specifying the same `analyticsPropertyId`. For Android Apps and iOS Apps, this call re-links data streams with their corresponding apps. However, for Web Apps, this call provisions a *new* data stream for each Web App. To call RemoveAnalytics, a project member must be an Owner for the FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_remove_analytics_builder()` + `firebase_projects_remove_analytics_execute()`.
/// For task-level control, use `firebase_projects_remove_analytics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_remove_analytics(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsRemoveAnalyticsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_remove_analytics_builder(client, &args.parent)?;
    firebase_projects_remove_analytics_execute(builder)
}

/// GET v1beta1/projects/{projectsId}:searchApps
/// Lists all available Apps for the specified FirebaseProject. This is a convenience method. Typically, interaction with an App should be done using the platform-specific service, but some tool use-cases require a summary of all known Apps (such as for App selector interfaces).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_search_apps_execute()` to send, or `firebase_projects_search_apps` for simplest API.

pub fn firebase_projects_search_apps_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    showDeleted: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}:searchApps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}:searchApps
/// Lists all available Apps for the specified FirebaseProject. This is a convenience method. Typically, interaction with an App should be done using the platform-specific service, but some tool use-cases require a summary of all known Apps (such as for App selector interfaces).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_search_apps_execute()` or `firebase_projects_search_apps`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_search_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_search_apps_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchFirebaseAppsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchFirebaseAppsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}:searchApps
/// Lists all available Apps for the specified FirebaseProject. This is a convenience method. Typically, interaction with an App should be done using the platform-specific service, but some tool use-cases require a summary of all known Apps (such as for App selector interfaces).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_search_apps_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_search_apps_task()`.
/// For the simplest API, use `firebase_projects_search_apps()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_search_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_search_apps_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchFirebaseAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_search_apps_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_search_apps`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsSearchAppsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<String>,
}

/// GET v1beta1/projects/{projectsId}:searchApps
/// Lists all available Apps for the specified FirebaseProject. This is a convenience method. Typically, interaction with an App should be done using the platform-specific service, but some tool use-cases require a summary of all known Apps (such as for App selector interfaces).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_search_apps_builder()` + `firebase_projects_search_apps_execute()`.
/// For task-level control, use `firebase_projects_search_apps_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_search_apps(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsSearchAppsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchFirebaseAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_search_apps_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.showDeleted,
    )?;
    firebase_projects_search_apps_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps
/// Requests the creation of a new AndroidApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_create_execute()` to send, or `firebase_projects_android_apps_create` for simplest API.

pub fn firebase_projects_android_apps_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps
/// Requests the creation of a new AndroidApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_create_execute()` or `firebase_projects_android_apps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/androidApps
/// Requests the creation of a new AndroidApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_create_task()`.
/// For the simplest API, use `firebase_projects_android_apps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/androidApps
/// Requests the creation of a new AndroidApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_create_builder()` + `firebase_projects_android_apps_create_execute()`.
/// For task-level control, use `firebase_projects_android_apps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_create(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_create_builder(client, &args.parent)?;
    firebase_projects_android_apps_create_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Gets the specified AndroidApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_get_execute()` to send, or `firebase_projects_android_apps_get` for simplest API.

pub fn firebase_projects_android_apps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Gets the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_get_execute()` or `firebase_projects_android_apps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AndroidApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AndroidApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Gets the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_get_task()`.
/// For the simplest API, use `firebase_projects_android_apps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AndroidApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Gets the specified AndroidApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_get_builder()` + `firebase_projects_android_apps_get_execute()`.
/// For task-level control, use `firebase_projects_android_apps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_get(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AndroidApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_get_builder(client, &args.name)?;
    firebase_projects_android_apps_get_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/config
/// Gets the configuration artifact associated with the specified AndroidApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_get_config_execute()` to send, or `firebase_projects_android_apps_get_config` for simplest API.

pub fn firebase_projects_android_apps_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/config
/// Gets the configuration artifact associated with the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_get_config_execute()` or `firebase_projects_android_apps_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AndroidAppConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AndroidAppConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/config
/// Gets the configuration artifact associated with the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_get_config_task()`.
/// For the simplest API, use `firebase_projects_android_apps_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AndroidAppConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/config
/// Gets the configuration artifact associated with the specified AndroidApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_get_config_builder()` + `firebase_projects_android_apps_get_config_execute()`.
/// For task-level control, use `firebase_projects_android_apps_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_get_config(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsGetConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AndroidAppConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_get_config_builder(client, &args.name)?;
    firebase_projects_android_apps_get_config_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps
/// Lists each AndroidApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_list_execute()` to send, or `firebase_projects_android_apps_list` for simplest API.

pub fn firebase_projects_android_apps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    showDeleted: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps
/// Lists each AndroidApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_list_execute()` or `firebase_projects_android_apps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAndroidAppsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAndroidAppsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/androidApps
/// Lists each AndroidApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_list_task()`.
/// For the simplest API, use `firebase_projects_android_apps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAndroidAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/androidApps
/// Lists each AndroidApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_list_builder()` + `firebase_projects_android_apps_list_execute()`.
/// For task-level control, use `firebase_projects_android_apps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_list(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAndroidAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.showDeleted,
    )?;
    firebase_projects_android_apps_list_execute(builder)
}

/// PATCH v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Updates the attributes of the specified AndroidApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_patch_execute()` to send, or `firebase_projects_android_apps_patch` for simplest API.

pub fn firebase_projects_android_apps_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Updates the attributes of the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_patch_execute()` or `firebase_projects_android_apps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AndroidApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AndroidApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Updates the attributes of the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_patch_task()`.
/// For the simplest API, use `firebase_projects_android_apps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AndroidApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1beta1/projects/{projectsId}/androidApps/{androidAppsId}
/// Updates the attributes of the specified AndroidApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_patch_builder()` + `firebase_projects_android_apps_patch_execute()`.
/// For task-level control, use `firebase_projects_android_apps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_patch(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AndroidApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        firebase_projects_android_apps_patch_builder(client, &args.name, &args.updateMask)?;
    firebase_projects_android_apps_patch_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:remove
/// Removes the specified AndroidApp from the FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_remove_execute()` to send, or `firebase_projects_android_apps_remove` for simplest API.

pub fn firebase_projects_android_apps_remove_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}:remove",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:remove
/// Removes the specified AndroidApp from the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_remove_execute()` or `firebase_projects_android_apps_remove`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_remove_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:remove
/// Removes the specified AndroidApp from the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_remove_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_remove_task()`.
/// For the simplest API, use `firebase_projects_android_apps_remove()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_remove_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_remove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsRemoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:remove
/// Removes the specified AndroidApp from the FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_remove_builder()` + `firebase_projects_android_apps_remove_execute()`.
/// For task-level control, use `firebase_projects_android_apps_remove_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_remove(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsRemoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_remove_builder(client, &args.name)?;
    firebase_projects_android_apps_remove_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:undelete
/// Restores the specified AndroidApp to the FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_undelete_execute()` to send, or `firebase_projects_android_apps_undelete` for simplest API.

pub fn firebase_projects_android_apps_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:undelete
/// Restores the specified AndroidApp to the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_undelete_execute()` or `firebase_projects_android_apps_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:undelete
/// Restores the specified AndroidApp to the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_undelete_task()`.
/// For the simplest API, use `firebase_projects_android_apps_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}:undelete
/// Restores the specified AndroidApp to the FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_undelete_builder()` + `firebase_projects_android_apps_undelete_execute()`.
/// For task-level control, use `firebase_projects_android_apps_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_undelete(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_undelete_builder(client, &args.name)?;
    firebase_projects_android_apps_undelete_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Adds a ShaCertificate to the specified AndroidApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_sha_create_execute()` to send, or `firebase_projects_android_apps_sha_create` for simplest API.

pub fn firebase_projects_android_apps_sha_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}/sha",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Adds a ShaCertificate to the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_sha_create_execute()` or `firebase_projects_android_apps_sha_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_sha_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_sha_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShaCertificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ShaCertificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Adds a ShaCertificate to the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_sha_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_sha_create_task()`.
/// For the simplest API, use `firebase_projects_android_apps_sha_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_sha_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_sha_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShaCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_sha_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_sha_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsShaCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Adds a ShaCertificate to the specified AndroidApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_sha_create_builder()` + `firebase_projects_android_apps_sha_create_execute()`.
/// For task-level control, use `firebase_projects_android_apps_sha_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_sha_create(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsShaCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ShaCertificate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_sha_create_builder(client, &args.parent)?;
    firebase_projects_android_apps_sha_create_execute(builder)
}

/// DELETE v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha/{shaId}
/// Removes a ShaCertificate from the specified AndroidApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_sha_delete_execute()` to send, or `firebase_projects_android_apps_sha_delete` for simplest API.

pub fn firebase_projects_android_apps_sha_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}/sha/{shaId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha/{shaId}
/// Removes a ShaCertificate from the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_sha_delete_execute()` or `firebase_projects_android_apps_sha_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_sha_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_sha_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha/{shaId}
/// Removes a ShaCertificate from the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_sha_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_sha_delete_task()`.
/// For the simplest API, use `firebase_projects_android_apps_sha_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_sha_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_sha_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_sha_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_sha_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsShaDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha/{shaId}
/// Removes a ShaCertificate from the specified AndroidApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_sha_delete_builder()` + `firebase_projects_android_apps_sha_delete_execute()`.
/// For task-level control, use `firebase_projects_android_apps_sha_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_sha_delete(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsShaDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_sha_delete_builder(client, &args.name)?;
    firebase_projects_android_apps_sha_delete_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Lists the SHA-1 and SHA-256 certificates for the specified AndroidApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_android_apps_sha_list_execute()` to send, or `firebase_projects_android_apps_sha_list` for simplest API.

pub fn firebase_projects_android_apps_sha_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}/sha",
        parent,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Lists the SHA-1 and SHA-256 certificates for the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_android_apps_sha_list_execute()` or `firebase_projects_android_apps_sha_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_sha_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_sha_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListShaCertificatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListShaCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Lists the SHA-1 and SHA-256 certificates for the specified AndroidApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_android_apps_sha_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_android_apps_sha_list_task()`.
/// For the simplest API, use `firebase_projects_android_apps_sha_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_android_apps_sha_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_android_apps_sha_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListShaCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_android_apps_sha_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_android_apps_sha_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAndroidAppsShaListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/sha
/// Lists the SHA-1 and SHA-256 certificates for the specified AndroidApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_android_apps_sha_list_builder()` + `firebase_projects_android_apps_sha_list_execute()`.
/// For task-level control, use `firebase_projects_android_apps_sha_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_android_apps_sha_list(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAndroidAppsShaListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListShaCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_android_apps_sha_list_builder(client, &args.parent)?;
    firebase_projects_android_apps_sha_list_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/availableLocations
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API (or associated documentation, as needed) to determine valid locations for each resource used in your Project._ Lists the valid ["locations for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified Project (including a FirebaseProject). One of these locations can be selected as the Project''s location for default Google Cloud resources, which is the geographical location where the Project''s resources associated with Google App Engine (such as the default Cloud Firestore instance) will be provisioned by default. However, if the location for default Google Cloud resources has already been set for the Project, then this setting cannot be changed. This call checks for any possible [location restrictions](<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`>) for the specified Project and, thus, might return a subset of all possible locations. To list all locations (regardless of any restrictions), call the endpoint without specifying a unique project identifier (that is, /v1beta1/{parent=`projects/-}/`listAvailableLocations``). To call ListAvailableLocations with a specified project, a member must be at minimum a Viewer of the Project. Calls without a specified project do not require any specific project permissions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_available_locations_list_execute()` to send, or `firebase_projects_available_locations_list` for simplest API.

pub fn firebase_projects_available_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/availableLocations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/availableLocations
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API (or associated documentation, as needed) to determine valid locations for each resource used in your Project._ Lists the valid ["locations for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified Project (including a FirebaseProject). One of these locations can be selected as the Project''s location for default Google Cloud resources, which is the geographical location where the Project''s resources associated with Google App Engine (such as the default Cloud Firestore instance) will be provisioned by default. However, if the location for default Google Cloud resources has already been set for the Project, then this setting cannot be changed. This call checks for any possible [location restrictions](<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`>) for the specified Project and, thus, might return a subset of all possible locations. To list all locations (regardless of any restrictions), call the endpoint without specifying a unique project identifier (that is, /v1beta1/{parent=`projects/-}/`listAvailableLocations``). To call ListAvailableLocations with a specified project, a member must be at minimum a Viewer of the Project. Calls without a specified project do not require any specific project permissions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_available_locations_list_execute()` or `firebase_projects_available_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_available_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_available_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAvailableLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAvailableLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/availableLocations
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API (or associated documentation, as needed) to determine valid locations for each resource used in your Project._ Lists the valid ["locations for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified Project (including a FirebaseProject). One of these locations can be selected as the Project''s location for default Google Cloud resources, which is the geographical location where the Project''s resources associated with Google App Engine (such as the default Cloud Firestore instance) will be provisioned by default. However, if the location for default Google Cloud resources has already been set for the Project, then this setting cannot be changed. This call checks for any possible [location restrictions](<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`>) for the specified Project and, thus, might return a subset of all possible locations. To list all locations (regardless of any restrictions), call the endpoint without specifying a unique project identifier (that is, /v1beta1/{parent=`projects/-}/`listAvailableLocations``). To call ListAvailableLocations with a specified project, a member must be at minimum a Viewer of the Project. Calls without a specified project do not require any specific project permissions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_available_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_available_locations_list_task()`.
/// For the simplest API, use `firebase_projects_available_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_available_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_available_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAvailableLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_available_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_available_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsAvailableLocationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/availableLocations
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API (or associated documentation, as needed) to determine valid locations for each resource used in your Project._ Lists the valid ["locations for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified Project (including a FirebaseProject). One of these locations can be selected as the Project''s location for default Google Cloud resources, which is the geographical location where the Project''s resources associated with Google App Engine (such as the default Cloud Firestore instance) will be provisioned by default. However, if the location for default Google Cloud resources has already been set for the Project, then this setting cannot be changed. This call checks for any possible [location restrictions](<https://cloud.google.`com/resource-manager/docs/organization-policy/defining-locations`>) for the specified Project and, thus, might return a subset of all possible locations. To list all locations (regardless of any restrictions), call the endpoint without specifying a unique project identifier (that is, /v1beta1/{parent=`projects/-}/`listAvailableLocations``). To call ListAvailableLocations with a specified project, a member must be at minimum a Viewer of the Project. Calls without a specified project do not require any specific project permissions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_available_locations_list_builder()` + `firebase_projects_available_locations_list_execute()`.
/// For task-level control, use `firebase_projects_available_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_available_locations_list(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsAvailableLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAvailableLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_available_locations_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    firebase_projects_available_locations_list_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/defaultLocation:finalize
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API to set the location for each resource used in your Project._ Sets the ["location for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified FirebaseProject. This method creates a Google App Engine application with a [default Cloud Storage bucket](<https://cloud.google.`com/appengine/docs/standard/python/googlecloudstorageclient/setting-up-cloud-storage`#activating_a_cloud_storage_bucket>), located in the specified [`locationId`](#body.request_body.FIELDS.location_id). This location must be one of the available [App Engine locations](<https://cloud.google.`com/about/locations`#region>). After the location for default Google Cloud resources is finalized, or if it was already set, it cannot be changed. The location for default Google Cloud resources for the specified FirebaseProject might already be set because either the underlying Google Cloud Project already has an App Engine application or FinalizeDefaultLocation was previously called with a specified `locationId`. The result of this call is an [Operation](../../v1beta1/operations), which can be used to track the provisioning process. The [response](../../v1beta1/operations#Operation.FIELDS.response) type of the Operation is google.protobuf.Empty. The Operation can be polled by its name using GetOperation until done is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation has succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) will be set to a google.protobuf.Empty; if the Operation has failed, its error will be set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. All fields listed in the [request body](#request-body) are required. To call FinalizeDefaultLocation, a member must be an Owner of the Project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_default_location_finalize_execute()` to send, or `firebase_projects_default_location_finalize` for simplest API.

pub fn firebase_projects_default_location_finalize_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/defaultLocation:finalize",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/defaultLocation:finalize
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API to set the location for each resource used in your Project._ Sets the ["location for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified FirebaseProject. This method creates a Google App Engine application with a [default Cloud Storage bucket](<https://cloud.google.`com/appengine/docs/standard/python/googlecloudstorageclient/setting-up-cloud-storage`#activating_a_cloud_storage_bucket>), located in the specified [`locationId`](#body.request_body.FIELDS.location_id). This location must be one of the available [App Engine locations](<https://cloud.google.`com/about/locations`#region>). After the location for default Google Cloud resources is finalized, or if it was already set, it cannot be changed. The location for default Google Cloud resources for the specified FirebaseProject might already be set because either the underlying Google Cloud Project already has an App Engine application or FinalizeDefaultLocation was previously called with a specified `locationId`. The result of this call is an [Operation](../../v1beta1/operations), which can be used to track the provisioning process. The [response](../../v1beta1/operations#Operation.FIELDS.response) type of the Operation is google.protobuf.Empty. The Operation can be polled by its name using GetOperation until done is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation has succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) will be set to a google.protobuf.Empty; if the Operation has failed, its error will be set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. All fields listed in the [request body](#request-body) are required. To call FinalizeDefaultLocation, a member must be an Owner of the Project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_default_location_finalize_execute()` or `firebase_projects_default_location_finalize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_default_location_finalize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_default_location_finalize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/defaultLocation:finalize
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API to set the location for each resource used in your Project._ Sets the ["location for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified FirebaseProject. This method creates a Google App Engine application with a [default Cloud Storage bucket](<https://cloud.google.`com/appengine/docs/standard/python/googlecloudstorageclient/setting-up-cloud-storage`#activating_a_cloud_storage_bucket>), located in the specified [`locationId`](#body.request_body.FIELDS.location_id). This location must be one of the available [App Engine locations](<https://cloud.google.`com/about/locations`#region>). After the location for default Google Cloud resources is finalized, or if it was already set, it cannot be changed. The location for default Google Cloud resources for the specified FirebaseProject might already be set because either the underlying Google Cloud Project already has an App Engine application or FinalizeDefaultLocation was previously called with a specified `locationId`. The result of this call is an [Operation](../../v1beta1/operations), which can be used to track the provisioning process. The [response](../../v1beta1/operations#Operation.FIELDS.response) type of the Operation is google.protobuf.Empty. The Operation can be polled by its name using GetOperation until done is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation has succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) will be set to a google.protobuf.Empty; if the Operation has failed, its error will be set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. All fields listed in the [request body](#request-body) are required. To call FinalizeDefaultLocation, a member must be an Owner of the Project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_default_location_finalize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_default_location_finalize_task()`.
/// For the simplest API, use `firebase_projects_default_location_finalize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_default_location_finalize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_default_location_finalize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_default_location_finalize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_default_location_finalize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsDefaultLocationFinalizeArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/defaultLocation:finalize
/// **DECOMMISSIONED.** **If called, this endpoint will return a 404 error.** _Instead, use the applicable resource-specific REST API to set the location for each resource used in your Project._ Sets the ["location for default Google Cloud resources"](<https://firebase.google.`com/docs/projects/locations`#default-cloud-location>) for the specified FirebaseProject. This method creates a Google App Engine application with a [default Cloud Storage bucket](<https://cloud.google.`com/appengine/docs/standard/python/googlecloudstorageclient/setting-up-cloud-storage`#activating_a_cloud_storage_bucket>), located in the specified [`locationId`](#body.request_body.FIELDS.location_id). This location must be one of the available [App Engine locations](<https://cloud.google.`com/about/locations`#region>). After the location for default Google Cloud resources is finalized, or if it was already set, it cannot be changed. The location for default Google Cloud resources for the specified FirebaseProject might already be set because either the underlying Google Cloud Project already has an App Engine application or FinalizeDefaultLocation was previously called with a specified `locationId`. The result of this call is an [Operation](../../v1beta1/operations), which can be used to track the provisioning process. The [response](../../v1beta1/operations#Operation.FIELDS.response) type of the Operation is google.protobuf.Empty. The Operation can be polled by its name using GetOperation until done is `true`. When done is `true`, the Operation has either succeeded or failed. If the Operation has succeeded, its [response](../../v1beta1/operations#Operation.FIELDS.response) will be set to a google.protobuf.Empty; if the Operation has failed, its error will be set to a google.rpc.Status. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation. All fields listed in the [request body](#request-body) are required. To call FinalizeDefaultLocation, a member must be an Owner of the Project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_default_location_finalize_builder()` + `firebase_projects_default_location_finalize_execute()`.
/// For task-level control, use `firebase_projects_default_location_finalize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_default_location_finalize(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsDefaultLocationFinalizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_default_location_finalize_builder(client, &args.parent)?;
    firebase_projects_default_location_finalize_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/iosApps
/// Requests the creation of a new IosApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_ios_apps_create_execute()` to send, or `firebase_projects_ios_apps_create` for simplest API.

pub fn firebase_projects_ios_apps_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/iosApps",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/iosApps
/// Requests the creation of a new IosApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_ios_apps_create_execute()` or `firebase_projects_ios_apps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/iosApps
/// Requests the creation of a new IosApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_ios_apps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_ios_apps_create_task()`.
/// For the simplest API, use `firebase_projects_ios_apps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_ios_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_ios_apps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_ios_apps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsIosAppsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/iosApps
/// Requests the creation of a new IosApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_ios_apps_create_builder()` + `firebase_projects_ios_apps_create_execute()`.
/// For task-level control, use `firebase_projects_ios_apps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_create(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsIosAppsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_ios_apps_create_builder(client, &args.parent)?;
    firebase_projects_ios_apps_create_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Gets the specified IosApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_ios_apps_get_execute()` to send, or `firebase_projects_ios_apps_get` for simplest API.

pub fn firebase_projects_ios_apps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/iosApps/{iosAppsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Gets the specified IosApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_ios_apps_get_execute()` or `firebase_projects_ios_apps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IosApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IosApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Gets the specified IosApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_ios_apps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_ios_apps_get_task()`.
/// For the simplest API, use `firebase_projects_ios_apps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_ios_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IosApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_ios_apps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_ios_apps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsIosAppsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Gets the specified IosApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_ios_apps_get_builder()` + `firebase_projects_ios_apps_get_execute()`.
/// For task-level control, use `firebase_projects_ios_apps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_get(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsIosAppsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IosApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_ios_apps_get_builder(client, &args.name)?;
    firebase_projects_ios_apps_get_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}/config
/// Gets the configuration artifact associated with the specified IosApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_ios_apps_get_config_execute()` to send, or `firebase_projects_ios_apps_get_config` for simplest API.

pub fn firebase_projects_ios_apps_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/iosApps/{iosAppsId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}/config
/// Gets the configuration artifact associated with the specified IosApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_ios_apps_get_config_execute()` or `firebase_projects_ios_apps_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IosAppConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IosAppConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}/config
/// Gets the configuration artifact associated with the specified IosApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_ios_apps_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_ios_apps_get_config_task()`.
/// For the simplest API, use `firebase_projects_ios_apps_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_ios_apps_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IosAppConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_ios_apps_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_ios_apps_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsIosAppsGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/iosApps/{iosAppsId}/config
/// Gets the configuration artifact associated with the specified IosApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_ios_apps_get_config_builder()` + `firebase_projects_ios_apps_get_config_execute()`.
/// For task-level control, use `firebase_projects_ios_apps_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_get_config(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsIosAppsGetConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IosAppConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_ios_apps_get_config_builder(client, &args.name)?;
    firebase_projects_ios_apps_get_config_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/iosApps
/// Lists each IosApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_ios_apps_list_execute()` to send, or `firebase_projects_ios_apps_list` for simplest API.

pub fn firebase_projects_ios_apps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    showDeleted: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/iosApps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/iosApps
/// Lists each IosApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_ios_apps_list_execute()` or `firebase_projects_ios_apps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListIosAppsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListIosAppsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/iosApps
/// Lists each IosApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_ios_apps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_ios_apps_list_task()`.
/// For the simplest API, use `firebase_projects_ios_apps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_ios_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListIosAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_ios_apps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_ios_apps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsIosAppsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/iosApps
/// Lists each IosApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_ios_apps_list_builder()` + `firebase_projects_ios_apps_list_execute()`.
/// For task-level control, use `firebase_projects_ios_apps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_list(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsIosAppsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListIosAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_ios_apps_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.showDeleted,
    )?;
    firebase_projects_ios_apps_list_execute(builder)
}

/// PATCH v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Updates the attributes of the specified IosApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_ios_apps_patch_execute()` to send, or `firebase_projects_ios_apps_patch` for simplest API.

pub fn firebase_projects_ios_apps_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/iosApps/{iosAppsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Updates the attributes of the specified IosApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_ios_apps_patch_execute()` or `firebase_projects_ios_apps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IosApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IosApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Updates the attributes of the specified IosApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_ios_apps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_ios_apps_patch_task()`.
/// For the simplest API, use `firebase_projects_ios_apps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_ios_apps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IosApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_ios_apps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_ios_apps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsIosAppsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1beta1/projects/{projectsId}/iosApps/{iosAppsId}
/// Updates the attributes of the specified IosApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_ios_apps_patch_builder()` + `firebase_projects_ios_apps_patch_execute()`.
/// For task-level control, use `firebase_projects_ios_apps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_patch(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsIosAppsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IosApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_ios_apps_patch_builder(client, &args.name, &args.updateMask)?;
    firebase_projects_ios_apps_patch_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:remove
/// Removes the specified IosApp from the FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_ios_apps_remove_execute()` to send, or `firebase_projects_ios_apps_remove` for simplest API.

pub fn firebase_projects_ios_apps_remove_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/iosApps/{iosAppsId}:remove",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:remove
/// Removes the specified IosApp from the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_ios_apps_remove_execute()` or `firebase_projects_ios_apps_remove`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_remove_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:remove
/// Removes the specified IosApp from the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_ios_apps_remove_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_ios_apps_remove_task()`.
/// For the simplest API, use `firebase_projects_ios_apps_remove()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_ios_apps_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_ios_apps_remove_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_ios_apps_remove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsIosAppsRemoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:remove
/// Removes the specified IosApp from the FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_ios_apps_remove_builder()` + `firebase_projects_ios_apps_remove_execute()`.
/// For task-level control, use `firebase_projects_ios_apps_remove_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_remove(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsIosAppsRemoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_ios_apps_remove_builder(client, &args.name)?;
    firebase_projects_ios_apps_remove_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:undelete
/// Restores the specified IosApp to the FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_ios_apps_undelete_execute()` to send, or `firebase_projects_ios_apps_undelete` for simplest API.

pub fn firebase_projects_ios_apps_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/iosApps/{iosAppsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:undelete
/// Restores the specified IosApp to the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_ios_apps_undelete_execute()` or `firebase_projects_ios_apps_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:undelete
/// Restores the specified IosApp to the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_ios_apps_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_ios_apps_undelete_task()`.
/// For the simplest API, use `firebase_projects_ios_apps_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_ios_apps_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_ios_apps_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_ios_apps_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_ios_apps_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsIosAppsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/projects/{projectsId}/iosApps/{iosAppsId}:undelete
/// Restores the specified IosApp to the FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_ios_apps_undelete_builder()` + `firebase_projects_ios_apps_undelete_execute()`.
/// For task-level control, use `firebase_projects_ios_apps_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_ios_apps_undelete(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsIosAppsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_ios_apps_undelete_builder(client, &args.name)?;
    firebase_projects_ios_apps_undelete_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/webApps
/// Requests the creation of a new WebApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_web_apps_create_execute()` to send, or `firebase_projects_web_apps_create` for simplest API.

pub fn firebase_projects_web_apps_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/webApps",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/webApps
/// Requests the creation of a new WebApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_web_apps_create_execute()` or `firebase_projects_web_apps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/webApps
/// Requests the creation of a new WebApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_web_apps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_web_apps_create_task()`.
/// For the simplest API, use `firebase_projects_web_apps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_web_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_web_apps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_web_apps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsWebAppsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/webApps
/// Requests the creation of a new WebApp in the specified FirebaseProject. The result of this call is an Operation which can be used to track the provisioning process. The Operation is automatically deleted after completion, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_web_apps_create_builder()` + `firebase_projects_web_apps_create_execute()`.
/// For task-level control, use `firebase_projects_web_apps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_create(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsWebAppsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_web_apps_create_builder(client, &args.parent)?;
    firebase_projects_web_apps_create_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Gets the specified WebApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_web_apps_get_execute()` to send, or `firebase_projects_web_apps_get` for simplest API.

pub fn firebase_projects_web_apps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/webApps/{webAppsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Gets the specified WebApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_web_apps_get_execute()` or `firebase_projects_web_apps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Gets the specified WebApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_web_apps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_web_apps_get_task()`.
/// For the simplest API, use `firebase_projects_web_apps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_web_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_web_apps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_web_apps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsWebAppsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Gets the specified WebApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_web_apps_get_builder()` + `firebase_projects_web_apps_get_execute()`.
/// For task-level control, use `firebase_projects_web_apps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_get(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsWebAppsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_web_apps_get_builder(client, &args.name)?;
    firebase_projects_web_apps_get_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}/config
/// Gets the configuration artifact associated with the specified WebApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_web_apps_get_config_execute()` to send, or `firebase_projects_web_apps_get_config` for simplest API.

pub fn firebase_projects_web_apps_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/webApps/{webAppsId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}/config
/// Gets the configuration artifact associated with the specified WebApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_web_apps_get_config_execute()` or `firebase_projects_web_apps_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebAppConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebAppConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}/config
/// Gets the configuration artifact associated with the specified WebApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_web_apps_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_web_apps_get_config_task()`.
/// For the simplest API, use `firebase_projects_web_apps_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_web_apps_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebAppConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_web_apps_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_web_apps_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsWebAppsGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/webApps/{webAppsId}/config
/// Gets the configuration artifact associated with the specified WebApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_web_apps_get_config_builder()` + `firebase_projects_web_apps_get_config_execute()`.
/// For task-level control, use `firebase_projects_web_apps_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_get_config(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsWebAppsGetConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebAppConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_web_apps_get_config_builder(client, &args.name)?;
    firebase_projects_web_apps_get_config_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/webApps
/// Lists each WebApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_web_apps_list_execute()` to send, or `firebase_projects_web_apps_list` for simplest API.

pub fn firebase_projects_web_apps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    showDeleted: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/webApps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/webApps
/// Lists each WebApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_web_apps_list_execute()` or `firebase_projects_web_apps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWebAppsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWebAppsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/webApps
/// Lists each WebApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_web_apps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_web_apps_list_task()`.
/// For the simplest API, use `firebase_projects_web_apps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_web_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWebAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebase_projects_web_apps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_web_apps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsWebAppsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/webApps
/// Lists each WebApp associated with the specified FirebaseProject. The elements are returned in no particular order, but will be a consistent view of the Apps when additional requests are made with a `pageToken`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_web_apps_list_builder()` + `firebase_projects_web_apps_list_execute()`.
/// For task-level control, use `firebase_projects_web_apps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_list(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsWebAppsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWebAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebase_projects_web_apps_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.showDeleted,
    )?;
    firebase_projects_web_apps_list_execute(builder)
}

/// PATCH v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Updates the attributes of the specified WebApp.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_web_apps_patch_execute()` to send, or `firebase_projects_web_apps_patch` for simplest API.

pub fn firebase_projects_web_apps_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/webApps/{webAppsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Updates the attributes of the specified WebApp.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_web_apps_patch_execute()` or `firebase_projects_web_apps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Updates the attributes of the specified WebApp.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_web_apps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_web_apps_patch_task()`.
/// For the simplest API, use `firebase_projects_web_apps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_web_apps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_web_apps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_web_apps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsWebAppsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1beta1/projects/{projectsId}/webApps/{webAppsId}
/// Updates the attributes of the specified WebApp.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_web_apps_patch_builder()` + `firebase_projects_web_apps_patch_execute()`.
/// For task-level control, use `firebase_projects_web_apps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_patch(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsWebAppsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_web_apps_patch_builder(client, &args.name, &args.updateMask)?;
    firebase_projects_web_apps_patch_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:remove
/// Removes the specified WebApp from the FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_web_apps_remove_execute()` to send, or `firebase_projects_web_apps_remove` for simplest API.

pub fn firebase_projects_web_apps_remove_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/webApps/{webAppsId}:remove",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:remove
/// Removes the specified WebApp from the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_web_apps_remove_execute()` or `firebase_projects_web_apps_remove`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_remove_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:remove
/// Removes the specified WebApp from the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_web_apps_remove_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_web_apps_remove_task()`.
/// For the simplest API, use `firebase_projects_web_apps_remove()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_remove_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_web_apps_remove_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_web_apps_remove_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_web_apps_remove`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsWebAppsRemoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:remove
/// Removes the specified WebApp from the FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_web_apps_remove_builder()` + `firebase_projects_web_apps_remove_execute()`.
/// For task-level control, use `firebase_projects_web_apps_remove_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_remove(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsWebAppsRemoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_web_apps_remove_builder(client, &args.name)?;
    firebase_projects_web_apps_remove_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:undelete
/// Restores the specified WebApp to the FirebaseProject.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebase_projects_web_apps_undelete_execute()` to send, or `firebase_projects_web_apps_undelete` for simplest API.

pub fn firebase_projects_web_apps_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://firebase.googleapis.com/v1beta1/projects/{}/webApps/{webAppsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:undelete
/// Restores the specified WebApp to the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebase_projects_web_apps_undelete_execute()` or `firebase_projects_web_apps_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:undelete
/// Restores the specified WebApp to the FirebaseProject.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebase_projects_web_apps_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebase_projects_web_apps_undelete_task()`.
/// For the simplest API, use `firebase_projects_web_apps_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebase_projects_web_apps_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebase_projects_web_apps_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebase_projects_web_apps_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebase_projects_web_apps_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaseProjectsWebAppsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/projects/{projectsId}/webApps/{webAppsId}:undelete
/// Restores the specified WebApp to the FirebaseProject.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebase_projects_web_apps_undelete_builder()` + `firebase_projects_web_apps_undelete_execute()`.
/// For task-level control, use `firebase_projects_web_apps_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebase_projects_web_apps_undelete(
    client: &SimpleHttpClient,
    args: &FirebaseProjectsWebAppsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebase_projects_web_apps_undelete_builder(client, &args.name)?;
    firebase_projects_web_apps_undelete_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for ListAvailableProjectsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAvailableProjectsResponse with FirebaseAvailableProjectsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseAvailableProjectsListArgs> for ListAvailableProjectsResponse {
    fn generate_resource_id(&self, input: &FirebaseAvailableProjectsListArgs) -> String {
        "gcp::firebase::ListAvailableProjectsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ListAvailableProjectsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseOperationsGetArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsAddFirebaseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAddFirebaseArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsAddFirebaseArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsAddGoogleAnalyticsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAddGoogleAnalyticsArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsAddGoogleAnalyticsArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirebaseProject
// =============================================================================

/// ResourceIdentifier implementation for FirebaseProject with FirebaseProjectsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsGetArgs> for FirebaseProject {
    fn generate_resource_id(&self, input: &FirebaseProjectsGetArgs) -> String {
        format!("gcp::firebase::FirebaseProject/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::FirebaseProject"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdminSdkConfig
// =============================================================================

/// ResourceIdentifier implementation for AdminSdkConfig with FirebaseProjectsGetAdminSdkConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsGetAdminSdkConfigArgs> for AdminSdkConfig {
    fn generate_resource_id(&self, input: &FirebaseProjectsGetAdminSdkConfigArgs) -> String {
        format!("gcp::firebase::AdminSdkConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::AdminSdkConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AnalyticsDetails
// =============================================================================

/// ResourceIdentifier implementation for AnalyticsDetails with FirebaseProjectsGetAnalyticsDetailsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsGetAnalyticsDetailsArgs> for AnalyticsDetails {
    fn generate_resource_id(&self, input: &FirebaseProjectsGetAnalyticsDetailsArgs) -> String {
        format!("gcp::firebase::AnalyticsDetails/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::AnalyticsDetails"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFirebaseProjectsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFirebaseProjectsResponse with FirebaseProjectsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsListArgs> for ListFirebaseProjectsResponse {
    fn generate_resource_id(&self, input: &FirebaseProjectsListArgs) -> String {
        "gcp::firebase::ListFirebaseProjectsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ListFirebaseProjectsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirebaseProject
// =============================================================================

/// ResourceIdentifier implementation for FirebaseProject with FirebaseProjectsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsPatchArgs> for FirebaseProject {
    fn generate_resource_id(&self, input: &FirebaseProjectsPatchArgs) -> String {
        format!("gcp::firebase::FirebaseProject/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::FirebaseProject"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirebaseProjectsRemoveAnalyticsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsRemoveAnalyticsArgs> for Empty {
    fn generate_resource_id(&self, input: &FirebaseProjectsRemoveAnalyticsArgs) -> String {
        format!("gcp::firebase::Empty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchFirebaseAppsResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchFirebaseAppsResponse with FirebaseProjectsSearchAppsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsSearchAppsArgs> for SearchFirebaseAppsResponse {
    fn generate_resource_id(&self, input: &FirebaseProjectsSearchAppsArgs) -> String {
        format!("gcp::firebase::SearchFirebaseAppsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::SearchFirebaseAppsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsAndroidAppsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsCreateArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AndroidApp
// =============================================================================

/// ResourceIdentifier implementation for AndroidApp with FirebaseProjectsAndroidAppsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsGetArgs> for AndroidApp {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsGetArgs) -> String {
        format!("gcp::firebase::AndroidApp/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::AndroidApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AndroidAppConfig
// =============================================================================

/// ResourceIdentifier implementation for AndroidAppConfig with FirebaseProjectsAndroidAppsGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsGetConfigArgs> for AndroidAppConfig {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsGetConfigArgs) -> String {
        format!("gcp::firebase::AndroidAppConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::AndroidAppConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAndroidAppsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAndroidAppsResponse with FirebaseProjectsAndroidAppsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsListArgs> for ListAndroidAppsResponse {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsListArgs) -> String {
        format!("gcp::firebase::ListAndroidAppsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ListAndroidAppsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AndroidApp
// =============================================================================

/// ResourceIdentifier implementation for AndroidApp with FirebaseProjectsAndroidAppsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsPatchArgs> for AndroidApp {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsPatchArgs) -> String {
        format!("gcp::firebase::AndroidApp/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::AndroidApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsAndroidAppsRemoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsRemoveArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsRemoveArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsAndroidAppsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsUndeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsUndeleteArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ShaCertificate
// =============================================================================

/// ResourceIdentifier implementation for ShaCertificate with FirebaseProjectsAndroidAppsShaCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsShaCreateArgs> for ShaCertificate {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsShaCreateArgs) -> String {
        format!("gcp::firebase::ShaCertificate/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ShaCertificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirebaseProjectsAndroidAppsShaDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsShaDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsShaDeleteArgs) -> String {
        format!("gcp::firebase::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListShaCertificatesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListShaCertificatesResponse with FirebaseProjectsAndroidAppsShaListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAndroidAppsShaListArgs> for ListShaCertificatesResponse {
    fn generate_resource_id(&self, input: &FirebaseProjectsAndroidAppsShaListArgs) -> String {
        format!(
            "gcp::firebase::ListShaCertificatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ListShaCertificatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAvailableLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAvailableLocationsResponse with FirebaseProjectsAvailableLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsAvailableLocationsListArgs>
    for ListAvailableLocationsResponse
{
    fn generate_resource_id(&self, input: &FirebaseProjectsAvailableLocationsListArgs) -> String {
        format!(
            "gcp::firebase::ListAvailableLocationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ListAvailableLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsDefaultLocationFinalizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsDefaultLocationFinalizeArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsDefaultLocationFinalizeArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsIosAppsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsIosAppsCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsIosAppsCreateArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for IosApp
// =============================================================================

/// ResourceIdentifier implementation for IosApp with FirebaseProjectsIosAppsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsIosAppsGetArgs> for IosApp {
    fn generate_resource_id(&self, input: &FirebaseProjectsIosAppsGetArgs) -> String {
        format!("gcp::firebase::IosApp/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::IosApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for IosAppConfig
// =============================================================================

/// ResourceIdentifier implementation for IosAppConfig with FirebaseProjectsIosAppsGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsIosAppsGetConfigArgs> for IosAppConfig {
    fn generate_resource_id(&self, input: &FirebaseProjectsIosAppsGetConfigArgs) -> String {
        format!("gcp::firebase::IosAppConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::IosAppConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListIosAppsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListIosAppsResponse with FirebaseProjectsIosAppsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsIosAppsListArgs> for ListIosAppsResponse {
    fn generate_resource_id(&self, input: &FirebaseProjectsIosAppsListArgs) -> String {
        format!("gcp::firebase::ListIosAppsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ListIosAppsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for IosApp
// =============================================================================

/// ResourceIdentifier implementation for IosApp with FirebaseProjectsIosAppsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsIosAppsPatchArgs> for IosApp {
    fn generate_resource_id(&self, input: &FirebaseProjectsIosAppsPatchArgs) -> String {
        format!("gcp::firebase::IosApp/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::IosApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsIosAppsRemoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsIosAppsRemoveArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsIosAppsRemoveArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsIosAppsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsIosAppsUndeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsIosAppsUndeleteArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsWebAppsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsWebAppsCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsWebAppsCreateArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WebApp
// =============================================================================

/// ResourceIdentifier implementation for WebApp with FirebaseProjectsWebAppsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsWebAppsGetArgs> for WebApp {
    fn generate_resource_id(&self, input: &FirebaseProjectsWebAppsGetArgs) -> String {
        format!("gcp::firebase::WebApp/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::WebApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WebAppConfig
// =============================================================================

/// ResourceIdentifier implementation for WebAppConfig with FirebaseProjectsWebAppsGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsWebAppsGetConfigArgs> for WebAppConfig {
    fn generate_resource_id(&self, input: &FirebaseProjectsWebAppsGetConfigArgs) -> String {
        format!("gcp::firebase::WebAppConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::WebAppConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListWebAppsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListWebAppsResponse with FirebaseProjectsWebAppsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsWebAppsListArgs> for ListWebAppsResponse {
    fn generate_resource_id(&self, input: &FirebaseProjectsWebAppsListArgs) -> String {
        format!("gcp::firebase::ListWebAppsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::ListWebAppsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WebApp
// =============================================================================

/// ResourceIdentifier implementation for WebApp with FirebaseProjectsWebAppsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsWebAppsPatchArgs> for WebApp {
    fn generate_resource_id(&self, input: &FirebaseProjectsWebAppsPatchArgs) -> String {
        format!("gcp::firebase::WebApp/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::WebApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsWebAppsRemoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsWebAppsRemoveArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsWebAppsRemoveArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with FirebaseProjectsWebAppsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirebaseProjectsWebAppsUndeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &FirebaseProjectsWebAppsUndeleteArgs) -> String {
        format!("gcp::firebase::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firebase::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
