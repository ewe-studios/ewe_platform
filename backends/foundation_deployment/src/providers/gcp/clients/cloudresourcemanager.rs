//! Auto-generated API clients for gcp/cloudresourcemanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/cloudresourcemanager`

#![cfg(feature = "gcp/cloudresourcemanager")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/effectiveTags
/// Return a list of effective tags for the given Google Cloud resource, as specified in parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_effective_tags_list_execute()` to send, or `cloudresourcemanager_effective_tags_list` for simplest API.

pub fn cloudresourcemanager_effective_tags_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/effectiveTags",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/effectiveTags
/// Return a list of effective tags for the given Google Cloud resource, as specified in parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_effective_tags_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_effective_tags_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_effective_tags_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEffectiveTagsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEffectiveTagsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_effective_tags_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerEffectiveTagsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v3/effectiveTags
/// Return a list of effective tags for the given Google Cloud resource, as specified in parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_effective_tags_list_builder()` + `cloudresourcemanager_effective_tags_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_effective_tags_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerEffectiveTagsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEffectiveTagsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_effective_tags_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    cloudresourcemanager_effective_tags_list_execute(builder)
}

/// GET v3/folders
/// Creates a folder in the resource hierarchy. Returns an Operation which can be used to track the progress of the folder creation workflow. Upon success, the Operation.response field will be populated with the created Folder. In order to succeed, the addition of this new folder must not violate the folder naming, height, or fanout constraints. + The folder's display_name must be distinct from all other folders that share its parent. + The addition of the folder must not cause the active folder hierarchy to exceed a height of 10. Note, the full active + deleted folder hierarchy is allowed to reach a height of 20; this provides additional headroom when moving folders that contain deleted folders. + The addition of the folder must not cause the total number of folders under its parent to exceed 300. If the operation fails due to a folder constraint violation, some errors may be returned by the CreateFolder request, with status code FAILED_PRECONDITION and an error description. Other folder constraint violations will be communicated in the Operation, with the specific PreconditionFailure returned in the details list in the Operation.error field. The caller must have resourcemanager.folders.create permission on the identified parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_create_execute()` to send, or `cloudresourcemanager_folders_create` for simplest API.

pub fn cloudresourcemanager_folders_create_builder(
    client: &SimpleHttpClient,
    body: &Folder,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/folders",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders
/// Creates a folder in the resource hierarchy. Returns an Operation which can be used to track the progress of the folder creation workflow. Upon success, the Operation.response field will be populated with the created Folder. In order to succeed, the addition of this new folder must not violate the folder naming, height, or fanout constraints. + The folder's display_name must be distinct from all other folders that share its parent. + The addition of the folder must not cause the active folder hierarchy to exceed a height of 10. Note, the full active + deleted folder hierarchy is allowed to reach a height of 20; this provides additional headroom when moving folders that contain deleted folders. + The addition of the folder must not cause the total number of folders under its parent to exceed 300. If the operation fails due to a folder constraint violation, some errors may be returned by the CreateFolder request, with status code FAILED_PRECONDITION and an error description. Other folder constraint violations will be communicated in the Operation, with the specific PreconditionFailure returned in the details list in the Operation.error field. The caller must have resourcemanager.folders.create permission on the identified parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersCreateArgs {
    /// Request body.
    pub body: Folder,
}

/// GET v3/folders
/// Creates a folder in the resource hierarchy. Returns an Operation which can be used to track the progress of the folder creation workflow. Upon success, the Operation.response field will be populated with the created Folder. In order to succeed, the addition of this new folder must not violate the folder naming, height, or fanout constraints. + The folder's display_name must be distinct from all other folders that share its parent. + The addition of the folder must not cause the active folder hierarchy to exceed a height of 10. Note, the full active + deleted folder hierarchy is allowed to reach a height of 20; this provides additional headroom when moving folders that contain deleted folders. + The addition of the folder must not cause the total number of folders under its parent to exceed 300. If the operation fails due to a folder constraint violation, some errors may be returned by the CreateFolder request, with status code FAILED_PRECONDITION and an error description. Other folder constraint violations will be communicated in the Operation, with the specific PreconditionFailure returned in the details list in the Operation.error field. The caller must have resourcemanager.folders.create permission on the identified parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_create_builder()` + `cloudresourcemanager_folders_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_create(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_create_builder(client, &args.body)?;
    cloudresourcemanager_folders_create_execute(builder)
}

/// GET v3/folders/{foldersId}
/// Requests deletion of a folder. The folder is moved into the DELETE_REQUESTED state immediately, and is deleted approximately 30 days later. This method may only be called on an empty folder, where a folder is empty if it doesn't contain any folders or projects in the `ACTIVE` state. If called on a folder in DELETE_REQUESTED state the operation will result in a no-op success. The caller must have resourcemanager.folders.delete permission on the identified folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_delete_execute()` to send, or `cloudresourcemanager_folders_delete` for simplest API.

pub fn cloudresourcemanager_folders_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/folders/{foldersId}
/// Requests deletion of a folder. The folder is moved into the DELETE_REQUESTED state immediately, and is deleted approximately 30 days later. This method may only be called on an empty folder, where a folder is empty if it doesn't contain any folders or projects in the `ACTIVE` state. If called on a folder in DELETE_REQUESTED state the operation will result in a no-op success. The caller must have resourcemanager.folders.delete permission on the identified folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/folders/{foldersId}
/// Requests deletion of a folder. The folder is moved into the DELETE_REQUESTED state immediately, and is deleted approximately 30 days later. This method may only be called on an empty folder, where a folder is empty if it doesn't contain any folders or projects in the `ACTIVE` state. If called on a folder in DELETE_REQUESTED state the operation will result in a no-op success. The caller must have resourcemanager.folders.delete permission on the identified folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_delete_builder()` + `cloudresourcemanager_folders_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_delete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_delete_builder(client, &args.name)?;
    cloudresourcemanager_folders_delete_execute(builder)
}

/// GET v3/folders/{foldersId}
/// Retrieves a folder identified by the supplied resource name. Valid folder resource names have the format `folders/{folder_id}` (for example, `folders/1234`). The caller must have resourcemanager.folders.get permission on the identified folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_get_execute()` to send, or `cloudresourcemanager_folders_get` for simplest API.

pub fn cloudresourcemanager_folders_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/folders/{foldersId}
/// Retrieves a folder identified by the supplied resource name. Valid folder resource names have the format `folders/{folder_id}` (for example, `folders/1234`). The caller must have resourcemanager.folders.get permission on the identified folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Folder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/folders/{foldersId}
/// Retrieves a folder identified by the supplied resource name. Valid folder resource names have the format `folders/{folder_id}` (for example, `folders/1234`). The caller must have resourcemanager.folders.get permission on the identified folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_get_builder()` + `cloudresourcemanager_folders_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Folder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_get_builder(client, &args.name)?;
    cloudresourcemanager_folders_get_execute(builder)
}

/// GET v3/folders/{foldersId}:getIamPolicy
/// Gets the access control policy for a folder. The returned policy may be empty if no such policy or resource exists. The resource field should be the folder's resource name, for example: "`folders/1234`". The caller must have resourcemanager.folders.`getIamPolicy` permission on the identified folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_get_iam_policy_execute()` to send, or `cloudresourcemanager_folders_get_iam_policy` for simplest API.

pub fn cloudresourcemanager_folders_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders/{foldersId}:getIamPolicy
/// Gets the access control policy for a folder. The returned policy may be empty if no such policy or resource exists. The resource field should be the folder's resource name, for example: "`folders/1234`". The caller must have resourcemanager.folders.`getIamPolicy` permission on the identified folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v3/folders/{foldersId}:getIamPolicy
/// Gets the access control policy for a folder. The returned policy may be empty if no such policy or resource exists. The resource field should be the folder's resource name, for example: "`folders/1234`". The caller must have resourcemanager.folders.`getIamPolicy` permission on the identified folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_get_iam_policy_builder()` + `cloudresourcemanager_folders_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_folders_get_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_folders_get_iam_policy_execute(builder)
}

/// GET v3/folders
/// Lists the folders that are direct descendants of supplied parent resource. list() provides a strongly consistent view of the folders underneath the specified parent resource. list() returns folders sorted based upon the (ascending) lexical ordering of their display_name. The caller must have resourcemanager.folders.list permission on the identified parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_list_execute()` to send, or `cloudresourcemanager_folders_list` for simplest API.

pub fn cloudresourcemanager_folders_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/folders",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/folders
/// Lists the folders that are direct descendants of supplied parent resource. list() provides a strongly consistent view of the folders underneath the specified parent resource. list() returns folders sorted based upon the (ascending) lexical ordering of their display_name. The caller must have resourcemanager.folders.list permission on the identified parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFoldersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFoldersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
}

/// GET v3/folders
/// Lists the folders that are direct descendants of supplied parent resource. list() provides a strongly consistent view of the folders underneath the specified parent resource. list() returns folders sorted based upon the (ascending) lexical ordering of their display_name. The caller must have resourcemanager.folders.list permission on the identified parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_list_builder()` + `cloudresourcemanager_folders_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFoldersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
        args.showDeleted,
    )?;
    cloudresourcemanager_folders_list_execute(builder)
}

/// GET v3/folders/{foldersId}:move
/// Moves a folder under a new resource parent. Returns an Operation which can be used to track the progress of the folder move workflow. Upon success, the Operation.response field will be populated with the moved folder. Upon failure, a FolderOperationError categorizing the failure cause will be returned - if the failure occurs synchronously then the FolderOperationError will be returned in the Status.details field. If it occurs asynchronously, then the FolderOperation will be returned in the Operation.error field. In addition, the Operation.metadata field will be populated with a FolderOperation message as an aid to stateless clients. Folder moves will be rejected if they violate either the naming, height, or fanout constraints described in the CreateFolder documentation. The caller must have resourcemanager.folders.move permission on the folder's current and proposed new parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_move_execute()` to send, or `cloudresourcemanager_folders_move` for simplest API.

pub fn cloudresourcemanager_folders_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MoveFolderRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders/{foldersId}:move
/// Moves a folder under a new resource parent. Returns an Operation which can be used to track the progress of the folder move workflow. Upon success, the Operation.response field will be populated with the moved folder. Upon failure, a FolderOperationError categorizing the failure cause will be returned - if the failure occurs synchronously then the FolderOperationError will be returned in the Status.details field. If it occurs asynchronously, then the FolderOperation will be returned in the Operation.error field. In addition, the Operation.metadata field will be populated with a FolderOperation message as an aid to stateless clients. Folder moves will be rejected if they violate either the naming, height, or fanout constraints described in the CreateFolder documentation. The caller must have resourcemanager.folders.move permission on the folder's current and proposed new parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: MoveFolderRequest,
}

/// GET v3/folders/{foldersId}:move
/// Moves a folder under a new resource parent. Returns an Operation which can be used to track the progress of the folder move workflow. Upon success, the Operation.response field will be populated with the moved folder. Upon failure, a FolderOperationError categorizing the failure cause will be returned - if the failure occurs synchronously then the FolderOperationError will be returned in the Status.details field. If it occurs asynchronously, then the FolderOperation will be returned in the Operation.error field. In addition, the Operation.metadata field will be populated with a FolderOperation message as an aid to stateless clients. Folder moves will be rejected if they violate either the naming, height, or fanout constraints described in the CreateFolder documentation. The caller must have resourcemanager.folders.move permission on the folder's current and proposed new parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_move_builder()` + `cloudresourcemanager_folders_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_move(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_move_builder(client, &args.name, &args.body)?;
    cloudresourcemanager_folders_move_execute(builder)
}

/// GET v3/folders/{foldersId}
/// Updates a folder, changing its display_name. Changes to the folder display_name will be rejected if they violate either the display_name formatting rules or the naming constraints described in the CreateFolder documentation. The folder's display_name must start and end with a letter or digit, may contain letters, digits, spaces, hyphens and underscores and can be between 3 and 30 characters. This is captured by the regular expression: \p{L}\p{N}{1,28}[\p{L}\p{N}]. The caller must have resourcemanager.folders.update permission on the identified folder. If the update fails due to the unique name constraint then a PreconditionFailure explaining this violation will be returned in the Status.details field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_patch_execute()` to send, or `cloudresourcemanager_folders_patch` for simplest API.

pub fn cloudresourcemanager_folders_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Folder,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders/{foldersId}
/// Updates a folder, changing its display_name. Changes to the folder display_name will be rejected if they violate either the display_name formatting rules or the naming constraints described in the CreateFolder documentation. The folder's display_name must start and end with a letter or digit, may contain letters, digits, spaces, hyphens and underscores and can be between 3 and 30 characters. This is captured by the regular expression: \p{L}\p{N}{1,28}[\p{L}\p{N}]. The caller must have resourcemanager.folders.update permission on the identified folder. If the update fails due to the unique name constraint then a PreconditionFailure explaining this violation will be returned in the Status.details field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Folder,
}

/// GET v3/folders/{foldersId}
/// Updates a folder, changing its display_name. Changes to the folder display_name will be rejected if they violate either the display_name formatting rules or the naming constraints described in the CreateFolder documentation. The folder's display_name must start and end with a letter or digit, may contain letters, digits, spaces, hyphens and underscores and can be between 3 and 30 characters. This is captured by the regular expression: \p{L}\p{N}{1,28}[\p{L}\p{N}]. The caller must have resourcemanager.folders.update permission on the identified folder. If the update fails due to the unique name constraint then a PreconditionFailure explaining this violation will be returned in the Status.details field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_patch_builder()` + `cloudresourcemanager_folders_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_patch(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudresourcemanager_folders_patch_execute(builder)
}

/// GET v3/folders:search
/// Search for folders that match specific filter criteria. search() provides an eventually consistent view of the folders a user has access to which meet the specified filter criteria. This will only return folders on which the caller has the permission resourcemanager.folders.get.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_search_execute()` to send, or `cloudresourcemanager_folders_search` for simplest API.

pub fn cloudresourcemanager_folders_search_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/folders:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/folders:search
/// Search for folders that match specific filter criteria. search() provides an eventually consistent view of the folders a user has access to which meet the specified filter criteria. This will only return folders on which the caller has the permission resourcemanager.folders.get.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFoldersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchFoldersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersSearchArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v3/folders:search
/// Search for folders that match specific filter criteria. search() provides an eventually consistent view of the folders a user has access to which meet the specified filter criteria. This will only return folders on which the caller has the permission resourcemanager.folders.get.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_search_builder()` + `cloudresourcemanager_folders_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_search(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFoldersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_search_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
    )?;
    cloudresourcemanager_folders_search_execute(builder)
}

/// GET v3/folders/{foldersId}:setIamPolicy
/// Sets the access control policy on a folder, replacing any existing policy. The resource field should be the folder's resource name, for example: "`folders/1234`". The caller must have resourcemanager.folders.`setIamPolicy` permission on the identified folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_set_iam_policy_execute()` to send, or `cloudresourcemanager_folders_set_iam_policy` for simplest API.

pub fn cloudresourcemanager_folders_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders/{foldersId}:setIamPolicy
/// Sets the access control policy on a folder, replacing any existing policy. The resource field should be the folder's resource name, for example: "`folders/1234`". The caller must have resourcemanager.folders.`setIamPolicy` permission on the identified folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v3/folders/{foldersId}:setIamPolicy
/// Sets the access control policy on a folder, replacing any existing policy. The resource field should be the folder's resource name, for example: "`folders/1234`". The caller must have resourcemanager.folders.`setIamPolicy` permission on the identified folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_set_iam_policy_builder()` + `cloudresourcemanager_folders_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_folders_set_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_folders_set_iam_policy_execute(builder)
}

/// GET v3/folders/{foldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified folder. The resource field should be the folder's resource name, for example: "`folders/1234`". There are no permissions required for making this API call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_test_iam_permissions_execute()` to send, or `cloudresourcemanager_folders_test_iam_permissions` for simplest API.

pub fn cloudresourcemanager_folders_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders/{foldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified folder. The resource field should be the folder's resource name, for example: "`folders/1234`". There are no permissions required for making this API call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v3/folders/{foldersId}:testIamPermissions
/// Returns permissions that a caller has on the specified folder. The resource field should be the folder's resource name, for example: "`folders/1234`". There are no permissions required for making this API call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_test_iam_permissions_builder()` + `cloudresourcemanager_folders_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudresourcemanager_folders_test_iam_permissions_execute(builder)
}

/// GET v3/folders/{foldersId}:undelete
/// Cancels the deletion request for a folder. This method may be called on a folder in any state. If the folder is in the `ACTIVE` state the result will be a no-op success. In order to succeed, the folder's parent must be in the `ACTIVE` state. In addition, reintroducing the folder into the tree must not violate folder naming, height, and fanout constraints described in the CreateFolder documentation. The caller must have resourcemanager.folders.undelete permission on the identified folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_undelete_execute()` to send, or `cloudresourcemanager_folders_undelete` for simplest API.

pub fn cloudresourcemanager_folders_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteFolderRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}:undelete",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders/{foldersId}:undelete
/// Cancels the deletion request for a folder. This method may be called on a folder in any state. If the folder is in the `ACTIVE` state the result will be a no-op success. In order to succeed, the folder's parent must be in the `ACTIVE` state. In addition, reintroducing the folder into the tree must not violate folder naming, height, and fanout constraints described in the CreateFolder documentation. The caller must have resourcemanager.folders.undelete permission on the identified folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersUndeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UndeleteFolderRequest,
}

/// GET v3/folders/{foldersId}:undelete
/// Cancels the deletion request for a folder. This method may be called on a folder in any state. If the folder is in the `ACTIVE` state the result will be a no-op success. In order to succeed, the folder's parent must be in the `ACTIVE` state. In addition, reintroducing the folder into the tree must not violate folder naming, height, and fanout constraints described in the CreateFolder documentation. The caller must have resourcemanager.folders.undelete permission on the identified folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_undelete_builder()` + `cloudresourcemanager_folders_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_undelete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_undelete_builder(client, &args.name, &args.body)?;
    cloudresourcemanager_folders_undelete_execute(builder)
}

/// GET v3/folders/{foldersId}/capabilities/{capabilitiesId}
/// Retrieves the Capability identified by the supplied resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_capabilities_get_execute()` to send, or `cloudresourcemanager_folders_capabilities_get` for simplest API.

pub fn cloudresourcemanager_folders_capabilities_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}/capabilities/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/folders/{foldersId}/capabilities/{capabilitiesId}
/// Retrieves the Capability identified by the supplied resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_capabilities_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_capabilities_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_capabilities_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Capability>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Capability = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_capabilities_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersCapabilitiesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/folders/{foldersId}/capabilities/{capabilitiesId}
/// Retrieves the Capability identified by the supplied resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_capabilities_get_builder()` + `cloudresourcemanager_folders_capabilities_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_capabilities_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersCapabilitiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Capability>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_capabilities_get_builder(client, &args.name)?;
    cloudresourcemanager_folders_capabilities_get_execute(builder)
}

/// GET v3/folders/{foldersId}/capabilities/{capabilitiesId}
/// Updates the Capability.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_folders_capabilities_patch_execute()` to send, or `cloudresourcemanager_folders_capabilities_patch` for simplest API.

pub fn cloudresourcemanager_folders_capabilities_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Capability,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/folders/{}/capabilities/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/folders/{foldersId}/capabilities/{capabilitiesId}
/// Updates the Capability.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_folders_capabilities_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_folders_capabilities_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_capabilities_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_folders_capabilities_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerFoldersCapabilitiesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Capability,
}

/// GET v3/folders/{foldersId}/capabilities/{capabilitiesId}
/// Updates the Capability.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_folders_capabilities_patch_builder()` + `cloudresourcemanager_folders_capabilities_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_folders_capabilities_patch(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerFoldersCapabilitiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_folders_capabilities_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudresourcemanager_folders_capabilities_patch_execute(builder)
}

/// GET v3/liens
/// Create a Lien which applies to the resource denoted by the parent field. Callers of this method will require permission on the parent resource. For example, applying to `projects/1234` requires permission resourcemanager.projects.`updateLiens`. NOTE: Some resources may limit the number of Liens which may be applied.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_liens_create_execute()` to send, or `cloudresourcemanager_liens_create` for simplest API.

pub fn cloudresourcemanager_liens_create_builder(
    client: &SimpleHttpClient,
    body: &Lien,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/liens",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/liens
/// Create a Lien which applies to the resource denoted by the parent field. Callers of this method will require permission on the parent resource. For example, applying to `projects/1234` requires permission resourcemanager.projects.`updateLiens`. NOTE: Some resources may limit the number of Liens which may be applied.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_liens_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_liens_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lien>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Lien = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_liens_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerLiensCreateArgs {
    /// Request body.
    pub body: Lien,
}

/// GET v3/liens
/// Create a Lien which applies to the resource denoted by the parent field. Callers of this method will require permission on the parent resource. For example, applying to `projects/1234` requires permission resourcemanager.projects.`updateLiens`. NOTE: Some resources may limit the number of Liens which may be applied.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_liens_create_builder()` + `cloudresourcemanager_liens_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_create(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerLiensCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lien>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_liens_create_builder(client, &args.body)?;
    cloudresourcemanager_liens_create_execute(builder)
}

/// GET v3/liens/{liensId}
/// Delete a Lien by name. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.`updateLiens`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_liens_delete_execute()` to send, or `cloudresourcemanager_liens_delete` for simplest API.

pub fn cloudresourcemanager_liens_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/liens/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/liens/{liensId}
/// Delete a Lien by name. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.`updateLiens`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_liens_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_liens_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_liens_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerLiensDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/liens/{liensId}
/// Delete a Lien by name. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.`updateLiens`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_liens_delete_builder()` + `cloudresourcemanager_liens_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_delete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerLiensDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_liens_delete_builder(client, &args.name)?;
    cloudresourcemanager_liens_delete_execute(builder)
}

/// GET v3/liens/{liensId}
/// Retrieve a Lien by name. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.get
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_liens_get_execute()` to send, or `cloudresourcemanager_liens_get` for simplest API.

pub fn cloudresourcemanager_liens_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/liens/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/liens/{liensId}
/// Retrieve a Lien by name. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.get
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_liens_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_liens_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lien>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Lien = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_liens_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerLiensGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/liens/{liensId}
/// Retrieve a Lien by name. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.get
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_liens_get_builder()` + `cloudresourcemanager_liens_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerLiensGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lien>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_liens_get_builder(client, &args.name)?;
    cloudresourcemanager_liens_get_execute(builder)
}

/// GET v3/liens
/// List all Liens applied to the parent resource. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.get.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_liens_list_execute()` to send, or `cloudresourcemanager_liens_list` for simplest API.

pub fn cloudresourcemanager_liens_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/liens",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/liens
/// List all Liens applied to the parent resource. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.get.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_liens_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_liens_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLiensResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLiensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_liens_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerLiensListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v3/liens
/// List all Liens applied to the parent resource. Callers of this method will require permission on the parent resource. For example, a Lien with a parent of `projects/1234` requires permission resourcemanager.projects.get.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_liens_list_builder()` + `cloudresourcemanager_liens_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_liens_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerLiensListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLiensResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_liens_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    cloudresourcemanager_liens_list_execute(builder)
}

/// GET v3/locations/{locationsId}/effectiveTagBindingCollections/{effectiveTagBindingCollectionsId}
/// Returns effective tag bindings on a GCP resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_locations_effective_tag_binding_collections_get_execute()` to send, or `cloudresourcemanager_locations_effective_tag_binding_collections_get` for simplest API.

pub fn cloudresourcemanager_locations_effective_tag_binding_collections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/locations/{}/effectiveTagBindingCollections/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/locations/{locationsId}/effectiveTagBindingCollections/{effectiveTagBindingCollectionsId}
/// Returns effective tag bindings on a GCP resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_locations_effective_tag_binding_collections_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_locations_effective_tag_binding_collections_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_locations_effective_tag_binding_collections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveTagBindingCollection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EffectiveTagBindingCollection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_locations_effective_tag_binding_collections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerLocationsEffectiveTagBindingCollectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/locations/{locationsId}/effectiveTagBindingCollections/{effectiveTagBindingCollectionsId}
/// Returns effective tag bindings on a GCP resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_locations_effective_tag_binding_collections_get_builder()` + `cloudresourcemanager_locations_effective_tag_binding_collections_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_locations_effective_tag_binding_collections_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerLocationsEffectiveTagBindingCollectionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EffectiveTagBindingCollection>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_locations_effective_tag_binding_collections_get_builder(
        client, &args.name,
    )?;
    cloudresourcemanager_locations_effective_tag_binding_collections_get_execute(builder)
}

/// GET v3/locations/{locationsId}/tagBindingCollections/{tagBindingCollectionsId}
/// Returns tag bindings directly attached to a GCP resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_locations_tag_binding_collections_get_execute()` to send, or `cloudresourcemanager_locations_tag_binding_collections_get` for simplest API.

pub fn cloudresourcemanager_locations_tag_binding_collections_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/locations/{}/tagBindingCollections/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/locations/{locationsId}/tagBindingCollections/{tagBindingCollectionsId}
/// Returns tag bindings directly attached to a GCP resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_locations_tag_binding_collections_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_locations_tag_binding_collections_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_locations_tag_binding_collections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagBindingCollection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TagBindingCollection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_locations_tag_binding_collections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerLocationsTagBindingCollectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/locations/{locationsId}/tagBindingCollections/{tagBindingCollectionsId}
/// Returns tag bindings directly attached to a GCP resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_locations_tag_binding_collections_get_builder()` + `cloudresourcemanager_locations_tag_binding_collections_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_locations_tag_binding_collections_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerLocationsTagBindingCollectionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagBindingCollection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_locations_tag_binding_collections_get_builder(client, &args.name)?;
    cloudresourcemanager_locations_tag_binding_collections_get_execute(builder)
}

/// GET v3/locations/{locationsId}/tagBindingCollections/{tagBindingCollectionsId}
/// Updates tag bindings directly attached to a GCP resource. Update_mask can be kept empty or "*".
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_locations_tag_binding_collections_patch_execute()` to send, or `cloudresourcemanager_locations_tag_binding_collections_patch` for simplest API.

pub fn cloudresourcemanager_locations_tag_binding_collections_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &TagBindingCollection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/locations/{}/tagBindingCollections/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/locations/{locationsId}/tagBindingCollections/{tagBindingCollectionsId}
/// Updates tag bindings directly attached to a GCP resource. Update_mask can be kept empty or "*".
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_locations_tag_binding_collections_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_locations_tag_binding_collections_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_locations_tag_binding_collections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_locations_tag_binding_collections_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerLocationsTagBindingCollectionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: TagBindingCollection,
}

/// GET v3/locations/{locationsId}/tagBindingCollections/{tagBindingCollectionsId}
/// Updates tag bindings directly attached to a GCP resource. Update_mask can be kept empty or "*".
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_locations_tag_binding_collections_patch_builder()` + `cloudresourcemanager_locations_tag_binding_collections_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_locations_tag_binding_collections_patch(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerLocationsTagBindingCollectionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_locations_tag_binding_collections_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudresourcemanager_locations_tag_binding_collections_patch_execute(builder)
}

/// GET v3/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_operations_get_execute()` to send, or `cloudresourcemanager_operations_get` for simplest API.

pub fn cloudresourcemanager_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_operations_get_builder()` + `cloudresourcemanager_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_operations_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_operations_get_builder(client, &args.name)?;
    cloudresourcemanager_operations_get_execute(builder)
}

/// GET v3/organizations/{organizationsId}
/// Fetches an organization resource identified by the specified resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_organizations_get_execute()` to send, or `cloudresourcemanager_organizations_get` for simplest API.

pub fn cloudresourcemanager_organizations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/organizations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/organizations/{organizationsId}
/// Fetches an organization resource identified by the specified resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_organizations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_organizations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Organization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Organization = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_organizations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerOrganizationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/organizations/{organizationsId}
/// Fetches an organization resource identified by the specified resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_organizations_get_builder()` + `cloudresourcemanager_organizations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerOrganizationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Organization>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_organizations_get_builder(client, &args.name)?;
    cloudresourcemanager_organizations_get_execute(builder)
}

/// GET v3/organizations/{organizationsId}:getIamPolicy
/// Gets the access control policy for an organization resource. The policy may be empty if no such policy or resource exists. The resource field should be the organization's resource name, for example: "`organizations/123`". Authorization requires the IAM permission resourcemanager.organizations.`getIamPolicy` on the specified organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_organizations_get_iam_policy_execute()` to send, or `cloudresourcemanager_organizations_get_iam_policy` for simplest API.

pub fn cloudresourcemanager_organizations_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/organizations/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/organizations/{organizationsId}:getIamPolicy
/// Gets the access control policy for an organization resource. The policy may be empty if no such policy or resource exists. The resource field should be the organization's resource name, for example: "`organizations/123`". Authorization requires the IAM permission resourcemanager.organizations.`getIamPolicy` on the specified organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_organizations_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_organizations_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_organizations_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerOrganizationsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v3/organizations/{organizationsId}:getIamPolicy
/// Gets the access control policy for an organization resource. The policy may be empty if no such policy or resource exists. The resource field should be the organization's resource name, for example: "`organizations/123`". Authorization requires the IAM permission resourcemanager.organizations.`getIamPolicy` on the specified organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_organizations_get_iam_policy_builder()` + `cloudresourcemanager_organizations_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerOrganizationsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_organizations_get_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudresourcemanager_organizations_get_iam_policy_execute(builder)
}

/// GET v3/organizations:search
/// Searches organization resources that are visible to the user and satisfy the specified filter. This method returns organizations in an unspecified order. New organizations do not necessarily appear at the end of the results, and may take a small amount of time to appear. Search will only return organizations on which the user has the permission resourcemanager.organizations.get or has super admin privileges.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_organizations_search_execute()` to send, or `cloudresourcemanager_organizations_search` for simplest API.

pub fn cloudresourcemanager_organizations_search_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/organizations:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/organizations:search
/// Searches organization resources that are visible to the user and satisfy the specified filter. This method returns organizations in an unspecified order. New organizations do not necessarily appear at the end of the results, and may take a small amount of time to appear. Search will only return organizations on which the user has the permission resourcemanager.organizations.get or has super admin privileges.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_organizations_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_organizations_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchOrganizationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_organizations_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerOrganizationsSearchArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v3/organizations:search
/// Searches organization resources that are visible to the user and satisfy the specified filter. This method returns organizations in an unspecified order. New organizations do not necessarily appear at the end of the results, and may take a small amount of time to appear. Search will only return organizations on which the user has the permission resourcemanager.organizations.get or has super admin privileges.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_organizations_search_builder()` + `cloudresourcemanager_organizations_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_search(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerOrganizationsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_organizations_search_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
    )?;
    cloudresourcemanager_organizations_search_execute(builder)
}

/// GET v3/organizations/{organizationsId}:setIamPolicy
/// Sets the access control policy on an organization resource. Replaces any existing policy. The resource field should be the organization's resource name, for example: "`organizations/123`". Authorization requires the IAM permission resourcemanager.organizations.`setIamPolicy` on the specified organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_organizations_set_iam_policy_execute()` to send, or `cloudresourcemanager_organizations_set_iam_policy` for simplest API.

pub fn cloudresourcemanager_organizations_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/organizations/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/organizations/{organizationsId}:setIamPolicy
/// Sets the access control policy on an organization resource. Replaces any existing policy. The resource field should be the organization's resource name, for example: "`organizations/123`". Authorization requires the IAM permission resourcemanager.organizations.`setIamPolicy` on the specified organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_organizations_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_organizations_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_organizations_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerOrganizationsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v3/organizations/{organizationsId}:setIamPolicy
/// Sets the access control policy on an organization resource. Replaces any existing policy. The resource field should be the organization's resource name, for example: "`organizations/123`". Authorization requires the IAM permission resourcemanager.organizations.`setIamPolicy` on the specified organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_organizations_set_iam_policy_builder()` + `cloudresourcemanager_organizations_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerOrganizationsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_organizations_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudresourcemanager_organizations_set_iam_policy_execute(builder)
}

/// GET v3/organizations/{organizationsId}:testIamPermissions
/// Returns the permissions that a caller has on the specified organization. The resource field should be the organization's resource name, for example: "`organizations/123`". There are no permissions required for making this API call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_organizations_test_iam_permissions_execute()` to send, or `cloudresourcemanager_organizations_test_iam_permissions` for simplest API.

pub fn cloudresourcemanager_organizations_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/organizations/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/organizations/{organizationsId}:testIamPermissions
/// Returns the permissions that a caller has on the specified organization. The resource field should be the organization's resource name, for example: "`organizations/123`". There are no permissions required for making this API call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_organizations_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_organizations_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_organizations_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerOrganizationsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v3/organizations/{organizationsId}:testIamPermissions
/// Returns the permissions that a caller has on the specified organization. The resource field should be the organization's resource name, for example: "`organizations/123`". There are no permissions required for making this API call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_organizations_test_iam_permissions_builder()` + `cloudresourcemanager_organizations_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_organizations_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerOrganizationsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_organizations_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudresourcemanager_organizations_test_iam_permissions_execute(builder)
}

/// GET v3/projects
/// Request that a new project be created. The result is an Operation which can be used to track the creation process. This process usually takes a few seconds, but can sometimes take much longer. The tracking Operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_create_execute()` to send, or `cloudresourcemanager_projects_create` for simplest API.

pub fn cloudresourcemanager_projects_create_builder(
    client: &SimpleHttpClient,
    body: &Project,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/projects",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects
/// Request that a new project be created. The result is an Operation which can be used to track the creation process. This process usually takes a few seconds, but can sometimes take much longer. The tracking Operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsCreateArgs {
    /// Request body.
    pub body: Project,
}

/// GET v3/projects
/// Request that a new project be created. The result is an Operation which can be used to track the creation process. This process usually takes a few seconds, but can sometimes take much longer. The tracking Operation is automatically deleted after a few hours, so there is no need to call DeleteOperation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_create_builder()` + `cloudresourcemanager_projects_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_create(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_create_builder(client, &args.body)?;
    cloudresourcemanager_projects_create_execute(builder)
}

/// GET v3/projects/{projectsId}
/// Marks the project identified by the specified name (for example, `projects/415104041262`) for deletion. This method will only affect the project if it has a lifecycle state of `ACTIVE`. This method changes the Project's lifecycle state from `ACTIVE` to DELETE_REQUESTED. The deletion starts at an unspecified time, at which point the Project is no longer accessible. Until the deletion completes, you can check the lifecycle state checked by retrieving the project with GetProject, and the project remains visible to ListProjects. However, you cannot update the project. After the deletion completes, the project is not retrievable by the GetProject, ListProjects, and SearchProjects methods. The caller must have resourcemanager.projects.delete permissions for this project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_delete_execute()` to send, or `cloudresourcemanager_projects_delete` for simplest API.

pub fn cloudresourcemanager_projects_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}
/// Marks the project identified by the specified name (for example, `projects/415104041262`) for deletion. This method will only affect the project if it has a lifecycle state of `ACTIVE`. This method changes the Project's lifecycle state from `ACTIVE` to DELETE_REQUESTED. The deletion starts at an unspecified time, at which point the Project is no longer accessible. Until the deletion completes, you can check the lifecycle state checked by retrieving the project with GetProject, and the project remains visible to ListProjects. However, you cannot update the project. After the deletion completes, the project is not retrievable by the GetProject, ListProjects, and SearchProjects methods. The caller must have resourcemanager.projects.delete permissions for this project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}
/// Marks the project identified by the specified name (for example, `projects/415104041262`) for deletion. This method will only affect the project if it has a lifecycle state of `ACTIVE`. This method changes the Project's lifecycle state from `ACTIVE` to DELETE_REQUESTED. The deletion starts at an unspecified time, at which point the Project is no longer accessible. Until the deletion completes, you can check the lifecycle state checked by retrieving the project with GetProject, and the project remains visible to ListProjects. However, you cannot update the project. After the deletion completes, the project is not retrievable by the GetProject, ListProjects, and SearchProjects methods. The caller must have resourcemanager.projects.delete permissions for this project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_delete_builder()` + `cloudresourcemanager_projects_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_delete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_delete_builder(client, &args.name)?;
    cloudresourcemanager_projects_delete_execute(builder)
}

/// GET v3/projects/{projectsId}
/// Retrieves the project identified by the specified name (for example, `projects/415104041262`). The caller must have resourcemanager.projects.get permission for this project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_get_execute()` to send, or `cloudresourcemanager_projects_get` for simplest API.

pub fn cloudresourcemanager_projects_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}
/// Retrieves the project identified by the specified name (for example, `projects/415104041262`). The caller must have resourcemanager.projects.get permission for this project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}
/// Retrieves the project identified by the specified name (for example, `projects/415104041262`). The caller must have resourcemanager.projects.get permission for this project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_get_builder()` + `cloudresourcemanager_projects_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_get_builder(client, &args.name)?;
    cloudresourcemanager_projects_get_execute(builder)
}

/// GET v3/projects/{projectsId}:getIamPolicy
/// Returns the IAM access control policy for the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`. Permission is denied if the policy or the resource do not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_get_iam_policy_execute()` to send, or `cloudresourcemanager_projects_get_iam_policy` for simplest API.

pub fn cloudresourcemanager_projects_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:getIamPolicy
/// Returns the IAM access control policy for the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`. Permission is denied if the policy or the resource do not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v3/projects/{projectsId}:getIamPolicy
/// Returns the IAM access control policy for the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`. Permission is denied if the policy or the resource do not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_get_iam_policy_builder()` + `cloudresourcemanager_projects_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_projects_get_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_projects_get_iam_policy_execute(builder)
}

/// GET v3/projects
/// Lists projects that are direct children of the specified folder or organization resource. list() provides a strongly consistent view of the projects underneath the specified parent resource. list() returns projects sorted based upon the (ascending) lexical ordering of their display_name. The caller must have resourcemanager.projects.list permission on the identified parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_list_execute()` to send, or `cloudresourcemanager_projects_list` for simplest API.

pub fn cloudresourcemanager_projects_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/projects",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects
/// Lists projects that are direct children of the specified folder or organization resource. list() provides a strongly consistent view of the projects underneath the specified parent resource. list() returns projects sorted based upon the (ascending) lexical ordering of their display_name. The caller must have resourcemanager.projects.list permission on the identified parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProjectsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<bool>,
}

/// GET v3/projects
/// Lists projects that are direct children of the specified folder or organization resource. list() provides a strongly consistent view of the projects underneath the specified parent resource. list() returns projects sorted based upon the (ascending) lexical ordering of their display_name. The caller must have resourcemanager.projects.list permission on the identified parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_list_builder()` + `cloudresourcemanager_projects_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProjectsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
        args.showDeleted,
    )?;
    cloudresourcemanager_projects_list_execute(builder)
}

/// GET v3/projects/{projectsId}:move
/// Move a project to another place in your resource hierarchy, under a new resource parent. Returns an operation which can be used to track the process of the project move workflow. Upon success, the Operation.response field will be populated with the moved project. The caller must have resourcemanager.projects.move permission on the project, on the project's current and proposed new parent. If project has no current parent, or it currently does not have an associated organization resource, you will also need the resourcemanager.projects.`setIamPolicy` permission in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_move_execute()` to send, or `cloudresourcemanager_projects_move` for simplest API.

pub fn cloudresourcemanager_projects_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MoveProjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:move
/// Move a project to another place in your resource hierarchy, under a new resource parent. Returns an operation which can be used to track the process of the project move workflow. Upon success, the Operation.response field will be populated with the moved project. The caller must have resourcemanager.projects.move permission on the project, on the project's current and proposed new parent. If project has no current parent, or it currently does not have an associated organization resource, you will also need the resourcemanager.projects.`setIamPolicy` permission in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: MoveProjectRequest,
}

/// GET v3/projects/{projectsId}:move
/// Move a project to another place in your resource hierarchy, under a new resource parent. Returns an operation which can be used to track the process of the project move workflow. Upon success, the Operation.response field will be populated with the moved project. The caller must have resourcemanager.projects.move permission on the project, on the project's current and proposed new parent. If project has no current parent, or it currently does not have an associated organization resource, you will also need the resourcemanager.projects.`setIamPolicy` permission in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_move_builder()` + `cloudresourcemanager_projects_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_move(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_move_builder(client, &args.name, &args.body)?;
    cloudresourcemanager_projects_move_execute(builder)
}

/// GET v3/projects/{projectsId}
/// Updates the display_name and labels of the project identified by the specified name (for example, `projects/415104041262`). Deleting all labels requires an update mask for labels field. The caller must have resourcemanager.projects.update permission for this project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_patch_execute()` to send, or `cloudresourcemanager_projects_patch` for simplest API.

pub fn cloudresourcemanager_projects_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Project,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}
/// Updates the display_name and labels of the project identified by the specified name (for example, `projects/415104041262`). Deleting all labels requires an update mask for labels field. The caller must have resourcemanager.projects.update permission for this project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Project,
}

/// GET v3/projects/{projectsId}
/// Updates the display_name and labels of the project identified by the specified name (for example, `projects/415104041262`). Deleting all labels requires an update mask for labels field. The caller must have resourcemanager.projects.update permission for this project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_patch_builder()` + `cloudresourcemanager_projects_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_patch(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudresourcemanager_projects_patch_execute(builder)
}

/// GET v3/projects:search
/// Search for projects that the caller has the resourcemanager.projects.get permission on, and also satisfy the specified query. This method returns projects in an unspecified order. This method is eventually consistent with project mutations; this means that a newly created project may not appear in the results or recent updates to an existing project may not be reflected in the results. To retrieve the latest state of a project, use the GetProject method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_search_execute()` to send, or `cloudresourcemanager_projects_search` for simplest API.

pub fn cloudresourcemanager_projects_search_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/projects:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects:search
/// Search for projects that the caller has the resourcemanager.projects.get permission on, and also satisfy the specified query. This method returns projects in an unspecified order. This method is eventually consistent with project mutations; this means that a newly created project may not appear in the results or recent updates to an existing project may not be reflected in the results. To retrieve the latest state of a project, use the GetProject method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchProjectsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchProjectsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsSearchArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v3/projects:search
/// Search for projects that the caller has the resourcemanager.projects.get permission on, and also satisfy the specified query. This method returns projects in an unspecified order. This method is eventually consistent with project mutations; this means that a newly created project may not appear in the results or recent updates to an existing project may not be reflected in the results. To retrieve the latest state of a project, use the GetProject method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_search_builder()` + `cloudresourcemanager_projects_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_search(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchProjectsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_search_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
    )?;
    cloudresourcemanager_projects_search_execute(builder)
}

/// GET v3/projects/{projectsId}:setIamPolicy
/// Sets the IAM access control policy for the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`. CAUTION: This method will replace the existing policy, and cannot be used to append additional IAM settings. Note: Removing service accounts from policies or changing their roles can render services completely inoperable. It is important to understand how the service account is being used before removing or updating its roles. The following constraints apply when using `setIamPolicy`(): + Project does not support `allUsers` and `allAuthenticatedUsers` as members in a Binding of a Policy. + The owner role can be granted to a user, `serviceAccount`, or a group that is part of an organization. For example, group@myownpersonaldomain.com could be added as an owner to a project in the myownpersonaldomain.com organization, but not the examplepetstore.com organization. + Service accounts can be made owners of a project directly without any restrictions. However, to be added as an owner, a user must be invited using the Cloud Platform console and must accept the invitation. + A user cannot be granted the owner role using `setIamPolicy`(). The user must be granted the owner role using the Cloud Platform Console and must explicitly accept the invitation. + Invitations to grant the owner role cannot be sent using `setIamPolicy`(); they must be sent only using the Cloud Platform Console. + If the project is not part of an organization, there must be at least one owner who has accepted the Terms of Service (ToS) agreement in the policy. Calling `setIamPolicy`() to remove the last ToS-accepted owner from the policy will fail. This restriction also applies to legacy projects that no longer have owners who have accepted the ToS. Edits to IAM policies will be rejected until the lack of a ToS-accepting owner is rectified. If the project is part of an organization, you can remove all owners, potentially making the organization inaccessible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_set_iam_policy_execute()` to send, or `cloudresourcemanager_projects_set_iam_policy` for simplest API.

pub fn cloudresourcemanager_projects_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:setIamPolicy
/// Sets the IAM access control policy for the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`. CAUTION: This method will replace the existing policy, and cannot be used to append additional IAM settings. Note: Removing service accounts from policies or changing their roles can render services completely inoperable. It is important to understand how the service account is being used before removing or updating its roles. The following constraints apply when using `setIamPolicy`(): + Project does not support `allUsers` and `allAuthenticatedUsers` as members in a Binding of a Policy. + The owner role can be granted to a user, `serviceAccount`, or a group that is part of an organization. For example, group@myownpersonaldomain.com could be added as an owner to a project in the myownpersonaldomain.com organization, but not the examplepetstore.com organization. + Service accounts can be made owners of a project directly without any restrictions. However, to be added as an owner, a user must be invited using the Cloud Platform console and must accept the invitation. + A user cannot be granted the owner role using `setIamPolicy`(). The user must be granted the owner role using the Cloud Platform Console and must explicitly accept the invitation. + Invitations to grant the owner role cannot be sent using `setIamPolicy`(); they must be sent only using the Cloud Platform Console. + If the project is not part of an organization, there must be at least one owner who has accepted the Terms of Service (ToS) agreement in the policy. Calling `setIamPolicy`() to remove the last ToS-accepted owner from the policy will fail. This restriction also applies to legacy projects that no longer have owners who have accepted the ToS. Edits to IAM policies will be rejected until the lack of a ToS-accepting owner is rectified. If the project is part of an organization, you can remove all owners, potentially making the organization inaccessible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v3/projects/{projectsId}:setIamPolicy
/// Sets the IAM access control policy for the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`. CAUTION: This method will replace the existing policy, and cannot be used to append additional IAM settings. Note: Removing service accounts from policies or changing their roles can render services completely inoperable. It is important to understand how the service account is being used before removing or updating its roles. The following constraints apply when using `setIamPolicy`(): + Project does not support `allUsers` and `allAuthenticatedUsers` as members in a Binding of a Policy. + The owner role can be granted to a user, `serviceAccount`, or a group that is part of an organization. For example, group@myownpersonaldomain.com could be added as an owner to a project in the myownpersonaldomain.com organization, but not the examplepetstore.com organization. + Service accounts can be made owners of a project directly without any restrictions. However, to be added as an owner, a user must be invited using the Cloud Platform console and must accept the invitation. + A user cannot be granted the owner role using `setIamPolicy`(). The user must be granted the owner role using the Cloud Platform Console and must explicitly accept the invitation. + Invitations to grant the owner role cannot be sent using `setIamPolicy`(); they must be sent only using the Cloud Platform Console. + If the project is not part of an organization, there must be at least one owner who has accepted the Terms of Service (ToS) agreement in the policy. Calling `setIamPolicy`() to remove the last ToS-accepted owner from the policy will fail. This restriction also applies to legacy projects that no longer have owners who have accepted the ToS. Edits to IAM policies will be rejected until the lack of a ToS-accepting owner is rectified. If the project is part of an organization, you can remove all owners, potentially making the organization inaccessible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_set_iam_policy_builder()` + `cloudresourcemanager_projects_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_projects_set_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_projects_set_iam_policy_execute(builder)
}

/// GET v3/projects/{projectsId}:testIamPermissions
/// Returns permissions that a caller has on the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`..
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_test_iam_permissions_execute()` to send, or `cloudresourcemanager_projects_test_iam_permissions` for simplest API.

pub fn cloudresourcemanager_projects_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:testIamPermissions
/// Returns permissions that a caller has on the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`..
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v3/projects/{projectsId}:testIamPermissions
/// Returns permissions that a caller has on the specified project, in the format `projects/{ProjectIdOrNumber}` e.g. `projects/123`..
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_test_iam_permissions_builder()` + `cloudresourcemanager_projects_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudresourcemanager_projects_test_iam_permissions_execute(builder)
}

/// GET v3/projects/{projectsId}:undelete
/// Restores the project identified by the specified name (for example, `projects/415104041262`). You can only use this method for a project that has a lifecycle state of DELETE_REQUESTED. After deletion starts, the project cannot be restored. The caller must have resourcemanager.projects.undelete permission for this project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_projects_undelete_execute()` to send, or `cloudresourcemanager_projects_undelete` for simplest API.

pub fn cloudresourcemanager_projects_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteProjectRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/projects/{}:undelete",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:undelete
/// Restores the project identified by the specified name (for example, `projects/415104041262`). You can only use this method for a project that has a lifecycle state of DELETE_REQUESTED. After deletion starts, the project cannot be restored. The caller must have resourcemanager.projects.undelete permission for this project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_projects_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_projects_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_projects_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerProjectsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UndeleteProjectRequest,
}

/// GET v3/projects/{projectsId}:undelete
/// Restores the project identified by the specified name (for example, `projects/415104041262`). You can only use this method for a project that has a lifecycle state of DELETE_REQUESTED. After deletion starts, the project cannot be restored. The caller must have resourcemanager.projects.undelete permission for this project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_projects_undelete_builder()` + `cloudresourcemanager_projects_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_projects_undelete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerProjectsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_projects_undelete_builder(client, &args.name, &args.body)?;
    cloudresourcemanager_projects_undelete_execute(builder)
}

/// GET v3/tagBindings
/// Creates a TagBinding between a TagValue and a Google Cloud resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_bindings_create_execute()` to send, or `cloudresourcemanager_tag_bindings_create` for simplest API.

pub fn cloudresourcemanager_tag_bindings_create_builder(
    client: &SimpleHttpClient,
    validateOnly: Option<bool>,
    body: &TagBinding,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagBindings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagBindings
/// Creates a TagBinding between a TagValue and a Google Cloud resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_bindings_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_bindings_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_bindings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_bindings_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagBindingsCreateArgs {
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: TagBinding,
}

/// GET v3/tagBindings
/// Creates a TagBinding between a TagValue and a Google Cloud resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_bindings_create_builder()` + `cloudresourcemanager_tag_bindings_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_bindings_create(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagBindingsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_bindings_create_builder(client, args.validateOnly, &args.body)?;
    cloudresourcemanager_tag_bindings_create_execute(builder)
}

/// GET v3/tagBindings/{tagBindingsId}
/// Deletes a TagBinding.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_bindings_delete_execute()` to send, or `cloudresourcemanager_tag_bindings_delete` for simplest API.

pub fn cloudresourcemanager_tag_bindings_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagBindings/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagBindings/{tagBindingsId}
/// Deletes a TagBinding.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_bindings_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_bindings_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_bindings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_bindings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagBindingsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/tagBindings/{tagBindingsId}
/// Deletes a TagBinding.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_bindings_delete_builder()` + `cloudresourcemanager_tag_bindings_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_bindings_delete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagBindingsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_bindings_delete_builder(client, &args.name)?;
    cloudresourcemanager_tag_bindings_delete_execute(builder)
}

/// GET v3/tagBindings
/// Lists the TagBindings for the given Google Cloud resource, as specified with parent. NOTE: The parent field is expected to be a full resource name: <https://cloud.google.`com/apis/design/resource_names`#full_resource_name>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_bindings_list_execute()` to send, or `cloudresourcemanager_tag_bindings_list` for simplest API.

pub fn cloudresourcemanager_tag_bindings_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagBindings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagBindings
/// Lists the TagBindings for the given Google Cloud resource, as specified with parent. NOTE: The parent field is expected to be a full resource name: <https://cloud.google.`com/apis/design/resource_names`#full_resource_name>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_bindings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_bindings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_bindings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagBindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTagBindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_bindings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagBindingsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v3/tagBindings
/// Lists the TagBindings for the given Google Cloud resource, as specified with parent. NOTE: The parent field is expected to be a full resource name: <https://cloud.google.`com/apis/design/resource_names`#full_resource_name>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_bindings_list_builder()` + `cloudresourcemanager_tag_bindings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_bindings_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagBindingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagBindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_bindings_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    cloudresourcemanager_tag_bindings_list_execute(builder)
}

/// GET v3/tagKeys
/// Creates a new TagKey. If another request with the same parameters is sent while the original request is in process, the second request will receive an error. A maximum of 1000 TagKeys can exist under a parent at any given time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_create_execute()` to send, or `cloudresourcemanager_tag_keys_create` for simplest API.

pub fn cloudresourcemanager_tag_keys_create_builder(
    client: &SimpleHttpClient,
    validateOnly: Option<bool>,
    body: &TagKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagKeys",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagKeys
/// Creates a new TagKey. If another request with the same parameters is sent while the original request is in process, the second request will receive an error. A maximum of 1000 TagKeys can exist under a parent at any given time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysCreateArgs {
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: TagKey,
}

/// GET v3/tagKeys
/// Creates a new TagKey. If another request with the same parameters is sent while the original request is in process, the second request will receive an error. A maximum of 1000 TagKeys can exist under a parent at any given time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_create_builder()` + `cloudresourcemanager_tag_keys_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_create(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_keys_create_builder(client, args.validateOnly, &args.body)?;
    cloudresourcemanager_tag_keys_create_execute(builder)
}

/// GET v3/tagKeys/{tagKeysId}
/// Deletes a TagKey. The TagKey cannot be deleted if it has any child TagValues.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_delete_execute()` to send, or `cloudresourcemanager_tag_keys_delete` for simplest API.

pub fn cloudresourcemanager_tag_keys_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagKeys/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagKeys/{tagKeysId}
/// Deletes a TagKey. The TagKey cannot be deleted if it has any child TagValues.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v3/tagKeys/{tagKeysId}
/// Deletes a TagKey. The TagKey cannot be deleted if it has any child TagValues.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_delete_builder()` + `cloudresourcemanager_tag_keys_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_delete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_keys_delete_builder(
        client,
        &args.name,
        args.etag.as_deref(),
        args.validateOnly,
    )?;
    cloudresourcemanager_tag_keys_delete_execute(builder)
}

/// GET v3/tagKeys/{tagKeysId}
/// Retrieves a TagKey. This method will return PERMISSION_DENIED if the key does not exist or the user does not have permission to view it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_get_execute()` to send, or `cloudresourcemanager_tag_keys_get` for simplest API.

pub fn cloudresourcemanager_tag_keys_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagKeys/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagKeys/{tagKeysId}
/// Retrieves a TagKey. This method will return PERMISSION_DENIED if the key does not exist or the user does not have permission to view it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TagKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/tagKeys/{tagKeysId}
/// Retrieves a TagKey. This method will return PERMISSION_DENIED if the key does not exist or the user does not have permission to view it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_get_builder()` + `cloudresourcemanager_tag_keys_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_keys_get_builder(client, &args.name)?;
    cloudresourcemanager_tag_keys_get_execute(builder)
}

/// GET v3/tagKeys/{tagKeysId}:getIamPolicy
/// Gets the access control policy for a TagKey. The returned policy may be empty if no such policy or resource exists. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". The caller must have cloudresourcemanager.googleapis.`com/`tagKeys``.`getIamPolicy` permission on the specified TagKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_get_iam_policy_execute()` to send, or `cloudresourcemanager_tag_keys_get_iam_policy` for simplest API.

pub fn cloudresourcemanager_tag_keys_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagKeys/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagKeys/{tagKeysId}:getIamPolicy
/// Gets the access control policy for a TagKey. The returned policy may be empty if no such policy or resource exists. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". The caller must have cloudresourcemanager.googleapis.`com/`tagKeys``.`getIamPolicy` permission on the specified TagKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v3/tagKeys/{tagKeysId}:getIamPolicy
/// Gets the access control policy for a TagKey. The returned policy may be empty if no such policy or resource exists. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". The caller must have cloudresourcemanager.googleapis.`com/`tagKeys``.`getIamPolicy` permission on the specified TagKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_get_iam_policy_builder()` + `cloudresourcemanager_tag_keys_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_keys_get_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_tag_keys_get_iam_policy_execute(builder)
}

/// GET v3/tagKeys/namespaced
/// Retrieves a TagKey by its namespaced name. This method will return PERMISSION_DENIED if the key does not exist or the user does not have permission to view it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_get_namespaced_execute()` to send, or `cloudresourcemanager_tag_keys_get_namespaced` for simplest API.

pub fn cloudresourcemanager_tag_keys_get_namespaced_builder(
    client: &SimpleHttpClient,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagKeys/namespaced",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagKeys/namespaced
/// Retrieves a TagKey by its namespaced name. This method will return PERMISSION_DENIED if the key does not exist or the user does not have permission to view it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_get_namespaced_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_get_namespaced_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_get_namespaced_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TagKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_get_namespaced`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysGetNamespacedArgs {
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET v3/tagKeys/namespaced
/// Retrieves a TagKey by its namespaced name. This method will return PERMISSION_DENIED if the key does not exist or the user does not have permission to view it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_get_namespaced_builder()` + `cloudresourcemanager_tag_keys_get_namespaced_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_get_namespaced(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysGetNamespacedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_keys_get_namespaced_builder(client, args.name.as_deref())?;
    cloudresourcemanager_tag_keys_get_namespaced_execute(builder)
}

/// GET v3/tagKeys
/// Lists all TagKeys for a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_list_execute()` to send, or `cloudresourcemanager_tag_keys_list` for simplest API.

pub fn cloudresourcemanager_tag_keys_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagKeys",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagKeys
/// Lists all TagKeys for a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTagKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v3/tagKeys
/// Lists all TagKeys for a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_list_builder()` + `cloudresourcemanager_tag_keys_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_keys_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    cloudresourcemanager_tag_keys_list_execute(builder)
}

/// GET v3/tagKeys/{tagKeysId}
/// Updates the attributes of the TagKey resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_patch_execute()` to send, or `cloudresourcemanager_tag_keys_patch` for simplest API.

pub fn cloudresourcemanager_tag_keys_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &TagKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagKeys/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagKeys/{tagKeysId}
/// Updates the attributes of the TagKey resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: TagKey,
}

/// GET v3/tagKeys/{tagKeysId}
/// Updates the attributes of the TagKey resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_patch_builder()` + `cloudresourcemanager_tag_keys_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_patch(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_keys_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    cloudresourcemanager_tag_keys_patch_execute(builder)
}

/// GET v3/tagKeys/{tagKeysId}:setIamPolicy
/// Sets the access control policy on a TagKey, replacing any existing policy. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". The caller must have resourcemanager.`tagKeys`.`setIamPolicy` permission on the identified `tagValue`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_set_iam_policy_execute()` to send, or `cloudresourcemanager_tag_keys_set_iam_policy` for simplest API.

pub fn cloudresourcemanager_tag_keys_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagKeys/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagKeys/{tagKeysId}:setIamPolicy
/// Sets the access control policy on a TagKey, replacing any existing policy. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". The caller must have resourcemanager.`tagKeys`.`setIamPolicy` permission on the identified `tagValue`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v3/tagKeys/{tagKeysId}:setIamPolicy
/// Sets the access control policy on a TagKey, replacing any existing policy. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". The caller must have resourcemanager.`tagKeys`.`setIamPolicy` permission on the identified `tagValue`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_set_iam_policy_builder()` + `cloudresourcemanager_tag_keys_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_keys_set_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_tag_keys_set_iam_policy_execute(builder)
}

/// GET v3/tagKeys/{tagKeysId}:testIamPermissions
/// Returns permissions that a caller has on the specified TagKey. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". There are no permissions required for making this API call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_keys_test_iam_permissions_execute()` to send, or `cloudresourcemanager_tag_keys_test_iam_permissions` for simplest API.

pub fn cloudresourcemanager_tag_keys_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagKeys/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagKeys/{tagKeysId}:testIamPermissions
/// Returns permissions that a caller has on the specified TagKey. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". There are no permissions required for making this API call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_keys_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_keys_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_keys_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagKeysTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v3/tagKeys/{tagKeysId}:testIamPermissions
/// Returns permissions that a caller has on the specified TagKey. The resource field should be the TagKey's resource name. For example, "tagK`eys/1234`". There are no permissions required for making this API call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_keys_test_iam_permissions_builder()` + `cloudresourcemanager_tag_keys_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_keys_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagKeysTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_keys_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudresourcemanager_tag_keys_test_iam_permissions_execute(builder)
}

/// GET v3/tagValues
/// Creates a TagValue as a child of the specified TagKey. If a another request with the same parameters is sent while the original request is in process the second request will receive an error. A maximum of 1000 TagValues can exist under a TagKey at any given time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_create_execute()` to send, or `cloudresourcemanager_tag_values_create` for simplest API.

pub fn cloudresourcemanager_tag_values_create_builder(
    client: &SimpleHttpClient,
    validateOnly: Option<bool>,
    body: &TagValue,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagValues",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagValues
/// Creates a TagValue as a child of the specified TagKey. If a another request with the same parameters is sent while the original request is in process the second request will receive an error. A maximum of 1000 TagValues can exist under a TagKey at any given time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesCreateArgs {
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: TagValue,
}

/// GET v3/tagValues
/// Creates a TagValue as a child of the specified TagKey. If a another request with the same parameters is sent while the original request is in process the second request will receive an error. A maximum of 1000 TagValues can exist under a TagKey at any given time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_create_builder()` + `cloudresourcemanager_tag_values_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_create(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_values_create_builder(client, args.validateOnly, &args.body)?;
    cloudresourcemanager_tag_values_create_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}
/// Deletes a TagValue. The TagValue cannot have any bindings when it is deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_delete_execute()` to send, or `cloudresourcemanager_tag_values_delete` for simplest API.

pub fn cloudresourcemanager_tag_values_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagValues/{tagValuesId}
/// Deletes a TagValue. The TagValue cannot have any bindings when it is deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v3/tagValues/{tagValuesId}
/// Deletes a TagValue. The TagValue cannot have any bindings when it is deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_delete_builder()` + `cloudresourcemanager_tag_values_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_delete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_delete_builder(
        client,
        &args.name,
        args.etag.as_deref(),
        args.validateOnly,
    )?;
    cloudresourcemanager_tag_values_delete_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}
/// Retrieves a TagValue. This method will return PERMISSION_DENIED if the value does not exist or the user does not have permission to view it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_get_execute()` to send, or `cloudresourcemanager_tag_values_get` for simplest API.

pub fn cloudresourcemanager_tag_values_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagValues/{tagValuesId}
/// Retrieves a TagValue. This method will return PERMISSION_DENIED if the value does not exist or the user does not have permission to view it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagValue>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TagValue = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/tagValues/{tagValuesId}
/// Retrieves a TagValue. This method will return PERMISSION_DENIED if the value does not exist or the user does not have permission to view it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_get_builder()` + `cloudresourcemanager_tag_values_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_get(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagValue>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_get_builder(client, &args.name)?;
    cloudresourcemanager_tag_values_get_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}:getIamPolicy
/// Gets the access control policy for a TagValue. The returned policy may be empty if no such policy or resource exists. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. The caller must have the cloudresourcemanager.googleapis.`com/`tagValues``.`getIamPolicy` permission on the identified TagValue to get the access control policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_get_iam_policy_execute()` to send, or `cloudresourcemanager_tag_values_get_iam_policy` for simplest API.

pub fn cloudresourcemanager_tag_values_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagValues/{tagValuesId}:getIamPolicy
/// Gets the access control policy for a TagValue. The returned policy may be empty if no such policy or resource exists. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. The caller must have the cloudresourcemanager.googleapis.`com/`tagValues``.`getIamPolicy` permission on the identified TagValue to get the access control policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v3/tagValues/{tagValuesId}:getIamPolicy
/// Gets the access control policy for a TagValue. The returned policy may be empty if no such policy or resource exists. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. The caller must have the cloudresourcemanager.googleapis.`com/`tagValues``.`getIamPolicy` permission on the identified TagValue to get the access control policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_get_iam_policy_builder()` + `cloudresourcemanager_tag_values_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_get_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_values_get_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_tag_values_get_iam_policy_execute(builder)
}

/// GET v3/tagValues/namespaced
/// Retrieves a TagValue by its namespaced name. This method will return PERMISSION_DENIED if the value does not exist or the user does not have permission to view it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_get_namespaced_execute()` to send, or `cloudresourcemanager_tag_values_get_namespaced` for simplest API.

pub fn cloudresourcemanager_tag_values_get_namespaced_builder(
    client: &SimpleHttpClient,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagValues/namespaced",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagValues/namespaced
/// Retrieves a TagValue by its namespaced name. This method will return PERMISSION_DENIED if the value does not exist or the user does not have permission to view it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_get_namespaced_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_get_namespaced_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_get_namespaced_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagValue>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TagValue = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_get_namespaced`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesGetNamespacedArgs {
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET v3/tagValues/namespaced
/// Retrieves a TagValue by its namespaced name. This method will return PERMISSION_DENIED if the value does not exist or the user does not have permission to view it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_get_namespaced_builder()` + `cloudresourcemanager_tag_values_get_namespaced_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_get_namespaced(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesGetNamespacedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TagValue>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_values_get_namespaced_builder(client, args.name.as_deref())?;
    cloudresourcemanager_tag_values_get_namespaced_execute(builder)
}

/// GET v3/tagValues
/// Lists all TagValues for a specific TagKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_list_execute()` to send, or `cloudresourcemanager_tag_values_list` for simplest API.

pub fn cloudresourcemanager_tag_values_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudresourcemanager.googleapis.com/v3/tagValues",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagValues
/// Lists all TagValues for a specific TagKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTagValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v3/tagValues
/// Lists all TagValues for a specific TagKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_list_builder()` + `cloudresourcemanager_tag_values_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_list_builder(
        client,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    cloudresourcemanager_tag_values_list_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}
/// Updates the attributes of the TagValue resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_patch_execute()` to send, or `cloudresourcemanager_tag_values_patch` for simplest API.

pub fn cloudresourcemanager_tag_values_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &TagValue,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagValues/{tagValuesId}
/// Updates the attributes of the TagValue resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: TagValue,
}

/// GET v3/tagValues/{tagValuesId}
/// Updates the attributes of the TagValue resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_patch_builder()` + `cloudresourcemanager_tag_values_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_patch(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    cloudresourcemanager_tag_values_patch_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}:setIamPolicy
/// Sets the access control policy on a TagValue, replacing any existing policy. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. The caller must have resourcemanager.`tagValues`.`setIamPolicy` permission on the identified `tagValue`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_set_iam_policy_execute()` to send, or `cloudresourcemanager_tag_values_set_iam_policy` for simplest API.

pub fn cloudresourcemanager_tag_values_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagValues/{tagValuesId}:setIamPolicy
/// Sets the access control policy on a TagValue, replacing any existing policy. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. The caller must have resourcemanager.`tagValues`.`setIamPolicy` permission on the identified `tagValue`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v3/tagValues/{tagValuesId}:setIamPolicy
/// Sets the access control policy on a TagValue, replacing any existing policy. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. The caller must have resourcemanager.`tagValues`.`setIamPolicy` permission on the identified `tagValue`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_set_iam_policy_builder()` + `cloudresourcemanager_tag_values_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_set_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudresourcemanager_tag_values_set_iam_policy_builder(client, &args.resource, &args.body)?;
    cloudresourcemanager_tag_values_set_iam_policy_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}:testIamPermissions
/// Returns permissions that a caller has on the specified TagValue. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. There are no permissions required for making this API call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_test_iam_permissions_execute()` to send, or `cloudresourcemanager_tag_values_test_iam_permissions` for simplest API.

pub fn cloudresourcemanager_tag_values_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagValues/{tagValuesId}:testIamPermissions
/// Returns permissions that a caller has on the specified TagValue. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. There are no permissions required for making this API call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v3/tagValues/{tagValuesId}:testIamPermissions
/// Returns permissions that a caller has on the specified TagValue. The resource field should be the TagValue's resource name. For example: tagV`alues/1234`. There are no permissions required for making this API call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_test_iam_permissions_builder()` + `cloudresourcemanager_tag_values_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    cloudresourcemanager_tag_values_test_iam_permissions_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}/tagHolds
/// Creates a TagHold. Returns ALREADY_EXISTS if a TagHold with the same resource and origin exists under the same TagValue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_tag_holds_create_execute()` to send, or `cloudresourcemanager_tag_values_tag_holds_create` for simplest API.

pub fn cloudresourcemanager_tag_values_tag_holds_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    validateOnly: Option<bool>,
    body: &TagHold,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}/tagHolds",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/tagValues/{tagValuesId}/tagHolds
/// Creates a TagHold. Returns ALREADY_EXISTS if a TagHold with the same resource and origin exists under the same TagValue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_tag_holds_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_tag_holds_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_tag_holds_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_tag_holds_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesTagHoldsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: TagHold,
}

/// GET v3/tagValues/{tagValuesId}/tagHolds
/// Creates a TagHold. Returns ALREADY_EXISTS if a TagHold with the same resource and origin exists under the same TagValue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_tag_holds_create_builder()` + `cloudresourcemanager_tag_values_tag_holds_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_tag_holds_create(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesTagHoldsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_tag_holds_create_builder(
        client,
        &args.parent,
        args.validateOnly,
        &args.body,
    )?;
    cloudresourcemanager_tag_values_tag_holds_create_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}/tagHolds/{tagHoldsId}
/// Deletes a TagHold.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_tag_holds_delete_execute()` to send, or `cloudresourcemanager_tag_values_tag_holds_delete` for simplest API.

pub fn cloudresourcemanager_tag_values_tag_holds_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}/tagHolds/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagValues/{tagValuesId}/tagHolds/{tagHoldsId}
/// Deletes a TagHold.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_tag_holds_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_tag_holds_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_tag_holds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_tag_holds_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesTagHoldsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
}

/// GET v3/tagValues/{tagValuesId}/tagHolds/{tagHoldsId}
/// Deletes a TagHold.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_tag_holds_delete_builder()` + `cloudresourcemanager_tag_values_tag_holds_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_tag_holds_delete(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesTagHoldsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_tag_holds_delete_builder(
        client,
        &args.name,
        args.validateOnly,
    )?;
    cloudresourcemanager_tag_values_tag_holds_delete_execute(builder)
}

/// GET v3/tagValues/{tagValuesId}/tagHolds
/// Lists TagHolds under a TagValue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudresourcemanager_tag_values_tag_holds_list_execute()` to send, or `cloudresourcemanager_tag_values_tag_holds_list` for simplest API.

pub fn cloudresourcemanager_tag_values_tag_holds_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudresourcemanager.googleapis.com/v3/tagValues/{}/tagHolds",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/tagValues/{tagValuesId}/tagHolds
/// Lists TagHolds under a TagValue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudresourcemanager_tag_values_tag_holds_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudresourcemanager_tag_values_tag_holds_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_tag_holds_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagHoldsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTagHoldsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudresourcemanager_tag_values_tag_holds_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudresourcemanagerTagValuesTagHoldsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/tagValues/{tagValuesId}/tagHolds
/// Lists TagHolds under a TagValue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudresourcemanager_tag_values_tag_holds_list_builder()` + `cloudresourcemanager_tag_values_tag_holds_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudresourcemanager_tag_values_tag_holds_list(
    client: &SimpleHttpClient,
    args: &CloudresourcemanagerTagValuesTagHoldsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTagHoldsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudresourcemanager_tag_values_tag_holds_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudresourcemanager_tag_values_tag_holds_list_execute(builder)
}
