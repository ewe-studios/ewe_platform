//! Auto-generated API clients for gcp/libraryagent.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/shelves/{shelvesId}
/// Gets a shelf. Returns NOT_FOUND if the shelf does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `libraryagent_shelves_get_execute()` to send, or `libraryagent_shelves_get` for simplest API.

pub fn libraryagent_shelves_get_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://libraryagent.googleapis.com/v1/shelves/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/shelves/{shelvesId}
/// Gets a shelf. Returns NOT_FOUND if the shelf does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `libraryagent_shelves_get_execute()` or `libraryagent_shelves_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `libraryagent_shelves_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn libraryagent_shelves_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleExampleLibraryagentV1Shelf>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleExampleLibraryagentV1Shelf = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/shelves/{shelvesId}
/// Gets a shelf. Returns NOT_FOUND if the shelf does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `libraryagent_shelves_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `libraryagent_shelves_get_task()`.
/// For the simplest API, use `libraryagent_shelves_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `libraryagent_shelves_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn libraryagent_shelves_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleExampleLibraryagentV1Shelf>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = libraryagent_shelves_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`libraryagent_shelves_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LibraryagentShelvesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/shelves/{shelvesId}
/// Gets a shelf. Returns NOT_FOUND if the shelf does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `libraryagent_shelves_get_builder()` + `libraryagent_shelves_get_execute()`.
/// For task-level control, use `libraryagent_shelves_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn libraryagent_shelves_get(
    client: &SimpleHttpClient,
    args: &LibraryagentShelvesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleExampleLibraryagentV1Shelf>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = libraryagent_shelves_get_builder(client, args.name.clone())?;
    libraryagent_shelves_get_execute(builder)
}

/// GET v1/shelves
/// Lists shelves. The order is unspecified but deterministic. Newly created shelves will not necessarily be added to the end of this list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `libraryagent_shelves_list_execute()` to send, or `libraryagent_shelves_list` for simplest API.

pub fn libraryagent_shelves_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://libraryagent.googleapis.com/v1/shelves",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/shelves
/// Lists shelves. The order is unspecified but deterministic. Newly created shelves will not necessarily be added to the end of this list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `libraryagent_shelves_list_execute()` or `libraryagent_shelves_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `libraryagent_shelves_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn libraryagent_shelves_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleExampleLibraryagentV1ListShelvesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleExampleLibraryagentV1ListShelvesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/shelves
/// Lists shelves. The order is unspecified but deterministic. Newly created shelves will not necessarily be added to the end of this list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `libraryagent_shelves_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `libraryagent_shelves_list_task()`.
/// For the simplest API, use `libraryagent_shelves_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `libraryagent_shelves_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn libraryagent_shelves_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleExampleLibraryagentV1ListShelvesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = libraryagent_shelves_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`libraryagent_shelves_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LibraryagentShelvesListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/shelves
/// Lists shelves. The order is unspecified but deterministic. Newly created shelves will not necessarily be added to the end of this list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `libraryagent_shelves_list_builder()` + `libraryagent_shelves_list_execute()`.
/// For task-level control, use `libraryagent_shelves_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn libraryagent_shelves_list(
    client: &SimpleHttpClient,
    args: &LibraryagentShelvesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleExampleLibraryagentV1ListShelvesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        libraryagent_shelves_list_builder(client, args.pageSize.clone(), args.pageToken.clone())?;
    libraryagent_shelves_list_execute(builder)
}

/// GET v1/shelves/{shelvesId}/books
/// Lists books in a shelf. The order is unspecified but deterministic. Newly created books will not necessarily be added to the end of this list. Returns NOT_FOUND if the shelf does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `libraryagent_shelves_books_list_execute()` to send, or `libraryagent_shelves_books_list` for simplest API.

pub fn libraryagent_shelves_books_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://libraryagent.googleapis.com/v1/shelves/{}/books",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/shelves/{shelvesId}/books
/// Lists books in a shelf. The order is unspecified but deterministic. Newly created books will not necessarily be added to the end of this list. Returns NOT_FOUND if the shelf does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `libraryagent_shelves_books_list_execute()` or `libraryagent_shelves_books_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `libraryagent_shelves_books_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn libraryagent_shelves_books_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleExampleLibraryagentV1ListBooksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleExampleLibraryagentV1ListBooksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/shelves/{shelvesId}/books
/// Lists books in a shelf. The order is unspecified but deterministic. Newly created books will not necessarily be added to the end of this list. Returns NOT_FOUND if the shelf does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `libraryagent_shelves_books_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `libraryagent_shelves_books_list_task()`.
/// For the simplest API, use `libraryagent_shelves_books_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `libraryagent_shelves_books_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn libraryagent_shelves_books_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleExampleLibraryagentV1ListBooksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = libraryagent_shelves_books_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`libraryagent_shelves_books_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LibraryagentShelvesBooksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/shelves/{shelvesId}/books
/// Lists books in a shelf. The order is unspecified but deterministic. Newly created books will not necessarily be added to the end of this list. Returns NOT_FOUND if the shelf does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `libraryagent_shelves_books_list_builder()` + `libraryagent_shelves_books_list_execute()`.
/// For task-level control, use `libraryagent_shelves_books_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn libraryagent_shelves_books_list(
    client: &SimpleHttpClient,
    args: &LibraryagentShelvesBooksListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleExampleLibraryagentV1ListBooksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = libraryagent_shelves_books_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    libraryagent_shelves_books_list_execute(builder)
}
