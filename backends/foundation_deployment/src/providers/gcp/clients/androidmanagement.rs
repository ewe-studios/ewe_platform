//! Auto-generated API clients for gcp/androidmanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/enterprises
/// Creates an enterprise. This is the last step in the enterprise signup flow. See also: SigninDetail
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_create_execute()` to send, or `androidmanagement_enterprises_create` for simplest API.

pub fn androidmanagement_enterprises_create_builder(
    client: &SimpleHttpClient,
    agreementAccepted: &Option<bool>,
    enterpriseToken: &Option<String>,
    projectId: &Option<String>,
    signupUrlName: &Option<String>,
    body: &Enterprise,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/enterprises",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = agreementAccepted.as_ref() {
        query_parts.push(format!("agreementAccepted={}", val));
    }
    if let Some(val) = enterpriseToken.as_ref() {
        query_parts.push(format!("enterpriseToken={}", val));
    }
    if let Some(val) = projectId.as_ref() {
        query_parts.push(format!("projectId={}", val));
    }
    if let Some(val) = signupUrlName.as_ref() {
        query_parts.push(format!("signupUrlName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises
/// Creates an enterprise. This is the last step in the enterprise signup flow. See also: SigninDetail
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_create_execute()` or `androidmanagement_enterprises_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Enterprise>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises
/// Creates an enterprise. This is the last step in the enterprise signup flow. See also: SigninDetail
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_create_task()`.
/// For the simplest API, use `androidmanagement_enterprises_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesCreateArgs {
    /// Query parameter: agreementAccepted
    pub agreementAccepted: Option<bool>,
    /// Query parameter: enterpriseToken
    pub enterpriseToken: Option<String>,
    /// Query parameter: projectId
    pub projectId: Option<String>,
    /// Query parameter: signupUrlName
    pub signupUrlName: Option<String>,
    /// Request body.
    pub body: Enterprise,
}

/// GET v1/enterprises
/// Creates an enterprise. This is the last step in the enterprise signup flow. See also: SigninDetail
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_create_builder()` + `androidmanagement_enterprises_create_execute()`.
/// For task-level control, use `androidmanagement_enterprises_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_create(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_create_builder(
        client,
        &args.agreementAccepted,
        &args.enterpriseToken,
        &args.projectId,
        &args.signupUrlName,
        &args.body,
    )?;
    androidmanagement_enterprises_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}
/// Permanently deletes an enterprise and all accounts and data associated with it. Warning: this will result in a cascaded deletion of all AM API devices associated with the deleted enterprise. Only available for EMM-managed enterprises.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_delete_execute()` to send, or `androidmanagement_enterprises_delete` for simplest API.

pub fn androidmanagement_enterprises_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/enterprises/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}
/// Permanently deletes an enterprise and all accounts and data associated with it. Warning: this will result in a cascaded deletion of all AM API devices associated with the deleted enterprise. Only available for EMM-managed enterprises.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_delete_execute()` or `androidmanagement_enterprises_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}
/// Permanently deletes an enterprise and all accounts and data associated with it. Warning: this will result in a cascaded deletion of all AM API devices associated with the deleted enterprise. Only available for EMM-managed enterprises.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_delete_task()`.
/// For the simplest API, use `androidmanagement_enterprises_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/enterprises/{enterprisesId}
/// Permanently deletes an enterprise and all accounts and data associated with it. Warning: this will result in a cascaded deletion of all AM API devices associated with the deleted enterprise. Only available for EMM-managed enterprises.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_delete_builder()` + `androidmanagement_enterprises_delete_execute()`.
/// For task-level control, use `androidmanagement_enterprises_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_delete(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_delete_builder(client, &args.name)?;
    androidmanagement_enterprises_delete_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}:generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide (<https://developers.google.`com/android/management/upgrade-an-enterprise`>) for more details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_generate_enterprise_upgrade_url_execute()` to send, or `androidmanagement_enterprises_generate_enterprise_upgrade_url` for simplest API.

pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GenerateEnterpriseUpgradeUrlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}:generateEnterpriseUpgradeUrl",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}:generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide (<https://developers.google.`com/android/management/upgrade-an-enterprise`>) for more details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_generate_enterprise_upgrade_url_execute()` or `androidmanagement_enterprises_generate_enterprise_upgrade_url`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_generate_enterprise_upgrade_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateEnterpriseUpgradeUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}:generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide (<https://developers.google.`com/android/management/upgrade-an-enterprise`>) for more details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_generate_enterprise_upgrade_url_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_generate_enterprise_upgrade_url_task()`.
/// For the simplest API, use `androidmanagement_enterprises_generate_enterprise_upgrade_url()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_generate_enterprise_upgrade_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_generate_enterprise_upgrade_url_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_generate_enterprise_upgrade_url`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesGenerateEnterpriseUpgradeUrlArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GenerateEnterpriseUpgradeUrlRequest,
}

/// GET v1/enterprises/{enterprisesId}:generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide (<https://developers.google.`com/android/management/upgrade-an-enterprise`>) for more details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_generate_enterprise_upgrade_url_builder()` + `androidmanagement_enterprises_generate_enterprise_upgrade_url_execute()`.
/// For task-level control, use `androidmanagement_enterprises_generate_enterprise_upgrade_url_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesGenerateEnterpriseUpgradeUrlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_generate_enterprise_upgrade_url_builder(
        client, &args.name, &args.body,
    )?;
    androidmanagement_enterprises_generate_enterprise_upgrade_url_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices
/// Lists devices for a given enterprise. Deleted devices are not returned in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_list_execute()` to send, or `androidmanagement_enterprises_devices_list` for simplest API.

pub fn androidmanagement_enterprises_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androidmanagement.googleapis.com/v1/enterprises/{}/devices",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices
/// Lists devices for a given enterprise. Deleted devices are not returned in the response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_devices_list_execute()` or `androidmanagement_enterprises_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}/devices
/// Lists devices for a given enterprise. Deleted devices are not returned in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_devices_list_task()`.
/// For the simplest API, use `androidmanagement_enterprises_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDevicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/enterprises/{enterprisesId}/devices
/// Lists devices for a given enterprise. Deleted devices are not returned in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_list_builder()` + `androidmanagement_enterprises_devices_list_execute()`.
/// For task-level control, use `androidmanagement_enterprises_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_list(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesDevicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDevicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_devices_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    androidmanagement_enterprises_devices_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Creates an enrollment token for a given enterprise. It's up to the caller's responsibility to manage the lifecycle of newly created tokens and deleting them when they're not intended to be used anymore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_enrollment_tokens_create_execute()` to send, or `androidmanagement_enterprises_enrollment_tokens_create` for simplest API.

pub fn androidmanagement_enterprises_enrollment_tokens_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &EnrollmentToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androidmanagement.googleapis.com/v1/enterprises/{}/enrollmentTokens",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Creates an enrollment token for a given enterprise. It's up to the caller's responsibility to manage the lifecycle of newly created tokens and deleting them when they're not intended to be used anymore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_enrollment_tokens_create_execute()` or `androidmanagement_enterprises_enrollment_tokens_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_enrollment_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnrollmentToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnrollmentToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Creates an enrollment token for a given enterprise. It's up to the caller's responsibility to manage the lifecycle of newly created tokens and deleting them when they're not intended to be used anymore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_enrollment_tokens_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_enrollment_tokens_create_task()`.
/// For the simplest API, use `androidmanagement_enterprises_enrollment_tokens_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_enrollment_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_enrollment_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_enrollment_tokens_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_enrollment_tokens_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesEnrollmentTokensCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: EnrollmentToken,
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Creates an enrollment token for a given enterprise. It's up to the caller's responsibility to manage the lifecycle of newly created tokens and deleting them when they're not intended to be used anymore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_create_builder()` + `androidmanagement_enterprises_enrollment_tokens_create_execute()`.
/// For task-level control, use `androidmanagement_enterprises_enrollment_tokens_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_create(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesEnrollmentTokensCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_enrollment_tokens_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    androidmanagement_enterprises_enrollment_tokens_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Creates a migration token, to migrate an existing device from being managed by the EMM's Device Policy Controller (DPC) to being managed by the Android Management API. See the guide (<https://developers.google.`com/android/management/dpc-migration`>) for more details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_migration_tokens_create_execute()` to send, or `androidmanagement_enterprises_migration_tokens_create` for simplest API.

pub fn androidmanagement_enterprises_migration_tokens_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &MigrationToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androidmanagement.googleapis.com/v1/enterprises/{}/migrationTokens",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Creates a migration token, to migrate an existing device from being managed by the EMM's Device Policy Controller (DPC) to being managed by the Android Management API. See the guide (<https://developers.google.`com/android/management/dpc-migration`>) for more details.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_migration_tokens_create_execute()` or `androidmanagement_enterprises_migration_tokens_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_migration_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MigrationToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigrationToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Creates a migration token, to migrate an existing device from being managed by the EMM's Device Policy Controller (DPC) to being managed by the Android Management API. See the guide (<https://developers.google.`com/android/management/dpc-migration`>) for more details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_migration_tokens_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_migration_tokens_create_task()`.
/// For the simplest API, use `androidmanagement_enterprises_migration_tokens_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_migration_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_migration_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_migration_tokens_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_migration_tokens_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesMigrationTokensCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: MigrationToken,
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Creates a migration token, to migrate an existing device from being managed by the EMM's Device Policy Controller (DPC) to being managed by the Android Management API. See the guide (<https://developers.google.`com/android/management/dpc-migration`>) for more details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_migration_tokens_create_builder()` + `androidmanagement_enterprises_migration_tokens_create_execute()`.
/// For task-level control, use `androidmanagement_enterprises_migration_tokens_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_create(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesMigrationTokensCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_migration_tokens_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    androidmanagement_enterprises_migration_tokens_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies
/// Lists policies for a given enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_policies_list_execute()` to send, or `androidmanagement_enterprises_policies_list` for simplest API.

pub fn androidmanagement_enterprises_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androidmanagement.googleapis.com/v1/enterprises/{}/policies",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies
/// Lists policies for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_policies_list_execute()` or `androidmanagement_enterprises_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}/policies
/// Lists policies for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_policies_list_task()`.
/// For the simplest API, use `androidmanagement_enterprises_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/enterprises/{enterprisesId}/policies
/// Lists policies for a given enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_policies_list_builder()` + `androidmanagement_enterprises_policies_list_execute()`.
/// For task-level control, use `androidmanagement_enterprises_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_list(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_policies_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    androidmanagement_enterprises_policies_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Creates a web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_apps_create_execute()` to send, or `androidmanagement_enterprises_web_apps_create` for simplest API.

pub fn androidmanagement_enterprises_web_apps_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &WebApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androidmanagement.googleapis.com/v1/enterprises/{}/webApps",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Creates a web app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_web_apps_create_execute()` or `androidmanagement_enterprises_web_apps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_web_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Creates a web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_web_apps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_web_apps_create_task()`.
/// For the simplest API, use `androidmanagement_enterprises_web_apps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_web_apps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_web_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_web_apps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_web_apps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebAppsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: WebApp,
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Creates a web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_apps_create_builder()` + `androidmanagement_enterprises_web_apps_create_execute()`.
/// For task-level control, use `androidmanagement_enterprises_web_apps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_create(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesWebAppsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_web_apps_create_builder(client, &args.parent, &args.body)?;
    androidmanagement_enterprises_web_apps_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webTokens
/// Creates a web token to access an embeddable managed Google Play web UI for a given enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_tokens_create_execute()` to send, or `androidmanagement_enterprises_web_tokens_create` for simplest API.

pub fn androidmanagement_enterprises_web_tokens_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &WebToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androidmanagement.googleapis.com/v1/enterprises/{}/webTokens",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webTokens
/// Creates a web token to access an embeddable managed Google Play web UI for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_enterprises_web_tokens_create_execute()` or `androidmanagement_enterprises_web_tokens_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_web_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_tokens_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/enterprises/{enterprisesId}/webTokens
/// Creates a web token to access an embeddable managed Google Play web UI for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_enterprises_web_tokens_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_enterprises_web_tokens_create_task()`.
/// For the simplest API, use `androidmanagement_enterprises_web_tokens_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_enterprises_web_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_enterprises_web_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebToken>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidmanagement_enterprises_web_tokens_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_enterprises_web_tokens_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebTokensCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: WebToken,
}

/// GET v1/enterprises/{enterprisesId}/webTokens
/// Creates a web token to access an embeddable managed Google Play web UI for a given enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_tokens_create_builder()` + `androidmanagement_enterprises_web_tokens_create_execute()`.
/// For task-level control, use `androidmanagement_enterprises_web_tokens_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_tokens_create(
    client: &SimpleHttpClient,
    args: &AndroidmanagementEnterprisesWebTokensCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebToken>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_web_tokens_create_builder(client, &args.parent, &args.body)?;
    androidmanagement_enterprises_web_tokens_create_execute(builder)
}

/// GET v1/provisioningInfo/{provisioningInfoId}
/// Get the device provisioning information by the identifier provided in the sign-in url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_provisioning_info_get_execute()` to send, or `androidmanagement_provisioning_info_get` for simplest API.

pub fn androidmanagement_provisioning_info_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/provisioningInfo/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/provisioningInfo/{provisioningInfoId}
/// Get the device provisioning information by the identifier provided in the sign-in url.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_provisioning_info_get_execute()` or `androidmanagement_provisioning_info_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_provisioning_info_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_provisioning_info_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProvisioningInfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/provisioningInfo/{provisioningInfoId}
/// Get the device provisioning information by the identifier provided in the sign-in url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_provisioning_info_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_provisioning_info_get_task()`.
/// For the simplest API, use `androidmanagement_provisioning_info_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_provisioning_info_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_provisioning_info_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidmanagement_provisioning_info_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_provisioning_info_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementProvisioningInfoGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/provisioningInfo/{provisioningInfoId}
/// Get the device provisioning information by the identifier provided in the sign-in url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_provisioning_info_get_builder()` + `androidmanagement_provisioning_info_get_execute()`.
/// For task-level control, use `androidmanagement_provisioning_info_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_provisioning_info_get(
    client: &SimpleHttpClient,
    args: &AndroidmanagementProvisioningInfoGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_provisioning_info_get_builder(client, &args.name)?;
    androidmanagement_provisioning_info_get_execute(builder)
}

/// GET v1/signupUrls
/// Creates an enterprise signup URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_signup_urls_create_execute()` to send, or `androidmanagement_signup_urls_create` for simplest API.

pub fn androidmanagement_signup_urls_create_builder(
    client: &SimpleHttpClient,
    adminEmail: &Option<String>,
    allowedDomains: &Option<String>,
    callbackUrl: &Option<String>,
    projectId: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/signupUrls",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = adminEmail.as_ref() {
        query_parts.push(format!("adminEmail={}", val));
    }
    if let Some(val) = allowedDomains.as_ref() {
        query_parts.push(format!("allowedDomains={}", val));
    }
    if let Some(val) = callbackUrl.as_ref() {
        query_parts.push(format!("callbackUrl={}", val));
    }
    if let Some(val) = projectId.as_ref() {
        query_parts.push(format!("projectId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/signupUrls
/// Creates an enterprise signup URL.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidmanagement_signup_urls_create_execute()` or `androidmanagement_signup_urls_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_signup_urls_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_signup_urls_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SignupUrl>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignupUrl = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/signupUrls
/// Creates an enterprise signup URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidmanagement_signup_urls_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidmanagement_signup_urls_create_task()`.
/// For the simplest API, use `androidmanagement_signup_urls_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidmanagement_signup_urls_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidmanagement_signup_urls_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupUrl>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidmanagement_signup_urls_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidmanagement_signup_urls_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidmanagementSignupUrlsCreateArgs {
    /// Query parameter: adminEmail
    pub adminEmail: Option<String>,
    /// Query parameter: allowedDomains
    pub allowedDomains: Option<String>,
    /// Query parameter: callbackUrl
    pub callbackUrl: Option<String>,
    /// Query parameter: projectId
    pub projectId: Option<String>,
}

/// GET v1/signupUrls
/// Creates an enterprise signup URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_signup_urls_create_builder()` + `androidmanagement_signup_urls_create_execute()`.
/// For task-level control, use `androidmanagement_signup_urls_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_signup_urls_create(
    client: &SimpleHttpClient,
    args: &AndroidmanagementSignupUrlsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupUrl>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_signup_urls_create_builder(
        client,
        &args.adminEmail,
        &args.allowedDomains,
        &args.callbackUrl,
        &args.projectId,
    )?;
    androidmanagement_signup_urls_create_execute(builder)
}
