//! Auto-generated API clients for gcp/androidmanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/androidmanagement`

#![cfg(feature = "gcp/androidmanagement")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/enterprises
/// Creates an enterprise. This is the last step in the enterprise signup flow. See also: SigninDetail
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_create_execute()` to send, or `androidmanagement_enterprises_create` for simplest API.

pub fn androidmanagement_enterprises_create_builder(
    client: &SimpleHttpClient,
    agreementAccepted: Option<bool>,
    enterpriseToken: Option<&str>,
    projectId: Option<&str>,
    signupUrlName: Option<&str>,
    body: &Enterprise,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://androidmanagement.googleapis.com/v1/enterprises",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = agreementAccepted {
        query_parts.push(format!("agreementAccepted={}", val));
    }
    if let Some(val) = enterpriseToken {
        query_parts.push(format!("enterpriseToken={}", val));
    }
    if let Some(val) = projectId {
        query_parts.push(format!("projectId={}", val));
    }
    if let Some(val) = signupUrlName {
        query_parts.push(format!("signupUrlName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises
/// Creates an enterprise. This is the last step in the enterprise signup flow. See also: SigninDetail
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises
/// Creates an enterprise. This is the last step in the enterprise signup flow. See also: SigninDetail
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_create_builder()` + `androidmanagement_enterprises_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_create(
    client: &SimpleHttpClient,
    agreementAccepted: Option<bool>,
    enterpriseToken: Option<&str>,
    projectId: Option<&str>,
    signupUrlName: Option<&str>,
    body: &Enterprise,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_create_builder(
        client,
        agreementAccepted,
        enterpriseToken,
        projectId,
        signupUrlName,
        body,
    )?;
    androidmanagement_enterprises_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}
/// Permanently deletes an enterprise and all accounts and data associated with it. Warning: this will result in a cascaded deletion of all AM API devices associated with the deleted enterprise. Only available for EMM-managed enterprises.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_delete_execute()` to send, or `androidmanagement_enterprises_delete` for simplest API.

pub fn androidmanagement_enterprises_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}
/// Permanently deletes an enterprise and all accounts and data associated with it. Warning: this will result in a cascaded deletion of all AM API devices associated with the deleted enterprise. Only available for EMM-managed enterprises.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}
/// Permanently deletes an enterprise and all accounts and data associated with it. Warning: this will result in a cascaded deletion of all AM API devices associated with the deleted enterprise. Only available for EMM-managed enterprises.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_delete_builder()` + `androidmanagement_enterprises_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_delete_builder(client, name)?;
    androidmanagement_enterprises_delete_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}:generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide (<https://developers.google.`com/android/management/upgrade-an-enterprise`>) for more details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_generate_enterprise_upgrade_url_execute()` to send, or `androidmanagement_enterprises_generate_enterprise_upgrade_url` for simplest API.

pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateEnterpriseUpgradeUrlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}:generateEnterpriseUpgradeUrl",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}:generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide (<https://developers.google.`com/android/management/upgrade-an-enterprise`>) for more details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_generate_enterprise_upgrade_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_generate_enterprise_upgrade_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateEnterpriseUpgradeUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}:generateEnterpriseUpgradeUrl
/// Generates an enterprise upgrade URL to upgrade an existing managed Google Play Accounts enterprise to a managed Google domain. See the guide (<https://developers.google.`com/android/management/upgrade-an-enterprise`>) for more details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_generate_enterprise_upgrade_url_builder()` + `androidmanagement_enterprises_generate_enterprise_upgrade_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateEnterpriseUpgradeUrlRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEnterpriseUpgradeUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_generate_enterprise_upgrade_url_builder(client, name, body)?;
    androidmanagement_enterprises_generate_enterprise_upgrade_url_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}
/// Gets an enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_get_execute()` to send, or `androidmanagement_enterprises_get` for simplest API.

pub fn androidmanagement_enterprises_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}
/// Gets an enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}
/// Gets an enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_get_builder()` + `androidmanagement_enterprises_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_get_builder(client, name)?;
    androidmanagement_enterprises_get_execute(builder)
}

/// GET v1/enterprises
/// Lists EMM-managed enterprises. Only BASIC fields are returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_list_execute()` to send, or `androidmanagement_enterprises_list` for simplest API.

pub fn androidmanagement_enterprises_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    projectId: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://androidmanagement.googleapis.com/v1/enterprises",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = projectId {
        query_parts.push(format!("projectId={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises
/// Lists EMM-managed enterprises. Only BASIC fields are returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEnterprisesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEnterprisesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises
/// Lists EMM-managed enterprises. Only BASIC fields are returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_list_builder()` + `androidmanagement_enterprises_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    projectId: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEnterprisesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_list_builder(client, pageSize, pageToken, projectId, view)?;
    androidmanagement_enterprises_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}
/// Updates an enterprise. See also: SigninDetail
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_patch_execute()` to send, or `androidmanagement_enterprises_patch` for simplest API.

pub fn androidmanagement_enterprises_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Enterprise,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}
/// Updates an enterprise. See also: SigninDetail
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Enterprise = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}
/// Updates an enterprise. See also: SigninDetail
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_patch_builder()` + `androidmanagement_enterprises_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Enterprise,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Enterprise>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_patch_builder(client, name, updateMask, body)?;
    androidmanagement_enterprises_patch_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/applications/{applicationsId}
/// Gets info about an application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_applications_get_execute()` to send, or `androidmanagement_enterprises_applications_get` for simplest API.

pub fn androidmanagement_enterprises_applications_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/applications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/applications/{applicationsId}
/// Gets info about an application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_applications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_applications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_applications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Application = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/applications/{applicationsId}
/// Gets info about an application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_applications_get_builder()` + `androidmanagement_enterprises_applications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_applications_get(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_applications_get_builder(client, name, languageCode)?;
    androidmanagement_enterprises_applications_get_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Deletes a device. This operation attempts to wipe the device but this is not guaranteed to succeed if the device is offline for an extended period. Deleted devices do not show up in enterprises.devices.list calls and a 404 is returned from enterprises.devices.get.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_delete_execute()` to send, or `androidmanagement_enterprises_devices_delete` for simplest API.

pub fn androidmanagement_enterprises_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    wipeDataFlags: Option<&str>,
    wipeReasonMessage: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = wipeDataFlags {
        query_parts.push(format!("wipeDataFlags={}", val));
    }
    if let Some(val) = wipeReasonMessage {
        query_parts.push(format!("wipeReasonMessage={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Deletes a device. This operation attempts to wipe the device but this is not guaranteed to succeed if the device is offline for an extended period. Deleted devices do not show up in enterprises.devices.list calls and a 404 is returned from enterprises.devices.get.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Deletes a device. This operation attempts to wipe the device but this is not guaranteed to succeed if the device is offline for an extended period. Deleted devices do not show up in enterprises.devices.list calls and a 404 is returned from enterprises.devices.get.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_delete_builder()` + `androidmanagement_enterprises_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_delete(
    client: &SimpleHttpClient,
    name: &str,
    wipeDataFlags: Option<&str>,
    wipeReasonMessage: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_devices_delete_builder(
        client,
        name,
        wipeDataFlags,
        wipeReasonMessage,
    )?;
    androidmanagement_enterprises_devices_delete_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Gets a device. Deleted devices will respond with a 404 error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_get_execute()` to send, or `androidmanagement_enterprises_devices_get` for simplest API.

pub fn androidmanagement_enterprises_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Gets a device. Deleted devices will respond with a 404 error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Gets a device. Deleted devices will respond with a 404 error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_get_builder()` + `androidmanagement_enterprises_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_devices_get_builder(client, name)?;
    androidmanagement_enterprises_devices_get_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}:issueCommand
/// Issues a command to a device. The Operation resource returned contains a Command in its metadata field. Use the get operation method to get the status of the command.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_issue_command_execute()` to send, or `androidmanagement_enterprises_devices_issue_command` for simplest API.

pub fn androidmanagement_enterprises_devices_issue_command_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &Command,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices/{}:issueCommand",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}:issueCommand
/// Issues a command to a device. The Operation resource returned contains a Command in its metadata field. Use the get operation method to get the status of the command.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_issue_command_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_issue_command_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_issue_command_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}:issueCommand
/// Issues a command to a device. The Operation resource returned contains a Command in its metadata field. Use the get operation method to get the status of the command.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_issue_command_builder()` + `androidmanagement_enterprises_devices_issue_command_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_issue_command(
    client: &SimpleHttpClient,
    name: &str,
    body: &Command,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_devices_issue_command_builder(client, name, body)?;
    androidmanagement_enterprises_devices_issue_command_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices
/// Lists devices for a given enterprise. Deleted devices are not returned in the response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_list_execute()` to send, or `androidmanagement_enterprises_devices_list` for simplest API.

pub fn androidmanagement_enterprises_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices
/// Lists devices for a given enterprise. Deleted devices are not returned in the response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDevicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices
/// Lists devices for a given enterprise. Deleted devices are not returned in the response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_list_builder()` + `androidmanagement_enterprises_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDevicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_devices_list_builder(client, parent, pageSize, pageToken)?;
    androidmanagement_enterprises_devices_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_patch_execute()` to send, or `androidmanagement_enterprises_devices_patch` for simplest API.

pub fn androidmanagement_enterprises_devices_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Device,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Device = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_patch_builder()` + `androidmanagement_enterprises_devices_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Device,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Device>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_devices_patch_builder(client, name, updateMask, body)?;
    androidmanagement_enterprises_devices_patch_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_operations_cancel_execute()` to send, or `androidmanagement_enterprises_devices_operations_cancel` for simplest API.

pub fn androidmanagement_enterprises_devices_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_operations_cancel_builder()` + `androidmanagement_enterprises_devices_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_devices_operations_cancel_builder(client, name)?;
    androidmanagement_enterprises_devices_operations_cancel_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_operations_get_execute()` to send, or `androidmanagement_enterprises_devices_operations_get` for simplest API.

pub fn androidmanagement_enterprises_devices_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_operations_get_builder()` + `androidmanagement_enterprises_devices_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_devices_operations_get_builder(client, name)?;
    androidmanagement_enterprises_devices_operations_get_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_devices_operations_list_execute()` to send, or `androidmanagement_enterprises_devices_operations_list` for simplest API.

pub fn androidmanagement_enterprises_devices_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/devices/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_devices_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_devices_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/devices/{devicesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_devices_operations_list_builder()` + `androidmanagement_enterprises_devices_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_devices_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_devices_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    androidmanagement_enterprises_devices_operations_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Creates an enrollment token for a given enterprise. It's up to the caller's responsibility to manage the lifecycle of newly created tokens and deleting them when they're not intended to be used anymore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_enrollment_tokens_create_execute()` to send, or `androidmanagement_enterprises_enrollment_tokens_create` for simplest API.

pub fn androidmanagement_enterprises_enrollment_tokens_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollmentToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/enrollmentTokens",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Creates an enrollment token for a given enterprise. It's up to the caller's responsibility to manage the lifecycle of newly created tokens and deleting them when they're not intended to be used anymore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_enrollment_tokens_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnrollmentToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Creates an enrollment token for a given enterprise. It's up to the caller's responsibility to manage the lifecycle of newly created tokens and deleting them when they're not intended to be used anymore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_create_builder()` + `androidmanagement_enterprises_enrollment_tokens_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &EnrollmentToken,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_enrollment_tokens_create_builder(client, parent, body)?;
    androidmanagement_enterprises_enrollment_tokens_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens/{enrollmentTokensId}
/// Deletes an enrollment token. This operation invalidates the token, preventing its future use.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_enrollment_tokens_delete_execute()` to send, or `androidmanagement_enterprises_enrollment_tokens_delete` for simplest API.

pub fn androidmanagement_enterprises_enrollment_tokens_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/enrollmentTokens/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens/{enrollmentTokensId}
/// Deletes an enrollment token. This operation invalidates the token, preventing its future use.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_enrollment_tokens_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens/{enrollmentTokensId}
/// Deletes an enrollment token. This operation invalidates the token, preventing its future use.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_delete_builder()` + `androidmanagement_enterprises_enrollment_tokens_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_enrollment_tokens_delete_builder(client, name)?;
    androidmanagement_enterprises_enrollment_tokens_delete_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens/{enrollmentTokensId}
/// Gets an active, unexpired enrollment token. A partial view of the enrollment token is returned. Only the following fields are populated: name, `expirationTimestamp`, `allowPersonalUsage`, value, `qrCode`. This method is meant to help manage active enrollment tokens lifecycle. For security reasons, it's recommended to delete active enrollment tokens as soon as they're not intended to be used anymore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_enrollment_tokens_get_execute()` to send, or `androidmanagement_enterprises_enrollment_tokens_get` for simplest API.

pub fn androidmanagement_enterprises_enrollment_tokens_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/enrollmentTokens/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens/{enrollmentTokensId}
/// Gets an active, unexpired enrollment token. A partial view of the enrollment token is returned. Only the following fields are populated: name, `expirationTimestamp`, `allowPersonalUsage`, value, `qrCode`. This method is meant to help manage active enrollment tokens lifecycle. For security reasons, it's recommended to delete active enrollment tokens as soon as they're not intended to be used anymore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_enrollment_tokens_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnrollmentToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens/{enrollmentTokensId}
/// Gets an active, unexpired enrollment token. A partial view of the enrollment token is returned. Only the following fields are populated: name, `expirationTimestamp`, `allowPersonalUsage`, value, `qrCode`. This method is meant to help manage active enrollment tokens lifecycle. For security reasons, it's recommended to delete active enrollment tokens as soon as they're not intended to be used anymore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_get_builder()` + `androidmanagement_enterprises_enrollment_tokens_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EnrollmentToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_enrollment_tokens_get_builder(client, name)?;
    androidmanagement_enterprises_enrollment_tokens_get_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Lists active, unexpired enrollment tokens for a given enterprise. The list items contain only a partial view of EnrollmentToken object. Only the following fields are populated: name, `expirationTimestamp`, `allowPersonalUsage`, value, `qrCode`. This method is meant to help manage active enrollment tokens lifecycle. For security reasons, it's recommended to delete active enrollment tokens as soon as they're not intended to be used anymore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_enrollment_tokens_list_execute()` to send, or `androidmanagement_enterprises_enrollment_tokens_list` for simplest API.

pub fn androidmanagement_enterprises_enrollment_tokens_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/enrollmentTokens",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Lists active, unexpired enrollment tokens for a given enterprise. The list items contain only a partial view of EnrollmentToken object. Only the following fields are populated: name, `expirationTimestamp`, `allowPersonalUsage`, value, `qrCode`. This method is meant to help manage active enrollment tokens lifecycle. For security reasons, it's recommended to delete active enrollment tokens as soon as they're not intended to be used anymore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_enrollment_tokens_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEnrollmentTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEnrollmentTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/enrollmentTokens
/// Lists active, unexpired enrollment tokens for a given enterprise. The list items contain only a partial view of EnrollmentToken object. Only the following fields are populated: name, `expirationTimestamp`, `allowPersonalUsage`, value, `qrCode`. This method is meant to help manage active enrollment tokens lifecycle. For security reasons, it's recommended to delete active enrollment tokens as soon as they're not intended to be used anymore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_enrollment_tokens_list_builder()` + `androidmanagement_enterprises_enrollment_tokens_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_enrollment_tokens_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListEnrollmentTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_enrollment_tokens_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    androidmanagement_enterprises_enrollment_tokens_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Creates a migration token, to migrate an existing device from being managed by the EMM's Device Policy Controller (DPC) to being managed by the Android Management API. See the guide (<https://developers.google.`com/android/management/dpc-migration`>) for more details.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_migration_tokens_create_execute()` to send, or `androidmanagement_enterprises_migration_tokens_create` for simplest API.

pub fn androidmanagement_enterprises_migration_tokens_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &MigrationToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/migrationTokens",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Creates a migration token, to migrate an existing device from being managed by the EMM's Device Policy Controller (DPC) to being managed by the Android Management API. See the guide (<https://developers.google.`com/android/management/dpc-migration`>) for more details.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_migration_tokens_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_migration_tokens_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigrationToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Creates a migration token, to migrate an existing device from being managed by the EMM's Device Policy Controller (DPC) to being managed by the Android Management API. See the guide (<https://developers.google.`com/android/management/dpc-migration`>) for more details.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_migration_tokens_create_builder()` + `androidmanagement_enterprises_migration_tokens_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &MigrationToken,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_migration_tokens_create_builder(client, parent, body)?;
    androidmanagement_enterprises_migration_tokens_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens/{migrationTokensId}
/// Gets a migration token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_migration_tokens_get_execute()` to send, or `androidmanagement_enterprises_migration_tokens_get` for simplest API.

pub fn androidmanagement_enterprises_migration_tokens_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/migrationTokens/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens/{migrationTokensId}
/// Gets a migration token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_migration_tokens_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_migration_tokens_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigrationToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens/{migrationTokensId}
/// Gets a migration token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_migration_tokens_get_builder()` + `androidmanagement_enterprises_migration_tokens_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_migration_tokens_get_builder(client, name)?;
    androidmanagement_enterprises_migration_tokens_get_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Lists migration tokens.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_migration_tokens_list_execute()` to send, or `androidmanagement_enterprises_migration_tokens_list` for simplest API.

pub fn androidmanagement_enterprises_migration_tokens_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/migrationTokens",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Lists migration tokens.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_migration_tokens_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_migration_tokens_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMigrationTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMigrationTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/migrationTokens
/// Lists migration tokens.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_migration_tokens_list_builder()` + `androidmanagement_enterprises_migration_tokens_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_migration_tokens_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMigrationTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_migration_tokens_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    androidmanagement_enterprises_migration_tokens_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Deletes a policy. This operation is only permitted if no devices are currently referencing the policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_policies_delete_execute()` to send, or `androidmanagement_enterprises_policies_delete` for simplest API.

pub fn androidmanagement_enterprises_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/policies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Deletes a policy. This operation is only permitted if no devices are currently referencing the policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Deletes a policy. This operation is only permitted if no devices are currently referencing the policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_policies_delete_builder()` + `androidmanagement_enterprises_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_policies_delete_builder(client, name)?;
    androidmanagement_enterprises_policies_delete_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Gets a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_policies_get_execute()` to send, or `androidmanagement_enterprises_policies_get` for simplest API.

pub fn androidmanagement_enterprises_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/policies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Gets a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Gets a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_policies_get_builder()` + `androidmanagement_enterprises_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_policies_get_builder(client, name)?;
    androidmanagement_enterprises_policies_get_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies
/// Lists policies for a given enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_policies_list_execute()` to send, or `androidmanagement_enterprises_policies_list` for simplest API.

pub fn androidmanagement_enterprises_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/policies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies
/// Lists policies for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies
/// Lists policies for a given enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_policies_list_builder()` + `androidmanagement_enterprises_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_policies_list_builder(client, parent, pageSize, pageToken)?;
    androidmanagement_enterprises_policies_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}:modifyPolicyApplications
/// Updates or creates applications in a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_policies_modify_policy_applications_execute()` to send, or `androidmanagement_enterprises_policies_modify_policy_applications` for simplest API.

pub fn androidmanagement_enterprises_policies_modify_policy_applications_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ModifyPolicyApplicationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/policies/{}:modifyPolicyApplications",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}:modifyPolicyApplications
/// Updates or creates applications in a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_policies_modify_policy_applications_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_policies_modify_policy_applications_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_modify_policy_applications_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyPolicyApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ModifyPolicyApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}:modifyPolicyApplications
/// Updates or creates applications in a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_policies_modify_policy_applications_builder()` + `androidmanagement_enterprises_policies_modify_policy_applications_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_modify_policy_applications(
    client: &SimpleHttpClient,
    name: &str,
    body: &ModifyPolicyApplicationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyPolicyApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_policies_modify_policy_applications_builder(
        client, name, body,
    )?;
    androidmanagement_enterprises_policies_modify_policy_applications_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Updates or creates a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_policies_patch_execute()` to send, or `androidmanagement_enterprises_policies_patch` for simplest API.

pub fn androidmanagement_enterprises_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/policies/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Updates or creates a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}
/// Updates or creates a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_policies_patch_builder()` + `androidmanagement_enterprises_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Policy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_policies_patch_builder(client, name, updateMask, body)?;
    androidmanagement_enterprises_policies_patch_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}:removePolicyApplications
/// Removes applications in a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_policies_remove_policy_applications_execute()` to send, or `androidmanagement_enterprises_policies_remove_policy_applications` for simplest API.

pub fn androidmanagement_enterprises_policies_remove_policy_applications_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RemovePolicyApplicationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/policies/{}:removePolicyApplications",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}:removePolicyApplications
/// Removes applications in a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_policies_remove_policy_applications_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_policies_remove_policy_applications_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_remove_policy_applications_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemovePolicyApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemovePolicyApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/policies/{policiesId}:removePolicyApplications
/// Removes applications in a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_policies_remove_policy_applications_builder()` + `androidmanagement_enterprises_policies_remove_policy_applications_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_policies_remove_policy_applications(
    client: &SimpleHttpClient,
    name: &str,
    body: &RemovePolicyApplicationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RemovePolicyApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_policies_remove_policy_applications_builder(
        client, name, body,
    )?;
    androidmanagement_enterprises_policies_remove_policy_applications_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Creates a web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_apps_create_execute()` to send, or `androidmanagement_enterprises_web_apps_create` for simplest API.

pub fn androidmanagement_enterprises_web_apps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WebApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/webApps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Creates a web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_web_apps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_web_apps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Creates a web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_apps_create_builder()` + `androidmanagement_enterprises_web_apps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WebApp,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_web_apps_create_builder(client, parent, body)?;
    androidmanagement_enterprises_web_apps_create_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Deletes a web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_apps_delete_execute()` to send, or `androidmanagement_enterprises_web_apps_delete` for simplest API.

pub fn androidmanagement_enterprises_web_apps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/webApps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Deletes a web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_web_apps_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_web_apps_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Deletes a web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_apps_delete_builder()` + `androidmanagement_enterprises_web_apps_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_web_apps_delete_builder(client, name)?;
    androidmanagement_enterprises_web_apps_delete_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Gets a web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_apps_get_execute()` to send, or `androidmanagement_enterprises_web_apps_get` for simplest API.

pub fn androidmanagement_enterprises_web_apps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/webApps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Gets a web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_web_apps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_web_apps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Gets a web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_apps_get_builder()` + `androidmanagement_enterprises_web_apps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_web_apps_get_builder(client, name)?;
    androidmanagement_enterprises_web_apps_get_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Lists web apps for a given enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_apps_list_execute()` to send, or `androidmanagement_enterprises_web_apps_list` for simplest API.

pub fn androidmanagement_enterprises_web_apps_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/webApps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Lists web apps for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_web_apps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_web_apps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWebAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWebAppsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps
/// Lists web apps for a given enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_apps_list_builder()` + `androidmanagement_enterprises_web_apps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWebAppsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_web_apps_list_builder(client, parent, pageSize, pageToken)?;
    androidmanagement_enterprises_web_apps_list_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Updates a web app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_apps_patch_execute()` to send, or `androidmanagement_enterprises_web_apps_patch` for simplest API.

pub fn androidmanagement_enterprises_web_apps_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &WebApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/webApps/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Updates a web app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_web_apps_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_web_apps_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webApps/{webAppsId}
/// Updates a web app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_apps_patch_builder()` + `androidmanagement_enterprises_web_apps_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_apps_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &WebApp,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidmanagement_enterprises_web_apps_patch_builder(client, name, updateMask, body)?;
    androidmanagement_enterprises_web_apps_patch_execute(builder)
}

/// GET v1/enterprises/{enterprisesId}/webTokens
/// Creates a web token to access an embeddable managed Google Play web UI for a given enterprise.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_enterprises_web_tokens_create_execute()` to send, or `androidmanagement_enterprises_web_tokens_create` for simplest API.

pub fn androidmanagement_enterprises_web_tokens_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WebToken,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/enterprises/{}/webTokens",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webTokens
/// Creates a web token to access an embeddable managed Google Play web UI for a given enterprise.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_enterprises_web_tokens_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_enterprises_web_tokens_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebToken>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/enterprises/{enterprisesId}/webTokens
/// Creates a web token to access an embeddable managed Google Play web UI for a given enterprise.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_enterprises_web_tokens_create_builder()` + `androidmanagement_enterprises_web_tokens_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_enterprises_web_tokens_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WebToken,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebToken>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_enterprises_web_tokens_create_builder(client, parent, body)?;
    androidmanagement_enterprises_web_tokens_create_execute(builder)
}

/// GET v1/provisioningInfo/{provisioningInfoId}
/// Get the device provisioning information by the identifier provided in the sign-in url.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_provisioning_info_get_execute()` to send, or `androidmanagement_provisioning_info_get` for simplest API.

pub fn androidmanagement_provisioning_info_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://androidmanagement.googleapis.com/v1/provisioningInfo/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/provisioningInfo/{provisioningInfoId}
/// Get the device provisioning information by the identifier provided in the sign-in url.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_provisioning_info_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_provisioning_info_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_provisioning_info_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningInfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/provisioningInfo/{provisioningInfoId}
/// Get the device provisioning information by the identifier provided in the sign-in url.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_provisioning_info_get_builder()` + `androidmanagement_provisioning_info_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_provisioning_info_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningInfo>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidmanagement_provisioning_info_get_builder(client, name)?;
    androidmanagement_provisioning_info_get_execute(builder)
}

/// GET v1/signupUrls
/// Creates an enterprise signup URL.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidmanagement_signup_urls_create_execute()` to send, or `androidmanagement_signup_urls_create` for simplest API.

pub fn androidmanagement_signup_urls_create_builder(
    client: &SimpleHttpClient,
    adminEmail: Option<&str>,
    allowedDomains: Option<&str>,
    callbackUrl: Option<&str>,
    projectId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://androidmanagement.googleapis.com/v1/signupUrls",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = adminEmail {
        query_parts.push(format!("adminEmail={}", val));
    }
    if let Some(val) = allowedDomains {
        query_parts.push(format!("allowedDomains={}", val));
    }
    if let Some(val) = callbackUrl {
        query_parts.push(format!("callbackUrl={}", val));
    }
    if let Some(val) = projectId {
        query_parts.push(format!("projectId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/signupUrls
/// Creates an enterprise signup URL.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidmanagement_signup_urls_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidmanagement_signup_urls_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_signup_urls_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupUrl>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignupUrl = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/signupUrls
/// Creates an enterprise signup URL.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidmanagement_signup_urls_create_builder()` + `androidmanagement_signup_urls_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidmanagement_signup_urls_create(
    client: &SimpleHttpClient,
    adminEmail: Option<&str>,
    allowedDomains: Option<&str>,
    callbackUrl: Option<&str>,
    projectId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignupUrl>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidmanagement_signup_urls_create_builder(
        client,
        adminEmail,
        allowedDomains,
        callbackUrl,
        projectId,
    )?;
    androidmanagement_signup_urls_create_execute(builder)
}
