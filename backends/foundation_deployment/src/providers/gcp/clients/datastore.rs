//! Auto-generated API clients for gcp/datastore.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectId}:allocateIds
/// Allocates IDs for the given keys, which is useful for referencing an entity before it is inserted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_allocate_ids_execute()` to send, or `datastore_projects_allocate_ids` for simplest API.

pub fn datastore_projects_allocate_ids_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &AllocateIdsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:allocateIds",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:allocateIds
/// Allocates IDs for the given keys, which is useful for referencing an entity before it is inserted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_allocate_ids_execute()` or `datastore_projects_allocate_ids`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_allocate_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_allocate_ids_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AllocateIdsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AllocateIdsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:allocateIds
/// Allocates IDs for the given keys, which is useful for referencing an entity before it is inserted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_allocate_ids_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_allocate_ids_task()`.
/// For the simplest API, use `datastore_projects_allocate_ids()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_allocate_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_allocate_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AllocateIdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_allocate_ids_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_allocate_ids`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsAllocateIdsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: AllocateIdsRequest,
}

/// GET v1/projects/{projectId}:allocateIds
/// Allocates IDs for the given keys, which is useful for referencing an entity before it is inserted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_allocate_ids_builder()` + `datastore_projects_allocate_ids_execute()`.
/// For task-level control, use `datastore_projects_allocate_ids_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_allocate_ids(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsAllocateIdsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AllocateIdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        datastore_projects_allocate_ids_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_allocate_ids_execute(builder)
}

/// GET v1/projects/{projectId}:beginTransaction
/// Begins a new transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_begin_transaction_execute()` to send, or `datastore_projects_begin_transaction` for simplest API.

pub fn datastore_projects_begin_transaction_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &BeginTransactionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:beginTransaction",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:beginTransaction
/// Begins a new transaction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_begin_transaction_execute()` or `datastore_projects_begin_transaction`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_begin_transaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_begin_transaction_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BeginTransactionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BeginTransactionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:beginTransaction
/// Begins a new transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_begin_transaction_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_begin_transaction_task()`.
/// For the simplest API, use `datastore_projects_begin_transaction()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_begin_transaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_begin_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BeginTransactionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_begin_transaction_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_begin_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsBeginTransactionArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: BeginTransactionRequest,
}

/// GET v1/projects/{projectId}:beginTransaction
/// Begins a new transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_begin_transaction_builder()` + `datastore_projects_begin_transaction_execute()`.
/// For task-level control, use `datastore_projects_begin_transaction_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_begin_transaction(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsBeginTransactionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BeginTransactionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        datastore_projects_begin_transaction_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_begin_transaction_execute(builder)
}

/// GET v1/projects/{projectId}:commit
/// Commits a transaction, optionally creating, deleting or modifying some entities.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_commit_execute()` to send, or `datastore_projects_commit` for simplest API.

pub fn datastore_projects_commit_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &CommitRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:commit",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:commit
/// Commits a transaction, optionally creating, deleting or modifying some entities.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_commit_execute()` or `datastore_projects_commit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_commit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:commit
/// Commits a transaction, optionally creating, deleting or modifying some entities.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_commit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_commit_task()`.
/// For the simplest API, use `datastore_projects_commit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_commit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_commit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_commit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsCommitArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: CommitRequest,
}

/// GET v1/projects/{projectId}:commit
/// Commits a transaction, optionally creating, deleting or modifying some entities.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_commit_builder()` + `datastore_projects_commit_execute()`.
/// For task-level control, use `datastore_projects_commit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_commit(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsCommitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_commit_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_commit_execute(builder)
}

/// GET v1/projects/{projectId}:export
/// Exports a copy of all or a subset of entities from Google Cloud Datastore to another storage system, such as Google Cloud Storage. Recent updates to entities may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_export_execute()` to send, or `datastore_projects_export` for simplest API.

pub fn datastore_projects_export_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &GoogleDatastoreAdminV1ExportEntitiesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:export",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:export
/// Exports a copy of all or a subset of entities from Google Cloud Datastore to another storage system, such as Google Cloud Storage. Recent updates to entities may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_export_execute()` or `datastore_projects_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:export
/// Exports a copy of all or a subset of entities from Google Cloud Datastore to another storage system, such as Google Cloud Storage. Recent updates to entities may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_export_task()`.
/// For the simplest API, use `datastore_projects_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsExportArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: GoogleDatastoreAdminV1ExportEntitiesRequest,
}

/// GET v1/projects/{projectId}:export
/// Exports a copy of all or a subset of entities from Google Cloud Datastore to another storage system, such as Google Cloud Storage. Recent updates to entities may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_export_builder()` + `datastore_projects_export_execute()`.
/// For task-level control, use `datastore_projects_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_export(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_export_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_export_execute(builder)
}

/// GET v1/projects/{projectId}:import
/// Imports entities into Google Cloud Datastore. Existing entities with the same key are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportEntities operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Datastore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_import_execute()` to send, or `datastore_projects_import` for simplest API.

pub fn datastore_projects_import_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &GoogleDatastoreAdminV1ImportEntitiesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:import",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:import
/// Imports entities into Google Cloud Datastore. Existing entities with the same key are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportEntities operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Datastore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_import_execute()` or `datastore_projects_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:import
/// Imports entities into Google Cloud Datastore. Existing entities with the same key are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportEntities operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Datastore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_import_task()`.
/// For the simplest API, use `datastore_projects_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsImportArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: GoogleDatastoreAdminV1ImportEntitiesRequest,
}

/// GET v1/projects/{projectId}:import
/// Imports entities into Google Cloud Datastore. Existing entities with the same key are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportEntities operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Datastore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_import_builder()` + `datastore_projects_import_execute()`.
/// For task-level control, use `datastore_projects_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_import(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_import_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_import_execute(builder)
}

/// GET v1/projects/{projectId}:lookup
/// Looks up entities by key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_lookup_execute()` to send, or `datastore_projects_lookup` for simplest API.

pub fn datastore_projects_lookup_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &LookupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:lookup",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:lookup
/// Looks up entities by key.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_lookup_execute()` or `datastore_projects_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:lookup
/// Looks up entities by key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_lookup_task()`.
/// For the simplest API, use `datastore_projects_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsLookupArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: LookupRequest,
}

/// GET v1/projects/{projectId}:lookup
/// Looks up entities by key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_lookup_builder()` + `datastore_projects_lookup_execute()`.
/// For task-level control, use `datastore_projects_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_lookup(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsLookupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_lookup_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_lookup_execute(builder)
}

/// GET v1/projects/{projectId}:reserveIds
/// Prevents the supplied keys' IDs from being auto-allocated by Cloud Datastore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_reserve_ids_execute()` to send, or `datastore_projects_reserve_ids` for simplest API.

pub fn datastore_projects_reserve_ids_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &ReserveIdsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:reserveIds",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:reserveIds
/// Prevents the supplied keys' IDs from being auto-allocated by Cloud Datastore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_reserve_ids_execute()` or `datastore_projects_reserve_ids`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_reserve_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_reserve_ids_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReserveIdsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReserveIdsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:reserveIds
/// Prevents the supplied keys' IDs from being auto-allocated by Cloud Datastore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_reserve_ids_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_reserve_ids_task()`.
/// For the simplest API, use `datastore_projects_reserve_ids()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_reserve_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_reserve_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReserveIdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_reserve_ids_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_reserve_ids`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsReserveIdsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: ReserveIdsRequest,
}

/// GET v1/projects/{projectId}:reserveIds
/// Prevents the supplied keys' IDs from being auto-allocated by Cloud Datastore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_reserve_ids_builder()` + `datastore_projects_reserve_ids_execute()`.
/// For task-level control, use `datastore_projects_reserve_ids_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_reserve_ids(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsReserveIdsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReserveIdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        datastore_projects_reserve_ids_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_reserve_ids_execute(builder)
}

/// GET v1/projects/{projectId}:rollback
/// Rolls back a transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_rollback_execute()` to send, or `datastore_projects_rollback` for simplest API.

pub fn datastore_projects_rollback_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &RollbackRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:rollback",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:rollback
/// Rolls back a transaction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_rollback_execute()` or `datastore_projects_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RollbackResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RollbackResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:rollback
/// Rolls back a transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_rollback_task()`.
/// For the simplest API, use `datastore_projects_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RollbackResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsRollbackArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: RollbackRequest,
}

/// GET v1/projects/{projectId}:rollback
/// Rolls back a transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_rollback_builder()` + `datastore_projects_rollback_execute()`.
/// For task-level control, use `datastore_projects_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_rollback(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsRollbackArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RollbackResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_rollback_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_rollback_execute(builder)
}

/// GET v1/projects/{projectId}:runAggregationQuery
/// Runs an aggregation query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_run_aggregation_query_execute()` to send, or `datastore_projects_run_aggregation_query` for simplest API.

pub fn datastore_projects_run_aggregation_query_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &RunAggregationQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:runAggregationQuery",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:runAggregationQuery
/// Runs an aggregation query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_run_aggregation_query_execute()` or `datastore_projects_run_aggregation_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_run_aggregation_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_run_aggregation_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunAggregationQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunAggregationQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:runAggregationQuery
/// Runs an aggregation query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_run_aggregation_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_run_aggregation_query_task()`.
/// For the simplest API, use `datastore_projects_run_aggregation_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_run_aggregation_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_run_aggregation_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunAggregationQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_run_aggregation_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_run_aggregation_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsRunAggregationQueryArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: RunAggregationQueryRequest,
}

/// GET v1/projects/{projectId}:runAggregationQuery
/// Runs an aggregation query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_run_aggregation_query_builder()` + `datastore_projects_run_aggregation_query_execute()`.
/// For task-level control, use `datastore_projects_run_aggregation_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_run_aggregation_query(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsRunAggregationQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunAggregationQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_run_aggregation_query_builder(
        client,
        args.projectId.clone(),
        &args.body,
    )?;
    datastore_projects_run_aggregation_query_execute(builder)
}

/// GET v1/projects/{projectId}:runQuery
/// Queries for entities.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_run_query_execute()` to send, or `datastore_projects_run_query` for simplest API.

pub fn datastore_projects_run_query_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &RunQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:runQuery",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}:runQuery
/// Queries for entities.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_run_query_execute()` or `datastore_projects_run_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_run_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_run_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}:runQuery
/// Queries for entities.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_run_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_run_query_task()`.
/// For the simplest API, use `datastore_projects_run_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_run_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_run_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_run_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_run_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsRunQueryArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: RunQueryRequest,
}

/// GET v1/projects/{projectId}:runQuery
/// Queries for entities.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_run_query_builder()` + `datastore_projects_run_query_execute()`.
/// For task-level control, use `datastore_projects_run_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_run_query(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsRunQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_run_query_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_run_query_execute(builder)
}

/// GET v1/projects/{projectId}/indexes
/// Creates the specified index. A newly created index's initial state is CREATING. On completion of the returned google.longrunning.Operation, the state will be READY. If the index already exists, the call will return an ALREADY_EXISTS status. During index creation, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, removing the index with delete, then re-creating the index with create. Indexes with a single property cannot be created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_indexes_create_execute()` to send, or `datastore_projects_indexes_create` for simplest API.

pub fn datastore_projects_indexes_create_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &GoogleDatastoreAdminV1Index,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}/indexes",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/indexes
/// Creates the specified index. A newly created index's initial state is CREATING. On completion of the returned google.longrunning.Operation, the state will be READY. If the index already exists, the call will return an ALREADY_EXISTS status. During index creation, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, removing the index with delete, then re-creating the index with create. Indexes with a single property cannot be created.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_indexes_create_execute()` or `datastore_projects_indexes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_indexes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_indexes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/indexes
/// Creates the specified index. A newly created index's initial state is CREATING. On completion of the returned google.longrunning.Operation, the state will be READY. If the index already exists, the call will return an ALREADY_EXISTS status. During index creation, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, removing the index with delete, then re-creating the index with create. Indexes with a single property cannot be created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_indexes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_indexes_create_task()`.
/// For the simplest API, use `datastore_projects_indexes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_indexes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_indexes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_indexes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_indexes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsIndexesCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: GoogleDatastoreAdminV1Index,
}

/// GET v1/projects/{projectId}/indexes
/// Creates the specified index. A newly created index's initial state is CREATING. On completion of the returned google.longrunning.Operation, the state will be READY. If the index already exists, the call will return an ALREADY_EXISTS status. During index creation, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, removing the index with delete, then re-creating the index with create. Indexes with a single property cannot be created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_indexes_create_builder()` + `datastore_projects_indexes_create_execute()`.
/// For task-level control, use `datastore_projects_indexes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_indexes_create(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsIndexesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datastore_projects_indexes_create_builder(client, args.projectId.clone(), &args.body)?;
    datastore_projects_indexes_create_execute(builder)
}

/// GET v1/projects/{projectId}/indexes/{indexId}
/// Deletes an existing index. An index can only be deleted if it is in a READY or `ERROR` state. On successful execution of the request, the index will be in a DELETING state. And on completion of the returned google.longrunning.Operation, the index will be removed. During index deletion, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, followed by calling delete again.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_indexes_delete_execute()` to send, or `datastore_projects_indexes_delete` for simplest API.

pub fn datastore_projects_indexes_delete_builder(
    client: &SimpleHttpClient,
    projectId: String,
    indexId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}/indexes/{}",
        projectId.as_str(),
        indexId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/indexes/{indexId}
/// Deletes an existing index. An index can only be deleted if it is in a READY or `ERROR` state. On successful execution of the request, the index will be in a DELETING state. And on completion of the returned google.longrunning.Operation, the index will be removed. During index deletion, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, followed by calling delete again.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_indexes_delete_execute()` or `datastore_projects_indexes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_indexes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_indexes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/indexes/{indexId}
/// Deletes an existing index. An index can only be deleted if it is in a READY or `ERROR` state. On successful execution of the request, the index will be in a DELETING state. And on completion of the returned google.longrunning.Operation, the index will be removed. During index deletion, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, followed by calling delete again.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_indexes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_indexes_delete_task()`.
/// For the simplest API, use `datastore_projects_indexes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_indexes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_indexes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_indexes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_indexes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsIndexesDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: indexId
    pub indexId: String,
}

/// GET v1/projects/{projectId}/indexes/{indexId}
/// Deletes an existing index. An index can only be deleted if it is in a READY or `ERROR` state. On successful execution of the request, the index will be in a DELETING state. And on completion of the returned google.longrunning.Operation, the index will be removed. During index deletion, the process could result in an error, in which case the index will move to the `ERROR` state. The process can be recovered by fixing the data that caused the error, followed by calling delete again.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_indexes_delete_builder()` + `datastore_projects_indexes_delete_execute()`.
/// For task-level control, use `datastore_projects_indexes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_indexes_delete(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsIndexesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_indexes_delete_builder(
        client,
        args.projectId.clone(),
        args.indexId.clone(),
    )?;
    datastore_projects_indexes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datastore_projects_operations_list_execute()` to send, or `datastore_projects_operations_list` for simplest API.

pub fn datastore_projects_operations_list_builder(
    client: &SimpleHttpClient,
    name: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://datastore.googleapis.com/v1/projects/{}/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datastore_projects_operations_list_execute()` or `datastore_projects_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datastore_projects_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datastore_projects_operations_list_task()`.
/// For the simplest API, use `datastore_projects_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datastore_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datastore_projects_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datastore_projects_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datastore_projects_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datastore_projects_operations_list_builder()` + `datastore_projects_operations_list_execute()`.
/// For task-level control, use `datastore_projects_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datastore_projects_operations_list(
    client: &SimpleHttpClient,
    args: &DatastoreProjectsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datastore_projects_operations_list_builder(
        client,
        args.name.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.returnPartialSuccess.clone(),
    )?;
    datastore_projects_operations_list_execute(builder)
}
