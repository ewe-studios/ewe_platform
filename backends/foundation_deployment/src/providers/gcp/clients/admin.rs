//! Auto-generated API clients for gcp/admin.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}
/// Retrieves a list of activities for a specific customer''s account and application such as the Admin console application or the Google Drive application. For more information, see the guides for administrator and Google Drive activity reports. For more information about the activity report''s parameters, see the activity parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_activities_list_execute()` to send, or `reports_activities_list` for simplest API.

pub fn reports_activities_list_builder<R>(
    client: &SimpleHttpClient<R>,
    userKey: &String,
    applicationName: &String,
    actorIpAddress: &Option<String>,
    applicationInfoFilter: &Option<String>,
    customerId: &Option<String>,
    endTime: &Option<String>,
    eventName: &Option<String>,
    filters: &Option<String>,
    groupIdFilter: &Option<String>,
    includeSensitiveData: &Option<String>,
    maxResults: &Option<String>,
    networkInfoFilter: &Option<String>,
    orgUnitID: &Option<String>,
    pageToken: &Option<String>,
    resourceDetailsFilter: &Option<String>,
    startTime: &Option<String>,
    statusFilter: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://admin.googleapis.com/admin/reports/v1/activity/users/{}/applications/{}",
        userKey, applicationName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = actorIpAddress.as_ref() {
        query_parts.push(format!("actorIpAddress={}", val));
    }
    if let Some(val) = applicationInfoFilter.as_ref() {
        query_parts.push(format!("applicationInfoFilter={}", val));
    }
    if let Some(val) = customerId.as_ref() {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = endTime.as_ref() {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = eventName.as_ref() {
        query_parts.push(format!("eventName={}", val));
    }
    if let Some(val) = filters.as_ref() {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = groupIdFilter.as_ref() {
        query_parts.push(format!("groupIdFilter={}", val));
    }
    if let Some(val) = includeSensitiveData.as_ref() {
        query_parts.push(format!("includeSensitiveData={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = networkInfoFilter.as_ref() {
        query_parts.push(format!("networkInfoFilter={}", val));
    }
    if let Some(val) = orgUnitID.as_ref() {
        query_parts.push(format!("orgUnitID={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceDetailsFilter.as_ref() {
        query_parts.push(format!("resourceDetailsFilter={}", val));
    }
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = statusFilter.as_ref() {
        query_parts.push(format!("statusFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}
/// Retrieves a list of activities for a specific customer''s account and application such as the Admin console application or the Google Drive application. For more information, see the guides for administrator and Google Drive activity reports. For more information about the activity report''s parameters, see the activity parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reports_activities_list_execute()` or `reports_activities_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_activities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Activities>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Activities = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}
/// Retrieves a list of activities for a specific customer''s account and application such as the Admin console application or the Google Drive application. For more information, see the guides for administrator and Google Drive activity reports. For more information about the activity report''s parameters, see the activity parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reports_activities_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reports_activities_list_task()`.
/// For the simplest API, use `reports_activities_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_activities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reports_activities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Activities>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reports_activities_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_activities_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsActivitiesListArgs {
    /// Path parameter: userKey
    pub userKey: String,
    /// Path parameter: applicationName
    pub applicationName: String,
    /// Query parameter: actorIpAddress
    pub actorIpAddress: Option<String>,
    /// Query parameter: applicationInfoFilter
    pub applicationInfoFilter: Option<String>,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: eventName
    pub eventName: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: groupIdFilter
    pub groupIdFilter: Option<String>,
    /// Query parameter: includeSensitiveData
    pub includeSensitiveData: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: networkInfoFilter
    pub networkInfoFilter: Option<String>,
    /// Query parameter: orgUnitID
    pub orgUnitID: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceDetailsFilter
    pub resourceDetailsFilter: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: statusFilter
    pub statusFilter: Option<String>,
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}
/// Retrieves a list of activities for a specific customer''s account and application such as the Admin console application or the Google Drive application. For more information, see the guides for administrator and Google Drive activity reports. For more information about the activity report''s parameters, see the activity parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_activities_list_builder()` + `reports_activities_list_execute()`.
/// For task-level control, use `reports_activities_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_list(
    client: &SimpleHttpClient,
    args: &ReportsActivitiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Activities>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reports_activities_list_builder(
        client,
        &args.userKey,
        &args.applicationName,
        &args.actorIpAddress,
        &args.applicationInfoFilter,
        &args.customerId,
        &args.endTime,
        &args.eventName,
        &args.filters,
        &args.groupIdFilter,
        &args.includeSensitiveData,
        &args.maxResults,
        &args.networkInfoFilter,
        &args.orgUnitID,
        &args.pageToken,
        &args.resourceDetailsFilter,
        &args.startTime,
        &args.statusFilter,
    )?;
    reports_activities_list_execute(builder)
}

/// POST admin/reports/v1/activity/users/{userKey}/applications/{applicationName}/watch
/// Start receiving notifications for account activities. For more information, see Receiving Push Notifications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_activities_watch_execute()` to send, or `reports_activities_watch` for simplest API.

pub fn reports_activities_watch_builder<R>(
    client: &SimpleHttpClient<R>,
    userKey: &String,
    applicationName: &String,
    actorIpAddress: &Option<String>,
    customerId: &Option<String>,
    endTime: &Option<String>,
    eventName: &Option<String>,
    filters: &Option<String>,
    groupIdFilter: &Option<String>,
    maxResults: &Option<String>,
    orgUnitID: &Option<String>,
    pageToken: &Option<String>,
    startTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://admin.googleapis.com/admin/reports/v1/activity/users/{}/applications/{}/watch",
        userKey, applicationName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = actorIpAddress.as_ref() {
        query_parts.push(format!("actorIpAddress={}", val));
    }
    if let Some(val) = customerId.as_ref() {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = endTime.as_ref() {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = eventName.as_ref() {
        query_parts.push(format!("eventName={}", val));
    }
    if let Some(val) = filters.as_ref() {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = groupIdFilter.as_ref() {
        query_parts.push(format!("groupIdFilter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orgUnitID.as_ref() {
        query_parts.push(format!("orgUnitID={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST admin/reports/v1/activity/users/{userKey}/applications/{applicationName}/watch
/// Start receiving notifications for account activities. For more information, see Receiving Push Notifications.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reports_activities_watch_execute()` or `reports_activities_watch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_activities_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_watch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST admin/reports/v1/activity/users/{userKey}/applications/{applicationName}/watch
/// Start receiving notifications for account activities. For more information, see Receiving Push Notifications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reports_activities_watch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reports_activities_watch_task()`.
/// For the simplest API, use `reports_activities_watch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_activities_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reports_activities_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reports_activities_watch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_activities_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsActivitiesWatchArgs {
    /// Path parameter: userKey
    pub userKey: String,
    /// Path parameter: applicationName
    pub applicationName: String,
    /// Query parameter: actorIpAddress
    pub actorIpAddress: Option<String>,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: eventName
    pub eventName: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: groupIdFilter
    pub groupIdFilter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: orgUnitID
    pub orgUnitID: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
}

/// POST admin/reports/v1/activity/users/{userKey}/applications/{applicationName}/watch
/// Start receiving notifications for account activities. For more information, see Receiving Push Notifications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_activities_watch_builder()` + `reports_activities_watch_execute()`.
/// For task-level control, use `reports_activities_watch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_watch(
    client: &SimpleHttpClient,
    args: &ReportsActivitiesWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reports_activities_watch_builder(
        client,
        &args.userKey,
        &args.applicationName,
        &args.actorIpAddress,
        &args.customerId,
        &args.endTime,
        &args.eventName,
        &args.filters,
        &args.groupIdFilter,
        &args.maxResults,
        &args.orgUnitID,
        &args.pageToken,
        &args.startTime,
    )?;
    reports_activities_watch_execute(builder)
}

/// POST admin/reports_v1/channels/stop
/// Stop watching resources through this channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `admin_channels_stop_execute()` to send, or `admin_channels_stop` for simplest API.

pub fn admin_channels_stop_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://admin.googleapis.com/admin/reports_v1/channels/stop",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST admin/reports_v1/channels/stop
/// Stop watching resources through this channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `admin_channels_stop_execute()` or `admin_channels_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `admin_channels_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn admin_channels_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST admin/reports_v1/channels/stop
/// Stop watching resources through this channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `admin_channels_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `admin_channels_stop_task()`.
/// For the simplest API, use `admin_channels_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `admin_channels_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn admin_channels_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = admin_channels_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST admin/reports_v1/channels/stop
/// Stop watching resources through this channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `admin_channels_stop_builder()` + `admin_channels_stop_execute()`.
/// For task-level control, use `admin_channels_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn admin_channels_stop(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = admin_channels_stop_builder(client)?;
    admin_channels_stop_execute(builder)
}

/// GET admin/reports/v1/usage/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a specific customer''s account. For more information, see the Customers Usage Report guide. For more information about the customer report''s parameters, see the Customers Usage parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_customer_usage_reports_get_execute()` to send, or `reports_customer_usage_reports_get` for simplest API.

pub fn reports_customer_usage_reports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    date: &String,
    customerId: &Option<String>,
    pageToken: &Option<String>,
    parameters: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://admin.googleapis.com/admin/reports/v1/usage/dates/{}",
        date,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId.as_ref() {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parameters.as_ref() {
        query_parts.push(format!("parameters={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/usage/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a specific customer''s account. For more information, see the Customers Usage Report guide. For more information about the customer report''s parameters, see the Customers Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reports_customer_usage_reports_get_execute()` or `reports_customer_usage_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_customer_usage_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_customer_usage_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsageReports>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsageReports = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET admin/reports/v1/usage/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a specific customer''s account. For more information, see the Customers Usage Report guide. For more information about the customer report''s parameters, see the Customers Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reports_customer_usage_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reports_customer_usage_reports_get_task()`.
/// For the simplest API, use `reports_customer_usage_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_customer_usage_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reports_customer_usage_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reports_customer_usage_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_customer_usage_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsCustomerUsageReportsGetArgs {
    /// Path parameter: date
    pub date: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parameters
    pub parameters: Option<String>,
}

/// GET admin/reports/v1/usage/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a specific customer''s account. For more information, see the Customers Usage Report guide. For more information about the customer report''s parameters, see the Customers Usage parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_customer_usage_reports_get_builder()` + `reports_customer_usage_reports_get_execute()`.
/// For task-level control, use `reports_customer_usage_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_customer_usage_reports_get(
    client: &SimpleHttpClient,
    args: &ReportsCustomerUsageReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reports_customer_usage_reports_get_builder(
        client,
        &args.date,
        &args.customerId,
        &args.pageToken,
        &args.parameters,
    )?;
    reports_customer_usage_reports_get_execute(builder)
}

/// GET admin/reports/v1/usage/{entityType}/{entityKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for entities used by users within the account. For more information, see the Entities Usage Report guide. For more information about the entities report''s parameters, see the Entities Usage parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_entity_usage_reports_get_execute()` to send, or `reports_entity_usage_reports_get` for simplest API.

pub fn reports_entity_usage_reports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    entityType: &String,
    entityKey: &String,
    date: &String,
    customerId: &Option<String>,
    filters: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
    parameters: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://admin.googleapis.com/admin/reports/v1/usage/{}/{}/dates/{}",
        entityType, entityKey, date,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId.as_ref() {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = filters.as_ref() {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parameters.as_ref() {
        query_parts.push(format!("parameters={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/usage/{entityType}/{entityKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for entities used by users within the account. For more information, see the Entities Usage Report guide. For more information about the entities report''s parameters, see the Entities Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reports_entity_usage_reports_get_execute()` or `reports_entity_usage_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_entity_usage_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_entity_usage_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsageReports>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsageReports = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET admin/reports/v1/usage/{entityType}/{entityKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for entities used by users within the account. For more information, see the Entities Usage Report guide. For more information about the entities report''s parameters, see the Entities Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reports_entity_usage_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reports_entity_usage_reports_get_task()`.
/// For the simplest API, use `reports_entity_usage_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_entity_usage_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reports_entity_usage_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reports_entity_usage_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_entity_usage_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsEntityUsageReportsGetArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Path parameter: entityKey
    pub entityKey: String,
    /// Path parameter: date
    pub date: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parameters
    pub parameters: Option<String>,
}

/// GET admin/reports/v1/usage/{entityType}/{entityKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for entities used by users within the account. For more information, see the Entities Usage Report guide. For more information about the entities report''s parameters, see the Entities Usage parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_entity_usage_reports_get_builder()` + `reports_entity_usage_reports_get_execute()`.
/// For task-level control, use `reports_entity_usage_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_entity_usage_reports_get(
    client: &SimpleHttpClient,
    args: &ReportsEntityUsageReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reports_entity_usage_reports_get_builder(
        client,
        &args.entityType,
        &args.entityKey,
        &args.date,
        &args.customerId,
        &args.filters,
        &args.maxResults,
        &args.pageToken,
        &args.parameters,
    )?;
    reports_entity_usage_reports_get_execute(builder)
}

/// GET admin/reports/v1/usage/users/{userKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a set of users with the account. For more information, see the User Usage Report guide. For more information about the user report''s parameters, see the Users Usage parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_user_usage_report_get_execute()` to send, or `reports_user_usage_report_get` for simplest API.

pub fn reports_user_usage_report_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userKey: &String,
    date: &String,
    customerId: &Option<String>,
    filters: &Option<String>,
    groupIdFilter: &Option<String>,
    maxResults: &Option<String>,
    orgUnitID: &Option<String>,
    pageToken: &Option<String>,
    parameters: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://admin.googleapis.com/admin/reports/v1/usage/users/{}/dates/{}",
        userKey, date,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId.as_ref() {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = filters.as_ref() {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = groupIdFilter.as_ref() {
        query_parts.push(format!("groupIdFilter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orgUnitID.as_ref() {
        query_parts.push(format!("orgUnitID={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parameters.as_ref() {
        query_parts.push(format!("parameters={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/usage/users/{userKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a set of users with the account. For more information, see the User Usage Report guide. For more information about the user report''s parameters, see the Users Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reports_user_usage_report_get_execute()` or `reports_user_usage_report_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_user_usage_report_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_user_usage_report_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsageReports>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsageReports = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET admin/reports/v1/usage/users/{userKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a set of users with the account. For more information, see the User Usage Report guide. For more information about the user report''s parameters, see the Users Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reports_user_usage_report_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reports_user_usage_report_get_task()`.
/// For the simplest API, use `reports_user_usage_report_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reports_user_usage_report_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reports_user_usage_report_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = reports_user_usage_report_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_user_usage_report_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsUserUsageReportGetArgs {
    /// Path parameter: userKey
    pub userKey: String,
    /// Path parameter: date
    pub date: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: groupIdFilter
    pub groupIdFilter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: orgUnitID
    pub orgUnitID: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parameters
    pub parameters: Option<String>,
}

/// GET admin/reports/v1/usage/users/{userKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a set of users with the account. For more information, see the User Usage Report guide. For more information about the user report''s parameters, see the Users Usage parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_user_usage_report_get_builder()` + `reports_user_usage_report_get_execute()`.
/// For task-level control, use `reports_user_usage_report_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_user_usage_report_get(
    client: &SimpleHttpClient,
    args: &ReportsUserUsageReportGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reports_user_usage_report_get_builder(
        client,
        &args.userKey,
        &args.date,
        &args.customerId,
        &args.filters,
        &args.groupIdFilter,
        &args.maxResults,
        &args.orgUnitID,
        &args.pageToken,
        &args.parameters,
    )?;
    reports_user_usage_report_get_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Activities
// =============================================================================

/// ResourceIdentifier implementation for Activities with ReportsActivitiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReportsActivitiesListArgs> for Activities {
    fn generate_resource_id(&self, input: &ReportsActivitiesListArgs) -> String {
        format!(
            "gcp::admin::Activities/{}/{}",
            input.userKey, input.applicationName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::admin::Activities"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Channel
// =============================================================================

/// ResourceIdentifier implementation for Channel with ReportsActivitiesWatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReportsActivitiesWatchArgs> for Channel {
    fn generate_resource_id(&self, input: &ReportsActivitiesWatchArgs) -> String {
        format!(
            "gcp::admin::Channel/{}/{}",
            input.userKey, input.applicationName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::admin::Channel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UsageReports
// =============================================================================

/// ResourceIdentifier implementation for UsageReports with ReportsCustomerUsageReportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReportsCustomerUsageReportsGetArgs> for UsageReports {
    fn generate_resource_id(&self, input: &ReportsCustomerUsageReportsGetArgs) -> String {
        format!("gcp::admin::UsageReports/{}", input.date)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::admin::UsageReports"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UsageReports
// =============================================================================

/// ResourceIdentifier implementation for UsageReports with ReportsEntityUsageReportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReportsEntityUsageReportsGetArgs> for UsageReports {
    fn generate_resource_id(&self, input: &ReportsEntityUsageReportsGetArgs) -> String {
        format!(
            "gcp::admin::UsageReports/{}/{}/{}",
            input.entityType, input.entityKey, input.date
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::admin::UsageReports"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UsageReports
// =============================================================================

/// ResourceIdentifier implementation for UsageReports with ReportsUserUsageReportGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReportsUserUsageReportGetArgs> for UsageReports {
    fn generate_resource_id(&self, input: &ReportsUserUsageReportGetArgs) -> String {
        format!("gcp::admin::UsageReports/{}/{}", input.userKey, input.date)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::admin::UsageReports"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
