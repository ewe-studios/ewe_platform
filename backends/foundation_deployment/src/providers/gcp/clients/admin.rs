//! Auto-generated API clients for gcp/admin.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/admin`

#![cfg(feature = "gcp/admin")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}
/// Retrieves a list of activities for a specific customer's account and application such as the Admin console application or the Google Drive application. For more information, see the guides for administrator and Google Drive activity reports. For more information about the activity report's parameters, see the activity parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_activities_list_execute()` to send, or `reports_activities_list` for simplest API.

pub fn reports_activities_list_builder(
    client: &SimpleHttpClient,
    userKey: &str,
    applicationName: &str,
    actorIpAddress: Option<&str>,
    applicationInfoFilter: Option<&str>,
    customerId: Option<&str>,
    endTime: Option<&str>,
    eventName: Option<&str>,
    filters: Option<&str>,
    groupIdFilter: Option<&str>,
    includeSensitiveData: Option<bool>,
    maxResults: Option<i32>,
    networkInfoFilter: Option<&str>,
    orgUnitID: Option<&str>,
    pageToken: Option<&str>,
    resourceDetailsFilter: Option<&str>,
    startTime: Option<&str>,
    statusFilter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://admin.googleapis.com/admin/reports/v1/activity/users/{}/applications/{}",
        userKey, applicationName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = actorIpAddress {
        query_parts.push(format!("actorIpAddress={}", val));
    }
    if let Some(val) = applicationInfoFilter {
        query_parts.push(format!("applicationInfoFilter={}", val));
    }
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = eventName {
        query_parts.push(format!("eventName={}", val));
    }
    if let Some(val) = filters {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = groupIdFilter {
        query_parts.push(format!("groupIdFilter={}", val));
    }
    if let Some(val) = includeSensitiveData {
        query_parts.push(format!("includeSensitiveData={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = networkInfoFilter {
        query_parts.push(format!("networkInfoFilter={}", val));
    }
    if let Some(val) = orgUnitID {
        query_parts.push(format!("orgUnitID={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceDetailsFilter {
        query_parts.push(format!("resourceDetailsFilter={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = statusFilter {
        query_parts.push(format!("statusFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}
/// Retrieves a list of activities for a specific customer's account and application such as the Admin console application or the Google Drive application. For more information, see the guides for administrator and Google Drive activity reports. For more information about the activity report's parameters, see the activity parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `reports_activities_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from reports_activities_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Activities>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Activities = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_activities_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsActivitiesListArgs {
    /// Path parameter: userKey
    pub userKey: String,
    /// Path parameter: applicationName
    pub applicationName: String,
    /// Query parameter: actorIpAddress
    pub actorIpAddress: Option<String>,
    /// Query parameter: applicationInfoFilter
    pub applicationInfoFilter: Option<String>,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: eventName
    pub eventName: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: groupIdFilter
    pub groupIdFilter: Option<String>,
    /// Query parameter: includeSensitiveData
    pub includeSensitiveData: Option<bool>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: networkInfoFilter
    pub networkInfoFilter: Option<String>,
    /// Query parameter: orgUnitID
    pub orgUnitID: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceDetailsFilter
    pub resourceDetailsFilter: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: statusFilter
    pub statusFilter: Option<String>,
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}
/// Retrieves a list of activities for a specific customer's account and application such as the Admin console application or the Google Drive application. For more information, see the guides for administrator and Google Drive activity reports. For more information about the activity report's parameters, see the activity parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_activities_list_builder()` + `reports_activities_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_list(
    client: &SimpleHttpClient,
    args: &ReportsActivitiesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Activities>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reports_activities_list_builder(
        client,
        &args.userKey,
        &args.applicationName,
        args.actorIpAddress.as_deref(),
        args.applicationInfoFilter.as_deref(),
        args.customerId.as_deref(),
        args.endTime.as_deref(),
        args.eventName.as_deref(),
        args.filters.as_deref(),
        args.groupIdFilter.as_deref(),
        args.includeSensitiveData,
        args.maxResults,
        args.networkInfoFilter.as_deref(),
        args.orgUnitID.as_deref(),
        args.pageToken.as_deref(),
        args.resourceDetailsFilter.as_deref(),
        args.startTime.as_deref(),
        args.statusFilter.as_deref(),
    )?;
    reports_activities_list_execute(builder)
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}/watch
/// Start receiving notifications for account activities. For more information, see Receiving Push Notifications.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_activities_watch_execute()` to send, or `reports_activities_watch` for simplest API.

pub fn reports_activities_watch_builder(
    client: &SimpleHttpClient,
    userKey: &str,
    applicationName: &str,
    actorIpAddress: Option<&str>,
    customerId: Option<&str>,
    endTime: Option<&str>,
    eventName: Option<&str>,
    filters: Option<&str>,
    groupIdFilter: Option<&str>,
    maxResults: Option<i32>,
    orgUnitID: Option<&str>,
    pageToken: Option<&str>,
    startTime: Option<&str>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://admin.googleapis.com/admin/reports/v1/activity/users/{}/applications/{}/watch",
        userKey, applicationName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = actorIpAddress {
        query_parts.push(format!("actorIpAddress={}", val));
    }
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = eventName {
        query_parts.push(format!("eventName={}", val));
    }
    if let Some(val) = filters {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = groupIdFilter {
        query_parts.push(format!("groupIdFilter={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orgUnitID {
        query_parts.push(format!("orgUnitID={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}/watch
/// Start receiving notifications for account activities. For more information, see Receiving Push Notifications.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `reports_activities_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from reports_activities_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_activities_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsActivitiesWatchArgs {
    /// Path parameter: userKey
    pub userKey: String,
    /// Path parameter: applicationName
    pub applicationName: String,
    /// Query parameter: actorIpAddress
    pub actorIpAddress: Option<String>,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: eventName
    pub eventName: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: groupIdFilter
    pub groupIdFilter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: orgUnitID
    pub orgUnitID: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// GET admin/reports/v1/activity/users/{userKey}/applications/{applicationName}/watch
/// Start receiving notifications for account activities. For more information, see Receiving Push Notifications.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_activities_watch_builder()` + `reports_activities_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_activities_watch(
    client: &SimpleHttpClient,
    args: &ReportsActivitiesWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reports_activities_watch_builder(
        client,
        &args.userKey,
        &args.applicationName,
        args.actorIpAddress.as_deref(),
        args.customerId.as_deref(),
        args.endTime.as_deref(),
        args.eventName.as_deref(),
        args.filters.as_deref(),
        args.groupIdFilter.as_deref(),
        args.maxResults,
        args.orgUnitID.as_deref(),
        args.pageToken.as_deref(),
        args.startTime.as_deref(),
        &args.body,
    )?;
    reports_activities_watch_execute(builder)
}

/// GET admin/reports_v1/channels/stop
/// Stop watching resources through this channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `admin_channels_stop_execute()` to send, or `admin_channels_stop` for simplest API.

pub fn admin_channels_stop_builder(
    client: &SimpleHttpClient,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://admin.googleapis.com/admin/reports_v1/channels/stop",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET admin/reports_v1/channels/stop
/// Stop watching resources through this channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `admin_channels_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from admin_channels_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn admin_channels_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`admin_channels_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdminChannelsStopArgs {
    /// Request body.
    pub body: Channel,
}

/// GET admin/reports_v1/channels/stop
/// Stop watching resources through this channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `admin_channels_stop_builder()` + `admin_channels_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn admin_channels_stop(
    client: &SimpleHttpClient,
    args: &AdminChannelsStopArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = admin_channels_stop_builder(client, &args.body)?;
    admin_channels_stop_execute(builder)
}

/// GET admin/reports/v1/usage/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a specific customer's account. For more information, see the Customers Usage Report guide. For more information about the customer report's parameters, see the Customers Usage parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_customer_usage_reports_get_execute()` to send, or `reports_customer_usage_reports_get` for simplest API.

pub fn reports_customer_usage_reports_get_builder(
    client: &SimpleHttpClient,
    date: &str,
    customerId: Option<&str>,
    pageToken: Option<&str>,
    parameters: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://admin.googleapis.com/admin/reports/v1/usage/dates/{}",
        date,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parameters {
        query_parts.push(format!("parameters={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/usage/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a specific customer's account. For more information, see the Customers Usage Report guide. For more information about the customer report's parameters, see the Customers Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `reports_customer_usage_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from reports_customer_usage_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_customer_usage_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsageReports = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_customer_usage_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsCustomerUsageReportsGetArgs {
    /// Path parameter: date
    pub date: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parameters
    pub parameters: Option<String>,
}

/// GET admin/reports/v1/usage/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a specific customer's account. For more information, see the Customers Usage Report guide. For more information about the customer report's parameters, see the Customers Usage parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_customer_usage_reports_get_builder()` + `reports_customer_usage_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_customer_usage_reports_get(
    client: &SimpleHttpClient,
    args: &ReportsCustomerUsageReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reports_customer_usage_reports_get_builder(
        client,
        &args.date,
        args.customerId.as_deref(),
        args.pageToken.as_deref(),
        args.parameters.as_deref(),
    )?;
    reports_customer_usage_reports_get_execute(builder)
}

/// GET admin/reports/v1/usage/{entityType}/{entityKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for entities used by users within the account. For more information, see the Entities Usage Report guide. For more information about the entities report's parameters, see the Entities Usage parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_entity_usage_reports_get_execute()` to send, or `reports_entity_usage_reports_get` for simplest API.

pub fn reports_entity_usage_reports_get_builder(
    client: &SimpleHttpClient,
    entityType: &str,
    entityKey: &str,
    date: &str,
    customerId: Option<&str>,
    filters: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    parameters: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://admin.googleapis.com/admin/reports/v1/usage/{}/{}/dates/{}",
        entityType, entityKey, date,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = filters {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parameters {
        query_parts.push(format!("parameters={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/usage/{entityType}/{entityKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for entities used by users within the account. For more information, see the Entities Usage Report guide. For more information about the entities report's parameters, see the Entities Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `reports_entity_usage_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from reports_entity_usage_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_entity_usage_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsageReports = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_entity_usage_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsEntityUsageReportsGetArgs {
    /// Path parameter: entityType
    pub entityType: String,
    /// Path parameter: entityKey
    pub entityKey: String,
    /// Path parameter: date
    pub date: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parameters
    pub parameters: Option<String>,
}

/// GET admin/reports/v1/usage/{entityType}/{entityKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for entities used by users within the account. For more information, see the Entities Usage Report guide. For more information about the entities report's parameters, see the Entities Usage parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_entity_usage_reports_get_builder()` + `reports_entity_usage_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_entity_usage_reports_get(
    client: &SimpleHttpClient,
    args: &ReportsEntityUsageReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reports_entity_usage_reports_get_builder(
        client,
        &args.entityType,
        &args.entityKey,
        &args.date,
        args.customerId.as_deref(),
        args.filters.as_deref(),
        args.maxResults,
        args.pageToken.as_deref(),
        args.parameters.as_deref(),
    )?;
    reports_entity_usage_reports_get_execute(builder)
}

/// GET admin/reports/v1/usage/users/{userKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a set of users with the account. For more information, see the User Usage Report guide. For more information about the user report's parameters, see the Users Usage parameters reference guides.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reports_user_usage_report_get_execute()` to send, or `reports_user_usage_report_get` for simplest API.

pub fn reports_user_usage_report_get_builder(
    client: &SimpleHttpClient,
    userKey: &str,
    date: &str,
    customerId: Option<&str>,
    filters: Option<&str>,
    groupIdFilter: Option<&str>,
    maxResults: Option<i32>,
    orgUnitID: Option<&str>,
    pageToken: Option<&str>,
    parameters: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://admin.googleapis.com/admin/reports/v1/usage/users/{}/dates/{}",
        userKey, date,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = filters {
        query_parts.push(format!("filters={}", val));
    }
    if let Some(val) = groupIdFilter {
        query_parts.push(format!("groupIdFilter={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orgUnitID {
        query_parts.push(format!("orgUnitID={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parameters {
        query_parts.push(format!("parameters={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET admin/reports/v1/usage/users/{userKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a set of users with the account. For more information, see the User Usage Report guide. For more information about the user report's parameters, see the Users Usage parameters reference guides.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `reports_user_usage_report_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from reports_user_usage_report_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_user_usage_report_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsageReports = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reports_user_usage_report_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReportsUserUsageReportGetArgs {
    /// Path parameter: userKey
    pub userKey: String,
    /// Path parameter: date
    pub date: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: filters
    pub filters: Option<String>,
    /// Query parameter: groupIdFilter
    pub groupIdFilter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: orgUnitID
    pub orgUnitID: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parameters
    pub parameters: Option<String>,
}

/// GET admin/reports/v1/usage/users/{userKey}/dates/{date}
/// Retrieves a report which is a collection of properties and statistics for a set of users with the account. For more information, see the User Usage Report guide. For more information about the user report's parameters, see the Users Usage parameters reference guides.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reports_user_usage_report_get_builder()` + `reports_user_usage_report_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reports_user_usage_report_get(
    client: &SimpleHttpClient,
    args: &ReportsUserUsageReportGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsageReports>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = reports_user_usage_report_get_builder(
        client,
        &args.userKey,
        &args.date,
        args.customerId.as_deref(),
        args.filters.as_deref(),
        args.groupIdFilter.as_deref(),
        args.maxResults,
        args.orgUnitID.as_deref(),
        args.pageToken.as_deref(),
        args.parameters.as_deref(),
    )?;
    reports_user_usage_report_get_execute(builder)
}
