//! Auto-generated API clients for gcp/playcustomapp.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET playcustomapp/v1/accounts/{account}/customApps
/// Creates a new custom app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playcustomapp_accounts_custom_apps_create_execute()` to send, or `playcustomapp_accounts_custom_apps_create` for simplest API.

pub fn playcustomapp_accounts_custom_apps_create_builder(
    client: &SimpleHttpClient,
    account: &str,
    body: &CustomApp,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.complaycustomapp/v1/accounts/{}/customApps",
        account,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET playcustomapp/v1/accounts/{account}/customApps
/// Creates a new custom app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `playcustomapp_accounts_custom_apps_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from playcustomapp_accounts_custom_apps_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playcustomapp_accounts_custom_apps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET playcustomapp/v1/accounts/{account}/customApps
/// Creates a new custom app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playcustomapp_accounts_custom_apps_create_builder()` + `playcustomapp_accounts_custom_apps_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playcustomapp_accounts_custom_apps_create(
    client: &SimpleHttpClient,
    account: &str,
    body: &CustomApp,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomApp>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = playcustomapp_accounts_custom_apps_create_builder(client, account, body)?;
    playcustomapp_accounts_custom_apps_create_execute(builder)
}
