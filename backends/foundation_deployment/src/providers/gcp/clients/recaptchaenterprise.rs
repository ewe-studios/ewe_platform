//! Auto-generated API clients for Google Cloud Platform - recaptchaenterprise.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}:annotate
/// Annotates a previously created Assessment to provide additional information on whether the event turned out to be authentic or fraudulent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_assessments_annotate_execute()` to send, or `recaptchaenterprise_projects_assessments_annotate` for simplest API.

pub fn recaptchaenterprise_projects_assessments_annotate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:annotate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:annotate
/// Annotates a previously created Assessment to provide additional information on whether the event turned out to be authentic or fraudulent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_assessments_annotate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_assessments_annotate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_assessments_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:annotate
/// Annotates a previously created Assessment to provide additional information on whether the event turned out to be authentic or fraudulent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_assessments_annotate_builder()` + `recaptchaenterprise_projects_assessments_annotate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_assessments_annotate(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_assessments_annotate_builder(client, name, body)?;
    recaptchaenterprise_projects_assessments_annotate_execute(builder)
}

/// GET v1/{+parent}/assessments
/// Creates an Assessment of the likelihood an event is legitimate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_assessments_create_execute()` to send, or `recaptchaenterprise_projects_assessments_create` for simplest API.

pub fn recaptchaenterprise_projects_assessments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1Assessment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/assessments", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/assessments
/// Creates an Assessment of the likelihood an event is legitimate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_assessments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_assessments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_assessments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/assessments
/// Creates an Assessment of the likelihood an event is legitimate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_assessments_create_builder()` + `recaptchaenterprise_projects_assessments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_assessments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1Assessment,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_assessments_create_builder(client, parent, body)?;
    recaptchaenterprise_projects_assessments_create_execute(builder)
}

/// GET v1/{+parent}/firewallpolicies
/// Creates a new FirewallPolicy, specifying conditions at which `reCAPTCHA` Enterprise actions can be executed. A project may have a maximum of 1000 policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_create_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_create` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/firewallpolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/firewallpolicies
/// Creates a new FirewallPolicy, specifying conditions at which `reCAPTCHA` Enterprise actions can be executed. A project may have a maximum of 1000 policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_firewallpolicies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1FirewallPolicy =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/firewallpolicies
/// Creates a new FirewallPolicy, specifying conditions at which `reCAPTCHA` Enterprise actions can be executed. A project may have a maximum of 1000 policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_create_builder()` + `recaptchaenterprise_projects_firewallpolicies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recaptchaenterprise_projects_firewallpolicies_create_builder(client, parent, body)?;
    recaptchaenterprise_projects_firewallpolicies_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_delete_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_delete` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_firewallpolicies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_delete_builder()` + `recaptchaenterprise_projects_firewallpolicies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_firewallpolicies_delete_builder(client, name)?;
    recaptchaenterprise_projects_firewallpolicies_delete_execute(builder)
}

/// GET v1/{+name}
/// Returns the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_get_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_get` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_firewallpolicies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1FirewallPolicy =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_get_builder()` + `recaptchaenterprise_projects_firewallpolicies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_firewallpolicies_get_builder(client, name)?;
    recaptchaenterprise_projects_firewallpolicies_get_execute(builder)
}

/// GET v1/{+parent}/firewallpolicies
/// Returns the list of all firewall policies that belong to a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_list_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_list` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/firewallpolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/firewallpolicies
/// Returns the list of all firewall policies that belong to a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_firewallpolicies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListFirewallPoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListFirewallPoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/firewallpolicies
/// Returns the list of all firewall policies that belong to a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_list_builder()` + `recaptchaenterprise_projects_firewallpolicies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListFirewallPoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_firewallpolicies_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    recaptchaenterprise_projects_firewallpolicies_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the specified firewall policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_patch_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_patch` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the specified firewall policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_firewallpolicies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1FirewallPolicy =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the specified firewall policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_patch_builder()` + `recaptchaenterprise_projects_firewallpolicies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_firewallpolicies_patch_builder(
        client, name, updateMask, body,
    )?;
    recaptchaenterprise_projects_firewallpolicies_patch_execute(builder)
}

/// GET v1/{+parent}/firewallpolicies:reorder
/// Reorders all firewall policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_reorder_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_reorder` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_reorder_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/firewallpolicies:reorder",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/firewallpolicies:reorder
/// Reorders all firewall policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_reorder_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_firewallpolicies_reorder_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_reorder_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/firewallpolicies:reorder
/// Reorders all firewall policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_reorder_builder()` + `recaptchaenterprise_projects_firewallpolicies_reorder_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_reorder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recaptchaenterprise_projects_firewallpolicies_reorder_builder(client, parent, body)?;
    recaptchaenterprise_projects_firewallpolicies_reorder_execute(builder)
}

/// GET v1/{+name}:addIpOverride
/// Adds an IP override to a key. The following restrictions hold: * The maximum number of IP overrides per key is 1000. * For any conflict (such as IP already exists or IP part of an existing IP range), an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_add_ip_override_execute()` to send, or `recaptchaenterprise_projects_keys_add_ip_override` for simplest API.

pub fn recaptchaenterprise_projects_keys_add_ip_override_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1AddIpOverrideRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:addIpOverride", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:addIpOverride
/// Adds an IP override to a key. The following restrictions hold: * The maximum number of IP overrides per key is 1000. * For any conflict (such as IP already exists or IP part of an existing IP range), an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_add_ip_override_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_add_ip_override_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_add_ip_override_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1AddIpOverrideResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1AddIpOverrideResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:addIpOverride
/// Adds an IP override to a key. The following restrictions hold: * The maximum number of IP overrides per key is 1000. * For any conflict (such as IP already exists or IP part of an existing IP range), an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_add_ip_override_builder()` + `recaptchaenterprise_projects_keys_add_ip_override_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_add_ip_override(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1AddIpOverrideRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1AddIpOverrideResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_add_ip_override_builder(client, name, body)?;
    recaptchaenterprise_projects_keys_add_ip_override_execute(builder)
}

/// GET v1/{+parent}/keys
/// Creates a new `reCAPTCHA` Enterprise key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_create_execute()` to send, or `recaptchaenterprise_projects_keys_create` for simplest API.

pub fn recaptchaenterprise_projects_keys_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1Key,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/keys", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/keys
/// Creates a new `reCAPTCHA` Enterprise key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/keys
/// Creates a new `reCAPTCHA` Enterprise key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_create_builder()` + `recaptchaenterprise_projects_keys_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudRecaptchaenterpriseV1Key,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_create_builder(client, parent, body)?;
    recaptchaenterprise_projects_keys_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes the specified key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_delete_execute()` to send, or `recaptchaenterprise_projects_keys_delete` for simplest API.

pub fn recaptchaenterprise_projects_keys_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the specified key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_delete_builder()` + `recaptchaenterprise_projects_keys_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_delete_builder(client, name)?;
    recaptchaenterprise_projects_keys_delete_execute(builder)
}

/// GET v1/{+name}
/// Returns the specified key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_get_execute()` to send, or `recaptchaenterprise_projects_keys_get` for simplest API.

pub fn recaptchaenterprise_projects_keys_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns the specified key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Returns the specified key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_get_builder()` + `recaptchaenterprise_projects_keys_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_get_builder(client, name)?;
    recaptchaenterprise_projects_keys_get_execute(builder)
}

/// GET v1/{+name}
/// Get some aggregated metrics for a Key. This data can be used to build dashboards.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_get_metrics_execute()` to send, or `recaptchaenterprise_projects_keys_get_metrics` for simplest API.

pub fn recaptchaenterprise_projects_keys_get_metrics_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get some aggregated metrics for a Key. This data can be used to build dashboards.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_get_metrics_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_get_metrics_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_get_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Metrics>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Metrics =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get some aggregated metrics for a Key. This data can be used to build dashboards.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_get_metrics_builder()` + `recaptchaenterprise_projects_keys_get_metrics_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_get_metrics(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Metrics>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_get_metrics_builder(client, name)?;
    recaptchaenterprise_projects_keys_get_metrics_execute(builder)
}

/// GET v1/{+parent}/keys
/// Returns the list of all keys that belong to a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_list_execute()` to send, or `recaptchaenterprise_projects_keys_list` for simplest API.

pub fn recaptchaenterprise_projects_keys_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/keys", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/keys
/// Returns the list of all keys that belong to a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1ListKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListKeysResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/keys
/// Returns the list of all keys that belong to a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_list_builder()` + `recaptchaenterprise_projects_keys_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1ListKeysResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recaptchaenterprise_projects_keys_list_builder(client, parent, pageSize, pageToken)?;
    recaptchaenterprise_projects_keys_list_execute(builder)
}

/// GET v1/{+parent}:listIpOverrides
/// Lists all IP overrides for a key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_list_ip_overrides_execute()` to send, or `recaptchaenterprise_projects_keys_list_ip_overrides` for simplest API.

pub fn recaptchaenterprise_projects_keys_list_ip_overrides_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}:listIpOverrides",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}:listIpOverrides
/// Lists all IP overrides for a key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_list_ip_overrides_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_list_ip_overrides_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_list_ip_overrides_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListIpOverridesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListIpOverridesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}:listIpOverrides
/// Lists all IP overrides for a key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_list_ip_overrides_builder()` + `recaptchaenterprise_projects_keys_list_ip_overrides_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_list_ip_overrides(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListIpOverridesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_list_ip_overrides_builder(
        client, parent, pageSize, pageToken,
    )?;
    recaptchaenterprise_projects_keys_list_ip_overrides_execute(builder)
}

/// GET v1/{+name}:migrate
/// Migrates an existing key from `reCAPTCHA` to `reCAPTCHA` Enterprise. Once a key is migrated, it can be used from either product. SiteVerify requests are billed as CreateAssessment calls. You must be authenticated as one of the current owners of the `reCAPTCHA` Key, and your user must have the `reCAPTCHA` Enterprise Admin IAM role in the destination project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_migrate_execute()` to send, or `recaptchaenterprise_projects_keys_migrate` for simplest API.

pub fn recaptchaenterprise_projects_keys_migrate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1MigrateKeyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:migrate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:migrate
/// Migrates an existing key from `reCAPTCHA` to `reCAPTCHA` Enterprise. Once a key is migrated, it can be used from either product. SiteVerify requests are billed as CreateAssessment calls. You must be authenticated as one of the current owners of the `reCAPTCHA` Key, and your user must have the `reCAPTCHA` Enterprise Admin IAM role in the destination project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_migrate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_migrate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_migrate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:migrate
/// Migrates an existing key from `reCAPTCHA` to `reCAPTCHA` Enterprise. Once a key is migrated, it can be used from either product. SiteVerify requests are billed as CreateAssessment calls. You must be authenticated as one of the current owners of the `reCAPTCHA` Key, and your user must have the `reCAPTCHA` Enterprise Admin IAM role in the destination project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_migrate_builder()` + `recaptchaenterprise_projects_keys_migrate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_migrate(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1MigrateKeyRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_migrate_builder(client, name, body)?;
    recaptchaenterprise_projects_keys_migrate_execute(builder)
}

/// GET v1/{+name}
/// Updates the specified key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_patch_execute()` to send, or `recaptchaenterprise_projects_keys_patch` for simplest API.

pub fn recaptchaenterprise_projects_keys_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudRecaptchaenterpriseV1Key,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the specified key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the specified key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_patch_builder()` + `recaptchaenterprise_projects_keys_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudRecaptchaenterpriseV1Key,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_patch_builder(client, name, updateMask, body)?;
    recaptchaenterprise_projects_keys_patch_execute(builder)
}

/// GET v1/{+name}:removeIpOverride
/// Removes an IP override from a key. The following restrictions hold: * If the IP isn't found in an existing IP override, a NOT_FOUND error is returned. * If the IP is found in an existing IP override, but the override type does not match, a NOT_FOUND error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_remove_ip_override_execute()` to send, or `recaptchaenterprise_projects_keys_remove_ip_override` for simplest API.

pub fn recaptchaenterprise_projects_keys_remove_ip_override_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:removeIpOverride", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:removeIpOverride
/// Removes an IP override from a key. The following restrictions hold: * If the IP isn't found in an existing IP override, a NOT_FOUND error is returned. * If the IP is found in an existing IP override, but the override type does not match, a NOT_FOUND error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_remove_ip_override_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_remove_ip_override_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_remove_ip_override_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:removeIpOverride
/// Removes an IP override from a key. The following restrictions hold: * If the IP isn't found in an existing IP override, a NOT_FOUND error is returned. * If the IP is found in an existing IP override, but the override type does not match, a NOT_FOUND error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_remove_ip_override_builder()` + `recaptchaenterprise_projects_keys_remove_ip_override_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_remove_ip_override(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_keys_remove_ip_override_builder(client, name, body)?;
    recaptchaenterprise_projects_keys_remove_ip_override_execute(builder)
}

/// GET v1/{+key}:retrieveLegacySecretKey
/// Returns the secret key related to the specified public key. You must use the legacy secret key only in a 3rd party integration with legacy `reCAPTCHA`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_execute()` to send, or `recaptchaenterprise_projects_keys_retrieve_legacy_secret_key` for simplest API.

pub fn recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_builder(
    client: &SimpleHttpClient,
    key: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+key}:retrieveLegacySecretKey",
        key,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+key}:retrieveLegacySecretKey
/// Returns the secret key related to the specified public key. You must use the legacy secret key only in a 3rd party integration with legacy `reCAPTCHA`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1RetrieveLegacySecretKeyResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1RetrieveLegacySecretKeyResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+key}:retrieveLegacySecretKey
/// Returns the secret key related to the specified public key. You must use the legacy secret key only in a 3rd party integration with legacy `reCAPTCHA`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_builder()` + `recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_retrieve_legacy_secret_key(
    client: &SimpleHttpClient,
    key: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1RetrieveLegacySecretKeyResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_builder(client, key)?;
    recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_execute(builder)
}

/// GET v1/{+project}/relatedaccountgroupmemberships:search
/// Search group memberships related to a given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute()` to send, or `recaptchaenterprise_projects_relatedaccountgroupmemberships_search` for simplest API.

pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder(
    client: &SimpleHttpClient,
    project: &str,
    body: &GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+project}/relatedaccountgroupmemberships:search",
        project,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+project}/relatedaccountgroupmemberships:search
/// Search group memberships related to a given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+project}/relatedaccountgroupmemberships:search
/// Search group memberships related to a given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder()` + `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search(
    client: &SimpleHttpClient,
    project: &str,
    body: &GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder(
        client, project, body,
    )?;
    recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute(builder)
}

/// GET v1/{+parent}/relatedaccountgroups
/// List groups of related accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_relatedaccountgroups_list_execute()` to send, or `recaptchaenterprise_projects_relatedaccountgroups_list` for simplest API.

pub fn recaptchaenterprise_projects_relatedaccountgroups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/relatedaccountgroups",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/relatedaccountgroups
/// List groups of related accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_relatedaccountgroups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/relatedaccountgroups
/// List groups of related accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroups_list_builder()` + `recaptchaenterprise_projects_relatedaccountgroups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroups_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_relatedaccountgroups_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    recaptchaenterprise_projects_relatedaccountgroups_list_execute(builder)
}

/// GET v1/{+parent}/memberships
/// Get memberships in a group of related accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_relatedaccountgroups_memberships_list_execute()` to send, or `recaptchaenterprise_projects_relatedaccountgroups_memberships_list` for simplest API.

pub fn recaptchaenterprise_projects_relatedaccountgroups_memberships_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/memberships", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/memberships
/// Get memberships in a group of related accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroups_memberships_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from recaptchaenterprise_projects_relatedaccountgroups_memberships_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroups_memberships_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupMembershipsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupMembershipsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/memberships
/// Get memberships in a group of related accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroups_memberships_list_builder()` + `recaptchaenterprise_projects_relatedaccountgroups_memberships_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroups_memberships_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupMembershipsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_relatedaccountgroups_memberships_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    recaptchaenterprise_projects_relatedaccountgroups_memberships_list_execute(builder)
}
