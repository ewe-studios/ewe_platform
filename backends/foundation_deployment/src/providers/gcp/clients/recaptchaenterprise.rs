//! Auto-generated API clients for gcp/recaptchaenterprise.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/assessments
/// Creates an Assessment of the likelihood an event is legitimate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_assessments_create_execute()` to send, or `recaptchaenterprise_projects_assessments_create` for simplest API.

pub fn recaptchaenterprise_projects_assessments_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudRecaptchaenterpriseV1Assessment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://recaptchaenterprise.googleapis.com/v1/projects/{}/assessments",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/assessments
/// Creates an Assessment of the likelihood an event is legitimate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recaptchaenterprise_projects_assessments_create_execute()` or `recaptchaenterprise_projects_assessments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_assessments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_assessments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Assessment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/assessments
/// Creates an Assessment of the likelihood an event is legitimate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recaptchaenterprise_projects_assessments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recaptchaenterprise_projects_assessments_create_task()`.
/// For the simplest API, use `recaptchaenterprise_projects_assessments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_assessments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recaptchaenterprise_projects_assessments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recaptchaenterprise_projects_assessments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recaptchaenterprise_projects_assessments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsAssessmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1Assessment,
}

/// GET v1/projects/{projectsId}/assessments
/// Creates an Assessment of the likelihood an event is legitimate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_assessments_create_builder()` + `recaptchaenterprise_projects_assessments_create_execute()`.
/// For task-level control, use `recaptchaenterprise_projects_assessments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_assessments_create(
    client: &SimpleHttpClient,
    args: &RecaptchaenterpriseProjectsAssessmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recaptchaenterprise_projects_assessments_create_builder(client, &args.parent, &args.body)?;
    recaptchaenterprise_projects_assessments_create_execute(builder)
}

/// GET v1/projects/{projectsId}/firewallpolicies
/// Creates a new FirewallPolicy, specifying conditions at which `reCAPTCHA` Enterprise actions can be executed. A project may have a maximum of 1000 policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_create_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_create` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://recaptchaenterprise.googleapis.com/v1/projects/{}/firewallpolicies",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/firewallpolicies
/// Creates a new FirewallPolicy, specifying conditions at which `reCAPTCHA` Enterprise actions can be executed. A project may have a maximum of 1000 policies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recaptchaenterprise_projects_firewallpolicies_create_execute()` or `recaptchaenterprise_projects_firewallpolicies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_firewallpolicies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1FirewallPolicy =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/firewallpolicies
/// Creates a new FirewallPolicy, specifying conditions at which `reCAPTCHA` Enterprise actions can be executed. A project may have a maximum of 1000 policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recaptchaenterprise_projects_firewallpolicies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recaptchaenterprise_projects_firewallpolicies_create_task()`.
/// For the simplest API, use `recaptchaenterprise_projects_firewallpolicies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_firewallpolicies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recaptchaenterprise_projects_firewallpolicies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recaptchaenterprise_projects_firewallpolicies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recaptchaenterprise_projects_firewallpolicies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
}

/// GET v1/projects/{projectsId}/firewallpolicies
/// Creates a new FirewallPolicy, specifying conditions at which `reCAPTCHA` Enterprise actions can be executed. A project may have a maximum of 1000 policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_create_builder()` + `recaptchaenterprise_projects_firewallpolicies_create_execute()`.
/// For task-level control, use `recaptchaenterprise_projects_firewallpolicies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_create(
    client: &SimpleHttpClient,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_firewallpolicies_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    recaptchaenterprise_projects_firewallpolicies_create_execute(builder)
}

/// GET v1/projects/{projectsId}/firewallpolicies:reorder
/// Reorders all firewall policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_firewallpolicies_reorder_execute()` to send, or `recaptchaenterprise_projects_firewallpolicies_reorder` for simplest API.

pub fn recaptchaenterprise_projects_firewallpolicies_reorder_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/projects/{}/firewallpolicies:reorder",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/firewallpolicies:reorder
/// Reorders all firewall policies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recaptchaenterprise_projects_firewallpolicies_reorder_execute()` or `recaptchaenterprise_projects_firewallpolicies_reorder`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_firewallpolicies_reorder_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_reorder_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/firewallpolicies:reorder
/// Reorders all firewall policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recaptchaenterprise_projects_firewallpolicies_reorder_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recaptchaenterprise_projects_firewallpolicies_reorder_task()`.
/// For the simplest API, use `recaptchaenterprise_projects_firewallpolicies_reorder()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_firewallpolicies_reorder_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recaptchaenterprise_projects_firewallpolicies_reorder_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recaptchaenterprise_projects_firewallpolicies_reorder_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recaptchaenterprise_projects_firewallpolicies_reorder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesReorderArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesRequest,
}

/// GET v1/projects/{projectsId}/firewallpolicies:reorder
/// Reorders all firewall policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_firewallpolicies_reorder_builder()` + `recaptchaenterprise_projects_firewallpolicies_reorder_execute()`.
/// For task-level control, use `recaptchaenterprise_projects_firewallpolicies_reorder_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_firewallpolicies_reorder(
    client: &SimpleHttpClient,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesReorderArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_firewallpolicies_reorder_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    recaptchaenterprise_projects_firewallpolicies_reorder_execute(builder)
}

/// GET v1/projects/{projectsId}/keys
/// Creates a new `reCAPTCHA` Enterprise key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_keys_create_execute()` to send, or `recaptchaenterprise_projects_keys_create` for simplest API.

pub fn recaptchaenterprise_projects_keys_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudRecaptchaenterpriseV1Key,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://recaptchaenterprise.googleapis.com/v1/projects/{}/keys",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/keys
/// Creates a new `reCAPTCHA` Enterprise key.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recaptchaenterprise_projects_keys_create_execute()` or `recaptchaenterprise_projects_keys_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/keys
/// Creates a new `reCAPTCHA` Enterprise key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recaptchaenterprise_projects_keys_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recaptchaenterprise_projects_keys_create_task()`.
/// For the simplest API, use `recaptchaenterprise_projects_keys_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recaptchaenterprise_projects_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recaptchaenterprise_projects_keys_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recaptchaenterprise_projects_keys_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1Key,
}

/// GET v1/projects/{projectsId}/keys
/// Creates a new `reCAPTCHA` Enterprise key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_keys_create_builder()` + `recaptchaenterprise_projects_keys_create_execute()`.
/// For task-level control, use `recaptchaenterprise_projects_keys_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_keys_create(
    client: &SimpleHttpClient,
    args: &RecaptchaenterpriseProjectsKeysCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recaptchaenterprise_projects_keys_create_builder(client, &args.parent, &args.body)?;
    recaptchaenterprise_projects_keys_create_execute(builder)
}

/// GET v1/projects/{projectsId}/relatedaccountgroupmemberships:search
/// Search group memberships related to a given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute()` to send, or `recaptchaenterprise_projects_relatedaccountgroupmemberships_search` for simplest API.

pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder(
    client: &SimpleHttpClient,
    project: &String,
    body: &GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/projects/{}/relatedaccountgroupmemberships:search",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/relatedaccountgroupmemberships:search
/// Search group memberships related to a given account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute()` or `recaptchaenterprise_projects_relatedaccountgroupmemberships_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/relatedaccountgroupmemberships:search
/// Search group memberships related to a given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_task()`.
/// For the simplest API, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recaptchaenterprise_projects_relatedaccountgroupmemberships_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recaptchaenterprise_projects_relatedaccountgroupmemberships_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsRelatedaccountgroupmembershipsSearchArgs {
    /// Path parameter: project
    pub project: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsRequest,
}

/// GET v1/projects/{projectsId}/relatedaccountgroupmemberships:search
/// Search group memberships related to a given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder()` + `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute()`.
/// For task-level control, use `recaptchaenterprise_projects_relatedaccountgroupmemberships_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search(
    client: &SimpleHttpClient,
    args: &RecaptchaenterpriseProjectsRelatedaccountgroupmembershipsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_relatedaccountgroupmemberships_search_builder(
        client,
        &args.project,
        &args.body,
    )?;
    recaptchaenterprise_projects_relatedaccountgroupmemberships_search_execute(builder)
}

/// GET v1/projects/{projectsId}/relatedaccountgroups
/// List groups of related accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recaptchaenterprise_projects_relatedaccountgroups_list_execute()` to send, or `recaptchaenterprise_projects_relatedaccountgroups_list` for simplest API.

pub fn recaptchaenterprise_projects_relatedaccountgroups_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://recaptchaenterprise.googleapis.com/v1/projects/{}/relatedaccountgroups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/relatedaccountgroups
/// List groups of related accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recaptchaenterprise_projects_relatedaccountgroups_list_execute()` or `recaptchaenterprise_projects_relatedaccountgroups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_relatedaccountgroups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/relatedaccountgroups
/// List groups of related accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recaptchaenterprise_projects_relatedaccountgroups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recaptchaenterprise_projects_relatedaccountgroups_list_task()`.
/// For the simplest API, use `recaptchaenterprise_projects_relatedaccountgroups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recaptchaenterprise_projects_relatedaccountgroups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recaptchaenterprise_projects_relatedaccountgroups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recaptchaenterprise_projects_relatedaccountgroups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recaptchaenterprise_projects_relatedaccountgroups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsRelatedaccountgroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/relatedaccountgroups
/// List groups of related accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recaptchaenterprise_projects_relatedaccountgroups_list_builder()` + `recaptchaenterprise_projects_relatedaccountgroups_list_execute()`.
/// For task-level control, use `recaptchaenterprise_projects_relatedaccountgroups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recaptchaenterprise_projects_relatedaccountgroups_list(
    client: &SimpleHttpClient,
    args: &RecaptchaenterpriseProjectsRelatedaccountgroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recaptchaenterprise_projects_relatedaccountgroups_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    recaptchaenterprise_projects_relatedaccountgroups_list_execute(builder)
}
