//! Auto-generated API clients for gcp/serviceconsumermanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_operations_cancel_execute()` to send, or `serviceconsumermanagement_operations_cancel` for simplest API.

pub fn serviceconsumermanagement_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_operations_cancel_execute()` or `serviceconsumermanagement_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_operations_cancel_task()`.
/// For the simplest API, use `serviceconsumermanagement_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_operations_cancel_builder()` + `serviceconsumermanagement_operations_cancel_execute()`.
/// For task-level control, use `serviceconsumermanagement_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_cancel(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_operations_cancel_builder(client, &args.name)?;
    serviceconsumermanagement_operations_cancel_execute(builder)
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_operations_delete_execute()` to send, or `serviceconsumermanagement_operations_delete` for simplest API.

pub fn serviceconsumermanagement_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_operations_delete_execute()` or `serviceconsumermanagement_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_operations_delete_task()`.
/// For the simplest API, use `serviceconsumermanagement_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_operations_delete_builder()` + `serviceconsumermanagement_operations_delete_execute()`.
/// For task-level control, use `serviceconsumermanagement_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_delete(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_operations_delete_builder(client, &args.name)?;
    serviceconsumermanagement_operations_delete_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_operations_get_execute()` to send, or `serviceconsumermanagement_operations_get` for simplest API.

pub fn serviceconsumermanagement_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_operations_get_execute()` or `serviceconsumermanagement_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_operations_get_task()`.
/// For the simplest API, use `serviceconsumermanagement_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_operations_get_builder()` + `serviceconsumermanagement_operations_get_execute()`.
/// For task-level control, use `serviceconsumermanagement_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_get(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_operations_get_builder(client, &args.name)?;
    serviceconsumermanagement_operations_get_execute(builder)
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_operations_list_execute()` to send, or `serviceconsumermanagement_operations_list` for simplest API.

pub fn serviceconsumermanagement_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://serviceconsumermanagement.googleapis.com/v1/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_operations_list_execute()` or `serviceconsumermanagement_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_operations_list_task()`.
/// For the simplest API, use `serviceconsumermanagement_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementOperationsListArgs {
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_operations_list_builder()` + `serviceconsumermanagement_operations_list_execute()`.
/// For task-level control, use `serviceconsumermanagement_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_operations_list(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_operations_list_builder(
        client,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    serviceconsumermanagement_operations_list_execute(builder)
}

/// GET v1/services/{servicesId}:search
/// Search tenancy units for a managed service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_search_execute()` to send, or `serviceconsumermanagement_services_search` for simplest API.

pub fn serviceconsumermanagement_services_search_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    query: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/services/{servicesId}:search
/// Search tenancy units for a managed service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_search_execute()` or `serviceconsumermanagement_services_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchTenancyUnitsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchTenancyUnitsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/services/{servicesId}:search
/// Search tenancy units for a managed service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_search_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTenancyUnitsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: query
    pub query: Option<Option<String>>,
}

/// GET v1/services/{servicesId}:search
/// Search tenancy units for a managed service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_search_builder()` + `serviceconsumermanagement_services_search_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_search(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTenancyUnitsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_services_search_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    serviceconsumermanagement_services_search_execute(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:addProject
/// Add a new tenant project to the tenancy unit. There can be a maximum of 1024 tenant projects in a tenancy unit. If there are previously failed AddTenantProject calls, you might need to call RemoveTenantProject first to resolve them before you can make another call to AddTenantProject with the same tag. Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_add_project_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_add_project` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_add_project_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:addProject",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:addProject
/// Add a new tenant project to the tenancy unit. There can be a maximum of 1024 tenant projects in a tenancy unit. If there are previously failed AddTenantProject calls, you might need to call RemoveTenantProject first to resolve them before you can make another call to AddTenantProject with the same tag. Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_add_project_execute()` or `serviceconsumermanagement_services_tenancy_units_add_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_add_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_add_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:addProject
/// Add a new tenant project to the tenancy unit. There can be a maximum of 1024 tenant projects in a tenancy unit. If there are previously failed AddTenantProject calls, you might need to call RemoveTenantProject first to resolve them before you can make another call to AddTenantProject with the same tag. Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_add_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_add_project_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_add_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_add_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_add_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_add_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_add_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsAddProjectArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:addProject
/// Add a new tenant project to the tenancy unit. There can be a maximum of 1024 tenant projects in a tenancy unit. If there are previously failed AddTenantProject calls, you might need to call RemoveTenantProject first to resolve them before you can make another call to AddTenantProject with the same tag. Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_add_project_builder()` + `serviceconsumermanagement_services_tenancy_units_add_project_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_add_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_add_project(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsAddProjectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        serviceconsumermanagement_services_tenancy_units_add_project_builder(client, &args.parent)?;
    serviceconsumermanagement_services_tenancy_units_add_project_execute(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:applyProjectConfig
/// Apply a configuration to an existing tenant project. This project must exist in an active state and have the original owner account. The caller must have permission to add a project to the given tenancy unit. The configuration is applied, but any existing settings on the project aren''t modified. Specified policy bindings are applied. Existing bindings aren''t modified. Specified services are activated. No service is deactivated. If specified, new billing configuration is applied. Omit a billing configuration to keep the existing one. A service account in the project is created if previously non existed. Specified labels will be appended to tenant project, note that the value of existing label key will be updated if the same label key is requested. The specified folder is ignored, as moving a tenant project to a different folder isn''t supported. The operation fails if any of the steps fail, but no rollback of already applied configuration changes is attempted. Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_apply_project_config_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_apply_project_config` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_apply_project_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:applyProjectConfig",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:applyProjectConfig
/// Apply a configuration to an existing tenant project. This project must exist in an active state and have the original owner account. The caller must have permission to add a project to the given tenancy unit. The configuration is applied, but any existing settings on the project aren''t modified. Specified policy bindings are applied. Existing bindings aren''t modified. Specified services are activated. No service is deactivated. If specified, new billing configuration is applied. Omit a billing configuration to keep the existing one. A service account in the project is created if previously non existed. Specified labels will be appended to tenant project, note that the value of existing label key will be updated if the same label key is requested. The specified folder is ignored, as moving a tenant project to a different folder isn''t supported. The operation fails if any of the steps fail, but no rollback of already applied configuration changes is attempted. Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_apply_project_config_execute()` or `serviceconsumermanagement_services_tenancy_units_apply_project_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_apply_project_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_apply_project_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:applyProjectConfig
/// Apply a configuration to an existing tenant project. This project must exist in an active state and have the original owner account. The caller must have permission to add a project to the given tenancy unit. The configuration is applied, but any existing settings on the project aren''t modified. Specified policy bindings are applied. Existing bindings aren''t modified. Specified services are activated. No service is deactivated. If specified, new billing configuration is applied. Omit a billing configuration to keep the existing one. A service account in the project is created if previously non existed. Specified labels will be appended to tenant project, note that the value of existing label key will be updated if the same label key is requested. The specified folder is ignored, as moving a tenant project to a different folder isn''t supported. The operation fails if any of the steps fail, but no rollback of already applied configuration changes is attempted. Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_apply_project_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_apply_project_config_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_apply_project_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_apply_project_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_apply_project_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_apply_project_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_apply_project_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsApplyProjectConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:applyProjectConfig
/// Apply a configuration to an existing tenant project. This project must exist in an active state and have the original owner account. The caller must have permission to add a project to the given tenancy unit. The configuration is applied, but any existing settings on the project aren''t modified. Specified policy bindings are applied. Existing bindings aren''t modified. Specified services are activated. No service is deactivated. If specified, new billing configuration is applied. Omit a billing configuration to keep the existing one. A service account in the project is created if previously non existed. Specified labels will be appended to tenant project, note that the value of existing label key will be updated if the same label key is requested. The specified folder is ignored, as moving a tenant project to a different folder isn''t supported. The operation fails if any of the steps fail, but no rollback of already applied configuration changes is attempted. Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_apply_project_config_builder()` + `serviceconsumermanagement_services_tenancy_units_apply_project_config_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_apply_project_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_apply_project_config(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsApplyProjectConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_services_tenancy_units_apply_project_config_builder(
        client, &args.name,
    )?;
    serviceconsumermanagement_services_tenancy_units_apply_project_config_execute(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:attachProject
/// Attach an existing project to the tenancy unit as a new tenant resource. The project could either be the tenant project reserved by calling AddTenantProject under a tenancy unit of a service producer''s project of a managed service, or from a separate project. The caller is checked against a set of permissions as if calling AddTenantProject on the same service consumer. To trigger the attachment, the targeted tenant project must be in a folder. Make sure the ServiceConsumerManagement service account is the owner of that project. These two requirements are already met if the project is reserved by calling AddTenantProject. Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_attach_project_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_attach_project` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_attach_project_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:attachProject",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:attachProject
/// Attach an existing project to the tenancy unit as a new tenant resource. The project could either be the tenant project reserved by calling AddTenantProject under a tenancy unit of a service producer''s project of a managed service, or from a separate project. The caller is checked against a set of permissions as if calling AddTenantProject on the same service consumer. To trigger the attachment, the targeted tenant project must be in a folder. Make sure the ServiceConsumerManagement service account is the owner of that project. These two requirements are already met if the project is reserved by calling AddTenantProject. Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_attach_project_execute()` or `serviceconsumermanagement_services_tenancy_units_attach_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_attach_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_attach_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:attachProject
/// Attach an existing project to the tenancy unit as a new tenant resource. The project could either be the tenant project reserved by calling AddTenantProject under a tenancy unit of a service producer''s project of a managed service, or from a separate project. The caller is checked against a set of permissions as if calling AddTenantProject on the same service consumer. To trigger the attachment, the targeted tenant project must be in a folder. Make sure the ServiceConsumerManagement service account is the owner of that project. These two requirements are already met if the project is reserved by calling AddTenantProject. Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_attach_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_attach_project_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_attach_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_attach_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_attach_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_attach_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_attach_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsAttachProjectArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:attachProject
/// Attach an existing project to the tenancy unit as a new tenant resource. The project could either be the tenant project reserved by calling AddTenantProject under a tenancy unit of a service producer''s project of a managed service, or from a separate project. The caller is checked against a set of permissions as if calling AddTenantProject on the same service consumer. To trigger the attachment, the targeted tenant project must be in a folder. Make sure the ServiceConsumerManagement service account is the owner of that project. These two requirements are already met if the project is reserved by calling AddTenantProject. Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_attach_project_builder()` + `serviceconsumermanagement_services_tenancy_units_attach_project_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_attach_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_attach_project(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsAttachProjectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_services_tenancy_units_attach_project_builder(
        client, &args.name,
    )?;
    serviceconsumermanagement_services_tenancy_units_attach_project_execute(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Creates a tenancy unit with no tenant resources. If tenancy unit already exists, it will be returned, however, in this case, returned TenancyUnit does not have tenant_resources field set and ListTenancyUnits has to be used to get a complete TenancyUnit with all fields populated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_create_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_create` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Creates a tenancy unit with no tenant resources. If tenancy unit already exists, it will be returned, however, in this case, returned TenancyUnit does not have tenant_resources field set and ListTenancyUnits has to be used to get a complete TenancyUnit with all fields populated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_create_execute()` or `serviceconsumermanagement_services_tenancy_units_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TenancyUnit>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TenancyUnit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Creates a tenancy unit with no tenant resources. If tenancy unit already exists, it will be returned, however, in this case, returned TenancyUnit does not have tenant_resources field set and ListTenancyUnits has to be used to get a complete TenancyUnit with all fields populated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_create_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TenancyUnit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Creates a tenancy unit with no tenant resources. If tenancy unit already exists, it will be returned, however, in this case, returned TenancyUnit does not have tenant_resources field set and ListTenancyUnits has to be used to get a complete TenancyUnit with all fields populated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_create_builder()` + `serviceconsumermanagement_services_tenancy_units_create_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_create(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TenancyUnit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        serviceconsumermanagement_services_tenancy_units_create_builder(client, &args.parent)?;
    serviceconsumermanagement_services_tenancy_units_create_execute(builder)
}

/// DELETE v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}
/// Delete a tenancy unit. Before you delete the tenancy unit, there should be no tenant resources in it that aren''t in a DELETED state. Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_delete_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_delete` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}
/// Delete a tenancy unit. Before you delete the tenancy unit, there should be no tenant resources in it that aren''t in a DELETED state. Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_delete_execute()` or `serviceconsumermanagement_services_tenancy_units_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}
/// Delete a tenancy unit. Before you delete the tenancy unit, there should be no tenant resources in it that aren''t in a DELETED state. Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_delete_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}
/// Delete a tenancy unit. Before you delete the tenancy unit, there should be no tenant resources in it that aren''t in a DELETED state. Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_delete_builder()` + `serviceconsumermanagement_services_tenancy_units_delete_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_delete(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        serviceconsumermanagement_services_tenancy_units_delete_builder(client, &args.name)?;
    serviceconsumermanagement_services_tenancy_units_delete_execute(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:deleteProject
/// Deletes the specified project resource identified by a tenant resource tag. The mothod removes a project lien with a ''TenantManager'' origin if that was added. It will then attempt to delete the project. If that operation fails, this method also fails. After the project has been deleted, the tenant resource state is set to DELETED. To permanently remove resource metadata, call the RemoveTenantProject method. New resources with the same tag can''t be added if there are existing resources in a DELETED state. Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_delete_project_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_delete_project` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_delete_project_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:deleteProject",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:deleteProject
/// Deletes the specified project resource identified by a tenant resource tag. The mothod removes a project lien with a ''TenantManager'' origin if that was added. It will then attempt to delete the project. If that operation fails, this method also fails. After the project has been deleted, the tenant resource state is set to DELETED. To permanently remove resource metadata, call the RemoveTenantProject method. New resources with the same tag can''t be added if there are existing resources in a DELETED state. Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_delete_project_execute()` or `serviceconsumermanagement_services_tenancy_units_delete_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_delete_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_delete_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:deleteProject
/// Deletes the specified project resource identified by a tenant resource tag. The mothod removes a project lien with a ''TenantManager'' origin if that was added. It will then attempt to delete the project. If that operation fails, this method also fails. After the project has been deleted, the tenant resource state is set to DELETED. To permanently remove resource metadata, call the RemoveTenantProject method. New resources with the same tag can''t be added if there are existing resources in a DELETED state. Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_delete_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_delete_project_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_delete_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_delete_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_delete_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_delete_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_delete_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsDeleteProjectArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:deleteProject
/// Deletes the specified project resource identified by a tenant resource tag. The mothod removes a project lien with a ''TenantManager'' origin if that was added. It will then attempt to delete the project. If that operation fails, this method also fails. After the project has been deleted, the tenant resource state is set to DELETED. To permanently remove resource metadata, call the RemoveTenantProject method. New resources with the same tag can''t be added if there are existing resources in a DELETED state. Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_delete_project_builder()` + `serviceconsumermanagement_services_tenancy_units_delete_project_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_delete_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_delete_project(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsDeleteProjectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_services_tenancy_units_delete_project_builder(
        client, &args.name,
    )?;
    serviceconsumermanagement_services_tenancy_units_delete_project_execute(builder)
}

/// GET v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Find the tenancy unit for a managed service and service consumer. This method shouldn''t be used in a service producer''s runtime path, for example to find the tenant project number when creating VMs. Service producers must persist the tenant project''s information after the project is created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_list_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_list` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Find the tenancy unit for a managed service and service consumer. This method shouldn''t be used in a service producer''s runtime path, for example to find the tenant project number when creating VMs. Service producers must persist the tenant project''s information after the project is created.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_list_execute()` or `serviceconsumermanagement_services_tenancy_units_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTenancyUnitsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTenancyUnitsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Find the tenancy unit for a managed service and service consumer. This method shouldn''t be used in a service producer''s runtime path, for example to find the tenant project number when creating VMs. Service producers must persist the tenant project''s information after the project is created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_list_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTenancyUnitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits
/// Find the tenancy unit for a managed service and service consumer. This method shouldn''t be used in a service producer''s runtime path, for example to find the tenant project number when creating VMs. Service producers must persist the tenant project''s information after the project is created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_list_builder()` + `serviceconsumermanagement_services_tenancy_units_list_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_list(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTenancyUnitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_services_tenancy_units_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    serviceconsumermanagement_services_tenancy_units_list_execute(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:removeProject
/// Removes the specified project resource identified by a tenant resource tag. The method removes the project lien with ''TenantManager'' origin if that was added. It then attempts to delete the project. If that operation fails, this method also fails. Calls to remove already removed or non-existent tenant project succeed. After the project has been deleted, or if was already in a DELETED state, resource metadata is permanently removed from the tenancy unit. Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_remove_project_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_remove_project` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_remove_project_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:removeProject",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:removeProject
/// Removes the specified project resource identified by a tenant resource tag. The method removes the project lien with ''TenantManager'' origin if that was added. It then attempts to delete the project. If that operation fails, this method also fails. Calls to remove already removed or non-existent tenant project succeed. After the project has been deleted, or if was already in a DELETED state, resource metadata is permanently removed from the tenancy unit. Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_remove_project_execute()` or `serviceconsumermanagement_services_tenancy_units_remove_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_remove_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_remove_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:removeProject
/// Removes the specified project resource identified by a tenant resource tag. The method removes the project lien with ''TenantManager'' origin if that was added. It then attempts to delete the project. If that operation fails, this method also fails. Calls to remove already removed or non-existent tenant project succeed. After the project has been deleted, or if was already in a DELETED state, resource metadata is permanently removed from the tenancy unit. Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_remove_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_remove_project_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_remove_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_remove_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_remove_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_remove_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_remove_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsRemoveProjectArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:removeProject
/// Removes the specified project resource identified by a tenant resource tag. The method removes the project lien with ''TenantManager'' origin if that was added. It then attempts to delete the project. If that operation fails, this method also fails. Calls to remove already removed or non-existent tenant project succeed. After the project has been deleted, or if was already in a DELETED state, resource metadata is permanently removed from the tenancy unit. Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_remove_project_builder()` + `serviceconsumermanagement_services_tenancy_units_remove_project_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_remove_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_remove_project(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsRemoveProjectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_services_tenancy_units_remove_project_builder(
        client, &args.name,
    )?;
    serviceconsumermanagement_services_tenancy_units_remove_project_execute(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:undeleteProject
/// Attempts to undelete a previously deleted tenant project. The project must be in a DELETED state. There are no guarantees that an undeleted project will be in a fully restored and functional state. Call the ApplyTenantProjectConfig method to update its configuration and then validate all managed service resources. Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `serviceconsumermanagement_services_tenancy_units_undelete_project_execute()` to send, or `serviceconsumermanagement_services_tenancy_units_undelete_project` for simplest API.

pub fn serviceconsumermanagement_services_tenancy_units_undelete_project_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://serviceconsumermanagement.googleapis.com/v1/services/{}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:undeleteProject",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:undeleteProject
/// Attempts to undelete a previously deleted tenant project. The project must be in a DELETED state. There are no guarantees that an undeleted project will be in a fully restored and functional state. Call the ApplyTenantProjectConfig method to update its configuration and then validate all managed service resources. Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `serviceconsumermanagement_services_tenancy_units_undelete_project_execute()` or `serviceconsumermanagement_services_tenancy_units_undelete_project`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_undelete_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_undelete_project_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:undeleteProject
/// Attempts to undelete a previously deleted tenant project. The project must be in a DELETED state. There are no guarantees that an undeleted project will be in a fully restored and functional state. Call the ApplyTenantProjectConfig method to update its configuration and then validate all managed service resources. Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `serviceconsumermanagement_services_tenancy_units_undelete_project_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_undelete_project_task()`.
/// For the simplest API, use `serviceconsumermanagement_services_tenancy_units_undelete_project()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `serviceconsumermanagement_services_tenancy_units_undelete_project_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn serviceconsumermanagement_services_tenancy_units_undelete_project_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = serviceconsumermanagement_services_tenancy_units_undelete_project_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`serviceconsumermanagement_services_tenancy_units_undelete_project`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ServiceconsumermanagementServicesTenancyUnitsUndeleteProjectArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/services/{servicesId}/{servicesId1}/{servicesId2}/tenancyUnits/{tenancyUnitsId}:undeleteProject
/// Attempts to undelete a previously deleted tenant project. The project must be in a DELETED state. There are no guarantees that an undeleted project will be in a fully restored and functional state. Call the ApplyTenantProjectConfig method to update its configuration and then validate all managed service resources. Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `serviceconsumermanagement_services_tenancy_units_undelete_project_builder()` + `serviceconsumermanagement_services_tenancy_units_undelete_project_execute()`.
/// For task-level control, use `serviceconsumermanagement_services_tenancy_units_undelete_project_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn serviceconsumermanagement_services_tenancy_units_undelete_project(
    client: &SimpleHttpClient,
    args: &ServiceconsumermanagementServicesTenancyUnitsUndeleteProjectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = serviceconsumermanagement_services_tenancy_units_undelete_project_builder(
        client, &args.name,
    )?;
    serviceconsumermanagement_services_tenancy_units_undelete_project_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ServiceconsumermanagementOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementOperationsCancelArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ServiceconsumermanagementOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementOperationsDeleteArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ServiceconsumermanagementOperationsGetArgs) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with ServiceconsumermanagementOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &ServiceconsumermanagementOperationsListArgs) -> String {
        "gcp::serviceconsumermanagement::ListOperationsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchTenancyUnitsResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchTenancyUnitsResponse with ServiceconsumermanagementServicesSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesSearchArgs>
    for SearchTenancyUnitsResponse
{
    fn generate_resource_id(&self, input: &ServiceconsumermanagementServicesSearchArgs) -> String {
        format!(
            "gcp::serviceconsumermanagement::SearchTenancyUnitsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::SearchTenancyUnitsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementServicesTenancyUnitsAddProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsAddProjectArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsAddProjectArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementServicesTenancyUnitsApplyProjectConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsApplyProjectConfigArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsApplyProjectConfigArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementServicesTenancyUnitsAttachProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsAttachProjectArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsAttachProjectArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TenancyUnit
// =============================================================================

/// ResourceIdentifier implementation for TenancyUnit with ServiceconsumermanagementServicesTenancyUnitsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsCreateArgs> for TenancyUnit {
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsCreateArgs,
    ) -> String {
        format!(
            "gcp::serviceconsumermanagement::TenancyUnit/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::TenancyUnit"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementServicesTenancyUnitsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsDeleteArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementServicesTenancyUnitsDeleteProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsDeleteProjectArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsDeleteProjectArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTenancyUnitsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTenancyUnitsResponse with ServiceconsumermanagementServicesTenancyUnitsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsListArgs>
    for ListTenancyUnitsResponse
{
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsListArgs,
    ) -> String {
        format!(
            "gcp::serviceconsumermanagement::ListTenancyUnitsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::ListTenancyUnitsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementServicesTenancyUnitsRemoveProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsRemoveProjectArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsRemoveProjectArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ServiceconsumermanagementServicesTenancyUnitsUndeleteProjectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ServiceconsumermanagementServicesTenancyUnitsUndeleteProjectArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &ServiceconsumermanagementServicesTenancyUnitsUndeleteProjectArgs,
    ) -> String {
        format!("gcp::serviceconsumermanagement::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::serviceconsumermanagement::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
