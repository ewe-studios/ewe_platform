//! Auto-generated API clients for Google Cloud Platform - dataproc.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+parent}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_create_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_create` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/autoscalingPolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_create_builder()` + `dataproc_projects_locations_autoscaling_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AutoscalingPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_autoscaling_policies_create_builder(client, parent, body)?;
    dataproc_projects_locations_autoscaling_policies_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_delete_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_delete` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_delete_builder()` + `dataproc_projects_locations_autoscaling_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_delete_builder(client, name)?;
    dataproc_projects_locations_autoscaling_policies_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieves autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_get_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_get` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_get_builder()` + `dataproc_projects_locations_autoscaling_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_get_builder(client, name)?;
    dataproc_projects_locations_autoscaling_policies_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_get_iam_policy` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder()` + `dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute(builder)
}

/// GET v1/{+parent}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_list_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_list` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/autoscalingPolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAutoscalingPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_list_builder()` + `dataproc_projects_locations_autoscaling_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    dataproc_projects_locations_autoscaling_policies_list_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_set_iam_policy` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder()` + `dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_test_iam_permissions` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder()` + `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder(
        client, resource, body,
    )?;
    dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute(builder)
}

/// GET v1/{+name}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_update_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_update` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_autoscaling_policies_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_update_builder()` + `dataproc_projects_locations_autoscaling_policies_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &AutoscalingPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_autoscaling_policies_update_builder(client, name, body)?;
    dataproc_projects_locations_autoscaling_policies_update_execute(builder)
}

/// GET v1/{+name}:analyze
/// Analyze a Batch for possible recommendations and insights.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_analyze_execute()` to send, or `dataproc_projects_locations_batches_analyze` for simplest API.

pub fn dataproc_projects_locations_batches_analyze_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AnalyzeBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:analyze", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:analyze
/// Analyze a Batch for possible recommendations and insights.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_analyze_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_analyze_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_analyze_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:analyze
/// Analyze a Batch for possible recommendations and insights.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_analyze_builder()` + `dataproc_projects_locations_batches_analyze_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_analyze(
    client: &SimpleHttpClient,
    name: &str,
    body: &AnalyzeBatchRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_analyze_builder(client, name, body)?;
    dataproc_projects_locations_batches_analyze_execute(builder)
}

/// GET v1/{+parent}/batches
/// Creates a batch workload that executes asynchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_create_execute()` to send, or `dataproc_projects_locations_batches_create` for simplest API.

pub fn dataproc_projects_locations_batches_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    batchId: Option<&str>,
    requestId: Option<&str>,
    body: &Batch,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/batches", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "batchId",
            batchId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/batches
/// Creates a batch workload that executes asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/batches
/// Creates a batch workload that executes asynchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_create_builder()` + `dataproc_projects_locations_batches_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_create(
    client: &SimpleHttpClient,
    parent: &str,
    batchId: Option<&str>,
    requestId: Option<&str>,
    body: &Batch,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_create_builder(
        client, parent, batchId, requestId, body,
    )?;
    dataproc_projects_locations_batches_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes the batch workload resource. If the batch is not in a CANCELLED, SUCCEEDED or FAILED State, the delete operation fails and the response returns FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_delete_execute()` to send, or `dataproc_projects_locations_batches_delete` for simplest API.

pub fn dataproc_projects_locations_batches_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the batch workload resource. If the batch is not in a CANCELLED, SUCCEEDED or FAILED State, the delete operation fails and the response returns FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the batch workload resource. If the batch is not in a CANCELLED, SUCCEEDED or FAILED State, the delete operation fails and the response returns FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_delete_builder()` + `dataproc_projects_locations_batches_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_delete_builder(client, name)?;
    dataproc_projects_locations_batches_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the batch workload resource representation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_get_execute()` to send, or `dataproc_projects_locations_batches_get` for simplest API.

pub fn dataproc_projects_locations_batches_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the batch workload resource representation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Batch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Batch = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the batch workload resource representation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_get_builder()` + `dataproc_projects_locations_batches_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Batch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_get_builder(client, name)?;
    dataproc_projects_locations_batches_get_execute(builder)
}

/// GET v1/{+parent}/batches
/// Lists batch workloads.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_list_execute()` to send, or `dataproc_projects_locations_batches_list` for simplest API.

pub fn dataproc_projects_locations_batches_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/batches", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/batches
/// Lists batch workloads.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBatchesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/batches
/// Lists batch workloads.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_list_builder()` + `dataproc_projects_locations_batches_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    dataproc_projects_locations_batches_list_execute(builder)
}

/// GET v1/{+name}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:access", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_builder()` + `dataproc_projects_locations_batches_spark_applications_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_builder(
        client, name, parent,
    )?;
    dataproc_projects_locations_batches_spark_applications_access_execute(builder)
}

/// GET v1/{+name}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_environment_info_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_environment_info` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:accessEnvironmentInfo",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_environment_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_access_environment_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_environment_info_builder()` + `dataproc_projects_locations_batches_spark_applications_access_environment_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_access_environment_info_builder(
            client, name, parent,
        )?;
    dataproc_projects_locations_batches_spark_applications_access_environment_info_execute(builder)
}

/// GET v1/{+name}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_job_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_job` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_job_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessJob", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "jobId",
            jobId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_job_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_access_job_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_job_builder()` + `dataproc_projects_locations_batches_spark_applications_access_job_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_job(
    client: &SimpleHttpClient,
    name: &str,
    jobId: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_job_builder(
        client, name, jobId, parent,
    )?;
    dataproc_projects_locations_batches_spark_applications_access_job_execute(builder)
}

/// GET v1/{+name}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_sql_plan` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessSqlPlan", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionId",
            executionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlSparkPlanGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder()` + `dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan(
    client: &SimpleHttpClient,
    name: &str,
    executionId: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlSparkPlanGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder(
        client,
        name,
        executionId,
        parent,
    )?;
    dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute(builder)
}

/// GET v1/{+name}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_sql_query_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_sql_query` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    executionId: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessSqlQuery", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "details",
            details
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "executionId",
            executionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "planDescription",
            planDescription
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_access_sql_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_query_builder()` + `dataproc_projects_locations_batches_spark_applications_access_sql_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    executionId: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_sql_query_builder(
        client,
        name,
        details,
        executionId,
        parent,
        planDescription,
    )?;
    dataproc_projects_locations_batches_spark_applications_access_sql_query_execute(builder)
}

/// GET v1/{+name}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_stage_attempt` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessStageAttempt", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "summaryMetricsMask",
            summaryMetricsMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder()` + `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder(
            client,
            name,
            parent,
            stageAttemptId,
            stageId,
            summaryMetricsMask,
        )?;
    dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute(builder)
}

/// GET v1/{+name}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:accessStageRddGraph",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageRddOperationGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder()` + `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageRddOperationGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder(
            client, name, parent, stageId,
        )?;
    dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute(builder)
}

/// GET v1/{+parent}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    applicationStatus: Option<&str>,
    maxEndTime: Option<&str>,
    maxTime: Option<&str>,
    minEndTime: Option<&str>,
    minTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/sparkApplications:search",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "applicationStatus",
            applicationStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxEndTime",
            maxEndTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxTime",
            maxTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minEndTime",
            minEndTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minTime",
            minTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_builder()` + `dataproc_projects_locations_batches_spark_applications_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search(
    client: &SimpleHttpClient,
    parent: &str,
    applicationStatus: Option<&str>,
    maxEndTime: Option<&str>,
    maxTime: Option<&str>,
    minEndTime: Option<&str>,
    minTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_builder(
        client,
        parent,
        applicationStatus,
        maxEndTime,
        maxTime,
        minEndTime,
        minTime,
        pageSize,
        pageToken,
    )?;
    dataproc_projects_locations_batches_spark_applications_search_execute(builder)
}

/// GET v1/{+name}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:searchExecutorStageSummary",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorStageSummaryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder()` + `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorStageSummaryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder(client, name, pageSize, pageToken, parent, stageAttemptId, stageId)?;
    dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute(
        builder,
    )
}

/// GET v1/{+name}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_executors_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_executors` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    executorStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchExecutors", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executorStatus",
            executorStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_executors_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_executors_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_executors_builder()` + `dataproc_projects_locations_batches_spark_applications_search_executors_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executors(
    client: &SimpleHttpClient,
    name: &str,
    executorStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_executors_builder(
        client,
        name,
        executorStatus,
        pageSize,
        pageToken,
        parent,
    )?;
    dataproc_projects_locations_batches_spark_applications_search_executors_execute(builder)
}

/// GET v1/{+name}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_jobs_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_jobs` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchJobs", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "jobStatus",
            jobStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_jobs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_jobs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_jobs_builder()` + `dataproc_projects_locations_batches_spark_applications_search_jobs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_jobs(
    client: &SimpleHttpClient,
    name: &str,
    jobStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_jobs_builder(
        client, name, jobStatus, pageSize, pageToken, parent,
    )?;
    dataproc_projects_locations_batches_spark_applications_search_jobs_execute(builder)
}

/// GET v1/{+name}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_sql_queries` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchSqlQueries", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "details",
            details
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "planDescription",
            planDescription
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationSqlQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder()` + `dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder(
            client,
            name,
            details,
            pageSize,
            pageToken,
            parent,
            planDescription,
        )?;
    dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute(builder)
}

/// GET v1/{+name}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    sortRuntime: Option<bool>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    taskStatus: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:searchStageAttemptTasks",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sortRuntime",
            sortRuntime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "taskStatus",
            taskStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder()` + `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    sortRuntime: Option<bool>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    taskStatus: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder(
            client,
            name,
            pageSize,
            pageToken,
            parent,
            sortRuntime,
            stageAttemptId,
            stageId,
            taskStatus,
        )?;
    dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/{+name}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_stage_attempts` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:searchStageAttempts",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "summaryMetricsMask",
            summaryMetricsMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder()` + `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder(
            client,
            name,
            pageSize,
            pageToken,
            parent,
            stageId,
            summaryMetricsMask,
        )?;
    dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute(builder)
}

/// GET v1/{+name}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_stages_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_stages` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageStatus: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchStages", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageStatus",
            stageStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "summaryMetricsMask",
            summaryMetricsMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_search_stages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stages_builder()` + `dataproc_projects_locations_batches_spark_applications_search_stages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stages(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageStatus: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_stages_builder(
        client,
        name,
        pageSize,
        pageToken,
        parent,
        stageStatus,
        summaryMetricsMask,
    )?;
    dataproc_projects_locations_batches_spark_applications_search_stages_execute(builder)
}

/// GET v1/{+name}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_executors_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_executors` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:summarizeExecutors", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_executors_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_summarize_executors_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_executors_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_executors_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_summarize_executors_builder(
            client, name, parent,
        )?;
    dataproc_projects_locations_batches_spark_applications_summarize_executors_execute(builder)
}

/// GET v1/{+name}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_jobs` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:summarizeJobs", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder(
        client, name, parent,
    )?;
    dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute(builder)
}

/// GET v1/{+name}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:summarizeStageAttemptTasks",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder(client, name, parent, stageAttemptId, stageId)?;
    dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/{+name}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_stages_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_stages` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:summarizeStages", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_summarize_stages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStagesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stages_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_stages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_summarize_stages_builder(
        client, name, parent,
    )?;
    dataproc_projects_locations_batches_spark_applications_summarize_stages_execute(builder)
}

/// GET v1/{+name}:write
/// Write wrapper objects from dataplane to spanner
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_write_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_write` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_write_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WriteSparkApplicationContextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:write", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:write
/// Write wrapper objects from dataplane to spanner
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_write_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_batches_spark_applications_write_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteSparkApplicationContextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:write
/// Write wrapper objects from dataplane to spanner
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_write_builder()` + `dataproc_projects_locations_batches_spark_applications_write_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_write(
    client: &SimpleHttpClient,
    name: &str,
    body: &WriteSparkApplicationContextRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_write_builder(client, name, body)?;
    dataproc_projects_locations_batches_spark_applications_write_execute(builder)
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_cancel_execute()` to send, or `dataproc_projects_locations_operations_cancel` for simplest API.

pub fn dataproc_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_cancel_builder()` + `dataproc_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_cancel_builder(client, name)?;
    dataproc_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_delete_execute()` to send, or `dataproc_projects_locations_operations_delete` for simplest API.

pub fn dataproc_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_delete_builder()` + `dataproc_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_delete_builder(client, name)?;
    dataproc_projects_locations_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_get_execute()` to send, or `dataproc_projects_locations_operations_get` for simplest API.

pub fn dataproc_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_get_builder()` + `dataproc_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_get_builder(client, name)?;
    dataproc_projects_locations_operations_get_execute(builder)
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_list_execute()` to send, or `dataproc_projects_locations_operations_list` for simplest API.

pub fn dataproc_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_list_builder()` + `dataproc_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    dataproc_projects_locations_operations_list_execute(builder)
}

/// GET v1/{+parent}/sessionTemplates
/// Create a session template synchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_create_execute()` to send, or `dataproc_projects_locations_session_templates_create` for simplest API.

pub fn dataproc_projects_locations_session_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SessionTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/sessionTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sessionTemplates
/// Create a session template synchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_session_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_session_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SessionTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sessionTemplates
/// Create a session template synchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_create_builder()` + `dataproc_projects_locations_session_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SessionTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_session_templates_create_builder(client, parent, body)?;
    dataproc_projects_locations_session_templates_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a session template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_delete_execute()` to send, or `dataproc_projects_locations_session_templates_delete` for simplest API.

pub fn dataproc_projects_locations_session_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a session template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_session_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_session_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a session template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_delete_builder()` + `dataproc_projects_locations_session_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_delete_builder(client, name)?;
    dataproc_projects_locations_session_templates_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the resource representation for a session template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_get_execute()` to send, or `dataproc_projects_locations_session_templates_get` for simplest API.

pub fn dataproc_projects_locations_session_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the resource representation for a session template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_session_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_session_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SessionTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the resource representation for a session template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_get_builder()` + `dataproc_projects_locations_session_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_get_builder(client, name)?;
    dataproc_projects_locations_session_templates_get_execute(builder)
}

/// GET v1/{+parent}/sessionTemplates
/// Lists session templates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_list_execute()` to send, or `dataproc_projects_locations_session_templates_list` for simplest API.

pub fn dataproc_projects_locations_session_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/sessionTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sessionTemplates
/// Lists session templates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_session_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_session_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSessionTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSessionTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sessionTemplates
/// Lists session templates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_list_builder()` + `dataproc_projects_locations_session_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSessionTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    dataproc_projects_locations_session_templates_list_execute(builder)
}

/// GET v1/{+name}
/// Updates the session template synchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_patch_execute()` to send, or `dataproc_projects_locations_session_templates_patch` for simplest API.

pub fn dataproc_projects_locations_session_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SessionTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates the session template synchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_session_templates_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_session_templates_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SessionTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates the session template synchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_patch_builder()` + `dataproc_projects_locations_session_templates_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &SessionTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_patch_builder(client, name, body)?;
    dataproc_projects_locations_session_templates_patch_execute(builder)
}

/// GET v1/{+parent}/sessions
/// Create an interactive session asynchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_create_execute()` to send, or `dataproc_projects_locations_sessions_create` for simplest API.

pub fn dataproc_projects_locations_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    sessionId: Option<&str>,
    body: &Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sessions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sessionId",
            sessionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/sessions
/// Create an interactive session asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sessions
/// Create an interactive session asynchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_create_builder()` + `dataproc_projects_locations_sessions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    sessionId: Option<&str>,
    body: &Session,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_create_builder(
        client, parent, requestId, sessionId, body,
    )?;
    dataproc_projects_locations_sessions_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes the interactive session resource. If the session is not in terminal state, it is terminated, and then deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_delete_execute()` to send, or `dataproc_projects_locations_sessions_delete` for simplest API.

pub fn dataproc_projects_locations_sessions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes the interactive session resource. If the session is not in terminal state, it is terminated, and then deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes the interactive session resource. If the session is not in terminal state, it is terminated, and then deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_delete_builder()` + `dataproc_projects_locations_sessions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_delete_builder(client, name, requestId)?;
    dataproc_projects_locations_sessions_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the resource representation for an interactive session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_get_execute()` to send, or `dataproc_projects_locations_sessions_get` for simplest API.

pub fn dataproc_projects_locations_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the resource representation for an interactive session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Session>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the resource representation for an interactive session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_get_builder()` + `dataproc_projects_locations_sessions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Session>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_get_builder(client, name)?;
    dataproc_projects_locations_sessions_get_execute(builder)
}

/// GET v1/{+parent}/sessions
/// Lists interactive sessions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_list_execute()` to send, or `dataproc_projects_locations_sessions_list` for simplest API.

pub fn dataproc_projects_locations_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/sessions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sessions
/// Lists interactive sessions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSessionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSessionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sessions
/// Lists interactive sessions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_list_builder()` + `dataproc_projects_locations_sessions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSessionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    dataproc_projects_locations_sessions_list_execute(builder)
}

/// GET v1/{+name}:terminate
/// Terminates the interactive session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_terminate_execute()` to send, or `dataproc_projects_locations_sessions_terminate` for simplest API.

pub fn dataproc_projects_locations_sessions_terminate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &TerminateSessionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:terminate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:terminate
/// Terminates the interactive session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_terminate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_terminate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_terminate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:terminate
/// Terminates the interactive session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_terminate_builder()` + `dataproc_projects_locations_sessions_terminate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_terminate(
    client: &SimpleHttpClient,
    name: &str,
    body: &TerminateSessionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_terminate_builder(client, name, body)?;
    dataproc_projects_locations_sessions_terminate_execute(builder)
}

/// GET v1/{+name}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:access", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_builder(
        client, name, parent,
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_execute(builder)
}

/// GET v1/{+name}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_environment_info` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:accessEnvironmentInfo",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder(
            client, name, parent,
        )?;
    dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute(builder)
}

/// GET v1/{+name}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_job_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_job` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_job_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessJob", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "jobId",
            jobId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_job_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_access_job_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_job_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_job_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_job(
    client: &SimpleHttpClient,
    name: &str,
    jobId: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_job_builder(
        client, name, jobId, parent,
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_job_execute(builder)
}

/// GET v1/{+name}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_sql_plan` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessSqlPlan", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executionId",
            executionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationSqlSparkPlanGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan(
    client: &SimpleHttpClient,
    name: &str,
    executionId: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationSqlSparkPlanGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder(
        client,
        name,
        executionId,
        parent,
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute(builder)
}

/// GET v1/{+name}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_sql_query` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    executionId: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessSqlQuery", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "details",
            details
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "executionId",
            executionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "planDescription",
            planDescription
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    executionId: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder(
        client,
        name,
        details,
        executionId,
        parent,
        planDescription,
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute(builder)
}

/// GET v1/{+name}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:accessStageAttempt", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "summaryMetricsMask",
            summaryMetricsMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder(
            client,
            name,
            parent,
            stageAttemptId,
            stageId,
            summaryMetricsMask,
        )?;
    dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute(builder)
}

/// GET v1/{+name}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:accessStageRddGraph",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationStageRddOperationGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationStageRddOperationGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder(
            client, name, parent, stageId,
        )?;
    dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute(builder)
}

/// GET v1/{+parent}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    applicationStatus: Option<&str>,
    maxEndTime: Option<&str>,
    maxTime: Option<&str>,
    minEndTime: Option<&str>,
    minTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/sparkApplications:search",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "applicationStatus",
            applicationStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxEndTime",
            maxEndTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxTime",
            maxTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minEndTime",
            minEndTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minTime",
            minTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search(
    client: &SimpleHttpClient,
    parent: &str,
    applicationStatus: Option<&str>,
    maxEndTime: Option<&str>,
    maxTime: Option<&str>,
    minEndTime: Option<&str>,
    minTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_builder(
        client,
        parent,
        applicationStatus,
        maxEndTime,
        maxTime,
        minEndTime,
        minTime,
        pageSize,
        pageToken,
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_execute(builder)
}

/// GET v1/{+name}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:searchExecutorStageSummary",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorStageSummaryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorStageSummaryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder(client, name, pageSize, pageToken, parent, stageAttemptId, stageId)?;
    dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute(
        builder,
    )
}

/// GET v1/{+name}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_executors_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_executors` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    executorStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchExecutors", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "executorStatus",
            executorStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_executors_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_executors_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_executors_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_executors_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executors(
    client: &SimpleHttpClient,
    name: &str,
    executorStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_executors_builder(
        client,
        name,
        executorStatus,
        pageSize,
        pageToken,
        parent,
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_executors_execute(builder)
}

/// GET v1/{+name}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_jobs_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_jobs` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobIds: Option<&str>,
    jobStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchJobs", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "jobIds",
            jobIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "jobStatus",
            jobStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_jobs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_jobs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_jobs_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_jobs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs(
    client: &SimpleHttpClient,
    name: &str,
    jobIds: Option<&str>,
    jobStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_jobs_builder(
        client, name, jobIds, jobStatus, pageSize, pageToken, parent,
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_jobs_execute(builder)
}

/// GET v1/{+name}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_sql_queries` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    operationIds: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchSqlQueries", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "details",
            details
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "operationIds",
            operationIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "planDescription",
            planDescription
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationSqlQueriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    operationIds: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder(
            client,
            name,
            details,
            operationIds,
            pageSize,
            pageToken,
            parent,
            planDescription,
        )?;
    dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute(builder)
}

/// GET v1/{+name}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    sortRuntime: Option<bool>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    taskStatus: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:searchStageAttemptTasks",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sortRuntime",
            sortRuntime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "taskStatus",
            taskStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    sortRuntime: Option<bool>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    taskStatus: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder(
            client,
            name,
            pageSize,
            pageToken,
            parent,
            sortRuntime,
            stageAttemptId,
            stageId,
            taskStatus,
        )?;
    dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/{+name}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:searchStageAttempts",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "summaryMetricsMask",
            summaryMetricsMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder(
            client,
            name,
            pageSize,
            pageToken,
            parent,
            stageId,
            summaryMetricsMask,
        )?;
    dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute(builder)
}

/// GET v1/{+name}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_stages_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_stages` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageIds: Option<&str>,
    stageStatus: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:searchStages", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageIds",
            stageIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageStatus",
            stageStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "summaryMetricsMask",
            summaryMetricsMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_search_stages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stages_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_stages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stages(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageIds: Option<&str>,
    stageStatus: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_stages_builder(
        client,
        name,
        pageSize,
        pageToken,
        parent,
        stageIds,
        stageStatus,
        summaryMetricsMask,
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_stages_execute(builder)
}

/// GET v1/{+name}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_executors` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:summarizeExecutors", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder(
            client, name, parent,
        )?;
    dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute(builder)
}

/// GET v1/{+name}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_jobs` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobIds: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:summarizeJobs", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "jobIds",
            jobIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs(
    client: &SimpleHttpClient,
    name: &str,
    jobIds: Option<&str>,
    parent: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder(
        client, name, jobIds, parent,
    )?;
    dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute(builder)
}

/// GET v1/{+name}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}:summarizeStageAttemptTasks",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageAttemptId",
            stageAttemptId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageId",
            stageId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SummarizeSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SummarizeSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder(client, name, parent, stageAttemptId, stageId)?;
    dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/{+name}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_stages` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:summarizeStages", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stageIds",
            stageIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageIds: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder(
        client, name, parent, stageIds,
    )?;
    dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute(builder)
}

/// GET v1/{+name}:write
/// Write wrapper objects from dataplane to spanner
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_write_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_write` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_write_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WriteSessionSparkApplicationContextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:write", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:write
/// Write wrapper objects from dataplane to spanner
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_write_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_sessions_spark_applications_write_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSessionSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteSessionSparkApplicationContextResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:write
/// Write wrapper objects from dataplane to spanner
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_write_builder()` + `dataproc_projects_locations_sessions_spark_applications_write_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_write(
    client: &SimpleHttpClient,
    name: &str,
    body: &WriteSessionSparkApplicationContextRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSessionSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_write_builder(client, name, body)?;
    dataproc_projects_locations_sessions_spark_applications_write_execute(builder)
}

/// GET v1/{+parent}/workflowTemplates
/// Creates new workflow template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_create_execute()` to send, or `dataproc_projects_locations_workflow_templates_create` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/workflowTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/workflowTemplates
/// Creates new workflow template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/workflowTemplates
/// Creates new workflow template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_create_builder()` + `dataproc_projects_locations_workflow_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WorkflowTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_workflow_templates_create_builder(client, parent, body)?;
    dataproc_projects_locations_workflow_templates_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_delete_execute()` to send, or `dataproc_projects_locations_workflow_templates_delete` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_delete_builder()` + `dataproc_projects_locations_workflow_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_workflow_templates_delete_builder(client, name, version)?;
    dataproc_projects_locations_workflow_templates_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_get_execute()` to send, or `dataproc_projects_locations_workflow_templates_get` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_get_builder()` + `dataproc_projects_locations_workflow_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_workflow_templates_get_builder(client, name, version)?;
    dataproc_projects_locations_workflow_templates_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_get_iam_policy_execute()` to send, or `dataproc_projects_locations_workflow_templates_get_iam_policy` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_get_iam_policy_builder()` + `dataproc_projects_locations_workflow_templates_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_get_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_locations_workflow_templates_get_iam_policy_execute(builder)
}

/// GET v1/{+name}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_instantiate_execute()` to send, or `dataproc_projects_locations_workflow_templates_instantiate` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_instantiate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &InstantiateWorkflowTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:instantiate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_instantiate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_instantiate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_instantiate_builder()` + `dataproc_projects_locations_workflow_templates_instantiate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate(
    client: &SimpleHttpClient,
    name: &str,
    body: &InstantiateWorkflowTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_workflow_templates_instantiate_builder(client, name, body)?;
    dataproc_projects_locations_workflow_templates_instantiate_execute(builder)
}

/// GET v1/{+parent}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_instantiate_inline_execute()` to send, or `dataproc_projects_locations_workflow_templates_instantiate_inline` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_instantiate_inline_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/workflowTemplates:instantiateInline",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_instantiate_inline_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_instantiate_inline_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate_inline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_instantiate_inline_builder()` + `dataproc_projects_locations_workflow_templates_instantiate_inline_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate_inline(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &WorkflowTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_instantiate_inline_builder(
        client, parent, requestId, body,
    )?;
    dataproc_projects_locations_workflow_templates_instantiate_inline_execute(builder)
}

/// GET v1/{+parent}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_list_execute()` to send, or `dataproc_projects_locations_workflow_templates_list` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/workflowTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkflowTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_list_builder()` + `dataproc_projects_locations_workflow_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    dataproc_projects_locations_workflow_templates_list_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_set_iam_policy_execute()` to send, or `dataproc_projects_locations_workflow_templates_set_iam_policy` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_set_iam_policy_builder()` + `dataproc_projects_locations_workflow_templates_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_set_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_locations_workflow_templates_set_iam_policy_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_test_iam_permissions_execute()` to send, or `dataproc_projects_locations_workflow_templates_test_iam_permissions` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_test_iam_permissions_builder()` + `dataproc_projects_locations_workflow_templates_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_test_iam_permissions_builder(
        client, resource, body,
    )?;
    dataproc_projects_locations_workflow_templates_test_iam_permissions_execute(builder)
}

/// GET v1/{+name}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_update_execute()` to send, or `dataproc_projects_locations_workflow_templates_update` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_locations_workflow_templates_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_locations_workflow_templates_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_update_builder()` + `dataproc_projects_locations_workflow_templates_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &WorkflowTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_workflow_templates_update_builder(client, name, body)?;
    dataproc_projects_locations_workflow_templates_update_execute(builder)
}

/// GET v1/{+parent}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_create_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_create` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/autoscalingPolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_create_builder()` + `dataproc_projects_regions_autoscaling_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AutoscalingPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_autoscaling_policies_create_builder(client, parent, body)?;
    dataproc_projects_regions_autoscaling_policies_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_delete_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_delete` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_delete_builder()` + `dataproc_projects_regions_autoscaling_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_delete_builder(client, name)?;
    dataproc_projects_regions_autoscaling_policies_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieves autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_get_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_get` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_get_builder()` + `dataproc_projects_regions_autoscaling_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_get_builder(client, name)?;
    dataproc_projects_regions_autoscaling_policies_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder()` + `dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute(builder)
}

/// GET v1/{+parent}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_list_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_list` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/autoscalingPolicies",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAutoscalingPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_list_builder()` + `dataproc_projects_regions_autoscaling_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    dataproc_projects_regions_autoscaling_policies_list_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder()` + `dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder()` + `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder(
        client, resource, body,
    )?;
    dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute(builder)
}

/// GET v1/{+name}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_update_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_update` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_autoscaling_policies_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_update_builder()` + `dataproc_projects_regions_autoscaling_policies_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &AutoscalingPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_autoscaling_policies_update_builder(client, name, body)?;
    dataproc_projects_regions_autoscaling_policies_update_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Creates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_create_execute()` to send, or `dataproc_projects_regions_clusters_create` for simplest API.

pub fn dataproc_projects_regions_clusters_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    actionOnFailedPrimaryWorkers: Option<&str>,
    requestId: Option<&str>,
    body: &Cluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters",
        projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "actionOnFailedPrimaryWorkers",
            actionOnFailedPrimaryWorkers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Creates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Creates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_create_builder()` + `dataproc_projects_regions_clusters_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_create(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    actionOnFailedPrimaryWorkers: Option<&str>,
    requestId: Option<&str>,
    body: &Cluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_create_builder(
        client,
        projectId,
        region,
        actionOnFailedPrimaryWorkers,
        requestId,
        body,
    )?;
    dataproc_projects_regions_clusters_create_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Deletes a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_delete_execute()` to send, or `dataproc_projects_regions_clusters_delete` for simplest API.

pub fn dataproc_projects_regions_clusters_delete_builder(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    clusterUuid: Option<&str>,
    gracefulTerminationTimeout: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters/{}",
        clusterName, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clusterUuid",
            clusterUuid
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "gracefulTerminationTimeout",
            gracefulTerminationTimeout
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Deletes a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Deletes a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_delete_builder()` + `dataproc_projects_regions_clusters_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_delete(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    clusterUuid: Option<&str>,
    gracefulTerminationTimeout: Option<&str>,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_delete_builder(
        client,
        clusterName,
        projectId,
        region,
        clusterUuid,
        gracefulTerminationTimeout,
        requestId,
    )?;
    dataproc_projects_regions_clusters_delete_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
/// Gets cluster diagnostic information. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). After the operation completes, Operation.response contains DiagnoseClusterResults (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#diagnoseclusterresults>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_diagnose_execute()` to send, or `dataproc_projects_regions_clusters_diagnose` for simplest API.

pub fn dataproc_projects_regions_clusters_diagnose_builder(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &DiagnoseClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters/{}:diagnose",
        clusterName, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
/// Gets cluster diagnostic information. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). After the operation completes, Operation.response contains DiagnoseClusterResults (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#diagnoseclusterresults>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_diagnose_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_diagnose_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_diagnose_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
/// Gets cluster diagnostic information. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). After the operation completes, Operation.response contains DiagnoseClusterResults (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#diagnoseclusterresults>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_diagnose_builder()` + `dataproc_projects_regions_clusters_diagnose_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_diagnose(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &DiagnoseClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_diagnose_builder(
        client,
        clusterName,
        projectId,
        region,
        body,
    )?;
    dataproc_projects_regions_clusters_diagnose_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Gets the resource representation for a cluster in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_get_execute()` to send, or `dataproc_projects_regions_clusters_get` for simplest API.

pub fn dataproc_projects_regions_clusters_get_builder(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters/{}",
        clusterName, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Gets the resource representation for a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Gets the resource representation for a cluster in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_get_builder()` + `dataproc_projects_regions_clusters_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_clusters_get_builder(client, clusterName, projectId, region)?;
    dataproc_projects_regions_clusters_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_get_iam_policy_execute()` to send, or `dataproc_projects_regions_clusters_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_clusters_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_get_iam_policy_builder()` + `dataproc_projects_regions_clusters_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_clusters_get_iam_policy_builder(client, resource, body)?;
    dataproc_projects_regions_clusters_get_iam_policy_execute(builder)
}

/// GET v1/{+project}/{+region}/{+cluster}:injectCredentials
/// Inject encrypted credentials into all of the VMs in a cluster.The target cluster must be a personal auth cluster assigned to the user who is issuing the RPC.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_inject_credentials_execute()` to send, or `dataproc_projects_regions_clusters_inject_credentials` for simplest API.

pub fn dataproc_projects_regions_clusters_inject_credentials_builder(
    client: &SimpleHttpClient,
    cluster: &str,
    project: &str,
    region: &str,
    body: &InjectCredentialsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+project}/{+region}/{+cluster}:injectCredentials",
        cluster, project, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+project}/{+region}/{+cluster}:injectCredentials
/// Inject encrypted credentials into all of the VMs in a cluster.The target cluster must be a personal auth cluster assigned to the user who is issuing the RPC.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_inject_credentials_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_inject_credentials_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_inject_credentials_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+project}/{+region}/{+cluster}:injectCredentials
/// Inject encrypted credentials into all of the VMs in a cluster.The target cluster must be a personal auth cluster assigned to the user who is issuing the RPC.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_inject_credentials_builder()` + `dataproc_projects_regions_clusters_inject_credentials_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_inject_credentials(
    client: &SimpleHttpClient,
    cluster: &str,
    project: &str,
    region: &str,
    body: &InjectCredentialsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_inject_credentials_builder(
        client, cluster, project, region, body,
    )?;
    dataproc_projects_regions_clusters_inject_credentials_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Lists all `regions/{region}/clusters` in a project alphabetically.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_list_execute()` to send, or `dataproc_projects_regions_clusters_list` for simplest API.

pub fn dataproc_projects_regions_clusters_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters",
        projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Lists all `regions/{region}/clusters` in a project alphabetically.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Lists all `regions/{region}/clusters` in a project alphabetically.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_list_builder()` + `dataproc_projects_regions_clusters_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_list(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_list_builder(
        client, projectId, region, filter, pageSize, pageToken,
    )?;
    dataproc_projects_regions_clusters_list_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Updates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). The cluster must be in a RUNNING state or an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_patch_execute()` to send, or `dataproc_projects_regions_clusters_patch` for simplest API.

pub fn dataproc_projects_regions_clusters_patch_builder(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    gracefulDecommissionTimeout: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Cluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters/{}",
        clusterName, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "gracefulDecommissionTimeout",
            gracefulDecommissionTimeout
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Updates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). The cluster must be in a RUNNING state or an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Updates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). The cluster must be in a RUNNING state or an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_patch_builder()` + `dataproc_projects_regions_clusters_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_patch(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    gracefulDecommissionTimeout: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Cluster,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_patch_builder(
        client,
        clusterName,
        projectId,
        region,
        gracefulDecommissionTimeout,
        requestId,
        updateMask,
        body,
    )?;
    dataproc_projects_regions_clusters_patch_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
/// Repairs a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_repair_execute()` to send, or `dataproc_projects_regions_clusters_repair` for simplest API.

pub fn dataproc_projects_regions_clusters_repair_builder(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &RepairClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters/{}:repair",
        clusterName, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
/// Repairs a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_repair_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_repair_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_repair_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
/// Repairs a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_repair_builder()` + `dataproc_projects_regions_clusters_repair_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_repair(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &RepairClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_repair_builder(
        client,
        clusterName,
        projectId,
        region,
        body,
    )?;
    dataproc_projects_regions_clusters_repair_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_set_iam_policy_execute()` to send, or `dataproc_projects_regions_clusters_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_clusters_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_set_iam_policy_builder()` + `dataproc_projects_regions_clusters_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_clusters_set_iam_policy_builder(client, resource, body)?;
    dataproc_projects_regions_clusters_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
/// Starts a cluster in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_start_execute()` to send, or `dataproc_projects_regions_clusters_start` for simplest API.

pub fn dataproc_projects_regions_clusters_start_builder(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &StartClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters/{}:start",
        clusterName, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
/// Starts a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_start_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_start_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
/// Starts a cluster in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_start_builder()` + `dataproc_projects_regions_clusters_start_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_start(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &StartClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_start_builder(
        client,
        clusterName,
        projectId,
        region,
        body,
    )?;
    dataproc_projects_regions_clusters_start_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
/// Stops a cluster in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_stop_execute()` to send, or `dataproc_projects_regions_clusters_stop` for simplest API.

pub fn dataproc_projects_regions_clusters_stop_builder(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &StopClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/clusters/{}:stop",
        clusterName, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
/// Stops a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
/// Stops a cluster in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_stop_builder()` + `dataproc_projects_regions_clusters_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_stop(
    client: &SimpleHttpClient,
    clusterName: &str,
    projectId: &str,
    region: &str,
    body: &StopClusterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_stop_builder(
        client,
        clusterName,
        projectId,
        region,
        body,
    )?;
    dataproc_projects_regions_clusters_stop_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_clusters_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_clusters_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_test_iam_permissions_builder()` + `dataproc_projects_regions_clusters_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_clusters_test_iam_permissions_builder(client, resource, body)?;
    dataproc_projects_regions_clusters_test_iam_permissions_execute(builder)
}

/// GET v1/{+parent}/nodeGroups
/// Creates a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_create_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_create` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    nodeGroupId: Option<&str>,
    parentOperationId: Option<&str>,
    requestId: Option<&str>,
    body: &NodeGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/nodeGroups", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "nodeGroupId",
            nodeGroupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parentOperationId",
            parentOperationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/nodeGroups
/// Creates a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_node_groups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/nodeGroups
/// Creates a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_create_builder()` + `dataproc_projects_regions_clusters_node_groups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_create(
    client: &SimpleHttpClient,
    parent: &str,
    nodeGroupId: Option<&str>,
    parentOperationId: Option<&str>,
    requestId: Option<&str>,
    body: &NodeGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_node_groups_create_builder(
        client,
        parent,
        nodeGroupId,
        parentOperationId,
        requestId,
        body,
    )?;
    dataproc_projects_regions_clusters_node_groups_create_execute(builder)
}

/// GET v1/{+name}
/// Gets the resource representation for a node group in a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_get_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_get` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the resource representation for a node group in a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_node_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the resource representation for a node group in a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_get_builder()` + `dataproc_projects_regions_clusters_node_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_node_groups_get_builder(client, name)?;
    dataproc_projects_regions_clusters_node_groups_get_execute(builder)
}

/// GET v1/{+name}:repair
/// Repair nodes in a node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_repair_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_repair` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_repair_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RepairNodeGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:repair", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:repair
/// Repair nodes in a node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_repair_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_node_groups_repair_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_repair_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:repair
/// Repair nodes in a node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_repair_builder()` + `dataproc_projects_regions_clusters_node_groups_repair_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_repair(
    client: &SimpleHttpClient,
    name: &str,
    body: &RepairNodeGroupRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_clusters_node_groups_repair_builder(client, name, body)?;
    dataproc_projects_regions_clusters_node_groups_repair_execute(builder)
}

/// GET v1/{+name}:resize
/// Resizes a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_resize_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_resize` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_resize_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResizeNodeGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:resize", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:resize
/// Resizes a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_resize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_clusters_node_groups_resize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:resize
/// Resizes a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_resize_builder()` + `dataproc_projects_regions_clusters_node_groups_resize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_resize(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResizeNodeGroupRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_clusters_node_groups_resize_builder(client, name, body)?;
    dataproc_projects_regions_clusters_node_groups_resize_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
/// Starts a job cancellation request. To access the job resource after cancellation, call `regions/{region}/jobs`.list (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/list`>) or `regions/{region}/jobs`.get (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/get`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_cancel_execute()` to send, or `dataproc_projects_regions_jobs_cancel` for simplest API.

pub fn dataproc_projects_regions_jobs_cancel_builder(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
    body: &CancelJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/jobs/{}:cancel",
        jobId, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
/// Starts a job cancellation request. To access the job resource after cancellation, call `regions/{region}/jobs`.list (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/list`>) or `regions/{region}/jobs`.get (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/get`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
/// Starts a job cancellation request. To access the job resource after cancellation, call `regions/{region}/jobs`.list (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/list`>) or `regions/{region}/jobs`.get (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/get`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_cancel_builder()` + `dataproc_projects_regions_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_cancel(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
    body: &CancelJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_jobs_cancel_builder(client, jobId, projectId, region, body)?;
    dataproc_projects_regions_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Deletes the job from the project. If the job is active, the delete fails, and the response returns FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_delete_execute()` to send, or `dataproc_projects_regions_jobs_delete` for simplest API.

pub fn dataproc_projects_regions_jobs_delete_builder(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/jobs/{}",
        jobId, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Deletes the job from the project. If the job is active, the delete fails, and the response returns FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Deletes the job from the project. If the job is active, the delete fails, and the response returns FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_delete_builder()` + `dataproc_projects_regions_jobs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_delete(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_delete_builder(client, jobId, projectId, region)?;
    dataproc_projects_regions_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Gets the resource representation for a job in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_get_execute()` to send, or `dataproc_projects_regions_jobs_get` for simplest API.

pub fn dataproc_projects_regions_jobs_get_builder(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/jobs/{}",
        jobId, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Gets the resource representation for a job in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Gets the resource representation for a job in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_get_builder()` + `dataproc_projects_regions_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_get_builder(client, jobId, projectId, region)?;
    dataproc_projects_regions_jobs_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_get_iam_policy_execute()` to send, or `dataproc_projects_regions_jobs_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_jobs_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_get_iam_policy_builder()` + `dataproc_projects_regions_jobs_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_get_iam_policy_builder(client, resource, body)?;
    dataproc_projects_regions_jobs_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs
/// Lists `regions/{region}/jobs` in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_list_execute()` to send, or `dataproc_projects_regions_jobs_list` for simplest API.

pub fn dataproc_projects_regions_jobs_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: Option<&str>,
    filter: Option<&str>,
    jobStateMatcher: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/jobs",
        projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "clusterName",
            clusterName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "jobStateMatcher",
            jobStateMatcher
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/projects/{projectId}/regions/{region}/jobs
/// Lists `regions/{region}/jobs` in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs
/// Lists `regions/{region}/jobs` in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_list_builder()` + `dataproc_projects_regions_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_list(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: Option<&str>,
    filter: Option<&str>,
    jobStateMatcher: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_list_builder(
        client,
        projectId,
        region,
        clusterName,
        filter,
        jobStateMatcher,
        pageSize,
        pageToken,
    )?;
    dataproc_projects_regions_jobs_list_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Updates a job in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_patch_execute()` to send, or `dataproc_projects_regions_jobs_patch` for simplest API.

pub fn dataproc_projects_regions_jobs_patch_builder(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/jobs/{}",
        jobId, projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Updates a job in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Updates a job in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_patch_builder()` + `dataproc_projects_regions_jobs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_patch(
    client: &SimpleHttpClient,
    jobId: &str,
    projectId: &str,
    region: &str,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_patch_builder(
        client, jobId, projectId, region, updateMask, body,
    )?;
    dataproc_projects_regions_jobs_patch_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_set_iam_policy_execute()` to send, or `dataproc_projects_regions_jobs_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_jobs_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_set_iam_policy_builder()` + `dataproc_projects_regions_jobs_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_set_iam_policy_builder(client, resource, body)?;
    dataproc_projects_regions_jobs_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submit
/// Submits a job to a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_submit_execute()` to send, or `dataproc_projects_regions_jobs_submit` for simplest API.

pub fn dataproc_projects_regions_jobs_submit_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    body: &SubmitJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/jobs:submit",
        projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submit
/// Submits a job to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_submit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_submit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submit
/// Submits a job to a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_submit_builder()` + `dataproc_projects_regions_jobs_submit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    body: &SubmitJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_submit_builder(client, projectId, region, body)?;
    dataproc_projects_regions_jobs_submit_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
/// Submits job to a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_submit_as_operation_execute()` to send, or `dataproc_projects_regions_jobs_submit_as_operation` for simplest API.

pub fn dataproc_projects_regions_jobs_submit_as_operation_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    body: &SubmitJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/projects/{}/regions/{}/jobs:submitAsOperation",
        projectId, region,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
/// Submits job to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_submit_as_operation_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_submit_as_operation_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit_as_operation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
/// Submits job to a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_submit_as_operation_builder()` + `dataproc_projects_regions_jobs_submit_as_operation_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit_as_operation(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    body: &SubmitJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_submit_as_operation_builder(
        client, projectId, region, body,
    )?;
    dataproc_projects_regions_jobs_submit_as_operation_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_jobs_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_jobs_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_jobs_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_jobs_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_test_iam_permissions_builder()` + `dataproc_projects_regions_jobs_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_jobs_test_iam_permissions_builder(client, resource, body)?;
    dataproc_projects_regions_jobs_test_iam_permissions_execute(builder)
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_cancel_execute()` to send, or `dataproc_projects_regions_operations_cancel` for simplest API.

pub fn dataproc_projects_regions_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_cancel_builder()` + `dataproc_projects_regions_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_cancel_builder(client, name)?;
    dataproc_projects_regions_operations_cancel_execute(builder)
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_delete_execute()` to send, or `dataproc_projects_regions_operations_delete` for simplest API.

pub fn dataproc_projects_regions_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_delete_builder()` + `dataproc_projects_regions_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_delete_builder(client, name)?;
    dataproc_projects_regions_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_get_execute()` to send, or `dataproc_projects_regions_operations_get` for simplest API.

pub fn dataproc_projects_regions_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_get_builder()` + `dataproc_projects_regions_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_get_builder(client, name)?;
    dataproc_projects_regions_operations_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_get_iam_policy_execute()` to send, or `dataproc_projects_regions_operations_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_operations_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_operations_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_operations_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_get_iam_policy_builder()` + `dataproc_projects_regions_operations_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_operations_get_iam_policy_builder(client, resource, body)?;
    dataproc_projects_regions_operations_get_iam_policy_execute(builder)
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_list_execute()` to send, or `dataproc_projects_regions_operations_list` for simplest API.

pub fn dataproc_projects_regions_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_list_builder()` + `dataproc_projects_regions_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    dataproc_projects_regions_operations_list_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_set_iam_policy_execute()` to send, or `dataproc_projects_regions_operations_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_operations_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_operations_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_operations_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_set_iam_policy_builder()` + `dataproc_projects_regions_operations_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_operations_set_iam_policy_builder(client, resource, body)?;
    dataproc_projects_regions_operations_set_iam_policy_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_operations_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_operations_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_operations_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_operations_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_test_iam_permissions_builder()` + `dataproc_projects_regions_operations_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_operations_test_iam_permissions_builder(client, resource, body)?;
    dataproc_projects_regions_operations_test_iam_permissions_execute(builder)
}

/// GET v1/{+parent}/workflowTemplates
/// Creates new workflow template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_create_execute()` to send, or `dataproc_projects_regions_workflow_templates_create` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/workflowTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/workflowTemplates
/// Creates new workflow template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/workflowTemplates
/// Creates new workflow template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_create_builder()` + `dataproc_projects_regions_workflow_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WorkflowTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_workflow_templates_create_builder(client, parent, body)?;
    dataproc_projects_regions_workflow_templates_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_delete_execute()` to send, or `dataproc_projects_regions_workflow_templates_delete` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_delete_builder()` + `dataproc_projects_regions_workflow_templates_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_delete(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_workflow_templates_delete_builder(client, name, version)?;
    dataproc_projects_regions_workflow_templates_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_get_execute()` to send, or `dataproc_projects_regions_workflow_templates_get` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_get_builder()` + `dataproc_projects_regions_workflow_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_get_builder(client, name, version)?;
    dataproc_projects_regions_workflow_templates_get_execute(builder)
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_get_iam_policy_execute()` to send, or `dataproc_projects_regions_workflow_templates_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_get_iam_policy_builder()` + `dataproc_projects_regions_workflow_templates_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_get_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_regions_workflow_templates_get_iam_policy_execute(builder)
}

/// GET v1/{+name}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_instantiate_execute()` to send, or `dataproc_projects_regions_workflow_templates_instantiate` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_instantiate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &InstantiateWorkflowTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:instantiate", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_instantiate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_instantiate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_instantiate_builder()` + `dataproc_projects_regions_workflow_templates_instantiate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate(
    client: &SimpleHttpClient,
    name: &str,
    body: &InstantiateWorkflowTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_workflow_templates_instantiate_builder(client, name, body)?;
    dataproc_projects_regions_workflow_templates_instantiate_execute(builder)
}

/// GET v1/{+parent}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_instantiate_inline_execute()` to send, or `dataproc_projects_regions_workflow_templates_instantiate_inline` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_instantiate_inline_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/workflowTemplates:instantiateInline",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_instantiate_inline_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_instantiate_inline_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate_inline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_instantiate_inline_builder()` + `dataproc_projects_regions_workflow_templates_instantiate_inline_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate_inline(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &WorkflowTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_instantiate_inline_builder(
        client, parent, requestId, body,
    )?;
    dataproc_projects_regions_workflow_templates_instantiate_inline_execute(builder)
}

/// GET v1/{+parent}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_list_execute()` to send, or `dataproc_projects_regions_workflow_templates_list` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/workflowTemplates",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkflowTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_list_builder()` + `dataproc_projects_regions_workflow_templates_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    dataproc_projects_regions_workflow_templates_list_execute(builder)
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_set_iam_policy_execute()` to send, or `dataproc_projects_regions_workflow_templates_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_set_iam_policy_builder()` + `dataproc_projects_regions_workflow_templates_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_set_iam_policy_builder(
        client, resource, body,
    )?;
    dataproc_projects_regions_workflow_templates_set_iam_policy_execute(builder)
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_workflow_templates_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_test_iam_permissions_builder()` + `dataproc_projects_regions_workflow_templates_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_test_iam_permissions_builder(
        client, resource, body,
    )?;
    dataproc_projects_regions_workflow_templates_test_iam_permissions_execute(builder)
}

/// GET v1/{+name}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_update_execute()` to send, or `dataproc_projects_regions_workflow_templates_update` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataproc_projects_regions_workflow_templates_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataproc_projects_regions_workflow_templates_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_update_builder()` + `dataproc_projects_regions_workflow_templates_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &WorkflowTemplate,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_update_builder(client, name, body)?;
    dataproc_projects_regions_workflow_templates_update_execute(builder)
}
