//! Auto-generated API clients for gcp/dataproc.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_create_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_create` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_create_execute()` or `dataproc_projects_locations_autoscaling_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_create_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_create_builder()` + `dataproc_projects_locations_autoscaling_policies_create_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    dataproc_projects_locations_autoscaling_policies_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_delete_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_delete` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_delete_execute()` or `dataproc_projects_locations_autoscaling_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_delete_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_delete_builder()` + `dataproc_projects_locations_autoscaling_policies_delete_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_autoscaling_policies_delete_builder(client, &args.name)?;
    dataproc_projects_locations_autoscaling_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_get_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_get` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_get_execute()` or `dataproc_projects_locations_autoscaling_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_get_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_get_builder()` + `dataproc_projects_locations_autoscaling_policies_get_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_get_builder(client, &args.name)?;
    dataproc_projects_locations_autoscaling_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_get_iam_policy` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute()` or `dataproc_projects_locations_autoscaling_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder()` + `dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_get_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_locations_autoscaling_policies_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_list_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_list` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_list_execute()` or `dataproc_projects_locations_autoscaling_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAutoscalingPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_list_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_list_builder()` + `dataproc_projects_locations_autoscaling_policies_list_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_locations_autoscaling_policies_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_set_iam_policy` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute()` or `dataproc_projects_locations_autoscaling_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder()` + `dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_locations_autoscaling_policies_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_test_iam_permissions` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute()` or `dataproc_projects_locations_autoscaling_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder()` + `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_locations_autoscaling_policies_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_autoscaling_policies_update_execute()` to send, or `dataproc_projects_locations_autoscaling_policies_update` for simplest API.

pub fn dataproc_projects_locations_autoscaling_policies_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/autoscalingPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_autoscaling_policies_update_execute()` or `dataproc_projects_locations_autoscaling_policies_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_autoscaling_policies_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_update_task()`.
/// For the simplest API, use `dataproc_projects_locations_autoscaling_policies_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_autoscaling_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_autoscaling_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_autoscaling_policies_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_autoscaling_policies_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_autoscaling_policies_update_builder()` + `dataproc_projects_locations_autoscaling_policies_update_execute()`.
/// For task-level control, use `dataproc_projects_locations_autoscaling_policies_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_autoscaling_policies_update(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsAutoscalingPoliciesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_autoscaling_policies_update_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_locations_autoscaling_policies_update_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}:analyze
/// Analyze a Batch for possible recommendations and insights.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_analyze_execute()` to send, or `dataproc_projects_locations_batches_analyze` for simplest API.

pub fn dataproc_projects_locations_batches_analyze_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AnalyzeBatchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}:analyze",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}:analyze
/// Analyze a Batch for possible recommendations and insights.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_analyze_execute()` or `dataproc_projects_locations_batches_analyze`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_analyze_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_analyze_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}:analyze
/// Analyze a Batch for possible recommendations and insights.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_analyze_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_analyze_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_analyze()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_analyze_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_analyze_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_analyze_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_analyze`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesAnalyzeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AnalyzeBatchRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}:analyze
/// Analyze a Batch for possible recommendations and insights.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_analyze_builder()` + `dataproc_projects_locations_batches_analyze_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_analyze_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_analyze(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesAnalyzeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_analyze_builder(client, &args.name, &args.body)?;
    dataproc_projects_locations_batches_analyze_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Creates a batch workload that executes asynchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_create_execute()` to send, or `dataproc_projects_locations_batches_create` for simplest API.

pub fn dataproc_projects_locations_batches_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    batchId: Option<&str>,
    requestId: Option<&str>,
    body: &Batch,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = batchId {
        query_parts.push(format!("batchId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Creates a batch workload that executes asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_create_execute()` or `dataproc_projects_locations_batches_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Creates a batch workload that executes asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_create_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: batchId
    pub batchId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Batch,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Creates a batch workload that executes asynchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_create_builder()` + `dataproc_projects_locations_batches_create_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_create_builder(
        client,
        &args.parent,
        args.batchId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    dataproc_projects_locations_batches_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Deletes the batch workload resource. If the batch is not in a CANCELLED, SUCCEEDED or FAILED State, the delete operation fails and the response returns FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_delete_execute()` to send, or `dataproc_projects_locations_batches_delete` for simplest API.

pub fn dataproc_projects_locations_batches_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Deletes the batch workload resource. If the batch is not in a CANCELLED, SUCCEEDED or FAILED State, the delete operation fails and the response returns FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_delete_execute()` or `dataproc_projects_locations_batches_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Deletes the batch workload resource. If the batch is not in a CANCELLED, SUCCEEDED or FAILED State, the delete operation fails and the response returns FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_delete_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Deletes the batch workload resource. If the batch is not in a CANCELLED, SUCCEEDED or FAILED State, the delete operation fails and the response returns FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_delete_builder()` + `dataproc_projects_locations_batches_delete_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_delete_builder(client, &args.name)?;
    dataproc_projects_locations_batches_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Gets the batch workload resource representation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_get_execute()` to send, or `dataproc_projects_locations_batches_get` for simplest API.

pub fn dataproc_projects_locations_batches_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Gets the batch workload resource representation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_get_execute()` or `dataproc_projects_locations_batches_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Batch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Batch = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Gets the batch workload resource representation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_get_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Batch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}
/// Gets the batch workload resource representation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_get_builder()` + `dataproc_projects_locations_batches_get_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Batch>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_get_builder(client, &args.name)?;
    dataproc_projects_locations_batches_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Lists batch workloads.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_list_execute()` to send, or `dataproc_projects_locations_batches_list` for simplest API.

pub fn dataproc_projects_locations_batches_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Lists batch workloads.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_list_execute()` or `dataproc_projects_locations_batches_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListBatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBatchesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Lists batch workloads.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_list_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches
/// Lists batch workloads.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_list_builder()` + `dataproc_projects_locations_batches_list_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBatchesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_locations_batches_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:access",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_access_execute()` or `dataproc_projects_locations_batches_spark_applications_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_builder()` + `dataproc_projects_locations_batches_spark_applications_access_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_builder(
        client,
        &args.name,
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_access_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_environment_info_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_environment_info` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:accessEnvironmentInfo",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_access_environment_info_execute()` or `dataproc_projects_locations_batches_spark_applications_access_environment_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_environment_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_access_environment_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_environment_info_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_access_environment_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_environment_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_access_environment_info_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_access_environment_info`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessEnvironmentInfoArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_environment_info_builder()` + `dataproc_projects_locations_batches_spark_applications_access_environment_info_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_environment_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessEnvironmentInfoArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_access_environment_info_builder(
            client,
            &args.name,
            args.parent.as_deref(),
        )?;
    dataproc_projects_locations_batches_spark_applications_access_environment_info_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_job_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_job` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_job_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:accessJob",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobId {
        query_parts.push(format!("jobId={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_access_job_execute()` or `dataproc_projects_locations_batches_spark_applications_access_job`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_job_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_access_job_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_job_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_access_job()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_access_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_access_job_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_access_job`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessJobArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: jobId
    pub jobId: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_job_builder()` + `dataproc_projects_locations_batches_spark_applications_access_job_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_job_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_job(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessJobArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_job_builder(
        client,
        &args.name,
        args.jobId.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_access_job_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_sql_plan` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:accessSqlPlan",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionId {
        query_parts.push(format!("executionId={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute()` or `dataproc_projects_locations_batches_spark_applications_access_sql_plan`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlSparkPlanGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlSparkPlanGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_access_sql_plan_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_access_sql_plan`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlPlanArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionId
    pub executionId: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder()` + `dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_sql_plan_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlPlanArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlSparkPlanGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_sql_plan_builder(
        client,
        &args.name,
        args.executionId.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_access_sql_plan_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_sql_query_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_sql_query` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    executionId: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:accessSqlQuery",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = details {
        query_parts.push(format!("details={}", val));
    }
    if let Some(val) = executionId {
        query_parts.push(format!("executionId={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = planDescription {
        query_parts.push(format!("planDescription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_access_sql_query_execute()` or `dataproc_projects_locations_batches_spark_applications_access_sql_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_sql_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_sql_query_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_access_sql_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_sql_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_access_sql_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_access_sql_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: details
    pub details: Option<bool>,
    /// Query parameter: executionId
    pub executionId: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: planDescription
    pub planDescription: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_sql_query_builder()` + `dataproc_projects_locations_batches_spark_applications_access_sql_query_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_sql_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_access_sql_query_builder(
        client,
        &args.name,
        args.details,
        args.executionId.as_deref(),
        args.parent.as_deref(),
        args.planDescription,
    )?;
    dataproc_projects_locations_batches_spark_applications_access_sql_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_stage_attempt` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:accessStageAttempt",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }
    if let Some(val) = summaryMetricsMask {
        query_parts.push(format!("summaryMetricsMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute()` or `dataproc_projects_locations_batches_spark_applications_access_stage_attempt`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_access_stage_attempt_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_access_stage_attempt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessStageAttemptArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
    /// Query parameter: summaryMetricsMask
    pub summaryMetricsMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder()` + `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_stage_attempt_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessStageAttemptArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_access_stage_attempt_builder(
            client,
            &args.name,
            args.parent.as_deref(),
            args.stageAttemptId,
            args.stageId.as_deref(),
            args.summaryMetricsMask.as_deref(),
        )?;
    dataproc_projects_locations_batches_spark_applications_access_stage_attempt_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:accessStageRddGraph",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute()` or `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageRddOperationGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageRddOperationGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessStageRddGraphArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder()` + `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessStageRddGraphArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSparkApplicationStageRddOperationGraphResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_builder(
            client,
            &args.name,
            args.parent.as_deref(),
            args.stageId.as_deref(),
        )?;
    dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    applicationStatus: Option<&str>,
    maxEndTime: Option<&str>,
    maxTime: Option<&str>,
    minEndTime: Option<&str>,
    minTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationStatus {
        query_parts.push(format!("applicationStatus={}", val));
    }
    if let Some(val) = maxEndTime {
        query_parts.push(format!("maxEndTime={}", val));
    }
    if let Some(val) = maxTime {
        query_parts.push(format!("maxTime={}", val));
    }
    if let Some(val) = minEndTime {
        query_parts.push(format!("minEndTime={}", val));
    }
    if let Some(val) = minTime {
        query_parts.push(format!("minTime={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_execute()` or `dataproc_projects_locations_batches_spark_applications_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: applicationStatus
    pub applicationStatus: Option<String>,
    /// Query parameter: maxEndTime
    pub maxEndTime: Option<String>,
    /// Query parameter: maxTime
    pub maxTime: Option<String>,
    /// Query parameter: minEndTime
    pub minEndTime: Option<String>,
    /// Query parameter: minTime
    pub minTime: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_builder()` + `dataproc_projects_locations_batches_spark_applications_search_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_builder(
        client,
        &args.parent,
        args.applicationStatus.as_deref(),
        args.maxEndTime.as_deref(),
        args.maxTime.as_deref(),
        args.minEndTime.as_deref(),
        args.minTime.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:searchExecutorStageSummary",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute()` or `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorStageSummaryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorStageSummaryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorStageSummaryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder()` + `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorStageSummaryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorStageSummaryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_builder(client, &args.name, args.pageSize, args.pageToken.as_deref(), args.parent.as_deref(), args.stageAttemptId, args.stageId.as_deref())?;
    dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_executors_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_executors` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    executorStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:searchExecutors",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executorStatus {
        query_parts.push(format!("executorStatus={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_executors_execute()` or `dataproc_projects_locations_batches_spark_applications_search_executors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_executors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_executors_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search_executors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_search_executors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search_executors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executorStatus
    pub executorStatus: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_executors_builder()` + `dataproc_projects_locations_batches_spark_applications_search_executors_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_executors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_executors(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_executors_builder(
        client,
        &args.name,
        args.executorStatus.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_search_executors_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_jobs_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_jobs` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:searchJobs",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobStatus {
        query_parts.push(format!("jobStatus={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_jobs_execute()` or `dataproc_projects_locations_batches_spark_applications_search_jobs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_jobs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_jobs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_jobs_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search_jobs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_search_jobs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search_jobs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchJobsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: jobStatus
    pub jobStatus: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_jobs_builder()` + `dataproc_projects_locations_batches_spark_applications_search_jobs_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_jobs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_jobs(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchJobsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_jobs_builder(
        client,
        &args.name,
        args.jobStatus.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_search_jobs_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_sql_queries` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:searchSqlQueries",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = details {
        query_parts.push(format!("details={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = planDescription {
        query_parts.push(format!("planDescription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute()` or `dataproc_projects_locations_batches_spark_applications_search_sql_queries`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationSqlQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_search_sql_queries_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search_sql_queries`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchSqlQueriesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: details
    pub details: Option<bool>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: planDescription
    pub planDescription: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder()` + `dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_sql_queries_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchSqlQueriesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_search_sql_queries_builder(
            client,
            &args.name,
            args.details,
            args.pageSize,
            args.pageToken.as_deref(),
            args.parent.as_deref(),
            args.planDescription,
        )?;
    dataproc_projects_locations_batches_spark_applications_search_sql_queries_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    sortRuntime: Option<bool>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    taskStatus: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:searchStageAttemptTasks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = sortRuntime {
        query_parts.push(format!("sortRuntime={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }
    if let Some(val) = taskStatus {
        query_parts.push(format!("taskStatus={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute()` or `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptTasksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: sortRuntime
    pub sortRuntime: Option<bool>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
    /// Query parameter: taskStatus
    pub taskStatus: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder()` + `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptTasksArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_builder(
            client,
            &args.name,
            args.pageSize,
            args.pageToken.as_deref(),
            args.parent.as_deref(),
            args.sortRuntime,
            args.stageAttemptId,
            args.stageId.as_deref(),
            args.taskStatus.as_deref(),
        )?;
    dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_stage_attempts` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:searchStageAttempts",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }
    if let Some(val) = summaryMetricsMask {
        query_parts.push(format!("summaryMetricsMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute()` or `dataproc_projects_locations_batches_spark_applications_search_stage_attempts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_search_stage_attempts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search_stage_attempts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
    /// Query parameter: summaryMetricsMask
    pub summaryMetricsMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder()` + `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_stage_attempts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_search_stage_attempts_builder(
            client,
            &args.name,
            args.pageSize,
            args.pageToken.as_deref(),
            args.parent.as_deref(),
            args.stageId.as_deref(),
            args.summaryMetricsMask.as_deref(),
        )?;
    dataproc_projects_locations_batches_spark_applications_search_stage_attempts_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_search_stages_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_search_stages` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_search_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageStatus: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:searchStages",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageStatus {
        query_parts.push(format!("stageStatus={}", val));
    }
    if let Some(val) = summaryMetricsMask {
        query_parts.push(format!("summaryMetricsMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_search_stages_execute()` or `dataproc_projects_locations_batches_spark_applications_search_stages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_search_stages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_stages_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_search_stages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_search_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_search_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_search_stages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_search_stages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStagesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageStatus
    pub stageStatus: Option<String>,
    /// Query parameter: summaryMetricsMask
    pub summaryMetricsMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_search_stages_builder()` + `dataproc_projects_locations_batches_spark_applications_search_stages_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_search_stages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_search_stages(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchStagesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_search_stages_builder(
        client,
        &args.name,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
        args.stageStatus.as_deref(),
        args.summaryMetricsMask.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_search_stages_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_executors_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_executors` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:summarizeExecutors",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_summarize_executors_execute()` or `dataproc_projects_locations_batches_spark_applications_summarize_executors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_summarize_executors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_executors_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_summarize_executors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_summarize_executors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_summarize_executors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeExecutorsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_executors_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_executors_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_executors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeExecutorsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_batches_spark_applications_summarize_executors_builder(
            client,
            &args.name,
            args.parent.as_deref(),
        )?;
    dataproc_projects_locations_batches_spark_applications_summarize_executors_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_jobs` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:summarizeJobs",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute()` or `dataproc_projects_locations_batches_spark_applications_summarize_jobs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_summarize_jobs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_summarize_jobs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeJobsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_jobs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeJobsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_summarize_jobs_builder(
        client,
        &args.name,
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_summarize_jobs_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:summarizeStageAttemptTasks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute()` or `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStageAttemptTasksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStageAttemptTasksArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStageAttemptTasksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_builder(client, &args.name, args.parent.as_deref(), args.stageAttemptId, args.stageId.as_deref())?;
    dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_summarize_stages_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_summarize_stages` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:summarizeStages",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_summarize_stages_execute()` or `dataproc_projects_locations_batches_spark_applications_summarize_stages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStagesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_stages_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_summarize_stages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_summarize_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_batches_spark_applications_summarize_stages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_summarize_stages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStagesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_summarize_stages_builder()` + `dataproc_projects_locations_batches_spark_applications_summarize_stages_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_summarize_stages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStagesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_summarize_stages_builder(
        client,
        &args.name,
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_batches_spark_applications_summarize_stages_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_batches_spark_applications_write_execute()` to send, or `dataproc_projects_locations_batches_spark_applications_write` for simplest API.

pub fn dataproc_projects_locations_batches_spark_applications_write_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WriteSparkApplicationContextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/batches/{}/sparkApplications/{}:write",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_batches_spark_applications_write_execute()` or `dataproc_projects_locations_batches_spark_applications_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<WriteSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteSparkApplicationContextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_batches_spark_applications_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_write_task()`.
/// For the simplest API, use `dataproc_projects_locations_batches_spark_applications_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_batches_spark_applications_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_batches_spark_applications_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_batches_spark_applications_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_batches_spark_applications_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsWriteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: WriteSparkApplicationContextRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_batches_spark_applications_write_builder()` + `dataproc_projects_locations_batches_spark_applications_write_execute()`.
/// For task-level control, use `dataproc_projects_locations_batches_spark_applications_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_batches_spark_applications_write(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsWriteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_batches_spark_applications_write_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_locations_batches_spark_applications_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_cancel_execute()` to send, or `dataproc_projects_locations_operations_cancel` for simplest API.

pub fn dataproc_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_operations_cancel_execute()` or `dataproc_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `dataproc_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_cancel_builder()` + `dataproc_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `dataproc_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_cancel_builder(client, &args.name)?;
    dataproc_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_delete_execute()` to send, or `dataproc_projects_locations_operations_delete` for simplest API.

pub fn dataproc_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_operations_delete_execute()` or `dataproc_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_operations_delete_task()`.
/// For the simplest API, use `dataproc_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_delete_builder()` + `dataproc_projects_locations_operations_delete_execute()`.
/// For task-level control, use `dataproc_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_delete_builder(client, &args.name)?;
    dataproc_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_get_execute()` to send, or `dataproc_projects_locations_operations_get` for simplest API.

pub fn dataproc_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_operations_get_execute()` or `dataproc_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_operations_get_task()`.
/// For the simplest API, use `dataproc_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_get_builder()` + `dataproc_projects_locations_operations_get_execute()`.
/// For task-level control, use `dataproc_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_get_builder(client, &args.name)?;
    dataproc_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_operations_list_execute()` to send, or `dataproc_projects_locations_operations_list` for simplest API.

pub fn dataproc_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_operations_list_execute()` or `dataproc_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_operations_list_task()`.
/// For the simplest API, use `dataproc_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_operations_list_builder()` + `dataproc_projects_locations_operations_list_execute()`.
/// For task-level control, use `dataproc_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    dataproc_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Create a session template synchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_create_execute()` to send, or `dataproc_projects_locations_session_templates_create` for simplest API.

pub fn dataproc_projects_locations_session_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SessionTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessionTemplates",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Create a session template synchronously.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_session_templates_create_execute()` or `dataproc_projects_locations_session_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SessionTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Create a session template synchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_session_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_session_templates_create_task()`.
/// For the simplest API, use `dataproc_projects_locations_session_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_session_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_session_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_session_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SessionTemplate,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Create a session template synchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_create_builder()` + `dataproc_projects_locations_session_templates_create_execute()`.
/// For task-level control, use `dataproc_projects_locations_session_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    dataproc_projects_locations_session_templates_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Deletes a session template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_delete_execute()` to send, or `dataproc_projects_locations_session_templates_delete` for simplest API.

pub fn dataproc_projects_locations_session_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessionTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Deletes a session template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_session_templates_delete_execute()` or `dataproc_projects_locations_session_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Deletes a session template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_session_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_session_templates_delete_task()`.
/// For the simplest API, use `dataproc_projects_locations_session_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_session_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_session_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_session_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Deletes a session template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_delete_builder()` + `dataproc_projects_locations_session_templates_delete_execute()`.
/// For task-level control, use `dataproc_projects_locations_session_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_delete_builder(client, &args.name)?;
    dataproc_projects_locations_session_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Gets the resource representation for a session template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_get_execute()` to send, or `dataproc_projects_locations_session_templates_get` for simplest API.

pub fn dataproc_projects_locations_session_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessionTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Gets the resource representation for a session template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_session_templates_get_execute()` or `dataproc_projects_locations_session_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SessionTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Gets the resource representation for a session template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_session_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_session_templates_get_task()`.
/// For the simplest API, use `dataproc_projects_locations_session_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_session_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_session_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_session_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Gets the resource representation for a session template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_get_builder()` + `dataproc_projects_locations_session_templates_get_execute()`.
/// For task-level control, use `dataproc_projects_locations_session_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_get_builder(client, &args.name)?;
    dataproc_projects_locations_session_templates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Lists session templates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_list_execute()` to send, or `dataproc_projects_locations_session_templates_list` for simplest API.

pub fn dataproc_projects_locations_session_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessionTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Lists session templates.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_session_templates_list_execute()` or `dataproc_projects_locations_session_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListSessionTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSessionTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Lists session templates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_session_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_session_templates_list_task()`.
/// For the simplest API, use `dataproc_projects_locations_session_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_session_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSessionTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_session_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_session_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates
/// Lists session templates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_list_builder()` + `dataproc_projects_locations_session_templates_list_execute()`.
/// For task-level control, use `dataproc_projects_locations_session_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSessionTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_locations_session_templates_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Updates the session template synchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_session_templates_patch_execute()` to send, or `dataproc_projects_locations_session_templates_patch` for simplest API.

pub fn dataproc_projects_locations_session_templates_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SessionTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessionTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Updates the session template synchronously.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_session_templates_patch_execute()` or `dataproc_projects_locations_session_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SessionTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Updates the session template synchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_session_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_session_templates_patch_task()`.
/// For the simplest API, use `dataproc_projects_locations_session_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_session_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_session_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_session_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_session_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SessionTemplate,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessionTemplates/{sessionTemplatesId}
/// Updates the session template synchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_session_templates_patch_builder()` + `dataproc_projects_locations_session_templates_patch_execute()`.
/// For task-level control, use `dataproc_projects_locations_session_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_session_templates_patch(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SessionTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_session_templates_patch_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_locations_session_templates_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Create an interactive session asynchronously.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_create_execute()` to send, or `dataproc_projects_locations_sessions_create` for simplest API.

pub fn dataproc_projects_locations_sessions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    sessionId: Option<&str>,
    body: &Session,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = sessionId {
        query_parts.push(format!("sessionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Create an interactive session asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_create_execute()` or `dataproc_projects_locations_sessions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Create an interactive session asynchronously.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_create_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: sessionId
    pub sessionId: Option<String>,
    /// Request body.
    pub body: Session,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Create an interactive session asynchronously.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_create_builder()` + `dataproc_projects_locations_sessions_create_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_create_builder(
        client,
        &args.parent,
        args.requestId.as_deref(),
        args.sessionId.as_deref(),
        &args.body,
    )?;
    dataproc_projects_locations_sessions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Deletes the interactive session resource. If the session is not in terminal state, it is terminated, and then deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_delete_execute()` to send, or `dataproc_projects_locations_sessions_delete` for simplest API.

pub fn dataproc_projects_locations_sessions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Deletes the interactive session resource. If the session is not in terminal state, it is terminated, and then deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_delete_execute()` or `dataproc_projects_locations_sessions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Deletes the interactive session resource. If the session is not in terminal state, it is terminated, and then deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_delete_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Deletes the interactive session resource. If the session is not in terminal state, it is terminated, and then deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_delete_builder()` + `dataproc_projects_locations_sessions_delete_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_delete_builder(
        client,
        &args.name,
        args.requestId.as_deref(),
    )?;
    dataproc_projects_locations_sessions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Gets the resource representation for an interactive session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_get_execute()` to send, or `dataproc_projects_locations_sessions_get` for simplest API.

pub fn dataproc_projects_locations_sessions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Gets the resource representation for an interactive session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_get_execute()` or `dataproc_projects_locations_sessions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Session>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Gets the resource representation for an interactive session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_get_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Session>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}
/// Gets the resource representation for an interactive session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_get_builder()` + `dataproc_projects_locations_sessions_get_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Session>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_get_builder(client, &args.name)?;
    dataproc_projects_locations_sessions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Lists interactive sessions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_list_execute()` to send, or `dataproc_projects_locations_sessions_list` for simplest API.

pub fn dataproc_projects_locations_sessions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Lists interactive sessions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_list_execute()` or `dataproc_projects_locations_sessions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListSessionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSessionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Lists interactive sessions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_list_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSessionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions
/// Lists interactive sessions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_list_builder()` + `dataproc_projects_locations_sessions_list_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSessionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_locations_sessions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}:terminate
/// Terminates the interactive session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_terminate_execute()` to send, or `dataproc_projects_locations_sessions_terminate` for simplest API.

pub fn dataproc_projects_locations_sessions_terminate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &TerminateSessionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}:terminate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}:terminate
/// Terminates the interactive session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_terminate_execute()` or `dataproc_projects_locations_sessions_terminate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_terminate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_terminate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}:terminate
/// Terminates the interactive session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_terminate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_terminate_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_terminate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_terminate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_terminate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_terminate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_terminate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsTerminateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: TerminateSessionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}:terminate
/// Terminates the interactive session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_terminate_builder()` + `dataproc_projects_locations_sessions_terminate_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_terminate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_terminate(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsTerminateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_terminate_builder(client, &args.name, &args.body)?;
    dataproc_projects_locations_sessions_terminate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:access",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_access_execute()` or `dataproc_projects_locations_sessions_spark_applications_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:access
/// Obtain high level information corresponding to a single Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_builder(
        client,
        &args.name,
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_environment_info` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:accessEnvironmentInfo",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute()` or `dataproc_projects_locations_sessions_spark_applications_access_environment_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_access_environment_info_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_access_environment_info`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessEnvironmentInfoArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
/// Obtain environment details for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_environment_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessEnvironmentInfoArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationEnvironmentInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_access_environment_info_builder(
            client,
            &args.name,
            args.parent.as_deref(),
        )?;
    dataproc_projects_locations_sessions_spark_applications_access_environment_info_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_job_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_job` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_job_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:accessJob",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobId {
        query_parts.push(format!("jobId={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_access_job_execute()` or `dataproc_projects_locations_sessions_spark_applications_access_job`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_job_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_access_job_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_job_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_access_job()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_job_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_access_job_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_access_job_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_access_job`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessJobArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: jobId
    pub jobId: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessJob
/// Obtain data corresponding to a spark job for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_job_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_job_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_job_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_job(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessJobArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationJobResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_job_builder(
        client,
        &args.name,
        args.jobId.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_job_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_sql_plan` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder(
    client: &SimpleHttpClient,
    name: &str,
    executionId: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:accessSqlPlan",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executionId {
        query_parts.push(format!("executionId={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute()` or `dataproc_projects_locations_sessions_spark_applications_access_sql_plan`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationSqlSparkPlanGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationSqlSparkPlanGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_access_sql_plan_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_access_sql_plan`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlPlanArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executionId
    pub executionId: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
/// Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_sql_plan_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlPlanArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationSqlSparkPlanGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_sql_plan_builder(
        client,
        &args.name,
        args.executionId.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_sql_plan_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_sql_query` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    executionId: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:accessSqlQuery",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = details {
        query_parts.push(format!("details={}", val));
    }
    if let Some(val) = executionId {
        query_parts.push(format!("executionId={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = planDescription {
        query_parts.push(format!("planDescription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute()` or `dataproc_projects_locations_sessions_spark_applications_access_sql_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_access_sql_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_access_sql_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlQueryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: details
    pub details: Option<bool>,
    /// Query parameter: executionId
    pub executionId: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: planDescription
    pub planDescription: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
/// Obtain data corresponding to a particular SQL Query for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_sql_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationSqlQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_access_sql_query_builder(
        client,
        &args.name,
        args.details,
        args.executionId.as_deref(),
        args.parent.as_deref(),
        args.planDescription,
    )?;
    dataproc_projects_locations_sessions_spark_applications_access_sql_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:accessStageAttempt",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }
    if let Some(val) = summaryMetricsMask {
        query_parts.push(format!("summaryMetricsMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute()` or `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_access_stage_attempt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessStageAttemptArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
    /// Query parameter: summaryMetricsMask
    pub summaryMetricsMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
/// Obtain data corresponding to a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessStageAttemptArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AccessSessionSparkApplicationStageAttemptResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_builder(
            client,
            &args.name,
            args.parent.as_deref(),
            args.stageAttemptId,
            args.stageId.as_deref(),
            args.summaryMetricsMask.as_deref(),
        )?;
    dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:accessStageRddGraph",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute()` or `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationStageRddOperationGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationStageRddOperationGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessStageRddGraphArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
/// Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as part of the graph to 10000.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder()` + `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessStageRddGraphArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<AccessSessionSparkApplicationStageRddOperationGraphResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_builder(
            client,
            &args.name,
            args.parent.as_deref(),
            args.stageId.as_deref(),
        )?;
    dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    applicationStatus: Option<&str>,
    maxEndTime: Option<&str>,
    maxTime: Option<&str>,
    minEndTime: Option<&str>,
    minTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationStatus {
        query_parts.push(format!("applicationStatus={}", val));
    }
    if let Some(val) = maxEndTime {
        query_parts.push(format!("maxEndTime={}", val));
    }
    if let Some(val) = maxTime {
        query_parts.push(format!("maxTime={}", val));
    }
    if let Some(val) = minEndTime {
        query_parts.push(format!("minEndTime={}", val));
    }
    if let Some(val) = minTime {
        query_parts.push(format!("minTime={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_execute()` or `dataproc_projects_locations_sessions_spark_applications_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: applicationStatus
    pub applicationStatus: Option<String>,
    /// Query parameter: maxEndTime
    pub maxEndTime: Option<String>,
    /// Query parameter: maxTime
    pub maxTime: Option<String>,
    /// Query parameter: minEndTime
    pub minEndTime: Option<String>,
    /// Query parameter: minTime
    pub minTime: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications:search
/// Obtain high level information and list of Spark Applications corresponding to a batch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_builder(
        client,
        &args.parent,
        args.applicationStatus.as_deref(),
        args.maxEndTime.as_deref(),
        args.maxTime.as_deref(),
        args.minEndTime.as_deref(),
        args.minTime.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:searchExecutorStageSummary",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute()` or `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorStageSummaryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorStageSummaryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorStageSummaryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
/// Obtain executor summary with respect to a spark stage attempt.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorStageSummaryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorStageSummaryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_builder(client, &args.name, args.pageSize, args.pageToken.as_deref(), args.parent.as_deref(), args.stageAttemptId, args.stageId.as_deref())?;
    dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_executors_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_executors` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    executorStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:searchExecutors",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executorStatus {
        query_parts.push(format!("executorStatus={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_executors_execute()` or `dataproc_projects_locations_sessions_spark_applications_search_executors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_executors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_executors_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search_executors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_search_executors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search_executors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: executorStatus
    pub executorStatus: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutors
/// Obtain data corresponding to executors for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_executors_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_executors_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_executors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_executors(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_executors_builder(
        client,
        &args.name,
        args.executorStatus.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_executors_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_jobs_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_jobs` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobIds: Option<&str>,
    jobStatus: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:searchJobs",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobIds {
        query_parts.push(format!("jobIds={}", val));
    }
    if let Some(val) = jobStatus {
        query_parts.push(format!("jobStatus={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_jobs_execute()` or `dataproc_projects_locations_sessions_spark_applications_search_jobs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_jobs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_jobs_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search_jobs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_search_jobs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search_jobs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchJobsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: jobIds
    pub jobIds: Option<String>,
    /// Query parameter: jobStatus
    pub jobStatus: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchJobs
/// Obtain list of spark jobs corresponding to a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_jobs_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_jobs_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_jobs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchJobsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_jobs_builder(
        client,
        &args.name,
        args.jobIds.as_deref(),
        args.jobStatus.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_jobs_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_sql_queries` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder(
    client: &SimpleHttpClient,
    name: &str,
    details: Option<bool>,
    operationIds: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    planDescription: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:searchSqlQueries",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = details {
        query_parts.push(format!("details={}", val));
    }
    if let Some(val) = operationIds {
        query_parts.push(format!("operationIds={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = planDescription {
        query_parts.push(format!("planDescription={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute()` or `dataproc_projects_locations_sessions_spark_applications_search_sql_queries`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationSqlQueriesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_search_sql_queries_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search_sql_queries`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchSqlQueriesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: details
    pub details: Option<bool>,
    /// Query parameter: operationIds
    pub operationIds: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: planDescription
    pub planDescription: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
/// Obtain data corresponding to SQL Queries for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_sql_queries_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchSqlQueriesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationSqlQueriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_search_sql_queries_builder(
            client,
            &args.name,
            args.details,
            args.operationIds.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
            args.parent.as_deref(),
            args.planDescription,
        )?;
    dataproc_projects_locations_sessions_spark_applications_search_sql_queries_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    sortRuntime: Option<bool>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
    taskStatus: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:searchStageAttemptTasks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = sortRuntime {
        query_parts.push(format!("sortRuntime={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }
    if let Some(val) = taskStatus {
        query_parts.push(format!("taskStatus={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute()` or `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptTasksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: sortRuntime
    pub sortRuntime: Option<bool>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
    /// Query parameter: taskStatus
    pub taskStatus: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
/// Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptTasksArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_builder(
            client,
            &args.name,
            args.pageSize,
            args.pageToken.as_deref(),
            args.parent.as_deref(),
            args.sortRuntime,
            args.stageAttemptId,
            args.stageId.as_deref(),
            args.taskStatus.as_deref(),
        )?;
    dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageId: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:searchStageAttempts",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }
    if let Some(val) = summaryMetricsMask {
        query_parts.push(format!("summaryMetricsMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute()` or `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search_stage_attempts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
    /// Query parameter: summaryMetricsMask
    pub summaryMetricsMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
/// Obtain data corresponding to a spark stage attempts for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStageAttemptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_builder(
            client,
            &args.name,
            args.pageSize,
            args.pageToken.as_deref(),
            args.parent.as_deref(),
            args.stageId.as_deref(),
            args.summaryMetricsMask.as_deref(),
        )?;
    dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_search_stages_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_search_stages` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    stageIds: Option<&str>,
    stageStatus: Option<&str>,
    summaryMetricsMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:searchStages",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageIds {
        query_parts.push(format!("stageIds={}", val));
    }
    if let Some(val) = stageStatus {
        query_parts.push(format!("stageStatus={}", val));
    }
    if let Some(val) = summaryMetricsMask {
        query_parts.push(format!("summaryMetricsMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_search_stages_execute()` or `dataproc_projects_locations_sessions_spark_applications_search_stages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_search_stages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_stages_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_search_stages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_search_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_search_stages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_search_stages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStagesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageIds
    pub stageIds: Option<String>,
    /// Query parameter: stageStatus
    pub stageStatus: Option<String>,
    /// Query parameter: summaryMetricsMask
    pub summaryMetricsMask: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStages
/// Obtain data corresponding to stages for a Spark Application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_search_stages_builder()` + `dataproc_projects_locations_sessions_spark_applications_search_stages_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_search_stages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_search_stages(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchStagesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_search_stages_builder(
        client,
        &args.name,
        args.pageSize,
        args.pageToken.as_deref(),
        args.parent.as_deref(),
        args.stageIds.as_deref(),
        args.stageStatus.as_deref(),
        args.summaryMetricsMask.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_search_stages_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_executors` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:summarizeExecutors",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute()` or `dataproc_projects_locations_sessions_spark_applications_summarize_executors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_summarize_executors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_summarize_executors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeExecutorsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
/// Obtain summary of Executor Summary for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_executors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeExecutorsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationExecutorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_locations_sessions_spark_applications_summarize_executors_builder(
            client,
            &args.name,
            args.parent.as_deref(),
        )?;
    dataproc_projects_locations_sessions_spark_applications_summarize_executors_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_jobs` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder(
    client: &SimpleHttpClient,
    name: &str,
    jobIds: Option<&str>,
    parent: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:summarizeJobs",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobIds {
        query_parts.push(format!("jobIds={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute()` or `dataproc_projects_locations_sessions_spark_applications_summarize_jobs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_summarize_jobs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_summarize_jobs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeJobsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: jobIds
    pub jobIds: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
/// Obtain summary of Jobs for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_jobs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeJobsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_summarize_jobs_builder(
        client,
        &args.name,
        args.jobIds.as_deref(),
        args.parent.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_summarize_jobs_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageAttemptId: Option<i32>,
    stageId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:summarizeStageAttemptTasks",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageAttemptId {
        query_parts.push(format!("stageAttemptId={}", val));
    }
    if let Some(val) = stageId {
        query_parts.push(format!("stageId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute()` or `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<SummarizeSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SummarizeSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStageAttemptTasksArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageAttemptId
    pub stageAttemptId: Option<i32>,
    /// Query parameter: stageId
    pub stageId: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
/// Obtain summary of Tasks for a Spark Application Stage Attempt
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStageAttemptTasksArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<SummarizeSessionSparkApplicationStageAttemptTasksResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_builder(client, &args.name, args.parent.as_deref(), args.stageAttemptId, args.stageId.as_deref())?;
    dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_summarize_stages` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder(
    client: &SimpleHttpClient,
    name: &str,
    parent: Option<&str>,
    stageIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:summarizeStages",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = stageIds {
        query_parts.push(format!("stageIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute()` or `dataproc_projects_locations_sessions_spark_applications_summarize_stages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dataproc_projects_locations_sessions_spark_applications_summarize_stages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_summarize_stages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStagesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: stageIds
    pub stageIds: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStages
/// Obtain summary of Stages for a Spark Application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder()` + `dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_summarize_stages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStagesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SummarizeSessionSparkApplicationStagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_summarize_stages_builder(
        client,
        &args.name,
        args.parent.as_deref(),
        args.stageIds.as_deref(),
    )?;
    dataproc_projects_locations_sessions_spark_applications_summarize_stages_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_sessions_spark_applications_write_execute()` to send, or `dataproc_projects_locations_sessions_spark_applications_write` for simplest API.

pub fn dataproc_projects_locations_sessions_spark_applications_write_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WriteSessionSparkApplicationContextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/sessions/{}/sparkApplications/{}:write",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_sessions_spark_applications_write_execute()` or `dataproc_projects_locations_sessions_spark_applications_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<WriteSessionSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteSessionSparkApplicationContextResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_sessions_spark_applications_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_write_task()`.
/// For the simplest API, use `dataproc_projects_locations_sessions_spark_applications_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_sessions_spark_applications_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_sessions_spark_applications_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSessionSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_sessions_spark_applications_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_sessions_spark_applications_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsWriteArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: WriteSessionSparkApplicationContextRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:write
/// Write wrapper objects from dataplane to spanner
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_sessions_spark_applications_write_builder()` + `dataproc_projects_locations_sessions_spark_applications_write_execute()`.
/// For task-level control, use `dataproc_projects_locations_sessions_spark_applications_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_sessions_spark_applications_write(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsWriteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WriteSessionSparkApplicationContextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_sessions_spark_applications_write_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_locations_sessions_spark_applications_write_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Creates new workflow template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_create_execute()` to send, or `dataproc_projects_locations_workflow_templates_create` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Creates new workflow template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_create_execute()` or `dataproc_projects_locations_workflow_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Creates new workflow template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_create_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Creates new workflow template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_create_builder()` + `dataproc_projects_locations_workflow_templates_create_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    dataproc_projects_locations_workflow_templates_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_delete_execute()` to send, or `dataproc_projects_locations_workflow_templates_delete` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = version {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_delete_execute()` or `dataproc_projects_locations_workflow_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_delete_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: version
    pub version: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_delete_builder()` + `dataproc_projects_locations_workflow_templates_delete_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_delete_builder(
        client,
        &args.name,
        args.version,
    )?;
    dataproc_projects_locations_workflow_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_get_execute()` to send, or `dataproc_projects_locations_workflow_templates_get` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = version {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_get_execute()` or `dataproc_projects_locations_workflow_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_get_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: version
    pub version: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_get_builder()` + `dataproc_projects_locations_workflow_templates_get_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_get_builder(
        client,
        &args.name,
        args.version,
    )?;
    dataproc_projects_locations_workflow_templates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_get_iam_policy_execute()` to send, or `dataproc_projects_locations_workflow_templates_get_iam_policy` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_get_iam_policy_execute()` or `dataproc_projects_locations_workflow_templates_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_get_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_get_iam_policy_builder()` + `dataproc_projects_locations_workflow_templates_get_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_get_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_locations_workflow_templates_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_instantiate_execute()` to send, or `dataproc_projects_locations_workflow_templates_instantiate` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_instantiate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &InstantiateWorkflowTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates/{}:instantiate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_instantiate_execute()` or `dataproc_projects_locations_workflow_templates_instantiate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_instantiate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_instantiate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_instantiate_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_instantiate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_instantiate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_instantiate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_instantiate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_instantiate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesInstantiateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: InstantiateWorkflowTemplateRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_instantiate_builder()` + `dataproc_projects_locations_workflow_templates_instantiate_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_instantiate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesInstantiateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_instantiate_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_locations_workflow_templates_instantiate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_instantiate_inline_execute()` to send, or `dataproc_projects_locations_workflow_templates_instantiate_inline` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_instantiate_inline_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates:instantiateInline",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_instantiate_inline_execute()` or `dataproc_projects_locations_workflow_templates_instantiate_inline`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_instantiate_inline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate_inline_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_instantiate_inline_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_instantiate_inline_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_instantiate_inline()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_instantiate_inline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_instantiate_inline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_instantiate_inline_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_instantiate_inline`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesInstantiateInlineArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_instantiate_inline_builder()` + `dataproc_projects_locations_workflow_templates_instantiate_inline_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_instantiate_inline_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_instantiate_inline(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesInstantiateInlineArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_instantiate_inline_builder(
        client,
        &args.parent,
        args.requestId.as_deref(),
        &args.body,
    )?;
    dataproc_projects_locations_workflow_templates_instantiate_inline_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_list_execute()` to send, or `dataproc_projects_locations_workflow_templates_list` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_list_execute()` or `dataproc_projects_locations_workflow_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkflowTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_list_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_list_builder()` + `dataproc_projects_locations_workflow_templates_list_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_locations_workflow_templates_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_set_iam_policy_execute()` to send, or `dataproc_projects_locations_workflow_templates_set_iam_policy` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_set_iam_policy_execute()` or `dataproc_projects_locations_workflow_templates_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_set_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_set_iam_policy_builder()` + `dataproc_projects_locations_workflow_templates_set_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_locations_workflow_templates_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_test_iam_permissions_execute()` to send, or `dataproc_projects_locations_workflow_templates_test_iam_permissions` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_test_iam_permissions_execute()` or `dataproc_projects_locations_workflow_templates_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_test_iam_permissions_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_test_iam_permissions_builder()` + `dataproc_projects_locations_workflow_templates_test_iam_permissions_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_locations_workflow_templates_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_locations_workflow_templates_update_execute()` to send, or `dataproc_projects_locations_workflow_templates_update` for simplest API.

pub fn dataproc_projects_locations_workflow_templates_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/locations/{}/workflowTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_locations_workflow_templates_update_execute()` or `dataproc_projects_locations_workflow_templates_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_locations_workflow_templates_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_update_task()`.
/// For the simplest API, use `dataproc_projects_locations_workflow_templates_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_locations_workflow_templates_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_locations_workflow_templates_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_locations_workflow_templates_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_locations_workflow_templates_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_locations_workflow_templates_update_builder()` + `dataproc_projects_locations_workflow_templates_update_execute()`.
/// For task-level control, use `dataproc_projects_locations_workflow_templates_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_locations_workflow_templates_update(
    client: &SimpleHttpClient,
    args: &DataprocProjectsLocationsWorkflowTemplatesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_locations_workflow_templates_update_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_locations_workflow_templates_update_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_create_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_create` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_create_execute()` or `dataproc_projects_regions_autoscaling_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_create_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Creates new autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_create_builder()` + `dataproc_projects_regions_autoscaling_policies_create_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    dataproc_projects_regions_autoscaling_policies_create_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_delete_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_delete` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_delete_execute()` or `dataproc_projects_regions_autoscaling_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_delete_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Deletes an autoscaling policy. It is an error to delete an autoscaling policy that is in use by one or more clusters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_delete_builder()` + `dataproc_projects_regions_autoscaling_policies_delete_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_autoscaling_policies_delete_builder(client, &args.name)?;
    dataproc_projects_regions_autoscaling_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_get_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_get` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_get_execute()` or `dataproc_projects_regions_autoscaling_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_get_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Retrieves autoscaling policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_get_builder()` + `dataproc_projects_regions_autoscaling_policies_get_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_get_builder(client, &args.name)?;
    dataproc_projects_regions_autoscaling_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute()` or `dataproc_projects_regions_autoscaling_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder()` + `dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_get_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_autoscaling_policies_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_list_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_list` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_list_execute()` or `dataproc_projects_regions_autoscaling_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAutoscalingPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_list_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies
/// Lists autoscaling policies in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_list_builder()` + `dataproc_projects_regions_autoscaling_policies_list_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutoscalingPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_regions_autoscaling_policies_list_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute()` or `dataproc_projects_regions_autoscaling_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder()` + `dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_autoscaling_policies_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute()` or `dataproc_projects_regions_autoscaling_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder()` + `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_autoscaling_policies_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_autoscaling_policies_update_execute()` to send, or `dataproc_projects_regions_autoscaling_policies_update` for simplest API.

pub fn dataproc_projects_regions_autoscaling_policies_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AutoscalingPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/autoscalingPolicies/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_autoscaling_policies_update_execute()` or `dataproc_projects_regions_autoscaling_policies_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_autoscaling_policies_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_update_task()`.
/// For the simplest API, use `dataproc_projects_regions_autoscaling_policies_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_autoscaling_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_autoscaling_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_autoscaling_policies_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_autoscaling_policies_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/autoscalingPolicies/{autoscalingPoliciesId}
/// Updates (replaces) autoscaling policy.Disabled check for update_mask, because all updates will be full replacements.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_autoscaling_policies_update_builder()` + `dataproc_projects_regions_autoscaling_policies_update_execute()`.
/// For task-level control, use `dataproc_projects_regions_autoscaling_policies_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_autoscaling_policies_update(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsAutoscalingPoliciesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutoscalingPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_autoscaling_policies_update_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_regions_autoscaling_policies_update_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Creates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_create_execute()` to send, or `dataproc_projects_regions_clusters_create` for simplest API.

pub fn dataproc_projects_regions_clusters_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    actionOnFailedPrimaryWorkers: Option<&str>,
    requestId: Option<&str>,
    body: &Cluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters",
        projectId, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = actionOnFailedPrimaryWorkers {
        query_parts.push(format!("actionOnFailedPrimaryWorkers={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Creates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_create_execute()` or `dataproc_projects_regions_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Creates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_create_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: actionOnFailedPrimaryWorkers
    pub actionOnFailedPrimaryWorkers: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: Cluster,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Creates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_create_builder()` + `dataproc_projects_regions_clusters_create_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_create_builder(
        client,
        &args.projectId,
        &args.region,
        args.actionOnFailedPrimaryWorkers.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    dataproc_projects_regions_clusters_create_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Deletes a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_delete_execute()` to send, or `dataproc_projects_regions_clusters_delete` for simplest API.

pub fn dataproc_projects_regions_clusters_delete_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: &str,
    clusterUuid: Option<&str>,
    gracefulTerminationTimeout: Option<&str>,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        projectId, region, clusterName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clusterUuid {
        query_parts.push(format!("clusterUuid={}", val));
    }
    if let Some(val) = gracefulTerminationTimeout {
        query_parts.push(format!("gracefulTerminationTimeout={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Deletes a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_delete_execute()` or `dataproc_projects_regions_clusters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Deletes a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_delete_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: clusterName
    pub clusterName: String,
    /// Query parameter: clusterUuid
    pub clusterUuid: Option<String>,
    /// Query parameter: gracefulTerminationTimeout
    pub gracefulTerminationTimeout: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Deletes a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_delete_builder()` + `dataproc_projects_regions_clusters_delete_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_delete_builder(
        client,
        &args.projectId,
        &args.region,
        &args.clusterName,
        args.clusterUuid.as_deref(),
        args.gracefulTerminationTimeout.as_deref(),
        args.requestId.as_deref(),
    )?;
    dataproc_projects_regions_clusters_delete_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
/// Gets cluster diagnostic information. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). After the operation completes, Operation.response contains DiagnoseClusterResults (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#diagnoseclusterresults>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_diagnose_execute()` to send, or `dataproc_projects_regions_clusters_diagnose` for simplest API.

pub fn dataproc_projects_regions_clusters_diagnose_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: &str,
    body: &DiagnoseClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:diagnose",
        projectId, region, clusterName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
/// Gets cluster diagnostic information. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). After the operation completes, Operation.response contains DiagnoseClusterResults (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#diagnoseclusterresults>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_diagnose_execute()` or `dataproc_projects_regions_clusters_diagnose`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_diagnose_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_diagnose_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
/// Gets cluster diagnostic information. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). After the operation completes, Operation.response contains DiagnoseClusterResults (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#diagnoseclusterresults>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_diagnose_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_diagnose_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_diagnose()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_diagnose_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_diagnose_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_diagnose_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_diagnose`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersDiagnoseArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: clusterName
    pub clusterName: String,
    /// Request body.
    pub body: DiagnoseClusterRequest,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
/// Gets cluster diagnostic information. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). After the operation completes, Operation.response contains DiagnoseClusterResults (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#diagnoseclusterresults>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_diagnose_builder()` + `dataproc_projects_regions_clusters_diagnose_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_diagnose_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_diagnose(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersDiagnoseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_diagnose_builder(
        client,
        &args.projectId,
        &args.region,
        &args.clusterName,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_diagnose_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Gets the resource representation for a cluster in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_get_execute()` to send, or `dataproc_projects_regions_clusters_get` for simplest API.

pub fn dataproc_projects_regions_clusters_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        projectId, region, clusterName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Gets the resource representation for a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_get_execute()` or `dataproc_projects_regions_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Gets the resource representation for a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_get_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: clusterName
    pub clusterName: String,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Gets the resource representation for a cluster in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_get_builder()` + `dataproc_projects_regions_clusters_get_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_get_builder(
        client,
        &args.projectId,
        &args.region,
        &args.clusterName,
    )?;
    dataproc_projects_regions_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_get_iam_policy_execute()` to send, or `dataproc_projects_regions_clusters_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_clusters_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_get_iam_policy_execute()` or `dataproc_projects_regions_clusters_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_get_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_get_iam_policy_builder()` + `dataproc_projects_regions_clusters_get_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_get_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:injectCredentials
/// Inject encrypted credentials into all of the VMs in a cluster.The target cluster must be a personal auth cluster assigned to the user who is issuing the RPC.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_inject_credentials_execute()` to send, or `dataproc_projects_regions_clusters_inject_credentials` for simplest API.

pub fn dataproc_projects_regions_clusters_inject_credentials_builder(
    client: &SimpleHttpClient,
    project: &str,
    region: &str,
    cluster: &str,
    body: &InjectCredentialsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:injectCredentials",
        project, region, cluster,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:injectCredentials
/// Inject encrypted credentials into all of the VMs in a cluster.The target cluster must be a personal auth cluster assigned to the user who is issuing the RPC.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_inject_credentials_execute()` or `dataproc_projects_regions_clusters_inject_credentials`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_inject_credentials_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_inject_credentials_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:injectCredentials
/// Inject encrypted credentials into all of the VMs in a cluster.The target cluster must be a personal auth cluster assigned to the user who is issuing the RPC.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_inject_credentials_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_inject_credentials_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_inject_credentials()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_inject_credentials_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_inject_credentials_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_inject_credentials_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_inject_credentials`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersInjectCredentialsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: cluster
    pub cluster: String,
    /// Request body.
    pub body: InjectCredentialsRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:injectCredentials
/// Inject encrypted credentials into all of the VMs in a cluster.The target cluster must be a personal auth cluster assigned to the user who is issuing the RPC.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_inject_credentials_builder()` + `dataproc_projects_regions_clusters_inject_credentials_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_inject_credentials_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_inject_credentials(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersInjectCredentialsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_inject_credentials_builder(
        client,
        &args.project,
        &args.region,
        &args.cluster,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_inject_credentials_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Lists all `regions/{region}/clusters` in a project alphabetically.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_list_execute()` to send, or `dataproc_projects_regions_clusters_list` for simplest API.

pub fn dataproc_projects_regions_clusters_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters",
        projectId, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Lists all `regions/{region}/clusters` in a project alphabetically.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_list_execute()` or `dataproc_projects_regions_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Lists all `regions/{region}/clusters` in a project alphabetically.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_list_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters
/// Lists all `regions/{region}/clusters` in a project alphabetically.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_list_builder()` + `dataproc_projects_regions_clusters_list_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_list_builder(
        client,
        &args.projectId,
        &args.region,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_regions_clusters_list_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Updates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). The cluster must be in a RUNNING state or an error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_patch_execute()` to send, or `dataproc_projects_regions_clusters_patch` for simplest API.

pub fn dataproc_projects_regions_clusters_patch_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: &str,
    gracefulDecommissionTimeout: Option<&str>,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Cluster,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        projectId, region, clusterName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gracefulDecommissionTimeout {
        query_parts.push(format!("gracefulDecommissionTimeout={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Updates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). The cluster must be in a RUNNING state or an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_patch_execute()` or `dataproc_projects_regions_clusters_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Updates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). The cluster must be in a RUNNING state or an error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_patch_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersPatchArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: clusterName
    pub clusterName: String,
    /// Query parameter: gracefulDecommissionTimeout
    pub gracefulDecommissionTimeout: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Cluster,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
/// Updates a cluster in a project. The returned Operation.metadata will be ClusterOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#clusteroperationmetadata>). The cluster must be in a RUNNING state or an error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_patch_builder()` + `dataproc_projects_regions_clusters_patch_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_patch(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_patch_builder(
        client,
        &args.projectId,
        &args.region,
        &args.clusterName,
        args.gracefulDecommissionTimeout.as_deref(),
        args.requestId.as_deref(),
        args.updateMask.as_deref(),
        &args.body,
    )?;
    dataproc_projects_regions_clusters_patch_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
/// Repairs a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_repair_execute()` to send, or `dataproc_projects_regions_clusters_repair` for simplest API.

pub fn dataproc_projects_regions_clusters_repair_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: &str,
    body: &RepairClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:repair",
        projectId, region, clusterName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
/// Repairs a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_repair_execute()` or `dataproc_projects_regions_clusters_repair`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_repair_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_repair_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
/// Repairs a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_repair_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_repair_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_repair()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_repair_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_repair_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_repair_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_repair`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersRepairArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: clusterName
    pub clusterName: String,
    /// Request body.
    pub body: RepairClusterRequest,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
/// Repairs a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_repair_builder()` + `dataproc_projects_regions_clusters_repair_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_repair_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_repair(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersRepairArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_repair_builder(
        client,
        &args.projectId,
        &args.region,
        &args.clusterName,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_repair_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_set_iam_policy_execute()` to send, or `dataproc_projects_regions_clusters_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_clusters_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_set_iam_policy_execute()` or `dataproc_projects_regions_clusters_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_set_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_set_iam_policy_builder()` + `dataproc_projects_regions_clusters_set_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
/// Starts a cluster in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_start_execute()` to send, or `dataproc_projects_regions_clusters_start` for simplest API.

pub fn dataproc_projects_regions_clusters_start_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: &str,
    body: &StartClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:start",
        projectId, region, clusterName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
/// Starts a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_start_execute()` or `dataproc_projects_regions_clusters_start`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_start_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
/// Starts a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_start_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_start_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_start()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_start_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_start`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersStartArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: clusterName
    pub clusterName: String,
    /// Request body.
    pub body: StartClusterRequest,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
/// Starts a cluster in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_start_builder()` + `dataproc_projects_regions_clusters_start_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_start_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_start(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersStartArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_start_builder(
        client,
        &args.projectId,
        &args.region,
        &args.clusterName,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_start_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
/// Stops a cluster in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_stop_execute()` to send, or `dataproc_projects_regions_clusters_stop` for simplest API.

pub fn dataproc_projects_regions_clusters_stop_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: &str,
    body: &StopClusterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:stop",
        projectId, region, clusterName,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
/// Stops a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_stop_execute()` or `dataproc_projects_regions_clusters_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
/// Stops a cluster in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_stop_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersStopArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: clusterName
    pub clusterName: String,
    /// Request body.
    pub body: StopClusterRequest,
}

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
/// Stops a cluster in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_stop_builder()` + `dataproc_projects_regions_clusters_stop_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_stop(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersStopArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_stop_builder(
        client,
        &args.projectId,
        &args.region,
        &args.clusterName,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_stop_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_clusters_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_clusters_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_test_iam_permissions_execute()` or `dataproc_projects_regions_clusters_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_test_iam_permissions_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_test_iam_permissions_builder()` + `dataproc_projects_regions_clusters_test_iam_permissions_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_clusters_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups
/// Creates a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_create_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_create` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    nodeGroupId: Option<&str>,
    parentOperationId: Option<&str>,
    requestId: Option<&str>,
    body: &NodeGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}/nodeGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = nodeGroupId {
        query_parts.push(format!("nodeGroupId={}", val));
    }
    if let Some(val) = parentOperationId {
        query_parts.push(format!("parentOperationId={}", val));
    }
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups
/// Creates a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_node_groups_create_execute()` or `dataproc_projects_regions_clusters_node_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups
/// Creates a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_node_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_create_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_node_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_node_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_node_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_node_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: nodeGroupId
    pub nodeGroupId: Option<String>,
    /// Query parameter: parentOperationId
    pub parentOperationId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: NodeGroup,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups
/// Creates a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_create_builder()` + `dataproc_projects_regions_clusters_node_groups_create_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersNodeGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_node_groups_create_builder(
        client,
        &args.parent,
        args.nodeGroupId.as_deref(),
        args.parentOperationId.as_deref(),
        args.requestId.as_deref(),
        &args.body,
    )?;
    dataproc_projects_regions_clusters_node_groups_create_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}
/// Gets the resource representation for a node group in a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_get_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_get` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}/nodeGroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}
/// Gets the resource representation for a node group in a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_node_groups_get_execute()` or `dataproc_projects_regions_clusters_node_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NodeGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}
/// Gets the resource representation for a node group in a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_node_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_get_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_node_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_node_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_node_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_node_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}
/// Gets the resource representation for a node group in a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_get_builder()` + `dataproc_projects_regions_clusters_node_groups_get_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersNodeGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NodeGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_node_groups_get_builder(client, &args.name)?;
    dataproc_projects_regions_clusters_node_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:repair
/// Repair nodes in a node group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_repair_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_repair` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_repair_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RepairNodeGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}/nodeGroups/{}:repair",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:repair
/// Repair nodes in a node group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_node_groups_repair_execute()` or `dataproc_projects_regions_clusters_node_groups_repair`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_repair_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_repair_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:repair
/// Repair nodes in a node group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_node_groups_repair_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_repair_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_node_groups_repair()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_repair_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_node_groups_repair_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_node_groups_repair_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_node_groups_repair`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsRepairArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RepairNodeGroupRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:repair
/// Repair nodes in a node group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_repair_builder()` + `dataproc_projects_regions_clusters_node_groups_repair_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_repair_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_repair(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersNodeGroupsRepairArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_node_groups_repair_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_regions_clusters_node_groups_repair_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:resize
/// Resizes a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_clusters_node_groups_resize_execute()` to send, or `dataproc_projects_regions_clusters_node_groups_resize` for simplest API.

pub fn dataproc_projects_regions_clusters_node_groups_resize_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResizeNodeGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}/nodeGroups/{}:resize",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:resize
/// Resizes a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_clusters_node_groups_resize_execute()` or `dataproc_projects_regions_clusters_node_groups_resize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_resize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:resize
/// Resizes a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_clusters_node_groups_resize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_resize_task()`.
/// For the simplest API, use `dataproc_projects_regions_clusters_node_groups_resize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_clusters_node_groups_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_clusters_node_groups_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_clusters_node_groups_resize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_clusters_node_groups_resize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsResizeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ResizeNodeGroupRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}/nodeGroups/{nodeGroupsId}:resize
/// Resizes a node group in a cluster. The returned Operation.metadata is NodeGroupOperationMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#nodegroupoperationmetadata>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_clusters_node_groups_resize_builder()` + `dataproc_projects_regions_clusters_node_groups_resize_execute()`.
/// For task-level control, use `dataproc_projects_regions_clusters_node_groups_resize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_clusters_node_groups_resize(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsClustersNodeGroupsResizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_clusters_node_groups_resize_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_regions_clusters_node_groups_resize_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
/// Starts a job cancellation request. To access the job resource after cancellation, call `regions/{region}/jobs`.list (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/list`>) or `regions/{region}/jobs`.get (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/get`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_cancel_execute()` to send, or `dataproc_projects_regions_jobs_cancel` for simplest API.

pub fn dataproc_projects_regions_jobs_cancel_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    jobId: &str,
    body: &CancelJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}:cancel",
        projectId, region, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
/// Starts a job cancellation request. To access the job resource after cancellation, call `regions/{region}/jobs`.list (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/list`>) or `regions/{region}/jobs`.get (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/get`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_cancel_execute()` or `dataproc_projects_regions_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
/// Starts a job cancellation request. To access the job resource after cancellation, call `regions/{region}/jobs`.list (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/list`>) or `regions/{region}/jobs`.get (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/get`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_cancel_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsCancelArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: CancelJobRequest,
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
/// Starts a job cancellation request. To access the job resource after cancellation, call `regions/{region}/jobs`.list (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/list`>) or `regions/{region}/jobs`.get (<https://cloud.google.`com/dataproc/docs/reference/rest/v1/projects`.regions.`jobs/get`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_cancel_builder()` + `dataproc_projects_regions_jobs_cancel_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_cancel(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_cancel_builder(
        client,
        &args.projectId,
        &args.region,
        &args.jobId,
        &args.body,
    )?;
    dataproc_projects_regions_jobs_cancel_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Deletes the job from the project. If the job is active, the delete fails, and the response returns FAILED_PRECONDITION.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_delete_execute()` to send, or `dataproc_projects_regions_jobs_delete` for simplest API.

pub fn dataproc_projects_regions_jobs_delete_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    jobId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}",
        projectId, region, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Deletes the job from the project. If the job is active, the delete fails, and the response returns FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_delete_execute()` or `dataproc_projects_regions_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Deletes the job from the project. If the job is active, the delete fails, and the response returns FAILED_PRECONDITION.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_delete_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: jobId
    pub jobId: String,
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Deletes the job from the project. If the job is active, the delete fails, and the response returns FAILED_PRECONDITION.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_delete_builder()` + `dataproc_projects_regions_jobs_delete_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_delete_builder(
        client,
        &args.projectId,
        &args.region,
        &args.jobId,
    )?;
    dataproc_projects_regions_jobs_delete_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Gets the resource representation for a job in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_get_execute()` to send, or `dataproc_projects_regions_jobs_get` for simplest API.

pub fn dataproc_projects_regions_jobs_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    jobId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}",
        projectId, region, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Gets the resource representation for a job in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_get_execute()` or `dataproc_projects_regions_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Gets the resource representation for a job in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_get_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: jobId
    pub jobId: String,
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Gets the resource representation for a job in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_get_builder()` + `dataproc_projects_regions_jobs_get_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_get_builder(
        client,
        &args.projectId,
        &args.region,
        &args.jobId,
    )?;
    dataproc_projects_regions_jobs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_get_iam_policy_execute()` to send, or `dataproc_projects_regions_jobs_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_jobs_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_get_iam_policy_execute()` or `dataproc_projects_regions_jobs_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_get_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_get_iam_policy_builder()` + `dataproc_projects_regions_jobs_get_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_jobs_get_iam_policy_builder(client, &args.resource, &args.body)?;
    dataproc_projects_regions_jobs_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs
/// Lists `regions/{region}/jobs` in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_list_execute()` to send, or `dataproc_projects_regions_jobs_list` for simplest API.

pub fn dataproc_projects_regions_jobs_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    clusterName: Option<&str>,
    filter: Option<&str>,
    jobStateMatcher: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs",
        projectId, region,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clusterName {
        query_parts.push(format!("clusterName={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = jobStateMatcher {
        query_parts.push(format!("jobStateMatcher={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs
/// Lists `regions/{region}/jobs` in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_list_execute()` or `dataproc_projects_regions_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs
/// Lists `regions/{region}/jobs` in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_list_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Query parameter: clusterName
    pub clusterName: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: jobStateMatcher
    pub jobStateMatcher: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectId}/regions/{region}/jobs
/// Lists `regions/{region}/jobs` in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_list_builder()` + `dataproc_projects_regions_jobs_list_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_list_builder(
        client,
        &args.projectId,
        &args.region,
        args.clusterName.as_deref(),
        args.filter.as_deref(),
        args.jobStateMatcher.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_regions_jobs_list_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Updates a job in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_patch_execute()` to send, or `dataproc_projects_regions_jobs_patch` for simplest API.

pub fn dataproc_projects_regions_jobs_patch_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    jobId: &str,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}",
        projectId, region, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Updates a job in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_patch_execute()` or `dataproc_projects_regions_jobs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Updates a job in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_patch_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsPatchArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Job,
}

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
/// Updates a job in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_patch_builder()` + `dataproc_projects_regions_jobs_patch_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_patch(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_patch_builder(
        client,
        &args.projectId,
        &args.region,
        &args.jobId,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    dataproc_projects_regions_jobs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_set_iam_policy_execute()` to send, or `dataproc_projects_regions_jobs_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_jobs_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_set_iam_policy_execute()` or `dataproc_projects_regions_jobs_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_set_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_set_iam_policy_builder()` + `dataproc_projects_regions_jobs_set_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_jobs_set_iam_policy_builder(client, &args.resource, &args.body)?;
    dataproc_projects_regions_jobs_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submit
/// Submits a job to a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_submit_execute()` to send, or `dataproc_projects_regions_jobs_submit` for simplest API.

pub fn dataproc_projects_regions_jobs_submit_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    body: &SubmitJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs:submit",
        projectId, region,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submit
/// Submits a job to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_submit_execute()` or `dataproc_projects_regions_jobs_submit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_submit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submit
/// Submits a job to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_submit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_submit_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_submit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_submit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_submit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_submit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsSubmitArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Request body.
    pub body: SubmitJobRequest,
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submit
/// Submits a job to a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_submit_builder()` + `dataproc_projects_regions_jobs_submit_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_submit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsSubmitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_submit_builder(
        client,
        &args.projectId,
        &args.region,
        &args.body,
    )?;
    dataproc_projects_regions_jobs_submit_execute(builder)
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
/// Submits job to a cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_submit_as_operation_execute()` to send, or `dataproc_projects_regions_jobs_submit_as_operation` for simplest API.

pub fn dataproc_projects_regions_jobs_submit_as_operation_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    region: &str,
    body: &SubmitJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs:submitAsOperation",
        projectId, region,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
/// Submits job to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_submit_as_operation_execute()` or `dataproc_projects_regions_jobs_submit_as_operation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_submit_as_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit_as_operation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
/// Submits job to a cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_submit_as_operation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_submit_as_operation_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_submit_as_operation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_submit_as_operation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_submit_as_operation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_submit_as_operation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_submit_as_operation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsSubmitAsOperationArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: region
    pub region: String,
    /// Request body.
    pub body: SubmitJobRequest,
}

/// GET v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
/// Submits job to a cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_submit_as_operation_builder()` + `dataproc_projects_regions_jobs_submit_as_operation_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_submit_as_operation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_submit_as_operation(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsSubmitAsOperationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_submit_as_operation_builder(
        client,
        &args.projectId,
        &args.region,
        &args.body,
    )?;
    dataproc_projects_regions_jobs_submit_as_operation_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_jobs_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_jobs_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_jobs_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_jobs_test_iam_permissions_execute()` or `dataproc_projects_regions_jobs_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_jobs_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_jobs_test_iam_permissions_task()`.
/// For the simplest API, use `dataproc_projects_regions_jobs_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_jobs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_jobs_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_jobs_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_jobs_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/jobs/{jobsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_jobs_test_iam_permissions_builder()` + `dataproc_projects_regions_jobs_test_iam_permissions_execute()`.
/// For task-level control, use `dataproc_projects_regions_jobs_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_jobs_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsJobsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_jobs_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_jobs_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_cancel_execute()` to send, or `dataproc_projects_regions_operations_cancel` for simplest API.

pub fn dataproc_projects_regions_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_operations_cancel_execute()` or `dataproc_projects_regions_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_operations_cancel_task()`.
/// For the simplest API, use `dataproc_projects_regions_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_cancel_builder()` + `dataproc_projects_regions_operations_cancel_execute()`.
/// For task-level control, use `dataproc_projects_regions_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_cancel(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_cancel_builder(client, &args.name)?;
    dataproc_projects_regions_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_delete_execute()` to send, or `dataproc_projects_regions_operations_delete` for simplest API.

pub fn dataproc_projects_regions_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_operations_delete_execute()` or `dataproc_projects_regions_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_operations_delete_task()`.
/// For the simplest API, use `dataproc_projects_regions_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_delete_builder()` + `dataproc_projects_regions_operations_delete_execute()`.
/// For task-level control, use `dataproc_projects_regions_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_delete_builder(client, &args.name)?;
    dataproc_projects_regions_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_get_execute()` to send, or `dataproc_projects_regions_operations_get` for simplest API.

pub fn dataproc_projects_regions_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_operations_get_execute()` or `dataproc_projects_regions_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_operations_get_task()`.
/// For the simplest API, use `dataproc_projects_regions_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_get_builder()` + `dataproc_projects_regions_operations_get_execute()`.
/// For task-level control, use `dataproc_projects_regions_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_get_builder(client, &args.name)?;
    dataproc_projects_regions_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_get_iam_policy_execute()` to send, or `dataproc_projects_regions_operations_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_operations_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/operations/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_operations_get_iam_policy_execute()` or `dataproc_projects_regions_operations_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_operations_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_operations_get_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_operations_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_operations_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_operations_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_operations_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_get_iam_policy_builder()` + `dataproc_projects_regions_operations_get_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_operations_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsOperationsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_get_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_operations_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_list_execute()` to send, or `dataproc_projects_regions_operations_list` for simplest API.

pub fn dataproc_projects_regions_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_operations_list_execute()` or `dataproc_projects_regions_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_operations_list_task()`.
/// For the simplest API, use `dataproc_projects_regions_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_list_builder()` + `dataproc_projects_regions_operations_list_execute()`.
/// For task-level control, use `dataproc_projects_regions_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    dataproc_projects_regions_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_set_iam_policy_execute()` to send, or `dataproc_projects_regions_operations_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_operations_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/operations/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_operations_set_iam_policy_execute()` or `dataproc_projects_regions_operations_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_operations_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_operations_set_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_operations_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_operations_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_operations_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_operations_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_set_iam_policy_builder()` + `dataproc_projects_regions_operations_set_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_operations_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsOperationsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_operations_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_operations_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_operations_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_operations_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/operations/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_operations_test_iam_permissions_execute()` or `dataproc_projects_regions_operations_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_operations_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_operations_test_iam_permissions_task()`.
/// For the simplest API, use `dataproc_projects_regions_operations_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_operations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_operations_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_operations_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_operations_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/operations/{operationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_operations_test_iam_permissions_builder()` + `dataproc_projects_regions_operations_test_iam_permissions_execute()`.
/// For task-level control, use `dataproc_projects_regions_operations_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_operations_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsOperationsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_operations_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_operations_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Creates new workflow template.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_create_execute()` to send, or `dataproc_projects_regions_workflow_templates_create` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Creates new workflow template.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_create_execute()` or `dataproc_projects_regions_workflow_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Creates new workflow template.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_create_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Creates new workflow template.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_create_builder()` + `dataproc_projects_regions_workflow_templates_create_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_create(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    dataproc_projects_regions_workflow_templates_create_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_delete_execute()` to send, or `dataproc_projects_regions_workflow_templates_delete` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = version {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_delete_execute()` or `dataproc_projects_regions_workflow_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_delete_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: version
    pub version: Option<i32>,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Deletes a workflow template. It does not cancel in-progress workflows.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_delete_builder()` + `dataproc_projects_regions_workflow_templates_delete_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_delete(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_delete_builder(
        client,
        &args.name,
        args.version,
    )?;
    dataproc_projects_regions_workflow_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_get_execute()` to send, or `dataproc_projects_regions_workflow_templates_get` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    version: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = version {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_get_execute()` or `dataproc_projects_regions_workflow_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_get_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: version
    pub version: Option<i32>,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying optional version parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_get_builder()` + `dataproc_projects_regions_workflow_templates_get_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataproc_projects_regions_workflow_templates_get_builder(client, &args.name, args.version)?;
    dataproc_projects_regions_workflow_templates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_get_iam_policy_execute()` to send, or `dataproc_projects_regions_workflow_templates_get_iam_policy` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_get_iam_policy_execute()` or `dataproc_projects_regions_workflow_templates_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_get_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_get_iam_policy_builder()` + `dataproc_projects_regions_workflow_templates_get_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_get_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_workflow_templates_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_instantiate_execute()` to send, or `dataproc_projects_regions_workflow_templates_instantiate` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_instantiate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &InstantiateWorkflowTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates/{}:instantiate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_instantiate_execute()` or `dataproc_projects_regions_workflow_templates_instantiate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_instantiate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_instantiate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_instantiate_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_instantiate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_instantiate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_instantiate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_instantiate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_instantiate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesInstantiateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: InstantiateWorkflowTemplateRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:instantiate
/// Instantiates a template and begins execution.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_instantiate_builder()` + `dataproc_projects_regions_workflow_templates_instantiate_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_instantiate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesInstantiateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_instantiate_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_regions_workflow_templates_instantiate_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_instantiate_inline_execute()` to send, or `dataproc_projects_regions_workflow_templates_instantiate_inline` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_instantiate_inline_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates:instantiateInline",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_instantiate_inline_execute()` or `dataproc_projects_regions_workflow_templates_instantiate_inline`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_instantiate_inline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate_inline_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_instantiate_inline_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_instantiate_inline_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_instantiate_inline()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_instantiate_inline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_instantiate_inline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_instantiate_inline_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_instantiate_inline`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesInstantiateInlineArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates:instantiateInline
/// Instantiates a template and begins execution.This method is equivalent to executing the sequence CreateWorkflowTemplate, InstantiateWorkflowTemplate, DeleteWorkflowTemplate.The returned Operation can be used to track execution of workflow by polling operations.get. The Operation will complete when entire workflow is finished.The running workflow can be aborted via operations.cancel. This will cause any inflight jobs to be cancelled and workflow-owned clusters to be deleted.The Operation.metadata will be WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/reference/rpc/google`.cloud.dataproc.v1#workflowmetadata>). Also see Using WorkflowMetadata (<https://cloud.google.`com/dataproc/docs/concepts/workflows/debugging`#using_workflowmetadata>).On successful completion, Operation.response will be Empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_instantiate_inline_builder()` + `dataproc_projects_regions_workflow_templates_instantiate_inline_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_instantiate_inline_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_instantiate_inline(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesInstantiateInlineArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_instantiate_inline_builder(
        client,
        &args.parent,
        args.requestId.as_deref(),
        &args.body,
    )?;
    dataproc_projects_regions_workflow_templates_instantiate_inline_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_list_execute()` to send, or `dataproc_projects_regions_workflow_templates_list` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_list_execute()` or `dataproc_projects_regions_workflow_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkflowTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_list_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates
/// Lists workflows that match the specified filter in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_list_builder()` + `dataproc_projects_regions_workflow_templates_list_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_list(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListWorkflowTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    dataproc_projects_regions_workflow_templates_list_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_set_iam_policy_execute()` to send, or `dataproc_projects_regions_workflow_templates_set_iam_policy` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_set_iam_policy_execute()` or `dataproc_projects_regions_workflow_templates_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_set_iam_policy_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_set_iam_policy_builder()` + `dataproc_projects_regions_workflow_templates_set_iam_policy_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_workflow_templates_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_test_iam_permissions_execute()` to send, or `dataproc_projects_regions_workflow_templates_test_iam_permissions` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_test_iam_permissions_execute()` or `dataproc_projects_regions_workflow_templates_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_test_iam_permissions_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_test_iam_permissions_builder()` + `dataproc_projects_regions_workflow_templates_test_iam_permissions_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    dataproc_projects_regions_workflow_templates_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataproc_projects_regions_workflow_templates_update_execute()` to send, or `dataproc_projects_regions_workflow_templates_update` for simplest API.

pub fn dataproc_projects_regions_workflow_templates_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WorkflowTemplate,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/workflowTemplates/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataproc_projects_regions_workflow_templates_update_execute()` or `dataproc_projects_regions_workflow_templates_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkflowTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataproc_projects_regions_workflow_templates_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_update_task()`.
/// For the simplest API, use `dataproc_projects_regions_workflow_templates_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataproc_projects_regions_workflow_templates_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataproc_projects_regions_workflow_templates_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataproc_projects_regions_workflow_templates_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataproc_projects_regions_workflow_templates_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// GET v1/projects/{projectsId}/regions/{regionsId}/workflowTemplates/{workflowTemplatesId}
/// Updates (replaces) workflow template. The updated template must contain version that matches the current server version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataproc_projects_regions_workflow_templates_update_builder()` + `dataproc_projects_regions_workflow_templates_update_execute()`.
/// For task-level control, use `dataproc_projects_regions_workflow_templates_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataproc_projects_regions_workflow_templates_update(
    client: &SimpleHttpClient,
    args: &DataprocProjectsRegionsWorkflowTemplatesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WorkflowTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataproc_projects_regions_workflow_templates_update_builder(
        client, &args.name, &args.body,
    )?;
    dataproc_projects_regions_workflow_templates_update_execute(builder)
}
