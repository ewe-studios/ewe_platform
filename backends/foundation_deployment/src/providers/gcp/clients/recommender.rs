//! Auto-generated API clients for gcp/recommender.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_insight_types_get_config_execute()` to send, or `recommender_billing_accounts_locations_insight_types_get_config` for simplest API.

pub fn recommender_billing_accounts_locations_insight_types_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/insightTypes/{insightTypesId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_insight_types_get_config_execute()` or `recommender_billing_accounts_locations_insight_types_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1InsightTypeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_insight_types_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_get_config_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_insight_types_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_insight_types_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_billing_accounts_locations_insight_types_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_insight_types_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsInsightTypesGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_insight_types_get_config_builder()` + `recommender_billing_accounts_locations_insight_types_get_config_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_get_config(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsInsightTypesGetConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_billing_accounts_locations_insight_types_get_config_builder(
        client, &args.name,
    )?;
    recommender_billing_accounts_locations_insight_types_get_config_execute(builder)
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_insight_types_update_config_execute()` to send, or `recommender_billing_accounts_locations_insight_types_update_config` for simplest API.

pub fn recommender_billing_accounts_locations_insight_types_update_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/insightTypes/{insightTypesId}/config",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_insight_types_update_config_execute()` or `recommender_billing_accounts_locations_insight_types_update_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_update_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1InsightTypeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_insight_types_update_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_update_config_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_insight_types_update_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_insight_types_update_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_billing_accounts_locations_insight_types_update_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_insight_types_update_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsInsightTypesUpdateConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_insight_types_update_config_builder()` + `recommender_billing_accounts_locations_insight_types_update_config_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_update_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_update_config(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsInsightTypesUpdateConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_billing_accounts_locations_insight_types_update_config_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    recommender_billing_accounts_locations_insight_types_update_config_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_insight_types_insights_get_execute()` to send, or `recommender_billing_accounts_locations_insight_types_insights_get` for simplest API.

pub fn recommender_billing_accounts_locations_insight_types_insights_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_insight_types_insights_get_execute()` or `recommender_billing_accounts_locations_insight_types_insights_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_insights_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_insight_types_insights_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_insights_get_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_insight_types_insights_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_insight_types_insights_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_billing_accounts_locations_insight_types_insights_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_insight_types_insights_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsInsightTypesInsightsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_insight_types_insights_get_builder()` + `recommender_billing_accounts_locations_insight_types_insights_get_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_insights_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_insights_get(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsInsightTypesInsightsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_billing_accounts_locations_insight_types_insights_get_builder(
        client, &args.name,
    )?;
    recommender_billing_accounts_locations_insight_types_insights_get_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_insight_types_insights_list_execute()` to send, or `recommender_billing_accounts_locations_insight_types_insights_list` for simplest API.

pub fn recommender_billing_accounts_locations_insight_types_insights_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_insight_types_insights_list_execute()` or `recommender_billing_accounts_locations_insight_types_insights_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_insights_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListInsightsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_insight_types_insights_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_insights_list_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_insight_types_insights_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_insight_types_insights_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_billing_accounts_locations_insight_types_insights_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_insight_types_insights_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsInsightTypesInsightsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_insight_types_insights_list_builder()` + `recommender_billing_accounts_locations_insight_types_insights_list_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_insights_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_insights_list(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsInsightTypesInsightsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_billing_accounts_locations_insight_types_insights_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_billing_accounts_locations_insight_types_insights_list_execute(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_execute()` to send, or `recommender_billing_accounts_locations_insight_types_insights_mark_accepted` for simplest API.

pub fn recommender_billing_accounts_locations_insight_types_insights_mark_accepted_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_execute()` or `recommender_billing_accounts_locations_insight_types_insights_mark_accepted`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_insights_mark_accepted_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_insight_types_insights_mark_accepted()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_insight_types_insights_mark_accepted_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_billing_accounts_locations_insight_types_insights_mark_accepted_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_insight_types_insights_mark_accepted`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsInsightTypesInsightsMarkAcceptedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_builder()` + `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_insight_types_insights_mark_accepted_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_insight_types_insights_mark_accepted(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsInsightTypesInsightsMarkAcceptedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_billing_accounts_locations_insight_types_insights_mark_accepted_builder(
            client, &args.name,
        )?;
    recommender_billing_accounts_locations_insight_types_insights_mark_accepted_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_get_config_execute()` to send, or `recommender_billing_accounts_locations_recommenders_get_config` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_get_config_execute()` or `recommender_billing_accounts_locations_recommenders_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1RecommenderConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_get_config_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_billing_accounts_locations_recommenders_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_get_config_builder()` + `recommender_billing_accounts_locations_recommenders_get_config_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_get_config(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersGetConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_billing_accounts_locations_recommenders_get_config_builder(client, &args.name)?;
    recommender_billing_accounts_locations_recommenders_get_config_execute(builder)
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_update_config_execute()` to send, or `recommender_billing_accounts_locations_recommenders_update_config` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_update_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/config",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_update_config_execute()` or `recommender_billing_accounts_locations_recommenders_update_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_update_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1RecommenderConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_update_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_update_config_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_update_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_update_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_billing_accounts_locations_recommenders_update_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_update_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersUpdateConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_update_config_builder()` + `recommender_billing_accounts_locations_recommenders_update_config_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_update_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_update_config(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersUpdateConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_billing_accounts_locations_recommenders_update_config_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    recommender_billing_accounts_locations_recommenders_update_config_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_recommendations_get_execute()` to send, or `recommender_billing_accounts_locations_recommenders_recommendations_get` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_recommendations_get_execute()` or `recommender_billing_accounts_locations_recommenders_recommendations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_recommendations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_get_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_recommendations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_billing_accounts_locations_recommenders_recommendations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_recommendations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersRecommendationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_recommendations_get_builder()` + `recommender_billing_accounts_locations_recommenders_recommendations_get_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_get(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersRecommendationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_billing_accounts_locations_recommenders_recommendations_get_builder(
        client, &args.name,
    )?;
    recommender_billing_accounts_locations_recommenders_recommendations_get_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_recommendations_list_execute()` to send, or `recommender_billing_accounts_locations_recommenders_recommendations_list` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_recommendations_list_execute()` or `recommender_billing_accounts_locations_recommenders_recommendations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListRecommendationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_recommendations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_list_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_recommendations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_billing_accounts_locations_recommenders_recommendations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_recommendations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersRecommendationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_recommendations_list_builder()` + `recommender_billing_accounts_locations_recommenders_recommendations_list_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_list(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersRecommendationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_billing_accounts_locations_recommenders_recommendations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_billing_accounts_locations_recommenders_recommendations_list_execute(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_execute()` to send, or `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_execute()` or `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkClaimedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_builder()` + `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkClaimedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_builder(
            client, &args.name,
        )?;
    recommender_billing_accounts_locations_recommenders_recommendations_mark_claimed_execute(
        builder,
    )
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_execute()` to send, or `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_execute()` or `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkDismissedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_builder()` + `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkDismissedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_builder(
            client, &args.name,
        )?;
    recommender_billing_accounts_locations_recommenders_recommendations_mark_dismissed_execute(
        builder,
    )
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_execute()` to send, or `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_execute()` or `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_recommendations_mark_failed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkFailedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_builder()` + `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_failed(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkFailedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_builder(
            client, &args.name,
        )?;
    recommender_billing_accounts_locations_recommenders_recommendations_mark_failed_execute(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_execute()` to send, or `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded` for simplest API.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/billingAccounts/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_execute()` or `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_task()`.
/// For the simplest API, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkSucceededArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/billingAccounts/{billingAccountsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_builder()` + `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_execute()`.
/// For task-level control, use `recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded(
    client: &SimpleHttpClient,
    args: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkSucceededArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_builder(
            client, &args.name,
        )?;
    recommender_billing_accounts_locations_recommenders_recommendations_mark_succeeded_execute(
        builder,
    )
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_insight_types_insights_get_execute()` to send, or `recommender_folders_locations_insight_types_insights_get` for simplest API.

pub fn recommender_folders_locations_insight_types_insights_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_insight_types_insights_get_execute()` or `recommender_folders_locations_insight_types_insights_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_insight_types_insights_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_insight_types_insights_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_insight_types_insights_get_task()`.
/// For the simplest API, use `recommender_folders_locations_insight_types_insights_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_insight_types_insights_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_folders_locations_insight_types_insights_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_insight_types_insights_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsInsightTypesInsightsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_insight_types_insights_get_builder()` + `recommender_folders_locations_insight_types_insights_get_execute()`.
/// For task-level control, use `recommender_folders_locations_insight_types_insights_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_insight_types_insights_get(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsInsightTypesInsightsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_folders_locations_insight_types_insights_get_builder(client, &args.name)?;
    recommender_folders_locations_insight_types_insights_get_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_insight_types_insights_list_execute()` to send, or `recommender_folders_locations_insight_types_insights_list` for simplest API.

pub fn recommender_folders_locations_insight_types_insights_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_insight_types_insights_list_execute()` or `recommender_folders_locations_insight_types_insights_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_insight_types_insights_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListInsightsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_insight_types_insights_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_insight_types_insights_list_task()`.
/// For the simplest API, use `recommender_folders_locations_insight_types_insights_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_insight_types_insights_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_folders_locations_insight_types_insights_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_insight_types_insights_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsInsightTypesInsightsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_insight_types_insights_list_builder()` + `recommender_folders_locations_insight_types_insights_list_execute()`.
/// For task-level control, use `recommender_folders_locations_insight_types_insights_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_insight_types_insights_list(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsInsightTypesInsightsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_folders_locations_insight_types_insights_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_folders_locations_insight_types_insights_list_execute(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_insight_types_insights_mark_accepted_execute()` to send, or `recommender_folders_locations_insight_types_insights_mark_accepted` for simplest API.

pub fn recommender_folders_locations_insight_types_insights_mark_accepted_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_insight_types_insights_mark_accepted_execute()` or `recommender_folders_locations_insight_types_insights_mark_accepted`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_insight_types_insights_mark_accepted_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_insight_types_insights_mark_accepted_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_insight_types_insights_mark_accepted_task()`.
/// For the simplest API, use `recommender_folders_locations_insight_types_insights_mark_accepted()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_insight_types_insights_mark_accepted_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_folders_locations_insight_types_insights_mark_accepted_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_insight_types_insights_mark_accepted`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsInsightTypesInsightsMarkAcceptedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_insight_types_insights_mark_accepted_builder()` + `recommender_folders_locations_insight_types_insights_mark_accepted_execute()`.
/// For task-level control, use `recommender_folders_locations_insight_types_insights_mark_accepted_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_insight_types_insights_mark_accepted(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsInsightTypesInsightsMarkAcceptedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_folders_locations_insight_types_insights_mark_accepted_builder(
        client, &args.name,
    )?;
    recommender_folders_locations_insight_types_insights_mark_accepted_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_recommenders_recommendations_get_execute()` to send, or `recommender_folders_locations_recommenders_recommendations_get` for simplest API.

pub fn recommender_folders_locations_recommenders_recommendations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_recommenders_recommendations_get_execute()` or `recommender_folders_locations_recommenders_recommendations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_recommenders_recommendations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_get_task()`.
/// For the simplest API, use `recommender_folders_locations_recommenders_recommendations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_recommenders_recommendations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_folders_locations_recommenders_recommendations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_recommenders_recommendations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsRecommendersRecommendationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_recommenders_recommendations_get_builder()` + `recommender_folders_locations_recommenders_recommendations_get_execute()`.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_get(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsRecommendersRecommendationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_folders_locations_recommenders_recommendations_get_builder(client, &args.name)?;
    recommender_folders_locations_recommenders_recommendations_get_execute(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_recommenders_recommendations_list_execute()` to send, or `recommender_folders_locations_recommenders_recommendations_list` for simplest API.

pub fn recommender_folders_locations_recommenders_recommendations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_recommenders_recommendations_list_execute()` or `recommender_folders_locations_recommenders_recommendations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListRecommendationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_recommenders_recommendations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_list_task()`.
/// For the simplest API, use `recommender_folders_locations_recommenders_recommendations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_recommenders_recommendations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_folders_locations_recommenders_recommendations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_recommenders_recommendations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsRecommendersRecommendationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_recommenders_recommendations_list_builder()` + `recommender_folders_locations_recommenders_recommendations_list_execute()`.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_list(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsRecommendersRecommendationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_folders_locations_recommenders_recommendations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_folders_locations_recommenders_recommendations_list_execute(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_recommenders_recommendations_mark_claimed_execute()` to send, or `recommender_folders_locations_recommenders_recommendations_mark_claimed` for simplest API.

pub fn recommender_folders_locations_recommenders_recommendations_mark_claimed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_recommenders_recommendations_mark_claimed_execute()` or `recommender_folders_locations_recommenders_recommendations_mark_claimed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_claimed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_recommenders_recommendations_mark_claimed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_claimed_task()`.
/// For the simplest API, use `recommender_folders_locations_recommenders_recommendations_mark_claimed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_recommenders_recommendations_mark_claimed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_folders_locations_recommenders_recommendations_mark_claimed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_recommenders_recommendations_mark_claimed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsRecommendersRecommendationsMarkClaimedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_recommenders_recommendations_mark_claimed_builder()` + `recommender_folders_locations_recommenders_recommendations_mark_claimed_execute()`.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_claimed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_claimed(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsRecommendersRecommendationsMarkClaimedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_folders_locations_recommenders_recommendations_mark_claimed_builder(
        client, &args.name,
    )?;
    recommender_folders_locations_recommenders_recommendations_mark_claimed_execute(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_recommenders_recommendations_mark_dismissed_execute()` to send, or `recommender_folders_locations_recommenders_recommendations_mark_dismissed` for simplest API.

pub fn recommender_folders_locations_recommenders_recommendations_mark_dismissed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_recommenders_recommendations_mark_dismissed_execute()` or `recommender_folders_locations_recommenders_recommendations_mark_dismissed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_dismissed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_recommenders_recommendations_mark_dismissed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_dismissed_task()`.
/// For the simplest API, use `recommender_folders_locations_recommenders_recommendations_mark_dismissed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_recommenders_recommendations_mark_dismissed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_folders_locations_recommenders_recommendations_mark_dismissed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_recommenders_recommendations_mark_dismissed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsRecommendersRecommendationsMarkDismissedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_recommenders_recommendations_mark_dismissed_builder()` + `recommender_folders_locations_recommenders_recommendations_mark_dismissed_execute()`.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_dismissed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_dismissed(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsRecommendersRecommendationsMarkDismissedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_folders_locations_recommenders_recommendations_mark_dismissed_builder(
            client, &args.name,
        )?;
    recommender_folders_locations_recommenders_recommendations_mark_dismissed_execute(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_recommenders_recommendations_mark_failed_execute()` to send, or `recommender_folders_locations_recommenders_recommendations_mark_failed` for simplest API.

pub fn recommender_folders_locations_recommenders_recommendations_mark_failed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_recommenders_recommendations_mark_failed_execute()` or `recommender_folders_locations_recommenders_recommendations_mark_failed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_failed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_recommenders_recommendations_mark_failed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_failed_task()`.
/// For the simplest API, use `recommender_folders_locations_recommenders_recommendations_mark_failed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_recommenders_recommendations_mark_failed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_folders_locations_recommenders_recommendations_mark_failed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_recommenders_recommendations_mark_failed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsRecommendersRecommendationsMarkFailedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_recommenders_recommendations_mark_failed_builder()` + `recommender_folders_locations_recommenders_recommendations_mark_failed_execute()`.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_failed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_failed(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsRecommendersRecommendationsMarkFailedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_folders_locations_recommenders_recommendations_mark_failed_builder(
        client, &args.name,
    )?;
    recommender_folders_locations_recommenders_recommendations_mark_failed_execute(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_folders_locations_recommenders_recommendations_mark_succeeded_execute()` to send, or `recommender_folders_locations_recommenders_recommendations_mark_succeeded` for simplest API.

pub fn recommender_folders_locations_recommenders_recommendations_mark_succeeded_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/folders/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_folders_locations_recommenders_recommendations_mark_succeeded_execute()` or `recommender_folders_locations_recommenders_recommendations_mark_succeeded`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_succeeded_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_folders_locations_recommenders_recommendations_mark_succeeded_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_succeeded_task()`.
/// For the simplest API, use `recommender_folders_locations_recommenders_recommendations_mark_succeeded()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_folders_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_folders_locations_recommenders_recommendations_mark_succeeded_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_folders_locations_recommenders_recommendations_mark_succeeded_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_folders_locations_recommenders_recommendations_mark_succeeded`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderFoldersLocationsRecommendersRecommendationsMarkSucceededArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/folders/{foldersId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_folders_locations_recommenders_recommendations_mark_succeeded_builder()` + `recommender_folders_locations_recommenders_recommendations_mark_succeeded_execute()`.
/// For task-level control, use `recommender_folders_locations_recommenders_recommendations_mark_succeeded_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_folders_locations_recommenders_recommendations_mark_succeeded(
    client: &SimpleHttpClient,
    args: &RecommenderFoldersLocationsRecommendersRecommendationsMarkSucceededArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_folders_locations_recommenders_recommendations_mark_succeeded_builder(
            client, &args.name,
        )?;
    recommender_folders_locations_recommenders_recommendations_mark_succeeded_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_insight_types_get_config_execute()` to send, or `recommender_organizations_locations_insight_types_get_config` for simplest API.

pub fn recommender_organizations_locations_insight_types_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/insightTypes/{insightTypesId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_insight_types_get_config_execute()` or `recommender_organizations_locations_insight_types_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1InsightTypeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_insight_types_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_insight_types_get_config_task()`.
/// For the simplest API, use `recommender_organizations_locations_insight_types_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_insight_types_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_insight_types_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_insight_types_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsInsightTypesGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_insight_types_get_config_builder()` + `recommender_organizations_locations_insight_types_get_config_execute()`.
/// For task-level control, use `recommender_organizations_locations_insight_types_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_get_config(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsInsightTypesGetConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_organizations_locations_insight_types_get_config_builder(client, &args.name)?;
    recommender_organizations_locations_insight_types_get_config_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_insight_types_update_config_execute()` to send, or `recommender_organizations_locations_insight_types_update_config` for simplest API.

pub fn recommender_organizations_locations_insight_types_update_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/insightTypes/{insightTypesId}/config",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_insight_types_update_config_execute()` or `recommender_organizations_locations_insight_types_update_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_update_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1InsightTypeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_insight_types_update_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_insight_types_update_config_task()`.
/// For the simplest API, use `recommender_organizations_locations_insight_types_update_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_insight_types_update_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_insight_types_update_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_insight_types_update_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsInsightTypesUpdateConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_insight_types_update_config_builder()` + `recommender_organizations_locations_insight_types_update_config_execute()`.
/// For task-level control, use `recommender_organizations_locations_insight_types_update_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_update_config(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsInsightTypesUpdateConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_organizations_locations_insight_types_update_config_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    recommender_organizations_locations_insight_types_update_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_insight_types_insights_get_execute()` to send, or `recommender_organizations_locations_insight_types_insights_get` for simplest API.

pub fn recommender_organizations_locations_insight_types_insights_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_insight_types_insights_get_execute()` or `recommender_organizations_locations_insight_types_insights_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_insights_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_insight_types_insights_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_insight_types_insights_get_task()`.
/// For the simplest API, use `recommender_organizations_locations_insight_types_insights_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_insight_types_insights_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_insight_types_insights_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_insight_types_insights_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsInsightTypesInsightsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_insight_types_insights_get_builder()` + `recommender_organizations_locations_insight_types_insights_get_execute()`.
/// For task-level control, use `recommender_organizations_locations_insight_types_insights_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_insights_get(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsInsightTypesInsightsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_organizations_locations_insight_types_insights_get_builder(client, &args.name)?;
    recommender_organizations_locations_insight_types_insights_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_insight_types_insights_list_execute()` to send, or `recommender_organizations_locations_insight_types_insights_list` for simplest API.

pub fn recommender_organizations_locations_insight_types_insights_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_insight_types_insights_list_execute()` or `recommender_organizations_locations_insight_types_insights_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_insights_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListInsightsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_insight_types_insights_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_insight_types_insights_list_task()`.
/// For the simplest API, use `recommender_organizations_locations_insight_types_insights_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_insight_types_insights_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_insight_types_insights_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_insight_types_insights_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsInsightTypesInsightsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_insight_types_insights_list_builder()` + `recommender_organizations_locations_insight_types_insights_list_execute()`.
/// For task-level control, use `recommender_organizations_locations_insight_types_insights_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_insights_list(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsInsightTypesInsightsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_organizations_locations_insight_types_insights_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_organizations_locations_insight_types_insights_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_insight_types_insights_mark_accepted_execute()` to send, or `recommender_organizations_locations_insight_types_insights_mark_accepted` for simplest API.

pub fn recommender_organizations_locations_insight_types_insights_mark_accepted_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_insight_types_insights_mark_accepted_execute()` or `recommender_organizations_locations_insight_types_insights_mark_accepted`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_insights_mark_accepted_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_insight_types_insights_mark_accepted_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_insight_types_insights_mark_accepted_task()`.
/// For the simplest API, use `recommender_organizations_locations_insight_types_insights_mark_accepted()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_insight_types_insights_mark_accepted_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_organizations_locations_insight_types_insights_mark_accepted_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_insight_types_insights_mark_accepted`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsInsightTypesInsightsMarkAcceptedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_insight_types_insights_mark_accepted_builder()` + `recommender_organizations_locations_insight_types_insights_mark_accepted_execute()`.
/// For task-level control, use `recommender_organizations_locations_insight_types_insights_mark_accepted_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_insight_types_insights_mark_accepted(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsInsightTypesInsightsMarkAcceptedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_organizations_locations_insight_types_insights_mark_accepted_builder(
        client, &args.name,
    )?;
    recommender_organizations_locations_insight_types_insights_mark_accepted_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_get_config_execute()` to send, or `recommender_organizations_locations_recommenders_get_config` for simplest API.

pub fn recommender_organizations_locations_recommenders_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_get_config_execute()` or `recommender_organizations_locations_recommenders_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1RecommenderConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_get_config_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_recommenders_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_get_config_builder()` + `recommender_organizations_locations_recommenders_get_config_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_get_config(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersGetConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_organizations_locations_recommenders_get_config_builder(client, &args.name)?;
    recommender_organizations_locations_recommenders_get_config_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_update_config_execute()` to send, or `recommender_organizations_locations_recommenders_update_config` for simplest API.

pub fn recommender_organizations_locations_recommenders_update_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/config",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_update_config_execute()` or `recommender_organizations_locations_recommenders_update_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_update_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1RecommenderConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_update_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_update_config_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_update_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_update_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_recommenders_update_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_update_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersUpdateConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_update_config_builder()` + `recommender_organizations_locations_recommenders_update_config_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_update_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_update_config(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersUpdateConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_organizations_locations_recommenders_update_config_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    recommender_organizations_locations_recommenders_update_config_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_recommendations_get_execute()` to send, or `recommender_organizations_locations_recommenders_recommendations_get` for simplest API.

pub fn recommender_organizations_locations_recommenders_recommendations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_recommendations_get_execute()` or `recommender_organizations_locations_recommenders_recommendations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_recommendations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_get_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_recommendations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_recommendations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_recommenders_recommendations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_recommendations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersRecommendationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_recommendations_get_builder()` + `recommender_organizations_locations_recommenders_recommendations_get_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_get(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersRecommendationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_organizations_locations_recommenders_recommendations_get_builder(
        client, &args.name,
    )?;
    recommender_organizations_locations_recommenders_recommendations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_recommendations_list_execute()` to send, or `recommender_organizations_locations_recommenders_recommendations_list` for simplest API.

pub fn recommender_organizations_locations_recommenders_recommendations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_recommendations_list_execute()` or `recommender_organizations_locations_recommenders_recommendations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListRecommendationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_recommendations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_list_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_recommendations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_recommendations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_recommenders_recommendations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_recommendations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersRecommendationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_recommendations_list_builder()` + `recommender_organizations_locations_recommenders_recommendations_list_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_list(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersRecommendationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_organizations_locations_recommenders_recommendations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_organizations_locations_recommenders_recommendations_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_recommendations_mark_claimed_execute()` to send, or `recommender_organizations_locations_recommenders_recommendations_mark_claimed` for simplest API.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_claimed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_recommendations_mark_claimed_execute()` or `recommender_organizations_locations_recommenders_recommendations_mark_claimed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_claimed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_recommendations_mark_claimed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_claimed_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_recommendations_mark_claimed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_claimed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_organizations_locations_recommenders_recommendations_mark_claimed_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_recommendations_mark_claimed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersRecommendationsMarkClaimedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_recommendations_mark_claimed_builder()` + `recommender_organizations_locations_recommenders_recommendations_mark_claimed_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_claimed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_claimed(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkClaimedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_organizations_locations_recommenders_recommendations_mark_claimed_builder(
            client, &args.name,
        )?;
    recommender_organizations_locations_recommenders_recommendations_mark_claimed_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_execute()` to send, or `recommender_organizations_locations_recommenders_recommendations_mark_dismissed` for simplest API.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_dismissed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_execute()` or `recommender_organizations_locations_recommenders_recommendations_mark_dismissed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_dismissed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_recommendations_mark_dismissed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_dismissed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_organizations_locations_recommenders_recommendations_mark_dismissed_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_recommendations_mark_dismissed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersRecommendationsMarkDismissedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_builder()` + `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_dismissed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_dismissed(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkDismissedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_organizations_locations_recommenders_recommendations_mark_dismissed_builder(
            client, &args.name,
        )?;
    recommender_organizations_locations_recommenders_recommendations_mark_dismissed_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_recommendations_mark_failed_execute()` to send, or `recommender_organizations_locations_recommenders_recommendations_mark_failed` for simplest API.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_failed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_recommendations_mark_failed_execute()` or `recommender_organizations_locations_recommenders_recommendations_mark_failed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_failed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_recommendations_mark_failed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_failed_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_recommendations_mark_failed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_failed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_organizations_locations_recommenders_recommendations_mark_failed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_recommendations_mark_failed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersRecommendationsMarkFailedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_recommendations_mark_failed_builder()` + `recommender_organizations_locations_recommenders_recommendations_mark_failed_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_failed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_failed(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkFailedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_organizations_locations_recommenders_recommendations_mark_failed_builder(
            client, &args.name,
        )?;
    recommender_organizations_locations_recommenders_recommendations_mark_failed_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_execute()` to send, or `recommender_organizations_locations_recommenders_recommendations_mark_succeeded` for simplest API.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_succeeded_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/organizations/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_execute()` or `recommender_organizations_locations_recommenders_recommendations_mark_succeeded`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_succeeded_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_task()`.
/// For the simplest API, use `recommender_organizations_locations_recommenders_recommendations_mark_succeeded()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_succeeded_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_organizations_locations_recommenders_recommendations_mark_succeeded_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_organizations_locations_recommenders_recommendations_mark_succeeded`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderOrganizationsLocationsRecommendersRecommendationsMarkSucceededArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_builder()` + `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_execute()`.
/// For task-level control, use `recommender_organizations_locations_recommenders_recommendations_mark_succeeded_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_organizations_locations_recommenders_recommendations_mark_succeeded(
    client: &SimpleHttpClient,
    args: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkSucceededArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_organizations_locations_recommenders_recommendations_mark_succeeded_builder(
            client, &args.name,
        )?;
    recommender_organizations_locations_recommenders_recommendations_mark_succeeded_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_insight_types_get_config_execute()` to send, or `recommender_projects_locations_insight_types_get_config` for simplest API.

pub fn recommender_projects_locations_insight_types_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/insightTypes/{insightTypesId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_insight_types_get_config_execute()` or `recommender_projects_locations_insight_types_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1InsightTypeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_insight_types_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_insight_types_get_config_task()`.
/// For the simplest API, use `recommender_projects_locations_insight_types_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_insight_types_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_insight_types_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_insight_types_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsInsightTypesGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_insight_types_get_config_builder()` + `recommender_projects_locations_insight_types_get_config_execute()`.
/// For task-level control, use `recommender_projects_locations_insight_types_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_get_config(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsInsightTypesGetConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_projects_locations_insight_types_get_config_builder(client, &args.name)?;
    recommender_projects_locations_insight_types_get_config_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_insight_types_update_config_execute()` to send, or `recommender_projects_locations_insight_types_update_config` for simplest API.

pub fn recommender_projects_locations_insight_types_update_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/insightTypes/{insightTypesId}/config",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_insight_types_update_config_execute()` or `recommender_projects_locations_insight_types_update_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_update_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1InsightTypeConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_insight_types_update_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_insight_types_update_config_task()`.
/// For the simplest API, use `recommender_projects_locations_insight_types_update_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_insight_types_update_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_insight_types_update_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_insight_types_update_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsInsightTypesUpdateConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/config
/// Updates an InsightTypeConfig change. This will create a new revision of the config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_insight_types_update_config_builder()` + `recommender_projects_locations_insight_types_update_config_execute()`.
/// For task-level control, use `recommender_projects_locations_insight_types_update_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_update_config(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsInsightTypesUpdateConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1InsightTypeConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_insight_types_update_config_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    recommender_projects_locations_insight_types_update_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_insight_types_insights_get_execute()` to send, or `recommender_projects_locations_insight_types_insights_get` for simplest API.

pub fn recommender_projects_locations_insight_types_insights_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_insight_types_insights_get_execute()` or `recommender_projects_locations_insight_types_insights_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_insights_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_insight_types_insights_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_insight_types_insights_get_task()`.
/// For the simplest API, use `recommender_projects_locations_insight_types_insights_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_insights_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_insight_types_insights_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_insight_types_insights_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_insight_types_insights_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsInsightTypesInsightsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}
/// Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_insight_types_insights_get_builder()` + `recommender_projects_locations_insight_types_insights_get_execute()`.
/// For task-level control, use `recommender_projects_locations_insight_types_insights_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_insights_get(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsInsightTypesInsightsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_projects_locations_insight_types_insights_get_builder(client, &args.name)?;
    recommender_projects_locations_insight_types_insights_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_insight_types_insights_list_execute()` to send, or `recommender_projects_locations_insight_types_insights_list` for simplest API.

pub fn recommender_projects_locations_insight_types_insights_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_insight_types_insights_list_execute()` or `recommender_projects_locations_insight_types_insights_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_insights_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListInsightsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_insight_types_insights_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_insight_types_insights_list_task()`.
/// For the simplest API, use `recommender_projects_locations_insight_types_insights_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_insights_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_insight_types_insights_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_insight_types_insights_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_insight_types_insights_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsInsightTypesInsightsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights
/// Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified insight type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_insight_types_insights_list_builder()` + `recommender_projects_locations_insight_types_insights_list_execute()`.
/// For task-level control, use `recommender_projects_locations_insight_types_insights_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_insights_list(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsInsightTypesInsightsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListInsightsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_insight_types_insights_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_projects_locations_insight_types_insights_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_insight_types_insights_mark_accepted_execute()` to send, or `recommender_projects_locations_insight_types_insights_mark_accepted` for simplest API.

pub fn recommender_projects_locations_insight_types_insights_mark_accepted_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_insight_types_insights_mark_accepted_execute()` or `recommender_projects_locations_insight_types_insights_mark_accepted`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_insights_mark_accepted_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Insight = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_insight_types_insights_mark_accepted_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_insight_types_insights_mark_accepted_task()`.
/// For the simplest API, use `recommender_projects_locations_insight_types_insights_mark_accepted()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_insight_types_insights_mark_accepted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_insight_types_insights_mark_accepted_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_insight_types_insights_mark_accepted_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_insight_types_insights_mark_accepted`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsInsightTypesInsightsMarkAcceptedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/insightTypes/{insightTypesId}/insights/{insightsId}:markAccepted
/// Marks the Insight State as Accepted. Users can use this method to indicate to the Recommender API that they have applied some action based on the insight. This stops the insight content from being updated. MarkInsightAccepted can be applied to insights in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified insight.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_insight_types_insights_mark_accepted_builder()` + `recommender_projects_locations_insight_types_insights_mark_accepted_execute()`.
/// For task-level control, use `recommender_projects_locations_insight_types_insights_mark_accepted_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_insight_types_insights_mark_accepted(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsInsightTypesInsightsMarkAcceptedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Insight>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_insight_types_insights_mark_accepted_builder(
        client, &args.name,
    )?;
    recommender_projects_locations_insight_types_insights_mark_accepted_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_get_config_execute()` to send, or `recommender_projects_locations_recommenders_get_config` for simplest API.

pub fn recommender_projects_locations_recommenders_get_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/config",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_get_config_execute()` or `recommender_projects_locations_recommenders_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1RecommenderConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_get_config_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_recommenders_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Gets the requested Recommender Config. There is only one instance of the config for each Recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_get_config_builder()` + `recommender_projects_locations_recommenders_get_config_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_get_config(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersGetConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_projects_locations_recommenders_get_config_builder(client, &args.name)?;
    recommender_projects_locations_recommenders_get_config_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_update_config_execute()` to send, or `recommender_projects_locations_recommenders_update_config` for simplest API.

pub fn recommender_projects_locations_recommenders_update_config_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
    validateOnly: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/config",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_update_config_execute()` or `recommender_projects_locations_recommenders_update_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_update_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1RecommenderConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_update_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_update_config_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_update_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_update_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_update_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_recommenders_update_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_update_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersUpdateConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/config
/// Updates a Recommender Config. This will create a new revision of the config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_update_config_builder()` + `recommender_projects_locations_recommenders_update_config_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_update_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_update_config(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersUpdateConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1RecommenderConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_recommenders_update_config_builder(
        client,
        &args.name,
        &args.updateMask,
        &args.validateOnly,
    )?;
    recommender_projects_locations_recommenders_update_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_recommendations_get_execute()` to send, or `recommender_projects_locations_recommenders_recommendations_get` for simplest API.

pub fn recommender_projects_locations_recommenders_recommendations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_recommendations_get_execute()` or `recommender_projects_locations_recommenders_recommendations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_recommendations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_get_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_recommendations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_recommendations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_recommenders_recommendations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_recommendations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersRecommendationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}
/// Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_recommendations_get_builder()` + `recommender_projects_locations_recommenders_recommendations_get_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_get(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersRecommendationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_recommenders_recommendations_get_builder(
        client, &args.name,
    )?;
    recommender_projects_locations_recommenders_recommendations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_recommendations_list_execute()` to send, or `recommender_projects_locations_recommenders_recommendations_list` for simplest API.

pub fn recommender_projects_locations_recommenders_recommendations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_recommendations_list_execute()` or `recommender_projects_locations_recommenders_recommendations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1ListRecommendationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_recommendations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_list_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_recommendations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_recommendations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommender_projects_locations_recommenders_recommendations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_recommendations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersRecommendationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations
/// Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_recommendations_list_builder()` + `recommender_projects_locations_recommenders_recommendations_list_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_list(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersRecommendationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1ListRecommendationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_recommenders_recommendations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommender_projects_locations_recommenders_recommendations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_recommendations_mark_claimed_execute()` to send, or `recommender_projects_locations_recommenders_recommendations_mark_claimed` for simplest API.

pub fn recommender_projects_locations_recommenders_recommendations_mark_claimed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_recommendations_mark_claimed_execute()` or `recommender_projects_locations_recommenders_recommendations_mark_claimed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_claimed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_recommendations_mark_claimed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_claimed_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_recommendations_mark_claimed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_claimed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_recommendations_mark_claimed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_projects_locations_recommenders_recommendations_mark_claimed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_recommendations_mark_claimed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersRecommendationsMarkClaimedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markClaimed
/// Marks the Recommendation State as Claimed. Users can use this method to indicate to the Recommender API that they are starting to apply the recommendation themselves. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationClaimed can be applied to recommendations in CLAIMED, SUCCEEDED, FAILED, or `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_recommendations_mark_claimed_builder()` + `recommender_projects_locations_recommenders_recommendations_mark_claimed_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_claimed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_claimed(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersRecommendationsMarkClaimedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_recommenders_recommendations_mark_claimed_builder(
        client, &args.name,
    )?;
    recommender_projects_locations_recommenders_recommendations_mark_claimed_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_recommendations_mark_dismissed_execute()` to send, or `recommender_projects_locations_recommenders_recommendations_mark_dismissed` for simplest API.

pub fn recommender_projects_locations_recommenders_recommendations_mark_dismissed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_recommendations_mark_dismissed_execute()` or `recommender_projects_locations_recommenders_recommendations_mark_dismissed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_dismissed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_recommendations_mark_dismissed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_dismissed_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_recommendations_mark_dismissed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_dismissed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_recommendations_mark_dismissed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_projects_locations_recommenders_recommendations_mark_dismissed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_recommendations_mark_dismissed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersRecommendationsMarkDismissedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markDismissed
/// Mark the Recommendation State as Dismissed. Users can use this method to indicate to the Recommender API that an `ACTIVE` recommendation has to be marked back as DISMISSED. MarkRecommendationDismissed can be applied to recommendations in `ACTIVE` state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_recommendations_mark_dismissed_builder()` + `recommender_projects_locations_recommenders_recommendations_mark_dismissed_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_dismissed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_dismissed(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersRecommendationsMarkDismissedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_projects_locations_recommenders_recommendations_mark_dismissed_builder(
            client, &args.name,
        )?;
    recommender_projects_locations_recommenders_recommendations_mark_dismissed_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_recommendations_mark_failed_execute()` to send, or `recommender_projects_locations_recommenders_recommendations_mark_failed` for simplest API.

pub fn recommender_projects_locations_recommenders_recommendations_mark_failed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_recommendations_mark_failed_execute()` or `recommender_projects_locations_recommenders_recommendations_mark_failed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_failed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_recommendations_mark_failed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_failed_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_recommendations_mark_failed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_failed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_recommendations_mark_failed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_projects_locations_recommenders_recommendations_mark_failed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_recommendations_mark_failed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersRecommendationsMarkFailedArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markFailed
/// Marks the Recommendation State as Failed. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation failed. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationFailed can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_recommendations_mark_failed_builder()` + `recommender_projects_locations_recommenders_recommendations_mark_failed_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_failed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_failed(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersRecommendationsMarkFailedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommender_projects_locations_recommenders_recommendations_mark_failed_builder(
        client, &args.name,
    )?;
    recommender_projects_locations_recommenders_recommendations_mark_failed_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommender_projects_locations_recommenders_recommendations_mark_succeeded_execute()` to send, or `recommender_projects_locations_recommenders_recommendations_mark_succeeded` for simplest API.

pub fn recommender_projects_locations_recommenders_recommendations_mark_succeeded_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommender.googleapis.com/v1/projects/{}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommender_projects_locations_recommenders_recommendations_mark_succeeded_execute()` or `recommender_projects_locations_recommenders_recommendations_mark_succeeded`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_succeeded_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommenderV1Recommendation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommender_projects_locations_recommenders_recommendations_mark_succeeded_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_succeeded_task()`.
/// For the simplest API, use `recommender_projects_locations_recommenders_recommendations_mark_succeeded()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommender_projects_locations_recommenders_recommendations_mark_succeeded_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommender_projects_locations_recommenders_recommendations_mark_succeeded_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommender_projects_locations_recommenders_recommendations_mark_succeeded_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommender_projects_locations_recommenders_recommendations_mark_succeeded`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommenderProjectsLocationsRecommendersRecommendationsMarkSucceededArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/recommenders/{recommendersId}/recommendations/{recommendationsId}:markSucceeded
/// Marks the Recommendation State as Succeeded. Users can use this method to indicate to the Recommender API that they have applied the recommendation themselves, and the operation was successful. This stops the recommendation content from being updated. Associated insights are frozen and placed in the ACCEPTED state. MarkRecommendationSucceeded can be applied to recommendations in `ACTIVE`, CLAIMED, SUCCEEDED, or FAILED state. Requires the recommender.*.update IAM permission for the specified recommender.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommender_projects_locations_recommenders_recommendations_mark_succeeded_builder()` + `recommender_projects_locations_recommenders_recommendations_mark_succeeded_execute()`.
/// For task-level control, use `recommender_projects_locations_recommenders_recommendations_mark_succeeded_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommender_projects_locations_recommenders_recommendations_mark_succeeded(
    client: &SimpleHttpClient,
    args: &RecommenderProjectsLocationsRecommendersRecommendationsMarkSucceededArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommenderV1Recommendation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommender_projects_locations_recommenders_recommendations_mark_succeeded_builder(
            client, &args.name,
        )?;
    recommender_projects_locations_recommenders_recommendations_mark_succeeded_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig with RecommenderBillingAccountsLocationsInsightTypesGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsInsightTypesGetConfigArgs>
    for GoogleCloudRecommenderV1InsightTypeConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsInsightTypesGetConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig with RecommenderBillingAccountsLocationsInsightTypesUpdateConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsInsightTypesUpdateConfigArgs>
    for GoogleCloudRecommenderV1InsightTypeConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsInsightTypesUpdateConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderBillingAccountsLocationsInsightTypesInsightsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsInsightTypesInsightsGetArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsInsightTypesInsightsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse with RecommenderBillingAccountsLocationsInsightTypesInsightsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsInsightTypesInsightsListArgs>
    for GoogleCloudRecommenderV1ListInsightsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsInsightTypesInsightsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderBillingAccountsLocationsInsightTypesInsightsMarkAcceptedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsInsightTypesInsightsMarkAcceptedArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsInsightTypesInsightsMarkAcceptedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig with RecommenderBillingAccountsLocationsRecommendersGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsRecommendersGetConfigArgs>
    for GoogleCloudRecommenderV1RecommenderConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersGetConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig with RecommenderBillingAccountsLocationsRecommendersUpdateConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsRecommendersUpdateConfigArgs>
    for GoogleCloudRecommenderV1RecommenderConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersUpdateConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderBillingAccountsLocationsRecommendersRecommendationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsRecommendersRecommendationsGetArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersRecommendationsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse with RecommenderBillingAccountsLocationsRecommendersRecommendationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderBillingAccountsLocationsRecommendersRecommendationsListArgs>
    for GoogleCloudRecommenderV1ListRecommendationsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersRecommendationsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkClaimedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkClaimedArgs,
    > for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkClaimedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkDismissedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkDismissedArgs,
    > for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkDismissedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkFailedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkFailedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkFailedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkSucceededArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkSucceededArgs,
    > for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderBillingAccountsLocationsRecommendersRecommendationsMarkSucceededArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderFoldersLocationsInsightTypesInsightsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsInsightTypesInsightsGetArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsInsightTypesInsightsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse with RecommenderFoldersLocationsInsightTypesInsightsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsInsightTypesInsightsListArgs>
    for GoogleCloudRecommenderV1ListInsightsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsInsightTypesInsightsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderFoldersLocationsInsightTypesInsightsMarkAcceptedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsInsightTypesInsightsMarkAcceptedArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsInsightTypesInsightsMarkAcceptedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderFoldersLocationsRecommendersRecommendationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsRecommendersRecommendationsGetArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsRecommendersRecommendationsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse with RecommenderFoldersLocationsRecommendersRecommendationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsRecommendersRecommendationsListArgs>
    for GoogleCloudRecommenderV1ListRecommendationsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsRecommendersRecommendationsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderFoldersLocationsRecommendersRecommendationsMarkClaimedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsRecommendersRecommendationsMarkClaimedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsRecommendersRecommendationsMarkClaimedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderFoldersLocationsRecommendersRecommendationsMarkDismissedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsRecommendersRecommendationsMarkDismissedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsRecommendersRecommendationsMarkDismissedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderFoldersLocationsRecommendersRecommendationsMarkFailedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsRecommendersRecommendationsMarkFailedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsRecommendersRecommendationsMarkFailedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderFoldersLocationsRecommendersRecommendationsMarkSucceededArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderFoldersLocationsRecommendersRecommendationsMarkSucceededArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderFoldersLocationsRecommendersRecommendationsMarkSucceededArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig with RecommenderOrganizationsLocationsInsightTypesGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsInsightTypesGetConfigArgs>
    for GoogleCloudRecommenderV1InsightTypeConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsInsightTypesGetConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig with RecommenderOrganizationsLocationsInsightTypesUpdateConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsInsightTypesUpdateConfigArgs>
    for GoogleCloudRecommenderV1InsightTypeConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsInsightTypesUpdateConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderOrganizationsLocationsInsightTypesInsightsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsInsightTypesInsightsGetArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsInsightTypesInsightsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse with RecommenderOrganizationsLocationsInsightTypesInsightsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsInsightTypesInsightsListArgs>
    for GoogleCloudRecommenderV1ListInsightsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsInsightTypesInsightsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderOrganizationsLocationsInsightTypesInsightsMarkAcceptedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsInsightTypesInsightsMarkAcceptedArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsInsightTypesInsightsMarkAcceptedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig with RecommenderOrganizationsLocationsRecommendersGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsRecommendersGetConfigArgs>
    for GoogleCloudRecommenderV1RecommenderConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersGetConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig with RecommenderOrganizationsLocationsRecommendersUpdateConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsRecommendersUpdateConfigArgs>
    for GoogleCloudRecommenderV1RecommenderConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersUpdateConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderOrganizationsLocationsRecommendersRecommendationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsRecommendersRecommendationsGetArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersRecommendationsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse with RecommenderOrganizationsLocationsRecommendersRecommendationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsRecommendersRecommendationsListArgs>
    for GoogleCloudRecommenderV1ListRecommendationsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersRecommendationsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderOrganizationsLocationsRecommendersRecommendationsMarkClaimedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsRecommendersRecommendationsMarkClaimedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkClaimedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderOrganizationsLocationsRecommendersRecommendationsMarkDismissedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RecommenderOrganizationsLocationsRecommendersRecommendationsMarkDismissedArgs,
    > for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkDismissedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderOrganizationsLocationsRecommendersRecommendationsMarkFailedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderOrganizationsLocationsRecommendersRecommendationsMarkFailedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkFailedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderOrganizationsLocationsRecommendersRecommendationsMarkSucceededArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RecommenderOrganizationsLocationsRecommendersRecommendationsMarkSucceededArgs,
    > for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderOrganizationsLocationsRecommendersRecommendationsMarkSucceededArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig with RecommenderProjectsLocationsInsightTypesGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsInsightTypesGetConfigArgs>
    for GoogleCloudRecommenderV1InsightTypeConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsInsightTypesGetConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1InsightTypeConfig with RecommenderProjectsLocationsInsightTypesUpdateConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsInsightTypesUpdateConfigArgs>
    for GoogleCloudRecommenderV1InsightTypeConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsInsightTypesUpdateConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1InsightTypeConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderProjectsLocationsInsightTypesInsightsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsInsightTypesInsightsGetArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsInsightTypesInsightsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListInsightsResponse with RecommenderProjectsLocationsInsightTypesInsightsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsInsightTypesInsightsListArgs>
    for GoogleCloudRecommenderV1ListInsightsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsInsightTypesInsightsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListInsightsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Insight with RecommenderProjectsLocationsInsightTypesInsightsMarkAcceptedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsInsightTypesInsightsMarkAcceptedArgs>
    for GoogleCloudRecommenderV1Insight
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsInsightTypesInsightsMarkAcceptedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Insight/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Insight"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig with RecommenderProjectsLocationsRecommendersGetConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersGetConfigArgs>
    for GoogleCloudRecommenderV1RecommenderConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersGetConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1RecommenderConfig with RecommenderProjectsLocationsRecommendersUpdateConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersUpdateConfigArgs>
    for GoogleCloudRecommenderV1RecommenderConfig
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersUpdateConfigArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1RecommenderConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderProjectsLocationsRecommendersRecommendationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersRecommendationsGetArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersRecommendationsGetArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1ListRecommendationsResponse with RecommenderProjectsLocationsRecommendersRecommendationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersRecommendationsListArgs>
    for GoogleCloudRecommenderV1ListRecommendationsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersRecommendationsListArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1ListRecommendationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderProjectsLocationsRecommendersRecommendationsMarkClaimedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersRecommendationsMarkClaimedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersRecommendationsMarkClaimedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderProjectsLocationsRecommendersRecommendationsMarkDismissedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersRecommendationsMarkDismissedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersRecommendationsMarkDismissedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderProjectsLocationsRecommendersRecommendationsMarkFailedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersRecommendationsMarkFailedArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersRecommendationsMarkFailedArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommenderV1Recommendation with RecommenderProjectsLocationsRecommendersRecommendationsMarkSucceededArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommenderProjectsLocationsRecommendersRecommendationsMarkSucceededArgs>
    for GoogleCloudRecommenderV1Recommendation
{
    fn generate_resource_id(
        &self,
        input: &RecommenderProjectsLocationsRecommendersRecommendationsMarkSucceededArgs,
    ) -> String {
        format!(
            "gcp::recommender::GoogleCloudRecommenderV1Recommendation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommender::GoogleCloudRecommenderV1Recommendation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
