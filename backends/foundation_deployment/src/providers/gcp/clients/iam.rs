//! Auto-generated API clients for gcp/iam.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/policies/{policiesId}/{policiesId1}
/// Creates a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iam_policies_create_policy_execute()` to send, or `iam_policies_create_policy` for simplest API.

pub fn iam_policies_create_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    policyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iam.googleapis.com/v2/policies/{}/{policiesId1}",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = policyId.as_ref() {
        query_parts.push(format!("policyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/policies/{policiesId}/{policiesId1}
/// Creates a policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iam_policies_create_policy_execute()` or `iam_policies_create_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_create_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_create_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/policies/{policiesId}/{policiesId1}
/// Creates a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iam_policies_create_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iam_policies_create_policy_task()`.
/// For the simplest API, use `iam_policies_create_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_create_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iam_policies_create_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iam_policies_create_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iam_policies_create_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamPoliciesCreatePolicyArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: policyId
    pub policyId: Option<Option<String>>,
}

/// POST v2/policies/{policiesId}/{policiesId1}
/// Creates a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iam_policies_create_policy_builder()` + `iam_policies_create_policy_execute()`.
/// For task-level control, use `iam_policies_create_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_create_policy(
    client: &SimpleHttpClient,
    args: &IamPoliciesCreatePolicyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iam_policies_create_policy_builder(client, &args.parent, &args.policyId)?;
    iam_policies_create_policy_execute(builder)
}

/// DELETE v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Deletes a policy. This action is permanent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iam_policies_delete_execute()` to send, or `iam_policies_delete` for simplest API.

pub fn iam_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iam.googleapis.com/v2/policies/{}/{policiesId1}/{policiesId2}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Deletes a policy. This action is permanent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iam_policies_delete_execute()` or `iam_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Deletes a policy. This action is permanent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iam_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iam_policies_delete_task()`.
/// For the simplest API, use `iam_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iam_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iam_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iam_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Deletes a policy. This action is permanent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iam_policies_delete_builder()` + `iam_policies_delete_execute()`.
/// For task-level control, use `iam_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_delete(
    client: &SimpleHttpClient,
    args: &IamPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iam_policies_delete_builder(client, &args.name, &args.etag)?;
    iam_policies_delete_execute(builder)
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Gets a policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iam_policies_get_execute()` to send, or `iam_policies_get` for simplest API.

pub fn iam_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iam.googleapis.com/v2/policies/{}/{policiesId1}/{policiesId2}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Gets a policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iam_policies_get_execute()` or `iam_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV2Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV2Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Gets a policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iam_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iam_policies_get_task()`.
/// For the simplest API, use `iam_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iam_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV2Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = iam_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iam_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Gets a policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iam_policies_get_builder()` + `iam_policies_get_execute()`.
/// For task-level control, use `iam_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_get(
    client: &SimpleHttpClient,
    args: &IamPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV2Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iam_policies_get_builder(client, &args.name)?;
    iam_policies_get_execute(builder)
}

/// GET v2/policies/{policiesId}/{policiesId1}
/// Retrieves the policies of the specified kind that are attached to a resource. The response lists only policy metadata. In particular, policy rules are omitted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iam_policies_list_policies_execute()` to send, or `iam_policies_list_policies` for simplest API.

pub fn iam_policies_list_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iam.googleapis.com/v2/policies/{}/{policiesId1}",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/policies/{policiesId}/{policiesId1}
/// Retrieves the policies of the specified kind that are attached to a resource. The response lists only policy metadata. In particular, policy rules are omitted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iam_policies_list_policies_execute()` or `iam_policies_list_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_list_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_list_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV2ListPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV2ListPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/policies/{policiesId}/{policiesId1}
/// Retrieves the policies of the specified kind that are attached to a resource. The response lists only policy metadata. In particular, policy rules are omitted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iam_policies_list_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iam_policies_list_policies_task()`.
/// For the simplest API, use `iam_policies_list_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_list_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iam_policies_list_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iam_policies_list_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iam_policies_list_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamPoliciesListPoliciesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/policies/{policiesId}/{policiesId1}
/// Retrieves the policies of the specified kind that are attached to a resource. The response lists only policy metadata. In particular, policy rules are omitted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iam_policies_list_policies_builder()` + `iam_policies_list_policies_execute()`.
/// For task-level control, use `iam_policies_list_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_list_policies(
    client: &SimpleHttpClient,
    args: &IamPoliciesListPoliciesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV2ListPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        iam_policies_list_policies_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    iam_policies_list_policies_execute(builder)
}

/// PUT v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Updates the specified policy. You can update only the rules and the display name for the policy. To update a policy, you should use a read-modify-write loop: 1. Use GetPolicy to read the current version of the policy. 2. Modify the policy as needed. 3. Use UpdatePolicy to write the updated policy. This pattern helps prevent conflicts between concurrent updates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iam_policies_update_execute()` to send, or `iam_policies_update` for simplest API.

pub fn iam_policies_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iam.googleapis.com/v2/policies/{}/{policiesId1}/{policiesId2}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Updates the specified policy. You can update only the rules and the display name for the policy. To update a policy, you should use a read-modify-write loop: 1. Use GetPolicy to read the current version of the policy. 2. Modify the policy as needed. 3. Use UpdatePolicy to write the updated policy. This pattern helps prevent conflicts between concurrent updates.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iam_policies_update_execute()` or `iam_policies_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Updates the specified policy. You can update only the rules and the display name for the policy. To update a policy, you should use a read-modify-write loop: 1. Use GetPolicy to read the current version of the policy. 2. Modify the policy as needed. 3. Use UpdatePolicy to write the updated policy. This pattern helps prevent conflicts between concurrent updates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iam_policies_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iam_policies_update_task()`.
/// For the simplest API, use `iam_policies_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iam_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iam_policies_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iam_policies_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamPoliciesUpdateArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v2/policies/{policiesId}/{policiesId1}/{policiesId2}
/// Updates the specified policy. You can update only the rules and the display name for the policy. To update a policy, you should use a read-modify-write loop: 1. Use GetPolicy to read the current version of the policy. 2. Modify the policy as needed. 3. Use UpdatePolicy to write the updated policy. This pattern helps prevent conflicts between concurrent updates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iam_policies_update_builder()` + `iam_policies_update_execute()`.
/// For task-level control, use `iam_policies_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_update(
    client: &SimpleHttpClient,
    args: &IamPoliciesUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iam_policies_update_builder(client, &args.name)?;
    iam_policies_update_execute(builder)
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iam_policies_operations_get_execute()` to send, or `iam_policies_operations_get` for simplest API.

pub fn iam_policies_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iam.googleapis.com/v2/policies/{}/{policiesId1}/{policiesId2}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iam_policies_operations_get_execute()` or `iam_policies_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iam_policies_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iam_policies_operations_get_task()`.
/// For the simplest API, use `iam_policies_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iam_policies_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iam_policies_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iam_policies_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iam_policies_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamPoliciesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/policies/{policiesId}/{policiesId1}/{policiesId2}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iam_policies_operations_get_builder()` + `iam_policies_operations_get_execute()`.
/// For task-level control, use `iam_policies_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iam_policies_operations_get(
    client: &SimpleHttpClient,
    args: &IamPoliciesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iam_policies_operations_get_builder(client, &args.name)?;
    iam_policies_operations_get_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with IamPoliciesCreatePolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamPoliciesCreatePolicyArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &IamPoliciesCreatePolicyArgs) -> String {
        format!("gcp::iam::GoogleLongrunningOperation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iam::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with IamPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamPoliciesDeleteArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &IamPoliciesDeleteArgs) -> String {
        format!("gcp::iam::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iam::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV2Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV2Policy with IamPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamPoliciesGetArgs> for GoogleIamV2Policy {
    fn generate_resource_id(&self, input: &IamPoliciesGetArgs) -> String {
        format!("gcp::iam::GoogleIamV2Policy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iam::GoogleIamV2Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV2ListPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV2ListPoliciesResponse with IamPoliciesListPoliciesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamPoliciesListPoliciesArgs> for GoogleIamV2ListPoliciesResponse {
    fn generate_resource_id(&self, input: &IamPoliciesListPoliciesArgs) -> String {
        format!("gcp::iam::GoogleIamV2ListPoliciesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iam::GoogleIamV2ListPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with IamPoliciesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamPoliciesUpdateArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &IamPoliciesUpdateArgs) -> String {
        format!("gcp::iam::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iam::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with IamPoliciesOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamPoliciesOperationsGetArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &IamPoliciesOperationsGetArgs) -> String {
        format!("gcp::iam::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iam::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
