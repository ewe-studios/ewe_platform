//! Auto-generated API clients for gcp/oauth2.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET oauth2/v2/userinfo
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `oauth2_userinfo_get_execute()` to send, or `oauth2_userinfo_get` for simplest API.

pub fn oauth2_userinfo_get_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/oauth2/v2/userinfo",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET oauth2/v2/userinfo
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `oauth2_userinfo_get_execute()` or `oauth2_userinfo_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oauth2_userinfo_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oauth2_userinfo_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Userinfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Userinfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET oauth2/v2/userinfo
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `oauth2_userinfo_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `oauth2_userinfo_get_task()`.
/// For the simplest API, use `oauth2_userinfo_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oauth2_userinfo_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn oauth2_userinfo_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Userinfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = oauth2_userinfo_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET oauth2/v2/userinfo
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `oauth2_userinfo_get_builder()` + `oauth2_userinfo_get_execute()`.
/// For task-level control, use `oauth2_userinfo_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oauth2_userinfo_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Userinfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = oauth2_userinfo_get_builder(client)?;
    oauth2_userinfo_get_execute(builder)
}

/// GET userinfo/v2/me
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `oauth2_userinfo_v2_me_get_execute()` to send, or `oauth2_userinfo_v2_me_get` for simplest API.

pub fn oauth2_userinfo_v2_me_get_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/userinfo/v2/me",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET userinfo/v2/me
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `oauth2_userinfo_v2_me_get_execute()` or `oauth2_userinfo_v2_me_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oauth2_userinfo_v2_me_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oauth2_userinfo_v2_me_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Userinfo>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Userinfo = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET userinfo/v2/me
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `oauth2_userinfo_v2_me_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `oauth2_userinfo_v2_me_get_task()`.
/// For the simplest API, use `oauth2_userinfo_v2_me_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oauth2_userinfo_v2_me_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn oauth2_userinfo_v2_me_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Userinfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = oauth2_userinfo_v2_me_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET userinfo/v2/me
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `oauth2_userinfo_v2_me_get_builder()` + `oauth2_userinfo_v2_me_get_execute()`.
/// For task-level control, use `oauth2_userinfo_v2_me_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oauth2_userinfo_v2_me_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Userinfo>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = oauth2_userinfo_v2_me_get_builder(client)?;
    oauth2_userinfo_v2_me_get_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Userinfo
// =============================================================================

/// ResourceIdentifier implementation for Userinfo with Oauth2UserinfoGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Oauth2UserinfoGetArgs> for Userinfo {
    fn generate_resource_id(&self, input: &Oauth2UserinfoGetArgs) -> String {
        "gcp::oauth2::Userinfo".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::oauth2::Userinfo"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Userinfo
// =============================================================================

/// ResourceIdentifier implementation for Userinfo with Oauth2UserinfoV2MeGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<Oauth2UserinfoV2MeGetArgs> for Userinfo {
    fn generate_resource_id(&self, input: &Oauth2UserinfoV2MeGetArgs) -> String {
        "gcp::oauth2::Userinfo".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::oauth2::Userinfo"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
