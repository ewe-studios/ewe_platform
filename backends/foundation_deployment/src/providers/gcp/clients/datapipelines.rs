//! Auto-generated API clients for gcp/datapipelines.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Creates a pipeline. For a batch pipeline, you can pass scheduler information. Data Pipelines uses the scheduler information to create an internal scheduler that runs jobs periodically. If the internal scheduler is not configured, you can use RunPipeline to run jobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_create_execute()` to send, or `datapipelines_projects_locations_pipelines_create` for simplest API.

pub fn datapipelines_projects_locations_pipelines_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Creates a pipeline. For a batch pipeline, you can pass scheduler information. Data Pipelines uses the scheduler information to create an internal scheduler that runs jobs periodically. If the internal scheduler is not configured, you can use RunPipeline to run jobs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_create_execute()` or `datapipelines_projects_locations_pipelines_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Creates a pipeline. For a batch pipeline, you can pass scheduler information. Data Pipelines uses the scheduler information to create an internal scheduler that runs jobs periodically. If the internal scheduler is not configured, you can use RunPipeline to run jobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_create_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Creates a pipeline. For a batch pipeline, you can pass scheduler information. Data Pipelines uses the scheduler information to create an internal scheduler that runs jobs periodically. If the internal scheduler is not configured, you can use RunPipeline to run jobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_create_builder()` + `datapipelines_projects_locations_pipelines_create_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_create(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_create_builder(client, &args.parent)?;
    datapipelines_projects_locations_pipelines_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Deletes a pipeline. If a scheduler job is attached to the pipeline, it will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_delete_execute()` to send, or `datapipelines_projects_locations_pipelines_delete` for simplest API.

pub fn datapipelines_projects_locations_pipelines_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines/{pipelinesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Deletes a pipeline. If a scheduler job is attached to the pipeline, it will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_delete_execute()` or `datapipelines_projects_locations_pipelines_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Deletes a pipeline. If a scheduler job is attached to the pipeline, it will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_delete_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Deletes a pipeline. If a scheduler job is attached to the pipeline, it will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_delete_builder()` + `datapipelines_projects_locations_pipelines_delete_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_delete(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_delete_builder(client, &args.name)?;
    datapipelines_projects_locations_pipelines_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Looks up a single pipeline. Returns a "NOT_FOUND" error if no such pipeline exists. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_get_execute()` to send, or `datapipelines_projects_locations_pipelines_get` for simplest API.

pub fn datapipelines_projects_locations_pipelines_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines/{pipelinesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Looks up a single pipeline. Returns a "NOT_FOUND" error if no such pipeline exists. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_get_execute()` or `datapipelines_projects_locations_pipelines_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Looks up a single pipeline. Returns a "NOT_FOUND" error if no such pipeline exists. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_get_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Looks up a single pipeline. Returns a "NOT_FOUND" error if no such pipeline exists. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_get_builder()` + `datapipelines_projects_locations_pipelines_get_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_get(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_get_builder(client, &args.name)?;
    datapipelines_projects_locations_pipelines_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Lists pipelines. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_list_execute()` to send, or `datapipelines_projects_locations_pipelines_list` for simplest API.

pub fn datapipelines_projects_locations_pipelines_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Lists pipelines. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_list_execute()` or `datapipelines_projects_locations_pipelines_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatapipelinesV1ListPipelinesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1ListPipelinesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Lists pipelines. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_list_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines
/// Lists pipelines. Returns a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_list_builder()` + `datapipelines_projects_locations_pipelines_list_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_list(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    datapipelines_projects_locations_pipelines_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Updates a pipeline. If successful, the updated Pipeline is returned. Returns NOT_FOUND if the pipeline doesn''t exist. If UpdatePipeline does not return successfully, you can retry the UpdatePipeline request until you receive a successful response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_patch_execute()` to send, or `datapipelines_projects_locations_pipelines_patch` for simplest API.

pub fn datapipelines_projects_locations_pipelines_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines/{pipelinesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Updates a pipeline. If successful, the updated Pipeline is returned. Returns NOT_FOUND if the pipeline doesn''t exist. If UpdatePipeline does not return successfully, you can retry the UpdatePipeline request until you receive a successful response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_patch_execute()` or `datapipelines_projects_locations_pipelines_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Updates a pipeline. If successful, the updated Pipeline is returned. Returns NOT_FOUND if the pipeline doesn''t exist. If UpdatePipeline does not return successfully, you can retry the UpdatePipeline request until you receive a successful response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_patch_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}
/// Updates a pipeline. If successful, the updated Pipeline is returned. Returns NOT_FOUND if the pipeline doesn''t exist. If UpdatePipeline does not return successfully, you can retry the UpdatePipeline request until you receive a successful response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_patch_builder()` + `datapipelines_projects_locations_pipelines_patch_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_patch(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    datapipelines_projects_locations_pipelines_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:run
/// Creates a job for the specified pipeline directly. You can use this method when the internal scheduler is not configured and you want to trigger the job directly or through an external system. Returns a "NOT_FOUND" error if the pipeline doesn''t exist. Returns a "FORBIDDEN" error if the user doesn''t have permission to access the pipeline or run jobs for the pipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_run_execute()` to send, or `datapipelines_projects_locations_pipelines_run` for simplest API.

pub fn datapipelines_projects_locations_pipelines_run_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines/{pipelinesId}:run",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:run
/// Creates a job for the specified pipeline directly. You can use this method when the internal scheduler is not configured and you want to trigger the job directly or through an external system. Returns a "NOT_FOUND" error if the pipeline doesn''t exist. Returns a "FORBIDDEN" error if the user doesn''t have permission to access the pipeline or run jobs for the pipeline.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_run_execute()` or `datapipelines_projects_locations_pipelines_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatapipelinesV1RunPipelineResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1RunPipelineResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:run
/// Creates a job for the specified pipeline directly. You can use this method when the internal scheduler is not configured and you want to trigger the job directly or through an external system. Returns a "NOT_FOUND" error if the pipeline doesn''t exist. Returns a "FORBIDDEN" error if the user doesn''t have permission to access the pipeline or run jobs for the pipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_run_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1RunPipelineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesRunArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:run
/// Creates a job for the specified pipeline directly. You can use this method when the internal scheduler is not configured and you want to trigger the job directly or through an external system. Returns a "NOT_FOUND" error if the pipeline doesn''t exist. Returns a "FORBIDDEN" error if the user doesn''t have permission to access the pipeline or run jobs for the pipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_run_builder()` + `datapipelines_projects_locations_pipelines_run_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_run(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesRunArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1RunPipelineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_run_builder(client, &args.name)?;
    datapipelines_projects_locations_pipelines_run_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:stop
/// Freezes pipeline execution permanently. If there''s a corresponding scheduler entry, it''s deleted, and the pipeline state is changed to "ARCHIVED". However, pipeline metadata is retained.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_stop_execute()` to send, or `datapipelines_projects_locations_pipelines_stop` for simplest API.

pub fn datapipelines_projects_locations_pipelines_stop_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines/{pipelinesId}:stop",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:stop
/// Freezes pipeline execution permanently. If there''s a corresponding scheduler entry, it''s deleted, and the pipeline state is changed to "ARCHIVED". However, pipeline metadata is retained.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_stop_execute()` or `datapipelines_projects_locations_pipelines_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:stop
/// Freezes pipeline execution permanently. If there''s a corresponding scheduler entry, it''s deleted, and the pipeline state is changed to "ARCHIVED". However, pipeline metadata is retained.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_stop_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesStopArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}:stop
/// Freezes pipeline execution permanently. If there''s a corresponding scheduler entry, it''s deleted, and the pipeline state is changed to "ARCHIVED". However, pipeline metadata is retained.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_stop_builder()` + `datapipelines_projects_locations_pipelines_stop_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_stop(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesStopArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_stop_builder(client, &args.name)?;
    datapipelines_projects_locations_pipelines_stop_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}/jobs
/// Lists jobs for a given pipeline. Throws a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_jobs_list_execute()` to send, or `datapipelines_projects_locations_pipelines_jobs_list` for simplest API.

pub fn datapipelines_projects_locations_pipelines_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://datapipelines.googleapis.com/v1/projects/{}/locations/{locationsId}/pipelines/{pipelinesId}/jobs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}/jobs
/// Lists jobs for a given pipeline. Throws a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datapipelines_projects_locations_pipelines_jobs_list_execute()` or `datapipelines_projects_locations_pipelines_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatapipelinesV1ListJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1ListJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}/jobs
/// Lists jobs for a given pipeline. Throws a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datapipelines_projects_locations_pipelines_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datapipelines_projects_locations_pipelines_jobs_list_task()`.
/// For the simplest API, use `datapipelines_projects_locations_pipelines_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datapipelines_projects_locations_pipelines_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datapipelines_projects_locations_pipelines_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datapipelines_projects_locations_pipelines_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datapipelines_projects_locations_pipelines_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatapipelinesProjectsLocationsPipelinesJobsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/pipelines/{pipelinesId}/jobs
/// Lists jobs for a given pipeline. Throws a "FORBIDDEN" error if the caller doesn''t have permission to access it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_jobs_list_builder()` + `datapipelines_projects_locations_pipelines_jobs_list_execute()`.
/// For task-level control, use `datapipelines_projects_locations_pipelines_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_jobs_list(
    client: &SimpleHttpClient,
    args: &DatapipelinesProjectsLocationsPipelinesJobsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_jobs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    datapipelines_projects_locations_pipelines_jobs_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline with DatapipelinesProjectsLocationsPipelinesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesCreateArgs>
    for GoogleCloudDatapipelinesV1Pipeline
{
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesCreateArgs,
    ) -> String {
        format!(
            "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DatapipelinesProjectsLocationsPipelinesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesDeleteArgs,
    ) -> String {
        format!("gcp::datapipelines::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline with DatapipelinesProjectsLocationsPipelinesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesGetArgs>
    for GoogleCloudDatapipelinesV1Pipeline
{
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesGetArgs,
    ) -> String {
        format!(
            "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1ListPipelinesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1ListPipelinesResponse with DatapipelinesProjectsLocationsPipelinesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesListArgs>
    for GoogleCloudDatapipelinesV1ListPipelinesResponse
{
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesListArgs,
    ) -> String {
        format!(
            "gcp::datapipelines::GoogleCloudDatapipelinesV1ListPipelinesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleCloudDatapipelinesV1ListPipelinesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline with DatapipelinesProjectsLocationsPipelinesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesPatchArgs>
    for GoogleCloudDatapipelinesV1Pipeline
{
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesPatchArgs,
    ) -> String {
        format!(
            "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1RunPipelineResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1RunPipelineResponse with DatapipelinesProjectsLocationsPipelinesRunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesRunArgs>
    for GoogleCloudDatapipelinesV1RunPipelineResponse
{
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesRunArgs,
    ) -> String {
        format!(
            "gcp::datapipelines::GoogleCloudDatapipelinesV1RunPipelineResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleCloudDatapipelinesV1RunPipelineResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1Pipeline with DatapipelinesProjectsLocationsPipelinesStopArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesStopArgs>
    for GoogleCloudDatapipelinesV1Pipeline
{
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesStopArgs,
    ) -> String {
        format!(
            "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleCloudDatapipelinesV1Pipeline"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1ListJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDatapipelinesV1ListJobsResponse with DatapipelinesProjectsLocationsPipelinesJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DatapipelinesProjectsLocationsPipelinesJobsListArgs>
    for GoogleCloudDatapipelinesV1ListJobsResponse
{
    fn generate_resource_id(
        &self,
        input: &DatapipelinesProjectsLocationsPipelinesJobsListArgs,
    ) -> String {
        format!(
            "gcp::datapipelines::GoogleCloudDatapipelinesV1ListJobsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::datapipelines::GoogleCloudDatapipelinesV1ListJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
