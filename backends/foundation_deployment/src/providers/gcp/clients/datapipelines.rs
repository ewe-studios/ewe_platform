//! Auto-generated API clients for gcp/datapipelines.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+parent}/pipelines
/// Creates a pipeline. For a batch pipeline, you can pass scheduler information. Data Pipelines uses the scheduler information to create an internal scheduler that runs jobs periodically. If the internal scheduler is not configured, you can use RunPipeline to run jobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_create_execute()` to send, or `datapipelines_projects_locations_pipelines_create` for simplest API.

pub fn datapipelines_projects_locations_pipelines_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDatapipelinesV1Pipeline,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/pipelines", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/pipelines
/// Creates a pipeline. For a batch pipeline, you can pass scheduler information. Data Pipelines uses the scheduler information to create an internal scheduler that runs jobs periodically. If the internal scheduler is not configured, you can use RunPipeline to run jobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/pipelines
/// Creates a pipeline. For a batch pipeline, you can pass scheduler information. Data Pipelines uses the scheduler information to create an internal scheduler that runs jobs periodically. If the internal scheduler is not configured, you can use RunPipeline to run jobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_create_builder()` + `datapipelines_projects_locations_pipelines_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDatapipelinesV1Pipeline,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_create_builder(client, parent, body)?;
    datapipelines_projects_locations_pipelines_create_execute(builder)
}

/// GET v1/{+name}
/// Deletes a pipeline. If a scheduler job is attached to the pipeline, it will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_delete_execute()` to send, or `datapipelines_projects_locations_pipelines_delete` for simplest API.

pub fn datapipelines_projects_locations_pipelines_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a pipeline. If a scheduler job is attached to the pipeline, it will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Deletes a pipeline. If a scheduler job is attached to the pipeline, it will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_delete_builder()` + `datapipelines_projects_locations_pipelines_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_delete_builder(client, name)?;
    datapipelines_projects_locations_pipelines_delete_execute(builder)
}

/// GET v1/{+name}
/// Looks up a single pipeline. Returns a "NOT_FOUND" error if no such pipeline exists. Returns a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_get_execute()` to send, or `datapipelines_projects_locations_pipelines_get` for simplest API.

pub fn datapipelines_projects_locations_pipelines_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Looks up a single pipeline. Returns a "NOT_FOUND" error if no such pipeline exists. Returns a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Looks up a single pipeline. Returns a "NOT_FOUND" error if no such pipeline exists. Returns a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_get_builder()` + `datapipelines_projects_locations_pipelines_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_get_builder(client, name)?;
    datapipelines_projects_locations_pipelines_get_execute(builder)
}

/// GET v1/{+parent}/pipelines
/// Lists pipelines. Returns a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_list_execute()` to send, or `datapipelines_projects_locations_pipelines_list` for simplest API.

pub fn datapipelines_projects_locations_pipelines_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/pipelines", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/pipelines
/// Lists pipelines. Returns a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1ListPipelinesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/pipelines
/// Lists pipelines. Returns a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_list_builder()` + `datapipelines_projects_locations_pipelines_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListPipelinesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    datapipelines_projects_locations_pipelines_list_execute(builder)
}

/// GET v1/{+name}
/// Updates a pipeline. If successful, the updated Pipeline is returned. Returns NOT_FOUND if the pipeline doesn't exist. If UpdatePipeline does not return successfully, you can retry the UpdatePipeline request until you receive a successful response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_patch_execute()` to send, or `datapipelines_projects_locations_pipelines_patch` for simplest API.

pub fn datapipelines_projects_locations_pipelines_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDatapipelinesV1Pipeline,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a pipeline. If successful, the updated Pipeline is returned. Returns NOT_FOUND if the pipeline doesn't exist. If UpdatePipeline does not return successfully, you can retry the UpdatePipeline request until you receive a successful response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a pipeline. If successful, the updated Pipeline is returned. Returns NOT_FOUND if the pipeline doesn't exist. If UpdatePipeline does not return successfully, you can retry the UpdatePipeline request until you receive a successful response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_patch_builder()` + `datapipelines_projects_locations_pipelines_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDatapipelinesV1Pipeline,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datapipelines_projects_locations_pipelines_patch_builder(client, name, updateMask, body)?;
    datapipelines_projects_locations_pipelines_patch_execute(builder)
}

/// GET v1/{+name}:run
/// Creates a job for the specified pipeline directly. You can use this method when the internal scheduler is not configured and you want to trigger the job directly or through an external system. Returns a "NOT_FOUND" error if the pipeline doesn't exist. Returns a "FORBIDDEN" error if the user doesn't have permission to access the pipeline or run jobs for the pipeline.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_run_execute()` to send, or `datapipelines_projects_locations_pipelines_run` for simplest API.

pub fn datapipelines_projects_locations_pipelines_run_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatapipelinesV1RunPipelineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:run", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:run
/// Creates a job for the specified pipeline directly. You can use this method when the internal scheduler is not configured and you want to trigger the job directly or through an external system. Returns a "NOT_FOUND" error if the pipeline doesn't exist. Returns a "FORBIDDEN" error if the user doesn't have permission to access the pipeline or run jobs for the pipeline.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1RunPipelineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1RunPipelineResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:run
/// Creates a job for the specified pipeline directly. You can use this method when the internal scheduler is not configured and you want to trigger the job directly or through an external system. Returns a "NOT_FOUND" error if the pipeline doesn't exist. Returns a "FORBIDDEN" error if the user doesn't have permission to access the pipeline or run jobs for the pipeline.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_run_builder()` + `datapipelines_projects_locations_pipelines_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_run(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatapipelinesV1RunPipelineRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1RunPipelineResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_run_builder(client, name, body)?;
    datapipelines_projects_locations_pipelines_run_execute(builder)
}

/// GET v1/{+name}:stop
/// Freezes pipeline execution permanently. If there's a corresponding scheduler entry, it's deleted, and the pipeline state is changed to "ARCHIVED". However, pipeline metadata is retained.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_stop_execute()` to send, or `datapipelines_projects_locations_pipelines_stop` for simplest API.

pub fn datapipelines_projects_locations_pipelines_stop_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatapipelinesV1StopPipelineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:stop", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:stop
/// Freezes pipeline execution permanently. If there's a corresponding scheduler entry, it's deleted, and the pipeline state is changed to "ARCHIVED". However, pipeline metadata is retained.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1Pipeline = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:stop
/// Freezes pipeline execution permanently. If there's a corresponding scheduler entry, it's deleted, and the pipeline state is changed to "ARCHIVED". However, pipeline metadata is retained.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_stop_builder()` + `datapipelines_projects_locations_pipelines_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_stop(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDatapipelinesV1StopPipelineRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1Pipeline>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_stop_builder(client, name, body)?;
    datapipelines_projects_locations_pipelines_stop_execute(builder)
}

/// GET v1/{+parent}/jobs
/// Lists jobs for a given pipeline. Throws a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datapipelines_projects_locations_pipelines_jobs_list_execute()` to send, or `datapipelines_projects_locations_pipelines_jobs_list` for simplest API.

pub fn datapipelines_projects_locations_pipelines_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/jobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/jobs
/// Lists jobs for a given pipeline. Throws a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `datapipelines_projects_locations_pipelines_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from datapipelines_projects_locations_pipelines_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatapipelinesV1ListJobsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/jobs
/// Lists jobs for a given pipeline. Throws a "FORBIDDEN" error if the caller doesn't have permission to access it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datapipelines_projects_locations_pipelines_jobs_list_builder()` + `datapipelines_projects_locations_pipelines_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datapipelines_projects_locations_pipelines_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatapipelinesV1ListJobsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datapipelines_projects_locations_pipelines_jobs_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    datapipelines_projects_locations_pipelines_jobs_list_execute(builder)
}
