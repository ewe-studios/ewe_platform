//! Auto-generated API clients for gcp/cloudsupport.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/caseClassifications:search
/// Retrieve valid classifications to use when creating a support case. Classifications are hierarchical. Each classification is a string containing all levels of the hierarchy separated by " &gt; ". For example, "Technical Issue &gt; Compute &gt; Compute Engine". Classification IDs returned by this endpoint are valid for at least six months. When a classification is deactivated, this endpoint immediately stops returning it. After six months, case.create requests using the classification will fail. EXAMPLES: `cURL`: shell curl \ --header "Authorization: Bearer $(gcloud auth print-access-token)" \ '<https://cloudsupport.googleapis.`com/v2/`caseClassifications``:search?query=display_name:"*Compute%20Engine*"'>  Python: python import googleapiclient.discovery `supportApiService` = googleapiclient.discovery.build( `serviceName`="cloudsupport", version="v2", `discoveryServiceUrl`=f"<https://cloudsupport.googleapis.com/$`discovery/rest`?version=v2",> ) request = `supportApiService`.`caseClassifications`().search( query='display_name:"*Compute Engine*"' ) print(request.execute())
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsupport_case_classifications_search_execute()` to send, or `cloudsupport_case_classifications_search` for simplest API.

pub fn cloudsupport_case_classifications_search_builder(
    client: &SimpleHttpClient,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://cloudsupport.googleapis.com/v2/caseClassifications:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/caseClassifications:search
/// Retrieve valid classifications to use when creating a support case. Classifications are hierarchical. Each classification is a string containing all levels of the hierarchy separated by " &gt; ". For example, "Technical Issue &gt; Compute &gt; Compute Engine". Classification IDs returned by this endpoint are valid for at least six months. When a classification is deactivated, this endpoint immediately stops returning it. After six months, case.create requests using the classification will fail. EXAMPLES: `cURL`: shell curl \ --header "Authorization: Bearer $(gcloud auth print-access-token)" \ '<https://cloudsupport.googleapis.`com/v2/`caseClassifications``:search?query=display_name:"*Compute%20Engine*"'>  Python: python import googleapiclient.discovery `supportApiService` = googleapiclient.discovery.build( `serviceName`="cloudsupport", version="v2", `discoveryServiceUrl`=f"<https://cloudsupport.googleapis.com/$`discovery/rest`?version=v2",> ) request = `supportApiService`.`caseClassifications`().search( query='display_name:"*Compute Engine*"' ) print(request.execute())
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsupport_case_classifications_search_execute()` or `cloudsupport_case_classifications_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsupport_case_classifications_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsupport_case_classifications_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchCaseClassificationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchCaseClassificationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/caseClassifications:search
/// Retrieve valid classifications to use when creating a support case. Classifications are hierarchical. Each classification is a string containing all levels of the hierarchy separated by " &gt; ". For example, "Technical Issue &gt; Compute &gt; Compute Engine". Classification IDs returned by this endpoint are valid for at least six months. When a classification is deactivated, this endpoint immediately stops returning it. After six months, case.create requests using the classification will fail. EXAMPLES: `cURL`: shell curl \ --header "Authorization: Bearer $(gcloud auth print-access-token)" \ '<https://cloudsupport.googleapis.`com/v2/`caseClassifications``:search?query=display_name:"*Compute%20Engine*"'>  Python: python import googleapiclient.discovery `supportApiService` = googleapiclient.discovery.build( `serviceName`="cloudsupport", version="v2", `discoveryServiceUrl`=f"<https://cloudsupport.googleapis.com/$`discovery/rest`?version=v2",> ) request = `supportApiService`.`caseClassifications`().search( query='display_name:"*Compute Engine*"' ) print(request.execute())
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsupport_case_classifications_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsupport_case_classifications_search_task()`.
/// For the simplest API, use `cloudsupport_case_classifications_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsupport_case_classifications_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsupport_case_classifications_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchCaseClassificationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsupport_case_classifications_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsupport_case_classifications_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsupportCaseClassificationsSearchArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v2/caseClassifications:search
/// Retrieve valid classifications to use when creating a support case. Classifications are hierarchical. Each classification is a string containing all levels of the hierarchy separated by " &gt; ". For example, "Technical Issue &gt; Compute &gt; Compute Engine". Classification IDs returned by this endpoint are valid for at least six months. When a classification is deactivated, this endpoint immediately stops returning it. After six months, case.create requests using the classification will fail. EXAMPLES: `cURL`: shell curl \ --header "Authorization: Bearer $(gcloud auth print-access-token)" \ '<https://cloudsupport.googleapis.`com/v2/`caseClassifications``:search?query=display_name:"*Compute%20Engine*"'>  Python: python import googleapiclient.discovery `supportApiService` = googleapiclient.discovery.build( `serviceName`="cloudsupport", version="v2", `discoveryServiceUrl`=f"<https://cloudsupport.googleapis.com/$`discovery/rest`?version=v2",> ) request = `supportApiService`.`caseClassifications`().search( query='display_name:"*Compute Engine*"' ) print(request.execute())
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsupport_case_classifications_search_builder()` + `cloudsupport_case_classifications_search_execute()`.
/// For task-level control, use `cloudsupport_case_classifications_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsupport_case_classifications_search(
    client: &SimpleHttpClient,
    args: &CloudsupportCaseClassificationsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchCaseClassificationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsupport_case_classifications_search_builder(
        client,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    cloudsupport_case_classifications_search_execute(builder)
}
