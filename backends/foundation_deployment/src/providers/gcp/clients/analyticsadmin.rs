//! Auto-generated API clients for gcp/analyticsadmin.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/analyticsadmin`

#![cfg(feature = "gcp/analyticsadmin")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1beta/accountSummaries
/// Returns summaries of all accounts accessible by the caller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_account_summaries_list_execute()` to send, or `analyticsadmin_account_summaries_list` for simplest API.

pub fn analyticsadmin_account_summaries_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://analyticsadmin.googleapis.com/v1beta/accountSummaries",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/accountSummaries
/// Returns summaries of all accounts accessible by the caller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_account_summaries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_account_summaries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_account_summaries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListAccountSummariesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListAccountSummariesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accountSummaries
/// Returns summaries of all accounts accessible by the caller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_account_summaries_list_builder()` + `analyticsadmin_account_summaries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_account_summaries_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListAccountSummariesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_account_summaries_list_builder(client, pageSize, pageToken)?;
    analyticsadmin_account_summaries_list_execute(builder)
}

/// GET v1beta/accounts/{accountsId}
/// Marks target Account as soft-deleted (ie: "trashed") and returns it. This API does not have a method to restore soft-deleted accounts. However, they can be restored using the Trash Can UI. If the accounts are not restored before the expiration time, the account and all child resources (eg: Properties, GoogleAdsLinks, Streams, AccessBindings) will be permanently purged. <https://support.google.`com/analytics/answer/6154772`> Returns an error if the target is not found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_delete_execute()` to send, or `analyticsadmin_accounts_delete` for simplest API.

pub fn analyticsadmin_accounts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/accounts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/accounts/{accountsId}
/// Marks target Account as soft-deleted (ie: "trashed") and returns it. This API does not have a method to restore soft-deleted accounts. However, they can be restored using the Trash Can UI. If the accounts are not restored before the expiration time, the account and all child resources (eg: Properties, GoogleAdsLinks, Streams, AccessBindings) will be permanently purged. <https://support.google.`com/analytics/answer/6154772`> Returns an error if the target is not found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}
/// Marks target Account as soft-deleted (ie: "trashed") and returns it. This API does not have a method to restore soft-deleted accounts. However, they can be restored using the Trash Can UI. If the accounts are not restored before the expiration time, the account and all child resources (eg: Properties, GoogleAdsLinks, Streams, AccessBindings) will be permanently purged. <https://support.google.`com/analytics/answer/6154772`> Returns an error if the target is not found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_delete_builder()` + `analyticsadmin_accounts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_accounts_delete_builder(client, name)?;
    analyticsadmin_accounts_delete_execute(builder)
}

/// GET v1beta/accounts/{accountsId}
/// Lookup for a single Account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_get_execute()` to send, or `analyticsadmin_accounts_get` for simplest API.

pub fn analyticsadmin_accounts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/accounts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/accounts/{accountsId}
/// Lookup for a single Account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}
/// Lookup for a single Account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_get_builder()` + `analyticsadmin_accounts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_accounts_get_builder(client, name)?;
    analyticsadmin_accounts_get_execute(builder)
}

/// GET v1beta/accounts/{accountsId}/dataSharingSettings
/// Get data sharing settings on an account. Data sharing settings are singletons.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_get_data_sharing_settings_execute()` to send, or `analyticsadmin_accounts_get_data_sharing_settings` for simplest API.

pub fn analyticsadmin_accounts_get_data_sharing_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/accounts/{}/dataSharingSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/accounts/{accountsId}/dataSharingSettings
/// Get data sharing settings on an account. Data sharing settings are singletons.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_get_data_sharing_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_get_data_sharing_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_get_data_sharing_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataSharingSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaDataSharingSettings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}/dataSharingSettings
/// Get data sharing settings on an account. Data sharing settings are singletons.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_get_data_sharing_settings_builder()` + `analyticsadmin_accounts_get_data_sharing_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_get_data_sharing_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataSharingSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_accounts_get_data_sharing_settings_builder(client, name)?;
    analyticsadmin_accounts_get_data_sharing_settings_execute(builder)
}

/// GET v1beta/accounts
/// Returns all accounts accessible by the caller. Note that these accounts might not currently have GA properties. Soft-deleted (ie: "trashed") accounts are excluded by default. Returns an empty list if no relevant accounts are found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_list_execute()` to send, or `analyticsadmin_accounts_list` for simplest API.

pub fn analyticsadmin_accounts_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://analyticsadmin.googleapis.com/v1beta/accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/accounts
/// Returns all accounts accessible by the caller. Note that these accounts might not currently have GA properties. Soft-deleted (ie: "trashed") accounts are excluded by default. Returns an empty list if no relevant accounts are found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListAccountsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts
/// Returns all accounts accessible by the caller. Note that these accounts might not currently have GA properties. Soft-deleted (ie: "trashed") accounts are excluded by default. Returns an empty list if no relevant accounts are found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_list_builder()` + `analyticsadmin_accounts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListAccountsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_accounts_list_builder(client, pageSize, pageToken, showDeleted)?;
    analyticsadmin_accounts_list_execute(builder)
}

/// GET v1beta/accounts/{accountsId}
/// Updates an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_patch_execute()` to send, or `analyticsadmin_accounts_patch` for simplest API.

pub fn analyticsadmin_accounts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaAccount,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/accounts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}
/// Updates an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaAccount = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}
/// Updates an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_patch_builder()` + `analyticsadmin_accounts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaAccount,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaAccount>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_accounts_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_accounts_patch_execute(builder)
}

/// GET v1beta/accounts:provisionAccountTicket
/// Requests a ticket for creating an account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_provision_account_ticket_execute()` to send, or `analyticsadmin_accounts_provision_account_ticket` for simplest API.

pub fn analyticsadmin_accounts_provision_account_ticket_builder(
    client: &SimpleHttpClient,
    body: &GoogleAnalyticsAdminV1betaProvisionAccountTicketRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://analyticsadmin.googleapis.com/v1beta/accounts:provisionAccountTicket",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts:provisionAccountTicket
/// Requests a ticket for creating an account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_provision_account_ticket_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_provision_account_ticket_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_provision_account_ticket_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaProvisionAccountTicketResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaProvisionAccountTicketResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts:provisionAccountTicket
/// Requests a ticket for creating an account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_provision_account_ticket_builder()` + `analyticsadmin_accounts_provision_account_ticket_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_provision_account_ticket(
    client: &SimpleHttpClient,
    body: &GoogleAnalyticsAdminV1betaProvisionAccountTicketRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaProvisionAccountTicketResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_accounts_provision_account_ticket_builder(client, body)?;
    analyticsadmin_accounts_provision_account_ticket_execute(builder)
}

/// GET v1beta/accounts/{accountsId}:runAccessReport
/// Returns a customized report of data access records. The report provides records of each time a user reads Google Analytics reporting data. Access records are retained for up to 2 years. Data Access Reports can be requested for a property. Reports may be requested for any property, but dimensions that aren't related to quota can only be requested on Google Analytics 360 properties. This method is only available to Administrators. These data access records include GA UI Reporting, GA UI Explorations, GA Data API, and other products like Firebase & Admob that can retrieve data from Google Analytics through a linkage. These records don't include property configuration changes like adding a stream or changing a property's time zone. For configuration change history, see [`searchChangeHistoryEvents`](<https://developers.google.`com/analytics/devguides/config/admin/v1/rest/v1alpha/accounts/`searchChangeHistoryEvents``>). To give your feedback on this API, complete the [Google Analytics Access Reports feedback](<https://docs.google.`com/forms/d/e/1FAIpQLSdmEBUrMzAEdiEKk5TV5dEHvDUZDRlgWYdQdAeSdtR4hVjEhw/viewform`>) form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_run_access_report_execute()` to send, or `analyticsadmin_accounts_run_access_report` for simplest API.

pub fn analyticsadmin_accounts_run_access_report_builder(
    client: &SimpleHttpClient,
    entity: &str,
    body: &GoogleAnalyticsAdminV1betaRunAccessReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/accounts/{}:runAccessReport",
        entity,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}:runAccessReport
/// Returns a customized report of data access records. The report provides records of each time a user reads Google Analytics reporting data. Access records are retained for up to 2 years. Data Access Reports can be requested for a property. Reports may be requested for any property, but dimensions that aren't related to quota can only be requested on Google Analytics 360 properties. This method is only available to Administrators. These data access records include GA UI Reporting, GA UI Explorations, GA Data API, and other products like Firebase & Admob that can retrieve data from Google Analytics through a linkage. These records don't include property configuration changes like adding a stream or changing a property's time zone. For configuration change history, see [`searchChangeHistoryEvents`](<https://developers.google.`com/analytics/devguides/config/admin/v1/rest/v1alpha/accounts/`searchChangeHistoryEvents``>). To give your feedback on this API, complete the [Google Analytics Access Reports feedback](<https://docs.google.`com/forms/d/e/1FAIpQLSdmEBUrMzAEdiEKk5TV5dEHvDUZDRlgWYdQdAeSdtR4hVjEhw/viewform`>) form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_run_access_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_run_access_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_run_access_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaRunAccessReportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaRunAccessReportResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}:runAccessReport
/// Returns a customized report of data access records. The report provides records of each time a user reads Google Analytics reporting data. Access records are retained for up to 2 years. Data Access Reports can be requested for a property. Reports may be requested for any property, but dimensions that aren't related to quota can only be requested on Google Analytics 360 properties. This method is only available to Administrators. These data access records include GA UI Reporting, GA UI Explorations, GA Data API, and other products like Firebase & Admob that can retrieve data from Google Analytics through a linkage. These records don't include property configuration changes like adding a stream or changing a property's time zone. For configuration change history, see [`searchChangeHistoryEvents`](<https://developers.google.`com/analytics/devguides/config/admin/v1/rest/v1alpha/accounts/`searchChangeHistoryEvents``>). To give your feedback on this API, complete the [Google Analytics Access Reports feedback](<https://docs.google.`com/forms/d/e/1FAIpQLSdmEBUrMzAEdiEKk5TV5dEHvDUZDRlgWYdQdAeSdtR4hVjEhw/viewform`>) form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_run_access_report_builder()` + `analyticsadmin_accounts_run_access_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_run_access_report(
    client: &SimpleHttpClient,
    entity: &str,
    body: &GoogleAnalyticsAdminV1betaRunAccessReportRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaRunAccessReportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_accounts_run_access_report_builder(client, entity, body)?;
    analyticsadmin_accounts_run_access_report_execute(builder)
}

/// GET v1beta/accounts/{accountsId}:searchChangeHistoryEvents
/// Searches through all changes to an account or its children given the specified set of filters. Only returns the subset of changes supported by the API. The UI may return additional changes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_accounts_search_change_history_events_execute()` to send, or `analyticsadmin_accounts_search_change_history_events` for simplest API.

pub fn analyticsadmin_accounts_search_change_history_events_builder(
    client: &SimpleHttpClient,
    account: &str,
    body: &GoogleAnalyticsAdminV1betaSearchChangeHistoryEventsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/accounts/{}:searchChangeHistoryEvents",
        account,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}:searchChangeHistoryEvents
/// Searches through all changes to an account or its children given the specified set of filters. Only returns the subset of changes supported by the API. The UI may return additional changes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_accounts_search_change_history_events_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_accounts_search_change_history_events_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_search_change_history_events_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaSearchChangeHistoryEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaSearchChangeHistoryEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/accounts/{accountsId}:searchChangeHistoryEvents
/// Searches through all changes to an account or its children given the specified set of filters. Only returns the subset of changes supported by the API. The UI may return additional changes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_accounts_search_change_history_events_builder()` + `analyticsadmin_accounts_search_change_history_events_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_accounts_search_change_history_events(
    client: &SimpleHttpClient,
    account: &str,
    body: &GoogleAnalyticsAdminV1betaSearchChangeHistoryEventsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaSearchChangeHistoryEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_accounts_search_change_history_events_builder(client, account, body)?;
    analyticsadmin_accounts_search_change_history_events_execute(builder)
}

/// GET v1beta/properties/{propertiesId}:acknowledgeUserDataCollection
/// Acknowledges the terms of user data collection for the specified property. This acknowledgement must be completed (either in the Google Analytics UI or through this API) before MeasurementProtocolSecret resources may be created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_acknowledge_user_data_collection_execute()` to send, or `analyticsadmin_properties_acknowledge_user_data_collection` for simplest API.

pub fn analyticsadmin_properties_acknowledge_user_data_collection_builder(
    client: &SimpleHttpClient,
    property: &str,
    body: &GoogleAnalyticsAdminV1betaAcknowledgeUserDataCollectionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}:acknowledgeUserDataCollection",
        property,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:acknowledgeUserDataCollection
/// Acknowledges the terms of user data collection for the specified property. This acknowledgement must be completed (either in the Google Analytics UI or through this API) before MeasurementProtocolSecret resources may be created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_acknowledge_user_data_collection_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_acknowledge_user_data_collection_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_acknowledge_user_data_collection_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaAcknowledgeUserDataCollectionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaAcknowledgeUserDataCollectionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:acknowledgeUserDataCollection
/// Acknowledges the terms of user data collection for the specified property. This acknowledgement must be completed (either in the Google Analytics UI or through this API) before MeasurementProtocolSecret resources may be created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_acknowledge_user_data_collection_builder()` + `analyticsadmin_properties_acknowledge_user_data_collection_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_acknowledge_user_data_collection(
    client: &SimpleHttpClient,
    property: &str,
    body: &GoogleAnalyticsAdminV1betaAcknowledgeUserDataCollectionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaAcknowledgeUserDataCollectionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_acknowledge_user_data_collection_builder(client, property, body)?;
    analyticsadmin_properties_acknowledge_user_data_collection_execute(builder)
}

/// GET v1beta/properties
/// Creates a Google Analytics property with the specified location and attributes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_create_execute()` to send, or `analyticsadmin_properties_create` for simplest API.

pub fn analyticsadmin_properties_create_builder(
    client: &SimpleHttpClient,
    body: &GoogleAnalyticsAdminV1betaProperty,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://analyticsadmin.googleapis.com/v1beta/properties",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties
/// Creates a Google Analytics property with the specified location and attributes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaProperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties
/// Creates a Google Analytics property with the specified location and attributes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_create_builder()` + `analyticsadmin_properties_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_create(
    client: &SimpleHttpClient,
    body: &GoogleAnalyticsAdminV1betaProperty,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_create_builder(client, body)?;
    analyticsadmin_properties_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}
/// Marks target Property as soft-deleted (ie: "trashed") and returns it. This API does not have a method to restore soft-deleted properties. However, they can be restored using the Trash Can UI. If the properties are not restored before the expiration time, the Property and all child resources (eg: GoogleAdsLinks, Streams, AccessBindings) will be permanently purged. <https://support.google.`com/analytics/answer/6154772`> Returns an error if the target is not found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_delete_execute()` to send, or `analyticsadmin_properties_delete` for simplest API.

pub fn analyticsadmin_properties_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}
/// Marks target Property as soft-deleted (ie: "trashed") and returns it. This API does not have a method to restore soft-deleted properties. However, they can be restored using the Trash Can UI. If the properties are not restored before the expiration time, the Property and all child resources (eg: GoogleAdsLinks, Streams, AccessBindings) will be permanently purged. <https://support.google.`com/analytics/answer/6154772`> Returns an error if the target is not found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaProperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}
/// Marks target Property as soft-deleted (ie: "trashed") and returns it. This API does not have a method to restore soft-deleted properties. However, they can be restored using the Trash Can UI. If the properties are not restored before the expiration time, the Property and all child resources (eg: GoogleAdsLinks, Streams, AccessBindings) will be permanently purged. <https://support.google.`com/analytics/answer/6154772`> Returns an error if the target is not found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_delete_builder()` + `analyticsadmin_properties_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_delete_builder(client, name)?;
    analyticsadmin_properties_delete_execute(builder)
}

/// GET v1beta/properties/{propertiesId}
/// Lookup for a single GA Property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_get_execute()` to send, or `analyticsadmin_properties_get` for simplest API.

pub fn analyticsadmin_properties_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}
/// Lookup for a single GA Property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaProperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}
/// Lookup for a single GA Property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_get_builder()` + `analyticsadmin_properties_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_get_builder(client, name)?;
    analyticsadmin_properties_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataRetentionSettings
/// Returns the singleton data retention settings for this property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_get_data_retention_settings_execute()` to send, or `analyticsadmin_properties_get_data_retention_settings` for simplest API.

pub fn analyticsadmin_properties_get_data_retention_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataRetentionSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/dataRetentionSettings
/// Returns the singleton data retention settings for this property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_get_data_retention_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_get_data_retention_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_get_data_retention_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataRetentionSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaDataRetentionSettings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataRetentionSettings
/// Returns the singleton data retention settings for this property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_get_data_retention_settings_builder()` + `analyticsadmin_properties_get_data_retention_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_get_data_retention_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataRetentionSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_get_data_retention_settings_builder(client, name)?;
    analyticsadmin_properties_get_data_retention_settings_execute(builder)
}

/// GET v1beta/properties
/// Returns child Properties under the specified parent Account. Properties will be excluded if the caller does not have access. Soft-deleted (ie: "trashed") properties are excluded by default. Returns an empty list if no relevant properties are found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_list_execute()` to send, or `analyticsadmin_properties_list` for simplest API.

pub fn analyticsadmin_properties_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://analyticsadmin.googleapis.com/v1beta/properties",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties
/// Returns child Properties under the specified parent Account. Properties will be excluded if the caller does not have access. Soft-deleted (ie: "trashed") properties are excluded by default. Returns an empty list if no relevant properties are found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListPropertiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListPropertiesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties
/// Returns child Properties under the specified parent Account. Properties will be excluded if the caller does not have access. Soft-deleted (ie: "trashed") properties are excluded by default. Returns an empty list if no relevant properties are found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_list_builder()` + `analyticsadmin_properties_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showDeleted: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListPropertiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_list_builder(client, filter, pageSize, pageToken, showDeleted)?;
    analyticsadmin_properties_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}
/// Updates a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_patch_execute()` to send, or `analyticsadmin_properties_patch` for simplest API.

pub fn analyticsadmin_properties_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaProperty,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}
/// Updates a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaProperty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}
/// Updates a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_patch_builder()` + `analyticsadmin_properties_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaProperty,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaProperty>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_properties_patch_execute(builder)
}

/// GET v1beta/properties/{propertiesId}:runAccessReport
/// Returns a customized report of data access records. The report provides records of each time a user reads Google Analytics reporting data. Access records are retained for up to 2 years. Data Access Reports can be requested for a property. Reports may be requested for any property, but dimensions that aren't related to quota can only be requested on Google Analytics 360 properties. This method is only available to Administrators. These data access records include GA UI Reporting, GA UI Explorations, GA Data API, and other products like Firebase & Admob that can retrieve data from Google Analytics through a linkage. These records don't include property configuration changes like adding a stream or changing a property's time zone. For configuration change history, see [`searchChangeHistoryEvents`](<https://developers.google.`com/analytics/devguides/config/admin/v1/rest/v1alpha/accounts/`searchChangeHistoryEvents``>). To give your feedback on this API, complete the [Google Analytics Access Reports feedback](<https://docs.google.`com/forms/d/e/1FAIpQLSdmEBUrMzAEdiEKk5TV5dEHvDUZDRlgWYdQdAeSdtR4hVjEhw/viewform`>) form.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_run_access_report_execute()` to send, or `analyticsadmin_properties_run_access_report` for simplest API.

pub fn analyticsadmin_properties_run_access_report_builder(
    client: &SimpleHttpClient,
    entity: &str,
    body: &GoogleAnalyticsAdminV1betaRunAccessReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}:runAccessReport",
        entity,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:runAccessReport
/// Returns a customized report of data access records. The report provides records of each time a user reads Google Analytics reporting data. Access records are retained for up to 2 years. Data Access Reports can be requested for a property. Reports may be requested for any property, but dimensions that aren't related to quota can only be requested on Google Analytics 360 properties. This method is only available to Administrators. These data access records include GA UI Reporting, GA UI Explorations, GA Data API, and other products like Firebase & Admob that can retrieve data from Google Analytics through a linkage. These records don't include property configuration changes like adding a stream or changing a property's time zone. For configuration change history, see [`searchChangeHistoryEvents`](<https://developers.google.`com/analytics/devguides/config/admin/v1/rest/v1alpha/accounts/`searchChangeHistoryEvents``>). To give your feedback on this API, complete the [Google Analytics Access Reports feedback](<https://docs.google.`com/forms/d/e/1FAIpQLSdmEBUrMzAEdiEKk5TV5dEHvDUZDRlgWYdQdAeSdtR4hVjEhw/viewform`>) form.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_run_access_report_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_run_access_report_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_run_access_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaRunAccessReportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaRunAccessReportResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}:runAccessReport
/// Returns a customized report of data access records. The report provides records of each time a user reads Google Analytics reporting data. Access records are retained for up to 2 years. Data Access Reports can be requested for a property. Reports may be requested for any property, but dimensions that aren't related to quota can only be requested on Google Analytics 360 properties. This method is only available to Administrators. These data access records include GA UI Reporting, GA UI Explorations, GA Data API, and other products like Firebase & Admob that can retrieve data from Google Analytics through a linkage. These records don't include property configuration changes like adding a stream or changing a property's time zone. For configuration change history, see [`searchChangeHistoryEvents`](<https://developers.google.`com/analytics/devguides/config/admin/v1/rest/v1alpha/accounts/`searchChangeHistoryEvents``>). To give your feedback on this API, complete the [Google Analytics Access Reports feedback](<https://docs.google.`com/forms/d/e/1FAIpQLSdmEBUrMzAEdiEKk5TV5dEHvDUZDRlgWYdQdAeSdtR4hVjEhw/viewform`>) form.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_run_access_report_builder()` + `analyticsadmin_properties_run_access_report_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_run_access_report(
    client: &SimpleHttpClient,
    entity: &str,
    body: &GoogleAnalyticsAdminV1betaRunAccessReportRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaRunAccessReportResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_run_access_report_builder(client, entity, body)?;
    analyticsadmin_properties_run_access_report_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataRetentionSettings
/// Updates the singleton data retention settings for this property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_update_data_retention_settings_execute()` to send, or `analyticsadmin_properties_update_data_retention_settings` for simplest API.

pub fn analyticsadmin_properties_update_data_retention_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaDataRetentionSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataRetentionSettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataRetentionSettings
/// Updates the singleton data retention settings for this property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_update_data_retention_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_update_data_retention_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_update_data_retention_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataRetentionSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaDataRetentionSettings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataRetentionSettings
/// Updates the singleton data retention settings for this property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_update_data_retention_settings_builder()` + `analyticsadmin_properties_update_data_retention_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_update_data_retention_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaDataRetentionSettings,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataRetentionSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_update_data_retention_settings_builder(
        client, name, updateMask, body,
    )?;
    analyticsadmin_properties_update_data_retention_settings_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents
/// Deprecated: Use CreateKeyEvent instead. Creates a conversion event with the specified attributes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_conversion_events_create_execute()` to send, or `analyticsadmin_properties_conversion_events_create` for simplest API.

pub fn analyticsadmin_properties_conversion_events_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaConversionEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/conversionEvents",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/conversionEvents
/// Deprecated: Use CreateKeyEvent instead. Creates a conversion event with the specified attributes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_conversion_events_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_conversion_events_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaConversionEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaConversionEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/conversionEvents
/// Deprecated: Use CreateKeyEvent instead. Creates a conversion event with the specified attributes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_conversion_events_create_builder()` + `analyticsadmin_properties_conversion_events_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaConversionEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaConversionEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_conversion_events_create_builder(client, parent, body)?;
    analyticsadmin_properties_conversion_events_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use DeleteKeyEvent instead. Deletes a conversion event in a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_conversion_events_delete_execute()` to send, or `analyticsadmin_properties_conversion_events_delete` for simplest API.

pub fn analyticsadmin_properties_conversion_events_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/conversionEvents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use DeleteKeyEvent instead. Deletes a conversion event in a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_conversion_events_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_conversion_events_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use DeleteKeyEvent instead. Deletes a conversion event in a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_conversion_events_delete_builder()` + `analyticsadmin_properties_conversion_events_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_conversion_events_delete_builder(client, name)?;
    analyticsadmin_properties_conversion_events_delete_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use GetKeyEvent instead. Retrieve a single conversion event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_conversion_events_get_execute()` to send, or `analyticsadmin_properties_conversion_events_get` for simplest API.

pub fn analyticsadmin_properties_conversion_events_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/conversionEvents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use GetKeyEvent instead. Retrieve a single conversion event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_conversion_events_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_conversion_events_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaConversionEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaConversionEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use GetKeyEvent instead. Retrieve a single conversion event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_conversion_events_get_builder()` + `analyticsadmin_properties_conversion_events_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaConversionEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_conversion_events_get_builder(client, name)?;
    analyticsadmin_properties_conversion_events_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents
/// Deprecated: Use ListKeyEvents instead. Returns a list of conversion events in the specified parent property. Returns an empty list if no conversion events are found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_conversion_events_list_execute()` to send, or `analyticsadmin_properties_conversion_events_list` for simplest API.

pub fn analyticsadmin_properties_conversion_events_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/conversionEvents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents
/// Deprecated: Use ListKeyEvents instead. Returns a list of conversion events in the specified parent property. Returns an empty list if no conversion events are found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_conversion_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_conversion_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListConversionEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListConversionEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/conversionEvents
/// Deprecated: Use ListKeyEvents instead. Returns a list of conversion events in the specified parent property. Returns an empty list if no conversion events are found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_conversion_events_list_builder()` + `analyticsadmin_properties_conversion_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListConversionEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_conversion_events_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    analyticsadmin_properties_conversion_events_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use UpdateKeyEvent instead. Updates a conversion event with the specified attributes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_conversion_events_patch_execute()` to send, or `analyticsadmin_properties_conversion_events_patch` for simplest API.

pub fn analyticsadmin_properties_conversion_events_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaConversionEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/conversionEvents/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use UpdateKeyEvent instead. Updates a conversion event with the specified attributes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_conversion_events_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_conversion_events_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaConversionEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaConversionEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/conversionEvents/{conversionEventsId}
/// Deprecated: Use UpdateKeyEvent instead. Updates a conversion event with the specified attributes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_conversion_events_patch_builder()` + `analyticsadmin_properties_conversion_events_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_conversion_events_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaConversionEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaConversionEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_conversion_events_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_properties_conversion_events_patch_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}:archive
/// Archives a CustomDimension on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_dimensions_archive_execute()` to send, or `analyticsadmin_properties_custom_dimensions_archive` for simplest API.

pub fn analyticsadmin_properties_custom_dimensions_archive_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAnalyticsAdminV1betaArchiveCustomDimensionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customDimensions/{}:archive",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}:archive
/// Archives a CustomDimension on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_dimensions_archive_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_dimensions_archive_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_archive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}:archive
/// Archives a CustomDimension on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_dimensions_archive_builder()` + `analyticsadmin_properties_custom_dimensions_archive_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_archive(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAnalyticsAdminV1betaArchiveCustomDimensionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_custom_dimensions_archive_builder(client, name, body)?;
    analyticsadmin_properties_custom_dimensions_archive_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customDimensions
/// Creates a CustomDimension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_dimensions_create_execute()` to send, or `analyticsadmin_properties_custom_dimensions_create` for simplest API.

pub fn analyticsadmin_properties_custom_dimensions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaCustomDimension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customDimensions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions
/// Creates a CustomDimension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_dimensions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_dimensions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomDimension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaCustomDimension = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions
/// Creates a CustomDimension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_dimensions_create_builder()` + `analyticsadmin_properties_custom_dimensions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaCustomDimension,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomDimension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_custom_dimensions_create_builder(client, parent, body)?;
    analyticsadmin_properties_custom_dimensions_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}
/// Lookup for a single CustomDimension.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_dimensions_get_execute()` to send, or `analyticsadmin_properties_custom_dimensions_get` for simplest API.

pub fn analyticsadmin_properties_custom_dimensions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customDimensions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}
/// Lookup for a single CustomDimension.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_dimensions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_dimensions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomDimension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaCustomDimension = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}
/// Lookup for a single CustomDimension.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_dimensions_get_builder()` + `analyticsadmin_properties_custom_dimensions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomDimension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_custom_dimensions_get_builder(client, name)?;
    analyticsadmin_properties_custom_dimensions_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customDimensions
/// Lists CustomDimensions on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_dimensions_list_execute()` to send, or `analyticsadmin_properties_custom_dimensions_list` for simplest API.

pub fn analyticsadmin_properties_custom_dimensions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customDimensions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/customDimensions
/// Lists CustomDimensions on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_dimensions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_dimensions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListCustomDimensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListCustomDimensionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions
/// Lists CustomDimensions on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_dimensions_list_builder()` + `analyticsadmin_properties_custom_dimensions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListCustomDimensionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_custom_dimensions_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    analyticsadmin_properties_custom_dimensions_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}
/// Updates a CustomDimension on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_dimensions_patch_execute()` to send, or `analyticsadmin_properties_custom_dimensions_patch` for simplest API.

pub fn analyticsadmin_properties_custom_dimensions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaCustomDimension,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customDimensions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}
/// Updates a CustomDimension on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_dimensions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_dimensions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomDimension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaCustomDimension = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customDimensions/{customDimensionsId}
/// Updates a CustomDimension on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_dimensions_patch_builder()` + `analyticsadmin_properties_custom_dimensions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_dimensions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaCustomDimension,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomDimension>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_custom_dimensions_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_properties_custom_dimensions_patch_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}:archive
/// Archives a CustomMetric on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_metrics_archive_execute()` to send, or `analyticsadmin_properties_custom_metrics_archive` for simplest API.

pub fn analyticsadmin_properties_custom_metrics_archive_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAnalyticsAdminV1betaArchiveCustomMetricRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customMetrics/{}:archive",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}:archive
/// Archives a CustomMetric on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_metrics_archive_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_metrics_archive_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_archive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}:archive
/// Archives a CustomMetric on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_metrics_archive_builder()` + `analyticsadmin_properties_custom_metrics_archive_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_archive(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAnalyticsAdminV1betaArchiveCustomMetricRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_custom_metrics_archive_builder(client, name, body)?;
    analyticsadmin_properties_custom_metrics_archive_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customMetrics
/// Creates a CustomMetric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_metrics_create_execute()` to send, or `analyticsadmin_properties_custom_metrics_create` for simplest API.

pub fn analyticsadmin_properties_custom_metrics_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaCustomMetric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customMetrics",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics
/// Creates a CustomMetric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_metrics_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_metrics_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomMetric>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaCustomMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics
/// Creates a CustomMetric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_metrics_create_builder()` + `analyticsadmin_properties_custom_metrics_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaCustomMetric,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomMetric>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_custom_metrics_create_builder(client, parent, body)?;
    analyticsadmin_properties_custom_metrics_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}
/// Lookup for a single CustomMetric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_metrics_get_execute()` to send, or `analyticsadmin_properties_custom_metrics_get` for simplest API.

pub fn analyticsadmin_properties_custom_metrics_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customMetrics/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}
/// Lookup for a single CustomMetric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_metrics_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_metrics_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomMetric>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaCustomMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}
/// Lookup for a single CustomMetric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_metrics_get_builder()` + `analyticsadmin_properties_custom_metrics_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomMetric>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_custom_metrics_get_builder(client, name)?;
    analyticsadmin_properties_custom_metrics_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customMetrics
/// Lists CustomMetrics on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_metrics_list_execute()` to send, or `analyticsadmin_properties_custom_metrics_list` for simplest API.

pub fn analyticsadmin_properties_custom_metrics_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customMetrics",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/customMetrics
/// Lists CustomMetrics on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListCustomMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListCustomMetricsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics
/// Lists CustomMetrics on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_metrics_list_builder()` + `analyticsadmin_properties_custom_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListCustomMetricsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_custom_metrics_list_builder(client, parent, pageSize, pageToken)?;
    analyticsadmin_properties_custom_metrics_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}
/// Updates a CustomMetric on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_custom_metrics_patch_execute()` to send, or `analyticsadmin_properties_custom_metrics_patch` for simplest API.

pub fn analyticsadmin_properties_custom_metrics_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaCustomMetric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/customMetrics/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}
/// Updates a CustomMetric on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_custom_metrics_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_custom_metrics_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomMetric>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaCustomMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/customMetrics/{customMetricsId}
/// Updates a CustomMetric on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_custom_metrics_patch_builder()` + `analyticsadmin_properties_custom_metrics_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_custom_metrics_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaCustomMetric,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaCustomMetric>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_custom_metrics_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_properties_custom_metrics_patch_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams
/// Creates a DataStream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_create_execute()` to send, or `analyticsadmin_properties_data_streams_create` for simplest API.

pub fn analyticsadmin_properties_data_streams_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaDataStream,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams
/// Creates a DataStream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataStream>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaDataStream = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams
/// Creates a DataStream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_create_builder()` + `analyticsadmin_properties_data_streams_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaDataStream,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataStream>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_data_streams_create_builder(client, parent, body)?;
    analyticsadmin_properties_data_streams_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Deletes a DataStream on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_delete_execute()` to send, or `analyticsadmin_properties_data_streams_delete` for simplest API.

pub fn analyticsadmin_properties_data_streams_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Deletes a DataStream on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Deletes a DataStream on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_delete_builder()` + `analyticsadmin_properties_data_streams_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_data_streams_delete_builder(client, name)?;
    analyticsadmin_properties_data_streams_delete_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Lookup for a single DataStream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_get_execute()` to send, or `analyticsadmin_properties_data_streams_get` for simplest API.

pub fn analyticsadmin_properties_data_streams_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Lookup for a single DataStream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataStream>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaDataStream = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Lookup for a single DataStream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_get_builder()` + `analyticsadmin_properties_data_streams_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataStream>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_data_streams_get_builder(client, name)?;
    analyticsadmin_properties_data_streams_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams
/// Lists DataStreams on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_list_execute()` to send, or `analyticsadmin_properties_data_streams_list` for simplest API.

pub fn analyticsadmin_properties_data_streams_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams
/// Lists DataStreams on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListDataStreamsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListDataStreamsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams
/// Lists DataStreams on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_list_builder()` + `analyticsadmin_properties_data_streams_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListDataStreamsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_data_streams_list_builder(client, parent, pageSize, pageToken)?;
    analyticsadmin_properties_data_streams_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Updates a DataStream on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_patch_execute()` to send, or `analyticsadmin_properties_data_streams_patch` for simplest API.

pub fn analyticsadmin_properties_data_streams_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaDataStream,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Updates a DataStream on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataStream>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaDataStream = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}
/// Updates a DataStream on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_patch_builder()` + `analyticsadmin_properties_data_streams_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaDataStream,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaDataStream>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_data_streams_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_properties_data_streams_patch_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets
/// Creates a measurement protocol secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_execute()` to send, or `analyticsadmin_properties_data_streams_measurement_protocol_secrets_create` for simplest API.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaMeasurementProtocolSecret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}/measurementProtocolSecrets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets
/// Creates a measurement protocol secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaMeasurementProtocolSecret>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaMeasurementProtocolSecret =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets
/// Creates a measurement protocol secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_builder()` + `analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaMeasurementProtocolSecret,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaMeasurementProtocolSecret>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_builder(
            client, parent, body,
        )?;
    analyticsadmin_properties_data_streams_measurement_protocol_secrets_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Deletes target MeasurementProtocolSecret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_execute()` to send, or `analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete` for simplest API.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}/measurementProtocolSecrets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Deletes target MeasurementProtocolSecret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Deletes target MeasurementProtocolSecret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_builder()` + `analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_builder(
            client, name,
        )?;
    analyticsadmin_properties_data_streams_measurement_protocol_secrets_delete_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Lookup for a single MeasurementProtocolSecret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_execute()` to send, or `analyticsadmin_properties_data_streams_measurement_protocol_secrets_get` for simplest API.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}/measurementProtocolSecrets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Lookup for a single MeasurementProtocolSecret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaMeasurementProtocolSecret>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaMeasurementProtocolSecret =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Lookup for a single MeasurementProtocolSecret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_builder()` + `analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaMeasurementProtocolSecret>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_builder(
        client, name,
    )?;
    analyticsadmin_properties_data_streams_measurement_protocol_secrets_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets
/// Returns child MeasurementProtocolSecrets under the specified parent Property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_execute()` to send, or `analyticsadmin_properties_data_streams_measurement_protocol_secrets_list` for simplest API.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}/measurementProtocolSecrets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets
/// Returns child MeasurementProtocolSecrets under the specified parent Property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListMeasurementProtocolSecretsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListMeasurementProtocolSecretsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets
/// Returns child MeasurementProtocolSecrets under the specified parent Property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_builder()` + `analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAnalyticsAdminV1betaListMeasurementProtocolSecretsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    analyticsadmin_properties_data_streams_measurement_protocol_secrets_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Updates a measurement protocol secret.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_execute()` to send, or `analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch` for simplest API.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaMeasurementProtocolSecret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/dataStreams/{}/measurementProtocolSecrets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Updates a measurement protocol secret.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaMeasurementProtocolSecret>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaMeasurementProtocolSecret =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/dataStreams/{dataStreamsId}/measurementProtocolSecrets/{measurementProtocolSecretsId}
/// Updates a measurement protocol secret.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_builder()` + `analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaMeasurementProtocolSecret,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaMeasurementProtocolSecret>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_builder(
            client, name, updateMask, body,
        )?;
    analyticsadmin_properties_data_streams_measurement_protocol_secrets_patch_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks
/// Creates a FirebaseLink. Properties can have at most one FirebaseLink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_firebase_links_create_execute()` to send, or `analyticsadmin_properties_firebase_links_create` for simplest API.

pub fn analyticsadmin_properties_firebase_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaFirebaseLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/firebaseLinks",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks
/// Creates a FirebaseLink. Properties can have at most one FirebaseLink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_firebase_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_firebase_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_firebase_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaFirebaseLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaFirebaseLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks
/// Creates a FirebaseLink. Properties can have at most one FirebaseLink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_firebase_links_create_builder()` + `analyticsadmin_properties_firebase_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_firebase_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaFirebaseLink,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaFirebaseLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_firebase_links_create_builder(client, parent, body)?;
    analyticsadmin_properties_firebase_links_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks/{firebaseLinksId}
/// Deletes a FirebaseLink on a property
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_firebase_links_delete_execute()` to send, or `analyticsadmin_properties_firebase_links_delete` for simplest API.

pub fn analyticsadmin_properties_firebase_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/firebaseLinks/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks/{firebaseLinksId}
/// Deletes a FirebaseLink on a property
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_firebase_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_firebase_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_firebase_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks/{firebaseLinksId}
/// Deletes a FirebaseLink on a property
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_firebase_links_delete_builder()` + `analyticsadmin_properties_firebase_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_firebase_links_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_firebase_links_delete_builder(client, name)?;
    analyticsadmin_properties_firebase_links_delete_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks
/// Lists FirebaseLinks on a property. Properties can have at most one FirebaseLink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_firebase_links_list_execute()` to send, or `analyticsadmin_properties_firebase_links_list` for simplest API.

pub fn analyticsadmin_properties_firebase_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/firebaseLinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks
/// Lists FirebaseLinks on a property. Properties can have at most one FirebaseLink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_firebase_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_firebase_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_firebase_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListFirebaseLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListFirebaseLinksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/firebaseLinks
/// Lists FirebaseLinks on a property. Properties can have at most one FirebaseLink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_firebase_links_list_builder()` + `analyticsadmin_properties_firebase_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_firebase_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListFirebaseLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_firebase_links_list_builder(client, parent, pageSize, pageToken)?;
    analyticsadmin_properties_firebase_links_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks
/// Creates a GoogleAdsLink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_google_ads_links_create_execute()` to send, or `analyticsadmin_properties_google_ads_links_create` for simplest API.

pub fn analyticsadmin_properties_google_ads_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaGoogleAdsLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/googleAdsLinks",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks
/// Creates a GoogleAdsLink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_google_ads_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_google_ads_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaGoogleAdsLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaGoogleAdsLink =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks
/// Creates a GoogleAdsLink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_google_ads_links_create_builder()` + `analyticsadmin_properties_google_ads_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaGoogleAdsLink,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaGoogleAdsLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_google_ads_links_create_builder(client, parent, body)?;
    analyticsadmin_properties_google_ads_links_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks/{googleAdsLinksId}
/// Deletes a GoogleAdsLink on a property
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_google_ads_links_delete_execute()` to send, or `analyticsadmin_properties_google_ads_links_delete` for simplest API.

pub fn analyticsadmin_properties_google_ads_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/googleAdsLinks/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks/{googleAdsLinksId}
/// Deletes a GoogleAdsLink on a property
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_google_ads_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_google_ads_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks/{googleAdsLinksId}
/// Deletes a GoogleAdsLink on a property
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_google_ads_links_delete_builder()` + `analyticsadmin_properties_google_ads_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_google_ads_links_delete_builder(client, name)?;
    analyticsadmin_properties_google_ads_links_delete_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks
/// Lists GoogleAdsLinks on a property.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_google_ads_links_list_execute()` to send, or `analyticsadmin_properties_google_ads_links_list` for simplest API.

pub fn analyticsadmin_properties_google_ads_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/googleAdsLinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks
/// Lists GoogleAdsLinks on a property.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_google_ads_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_google_ads_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListGoogleAdsLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListGoogleAdsLinksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks
/// Lists GoogleAdsLinks on a property.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_google_ads_links_list_builder()` + `analyticsadmin_properties_google_ads_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListGoogleAdsLinksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_google_ads_links_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    analyticsadmin_properties_google_ads_links_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks/{googleAdsLinksId}
/// Updates a GoogleAdsLink on a property
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_google_ads_links_patch_execute()` to send, or `analyticsadmin_properties_google_ads_links_patch` for simplest API.

pub fn analyticsadmin_properties_google_ads_links_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaGoogleAdsLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/googleAdsLinks/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks/{googleAdsLinksId}
/// Updates a GoogleAdsLink on a property
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_google_ads_links_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_google_ads_links_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaGoogleAdsLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaGoogleAdsLink =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/googleAdsLinks/{googleAdsLinksId}
/// Updates a GoogleAdsLink on a property
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_google_ads_links_patch_builder()` + `analyticsadmin_properties_google_ads_links_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_google_ads_links_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaGoogleAdsLink,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaGoogleAdsLink>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_google_ads_links_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_properties_google_ads_links_patch_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents
/// Creates a Key Event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_key_events_create_execute()` to send, or `analyticsadmin_properties_key_events_create` for simplest API.

pub fn analyticsadmin_properties_key_events_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaKeyEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/keyEvents",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/keyEvents
/// Creates a Key Event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_key_events_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_key_events_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaKeyEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaKeyEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/keyEvents
/// Creates a Key Event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_key_events_create_builder()` + `analyticsadmin_properties_key_events_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAnalyticsAdminV1betaKeyEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaKeyEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_key_events_create_builder(client, parent, body)?;
    analyticsadmin_properties_key_events_create_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Deletes a Key Event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_key_events_delete_execute()` to send, or `analyticsadmin_properties_key_events_delete` for simplest API.

pub fn analyticsadmin_properties_key_events_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/keyEvents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Deletes a Key Event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_key_events_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_key_events_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Deletes a Key Event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_key_events_delete_builder()` + `analyticsadmin_properties_key_events_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_key_events_delete_builder(client, name)?;
    analyticsadmin_properties_key_events_delete_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Retrieve a single Key Event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_key_events_get_execute()` to send, or `analyticsadmin_properties_key_events_get` for simplest API.

pub fn analyticsadmin_properties_key_events_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/keyEvents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Retrieve a single Key Event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_key_events_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_key_events_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaKeyEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaKeyEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Retrieve a single Key Event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_key_events_get_builder()` + `analyticsadmin_properties_key_events_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaKeyEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = analyticsadmin_properties_key_events_get_builder(client, name)?;
    analyticsadmin_properties_key_events_get_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents
/// Returns a list of Key Events in the specified parent property. Returns an empty list if no Key Events are found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_key_events_list_execute()` to send, or `analyticsadmin_properties_key_events_list` for simplest API.

pub fn analyticsadmin_properties_key_events_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/keyEvents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents
/// Returns a list of Key Events in the specified parent property. Returns an empty list if no Key Events are found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_key_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_key_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListKeyEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaListKeyEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/keyEvents
/// Returns a list of Key Events in the specified parent property. Returns an empty list if no Key Events are found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_key_events_list_builder()` + `analyticsadmin_properties_key_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaListKeyEventsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_key_events_list_builder(client, parent, pageSize, pageToken)?;
    analyticsadmin_properties_key_events_list_execute(builder)
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Updates a Key Event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `analyticsadmin_properties_key_events_patch_execute()` to send, or `analyticsadmin_properties_key_events_patch` for simplest API.

pub fn analyticsadmin_properties_key_events_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaKeyEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://analyticsadmin.googleapis.com/v1beta/properties/{}/keyEvents/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Updates a Key Event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `analyticsadmin_properties_key_events_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from analyticsadmin_properties_key_events_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaKeyEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAnalyticsAdminV1betaKeyEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/properties/{propertiesId}/keyEvents/{keyEventsId}
/// Updates a Key Event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `analyticsadmin_properties_key_events_patch_builder()` + `analyticsadmin_properties_key_events_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn analyticsadmin_properties_key_events_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleAnalyticsAdminV1betaKeyEvent,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAnalyticsAdminV1betaKeyEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        analyticsadmin_properties_key_events_patch_builder(client, name, updateMask, body)?;
    analyticsadmin_properties_key_events_patch_execute(builder)
}
