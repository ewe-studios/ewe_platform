//! Auto-generated API clients for gcp/deploymentmanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/cancelPreview
/// Cancels and removes the preview currently associated with the deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_deployments_cancel_preview_execute()` to send, or `deploymentmanager_deployments_cancel_preview` for simplest API.

pub fn deploymentmanager_deployments_cancel_preview_builder(
    client: &SimpleHttpClient,
    project: &String,
    deployment: &String,
    body: &DeploymentsCancelPreviewRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/cancelPreview",
        project,
        deployment,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/cancelPreview
/// Cancels and removes the preview currently associated with the deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_deployments_cancel_preview_execute()` or `deploymentmanager_deployments_cancel_preview`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_cancel_preview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_cancel_preview_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/cancelPreview
/// Cancels and removes the preview currently associated with the deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_deployments_cancel_preview_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_deployments_cancel_preview_task()`.
/// For the simplest API, use `deploymentmanager_deployments_cancel_preview()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_cancel_preview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_deployments_cancel_preview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_deployments_cancel_preview_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_deployments_cancel_preview`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsCancelPreviewArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: deployment
    pub deployment: String,
    /// Request body.
    pub body: DeploymentsCancelPreviewRequest,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/cancelPreview
/// Cancels and removes the preview currently associated with the deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_deployments_cancel_preview_builder()` + `deploymentmanager_deployments_cancel_preview_execute()`.
/// For task-level control, use `deploymentmanager_deployments_cancel_preview_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_cancel_preview(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerDeploymentsCancelPreviewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_deployments_cancel_preview_builder(
        client,
        &args.project,
        &args.deployment,
        &args.body,
    )?;
    deploymentmanager_deployments_cancel_preview_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
/// Deletes a deployment and all of the resources in the deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_deployments_delete_execute()` to send, or `deploymentmanager_deployments_delete` for simplest API.

pub fn deploymentmanager_deployments_delete_builder(
    client: &SimpleHttpClient,
    project: &String,
    deployment: &String,
    deletePolicy: &Option<String>,
    header_bypassBillingFilter: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}",
        project,
        deployment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deletePolicy.as_ref() {
        query_parts.push(format!("deletePolicy={}", val));
    }
    if let Some(val) = header_bypassBillingFilter.as_ref() {
        query_parts.push(format!("header.bypassBillingFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
/// Deletes a deployment and all of the resources in the deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_deployments_delete_execute()` or `deploymentmanager_deployments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
/// Deletes a deployment and all of the resources in the deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_deployments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_deployments_delete_task()`.
/// For the simplest API, use `deploymentmanager_deployments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_deployments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: deployment
    pub deployment: String,
    /// Query parameter: deletePolicy
    pub deletePolicy: Option<String>,
    /// Query parameter: header_bypassBillingFilter
    pub header_bypassBillingFilter: Option<bool>,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
/// Deletes a deployment and all of the resources in the deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_deployments_delete_builder()` + `deploymentmanager_deployments_delete_execute()`.
/// For task-level control, use `deploymentmanager_deployments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_delete(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_deployments_delete_builder(
        client,
        &args.project,
        &args.deployment,
        &args.deletePolicy,
        &args.header_bypassBillingFilter,
    )?;
    deploymentmanager_deployments_delete_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_deployments_get_iam_policy_execute()` to send, or `deploymentmanager_deployments_get_iam_policy` for simplest API.

pub fn deploymentmanager_deployments_get_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &String,
    resource: &String,
    header_bypassBillingFilter: &Option<bool>,
    optionsRequestedPolicyVersion: &Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/getIamPolicy",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = header_bypassBillingFilter.as_ref() {
        query_parts.push(format!("header.bypassBillingFilter={}", val));
    }
    if let Some(val) = optionsRequestedPolicyVersion.as_ref() {
        query_parts.push(format!("optionsRequestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_deployments_get_iam_policy_execute()` or `deploymentmanager_deployments_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_deployments_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_deployments_get_iam_policy_task()`.
/// For the simplest API, use `deploymentmanager_deployments_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_deployments_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_deployments_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_deployments_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsGetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: header_bypassBillingFilter
    pub header_bypassBillingFilter: Option<bool>,
    /// Query parameter: optionsRequestedPolicyVersion
    pub optionsRequestedPolicyVersion: Option<i32>,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/getIamPolicy
/// Gets the access control policy for a resource. May be empty if no such policy or resource exists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_deployments_get_iam_policy_builder()` + `deploymentmanager_deployments_get_iam_policy_execute()`.
/// For task-level control, use `deploymentmanager_deployments_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerDeploymentsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_deployments_get_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.header_bypassBillingFilter,
        &args.optionsRequestedPolicyVersion,
    )?;
    deploymentmanager_deployments_get_iam_policy_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments
/// Creates a deployment and all of the resources described by the deployment manifest.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_deployments_insert_execute()` to send, or `deploymentmanager_deployments_insert` for simplest API.

pub fn deploymentmanager_deployments_insert_builder(
    client: &SimpleHttpClient,
    project: &String,
    createPolicy: &Option<String>,
    header_bypassBillingFilter: &Option<bool>,
    preview: &Option<bool>,
    body: &Deployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createPolicy.as_ref() {
        query_parts.push(format!("createPolicy={}", val));
    }
    if let Some(val) = header_bypassBillingFilter.as_ref() {
        query_parts.push(format!("header.bypassBillingFilter={}", val));
    }
    if let Some(val) = preview.as_ref() {
        query_parts.push(format!("preview={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments
/// Creates a deployment and all of the resources described by the deployment manifest.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_deployments_insert_execute()` or `deploymentmanager_deployments_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments
/// Creates a deployment and all of the resources described by the deployment manifest.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_deployments_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_deployments_insert_task()`.
/// For the simplest API, use `deploymentmanager_deployments_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_deployments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_deployments_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_deployments_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: createPolicy
    pub createPolicy: Option<String>,
    /// Query parameter: header_bypassBillingFilter
    pub header_bypassBillingFilter: Option<bool>,
    /// Query parameter: preview
    pub preview: Option<bool>,
    /// Request body.
    pub body: Deployment,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments
/// Creates a deployment and all of the resources described by the deployment manifest.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_deployments_insert_builder()` + `deploymentmanager_deployments_insert_execute()`.
/// For task-level control, use `deploymentmanager_deployments_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_insert(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerDeploymentsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_deployments_insert_builder(
        client,
        &args.project,
        &args.createPolicy,
        &args.header_bypassBillingFilter,
        &args.preview,
        &args.body,
    )?;
    deploymentmanager_deployments_insert_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_deployments_set_iam_policy_execute()` to send, or `deploymentmanager_deployments_set_iam_policy` for simplest API.

pub fn deploymentmanager_deployments_set_iam_policy_builder(
    client: &SimpleHttpClient,
    project: &String,
    resource: &String,
    body: &GlobalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/setIamPolicy",
        project,
        resource,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_deployments_set_iam_policy_execute()` or `deploymentmanager_deployments_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_deployments_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_deployments_set_iam_policy_task()`.
/// For the simplest API, use `deploymentmanager_deployments_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_deployments_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_deployments_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_deployments_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsSetIamPolicyArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_deployments_set_iam_policy_builder()` + `deploymentmanager_deployments_set_iam_policy_execute()`.
/// For task-level control, use `deploymentmanager_deployments_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerDeploymentsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_deployments_set_iam_policy_builder(
        client,
        &args.project,
        &args.resource,
        &args.body,
    )?;
    deploymentmanager_deployments_set_iam_policy_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/stop
/// Stops an ongoing operation. This does not roll back any work that has already been completed, but prevents any new work from being started.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_deployments_stop_execute()` to send, or `deploymentmanager_deployments_stop` for simplest API.

pub fn deploymentmanager_deployments_stop_builder(
    client: &SimpleHttpClient,
    project: &String,
    deployment: &String,
    body: &DeploymentsStopRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/stop",
        project,
        deployment,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/stop
/// Stops an ongoing operation. This does not roll back any work that has already been completed, but prevents any new work from being started.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_deployments_stop_execute()` or `deploymentmanager_deployments_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/stop
/// Stops an ongoing operation. This does not roll back any work that has already been completed, but prevents any new work from being started.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_deployments_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_deployments_stop_task()`.
/// For the simplest API, use `deploymentmanager_deployments_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_deployments_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_deployments_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_deployments_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsStopArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: deployment
    pub deployment: String,
    /// Request body.
    pub body: DeploymentsStopRequest,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/stop
/// Stops an ongoing operation. This does not roll back any work that has already been completed, but prevents any new work from being started.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_deployments_stop_builder()` + `deploymentmanager_deployments_stop_execute()`.
/// For task-level control, use `deploymentmanager_deployments_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_stop(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerDeploymentsStopArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_deployments_stop_builder(
        client,
        &args.project,
        &args.deployment,
        &args.body,
    )?;
    deploymentmanager_deployments_stop_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_deployments_test_iam_permissions_execute()` to send, or `deploymentmanager_deployments_test_iam_permissions` for simplest API.

pub fn deploymentmanager_deployments_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    project: &String,
    resource: &String,
    header_bypassBillingFilter: &Option<bool>,
    body: &TestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/testIamPermissions",
        project,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = header_bypassBillingFilter.as_ref() {
        query_parts.push(format!("header.bypassBillingFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_deployments_test_iam_permissions_execute()` or `deploymentmanager_deployments_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_deployments_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_deployments_test_iam_permissions_task()`.
/// For the simplest API, use `deploymentmanager_deployments_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_deployments_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_deployments_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = deploymentmanager_deployments_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_deployments_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsTestIamPermissionsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: header_bypassBillingFilter
    pub header_bypassBillingFilter: Option<bool>,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/testIamPermissions
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_deployments_test_iam_permissions_builder()` + `deploymentmanager_deployments_test_iam_permissions_execute()`.
/// For task-level control, use `deploymentmanager_deployments_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_deployments_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerDeploymentsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = deploymentmanager_deployments_test_iam_permissions_builder(
        client,
        &args.project,
        &args.resource,
        &args.header_bypassBillingFilter,
        &args.body,
    )?;
    deploymentmanager_deployments_test_iam_permissions_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests/{manifest}
/// Gets information about a specific manifest.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_manifests_get_execute()` to send, or `deploymentmanager_manifests_get` for simplest API.

pub fn deploymentmanager_manifests_get_builder(
    client: &SimpleHttpClient,
    project: &String,
    deployment: &String,
    manifest: &String,
    header_bypassBillingFilter: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/manifests/{}",
        project,
        deployment,
        manifest,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = header_bypassBillingFilter.as_ref() {
        query_parts.push(format!("header.bypassBillingFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests/{manifest}
/// Gets information about a specific manifest.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_manifests_get_execute()` or `deploymentmanager_manifests_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_manifests_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_manifests_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Manifest>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Manifest = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests/{manifest}
/// Gets information about a specific manifest.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_manifests_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_manifests_get_task()`.
/// For the simplest API, use `deploymentmanager_manifests_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_manifests_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_manifests_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Manifest>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_manifests_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_manifests_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerManifestsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: deployment
    pub deployment: String,
    /// Path parameter: manifest
    pub manifest: String,
    /// Query parameter: header_bypassBillingFilter
    pub header_bypassBillingFilter: Option<bool>,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests/{manifest}
/// Gets information about a specific manifest.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_manifests_get_builder()` + `deploymentmanager_manifests_get_execute()`.
/// For task-level control, use `deploymentmanager_manifests_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_manifests_get(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerManifestsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Manifest>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_manifests_get_builder(
        client,
        &args.project,
        &args.deployment,
        &args.manifest,
        &args.header_bypassBillingFilter,
    )?;
    deploymentmanager_manifests_get_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests
/// Lists all manifests for a given deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_manifests_list_execute()` to send, or `deploymentmanager_manifests_list` for simplest API.

pub fn deploymentmanager_manifests_list_builder(
    client: &SimpleHttpClient,
    project: &String,
    deployment: &String,
    filter: &Option<String>,
    maxResults: &Option<i32>,
    orderBy: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/manifests",
        project,
        deployment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests
/// Lists all manifests for a given deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_manifests_list_execute()` or `deploymentmanager_manifests_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_manifests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_manifests_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManifestsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManifestsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests
/// Lists all manifests for a given deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_manifests_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_manifests_list_task()`.
/// For the simplest API, use `deploymentmanager_manifests_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_manifests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_manifests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManifestsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = deploymentmanager_manifests_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_manifests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerManifestsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: deployment
    pub deployment: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests
/// Lists all manifests for a given deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_manifests_list_builder()` + `deploymentmanager_manifests_list_execute()`.
/// For task-level control, use `deploymentmanager_manifests_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_manifests_list(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerManifestsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManifestsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = deploymentmanager_manifests_list_builder(
        client,
        &args.project,
        &args.deployment,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
    )?;
    deploymentmanager_manifests_list_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/operations/{operation}
/// Gets information about a specific operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_operations_get_execute()` to send, or `deploymentmanager_operations_get` for simplest API.

pub fn deploymentmanager_operations_get_builder(
    client: &SimpleHttpClient,
    project: &String,
    operation: &String,
    header_bypassBillingFilter: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/operations/{}",
        project,
        operation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = header_bypassBillingFilter.as_ref() {
        query_parts.push(format!("header.bypassBillingFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/operations/{operation}
/// Gets information about a specific operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_operations_get_execute()` or `deploymentmanager_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/operations/{operation}
/// Gets information about a specific operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_operations_get_task()`.
/// For the simplest API, use `deploymentmanager_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerOperationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
    /// Query parameter: header_bypassBillingFilter
    pub header_bypassBillingFilter: Option<bool>,
}

/// GET deploymentmanager/v2/projects/{project}/global/operations/{operation}
/// Gets information about a specific operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_operations_get_builder()` + `deploymentmanager_operations_get_execute()`.
/// For task-level control, use `deploymentmanager_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_operations_get(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_operations_get_builder(
        client,
        &args.project,
        &args.operation,
        &args.header_bypassBillingFilter,
    )?;
    deploymentmanager_operations_get_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/operations
/// Lists all operations for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_operations_list_execute()` to send, or `deploymentmanager_operations_list` for simplest API.

pub fn deploymentmanager_operations_list_builder(
    client: &SimpleHttpClient,
    project: &String,
    filter: &Option<String>,
    maxResults: &Option<i32>,
    orderBy: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/operations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/operations
/// Lists all operations for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_operations_list_execute()` or `deploymentmanager_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/operations
/// Lists all operations for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_operations_list_task()`.
/// For the simplest API, use `deploymentmanager_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = deploymentmanager_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerOperationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET deploymentmanager/v2/projects/{project}/global/operations
/// Lists all operations for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_operations_list_builder()` + `deploymentmanager_operations_list_execute()`.
/// For task-level control, use `deploymentmanager_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_operations_list(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = deploymentmanager_operations_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
    )?;
    deploymentmanager_operations_list_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources/{resource}
/// Gets information about a single resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_resources_get_execute()` to send, or `deploymentmanager_resources_get` for simplest API.

pub fn deploymentmanager_resources_get_builder(
    client: &SimpleHttpClient,
    project: &String,
    deployment: &String,
    resource: &String,
    header_bypassBillingFilter: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/resources/{}",
        project,
        deployment,
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = header_bypassBillingFilter.as_ref() {
        query_parts.push(format!("header.bypassBillingFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources/{resource}
/// Gets information about a single resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_resources_get_execute()` or `deploymentmanager_resources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_resources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Resource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Resource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources/{resource}
/// Gets information about a single resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_resources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_resources_get_task()`.
/// For the simplest API, use `deploymentmanager_resources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Resource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = deploymentmanager_resources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_resources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerResourcesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: deployment
    pub deployment: String,
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: header_bypassBillingFilter
    pub header_bypassBillingFilter: Option<bool>,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources/{resource}
/// Gets information about a single resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_resources_get_builder()` + `deploymentmanager_resources_get_execute()`.
/// For task-level control, use `deploymentmanager_resources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_resources_get(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerResourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Resource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = deploymentmanager_resources_get_builder(
        client,
        &args.project,
        &args.deployment,
        &args.resource,
        &args.header_bypassBillingFilter,
    )?;
    deploymentmanager_resources_get_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources
/// Lists all resources in a given deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_resources_list_execute()` to send, or `deploymentmanager_resources_list` for simplest API.

pub fn deploymentmanager_resources_list_builder(
    client: &SimpleHttpClient,
    project: &String,
    deployment: &String,
    filter: &Option<String>,
    maxResults: &Option<i32>,
    orderBy: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/resources",
        project,
        deployment,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources
/// Lists all resources in a given deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_resources_list_execute()` or `deploymentmanager_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourcesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources
/// Lists all resources in a given deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_resources_list_task()`.
/// For the simplest API, use `deploymentmanager_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = deploymentmanager_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerResourcesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: deployment
    pub deployment: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources
/// Lists all resources in a given deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_resources_list_builder()` + `deploymentmanager_resources_list_execute()`.
/// For task-level control, use `deploymentmanager_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_resources_list(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerResourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourcesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = deploymentmanager_resources_list_builder(
        client,
        &args.project,
        &args.deployment,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
    )?;
    deploymentmanager_resources_list_execute(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/types
/// Lists all resource types for Deployment Manager.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `deploymentmanager_types_list_execute()` to send, or `deploymentmanager_types_list` for simplest API.

pub fn deploymentmanager_types_list_builder(
    client: &SimpleHttpClient,
    project: &String,
    filter: &Option<String>,
    maxResults: &Option<i32>,
    orderBy: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/types",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET deploymentmanager/v2/projects/{project}/global/types
/// Lists all resource types for Deployment Manager.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `deploymentmanager_types_list_execute()` or `deploymentmanager_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TypesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TypesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET deploymentmanager/v2/projects/{project}/global/types
/// Lists all resource types for Deployment Manager.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `deploymentmanager_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `deploymentmanager_types_list_task()`.
/// For the simplest API, use `deploymentmanager_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `deploymentmanager_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn deploymentmanager_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TypesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = deploymentmanager_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`deploymentmanager_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeploymentmanagerTypesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET deploymentmanager/v2/projects/{project}/global/types
/// Lists all resource types for Deployment Manager.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `deploymentmanager_types_list_builder()` + `deploymentmanager_types_list_execute()`.
/// For task-level control, use `deploymentmanager_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn deploymentmanager_types_list(
    client: &SimpleHttpClient,
    args: &DeploymentmanagerTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TypesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = deploymentmanager_types_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.orderBy,
        &args.pageToken,
    )?;
    deploymentmanager_types_list_execute(builder)
}
