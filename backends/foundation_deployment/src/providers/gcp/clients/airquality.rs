//! Auto-generated API clients for gcp/airquality.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/currentConditions:lookup
/// The Current Conditions endpoint provides hourly air quality information in more than 100 countries, up to a 500 x 500 meters resolution. Includes over 70 local indexes and global air quality index and categories.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `airquality_current_conditions_lookup_execute()` to send, or `airquality_current_conditions_lookup` for simplest API.

pub fn airquality_current_conditions_lookup_builder(
    client: &SimpleHttpClient,
    body: &LookupCurrentConditionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://airquality.googleapis.com/v1/currentConditions:lookup",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/currentConditions:lookup
/// The Current Conditions endpoint provides hourly air quality information in more than 100 countries, up to a 500 x 500 meters resolution. Includes over 70 local indexes and global air quality index and categories.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `airquality_current_conditions_lookup_execute()` or `airquality_current_conditions_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_current_conditions_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_current_conditions_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupCurrentConditionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupCurrentConditionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/currentConditions:lookup
/// The Current Conditions endpoint provides hourly air quality information in more than 100 countries, up to a 500 x 500 meters resolution. Includes over 70 local indexes and global air quality index and categories.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `airquality_current_conditions_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `airquality_current_conditions_lookup_task()`.
/// For the simplest API, use `airquality_current_conditions_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_current_conditions_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn airquality_current_conditions_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupCurrentConditionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = airquality_current_conditions_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`airquality_current_conditions_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AirqualityCurrentConditionsLookupArgs {
    /// Request body.
    pub body: LookupCurrentConditionsRequest,
}

/// GET v1/currentConditions:lookup
/// The Current Conditions endpoint provides hourly air quality information in more than 100 countries, up to a 500 x 500 meters resolution. Includes over 70 local indexes and global air quality index and categories.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `airquality_current_conditions_lookup_builder()` + `airquality_current_conditions_lookup_execute()`.
/// For task-level control, use `airquality_current_conditions_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_current_conditions_lookup(
    client: &SimpleHttpClient,
    args: &AirqualityCurrentConditionsLookupArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupCurrentConditionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = airquality_current_conditions_lookup_builder(client, &args.body)?;
    airquality_current_conditions_lookup_execute(builder)
}

/// GET v1/forecast:lookup
/// Returns air quality forecast for a specific location for a given time range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `airquality_forecast_lookup_execute()` to send, or `airquality_forecast_lookup` for simplest API.

pub fn airquality_forecast_lookup_builder(
    client: &SimpleHttpClient,
    body: &LookupForecastRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://airquality.googleapis.com/v1/forecast:lookup",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/forecast:lookup
/// Returns air quality forecast for a specific location for a given time range.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `airquality_forecast_lookup_execute()` or `airquality_forecast_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_forecast_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_forecast_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupForecastResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupForecastResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/forecast:lookup
/// Returns air quality forecast for a specific location for a given time range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `airquality_forecast_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `airquality_forecast_lookup_task()`.
/// For the simplest API, use `airquality_forecast_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_forecast_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn airquality_forecast_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupForecastResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = airquality_forecast_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`airquality_forecast_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AirqualityForecastLookupArgs {
    /// Request body.
    pub body: LookupForecastRequest,
}

/// GET v1/forecast:lookup
/// Returns air quality forecast for a specific location for a given time range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `airquality_forecast_lookup_builder()` + `airquality_forecast_lookup_execute()`.
/// For task-level control, use `airquality_forecast_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_forecast_lookup(
    client: &SimpleHttpClient,
    args: &AirqualityForecastLookupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupForecastResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = airquality_forecast_lookup_builder(client, &args.body)?;
    airquality_forecast_lookup_execute(builder)
}

/// GET v1/history:lookup
/// Returns air quality history for a specific location for a given time range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `airquality_history_lookup_execute()` to send, or `airquality_history_lookup` for simplest API.

pub fn airquality_history_lookup_builder(
    client: &SimpleHttpClient,
    body: &LookupHistoryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://airquality.googleapis.com/v1/history:lookup",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/history:lookup
/// Returns air quality history for a specific location for a given time range.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `airquality_history_lookup_execute()` or `airquality_history_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_history_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_history_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupHistoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupHistoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/history:lookup
/// Returns air quality history for a specific location for a given time range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `airquality_history_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `airquality_history_lookup_task()`.
/// For the simplest API, use `airquality_history_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_history_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn airquality_history_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupHistoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = airquality_history_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`airquality_history_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AirqualityHistoryLookupArgs {
    /// Request body.
    pub body: LookupHistoryRequest,
}

/// GET v1/history:lookup
/// Returns air quality history for a specific location for a given time range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `airquality_history_lookup_builder()` + `airquality_history_lookup_execute()`.
/// For task-level control, use `airquality_history_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_history_lookup(
    client: &SimpleHttpClient,
    args: &AirqualityHistoryLookupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LookupHistoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = airquality_history_lookup_builder(client, &args.body)?;
    airquality_history_lookup_execute(builder)
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a bytes array containing the data of the tile PNG image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_execute()` to send, or `airquality_map_types_heatmap_tiles_lookup_heatmap_tile` for simplest API.

pub fn airquality_map_types_heatmap_tiles_lookup_heatmap_tile_builder(
    client: &SimpleHttpClient,
    mapType: &String,
    zoom: &i32,
    x: &i32,
    y: &i32,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://airquality.googleapis.com/v1/mapTypes/{}/heatmapTiles/{}/{}/{}",
        mapType, zoom, x, y,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a bytes array containing the data of the tile PNG image.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_execute()` or `airquality_map_types_heatmap_tiles_lookup_heatmap_tile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_map_types_heatmap_tiles_lookup_heatmap_tile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a bytes array containing the data of the tile PNG image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_task()`.
/// For the simplest API, use `airquality_map_types_heatmap_tiles_lookup_heatmap_tile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn airquality_map_types_heatmap_tiles_lookup_heatmap_tile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = airquality_map_types_heatmap_tiles_lookup_heatmap_tile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`airquality_map_types_heatmap_tiles_lookup_heatmap_tile`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AirqualityMapTypesHeatmapTilesLookupHeatmapTileArgs {
    /// Path parameter: mapType
    pub mapType: String,
    /// Path parameter: zoom
    pub zoom: i32,
    /// Path parameter: x
    pub x: i32,
    /// Path parameter: y
    pub y: i32,
}

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
/// Returns a bytes array containing the data of the tile PNG image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_builder()` + `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_execute()`.
/// For task-level control, use `airquality_map_types_heatmap_tiles_lookup_heatmap_tile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn airquality_map_types_heatmap_tiles_lookup_heatmap_tile(
    client: &SimpleHttpClient,
    args: &AirqualityMapTypesHeatmapTilesLookupHeatmapTileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HttpBody>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = airquality_map_types_heatmap_tiles_lookup_heatmap_tile_builder(
        client,
        &args.mapType,
        &args.zoom,
        &args.x,
        &args.y,
    )?;
    airquality_map_types_heatmap_tiles_lookup_heatmap_tile_execute(builder)
}
