//! Auto-generated API clients for gcp/localservices.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accountReports:search
/// Get account reports containing aggregate account data of all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `localservices_account_reports_search_execute()` to send, or `localservices_account_reports_search` for simplest API.

pub fn localservices_account_reports_search_builder<R>(
    client: &SimpleHttpClient<R>,
    endDate_day: &Option<String>,
    endDate_month: &Option<String>,
    endDate_year: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    query: &Option<String>,
    startDate_day: &Option<String>,
    startDate_month: &Option<String>,
    startDate_year: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://localservices.googleapis.com/v1/accountReports:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate_day.as_ref() {
        query_parts.push(format!("endDate.day={}", val));
    }
    if let Some(val) = endDate_month.as_ref() {
        query_parts.push(format!("endDate.month={}", val));
    }
    if let Some(val) = endDate_year.as_ref() {
        query_parts.push(format!("endDate.year={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = startDate_day.as_ref() {
        query_parts.push(format!("startDate.day={}", val));
    }
    if let Some(val) = startDate_month.as_ref() {
        query_parts.push(format!("startDate.month={}", val));
    }
    if let Some(val) = startDate_year.as_ref() {
        query_parts.push(format!("startDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accountReports:search
/// Get account reports containing aggregate account data of all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `localservices_account_reports_search_execute()` or `localservices_account_reports_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `localservices_account_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn localservices_account_reports_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accountReports:search
/// Get account reports containing aggregate account data of all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `localservices_account_reports_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `localservices_account_reports_search_task()`.
/// For the simplest API, use `localservices_account_reports_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `localservices_account_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn localservices_account_reports_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = localservices_account_reports_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`localservices_account_reports_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LocalservicesAccountReportsSearchArgs {
    /// Query parameter: endDate_day
    pub endDate_day: Option<String>,
    /// Query parameter: endDate_month
    pub endDate_month: Option<String>,
    /// Query parameter: endDate_year
    pub endDate_year: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
    /// Query parameter: startDate_day
    pub startDate_day: Option<String>,
    /// Query parameter: startDate_month
    pub startDate_month: Option<String>,
    /// Query parameter: startDate_year
    pub startDate_year: Option<String>,
}

/// GET v1/accountReports:search
/// Get account reports containing aggregate account data of all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `localservices_account_reports_search_builder()` + `localservices_account_reports_search_execute()`.
/// For task-level control, use `localservices_account_reports_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn localservices_account_reports_search(
    client: &SimpleHttpClient,
    args: &LocalservicesAccountReportsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = localservices_account_reports_search_builder(
        client,
        &args.endDate_day,
        &args.endDate_month,
        &args.endDate_year,
        &args.pageSize,
        &args.pageToken,
        &args.query,
        &args.startDate_day,
        &args.startDate_month,
        &args.startDate_year,
    )?;
    localservices_account_reports_search_execute(builder)
}

/// GET v1/detailedLeadReports:search
/// Get detailed lead reports containing leads that have been received by all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `localservices_detailed_lead_reports_search_execute()` to send, or `localservices_detailed_lead_reports_search` for simplest API.

pub fn localservices_detailed_lead_reports_search_builder<R>(
    client: &SimpleHttpClient<R>,
    endDate_day: &Option<String>,
    endDate_month: &Option<String>,
    endDate_year: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    query: &Option<String>,
    startDate_day: &Option<String>,
    startDate_month: &Option<String>,
    startDate_year: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://localservices.googleapis.com/v1/detailedLeadReports:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate_day.as_ref() {
        query_parts.push(format!("endDate.day={}", val));
    }
    if let Some(val) = endDate_month.as_ref() {
        query_parts.push(format!("endDate.month={}", val));
    }
    if let Some(val) = endDate_year.as_ref() {
        query_parts.push(format!("endDate.year={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = startDate_day.as_ref() {
        query_parts.push(format!("startDate.day={}", val));
    }
    if let Some(val) = startDate_month.as_ref() {
        query_parts.push(format!("startDate.month={}", val));
    }
    if let Some(val) = startDate_year.as_ref() {
        query_parts.push(format!("startDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/detailedLeadReports:search
/// Get detailed lead reports containing leads that have been received by all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `localservices_detailed_lead_reports_search_execute()` or `localservices_detailed_lead_reports_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `localservices_detailed_lead_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn localservices_detailed_lead_reports_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse =
                    serde_json::from_str(&body).map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/detailedLeadReports:search
/// Get detailed lead reports containing leads that have been received by all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `localservices_detailed_lead_reports_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `localservices_detailed_lead_reports_search_task()`.
/// For the simplest API, use `localservices_detailed_lead_reports_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `localservices_detailed_lead_reports_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn localservices_detailed_lead_reports_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = localservices_detailed_lead_reports_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`localservices_detailed_lead_reports_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LocalservicesDetailedLeadReportsSearchArgs {
    /// Query parameter: endDate_day
    pub endDate_day: Option<String>,
    /// Query parameter: endDate_month
    pub endDate_month: Option<String>,
    /// Query parameter: endDate_year
    pub endDate_year: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
    /// Query parameter: startDate_day
    pub startDate_day: Option<String>,
    /// Query parameter: startDate_month
    pub startDate_month: Option<String>,
    /// Query parameter: startDate_year
    pub startDate_year: Option<String>,
}

/// GET v1/detailedLeadReports:search
/// Get detailed lead reports containing leads that have been received by all linked GLS accounts. Caller needs to provide their manager customer id and the associated auth credential that allows them read permissions on their linked accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `localservices_detailed_lead_reports_search_builder()` + `localservices_detailed_lead_reports_search_execute()`.
/// For task-level control, use `localservices_detailed_lead_reports_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn localservices_detailed_lead_reports_search(
    client: &SimpleHttpClient,
    args: &LocalservicesDetailedLeadReportsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = localservices_detailed_lead_reports_search_builder(
        client,
        &args.endDate_day,
        &args.endDate_month,
        &args.endDate_year,
        &args.pageSize,
        &args.pageToken,
        &args.query,
        &args.startDate_day,
        &args.startDate_month,
        &args.startDate_year,
    )?;
    localservices_detailed_lead_reports_search_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse with LocalservicesAccountReportsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LocalservicesAccountReportsSearchArgs>
    for GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse
{
    fn generate_resource_id(&self, input: &LocalservicesAccountReportsSearchArgs) -> String {
        "gcp::localservices::GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse"
            .to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::localservices::GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse with LocalservicesDetailedLeadReportsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LocalservicesDetailedLeadReportsSearchArgs>
    for GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse
{
    fn generate_resource_id(&self, input: &LocalservicesDetailedLeadReportsSearchArgs) -> String {
        "gcp::localservices::GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse"
            .to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::localservices::GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
