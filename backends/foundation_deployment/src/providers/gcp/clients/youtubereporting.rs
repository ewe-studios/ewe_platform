//! Auto-generated API clients for gcp/youtubereporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/jobs
/// Creates a job and returns it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_create_execute()` to send, or `youtubereporting_jobs_create` for simplest API.

pub fn youtubereporting_jobs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    onBehalfOfContentOwner: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/jobs
/// Creates a job and returns it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_create_execute()` or `youtubereporting_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/jobs
/// Creates a job and returns it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_create_task()`.
/// For the simplest API, use `youtubereporting_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsCreateArgs {
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<Option<String>>,
}

/// POST v1/jobs
/// Creates a job and returns it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_create_builder()` + `youtubereporting_jobs_create_execute()`.
/// For task-level control, use `youtubereporting_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_create(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtubereporting_jobs_create_builder(client, &args.onBehalfOfContentOwner)?;
    youtubereporting_jobs_create_execute(builder)
}

/// DELETE v1/jobs/{jobId}
/// Deletes a job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_delete_execute()` to send, or `youtubereporting_jobs_delete` for simplest API.

pub fn youtubereporting_jobs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    jobId: &String,
    onBehalfOfContentOwner: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs/{}", jobId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/jobs/{jobId}
/// Deletes a job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_delete_execute()` or `youtubereporting_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/jobs/{jobId}
/// Deletes a job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_delete_task()`.
/// For the simplest API, use `youtubereporting_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsDeleteArgs {
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<Option<String>>,
}

/// DELETE v1/jobs/{jobId}
/// Deletes a job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_delete_builder()` + `youtubereporting_jobs_delete_execute()`.
/// For task-level control, use `youtubereporting_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_delete(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        youtubereporting_jobs_delete_builder(client, &args.jobId, &args.onBehalfOfContentOwner)?;
    youtubereporting_jobs_delete_execute(builder)
}

/// GET v1/jobs/{jobId}
/// Gets a job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_get_execute()` to send, or `youtubereporting_jobs_get` for simplest API.

pub fn youtubereporting_jobs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    jobId: &String,
    onBehalfOfContentOwner: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs/{}", jobId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/jobs/{jobId}
/// Gets a job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_get_execute()` or `youtubereporting_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs/{jobId}
/// Gets a job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_get_task()`.
/// For the simplest API, use `youtubereporting_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsGetArgs {
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<Option<String>>,
}

/// GET v1/jobs/{jobId}
/// Gets a job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_get_builder()` + `youtubereporting_jobs_get_execute()`.
/// For task-level control, use `youtubereporting_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_get(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        youtubereporting_jobs_get_builder(client, &args.jobId, &args.onBehalfOfContentOwner)?;
    youtubereporting_jobs_get_execute(builder)
}

/// GET v1/jobs
/// Lists jobs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_list_execute()` to send, or `youtubereporting_jobs_list` for simplest API.

pub fn youtubereporting_jobs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    includeSystemManaged: &Option<Option<String>>,
    onBehalfOfContentOwner: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeSystemManaged.as_ref() {
        query_parts.push(format!("includeSystemManaged={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/jobs
/// Lists jobs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_list_execute()` or `youtubereporting_jobs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs
/// Lists jobs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_list_task()`.
/// For the simplest API, use `youtubereporting_jobs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsListArgs {
    /// Query parameter: includeSystemManaged
    pub includeSystemManaged: Option<Option<String>>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/jobs
/// Lists jobs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_list_builder()` + `youtubereporting_jobs_list_execute()`.
/// For task-level control, use `youtubereporting_jobs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_list(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtubereporting_jobs_list_builder(
        client,
        &args.includeSystemManaged,
        &args.onBehalfOfContentOwner,
        &args.pageSize,
        &args.pageToken,
    )?;
    youtubereporting_jobs_list_execute(builder)
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_reports_get_execute()` to send, or `youtubereporting_jobs_reports_get` for simplest API.

pub fn youtubereporting_jobs_reports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    jobId: &String,
    reportId: &String,
    onBehalfOfContentOwner: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}/reports/{}",
        jobId, reportId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_reports_get_execute()` or `youtubereporting_jobs_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_reports_get_task()`.
/// For the simplest API, use `youtubereporting_jobs_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsReportsGetArgs {
    /// Path parameter: jobId
    pub jobId: String,
    /// Path parameter: reportId
    pub reportId: String,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<Option<String>>,
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_reports_get_builder()` + `youtubereporting_jobs_reports_get_execute()`.
/// For task-level control, use `youtubereporting_jobs_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_get(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtubereporting_jobs_reports_get_builder(
        client,
        &args.jobId,
        &args.reportId,
        &args.onBehalfOfContentOwner,
    )?;
    youtubereporting_jobs_reports_get_execute(builder)
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_reports_list_execute()` to send, or `youtubereporting_jobs_reports_list` for simplest API.

pub fn youtubereporting_jobs_reports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    jobId: &String,
    createdAfter: &Option<Option<String>>,
    onBehalfOfContentOwner: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    startTimeAtOrAfter: &Option<Option<String>>,
    startTimeBefore: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}/reports",
        jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createdAfter.as_ref() {
        query_parts.push(format!("createdAfter={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTimeAtOrAfter.as_ref() {
        query_parts.push(format!("startTimeAtOrAfter={}", val));
    }
    if let Some(val) = startTimeBefore.as_ref() {
        query_parts.push(format!("startTimeBefore={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_reports_list_execute()` or `youtubereporting_jobs_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_reports_list_task()`.
/// For the simplest API, use `youtubereporting_jobs_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsReportsListArgs {
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: createdAfter
    pub createdAfter: Option<Option<String>>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: startTimeAtOrAfter
    pub startTimeAtOrAfter: Option<Option<String>>,
    /// Query parameter: startTimeBefore
    pub startTimeBefore: Option<Option<String>>,
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_reports_list_builder()` + `youtubereporting_jobs_reports_list_execute()`.
/// For task-level control, use `youtubereporting_jobs_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_list(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsReportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtubereporting_jobs_reports_list_builder(
        client,
        &args.jobId,
        &args.createdAfter,
        &args.onBehalfOfContentOwner,
        &args.pageSize,
        &args.pageToken,
        &args.startTimeAtOrAfter,
        &args.startTimeBefore,
    )?;
    youtubereporting_jobs_reports_list_execute(builder)
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_media_download_execute()` to send, or `youtubereporting_media_download` for simplest API.

pub fn youtubereporting_media_download_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/media/{}",
        resourceName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_media_download_execute()` or `youtubereporting_media_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_media_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GdataMedia>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GdataMedia = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_media_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_media_download_task()`.
/// For the simplest API, use `youtubereporting_media_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_media_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GdataMedia>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_media_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_media_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingMediaDownloadArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_media_download_builder()` + `youtubereporting_media_download_execute()`.
/// For task-level control, use `youtubereporting_media_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_media_download(
    client: &SimpleHttpClient,
    args: &YoutubereportingMediaDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GdataMedia>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtubereporting_media_download_builder(client, &args.resourceName)?;
    youtubereporting_media_download_execute(builder)
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_report_types_list_execute()` to send, or `youtubereporting_report_types_list` for simplest API.

pub fn youtubereporting_report_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    includeSystemManaged: &Option<Option<String>>,
    onBehalfOfContentOwner: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/reportTypes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeSystemManaged.as_ref() {
        query_parts.push(format!("includeSystemManaged={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_report_types_list_execute()` or `youtubereporting_report_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_report_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_report_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportTypesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_report_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_report_types_list_task()`.
/// For the simplest API, use `youtubereporting_report_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_report_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_report_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtubereporting_report_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_report_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingReportTypesListArgs {
    /// Query parameter: includeSystemManaged
    pub includeSystemManaged: Option<Option<String>>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_report_types_list_builder()` + `youtubereporting_report_types_list_execute()`.
/// For task-level control, use `youtubereporting_report_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_report_types_list(
    client: &SimpleHttpClient,
    args: &YoutubereportingReportTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtubereporting_report_types_list_builder(
        client,
        &args.includeSystemManaged,
        &args.onBehalfOfContentOwner,
        &args.pageSize,
        &args.pageToken,
    )?;
    youtubereporting_report_types_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Job
// =============================================================================

/// ResourceIdentifier implementation for Job with YoutubereportingJobsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingJobsCreateArgs> for Job {
    fn generate_resource_id(&self, input: &YoutubereportingJobsCreateArgs) -> String {
        "gcp::youtubereporting::Job".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::Job"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with YoutubereportingJobsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingJobsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &YoutubereportingJobsDeleteArgs) -> String {
        format!("gcp::youtubereporting::Empty/{}", input.jobId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Job
// =============================================================================

/// ResourceIdentifier implementation for Job with YoutubereportingJobsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingJobsGetArgs> for Job {
    fn generate_resource_id(&self, input: &YoutubereportingJobsGetArgs) -> String {
        format!("gcp::youtubereporting::Job/{}", input.jobId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::Job"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListJobsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListJobsResponse with YoutubereportingJobsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingJobsListArgs> for ListJobsResponse {
    fn generate_resource_id(&self, input: &YoutubereportingJobsListArgs) -> String {
        "gcp::youtubereporting::ListJobsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::ListJobsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Report
// =============================================================================

/// ResourceIdentifier implementation for Report with YoutubereportingJobsReportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingJobsReportsGetArgs> for Report {
    fn generate_resource_id(&self, input: &YoutubereportingJobsReportsGetArgs) -> String {
        format!(
            "gcp::youtubereporting::Report/{}/{}",
            input.jobId, input.reportId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::Report"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReportsResponse with YoutubereportingJobsReportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingJobsReportsListArgs> for ListReportsResponse {
    fn generate_resource_id(&self, input: &YoutubereportingJobsReportsListArgs) -> String {
        format!("gcp::youtubereporting::ListReportsResponse/{}", input.jobId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::ListReportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GdataMedia
// =============================================================================

/// ResourceIdentifier implementation for GdataMedia with YoutubereportingMediaDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingMediaDownloadArgs> for GdataMedia {
    fn generate_resource_id(&self, input: &YoutubereportingMediaDownloadArgs) -> String {
        format!("gcp::youtubereporting::GdataMedia/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::GdataMedia"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReportTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReportTypesResponse with YoutubereportingReportTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<YoutubereportingReportTypesListArgs> for ListReportTypesResponse {
    fn generate_resource_id(&self, input: &YoutubereportingReportTypesListArgs) -> String {
        "gcp::youtubereporting::ListReportTypesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::youtubereporting::ListReportTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
