//! Auto-generated API clients for gcp/youtubereporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/jobs
/// Creates a job and returns it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_create_execute()` to send, or `youtubereporting_jobs_create` for simplest API.

pub fn youtubereporting_jobs_create_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: &Option<String>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/jobs
/// Creates a job and returns it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_create_execute()` or `youtubereporting_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs
/// Creates a job and returns it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_create_task()`.
/// For the simplest API, use `youtubereporting_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsCreateArgs {
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
    /// Request body.
    pub body: Job,
}

/// GET v1/jobs
/// Creates a job and returns it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_create_builder()` + `youtubereporting_jobs_create_execute()`.
/// For task-level control, use `youtubereporting_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_create(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        youtubereporting_jobs_create_builder(client, &args.onBehalfOfContentOwner, &args.body)?;
    youtubereporting_jobs_create_execute(builder)
}

/// GET v1/jobs/{jobId}
/// Deletes a job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_delete_execute()` to send, or `youtubereporting_jobs_delete` for simplest API.

pub fn youtubereporting_jobs_delete_builder(
    client: &SimpleHttpClient,
    jobId: &String,
    onBehalfOfContentOwner: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs/{}", jobId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/jobs/{jobId}
/// Deletes a job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_delete_execute()` or `youtubereporting_jobs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs/{jobId}
/// Deletes a job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_delete_task()`.
/// For the simplest API, use `youtubereporting_jobs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsDeleteArgs {
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
}

/// GET v1/jobs/{jobId}
/// Deletes a job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_delete_builder()` + `youtubereporting_jobs_delete_execute()`.
/// For task-level control, use `youtubereporting_jobs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_delete(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        youtubereporting_jobs_delete_builder(client, &args.jobId, &args.onBehalfOfContentOwner)?;
    youtubereporting_jobs_delete_execute(builder)
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_reports_get_execute()` to send, or `youtubereporting_jobs_reports_get` for simplest API.

pub fn youtubereporting_jobs_reports_get_builder(
    client: &SimpleHttpClient,
    jobId: &String,
    reportId: &String,
    onBehalfOfContentOwner: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}/reports/{}",
        jobId, reportId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_reports_get_execute()` or `youtubereporting_jobs_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_reports_get_task()`.
/// For the simplest API, use `youtubereporting_jobs_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsReportsGetArgs {
    /// Path parameter: jobId
    pub jobId: String,
    /// Path parameter: reportId
    pub reportId: String,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
}

/// GET v1/jobs/{jobId}/reports/{reportId}
/// Gets the metadata of a specific report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_reports_get_builder()` + `youtubereporting_jobs_reports_get_execute()`.
/// For task-level control, use `youtubereporting_jobs_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_get(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtubereporting_jobs_reports_get_builder(
        client,
        &args.jobId,
        &args.reportId,
        &args.onBehalfOfContentOwner,
    )?;
    youtubereporting_jobs_reports_get_execute(builder)
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_jobs_reports_list_execute()` to send, or `youtubereporting_jobs_reports_list` for simplest API.

pub fn youtubereporting_jobs_reports_list_builder(
    client: &SimpleHttpClient,
    jobId: &String,
    createdAfter: &Option<String>,
    onBehalfOfContentOwner: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    startTimeAtOrAfter: &Option<String>,
    startTimeBefore: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}/reports",
        jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = createdAfter.as_ref() {
        query_parts.push(format!("createdAfter={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTimeAtOrAfter.as_ref() {
        query_parts.push(format!("startTimeAtOrAfter={}", val));
    }
    if let Some(val) = startTimeBefore.as_ref() {
        query_parts.push(format!("startTimeBefore={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_jobs_reports_list_execute()` or `youtubereporting_jobs_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_jobs_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_jobs_reports_list_task()`.
/// For the simplest API, use `youtubereporting_jobs_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_jobs_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_jobs_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtubereporting_jobs_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_jobs_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingJobsReportsListArgs {
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: createdAfter
    pub createdAfter: Option<String>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: startTimeAtOrAfter
    pub startTimeAtOrAfter: Option<String>,
    /// Query parameter: startTimeBefore
    pub startTimeBefore: Option<String>,
}

/// GET v1/jobs/{jobId}/reports
/// Lists reports created by a specific job. Returns NOT_FOUND if the job does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_jobs_reports_list_builder()` + `youtubereporting_jobs_reports_list_execute()`.
/// For task-level control, use `youtubereporting_jobs_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_jobs_reports_list(
    client: &SimpleHttpClient,
    args: &YoutubereportingJobsReportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtubereporting_jobs_reports_list_builder(
        client,
        &args.jobId,
        &args.createdAfter,
        &args.onBehalfOfContentOwner,
        &args.pageSize,
        &args.pageToken,
        &args.startTimeAtOrAfter,
        &args.startTimeBefore,
    )?;
    youtubereporting_jobs_reports_list_execute(builder)
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_media_download_execute()` to send, or `youtubereporting_media_download` for simplest API.

pub fn youtubereporting_media_download_builder(
    client: &SimpleHttpClient,
    resourceName: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/media/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_media_download_execute()` or `youtubereporting_media_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_media_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GdataMedia>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GdataMedia = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_media_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_media_download_task()`.
/// For the simplest API, use `youtubereporting_media_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_media_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GdataMedia>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtubereporting_media_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_media_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingMediaDownloadArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// GET v1/media/{mediaId}
/// Method for media download. Download is supported on the URI /v1/`media/{`+name}?alt=media.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_media_download_builder()` + `youtubereporting_media_download_execute()`.
/// For task-level control, use `youtubereporting_media_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_media_download(
    client: &SimpleHttpClient,
    args: &YoutubereportingMediaDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GdataMedia>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtubereporting_media_download_builder(client, &args.resourceName)?;
    youtubereporting_media_download_execute(builder)
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtubereporting_report_types_list_execute()` to send, or `youtubereporting_report_types_list` for simplest API.

pub fn youtubereporting_report_types_list_builder(
    client: &SimpleHttpClient,
    includeSystemManaged: &Option<bool>,
    onBehalfOfContentOwner: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/reportTypes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeSystemManaged.as_ref() {
        query_parts.push(format!("includeSystemManaged={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner.as_ref() {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtubereporting_report_types_list_execute()` or `youtubereporting_report_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_report_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_report_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportTypesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtubereporting_report_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtubereporting_report_types_list_task()`.
/// For the simplest API, use `youtubereporting_report_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtubereporting_report_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtubereporting_report_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtubereporting_report_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtubereporting_report_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubereportingReportTypesListArgs {
    /// Query parameter: includeSystemManaged
    pub includeSystemManaged: Option<bool>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/reportTypes
/// Lists report types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtubereporting_report_types_list_builder()` + `youtubereporting_report_types_list_execute()`.
/// For task-level control, use `youtubereporting_report_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtubereporting_report_types_list(
    client: &SimpleHttpClient,
    args: &YoutubereportingReportTypesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportTypesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtubereporting_report_types_list_builder(
        client,
        &args.includeSystemManaged,
        &args.onBehalfOfContentOwner,
        &args.pageSize,
        &args.pageToken,
    )?;
    youtubereporting_report_types_list_execute(builder)
}
