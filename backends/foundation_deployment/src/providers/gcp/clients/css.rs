//! Auto-generated API clients for gcp/css.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accounts/{accountsId}
/// Retrieves a single CSS/MC account by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `css_accounts_get_execute()` to send, or `css_accounts_get` for simplest API.

pub fn css_accounts_get_builder(
    client: &SimpleHttpClient,
    name: String,
    parent: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://css.googleapis.com/v1/accounts/{}",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}
/// Retrieves a single CSS/MC account by ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `css_accounts_get_execute()` or `css_accounts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}
/// Retrieves a single CSS/MC account by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `css_accounts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `css_accounts_get_task()`.
/// For the simplest API, use `css_accounts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn css_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = css_accounts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`css_accounts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: parent
    pub parent: Option<String>,
}

/// GET v1/accounts/{accountsId}
/// Retrieves a single CSS/MC account by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `css_accounts_get_builder()` + `css_accounts_get_execute()`.
/// For task-level control, use `css_accounts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_get(
    client: &SimpleHttpClient,
    args: &CssAccountsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = css_accounts_get_builder(client, args.name.clone(), args.parent.clone())?;
    css_accounts_get_execute(builder)
}

/// GET v1/accounts/{accountsId}:listChildAccounts
/// Lists all the accounts under the specified CSS account ID, and optionally filters by label ID and account name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `css_accounts_list_child_accounts_execute()` to send, or `css_accounts_list_child_accounts` for simplest API.

pub fn css_accounts_list_child_accounts_builder(
    client: &SimpleHttpClient,
    parent: String,
    fullName: Option<String>,
    labelId: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://css.googleapis.com/v1/accounts/{}:listChildAccounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fullName {
        query_parts.push(format!("fullName={}", val));
    }
    if let Some(val) = labelId {
        query_parts.push(format!("labelId={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}:listChildAccounts
/// Lists all the accounts under the specified CSS account ID, and optionally filters by label ID and account name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `css_accounts_list_child_accounts_execute()` or `css_accounts_list_child_accounts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_list_child_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_list_child_accounts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListChildAccountsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListChildAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:listChildAccounts
/// Lists all the accounts under the specified CSS account ID, and optionally filters by label ID and account name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `css_accounts_list_child_accounts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `css_accounts_list_child_accounts_task()`.
/// For the simplest API, use `css_accounts_list_child_accounts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_list_child_accounts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn css_accounts_list_child_accounts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChildAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = css_accounts_list_child_accounts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`css_accounts_list_child_accounts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsListChildAccountsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: fullName
    pub fullName: Option<String>,
    /// Query parameter: labelId
    pub labelId: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/accounts/{accountsId}:listChildAccounts
/// Lists all the accounts under the specified CSS account ID, and optionally filters by label ID and account name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `css_accounts_list_child_accounts_builder()` + `css_accounts_list_child_accounts_execute()`.
/// For task-level control, use `css_accounts_list_child_accounts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_list_child_accounts(
    client: &SimpleHttpClient,
    args: &CssAccountsListChildAccountsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChildAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = css_accounts_list_child_accounts_builder(
        client,
        args.parent.clone(),
        args.fullName.clone(),
        args.labelId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    css_accounts_list_child_accounts_execute(builder)
}

/// GET v1/accounts/{accountsId}:updateLabels
/// Updates labels assigned to CSS/MC accounts by a CSS domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `css_accounts_update_labels_execute()` to send, or `css_accounts_update_labels` for simplest API.

pub fn css_accounts_update_labels_builder(
    client: &SimpleHttpClient,
    name: String,
    body: &UpdateAccountLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://css.googleapis.com/v1/accounts/{}:updateLabels",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}:updateLabels
/// Updates labels assigned to CSS/MC accounts by a CSS domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `css_accounts_update_labels_execute()` or `css_accounts_update_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_update_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_update_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}:updateLabels
/// Updates labels assigned to CSS/MC accounts by a CSS domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `css_accounts_update_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `css_accounts_update_labels_task()`.
/// For the simplest API, use `css_accounts_update_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_update_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn css_accounts_update_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = css_accounts_update_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`css_accounts_update_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsUpdateLabelsArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UpdateAccountLabelsRequest,
}

/// GET v1/accounts/{accountsId}:updateLabels
/// Updates labels assigned to CSS/MC accounts by a CSS domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `css_accounts_update_labels_builder()` + `css_accounts_update_labels_execute()`.
/// For task-level control, use `css_accounts_update_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_update_labels(
    client: &SimpleHttpClient,
    args: &CssAccountsUpdateLabelsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = css_accounts_update_labels_builder(client, args.name.clone(), &args.body)?;
    css_accounts_update_labels_execute(builder)
}

/// GET v1/accounts/{accountsId}/cssProductInputs:insert
/// Uploads a CssProductInput to your CSS Center account. If an input with the same `contentLanguage`, identity, `feedLabel` and `feedId` already exists, this method replaces that entry. After inserting, updating, or deleting a CSS Product input, it may take several minutes before the processed CSS Product can be retrieved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `css_accounts_css_product_inputs_insert_execute()` to send, or `css_accounts_css_product_inputs_insert` for simplest API.

pub fn css_accounts_css_product_inputs_insert_builder(
    client: &SimpleHttpClient,
    parent: String,
    feedId: Option<String>,
    body: &CssProductInput,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://css.googleapis.com/v1/accounts/{}/cssProductInputs:insert",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = feedId {
        query_parts.push(format!("feedId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/cssProductInputs:insert
/// Uploads a CssProductInput to your CSS Center account. If an input with the same `contentLanguage`, identity, `feedLabel` and `feedId` already exists, this method replaces that entry. After inserting, updating, or deleting a CSS Product input, it may take several minutes before the processed CSS Product can be retrieved.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `css_accounts_css_product_inputs_insert_execute()` or `css_accounts_css_product_inputs_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_css_product_inputs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_css_product_inputs_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CssProductInput>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CssProductInput = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/cssProductInputs:insert
/// Uploads a CssProductInput to your CSS Center account. If an input with the same `contentLanguage`, identity, `feedLabel` and `feedId` already exists, this method replaces that entry. After inserting, updating, or deleting a CSS Product input, it may take several minutes before the processed CSS Product can be retrieved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `css_accounts_css_product_inputs_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `css_accounts_css_product_inputs_insert_task()`.
/// For the simplest API, use `css_accounts_css_product_inputs_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_css_product_inputs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn css_accounts_css_product_inputs_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CssProductInput>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = css_accounts_css_product_inputs_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`css_accounts_css_product_inputs_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsCssProductInputsInsertArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: feedId
    pub feedId: Option<String>,
    /// Request body.
    pub body: CssProductInput,
}

/// GET v1/accounts/{accountsId}/cssProductInputs:insert
/// Uploads a CssProductInput to your CSS Center account. If an input with the same `contentLanguage`, identity, `feedLabel` and `feedId` already exists, this method replaces that entry. After inserting, updating, or deleting a CSS Product input, it may take several minutes before the processed CSS Product can be retrieved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `css_accounts_css_product_inputs_insert_builder()` + `css_accounts_css_product_inputs_insert_execute()`.
/// For task-level control, use `css_accounts_css_product_inputs_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_css_product_inputs_insert(
    client: &SimpleHttpClient,
    args: &CssAccountsCssProductInputsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CssProductInput>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = css_accounts_css_product_inputs_insert_builder(
        client,
        args.parent.clone(),
        args.feedId.clone(),
        &args.body,
    )?;
    css_accounts_css_product_inputs_insert_execute(builder)
}

/// GET v1/accounts/{accountsId}/cssProducts
/// Lists the processed CSS Products in your CSS Center account. The response might contain fewer items than specified by `pageSize`. Rely on `pageToken` to determine if there are more items to be requested. After inserting, updating, or deleting a CSS product input, it may take several minutes before the updated processed CSS product can be retrieved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `css_accounts_css_products_list_execute()` to send, or `css_accounts_css_products_list` for simplest API.

pub fn css_accounts_css_products_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://css.googleapis.com/v1/accounts/{}/cssProducts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/cssProducts
/// Lists the processed CSS Products in your CSS Center account. The response might contain fewer items than specified by `pageSize`. Rely on `pageToken` to determine if there are more items to be requested. After inserting, updating, or deleting a CSS product input, it may take several minutes before the updated processed CSS product can be retrieved.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `css_accounts_css_products_list_execute()` or `css_accounts_css_products_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_css_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_css_products_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCssProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCssProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/cssProducts
/// Lists the processed CSS Products in your CSS Center account. The response might contain fewer items than specified by `pageSize`. Rely on `pageToken` to determine if there are more items to be requested. After inserting, updating, or deleting a CSS product input, it may take several minutes before the updated processed CSS product can be retrieved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `css_accounts_css_products_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `css_accounts_css_products_list_task()`.
/// For the simplest API, use `css_accounts_css_products_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_css_products_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn css_accounts_css_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCssProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = css_accounts_css_products_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`css_accounts_css_products_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsCssProductsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/accounts/{accountsId}/cssProducts
/// Lists the processed CSS Products in your CSS Center account. The response might contain fewer items than specified by `pageSize`. Rely on `pageToken` to determine if there are more items to be requested. After inserting, updating, or deleting a CSS product input, it may take several minutes before the updated processed CSS product can be retrieved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `css_accounts_css_products_list_builder()` + `css_accounts_css_products_list_execute()`.
/// For task-level control, use `css_accounts_css_products_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_css_products_list(
    client: &SimpleHttpClient,
    args: &CssAccountsCssProductsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCssProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = css_accounts_css_products_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    css_accounts_css_products_list_execute(builder)
}

/// GET v1/accounts/{accountsId}/labels
/// Creates a new label, not assigned to any account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `css_accounts_labels_create_execute()` to send, or `css_accounts_labels_create` for simplest API.

pub fn css_accounts_labels_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &AccountLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://css.googleapis.com/v1/accounts/{}/labels",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/labels
/// Creates a new label, not assigned to any account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `css_accounts_labels_create_execute()` or `css_accounts_labels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_labels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountLabel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccountLabel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/labels
/// Creates a new label, not assigned to any account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `css_accounts_labels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `css_accounts_labels_create_task()`.
/// For the simplest API, use `css_accounts_labels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn css_accounts_labels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountLabel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = css_accounts_labels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`css_accounts_labels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsLabelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AccountLabel,
}

/// GET v1/accounts/{accountsId}/labels
/// Creates a new label, not assigned to any account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `css_accounts_labels_create_builder()` + `css_accounts_labels_create_execute()`.
/// For task-level control, use `css_accounts_labels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_labels_create(
    client: &SimpleHttpClient,
    args: &CssAccountsLabelsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccountLabel>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = css_accounts_labels_create_builder(client, args.parent.clone(), &args.body)?;
    css_accounts_labels_create_execute(builder)
}

/// GET v1/accounts/{accountsId}/quotas
/// Lists the daily call quota and usage per group for your CSS Center account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `css_accounts_quotas_list_execute()` to send, or `css_accounts_quotas_list` for simplest API.

pub fn css_accounts_quotas_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://css.googleapis.com/v1/accounts/{}/quotas",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/quotas
/// Lists the daily call quota and usage per group for your CSS Center account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `css_accounts_quotas_list_execute()` or `css_accounts_quotas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_quotas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_quotas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListQuotaGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListQuotaGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/quotas
/// Lists the daily call quota and usage per group for your CSS Center account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `css_accounts_quotas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `css_accounts_quotas_list_task()`.
/// For the simplest API, use `css_accounts_quotas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `css_accounts_quotas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn css_accounts_quotas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQuotaGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = css_accounts_quotas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`css_accounts_quotas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsQuotasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/accounts/{accountsId}/quotas
/// Lists the daily call quota and usage per group for your CSS Center account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `css_accounts_quotas_list_builder()` + `css_accounts_quotas_list_execute()`.
/// For task-level control, use `css_accounts_quotas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn css_accounts_quotas_list(
    client: &SimpleHttpClient,
    args: &CssAccountsQuotasListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQuotaGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = css_accounts_quotas_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    css_accounts_quotas_list_execute(builder)
}
