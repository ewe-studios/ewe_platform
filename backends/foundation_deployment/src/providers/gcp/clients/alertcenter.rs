//! Auto-generated API clients for gcp/alertcenter.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta1/alerts:batchDelete
/// Performs batch delete operation on alerts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_batch_delete_execute()` to send, or `alertcenter_alerts_batch_delete` for simplest API.

pub fn alertcenter_alerts_batch_delete_builder(
    client: &SimpleHttpClient,
    body: &BatchDeleteAlertsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://alertcenter.googleapis.com/v1beta1/alerts:batchDelete",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/alerts:batchDelete
/// Performs batch delete operation on alerts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_batch_delete_execute()` or `alertcenter_alerts_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BatchDeleteAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchDeleteAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts:batchDelete
/// Performs batch delete operation on alerts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_batch_delete_task()`.
/// For the simplest API, use `alertcenter_alerts_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchDeleteAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsBatchDeleteArgs {
    /// Request body.
    pub body: BatchDeleteAlertsRequest,
}

/// GET v1beta1/alerts:batchDelete
/// Performs batch delete operation on alerts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_batch_delete_builder()` + `alertcenter_alerts_batch_delete_execute()`.
/// For task-level control, use `alertcenter_alerts_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_batch_delete(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchDeleteAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = alertcenter_alerts_batch_delete_builder(client, &args.body)?;
    alertcenter_alerts_batch_delete_execute(builder)
}

/// GET v1beta1/alerts:batchUndelete
/// Performs batch undelete operation on alerts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_batch_undelete_execute()` to send, or `alertcenter_alerts_batch_undelete` for simplest API.

pub fn alertcenter_alerts_batch_undelete_builder(
    client: &SimpleHttpClient,
    body: &BatchUndeleteAlertsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://alertcenter.googleapis.com/v1beta1/alerts:batchUndelete",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/alerts:batchUndelete
/// Performs batch undelete operation on alerts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_batch_undelete_execute()` or `alertcenter_alerts_batch_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_batch_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_batch_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<BatchUndeleteAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUndeleteAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts:batchUndelete
/// Performs batch undelete operation on alerts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_batch_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_batch_undelete_task()`.
/// For the simplest API, use `alertcenter_alerts_batch_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_batch_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_batch_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUndeleteAlertsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_batch_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_batch_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsBatchUndeleteArgs {
    /// Request body.
    pub body: BatchUndeleteAlertsRequest,
}

/// GET v1beta1/alerts:batchUndelete
/// Performs batch undelete operation on alerts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_batch_undelete_builder()` + `alertcenter_alerts_batch_undelete_execute()`.
/// For task-level control, use `alertcenter_alerts_batch_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_batch_undelete(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsBatchUndeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUndeleteAlertsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = alertcenter_alerts_batch_undelete_builder(client, &args.body)?;
    alertcenter_alerts_batch_undelete_execute(builder)
}

/// GET v1beta1/alerts/{alertId}
/// Marks the specified alert for deletion. An alert that has been marked for deletion is removed from Alert Center after 30 days. Marking an alert for deletion has no effect on an alert which has already been marked for deletion. Attempting to mark a nonexistent alert for deletion results in a NOT_FOUND error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_delete_execute()` to send, or `alertcenter_alerts_delete` for simplest API.

pub fn alertcenter_alerts_delete_builder(
    client: &SimpleHttpClient,
    alertId: &str,
    customerId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://alertcenter.googleapis.com/v1beta1/alerts/{}",
        alertId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/alerts/{alertId}
/// Marks the specified alert for deletion. An alert that has been marked for deletion is removed from Alert Center after 30 days. Marking an alert for deletion has no effect on an alert which has already been marked for deletion. Attempting to mark a nonexistent alert for deletion results in a NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_delete_execute()` or `alertcenter_alerts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts/{alertId}
/// Marks the specified alert for deletion. An alert that has been marked for deletion is removed from Alert Center after 30 days. Marking an alert for deletion has no effect on an alert which has already been marked for deletion. Attempting to mark a nonexistent alert for deletion results in a NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_delete_task()`.
/// For the simplest API, use `alertcenter_alerts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsDeleteArgs {
    /// Path parameter: alertId
    pub alertId: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
}

/// GET v1beta1/alerts/{alertId}
/// Marks the specified alert for deletion. An alert that has been marked for deletion is removed from Alert Center after 30 days. Marking an alert for deletion has no effect on an alert which has already been marked for deletion. Attempting to mark a nonexistent alert for deletion results in a NOT_FOUND error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_delete_builder()` + `alertcenter_alerts_delete_execute()`.
/// For task-level control, use `alertcenter_alerts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_delete(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        alertcenter_alerts_delete_builder(client, &args.alertId, args.customerId.as_deref())?;
    alertcenter_alerts_delete_execute(builder)
}

/// GET v1beta1/alerts/{alertId}
/// Gets the specified alert. Attempting to get a nonexistent alert returns NOT_FOUND error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_get_execute()` to send, or `alertcenter_alerts_get` for simplest API.

pub fn alertcenter_alerts_get_builder(
    client: &SimpleHttpClient,
    alertId: &str,
    customerId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://alertcenter.googleapis.com/v1beta1/alerts/{}",
        alertId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/alerts/{alertId}
/// Gets the specified alert. Attempting to get a nonexistent alert returns NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_get_execute()` or `alertcenter_alerts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts/{alertId}
/// Gets the specified alert. Attempting to get a nonexistent alert returns NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_get_task()`.
/// For the simplest API, use `alertcenter_alerts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsGetArgs {
    /// Path parameter: alertId
    pub alertId: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
}

/// GET v1beta1/alerts/{alertId}
/// Gets the specified alert. Attempting to get a nonexistent alert returns NOT_FOUND error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_get_builder()` + `alertcenter_alerts_get_execute()`.
/// For task-level control, use `alertcenter_alerts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_get(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        alertcenter_alerts_get_builder(client, &args.alertId, args.customerId.as_deref())?;
    alertcenter_alerts_get_execute(builder)
}

/// GET v1beta1/alerts/{alertId}/metadata
/// Returns the metadata of an alert. Attempting to get metadata for a non-existent alert returns NOT_FOUND error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_get_metadata_execute()` to send, or `alertcenter_alerts_get_metadata` for simplest API.

pub fn alertcenter_alerts_get_metadata_builder(
    client: &SimpleHttpClient,
    alertId: &str,
    customerId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://alertcenter.googleapis.com/v1beta1/alerts/{}/metadata",
        alertId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/alerts/{alertId}/metadata
/// Returns the metadata of an alert. Attempting to get metadata for a non-existent alert returns NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_get_metadata_execute()` or `alertcenter_alerts_get_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_get_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_get_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AlertMetadata>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertMetadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts/{alertId}/metadata
/// Returns the metadata of an alert. Attempting to get metadata for a non-existent alert returns NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_get_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_get_metadata_task()`.
/// For the simplest API, use `alertcenter_alerts_get_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_get_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_get_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_get_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_get_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsGetMetadataArgs {
    /// Path parameter: alertId
    pub alertId: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
}

/// GET v1beta1/alerts/{alertId}/metadata
/// Returns the metadata of an alert. Attempting to get metadata for a non-existent alert returns NOT_FOUND error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_get_metadata_builder()` + `alertcenter_alerts_get_metadata_execute()`.
/// For task-level control, use `alertcenter_alerts_get_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_get_metadata(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsGetMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        alertcenter_alerts_get_metadata_builder(client, &args.alertId, args.customerId.as_deref())?;
    alertcenter_alerts_get_metadata_execute(builder)
}

/// GET v1beta1/alerts
/// Lists the alerts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_list_execute()` to send, or `alertcenter_alerts_list` for simplest API.

pub fn alertcenter_alerts_list_builder(
    client: &SimpleHttpClient,
    customerId: Option<&str>,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://alertcenter.googleapis.com/v1beta1/alerts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/alerts
/// Lists the alerts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_list_execute()` or `alertcenter_alerts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts
/// Lists the alerts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_list_task()`.
/// For the simplest API, use `alertcenter_alerts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsListArgs {
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/alerts
/// Lists the alerts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_list_builder()` + `alertcenter_alerts_list_execute()`.
/// For task-level control, use `alertcenter_alerts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_list(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = alertcenter_alerts_list_builder(
        client,
        args.customerId.as_deref(),
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    alertcenter_alerts_list_execute(builder)
}

/// GET v1beta1/alerts/{alertId}:undelete
/// Restores, or "undeletes", an alert that was marked for deletion within the past 30 days. Attempting to undelete an alert which was marked for deletion over 30 days ago (which has been removed from the Alert Center database) or a nonexistent alert returns a NOT_FOUND error. Attempting to undelete an alert which has not been marked for deletion has no effect.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_undelete_execute()` to send, or `alertcenter_alerts_undelete` for simplest API.

pub fn alertcenter_alerts_undelete_builder(
    client: &SimpleHttpClient,
    alertId: &str,
    body: &UndeleteAlertRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://alertcenter.googleapis.com/v1beta1/alerts/{}:undelete",
        alertId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/alerts/{alertId}:undelete
/// Restores, or "undeletes", an alert that was marked for deletion within the past 30 days. Attempting to undelete an alert which was marked for deletion over 30 days ago (which has been removed from the Alert Center database) or a nonexistent alert returns a NOT_FOUND error. Attempting to undelete an alert which has not been marked for deletion has no effect.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_undelete_execute()` or `alertcenter_alerts_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts/{alertId}:undelete
/// Restores, or "undeletes", an alert that was marked for deletion within the past 30 days. Attempting to undelete an alert which was marked for deletion over 30 days ago (which has been removed from the Alert Center database) or a nonexistent alert returns a NOT_FOUND error. Attempting to undelete an alert which has not been marked for deletion has no effect.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_undelete_task()`.
/// For the simplest API, use `alertcenter_alerts_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsUndeleteArgs {
    /// Path parameter: alertId
    pub alertId: String,
    /// Request body.
    pub body: UndeleteAlertRequest,
}

/// GET v1beta1/alerts/{alertId}:undelete
/// Restores, or "undeletes", an alert that was marked for deletion within the past 30 days. Attempting to undelete an alert which was marked for deletion over 30 days ago (which has been removed from the Alert Center database) or a nonexistent alert returns a NOT_FOUND error. Attempting to undelete an alert which has not been marked for deletion has no effect.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_undelete_builder()` + `alertcenter_alerts_undelete_execute()`.
/// For task-level control, use `alertcenter_alerts_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_undelete(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = alertcenter_alerts_undelete_builder(client, &args.alertId, &args.body)?;
    alertcenter_alerts_undelete_execute(builder)
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Creates new feedback for an alert. Attempting to create a feedback for a non-existent alert returns NOT_FOUND error. Attempting to create a feedback for an alert that is marked for deletion returns FAILED_PRECONDITION' error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_feedback_create_execute()` to send, or `alertcenter_alerts_feedback_create` for simplest API.

pub fn alertcenter_alerts_feedback_create_builder(
    client: &SimpleHttpClient,
    alertId: &str,
    customerId: Option<&str>,
    body: &AlertFeedback,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://alertcenter.googleapis.com/v1beta1/alerts/{}/feedback",
        alertId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Creates new feedback for an alert. Attempting to create a feedback for a non-existent alert returns NOT_FOUND error. Attempting to create a feedback for an alert that is marked for deletion returns FAILED_PRECONDITION' error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_feedback_create_execute()` or `alertcenter_alerts_feedback_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_feedback_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_feedback_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AlertFeedback>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertFeedback = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Creates new feedback for an alert. Attempting to create a feedback for a non-existent alert returns NOT_FOUND error. Attempting to create a feedback for an alert that is marked for deletion returns FAILED_PRECONDITION' error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_feedback_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_feedback_create_task()`.
/// For the simplest API, use `alertcenter_alerts_feedback_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_feedback_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_feedback_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertFeedback>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_feedback_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_feedback_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsFeedbackCreateArgs {
    /// Path parameter: alertId
    pub alertId: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Request body.
    pub body: AlertFeedback,
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Creates new feedback for an alert. Attempting to create a feedback for a non-existent alert returns NOT_FOUND error. Attempting to create a feedback for an alert that is marked for deletion returns FAILED_PRECONDITION' error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_feedback_create_builder()` + `alertcenter_alerts_feedback_create_execute()`.
/// For task-level control, use `alertcenter_alerts_feedback_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_feedback_create(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsFeedbackCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertFeedback>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = alertcenter_alerts_feedback_create_builder(
        client,
        &args.alertId,
        args.customerId.as_deref(),
        &args.body,
    )?;
    alertcenter_alerts_feedback_create_execute(builder)
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Lists all the feedback for an alert. Attempting to list feedbacks for a non-existent alert returns NOT_FOUND error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_alerts_feedback_list_execute()` to send, or `alertcenter_alerts_feedback_list` for simplest API.

pub fn alertcenter_alerts_feedback_list_builder(
    client: &SimpleHttpClient,
    alertId: &str,
    customerId: Option<&str>,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://alertcenter.googleapis.com/v1beta1/alerts/{}/feedback",
        alertId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Lists all the feedback for an alert. Attempting to list feedbacks for a non-existent alert returns NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_alerts_feedback_list_execute()` or `alertcenter_alerts_feedback_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_feedback_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_feedback_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAlertFeedbackResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertFeedbackResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Lists all the feedback for an alert. Attempting to list feedbacks for a non-existent alert returns NOT_FOUND error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_alerts_feedback_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_alerts_feedback_list_task()`.
/// For the simplest API, use `alertcenter_alerts_feedback_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_alerts_feedback_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_alerts_feedback_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertFeedbackResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = alertcenter_alerts_feedback_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_alerts_feedback_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterAlertsFeedbackListArgs {
    /// Path parameter: alertId
    pub alertId: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET v1beta1/alerts/{alertId}/feedback
/// Lists all the feedback for an alert. Attempting to list feedbacks for a non-existent alert returns NOT_FOUND error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_alerts_feedback_list_builder()` + `alertcenter_alerts_feedback_list_execute()`.
/// For task-level control, use `alertcenter_alerts_feedback_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_alerts_feedback_list(
    client: &SimpleHttpClient,
    args: &AlertcenterAlertsFeedbackListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertFeedbackResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = alertcenter_alerts_feedback_list_builder(
        client,
        &args.alertId,
        args.customerId.as_deref(),
        args.filter.as_deref(),
    )?;
    alertcenter_alerts_feedback_list_execute(builder)
}

/// GET v1beta1/settings
/// Returns customer-level settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_get_settings_execute()` to send, or `alertcenter_get_settings` for simplest API.

pub fn alertcenter_get_settings_builder(
    client: &SimpleHttpClient,
    customerId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://alertcenter.googleapis.com/v1beta1/settings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/settings
/// Returns customer-level settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_get_settings_execute()` or `alertcenter_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/settings
/// Returns customer-level settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_get_settings_task()`.
/// For the simplest API, use `alertcenter_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = alertcenter_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterGetSettingsArgs {
    /// Query parameter: customerId
    pub customerId: Option<String>,
}

/// GET v1beta1/settings
/// Returns customer-level settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_get_settings_builder()` + `alertcenter_get_settings_execute()`.
/// For task-level control, use `alertcenter_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_get_settings(
    client: &SimpleHttpClient,
    args: &AlertcenterGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = alertcenter_get_settings_builder(client, args.customerId.as_deref())?;
    alertcenter_get_settings_execute(builder)
}

/// GET v1beta1/settings
/// Updates the customer-level settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `alertcenter_update_settings_execute()` to send, or `alertcenter_update_settings` for simplest API.

pub fn alertcenter_update_settings_builder(
    client: &SimpleHttpClient,
    customerId: Option<&str>,
    body: &Settings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://alertcenter.googleapis.com/v1beta1/settings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/settings
/// Updates the customer-level settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `alertcenter_update_settings_execute()` or `alertcenter_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/settings
/// Updates the customer-level settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `alertcenter_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `alertcenter_update_settings_task()`.
/// For the simplest API, use `alertcenter_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `alertcenter_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn alertcenter_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = alertcenter_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`alertcenter_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AlertcenterUpdateSettingsArgs {
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Request body.
    pub body: Settings,
}

/// GET v1beta1/settings
/// Updates the customer-level settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `alertcenter_update_settings_builder()` + `alertcenter_update_settings_execute()`.
/// For task-level control, use `alertcenter_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn alertcenter_update_settings(
    client: &SimpleHttpClient,
    args: &AlertcenterUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        alertcenter_update_settings_builder(client, args.customerId.as_deref(), &args.body)?;
    alertcenter_update_settings_execute(builder)
}
