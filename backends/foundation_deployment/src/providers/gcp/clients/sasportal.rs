//! Auto-generated API clients for gcp/sasportal.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/sasportal`

#![cfg(feature = "gcp/sasportal")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_get_execute()` to send, or `sasportal_customers_get` for simplest API.

pub fn sasportal_customers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalCustomer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_get_builder()` + `sasportal_customers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_get(
    client: &SimpleHttpClient,
    args: &SasportalCustomersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_get_builder(client, &args.name)?;
    sasportal_customers_get_execute(builder)
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_list_execute()` to send, or `sasportal_customers_list` for simplest API.

pub fn sasportal_customers_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/customers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_list_builder()` + `sasportal_customers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_customers_list_builder(client, args.pageSize, args.pageToken.as_deref())?;
    sasportal_customers_list_execute(builder)
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_list_gcp_project_deployments_execute()` to send, or `sasportal_customers_list_gcp_project_deployments` for simplest API.

pub fn sasportal_customers_list_gcp_project_deployments_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://sasportal.googleapis.com/v1alpha1/customers:listGcpProjectDeployments",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_list_gcp_project_deployments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_list_gcp_project_deployments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_list_gcp_project_deployments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListGcpProjectDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListGcpProjectDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_list_gcp_project_deployments_builder()` + `sasportal_customers_list_gcp_project_deployments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_list_gcp_project_deployments(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListGcpProjectDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_list_gcp_project_deployments_builder(client)?;
    sasportal_customers_list_gcp_project_deployments_execute(builder)
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_list_legacy_organizations_execute()` to send, or `sasportal_customers_list_legacy_organizations` for simplest API.

pub fn sasportal_customers_list_legacy_organizations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://sasportal.googleapis.com/v1alpha1/customers:listLegacyOrganizations",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_list_legacy_organizations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_list_legacy_organizations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_list_legacy_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListLegacyOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListLegacyOrganizationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_list_legacy_organizations_builder()` + `sasportal_customers_list_legacy_organizations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_list_legacy_organizations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListLegacyOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_list_legacy_organizations_builder(client)?;
    sasportal_customers_list_legacy_organizations_execute(builder)
}

/// GET v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_migrate_organization_execute()` to send, or `sasportal_customers_migrate_organization` for simplest API.

pub fn sasportal_customers_migrate_organization_builder(
    client: &SimpleHttpClient,
    body: &SasPortalMigrateOrganizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/customers:migrateOrganization",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_migrate_organization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_migrate_organization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_migrate_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_migrate_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersMigrateOrganizationArgs {
    /// Request body.
    pub body: SasPortalMigrateOrganizationRequest,
}

/// GET v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_migrate_organization_builder()` + `sasportal_customers_migrate_organization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_migrate_organization(
    client: &SimpleHttpClient,
    args: &SasportalCustomersMigrateOrganizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_migrate_organization_builder(client, &args.body)?;
    sasportal_customers_migrate_organization_execute(builder)
}

/// GET v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_patch_execute()` to send, or `sasportal_customers_patch` for simplest API.

pub fn sasportal_customers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalCustomer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalCustomer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalCustomer,
}

/// GET v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_patch_builder()` + `sasportal_customers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_patch(
    client: &SimpleHttpClient,
    args: &SasportalCustomersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_customers_patch_execute(builder)
}

/// GET v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_provision_deployment_execute()` to send, or `sasportal_customers_provision_deployment` for simplest API.

pub fn sasportal_customers_provision_deployment_builder(
    client: &SimpleHttpClient,
    body: &SasPortalProvisionDeploymentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/customers:provisionDeployment",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_provision_deployment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_provision_deployment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_provision_deployment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalProvisionDeploymentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalProvisionDeploymentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_provision_deployment`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersProvisionDeploymentArgs {
    /// Request body.
    pub body: SasPortalProvisionDeploymentRequest,
}

/// GET v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_provision_deployment_builder()` + `sasportal_customers_provision_deployment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_provision_deployment(
    client: &SimpleHttpClient,
    args: &SasportalCustomersProvisionDeploymentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalProvisionDeploymentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_provision_deployment_builder(client, &args.body)?;
    sasportal_customers_provision_deployment_execute(builder)
}

/// GET v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_setup_sas_analytics_execute()` to send, or `sasportal_customers_setup_sas_analytics` for simplest API.

pub fn sasportal_customers_setup_sas_analytics_builder(
    client: &SimpleHttpClient,
    body: &SasPortalSetupSasAnalyticsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/customers:setupSasAnalytics",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_setup_sas_analytics_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_setup_sas_analytics_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_setup_sas_analytics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_setup_sas_analytics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersSetupSasAnalyticsArgs {
    /// Request body.
    pub body: SasPortalSetupSasAnalyticsRequest,
}

/// GET v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_setup_sas_analytics_builder()` + `sasportal_customers_setup_sas_analytics_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_setup_sas_analytics(
    client: &SimpleHttpClient,
    args: &SasportalCustomersSetupSasAnalyticsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_setup_sas_analytics_builder(client, &args.body)?;
    sasportal_customers_setup_sas_analytics_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_create_execute()` to send, or `sasportal_customers_deployments_create` for simplest API.

pub fn sasportal_customers_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDeployment,
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_create_builder()` + `sasportal_customers_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_create(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_create_builder(client, &args.parent, &args.body)?;
    sasportal_customers_deployments_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_delete_execute()` to send, or `sasportal_customers_deployments_delete` for simplest API.

pub fn sasportal_customers_deployments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_delete_builder()` + `sasportal_customers_deployments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_delete(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_delete_builder(client, &args.name)?;
    sasportal_customers_deployments_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_get_execute()` to send, or `sasportal_customers_deployments_get` for simplest API.

pub fn sasportal_customers_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_get_builder()` + `sasportal_customers_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_get(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_get_builder(client, &args.name)?;
    sasportal_customers_deployments_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_list_execute()` to send, or `sasportal_customers_deployments_list` for simplest API.

pub fn sasportal_customers_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_list_builder()` + `sasportal_customers_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_customers_deployments_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_move_execute()` to send, or `sasportal_customers_deployments_move` for simplest API.

pub fn sasportal_customers_deployments_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeploymentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalMoveDeploymentRequest,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_move_builder()` + `sasportal_customers_deployments_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_move(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_move_builder(client, &args.name, &args.body)?;
    sasportal_customers_deployments_move_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_patch_execute()` to send, or `sasportal_customers_deployments_patch` for simplest API.

pub fn sasportal_customers_deployments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalDeployment,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_patch_builder()` + `sasportal_customers_deployments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_patch(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_customers_deployments_patch_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_devices_create_execute()` to send, or `sasportal_customers_deployments_devices_create` for simplest API.

pub fn sasportal_customers_deployments_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_devices_create_builder()` + `sasportal_customers_deployments_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_devices_create(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_customers_deployments_devices_create_builder(client, &args.parent, &args.body)?;
    sasportal_customers_deployments_devices_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_devices_create_signed_execute()` to send, or `sasportal_customers_deployments_devices_create_signed` for simplest API.

pub fn sasportal_customers_deployments_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalCreateSignedDeviceRequest,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_devices_create_signed_builder()` + `sasportal_customers_deployments_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_devices_create_signed(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_devices_create_signed_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    sasportal_customers_deployments_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_deployments_devices_list_execute()` to send, or `sasportal_customers_deployments_devices_list` for simplest API.

pub fn sasportal_customers_deployments_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_deployments_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_deployments_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_deployments_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_deployments_devices_list_builder()` + `sasportal_customers_deployments_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_deployments_devices_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDeploymentsDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_deployments_devices_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_customers_deployments_devices_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_create_execute()` to send, or `sasportal_customers_devices_create` for simplest API.

pub fn sasportal_customers_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_create_builder()` + `sasportal_customers_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_create(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_devices_create_builder(client, &args.parent, &args.body)?;
    sasportal_customers_devices_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_create_signed_execute()` to send, or `sasportal_customers_devices_create_signed` for simplest API.

pub fn sasportal_customers_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalCreateSignedDeviceRequest,
}

/// GET v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_create_signed_builder()` + `sasportal_customers_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_create_signed(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_customers_devices_create_signed_builder(client, &args.parent, &args.body)?;
    sasportal_customers_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_delete_execute()` to send, or `sasportal_customers_devices_delete` for simplest API.

pub fn sasportal_customers_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_delete_builder()` + `sasportal_customers_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_delete(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_devices_delete_builder(client, &args.name)?;
    sasportal_customers_devices_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_get_execute()` to send, or `sasportal_customers_devices_get` for simplest API.

pub fn sasportal_customers_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_get_builder()` + `sasportal_customers_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_get(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_devices_get_builder(client, &args.name)?;
    sasportal_customers_devices_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_list_execute()` to send, or `sasportal_customers_devices_list` for simplest API.

pub fn sasportal_customers_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_list_builder()` + `sasportal_customers_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_devices_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_customers_devices_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_move_execute()` to send, or `sasportal_customers_devices_move` for simplest API.

pub fn sasportal_customers_devices_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalMoveDeviceRequest,
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_move_builder()` + `sasportal_customers_devices_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_move(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_devices_move_builder(client, &args.name, &args.body)?;
    sasportal_customers_devices_move_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_patch_execute()` to send, or `sasportal_customers_devices_patch` for simplest API.

pub fn sasportal_customers_devices_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_patch_builder()` + `sasportal_customers_devices_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_patch(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_devices_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_customers_devices_patch_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_sign_device_execute()` to send, or `sasportal_customers_devices_sign_device` for simplest API.

pub fn sasportal_customers_devices_sign_device_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_sign_device_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_sign_device_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_sign_device`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesSignDeviceArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalSignDeviceRequest,
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_sign_device_builder()` + `sasportal_customers_devices_sign_device_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_sign_device(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesSignDeviceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_devices_sign_device_builder(client, &args.name, &args.body)?;
    sasportal_customers_devices_sign_device_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_devices_update_signed_execute()` to send, or `sasportal_customers_devices_update_signed` for simplest API.

pub fn sasportal_customers_devices_update_signed_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_devices_update_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_devices_update_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_devices_update_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesUpdateSignedArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalUpdateSignedDeviceRequest,
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_devices_update_signed_builder()` + `sasportal_customers_devices_update_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_devices_update_signed(
    client: &SimpleHttpClient,
    args: &SasportalCustomersDevicesUpdateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_customers_devices_update_signed_builder(client, &args.name, &args.body)?;
    sasportal_customers_devices_update_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_create_execute()` to send, or `sasportal_customers_nodes_create` for simplest API.

pub fn sasportal_customers_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalNode,
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_create_builder()` + `sasportal_customers_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_create(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_create_builder(client, &args.parent, &args.body)?;
    sasportal_customers_nodes_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_delete_execute()` to send, or `sasportal_customers_nodes_delete` for simplest API.

pub fn sasportal_customers_nodes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_delete_builder()` + `sasportal_customers_nodes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_delete(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_delete_builder(client, &args.name)?;
    sasportal_customers_nodes_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_get_execute()` to send, or `sasportal_customers_nodes_get` for simplest API.

pub fn sasportal_customers_nodes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_get_builder()` + `sasportal_customers_nodes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_get(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_get_builder(client, &args.name)?;
    sasportal_customers_nodes_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_list_execute()` to send, or `sasportal_customers_nodes_list` for simplest API.

pub fn sasportal_customers_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_list_builder()` + `sasportal_customers_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_customers_nodes_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_move_execute()` to send, or `sasportal_customers_nodes_move` for simplest API.

pub fn sasportal_customers_nodes_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveNodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalMoveNodeRequest,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_move_builder()` + `sasportal_customers_nodes_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_move(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_move_builder(client, &args.name, &args.body)?;
    sasportal_customers_nodes_move_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_patch_execute()` to send, or `sasportal_customers_nodes_patch` for simplest API.

pub fn sasportal_customers_nodes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalNode,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_patch_builder()` + `sasportal_customers_nodes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_patch(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_customers_nodes_patch_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_deployments_create_execute()` to send, or `sasportal_customers_nodes_deployments_create` for simplest API.

pub fn sasportal_customers_nodes_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDeployment,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_deployments_create_builder()` + `sasportal_customers_nodes_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_deployments_create(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_customers_nodes_deployments_create_builder(client, &args.parent, &args.body)?;
    sasportal_customers_nodes_deployments_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_deployments_list_execute()` to send, or `sasportal_customers_nodes_deployments_list` for simplest API.

pub fn sasportal_customers_nodes_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_deployments_list_builder()` + `sasportal_customers_nodes_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_deployments_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_deployments_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_customers_nodes_deployments_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_devices_create_execute()` to send, or `sasportal_customers_nodes_devices_create` for simplest API.

pub fn sasportal_customers_nodes_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_devices_create_builder()` + `sasportal_customers_nodes_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_devices_create(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_customers_nodes_devices_create_builder(client, &args.parent, &args.body)?;
    sasportal_customers_nodes_devices_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_devices_create_signed_execute()` to send, or `sasportal_customers_nodes_devices_create_signed` for simplest API.

pub fn sasportal_customers_nodes_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalCreateSignedDeviceRequest,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_devices_create_signed_builder()` + `sasportal_customers_nodes_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_customers_nodes_devices_create_signed_builder(client, &args.parent, &args.body)?;
    sasportal_customers_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_devices_list_execute()` to send, or `sasportal_customers_nodes_devices_list` for simplest API.

pub fn sasportal_customers_nodes_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_devices_list_builder()` + `sasportal_customers_nodes_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_devices_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_devices_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_customers_nodes_devices_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_nodes_create_execute()` to send, or `sasportal_customers_nodes_nodes_create` for simplest API.

pub fn sasportal_customers_nodes_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalNode,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_nodes_create_builder()` + `sasportal_customers_nodes_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_nodes_create(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_nodes_create_builder(client, &args.parent, &args.body)?;
    sasportal_customers_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_customers_nodes_nodes_list_execute()` to send, or `sasportal_customers_nodes_nodes_list` for simplest API.

pub fn sasportal_customers_nodes_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_customers_nodes_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_customers_nodes_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_customers_nodes_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_customers_nodes_nodes_list_builder()` + `sasportal_customers_nodes_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_customers_nodes_nodes_list(
    client: &SimpleHttpClient,
    args: &SasportalCustomersNodesNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_customers_nodes_nodes_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_customers_nodes_nodes_list_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_deployments_get_execute()` to send, or `sasportal_deployments_get` for simplest API.

pub fn sasportal_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_deployments_get_builder()` + `sasportal_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_get(
    client: &SimpleHttpClient,
    args: &SasportalDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_deployments_get_builder(client, &args.name)?;
    sasportal_deployments_get_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_deployments_devices_delete_execute()` to send, or `sasportal_deployments_devices_delete` for simplest API.

pub fn sasportal_deployments_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_deployments_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_deployments_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_deployments_devices_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalDeploymentsDevicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_deployments_devices_delete_builder()` + `sasportal_deployments_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_delete(
    client: &SimpleHttpClient,
    args: &SasportalDeploymentsDevicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_deployments_devices_delete_builder(client, &args.name)?;
    sasportal_deployments_devices_delete_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_deployments_devices_get_execute()` to send, or `sasportal_deployments_devices_get` for simplest API.

pub fn sasportal_deployments_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_deployments_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_deployments_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_deployments_devices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalDeploymentsDevicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_deployments_devices_get_builder()` + `sasportal_deployments_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_get(
    client: &SimpleHttpClient,
    args: &SasportalDeploymentsDevicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_deployments_devices_get_builder(client, &args.name)?;
    sasportal_deployments_devices_get_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_deployments_devices_move_execute()` to send, or `sasportal_deployments_devices_move` for simplest API.

pub fn sasportal_deployments_devices_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_deployments_devices_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_deployments_devices_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_deployments_devices_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalDeploymentsDevicesMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalMoveDeviceRequest,
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_deployments_devices_move_builder()` + `sasportal_deployments_devices_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_move(
    client: &SimpleHttpClient,
    args: &SasportalDeploymentsDevicesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_deployments_devices_move_builder(client, &args.name, &args.body)?;
    sasportal_deployments_devices_move_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_deployments_devices_patch_execute()` to send, or `sasportal_deployments_devices_patch` for simplest API.

pub fn sasportal_deployments_devices_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_deployments_devices_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_deployments_devices_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_deployments_devices_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalDeploymentsDevicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_deployments_devices_patch_builder()` + `sasportal_deployments_devices_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_patch(
    client: &SimpleHttpClient,
    args: &SasportalDeploymentsDevicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_deployments_devices_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_deployments_devices_patch_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_deployments_devices_sign_device_execute()` to send, or `sasportal_deployments_devices_sign_device` for simplest API.

pub fn sasportal_deployments_devices_sign_device_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_deployments_devices_sign_device_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_deployments_devices_sign_device_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_deployments_devices_sign_device`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalDeploymentsDevicesSignDeviceArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalSignDeviceRequest,
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_deployments_devices_sign_device_builder()` + `sasportal_deployments_devices_sign_device_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_sign_device(
    client: &SimpleHttpClient,
    args: &SasportalDeploymentsDevicesSignDeviceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_deployments_devices_sign_device_builder(client, &args.name, &args.body)?;
    sasportal_deployments_devices_sign_device_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_deployments_devices_update_signed_execute()` to send, or `sasportal_deployments_devices_update_signed` for simplest API.

pub fn sasportal_deployments_devices_update_signed_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_deployments_devices_update_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_deployments_devices_update_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_deployments_devices_update_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalDeploymentsDevicesUpdateSignedArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalUpdateSignedDeviceRequest,
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_deployments_devices_update_signed_builder()` + `sasportal_deployments_devices_update_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_deployments_devices_update_signed(
    client: &SimpleHttpClient,
    args: &SasportalDeploymentsDevicesUpdateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_deployments_devices_update_signed_builder(client, &args.name, &args.body)?;
    sasportal_deployments_devices_update_signed_execute(builder)
}

/// GET v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_installer_generate_secret_execute()` to send, or `sasportal_installer_generate_secret` for simplest API.

pub fn sasportal_installer_generate_secret_builder(
    client: &SimpleHttpClient,
    body: &SasPortalGenerateSecretRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/installer:generateSecret",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_installer_generate_secret_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_installer_generate_secret_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_installer_generate_secret_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalGenerateSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalGenerateSecretResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_installer_generate_secret`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalInstallerGenerateSecretArgs {
    /// Request body.
    pub body: SasPortalGenerateSecretRequest,
}

/// GET v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_installer_generate_secret_builder()` + `sasportal_installer_generate_secret_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_installer_generate_secret(
    client: &SimpleHttpClient,
    args: &SasportalInstallerGenerateSecretArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalGenerateSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_installer_generate_secret_builder(client, &args.body)?;
    sasportal_installer_generate_secret_execute(builder)
}

/// GET v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_installer_validate_execute()` to send, or `sasportal_installer_validate` for simplest API.

pub fn sasportal_installer_validate_builder(
    client: &SimpleHttpClient,
    body: &SasPortalValidateInstallerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/installer:validate",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_installer_validate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_installer_validate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_installer_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalValidateInstallerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalValidateInstallerResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_installer_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalInstallerValidateArgs {
    /// Request body.
    pub body: SasPortalValidateInstallerRequest,
}

/// GET v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_installer_validate_builder()` + `sasportal_installer_validate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_installer_validate(
    client: &SimpleHttpClient,
    args: &SasportalInstallerValidateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalValidateInstallerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_installer_validate_builder(client, &args.body)?;
    sasportal_installer_validate_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_get_execute()` to send, or `sasportal_nodes_get` for simplest API.

pub fn sasportal_nodes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/nodes/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_get_builder()` + `sasportal_nodes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_get(
    client: &SimpleHttpClient,
    args: &SasportalNodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_get_builder(client, &args.name)?;
    sasportal_nodes_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_delete_execute()` to send, or `sasportal_nodes_deployments_delete` for simplest API.

pub fn sasportal_nodes_deployments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_delete_builder()` + `sasportal_nodes_deployments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_delete(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_deployments_delete_builder(client, &args.name)?;
    sasportal_nodes_deployments_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_get_execute()` to send, or `sasportal_nodes_deployments_get` for simplest API.

pub fn sasportal_nodes_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_get_builder()` + `sasportal_nodes_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_get(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_deployments_get_builder(client, &args.name)?;
    sasportal_nodes_deployments_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_list_execute()` to send, or `sasportal_nodes_deployments_list` for simplest API.

pub fn sasportal_nodes_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_list_builder()` + `sasportal_nodes_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_list(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_deployments_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_nodes_deployments_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_move_execute()` to send, or `sasportal_nodes_deployments_move` for simplest API.

pub fn sasportal_nodes_deployments_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeploymentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalMoveDeploymentRequest,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_move_builder()` + `sasportal_nodes_deployments_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_move(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_deployments_move_builder(client, &args.name, &args.body)?;
    sasportal_nodes_deployments_move_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_patch_execute()` to send, or `sasportal_nodes_deployments_patch` for simplest API.

pub fn sasportal_nodes_deployments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalDeployment,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_patch_builder()` + `sasportal_nodes_deployments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_patch(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_deployments_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_nodes_deployments_patch_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_devices_create_execute()` to send, or `sasportal_nodes_deployments_devices_create` for simplest API.

pub fn sasportal_nodes_deployments_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_devices_create_builder()` + `sasportal_nodes_deployments_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_devices_create(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_nodes_deployments_devices_create_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_deployments_devices_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_devices_create_signed_execute()` to send, or `sasportal_nodes_deployments_devices_create_signed` for simplest API.

pub fn sasportal_nodes_deployments_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalCreateSignedDeviceRequest,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_devices_create_signed_builder()` + `sasportal_nodes_deployments_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_devices_create_signed(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_deployments_devices_create_signed_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    sasportal_nodes_deployments_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_deployments_devices_list_execute()` to send, or `sasportal_nodes_deployments_devices_list` for simplest API.

pub fn sasportal_nodes_deployments_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_deployments_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_deployments_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_deployments_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_deployments_devices_list_builder()` + `sasportal_nodes_deployments_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_deployments_devices_list(
    client: &SimpleHttpClient,
    args: &SasportalNodesDeploymentsDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_deployments_devices_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_nodes_deployments_devices_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_create_execute()` to send, or `sasportal_nodes_devices_create` for simplest API.

pub fn sasportal_nodes_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_create_builder()` + `sasportal_nodes_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_create(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_create_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_devices_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_create_signed_execute()` to send, or `sasportal_nodes_devices_create_signed` for simplest API.

pub fn sasportal_nodes_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalCreateSignedDeviceRequest,
}

/// GET v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_create_signed_builder()` + `sasportal_nodes_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_create_signed_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_delete_execute()` to send, or `sasportal_nodes_devices_delete` for simplest API.

pub fn sasportal_nodes_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_delete_builder()` + `sasportal_nodes_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_delete(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_delete_builder(client, &args.name)?;
    sasportal_nodes_devices_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_get_execute()` to send, or `sasportal_nodes_devices_get` for simplest API.

pub fn sasportal_nodes_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_get_builder()` + `sasportal_nodes_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_get(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_get_builder(client, &args.name)?;
    sasportal_nodes_devices_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_list_execute()` to send, or `sasportal_nodes_devices_list` for simplest API.

pub fn sasportal_nodes_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_list_builder()` + `sasportal_nodes_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_list(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_nodes_devices_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_move_execute()` to send, or `sasportal_nodes_devices_move` for simplest API.

pub fn sasportal_nodes_devices_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalMoveDeviceRequest,
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_move_builder()` + `sasportal_nodes_devices_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_move(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_move_builder(client, &args.name, &args.body)?;
    sasportal_nodes_devices_move_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_patch_execute()` to send, or `sasportal_nodes_devices_patch` for simplest API.

pub fn sasportal_nodes_devices_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_patch_builder()` + `sasportal_nodes_devices_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_patch(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_nodes_devices_patch_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_sign_device_execute()` to send, or `sasportal_nodes_devices_sign_device` for simplest API.

pub fn sasportal_nodes_devices_sign_device_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_sign_device_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_sign_device_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_sign_device`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesSignDeviceArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalSignDeviceRequest,
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_sign_device_builder()` + `sasportal_nodes_devices_sign_device_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_sign_device(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesSignDeviceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_sign_device_builder(client, &args.name, &args.body)?;
    sasportal_nodes_devices_sign_device_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_devices_update_signed_execute()` to send, or `sasportal_nodes_devices_update_signed` for simplest API.

pub fn sasportal_nodes_devices_update_signed_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_devices_update_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_devices_update_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_devices_update_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesUpdateSignedArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalUpdateSignedDeviceRequest,
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_devices_update_signed_builder()` + `sasportal_nodes_devices_update_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_devices_update_signed(
    client: &SimpleHttpClient,
    args: &SasportalNodesDevicesUpdateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_devices_update_signed_builder(client, &args.name, &args.body)?;
    sasportal_nodes_devices_update_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_create_execute()` to send, or `sasportal_nodes_nodes_create` for simplest API.

pub fn sasportal_nodes_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalNode,
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_create_builder()` + `sasportal_nodes_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_create(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_create_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_delete_execute()` to send, or `sasportal_nodes_nodes_delete` for simplest API.

pub fn sasportal_nodes_nodes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_delete_builder()` + `sasportal_nodes_nodes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_delete(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_delete_builder(client, &args.name)?;
    sasportal_nodes_nodes_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_get_execute()` to send, or `sasportal_nodes_nodes_get` for simplest API.

pub fn sasportal_nodes_nodes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_get_builder()` + `sasportal_nodes_nodes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_get(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_get_builder(client, &args.name)?;
    sasportal_nodes_nodes_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_list_execute()` to send, or `sasportal_nodes_nodes_list` for simplest API.

pub fn sasportal_nodes_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_list_builder()` + `sasportal_nodes_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_list(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_nodes_nodes_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_move_execute()` to send, or `sasportal_nodes_nodes_move` for simplest API.

pub fn sasportal_nodes_nodes_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveNodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesMoveArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: SasPortalMoveNodeRequest,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_move_builder()` + `sasportal_nodes_nodes_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_move(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_move_builder(client, &args.name, &args.body)?;
    sasportal_nodes_nodes_move_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_patch_execute()` to send, or `sasportal_nodes_nodes_patch` for simplest API.

pub fn sasportal_nodes_nodes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SasPortalNode,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_patch_builder()` + `sasportal_nodes_nodes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_patch(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sasportal_nodes_nodes_patch_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_deployments_create_execute()` to send, or `sasportal_nodes_nodes_deployments_create` for simplest API.

pub fn sasportal_nodes_nodes_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDeployment,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_deployments_create_builder()` + `sasportal_nodes_nodes_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_deployments_create(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_nodes_nodes_deployments_create_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_nodes_deployments_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_deployments_list_execute()` to send, or `sasportal_nodes_nodes_deployments_list` for simplest API.

pub fn sasportal_nodes_nodes_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_deployments_list_builder()` + `sasportal_nodes_nodes_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_deployments_list(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_deployments_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_nodes_nodes_deployments_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_devices_create_execute()` to send, or `sasportal_nodes_nodes_devices_create` for simplest API.

pub fn sasportal_nodes_nodes_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalDevice,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_devices_create_builder()` + `sasportal_nodes_nodes_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_devices_create(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_devices_create_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_nodes_devices_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_devices_create_signed_execute()` to send, or `sasportal_nodes_nodes_devices_create_signed` for simplest API.

pub fn sasportal_nodes_nodes_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalCreateSignedDeviceRequest,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_devices_create_signed_builder()` + `sasportal_nodes_nodes_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sasportal_nodes_nodes_devices_create_signed_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_devices_list_execute()` to send, or `sasportal_nodes_nodes_devices_list` for simplest API.

pub fn sasportal_nodes_nodes_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_devices_list_builder()` + `sasportal_nodes_nodes_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_devices_list(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_devices_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_nodes_nodes_devices_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_nodes_create_execute()` to send, or `sasportal_nodes_nodes_nodes_create` for simplest API.

pub fn sasportal_nodes_nodes_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SasPortalNode,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_nodes_create_builder()` + `sasportal_nodes_nodes_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_nodes_create(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_nodes_create_builder(client, &args.parent, &args.body)?;
    sasportal_nodes_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_nodes_nodes_nodes_list_execute()` to send, or `sasportal_nodes_nodes_nodes_list` for simplest API.

pub fn sasportal_nodes_nodes_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_nodes_nodes_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_nodes_nodes_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_nodes_nodes_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_nodes_nodes_nodes_list_builder()` + `sasportal_nodes_nodes_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_nodes_nodes_nodes_list(
    client: &SimpleHttpClient,
    args: &SasportalNodesNodesNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_nodes_nodes_nodes_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sasportal_nodes_nodes_nodes_list_execute(builder)
}

/// GET v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_policies_get_execute()` to send, or `sasportal_policies_get` for simplest API.

pub fn sasportal_policies_get_builder(
    client: &SimpleHttpClient,
    body: &SasPortalGetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/policies:get",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalPoliciesGetArgs {
    /// Request body.
    pub body: SasPortalGetPolicyRequest,
}

/// GET v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_policies_get_builder()` + `sasportal_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_policies_get(
    client: &SimpleHttpClient,
    args: &SasportalPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_policies_get_builder(client, &args.body)?;
    sasportal_policies_get_execute(builder)
}

/// GET v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_policies_set_execute()` to send, or `sasportal_policies_set` for simplest API.

pub fn sasportal_policies_set_builder(
    client: &SimpleHttpClient,
    body: &SasPortalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/policies:set",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_policies_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_policies_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_policies_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_policies_set`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalPoliciesSetArgs {
    /// Request body.
    pub body: SasPortalSetPolicyRequest,
}

/// GET v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_policies_set_builder()` + `sasportal_policies_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_policies_set(
    client: &SimpleHttpClient,
    args: &SasportalPoliciesSetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_policies_set_builder(client, &args.body)?;
    sasportal_policies_set_execute(builder)
}

/// GET v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sasportal_policies_test_execute()` to send, or `sasportal_policies_test` for simplest API.

pub fn sasportal_policies_test_builder(
    client: &SimpleHttpClient,
    body: &SasPortalTestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sasportal.googleapis.com/v1alpha1/policies:test",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sasportal_policies_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sasportal_policies_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_policies_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalTestPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalTestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sasportal_policies_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalPoliciesTestArgs {
    /// Request body.
    pub body: SasPortalTestPermissionsRequest,
}

/// GET v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sasportal_policies_test_builder()` + `sasportal_policies_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sasportal_policies_test(
    client: &SimpleHttpClient,
    args: &SasportalPoliciesTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalTestPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sasportal_policies_test_builder(client, &args.body)?;
    sasportal_policies_test_execute(builder)
}
