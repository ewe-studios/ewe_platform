//! Auto-generated API clients for gcp/managedidentities.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_get_execute()` to send, or `managedidentities_projects_locations_get` for simplest API.

pub fn managedidentities_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_get_execute()` or `managedidentities_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_get_task()`.
/// For the simplest API, use `managedidentities_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_get_builder()` + `managedidentities_projects_locations_get_execute()`.
/// For task-level control, use `managedidentities_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_get(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_get_builder(client, &args.name)?;
    managedidentities_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_list_execute()` to send, or `managedidentities_projects_locations_list` for simplest API.

pub fn managedidentities_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_list_execute()` or `managedidentities_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_list_task()`.
/// For the simplest API, use `managedidentities_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_list_builder()` + `managedidentities_projects_locations_list_execute()`.
/// For task-level control, use `managedidentities_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_list(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    managedidentities_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:attachTrust
/// Adds an AD trust to a domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_attach_trust_execute()` to send, or `managedidentities_projects_locations_global_domains_attach_trust` for simplest API.

pub fn managedidentities_projects_locations_global_domains_attach_trust_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AttachTrustRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:attachTrust",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:attachTrust
/// Adds an AD trust to a domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_attach_trust_execute()` or `managedidentities_projects_locations_global_domains_attach_trust`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_attach_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_attach_trust_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:attachTrust
/// Adds an AD trust to a domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_attach_trust_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_attach_trust_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_attach_trust()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_attach_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_attach_trust_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_attach_trust_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_attach_trust`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsAttachTrustArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AttachTrustRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:attachTrust
/// Adds an AD trust to a domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_attach_trust_builder()` + `managedidentities_projects_locations_global_domains_attach_trust_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_attach_trust_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_attach_trust(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsAttachTrustArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_attach_trust_builder(
        client, &args.name, &args.body,
    )?;
    managedidentities_projects_locations_global_domains_attach_trust_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:checkMigrationPermission
/// CheckMigrationPermission API gets the current state of DomainMigration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_check_migration_permission_execute()` to send, or `managedidentities_projects_locations_global_domains_check_migration_permission` for simplest API.

pub fn managedidentities_projects_locations_global_domains_check_migration_permission_builder(
    client: &SimpleHttpClient,
    domain: &str,
    body: &CheckMigrationPermissionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:checkMigrationPermission",
        domain,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:checkMigrationPermission
/// CheckMigrationPermission API gets the current state of DomainMigration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_check_migration_permission_execute()` or `managedidentities_projects_locations_global_domains_check_migration_permission`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_check_migration_permission_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_check_migration_permission_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<CheckMigrationPermissionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckMigrationPermissionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:checkMigrationPermission
/// CheckMigrationPermission API gets the current state of DomainMigration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_check_migration_permission_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_check_migration_permission_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_check_migration_permission()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_check_migration_permission_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_check_migration_permission_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckMigrationPermissionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_check_migration_permission_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_check_migration_permission`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsCheckMigrationPermissionArgs {
    /// Path parameter: domain
    pub domain: String,
    /// Request body.
    pub body: CheckMigrationPermissionRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:checkMigrationPermission
/// CheckMigrationPermission API gets the current state of DomainMigration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_check_migration_permission_builder()` + `managedidentities_projects_locations_global_domains_check_migration_permission_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_check_migration_permission_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_check_migration_permission(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsCheckMigrationPermissionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckMigrationPermissionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_domains_check_migration_permission_builder(
            client,
            &args.domain,
            &args.body,
        )?;
    managedidentities_projects_locations_global_domains_check_migration_permission_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Creates a Microsoft AD domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_create_execute()` to send, or `managedidentities_projects_locations_global_domains_create` for simplest API.

pub fn managedidentities_projects_locations_global_domains_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    domainName: Option<&str>,
    body: &Domain,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = domainName {
        query_parts.push(format!("domainName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Creates a Microsoft AD domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_create_execute()` or `managedidentities_projects_locations_global_domains_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Creates a Microsoft AD domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_create_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: domainName
    pub domainName: Option<String>,
    /// Request body.
    pub body: Domain,
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Creates a Microsoft AD domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_create_builder()` + `managedidentities_projects_locations_global_domains_create_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_create(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_create_builder(
        client,
        &args.parent,
        args.domainName.as_deref(),
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Deletes a domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_delete_execute()` to send, or `managedidentities_projects_locations_global_domains_delete` for simplest API.

pub fn managedidentities_projects_locations_global_domains_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Deletes a domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_delete_execute()` or `managedidentities_projects_locations_global_domains_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Deletes a domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_delete_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Deletes a domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_delete_builder()` + `managedidentities_projects_locations_global_domains_delete_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_delete(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_domains_delete_builder(client, &args.name)?;
    managedidentities_projects_locations_global_domains_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:detachTrust
/// Removes an AD trust.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_detach_trust_execute()` to send, or `managedidentities_projects_locations_global_domains_detach_trust` for simplest API.

pub fn managedidentities_projects_locations_global_domains_detach_trust_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DetachTrustRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:detachTrust",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:detachTrust
/// Removes an AD trust.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_detach_trust_execute()` or `managedidentities_projects_locations_global_domains_detach_trust`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_detach_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_detach_trust_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:detachTrust
/// Removes an AD trust.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_detach_trust_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_detach_trust_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_detach_trust()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_detach_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_detach_trust_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_detach_trust_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_detach_trust`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsDetachTrustArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: DetachTrustRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:detachTrust
/// Removes an AD trust.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_detach_trust_builder()` + `managedidentities_projects_locations_global_domains_detach_trust_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_detach_trust_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_detach_trust(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsDetachTrustArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_detach_trust_builder(
        client, &args.name, &args.body,
    )?;
    managedidentities_projects_locations_global_domains_detach_trust_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:disableMigration
/// Disable Domain Migration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_disable_migration_execute()` to send, or `managedidentities_projects_locations_global_domains_disable_migration` for simplest API.

pub fn managedidentities_projects_locations_global_domains_disable_migration_builder(
    client: &SimpleHttpClient,
    domain: &str,
    body: &DisableMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:disableMigration",
        domain,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:disableMigration
/// Disable Domain Migration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_disable_migration_execute()` or `managedidentities_projects_locations_global_domains_disable_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_disable_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_disable_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:disableMigration
/// Disable Domain Migration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_disable_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_disable_migration_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_disable_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_disable_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_disable_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_disable_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_disable_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsDisableMigrationArgs {
    /// Path parameter: domain
    pub domain: String,
    /// Request body.
    pub body: DisableMigrationRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:disableMigration
/// Disable Domain Migration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_disable_migration_builder()` + `managedidentities_projects_locations_global_domains_disable_migration_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_disable_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_disable_migration(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsDisableMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_disable_migration_builder(
        client,
        &args.domain,
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_disable_migration_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:domainJoinMachine
/// DomainJoinMachine API joins a Compute Engine VM to the domain
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_domain_join_machine_execute()` to send, or `managedidentities_projects_locations_global_domains_domain_join_machine` for simplest API.

pub fn managedidentities_projects_locations_global_domains_domain_join_machine_builder(
    client: &SimpleHttpClient,
    domain: &str,
    body: &DomainJoinMachineRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:domainJoinMachine",
        domain,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:domainJoinMachine
/// DomainJoinMachine API joins a Compute Engine VM to the domain
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_domain_join_machine_execute()` or `managedidentities_projects_locations_global_domains_domain_join_machine`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_domain_join_machine_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_domain_join_machine_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<DomainJoinMachineResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DomainJoinMachineResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:domainJoinMachine
/// DomainJoinMachine API joins a Compute Engine VM to the domain
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_domain_join_machine_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_domain_join_machine_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_domain_join_machine()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_domain_join_machine_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_domain_join_machine_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainJoinMachineResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_domain_join_machine_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_domain_join_machine`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsDomainJoinMachineArgs {
    /// Path parameter: domain
    pub domain: String,
    /// Request body.
    pub body: DomainJoinMachineRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:domainJoinMachine
/// DomainJoinMachine API joins a Compute Engine VM to the domain
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_domain_join_machine_builder()` + `managedidentities_projects_locations_global_domains_domain_join_machine_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_domain_join_machine_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_domain_join_machine(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsDomainJoinMachineArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainJoinMachineResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_domain_join_machine_builder(
        client,
        &args.domain,
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_domain_join_machine_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:enableMigration
/// Enable Domain Migration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_enable_migration_execute()` to send, or `managedidentities_projects_locations_global_domains_enable_migration` for simplest API.

pub fn managedidentities_projects_locations_global_domains_enable_migration_builder(
    client: &SimpleHttpClient,
    domain: &str,
    body: &EnableMigrationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:enableMigration",
        domain,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:enableMigration
/// Enable Domain Migration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_enable_migration_execute()` or `managedidentities_projects_locations_global_domains_enable_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_enable_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_enable_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:enableMigration
/// Enable Domain Migration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_enable_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_enable_migration_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_enable_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_enable_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_enable_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_enable_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_enable_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsEnableMigrationArgs {
    /// Path parameter: domain
    pub domain: String,
    /// Request body.
    pub body: EnableMigrationRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:enableMigration
/// Enable Domain Migration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_enable_migration_builder()` + `managedidentities_projects_locations_global_domains_enable_migration_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_enable_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_enable_migration(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsEnableMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_enable_migration_builder(
        client,
        &args.domain,
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_enable_migration_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:extendSchema
/// Extend Schema for Domain
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_extend_schema_execute()` to send, or `managedidentities_projects_locations_global_domains_extend_schema` for simplest API.

pub fn managedidentities_projects_locations_global_domains_extend_schema_builder(
    client: &SimpleHttpClient,
    domain: &str,
    body: &ExtendSchemaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:extendSchema",
        domain,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:extendSchema
/// Extend Schema for Domain
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_extend_schema_execute()` or `managedidentities_projects_locations_global_domains_extend_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_extend_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_extend_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:extendSchema
/// Extend Schema for Domain
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_extend_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_extend_schema_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_extend_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_extend_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_extend_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_extend_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_extend_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsExtendSchemaArgs {
    /// Path parameter: domain
    pub domain: String,
    /// Request body.
    pub body: ExtendSchemaRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:extendSchema
/// Extend Schema for Domain
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_extend_schema_builder()` + `managedidentities_projects_locations_global_domains_extend_schema_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_extend_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_extend_schema(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsExtendSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_extend_schema_builder(
        client,
        &args.domain,
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_extend_schema_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Gets information about a domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_get_execute()` to send, or `managedidentities_projects_locations_global_domains_get` for simplest API.

pub fn managedidentities_projects_locations_global_domains_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Gets information about a domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_get_execute()` or `managedidentities_projects_locations_global_domains_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Domain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Gets information about a domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_get_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Domain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Gets information about a domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_get_builder()` + `managedidentities_projects_locations_global_domains_get_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_get(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Domain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_domains_get_builder(client, &args.name)?;
    managedidentities_projects_locations_global_domains_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_get_iam_policy_execute()` to send, or `managedidentities_projects_locations_global_domains_get_iam_policy` for simplest API.

pub fn managedidentities_projects_locations_global_domains_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_get_iam_policy_execute()` or `managedidentities_projects_locations_global_domains_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_get_iam_policy_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_get_iam_policy_builder()` + `managedidentities_projects_locations_global_domains_get_iam_policy_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    managedidentities_projects_locations_global_domains_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Gets the domain ldaps settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_get_ldapssettings_execute()` to send, or `managedidentities_projects_locations_global_domains_get_ldapssettings` for simplest API.

pub fn managedidentities_projects_locations_global_domains_get_ldapssettings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/ldapssettings",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Gets the domain ldaps settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_get_ldapssettings_execute()` or `managedidentities_projects_locations_global_domains_get_ldapssettings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_get_ldapssettings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_get_ldapssettings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<LDAPSSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LDAPSSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Gets the domain ldaps settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_get_ldapssettings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_get_ldapssettings_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_get_ldapssettings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_get_ldapssettings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_get_ldapssettings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LDAPSSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_get_ldapssettings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_get_ldapssettings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsGetLdapssettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Gets the domain ldaps settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_get_ldapssettings_builder()` + `managedidentities_projects_locations_global_domains_get_ldapssettings_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_get_ldapssettings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_get_ldapssettings(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsGetLdapssettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LDAPSSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_get_ldapssettings_builder(
        client, &args.name,
    )?;
    managedidentities_projects_locations_global_domains_get_ldapssettings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Lists domains in a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_list_execute()` to send, or `managedidentities_projects_locations_global_domains_list` for simplest API.

pub fn managedidentities_projects_locations_global_domains_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Lists domains in a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_list_execute()` or `managedidentities_projects_locations_global_domains_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListDomainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDomainsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Lists domains in a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_list_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDomainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/domains
/// Lists domains in a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_list_builder()` + `managedidentities_projects_locations_global_domains_list_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_list(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDomainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    managedidentities_projects_locations_global_domains_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Updates the metadata and configuration of a domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_patch_execute()` to send, or `managedidentities_projects_locations_global_domains_patch` for simplest API.

pub fn managedidentities_projects_locations_global_domains_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Domain,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Updates the metadata and configuration of a domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_patch_execute()` or `managedidentities_projects_locations_global_domains_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Updates the metadata and configuration of a domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_patch_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Domain,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}
/// Updates the metadata and configuration of a domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_patch_builder()` + `managedidentities_projects_locations_global_domains_patch_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_patch(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:reconfigureTrust
/// Updates the DNS conditional forwarder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_reconfigure_trust_execute()` to send, or `managedidentities_projects_locations_global_domains_reconfigure_trust` for simplest API.

pub fn managedidentities_projects_locations_global_domains_reconfigure_trust_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReconfigureTrustRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:reconfigureTrust",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:reconfigureTrust
/// Updates the DNS conditional forwarder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_reconfigure_trust_execute()` or `managedidentities_projects_locations_global_domains_reconfigure_trust`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_reconfigure_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_reconfigure_trust_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:reconfigureTrust
/// Updates the DNS conditional forwarder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_reconfigure_trust_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_reconfigure_trust_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_reconfigure_trust()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_reconfigure_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_reconfigure_trust_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_reconfigure_trust_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_reconfigure_trust`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsReconfigureTrustArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ReconfigureTrustRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:reconfigureTrust
/// Updates the DNS conditional forwarder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_reconfigure_trust_builder()` + `managedidentities_projects_locations_global_domains_reconfigure_trust_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_reconfigure_trust_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_reconfigure_trust(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsReconfigureTrustArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_reconfigure_trust_builder(
        client, &args.name, &args.body,
    )?;
    managedidentities_projects_locations_global_domains_reconfigure_trust_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:resetAdminPassword
/// Resets a domain's administrator password.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_reset_admin_password_execute()` to send, or `managedidentities_projects_locations_global_domains_reset_admin_password` for simplest API.

pub fn managedidentities_projects_locations_global_domains_reset_admin_password_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetAdminPasswordRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:resetAdminPassword",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:resetAdminPassword
/// Resets a domain's administrator password.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_reset_admin_password_execute()` or `managedidentities_projects_locations_global_domains_reset_admin_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_reset_admin_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_reset_admin_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ResetAdminPasswordResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResetAdminPasswordResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:resetAdminPassword
/// Resets a domain's administrator password.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_reset_admin_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_reset_admin_password_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_reset_admin_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_reset_admin_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_reset_admin_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResetAdminPasswordResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_reset_admin_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_reset_admin_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsResetAdminPasswordArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ResetAdminPasswordRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:resetAdminPassword
/// Resets a domain's administrator password.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_reset_admin_password_builder()` + `managedidentities_projects_locations_global_domains_reset_admin_password_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_reset_admin_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_reset_admin_password(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsResetAdminPasswordArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResetAdminPasswordResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_reset_admin_password_builder(
        client, &args.name, &args.body,
    )?;
    managedidentities_projects_locations_global_domains_reset_admin_password_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:restore
/// RestoreDomain restores domain backup mentioned in the RestoreDomainRequest
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_restore_execute()` to send, or `managedidentities_projects_locations_global_domains_restore` for simplest API.

pub fn managedidentities_projects_locations_global_domains_restore_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RestoreDomainRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:restore",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:restore
/// RestoreDomain restores domain backup mentioned in the RestoreDomainRequest
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_restore_execute()` or `managedidentities_projects_locations_global_domains_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:restore
/// RestoreDomain restores domain backup mentioned in the RestoreDomainRequest
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_restore_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsRestoreArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RestoreDomainRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:restore
/// RestoreDomain restores domain backup mentioned in the RestoreDomainRequest
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_restore_builder()` + `managedidentities_projects_locations_global_domains_restore_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_restore(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsRestoreArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_restore_builder(
        client, &args.name, &args.body,
    )?;
    managedidentities_projects_locations_global_domains_restore_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_set_iam_policy_execute()` to send, or `managedidentities_projects_locations_global_domains_set_iam_policy` for simplest API.

pub fn managedidentities_projects_locations_global_domains_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_set_iam_policy_execute()` or `managedidentities_projects_locations_global_domains_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_set_iam_policy_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_set_iam_policy_builder()` + `managedidentities_projects_locations_global_domains_set_iam_policy_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_test_iam_permissions_execute()` to send, or `managedidentities_projects_locations_global_domains_test_iam_permissions` for simplest API.

pub fn managedidentities_projects_locations_global_domains_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_test_iam_permissions_execute()` or `managedidentities_projects_locations_global_domains_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_test_iam_permissions_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_test_iam_permissions_builder()` + `managedidentities_projects_locations_global_domains_test_iam_permissions_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_test_iam_permissions_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Patches a single ldaps settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_update_ldapssettings_execute()` to send, or `managedidentities_projects_locations_global_domains_update_ldapssettings` for simplest API.

pub fn managedidentities_projects_locations_global_domains_update_ldapssettings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LDAPSSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/ldapssettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Patches a single ldaps settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_update_ldapssettings_execute()` or `managedidentities_projects_locations_global_domains_update_ldapssettings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_update_ldapssettings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_update_ldapssettings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Patches a single ldaps settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_update_ldapssettings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_update_ldapssettings_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_update_ldapssettings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_update_ldapssettings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_update_ldapssettings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_update_ldapssettings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_update_ldapssettings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsUpdateLdapssettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: LDAPSSettings,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/ldapssettings
/// Patches a single ldaps settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_update_ldapssettings_builder()` + `managedidentities_projects_locations_global_domains_update_ldapssettings_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_update_ldapssettings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_update_ldapssettings(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsUpdateLdapssettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_update_ldapssettings_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_update_ldapssettings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:validateTrust
/// Validates a trust state, that the target domain is reachable, and that the target domain is able to accept incoming trust requests.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_validate_trust_execute()` to send, or `managedidentities_projects_locations_global_domains_validate_trust` for simplest API.

pub fn managedidentities_projects_locations_global_domains_validate_trust_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ValidateTrustRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}:validateTrust",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:validateTrust
/// Validates a trust state, that the target domain is reachable, and that the target domain is able to accept incoming trust requests.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_validate_trust_execute()` or `managedidentities_projects_locations_global_domains_validate_trust`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_validate_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_validate_trust_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:validateTrust
/// Validates a trust state, that the target domain is reachable, and that the target domain is able to accept incoming trust requests.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_validate_trust_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_validate_trust_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_validate_trust()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_validate_trust_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_validate_trust_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_validate_trust_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_validate_trust`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsValidateTrustArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: ValidateTrustRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}:validateTrust
/// Validates a trust state, that the target domain is reachable, and that the target domain is able to accept incoming trust requests.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_validate_trust_builder()` + `managedidentities_projects_locations_global_domains_validate_trust_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_validate_trust_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_validate_trust(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsValidateTrustArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_validate_trust_builder(
        client, &args.name, &args.body,
    )?;
    managedidentities_projects_locations_global_domains_validate_trust_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Creates a Backup for a domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_create_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_create` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    backupId: Option<&str>,
    body: &Backup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = backupId {
        query_parts.push(format!("backupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Creates a Backup for a domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_create_execute()` or `managedidentities_projects_locations_global_domains_backups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Creates a Backup for a domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_create_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_backups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: backupId
    pub backupId: Option<String>,
    /// Request body.
    pub body: Backup,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Creates a Backup for a domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_create_builder()` + `managedidentities_projects_locations_global_domains_backups_create_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_create(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_backups_create_builder(
        client,
        &args.parent,
        args.backupId.as_deref(),
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_backups_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Deletes identified Backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_delete_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_delete` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Deletes identified Backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_delete_execute()` or `managedidentities_projects_locations_global_domains_backups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Deletes identified Backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_delete_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_backups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Deletes identified Backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_delete_builder()` + `managedidentities_projects_locations_global_domains_backups_delete_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_delete(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_backups_delete_builder(
        client, &args.name,
    )?;
    managedidentities_projects_locations_global_domains_backups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Gets details of a single Backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_get_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_get` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Gets details of a single Backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_get_execute()` or `managedidentities_projects_locations_global_domains_backups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Backup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Gets details of a single Backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_get_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_backups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Gets details of a single Backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_get_builder()` + `managedidentities_projects_locations_global_domains_backups_get_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_get(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_backups_get_builder(
        client, &args.name,
    )?;
    managedidentities_projects_locations_global_domains_backups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_get_iam_policy_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_get_iam_policy` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_get_iam_policy_execute()` or `managedidentities_projects_locations_global_domains_backups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_get_iam_policy_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_backups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_get_iam_policy_builder()` + `managedidentities_projects_locations_global_domains_backups_get_iam_policy_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_domains_backups_get_iam_policy_builder(
            client,
            &args.resource,
            args.options_requestedPolicyVersion,
        )?;
    managedidentities_projects_locations_global_domains_backups_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Lists Backup in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_list_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_list` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Lists Backup in a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_list_execute()` or `managedidentities_projects_locations_global_domains_backups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Lists Backup in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_list_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_backups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups
/// Lists Backup in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_list_builder()` + `managedidentities_projects_locations_global_domains_backups_list_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_list(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_backups_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    managedidentities_projects_locations_global_domains_backups_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Updates the labels for specified Backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_patch_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_patch` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Backup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Updates the labels for specified Backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_patch_execute()` or `managedidentities_projects_locations_global_domains_backups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Updates the labels for specified Backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_patch_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_domains_backups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Backup,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}
/// Updates the labels for specified Backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_patch_builder()` + `managedidentities_projects_locations_global_domains_backups_patch_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_patch(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_backups_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    managedidentities_projects_locations_global_domains_backups_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_set_iam_policy_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_set_iam_policy` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_set_iam_policy_execute()` or `managedidentities_projects_locations_global_domains_backups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_set_iam_policy_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_backups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_set_iam_policy_builder()` + `managedidentities_projects_locations_global_domains_backups_set_iam_policy_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_domains_backups_set_iam_policy_builder(
            client,
            &args.resource,
            &args.body,
        )?;
    managedidentities_projects_locations_global_domains_backups_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_execute()` to send, or `managedidentities_projects_locations_global_domains_backups_test_iam_permissions` for simplest API.

pub fn managedidentities_projects_locations_global_domains_backups_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/backups/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_execute()` or `managedidentities_projects_locations_global_domains_backups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_backups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_backups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_backups_test_iam_permissions_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_backups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsBackupsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/backups/{backupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_builder()` + `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_backups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_backups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsBackupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_domains_backups_test_iam_permissions_builder(
            client,
            &args.resource,
            &args.body,
        )?;
    managedidentities_projects_locations_global_domains_backups_test_iam_permissions_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations/{sqlIntegrationsId}
/// Gets details of a single `sqlIntegration`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_sql_integrations_get_execute()` to send, or `managedidentities_projects_locations_global_domains_sql_integrations_get` for simplest API.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/sqlIntegrations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations/{sqlIntegrationsId}
/// Gets details of a single `sqlIntegration`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_sql_integrations_get_execute()` or `managedidentities_projects_locations_global_domains_sql_integrations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_sql_integrations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SqlIntegration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlIntegration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations/{sqlIntegrationsId}
/// Gets details of a single `sqlIntegration`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_sql_integrations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_sql_integrations_get_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_sql_integrations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_sql_integrations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SqlIntegration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_sql_integrations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_sql_integrations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsSqlIntegrationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations/{sqlIntegrationsId}
/// Gets details of a single `sqlIntegration`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_sql_integrations_get_builder()` + `managedidentities_projects_locations_global_domains_sql_integrations_get_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_sql_integrations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_get(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsSqlIntegrationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SqlIntegration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_domains_sql_integrations_get_builder(
        client, &args.name,
    )?;
    managedidentities_projects_locations_global_domains_sql_integrations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations
/// Lists SqlIntegrations in a given domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_domains_sql_integrations_list_execute()` to send, or `managedidentities_projects_locations_global_domains_sql_integrations_list` for simplest API.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/domains/{}/sqlIntegrations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations
/// Lists SqlIntegrations in a given domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_domains_sql_integrations_list_execute()` or `managedidentities_projects_locations_global_domains_sql_integrations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_sql_integrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListSqlIntegrationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSqlIntegrationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations
/// Lists SqlIntegrations in a given domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_domains_sql_integrations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_domains_sql_integrations_list_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_domains_sql_integrations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_domains_sql_integrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSqlIntegrationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_domains_sql_integrations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_domains_sql_integrations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalDomainsSqlIntegrationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/domains/{domainsId}/sqlIntegrations
/// Lists SqlIntegrations in a given domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_domains_sql_integrations_list_builder()` + `managedidentities_projects_locations_global_domains_sql_integrations_list_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_domains_sql_integrations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_domains_sql_integrations_list(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalDomainsSqlIntegrationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSqlIntegrationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_domains_sql_integrations_list_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.orderBy.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    managedidentities_projects_locations_global_domains_sql_integrations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_operations_cancel_execute()` to send, or `managedidentities_projects_locations_global_operations_cancel` for simplest API.

pub fn managedidentities_projects_locations_global_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_operations_cancel_execute()` or `managedidentities_projects_locations_global_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_operations_cancel_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_operations_cancel_builder()` + `managedidentities_projects_locations_global_operations_cancel_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_cancel(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_operations_cancel_builder(
        client, &args.name, &args.body,
    )?;
    managedidentities_projects_locations_global_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_operations_delete_execute()` to send, or `managedidentities_projects_locations_global_operations_delete` for simplest API.

pub fn managedidentities_projects_locations_global_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_operations_delete_execute()` or `managedidentities_projects_locations_global_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_operations_delete_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_operations_delete_builder()` + `managedidentities_projects_locations_global_operations_delete_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_delete(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_operations_delete_builder(client, &args.name)?;
    managedidentities_projects_locations_global_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_operations_get_execute()` to send, or `managedidentities_projects_locations_global_operations_get` for simplest API.

pub fn managedidentities_projects_locations_global_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_operations_get_execute()` or `managedidentities_projects_locations_global_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_operations_get_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_operations_get_builder()` + `managedidentities_projects_locations_global_operations_get_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_get(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_operations_get_builder(client, &args.name)?;
    managedidentities_projects_locations_global_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_operations_list_execute()` to send, or `managedidentities_projects_locations_global_operations_list` for simplest API.

pub fn managedidentities_projects_locations_global_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_operations_list_execute()` or `managedidentities_projects_locations_global_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_operations_list_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/global/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_operations_list_builder()` + `managedidentities_projects_locations_global_operations_list_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_operations_list(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    managedidentities_projects_locations_global_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Creates a Peering for Managed AD instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_create_execute()` to send, or `managedidentities_projects_locations_global_peerings_create` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    peeringId: Option<&str>,
    body: &Peering,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = peeringId {
        query_parts.push(format!("peeringId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Creates a Peering for Managed AD instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_create_execute()` or `managedidentities_projects_locations_global_peerings_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Creates a Peering for Managed AD instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_create_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_peerings_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: peeringId
    pub peeringId: Option<String>,
    /// Request body.
    pub body: Peering,
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Creates a Peering for Managed AD instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_create_builder()` + `managedidentities_projects_locations_global_peerings_create_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_create(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_peerings_create_builder(
        client,
        &args.parent,
        args.peeringId.as_deref(),
        &args.body,
    )?;
    managedidentities_projects_locations_global_peerings_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Deletes identified Peering.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_delete_execute()` to send, or `managedidentities_projects_locations_global_peerings_delete` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Deletes identified Peering.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_delete_execute()` or `managedidentities_projects_locations_global_peerings_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Deletes identified Peering.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_delete_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_peerings_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Deletes identified Peering.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_delete_builder()` + `managedidentities_projects_locations_global_peerings_delete_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_delete(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_peerings_delete_builder(client, &args.name)?;
    managedidentities_projects_locations_global_peerings_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Gets details of a single Peering.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_get_execute()` to send, or `managedidentities_projects_locations_global_peerings_get` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Gets details of a single Peering.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_get_execute()` or `managedidentities_projects_locations_global_peerings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Peering>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Peering = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Gets details of a single Peering.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_get_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Peering>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_peerings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Gets details of a single Peering.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_get_builder()` + `managedidentities_projects_locations_global_peerings_get_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_get(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Peering>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_peerings_get_builder(client, &args.name)?;
    managedidentities_projects_locations_global_peerings_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_get_iam_policy_execute()` to send, or `managedidentities_projects_locations_global_peerings_get_iam_policy` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_get_iam_policy_execute()` or `managedidentities_projects_locations_global_peerings_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_get_iam_policy_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_peerings_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_get_iam_policy_builder()` + `managedidentities_projects_locations_global_peerings_get_iam_policy_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_peerings_get_iam_policy_builder(
        client,
        &args.resource,
        args.options_requestedPolicyVersion,
    )?;
    managedidentities_projects_locations_global_peerings_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Lists Peerings in a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_list_execute()` to send, or `managedidentities_projects_locations_global_peerings_list` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Lists Peerings in a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_list_execute()` or `managedidentities_projects_locations_global_peerings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListPeeringsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPeeringsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Lists Peerings in a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_list_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPeeringsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_peerings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/peerings
/// Lists Peerings in a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_list_builder()` + `managedidentities_projects_locations_global_peerings_list_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_list(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPeeringsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_peerings_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    managedidentities_projects_locations_global_peerings_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Updates the labels for specified Peering.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_patch_execute()` to send, or `managedidentities_projects_locations_global_peerings_patch` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Peering,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Updates the labels for specified Peering.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_patch_execute()` or `managedidentities_projects_locations_global_peerings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Updates the labels for specified Peering.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_patch_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_peerings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Peering,
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}
/// Updates the labels for specified Peering.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_patch_builder()` + `managedidentities_projects_locations_global_peerings_patch_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_patch(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_peerings_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    managedidentities_projects_locations_global_peerings_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_set_iam_policy_execute()` to send, or `managedidentities_projects_locations_global_peerings_set_iam_policy` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_set_iam_policy_execute()` or `managedidentities_projects_locations_global_peerings_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_set_iam_policy_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = managedidentities_projects_locations_global_peerings_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_set_iam_policy_builder()` + `managedidentities_projects_locations_global_peerings_set_iam_policy_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = managedidentities_projects_locations_global_peerings_set_iam_policy_builder(
        client,
        &args.resource,
        &args.body,
    )?;
    managedidentities_projects_locations_global_peerings_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `managedidentities_projects_locations_global_peerings_test_iam_permissions_execute()` to send, or `managedidentities_projects_locations_global_peerings_test_iam_permissions` for simplest API.

pub fn managedidentities_projects_locations_global_peerings_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://managedidentities.googleapis.com/v1/projects/{}/locations/global/peerings/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `managedidentities_projects_locations_global_peerings_test_iam_permissions_execute()` or `managedidentities_projects_locations_global_peerings_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `managedidentities_projects_locations_global_peerings_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_test_iam_permissions_task()`.
/// For the simplest API, use `managedidentities_projects_locations_global_peerings_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `managedidentities_projects_locations_global_peerings_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn managedidentities_projects_locations_global_peerings_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        managedidentities_projects_locations_global_peerings_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`managedidentities_projects_locations_global_peerings_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ManagedidentitiesProjectsLocationsGlobalPeeringsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/global/peerings/{peeringsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `managedidentities_projects_locations_global_peerings_test_iam_permissions_builder()` + `managedidentities_projects_locations_global_peerings_test_iam_permissions_execute()`.
/// For task-level control, use `managedidentities_projects_locations_global_peerings_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn managedidentities_projects_locations_global_peerings_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ManagedidentitiesProjectsLocationsGlobalPeeringsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        managedidentities_projects_locations_global_peerings_test_iam_permissions_builder(
            client,
            &args.resource,
            &args.body,
        )?;
    managedidentities_projects_locations_global_peerings_test_iam_permissions_execute(builder)
}
