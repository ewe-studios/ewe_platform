//! Auto-generated API clients for gcp/prod_tt_sasportal.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/prod_tt_sasportal`

#![cfg(feature = "gcp/prod_tt_sasportal")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_get_execute()` to send, or `prod_tt_sasportal_customers_get` for simplest API.

pub fn prod_tt_sasportal_customers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalCustomer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_get_builder()` + `prod_tt_sasportal_customers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_get_builder(client, name)?;
    prod_tt_sasportal_customers_get_execute(builder)
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_list_execute()` to send, or `prod_tt_sasportal_customers_list` for simplest API.

pub fn prod_tt_sasportal_customers_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_list_builder()` + `prod_tt_sasportal_customers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_list_builder(client, pageSize, pageToken)?;
    prod_tt_sasportal_customers_list_execute(builder)
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_list_gcp_project_deployments_execute()` to send, or `prod_tt_sasportal_customers_list_gcp_project_deployments` for simplest API.

pub fn prod_tt_sasportal_customers_list_gcp_project_deployments_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:listGcpProjectDeployments",
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_list_gcp_project_deployments_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_list_gcp_project_deployments_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_gcp_project_deployments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListGcpProjectDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListGcpProjectDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_list_gcp_project_deployments_builder()` + `prod_tt_sasportal_customers_list_gcp_project_deployments_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_gcp_project_deployments(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListGcpProjectDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_list_gcp_project_deployments_builder(client)?;
    prod_tt_sasportal_customers_list_gcp_project_deployments_execute(builder)
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_list_legacy_organizations_execute()` to send, or `prod_tt_sasportal_customers_list_legacy_organizations` for simplest API.

pub fn prod_tt_sasportal_customers_list_legacy_organizations_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:listLegacyOrganizations",
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_list_legacy_organizations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_list_legacy_organizations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_legacy_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListLegacyOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListLegacyOrganizationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_list_legacy_organizations_builder()` + `prod_tt_sasportal_customers_list_legacy_organizations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_legacy_organizations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListLegacyOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_list_legacy_organizations_builder(client)?;
    prod_tt_sasportal_customers_list_legacy_organizations_execute(builder)
}

/// GET v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_migrate_organization_execute()` to send, or `prod_tt_sasportal_customers_migrate_organization` for simplest API.

pub fn prod_tt_sasportal_customers_migrate_organization_builder(
    client: &SimpleHttpClient,
    body: &SasPortalMigrateOrganizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:migrateOrganization",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_migrate_organization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_migrate_organization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_migrate_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_migrate_organization_builder()` + `prod_tt_sasportal_customers_migrate_organization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_migrate_organization(
    client: &SimpleHttpClient,
    body: &SasPortalMigrateOrganizationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_migrate_organization_builder(client, body)?;
    prod_tt_sasportal_customers_migrate_organization_execute(builder)
}

/// GET v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_patch_execute()` to send, or `prod_tt_sasportal_customers_patch` for simplest API.

pub fn prod_tt_sasportal_customers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalCustomer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalCustomer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_patch_builder()` + `prod_tt_sasportal_customers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalCustomer,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_customers_patch_execute(builder)
}

/// GET v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_provision_deployment_execute()` to send, or `prod_tt_sasportal_customers_provision_deployment` for simplest API.

pub fn prod_tt_sasportal_customers_provision_deployment_builder(
    client: &SimpleHttpClient,
    body: &SasPortalProvisionDeploymentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:provisionDeployment",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_provision_deployment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_provision_deployment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_provision_deployment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalProvisionDeploymentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalProvisionDeploymentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_provision_deployment_builder()` + `prod_tt_sasportal_customers_provision_deployment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_provision_deployment(
    client: &SimpleHttpClient,
    body: &SasPortalProvisionDeploymentRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalProvisionDeploymentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_provision_deployment_builder(client, body)?;
    prod_tt_sasportal_customers_provision_deployment_execute(builder)
}

/// GET v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_setup_sas_analytics_execute()` to send, or `prod_tt_sasportal_customers_setup_sas_analytics` for simplest API.

pub fn prod_tt_sasportal_customers_setup_sas_analytics_builder(
    client: &SimpleHttpClient,
    body: &SasPortalSetupSasAnalyticsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:setupSasAnalytics",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_setup_sas_analytics_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_setup_sas_analytics_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_setup_sas_analytics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_setup_sas_analytics_builder()` + `prod_tt_sasportal_customers_setup_sas_analytics_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_setup_sas_analytics(
    client: &SimpleHttpClient,
    body: &SasPortalSetupSasAnalyticsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_setup_sas_analytics_builder(client, body)?;
    prod_tt_sasportal_customers_setup_sas_analytics_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_create_execute()` to send, or `prod_tt_sasportal_customers_deployments_create` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_create_builder()` + `prod_tt_sasportal_customers_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_create_builder(client, parent, body)?;
    prod_tt_sasportal_customers_deployments_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_delete_execute()` to send, or `prod_tt_sasportal_customers_deployments_delete` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_delete_builder()` + `prod_tt_sasportal_customers_deployments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_delete_builder(client, name)?;
    prod_tt_sasportal_customers_deployments_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_get_execute()` to send, or `prod_tt_sasportal_customers_deployments_get` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_get_builder()` + `prod_tt_sasportal_customers_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_get_builder(client, name)?;
    prod_tt_sasportal_customers_deployments_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_list_execute()` to send, or `prod_tt_sasportal_customers_deployments_list` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_list_builder()` + `prod_tt_sasportal_customers_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_customers_deployments_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_move_execute()` to send, or `prod_tt_sasportal_customers_deployments_move` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeploymentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_move_builder()` + `prod_tt_sasportal_customers_deployments_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeploymentRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_move_builder(client, name, body)?;
    prod_tt_sasportal_customers_deployments_move_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_patch_execute()` to send, or `prod_tt_sasportal_customers_deployments_patch` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_patch_builder()` + `prod_tt_sasportal_customers_deployments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDeployment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_deployments_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_customers_deployments_patch_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_devices_create_execute()` to send, or `prod_tt_sasportal_customers_deployments_devices_create` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_create_builder()` + `prod_tt_sasportal_customers_deployments_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_deployments_devices_create_builder(client, parent, body)?;
    prod_tt_sasportal_customers_deployments_devices_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_devices_create_signed_execute()` to send, or `prod_tt_sasportal_customers_deployments_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_create_signed_builder()` + `prod_tt_sasportal_customers_deployments_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create_signed(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_devices_create_signed_builder(
        client, parent, body,
    )?;
    prod_tt_sasportal_customers_deployments_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_devices_list_execute()` to send, or `prod_tt_sasportal_customers_deployments_devices_list` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_deployments_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_list_builder()` + `prod_tt_sasportal_customers_deployments_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_devices_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_customers_deployments_devices_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_create_execute()` to send, or `prod_tt_sasportal_customers_devices_create` for simplest API.

pub fn prod_tt_sasportal_customers_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_create_builder()` + `prod_tt_sasportal_customers_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_create_builder(client, parent, body)?;
    prod_tt_sasportal_customers_devices_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_create_signed_execute()` to send, or `prod_tt_sasportal_customers_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_customers_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_create_signed_builder()` + `prod_tt_sasportal_customers_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create_signed(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_create_signed_builder(client, parent, body)?;
    prod_tt_sasportal_customers_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_delete_execute()` to send, or `prod_tt_sasportal_customers_devices_delete` for simplest API.

pub fn prod_tt_sasportal_customers_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_delete_builder()` + `prod_tt_sasportal_customers_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_delete_builder(client, name)?;
    prod_tt_sasportal_customers_devices_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_get_execute()` to send, or `prod_tt_sasportal_customers_devices_get` for simplest API.

pub fn prod_tt_sasportal_customers_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_get_builder()` + `prod_tt_sasportal_customers_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_get_builder(client, name)?;
    prod_tt_sasportal_customers_devices_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_list_execute()` to send, or `prod_tt_sasportal_customers_devices_list` for simplest API.

pub fn prod_tt_sasportal_customers_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_list_builder()` + `prod_tt_sasportal_customers_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_customers_devices_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_move_execute()` to send, or `prod_tt_sasportal_customers_devices_move` for simplest API.

pub fn prod_tt_sasportal_customers_devices_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_move_builder()` + `prod_tt_sasportal_customers_devices_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_move_builder(client, name, body)?;
    prod_tt_sasportal_customers_devices_move_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_patch_execute()` to send, or `prod_tt_sasportal_customers_devices_patch` for simplest API.

pub fn prod_tt_sasportal_customers_devices_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_patch_builder()` + `prod_tt_sasportal_customers_devices_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_devices_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_customers_devices_patch_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_sign_device_execute()` to send, or `prod_tt_sasportal_customers_devices_sign_device` for simplest API.

pub fn prod_tt_sasportal_customers_devices_sign_device_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_sign_device_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_sign_device_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_sign_device_builder()` + `prod_tt_sasportal_customers_devices_sign_device_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_sign_device(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_sign_device_builder(client, name, body)?;
    prod_tt_sasportal_customers_devices_sign_device_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_update_signed_execute()` to send, or `prod_tt_sasportal_customers_devices_update_signed` for simplest API.

pub fn prod_tt_sasportal_customers_devices_update_signed_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_devices_update_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_devices_update_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_update_signed_builder()` + `prod_tt_sasportal_customers_devices_update_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_update_signed(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_update_signed_builder(client, name, body)?;
    prod_tt_sasportal_customers_devices_update_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_create_builder()` + `prod_tt_sasportal_customers_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_create_builder(client, parent, body)?;
    prod_tt_sasportal_customers_nodes_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_delete_execute()` to send, or `prod_tt_sasportal_customers_nodes_delete` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_delete_builder()` + `prod_tt_sasportal_customers_nodes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_delete_builder(client, name)?;
    prod_tt_sasportal_customers_nodes_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_get_execute()` to send, or `prod_tt_sasportal_customers_nodes_get` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_get_builder()` + `prod_tt_sasportal_customers_nodes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_get_builder(client, name)?;
    prod_tt_sasportal_customers_nodes_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_list_builder()` + `prod_tt_sasportal_customers_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_move_execute()` to send, or `prod_tt_sasportal_customers_nodes_move` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveNodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_move_builder()` + `prod_tt_sasportal_customers_nodes_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveNodeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_move_builder(client, name, body)?;
    prod_tt_sasportal_customers_nodes_move_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_patch_execute()` to send, or `prod_tt_sasportal_customers_nodes_patch` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_patch_builder()` + `prod_tt_sasportal_customers_nodes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalNode,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_customers_nodes_patch_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_deployments_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_deployments_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_deployments_create_builder()` + `prod_tt_sasportal_customers_nodes_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_nodes_deployments_create_builder(client, parent, body)?;
    prod_tt_sasportal_customers_nodes_deployments_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_deployments_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_deployments_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_deployments_list_builder()` + `prod_tt_sasportal_customers_nodes_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_deployments_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_deployments_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_devices_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_devices_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_create_builder()` + `prod_tt_sasportal_customers_nodes_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_devices_create_builder(client, parent, body)?;
    prod_tt_sasportal_customers_nodes_devices_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_devices_create_signed_execute()` to send, or `prod_tt_sasportal_customers_nodes_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_create_signed_builder()` + `prod_tt_sasportal_customers_nodes_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_nodes_devices_create_signed_builder(client, parent, body)?;
    prod_tt_sasportal_customers_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_devices_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_devices_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_list_builder()` + `prod_tt_sasportal_customers_nodes_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_devices_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_devices_list_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_nodes_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_nodes_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_nodes_create_builder()` + `prod_tt_sasportal_customers_nodes_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_nodes_create_builder(client, parent, body)?;
    prod_tt_sasportal_customers_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_nodes_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_nodes_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_customers_nodes_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_customers_nodes_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_nodes_list_builder()` + `prod_tt_sasportal_customers_nodes_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_nodes_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_nodes_list_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_get_execute()` to send, or `prod_tt_sasportal_deployments_get` for simplest API.

pub fn prod_tt_sasportal_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_get_builder()` + `prod_tt_sasportal_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_get_builder(client, name)?;
    prod_tt_sasportal_deployments_get_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_delete_execute()` to send, or `prod_tt_sasportal_deployments_devices_delete` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_deployments_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_deployments_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_delete_builder()` + `prod_tt_sasportal_deployments_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_delete_builder(client, name)?;
    prod_tt_sasportal_deployments_devices_delete_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_get_execute()` to send, or `prod_tt_sasportal_deployments_devices_get` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_deployments_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_deployments_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_get_builder()` + `prod_tt_sasportal_deployments_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_get_builder(client, name)?;
    prod_tt_sasportal_deployments_devices_get_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_move_execute()` to send, or `prod_tt_sasportal_deployments_devices_move` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_deployments_devices_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_deployments_devices_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_move_builder()` + `prod_tt_sasportal_deployments_devices_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_move_builder(client, name, body)?;
    prod_tt_sasportal_deployments_devices_move_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_patch_execute()` to send, or `prod_tt_sasportal_deployments_devices_patch` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_deployments_devices_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_deployments_devices_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_patch_builder()` + `prod_tt_sasportal_deployments_devices_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_deployments_devices_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_deployments_devices_patch_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_sign_device_execute()` to send, or `prod_tt_sasportal_deployments_devices_sign_device` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_sign_device_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_deployments_devices_sign_device_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_deployments_devices_sign_device_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_sign_device_builder()` + `prod_tt_sasportal_deployments_devices_sign_device_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_sign_device(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_sign_device_builder(client, name, body)?;
    prod_tt_sasportal_deployments_devices_sign_device_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_update_signed_execute()` to send, or `prod_tt_sasportal_deployments_devices_update_signed` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_update_signed_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_deployments_devices_update_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_deployments_devices_update_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_update_signed_builder()` + `prod_tt_sasportal_deployments_devices_update_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_update_signed(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_update_signed_builder(client, name, body)?;
    prod_tt_sasportal_deployments_devices_update_signed_execute(builder)
}

/// GET v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_installer_generate_secret_execute()` to send, or `prod_tt_sasportal_installer_generate_secret` for simplest API.

pub fn prod_tt_sasportal_installer_generate_secret_builder(
    client: &SimpleHttpClient,
    body: &SasPortalGenerateSecretRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/installer:generateSecret",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_installer_generate_secret_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_installer_generate_secret_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_generate_secret_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalGenerateSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalGenerateSecretResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_installer_generate_secret_builder()` + `prod_tt_sasportal_installer_generate_secret_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_generate_secret(
    client: &SimpleHttpClient,
    body: &SasPortalGenerateSecretRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalGenerateSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_installer_generate_secret_builder(client, body)?;
    prod_tt_sasportal_installer_generate_secret_execute(builder)
}

/// GET v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_installer_validate_execute()` to send, or `prod_tt_sasportal_installer_validate` for simplest API.

pub fn prod_tt_sasportal_installer_validate_builder(
    client: &SimpleHttpClient,
    body: &SasPortalValidateInstallerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/installer:validate",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_installer_validate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_installer_validate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalValidateInstallerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalValidateInstallerResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_installer_validate_builder()` + `prod_tt_sasportal_installer_validate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_validate(
    client: &SimpleHttpClient,
    body: &SasPortalValidateInstallerRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalValidateInstallerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_installer_validate_builder(client, body)?;
    prod_tt_sasportal_installer_validate_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_get_execute()` to send, or `prod_tt_sasportal_nodes_get` for simplest API.

pub fn prod_tt_sasportal_nodes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_get_builder()` + `prod_tt_sasportal_nodes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_get_builder(client, name)?;
    prod_tt_sasportal_nodes_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_delete_execute()` to send, or `prod_tt_sasportal_nodes_deployments_delete` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_delete_builder()` + `prod_tt_sasportal_nodes_deployments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_delete_builder(client, name)?;
    prod_tt_sasportal_nodes_deployments_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_get_execute()` to send, or `prod_tt_sasportal_nodes_deployments_get` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_get_builder()` + `prod_tt_sasportal_nodes_deployments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_get_builder(client, name)?;
    prod_tt_sasportal_nodes_deployments_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_list_execute()` to send, or `prod_tt_sasportal_nodes_deployments_list` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_list_builder()` + `prod_tt_sasportal_nodes_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_nodes_deployments_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_move_execute()` to send, or `prod_tt_sasportal_nodes_deployments_move` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeploymentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_move_builder()` + `prod_tt_sasportal_nodes_deployments_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeploymentRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_move_builder(client, name, body)?;
    prod_tt_sasportal_nodes_deployments_move_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_patch_execute()` to send, or `prod_tt_sasportal_nodes_deployments_patch` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_patch_builder()` + `prod_tt_sasportal_nodes_deployments_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDeployment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_deployments_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_nodes_deployments_patch_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_devices_create_execute()` to send, or `prod_tt_sasportal_nodes_deployments_devices_create` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_create_builder()` + `prod_tt_sasportal_nodes_deployments_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_devices_create_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_deployments_devices_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_devices_create_signed_execute()` to send, or `prod_tt_sasportal_nodes_deployments_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_create_signed_builder()` + `prod_tt_sasportal_nodes_deployments_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_signed(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_deployments_devices_create_signed_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_deployments_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_devices_list_execute()` to send, or `prod_tt_sasportal_nodes_deployments_devices_list` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_deployments_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_list_builder()` + `prod_tt_sasportal_nodes_deployments_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_devices_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_nodes_deployments_devices_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_create_execute()` to send, or `prod_tt_sasportal_nodes_devices_create` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_create_builder()` + `prod_tt_sasportal_nodes_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_create_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_devices_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_create_signed_execute()` to send, or `prod_tt_sasportal_nodes_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_create_signed_builder()` + `prod_tt_sasportal_nodes_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_create_signed_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_delete_execute()` to send, or `prod_tt_sasportal_nodes_devices_delete` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_delete_builder()` + `prod_tt_sasportal_nodes_devices_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_delete_builder(client, name)?;
    prod_tt_sasportal_nodes_devices_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_get_execute()` to send, or `prod_tt_sasportal_nodes_devices_get` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_get_builder()` + `prod_tt_sasportal_nodes_devices_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_get_builder(client, name)?;
    prod_tt_sasportal_nodes_devices_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_list_execute()` to send, or `prod_tt_sasportal_nodes_devices_list` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_list_builder()` + `prod_tt_sasportal_nodes_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_devices_list_builder(client, parent, filter, pageSize, pageToken)?;
    prod_tt_sasportal_nodes_devices_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_move_execute()` to send, or `prod_tt_sasportal_nodes_devices_move` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_move_builder()` + `prod_tt_sasportal_nodes_devices_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_move_builder(client, name, body)?;
    prod_tt_sasportal_nodes_devices_move_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_patch_execute()` to send, or `prod_tt_sasportal_nodes_devices_patch` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_patch_builder()` + `prod_tt_sasportal_nodes_devices_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_nodes_devices_patch_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_sign_device_execute()` to send, or `prod_tt_sasportal_nodes_devices_sign_device` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_sign_device_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_sign_device_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_sign_device_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_sign_device_builder()` + `prod_tt_sasportal_nodes_devices_sign_device_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_sign_device(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalSignDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_sign_device_builder(client, name, body)?;
    prod_tt_sasportal_nodes_devices_sign_device_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_update_signed_execute()` to send, or `prod_tt_sasportal_nodes_devices_update_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_update_signed_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_devices_update_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_devices_update_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_update_signed_builder()` + `prod_tt_sasportal_nodes_devices_update_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_update_signed(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalUpdateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_update_signed_builder(client, name, body)?;
    prod_tt_sasportal_nodes_devices_update_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_create_builder()` + `prod_tt_sasportal_nodes_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_create_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_delete_execute()` to send, or `prod_tt_sasportal_nodes_nodes_delete` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_delete_builder()` + `prod_tt_sasportal_nodes_nodes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_delete_builder(client, name)?;
    prod_tt_sasportal_nodes_nodes_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_get_execute()` to send, or `prod_tt_sasportal_nodes_nodes_get` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_get_builder()` + `prod_tt_sasportal_nodes_nodes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_get_builder(client, name)?;
    prod_tt_sasportal_nodes_nodes_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_list_builder()` + `prod_tt_sasportal_nodes_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_nodes_list_builder(client, parent, filter, pageSize, pageToken)?;
    prod_tt_sasportal_nodes_nodes_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_move_execute()` to send, or `prod_tt_sasportal_nodes_nodes_move` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_move_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveNodeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}:move",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_move_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_move_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_move_builder()` + `prod_tt_sasportal_nodes_nodes_move_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_move(
    client: &SimpleHttpClient,
    name: &str,
    body: &SasPortalMoveNodeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_move_builder(client, name, body)?;
    prod_tt_sasportal_nodes_nodes_move_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_patch_execute()` to send, or `prod_tt_sasportal_nodes_nodes_patch` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_patch_builder()` + `prod_tt_sasportal_nodes_nodes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SasPortalNode,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_patch_builder(client, name, updateMask, body)?;
    prod_tt_sasportal_nodes_nodes_patch_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_deployments_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_deployments_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_deployments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_deployments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_deployments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_deployments_create_builder()` + `prod_tt_sasportal_nodes_nodes_deployments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDeployment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_deployments_create_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_nodes_deployments_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_deployments_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_deployments_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_deployments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_deployments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_deployments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_deployments_list_builder()` + `prod_tt_sasportal_nodes_nodes_deployments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_deployments_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_nodes_nodes_deployments_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_devices_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_devices_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_devices_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_create_builder()` + `prod_tt_sasportal_nodes_nodes_devices_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalDevice,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_devices_create_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_nodes_devices_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_devices_create_signed_execute()` to send, or `prod_tt_sasportal_nodes_nodes_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_signed_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_create_signed_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_devices_create_signed_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_create_signed_builder()` + `prod_tt_sasportal_nodes_nodes_devices_create_signed_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalCreateSignedDeviceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_nodes_devices_create_signed_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_devices_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_devices_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_devices_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_devices_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_list_builder()` + `prod_tt_sasportal_nodes_nodes_devices_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_devices_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_nodes_nodes_devices_list_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_nodes_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_nodes_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_nodes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_nodes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_nodes_create_builder()` + `prod_tt_sasportal_nodes_nodes_nodes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SasPortalNode,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_nodes_create_builder(client, parent, body)?;
    prod_tt_sasportal_nodes_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_nodes_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_nodes_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_nodes_nodes_nodes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_nodes_nodes_nodes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_nodes_list_builder()` + `prod_tt_sasportal_nodes_nodes_nodes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_nodes_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    prod_tt_sasportal_nodes_nodes_nodes_list_execute(builder)
}

/// GET v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_policies_get_execute()` to send, or `prod_tt_sasportal_policies_get` for simplest API.

pub fn prod_tt_sasportal_policies_get_builder(
    client: &SimpleHttpClient,
    body: &SasPortalGetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/policies:get",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_policies_get_builder()` + `prod_tt_sasportal_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_get(
    client: &SimpleHttpClient,
    body: &SasPortalGetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_policies_get_builder(client, body)?;
    prod_tt_sasportal_policies_get_execute(builder)
}

/// GET v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_policies_set_execute()` to send, or `prod_tt_sasportal_policies_set` for simplest API.

pub fn prod_tt_sasportal_policies_set_builder(
    client: &SimpleHttpClient,
    body: &SasPortalSetPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/policies:set",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_policies_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_policies_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_policies_set_builder()` + `prod_tt_sasportal_policies_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_set(
    client: &SimpleHttpClient,
    body: &SasPortalSetPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_policies_set_builder(client, body)?;
    prod_tt_sasportal_policies_set_execute(builder)
}

/// GET v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_policies_test_execute()` to send, or `prod_tt_sasportal_policies_test` for simplest API.

pub fn prod_tt_sasportal_policies_test_builder(
    client: &SimpleHttpClient,
    body: &SasPortalTestPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/policies:test",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `prod_tt_sasportal_policies_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from prod_tt_sasportal_policies_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalTestPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalTestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_policies_test_builder()` + `prod_tt_sasportal_policies_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_test(
    client: &SimpleHttpClient,
    body: &SasPortalTestPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalTestPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_policies_test_builder(client, body)?;
    prod_tt_sasportal_policies_test_execute(builder)
}
