//! Auto-generated API clients for gcp/prod_tt_sasportal.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_get_execute()` to send, or `prod_tt_sasportal_customers_get` for simplest API.

pub fn prod_tt_sasportal_customers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_get_execute()` or `prod_tt_sasportal_customers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalCustomer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalCustomer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}
/// Returns a requested customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_get_builder()` + `prod_tt_sasportal_customers_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_get_builder(client, &args.name)?;
    prod_tt_sasportal_customers_get_execute(builder)
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_list_execute()` to send, or `prod_tt_sasportal_customers_list` for simplest API.

pub fn prod_tt_sasportal_customers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_list_execute()` or `prod_tt_sasportal_customers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListCustomersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers
/// Returns a list of requested customers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_list_builder()` + `prod_tt_sasportal_customers_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListCustomersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_list_builder(client, &args.pageSize, &args.pageToken)?;
    prod_tt_sasportal_customers_list_execute(builder)
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_list_gcp_project_deployments_execute()` to send, or `prod_tt_sasportal_customers_list_gcp_project_deployments` for simplest API.

pub fn prod_tt_sasportal_customers_list_gcp_project_deployments_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:listGcpProjectDeployments",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_list_gcp_project_deployments_execute()` or `prod_tt_sasportal_customers_list_gcp_project_deployments`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_list_gcp_project_deployments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_gcp_project_deployments_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListGcpProjectDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListGcpProjectDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_list_gcp_project_deployments_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_list_gcp_project_deployments_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_list_gcp_project_deployments()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_list_gcp_project_deployments_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_list_gcp_project_deployments_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListGcpProjectDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_list_gcp_project_deployments_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:listGcpProjectDeployments
/// Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytics has been enabled or not.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_list_gcp_project_deployments_builder()` + `prod_tt_sasportal_customers_list_gcp_project_deployments_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_list_gcp_project_deployments_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_gcp_project_deployments(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListGcpProjectDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_list_gcp_project_deployments_builder(client)?;
    prod_tt_sasportal_customers_list_gcp_project_deployments_execute(builder)
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_list_legacy_organizations_execute()` to send, or `prod_tt_sasportal_customers_list_legacy_organizations` for simplest API.

pub fn prod_tt_sasportal_customers_list_legacy_organizations_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:listLegacyOrganizations",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_list_legacy_organizations_execute()` or `prod_tt_sasportal_customers_list_legacy_organizations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_list_legacy_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_legacy_organizations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListLegacyOrganizationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListLegacyOrganizationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_list_legacy_organizations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_list_legacy_organizations_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_list_legacy_organizations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_list_legacy_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_list_legacy_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListLegacyOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_list_legacy_organizations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/customers:listLegacyOrganizations
/// Returns a list of legacy organizations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_list_legacy_organizations_builder()` + `prod_tt_sasportal_customers_list_legacy_organizations_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_list_legacy_organizations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_list_legacy_organizations(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListLegacyOrganizationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_list_legacy_organizations_builder(client)?;
    prod_tt_sasportal_customers_list_legacy_organizations_execute(builder)
}

/// POST v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_migrate_organization_execute()` to send, or `prod_tt_sasportal_customers_migrate_organization` for simplest API.

pub fn prod_tt_sasportal_customers_migrate_organization_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:migrateOrganization",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_migrate_organization_execute()` or `prod_tt_sasportal_customers_migrate_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_migrate_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_migrate_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_migrate_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_migrate_organization_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_migrate_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_migrate_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_migrate_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_migrate_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/customers:migrateOrganization
/// Migrates a SAS organization to the cloud. This will create GCP projects for each deployment and associate them. The SAS Organization is linked to the gcp project that called the command. `go/sas-legacy-customer-migration`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_migrate_organization_builder()` + `prod_tt_sasportal_customers_migrate_organization_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_migrate_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_migrate_organization(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_migrate_organization_builder(client)?;
    prod_tt_sasportal_customers_migrate_organization_execute(builder)
}

/// PATCH v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_patch_execute()` to send, or `prod_tt_sasportal_customers_patch` for simplest API.

pub fn prod_tt_sasportal_customers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_patch_execute()` or `prod_tt_sasportal_customers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalCustomer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalCustomer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/customers/{customersId}
/// Updates an existing customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_patch_builder()` + `prod_tt_sasportal_customers_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalCustomer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_patch_builder(client, &args.name, &args.updateMask)?;
    prod_tt_sasportal_customers_patch_execute(builder)
}

/// POST v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_provision_deployment_execute()` to send, or `prod_tt_sasportal_customers_provision_deployment` for simplest API.

pub fn prod_tt_sasportal_customers_provision_deployment_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:provisionDeployment",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_provision_deployment_execute()` or `prod_tt_sasportal_customers_provision_deployment`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_provision_deployment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_provision_deployment_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalProvisionDeploymentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalProvisionDeploymentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_provision_deployment_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_provision_deployment_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_provision_deployment()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_provision_deployment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_provision_deployment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalProvisionDeploymentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_provision_deployment_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/customers:provisionDeployment
/// Creates a new SAS deployment through the GCP workflow. Creates a SAS organization if an organization match is not found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_provision_deployment_builder()` + `prod_tt_sasportal_customers_provision_deployment_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_provision_deployment_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_provision_deployment(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalProvisionDeploymentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_provision_deployment_builder(client)?;
    prod_tt_sasportal_customers_provision_deployment_execute(builder)
}

/// POST v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_setup_sas_analytics_execute()` to send, or `prod_tt_sasportal_customers_setup_sas_analytics` for simplest API.

pub fn prod_tt_sasportal_customers_setup_sas_analytics_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/customers:setupSasAnalytics",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_setup_sas_analytics_execute()` or `prod_tt_sasportal_customers_setup_sas_analytics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_setup_sas_analytics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_setup_sas_analytics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_setup_sas_analytics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_setup_sas_analytics_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_setup_sas_analytics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_setup_sas_analytics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_setup_sas_analytics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_setup_sas_analytics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/customers:setupSasAnalytics
/// Setups the a GCP Project to receive SAS Analytics messages via GCP P`ub/Sub` with a subscription to BigQuery. All the P`ub/Sub` topics and BigQuery tables are created automatically as part of this service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_setup_sas_analytics_builder()` + `prod_tt_sasportal_customers_setup_sas_analytics_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_setup_sas_analytics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_setup_sas_analytics(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_setup_sas_analytics_builder(client)?;
    prod_tt_sasportal_customers_setup_sas_analytics_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_create_execute()` to send, or `prod_tt_sasportal_customers_deployments_create` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_create_execute()` or `prod_tt_sasportal_customers_deployments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_create_builder()` + `prod_tt_sasportal_customers_deployments_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_create_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_deployments_create_execute(builder)
}

/// DELETE v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_delete_execute()` to send, or `prod_tt_sasportal_customers_deployments_delete` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_delete_execute()` or `prod_tt_sasportal_customers_deployments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_delete_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_delete_builder()` + `prod_tt_sasportal_customers_deployments_delete_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_delete(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_delete_builder(client, &args.name)?;
    prod_tt_sasportal_customers_deployments_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_get_execute()` to send, or `prod_tt_sasportal_customers_deployments_get` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_get_execute()` or `prod_tt_sasportal_customers_deployments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_get_builder()` + `prod_tt_sasportal_customers_deployments_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_get_builder(client, &args.name)?;
    prod_tt_sasportal_customers_deployments_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_list_execute()` to send, or `prod_tt_sasportal_customers_deployments_list` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_list_execute()` or `prod_tt_sasportal_customers_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers/{customersId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_list_builder()` + `prod_tt_sasportal_customers_deployments_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_customers_deployments_list_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_move_execute()` to send, or `prod_tt_sasportal_customers_deployments_move` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{deploymentsId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_move_execute()` or `prod_tt_sasportal_customers_deployments_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_move_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_move_builder()` + `prod_tt_sasportal_customers_deployments_move_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_move(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_move_builder(client, &args.name)?;
    prod_tt_sasportal_customers_deployments_move_execute(builder)
}

/// PATCH v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_patch_execute()` to send, or `prod_tt_sasportal_customers_deployments_patch` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_patch_execute()` or `prod_tt_sasportal_customers_deployments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/customers/{customersId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_patch_builder()` + `prod_tt_sasportal_customers_deployments_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    prod_tt_sasportal_customers_deployments_patch_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_devices_create_execute()` to send, or `prod_tt_sasportal_customers_deployments_devices_create` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_devices_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{deploymentsId}/devices",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_devices_create_execute()` or `prod_tt_sasportal_customers_deployments_devices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_devices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_devices_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_devices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_devices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_create_builder()` + `prod_tt_sasportal_customers_deployments_devices_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_devices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_deployments_devices_create_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_deployments_devices_create_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_devices_create_signed_execute()` to send, or `prod_tt_sasportal_customers_deployments_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_devices_create_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{deploymentsId}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_devices_create_signed_execute()` or `prod_tt_sasportal_customers_deployments_devices_create_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_devices_create_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_devices_create_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_devices_create_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_devices_create_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_create_signed_builder()` + `prod_tt_sasportal_customers_deployments_devices_create_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_devices_create_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_create_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_devices_create_signed_builder(
        client,
        &args.parent,
    )?;
    prod_tt_sasportal_customers_deployments_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_deployments_devices_list_execute()` to send, or `prod_tt_sasportal_customers_deployments_devices_list` for simplest API.

pub fn prod_tt_sasportal_customers_deployments_devices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/deployments/{deploymentsId}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_deployments_devices_list_execute()` or `prod_tt_sasportal_customers_deployments_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_deployments_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_devices_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_deployments_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_deployments_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_deployments_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_deployments_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_deployments_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDeploymentsDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_deployments_devices_list_builder()` + `prod_tt_sasportal_customers_deployments_devices_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_deployments_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_deployments_devices_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDeploymentsDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_deployments_devices_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_customers_deployments_devices_list_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_create_execute()` to send, or `prod_tt_sasportal_customers_devices_create` for simplest API.

pub fn prod_tt_sasportal_customers_devices_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_create_execute()` or `prod_tt_sasportal_customers_devices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_create_builder()` + `prod_tt_sasportal_customers_devices_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_create_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_devices_create_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_create_signed_execute()` to send, or `prod_tt_sasportal_customers_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_customers_devices_create_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_create_signed_execute()` or `prod_tt_sasportal_customers_devices_create_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_create_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_create_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_create_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_create_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_create_signed_builder()` + `prod_tt_sasportal_customers_devices_create_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_create_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_create_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_create_signed_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_devices_create_signed_execute(builder)
}

/// DELETE v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_delete_execute()` to send, or `prod_tt_sasportal_customers_devices_delete` for simplest API.

pub fn prod_tt_sasportal_customers_devices_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_delete_execute()` or `prod_tt_sasportal_customers_devices_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_delete_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha1/customers/{customersId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_delete_builder()` + `prod_tt_sasportal_customers_devices_delete_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_delete(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_delete_builder(client, &args.name)?;
    prod_tt_sasportal_customers_devices_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_get_execute()` to send, or `prod_tt_sasportal_customers_devices_get` for simplest API.

pub fn prod_tt_sasportal_customers_devices_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_get_execute()` or `prod_tt_sasportal_customers_devices_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_get_builder()` + `prod_tt_sasportal_customers_devices_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_get_builder(client, &args.name)?;
    prod_tt_sasportal_customers_devices_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_list_execute()` to send, or `prod_tt_sasportal_customers_devices_list` for simplest API.

pub fn prod_tt_sasportal_customers_devices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_list_execute()` or `prod_tt_sasportal_customers_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers/{customersId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_list_builder()` + `prod_tt_sasportal_customers_devices_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_customers_devices_list_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_move_execute()` to send, or `prod_tt_sasportal_customers_devices_move` for simplest API.

pub fn prod_tt_sasportal_customers_devices_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{devicesId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_move_execute()` or `prod_tt_sasportal_customers_devices_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_move_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_move_builder()` + `prod_tt_sasportal_customers_devices_move_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_move(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_move_builder(client, &args.name)?;
    prod_tt_sasportal_customers_devices_move_execute(builder)
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_patch_execute()` to send, or `prod_tt_sasportal_customers_devices_patch` for simplest API.

pub fn prod_tt_sasportal_customers_devices_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_patch_execute()` or `prod_tt_sasportal_customers_devices_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_patch_builder()` + `prod_tt_sasportal_customers_devices_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_devices_patch_builder(client, &args.name, &args.updateMask)?;
    prod_tt_sasportal_customers_devices_patch_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_sign_device_execute()` to send, or `prod_tt_sasportal_customers_devices_sign_device` for simplest API.

pub fn prod_tt_sasportal_customers_devices_sign_device_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{devicesId}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_sign_device_execute()` or `prod_tt_sasportal_customers_devices_sign_device`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_sign_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_sign_device_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_sign_device_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_sign_device_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_sign_device()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_sign_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_sign_device_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_sign_device`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesSignDeviceArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/customers/{customersId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_sign_device_builder()` + `prod_tt_sasportal_customers_devices_sign_device_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_sign_device_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_sign_device(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesSignDeviceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_sign_device_builder(client, &args.name)?;
    prod_tt_sasportal_customers_devices_sign_device_execute(builder)
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_devices_update_signed_execute()` to send, or `prod_tt_sasportal_customers_devices_update_signed` for simplest API.

pub fn prod_tt_sasportal_customers_devices_update_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/devices/{devicesId}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_devices_update_signed_execute()` or `prod_tt_sasportal_customers_devices_update_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_update_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_update_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_devices_update_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_devices_update_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_devices_update_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_devices_update_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_devices_update_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_devices_update_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersDevicesUpdateSignedArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1alpha1/customers/{customersId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_devices_update_signed_builder()` + `prod_tt_sasportal_customers_devices_update_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_devices_update_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_devices_update_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersDevicesUpdateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_devices_update_signed_builder(client, &args.name)?;
    prod_tt_sasportal_customers_devices_update_signed_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_create_execute()` or `prod_tt_sasportal_customers_nodes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_create_builder()` + `prod_tt_sasportal_customers_nodes_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_create_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_nodes_create_execute(builder)
}

/// DELETE v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_delete_execute()` to send, or `prod_tt_sasportal_customers_nodes_delete` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_delete_execute()` or `prod_tt_sasportal_customers_nodes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_delete_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Deletes a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_delete_builder()` + `prod_tt_sasportal_customers_nodes_delete_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_delete(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_delete_builder(client, &args.name)?;
    prod_tt_sasportal_customers_nodes_delete_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_get_execute()` to send, or `prod_tt_sasportal_customers_nodes_get` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_get_execute()` or `prod_tt_sasportal_customers_nodes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_get_builder()` + `prod_tt_sasportal_customers_nodes_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_get_builder(client, &args.name)?;
    prod_tt_sasportal_customers_nodes_get_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_list_execute()` or `prod_tt_sasportal_customers_nodes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers/{customersId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_list_builder()` + `prod_tt_sasportal_customers_nodes_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_list_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_move_execute()` to send, or `prod_tt_sasportal_customers_nodes_move` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_move_execute()` or `prod_tt_sasportal_customers_nodes_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_move_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}:move
/// Moves a node under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_move_builder()` + `prod_tt_sasportal_customers_nodes_move_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_move(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_move_builder(client, &args.name)?;
    prod_tt_sasportal_customers_nodes_move_execute(builder)
}

/// PATCH v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_patch_execute()` to send, or `prod_tt_sasportal_customers_nodes_patch` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_patch_execute()` or `prod_tt_sasportal_customers_nodes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/customers/{customersId}/nodes/{nodesId}
/// Updates an existing node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_patch_builder()` + `prod_tt_sasportal_customers_nodes_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_nodes_patch_builder(client, &args.name, &args.updateMask)?;
    prod_tt_sasportal_customers_nodes_patch_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_deployments_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_deployments_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_deployments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_deployments_create_execute()` or `prod_tt_sasportal_customers_nodes_deployments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_deployments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_deployments_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_deployments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_deployments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_deployments_create_builder()` + `prod_tt_sasportal_customers_nodes_deployments_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_deployments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_nodes_deployments_create_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_nodes_deployments_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_deployments_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_deployments_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_deployments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_deployments_list_execute()` or `prod_tt_sasportal_customers_nodes_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_deployments_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_deployments_list_builder()` + `prod_tt_sasportal_customers_nodes_deployments_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_deployments_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_deployments_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_deployments_list_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_devices_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_devices_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_devices_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}/devices",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_devices_create_execute()` or `prod_tt_sasportal_customers_nodes_devices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_devices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_devices_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_devices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_devices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_create_builder()` + `prod_tt_sasportal_customers_nodes_devices_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_devices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_devices_create_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_nodes_devices_create_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_devices_create_signed_execute()` to send, or `prod_tt_sasportal_customers_nodes_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_devices_create_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_devices_create_signed_execute()` or `prod_tt_sasportal_customers_nodes_devices_create_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_devices_create_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_devices_create_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_devices_create_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_devices_create_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_create_signed_builder()` + `prod_tt_sasportal_customers_nodes_devices_create_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_devices_create_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_customers_nodes_devices_create_signed_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_devices_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_devices_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_devices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_devices_list_execute()` or `prod_tt_sasportal_customers_nodes_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_devices_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_devices_list_builder()` + `prod_tt_sasportal_customers_nodes_devices_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_devices_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_devices_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_devices_list_execute(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_nodes_create_execute()` to send, or `prod_tt_sasportal_customers_nodes_nodes_create` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_nodes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_nodes_create_execute()` or `prod_tt_sasportal_customers_nodes_nodes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_nodes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_nodes_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_nodes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_nodes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_nodes_create_builder()` + `prod_tt_sasportal_customers_nodes_nodes_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_nodes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_nodes_create_builder(client, &args.parent)?;
    prod_tt_sasportal_customers_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_customers_nodes_nodes_list_execute()` to send, or `prod_tt_sasportal_customers_nodes_nodes_list` for simplest API.

pub fn prod_tt_sasportal_customers_nodes_nodes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/customers/{}/nodes/{nodesId}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_customers_nodes_nodes_list_execute()` or `prod_tt_sasportal_customers_nodes_nodes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_customers_nodes_nodes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_nodes_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_customers_nodes_nodes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_customers_nodes_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_customers_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_customers_nodes_nodes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_customers_nodes_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalCustomersNodesNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/customers/{customersId}/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_customers_nodes_nodes_list_builder()` + `prod_tt_sasportal_customers_nodes_nodes_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_customers_nodes_nodes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_customers_nodes_nodes_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalCustomersNodesNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_customers_nodes_nodes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_customers_nodes_nodes_list_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_get_execute()` to send, or `prod_tt_sasportal_deployments_get` for simplest API.

pub fn prod_tt_sasportal_deployments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_deployments_get_execute()` or `prod_tt_sasportal_deployments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_deployments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_deployments_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_deployments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_deployments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_get_builder()` + `prod_tt_sasportal_deployments_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_deployments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_get_builder(client, &args.name)?;
    prod_tt_sasportal_deployments_get_execute(builder)
}

/// DELETE v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_delete_execute()` to send, or `prod_tt_sasportal_deployments_devices_delete` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_deployments_devices_delete_execute()` or `prod_tt_sasportal_deployments_devices_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_deployments_devices_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_delete_task()`.
/// For the simplest API, use `prod_tt_sasportal_deployments_devices_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_deployments_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_deployments_devices_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_deployments_devices_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalDeploymentsDevicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_delete_builder()` + `prod_tt_sasportal_deployments_devices_delete_execute()`.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_delete(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalDeploymentsDevicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_delete_builder(client, &args.name)?;
    prod_tt_sasportal_deployments_devices_delete_execute(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_get_execute()` to send, or `prod_tt_sasportal_deployments_devices_get` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_deployments_devices_get_execute()` or `prod_tt_sasportal_deployments_devices_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_deployments_devices_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_deployments_devices_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_deployments_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_deployments_devices_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_deployments_devices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalDeploymentsDevicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_get_builder()` + `prod_tt_sasportal_deployments_devices_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalDeploymentsDevicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_get_builder(client, &args.name)?;
    prod_tt_sasportal_deployments_devices_get_execute(builder)
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_move_execute()` to send, or `prod_tt_sasportal_deployments_devices_move` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{devicesId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_deployments_devices_move_execute()` or `prod_tt_sasportal_deployments_devices_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_deployments_devices_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_move_task()`.
/// For the simplest API, use `prod_tt_sasportal_deployments_devices_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_deployments_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_deployments_devices_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_deployments_devices_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalDeploymentsDevicesMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_move_builder()` + `prod_tt_sasportal_deployments_devices_move_execute()`.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_move(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalDeploymentsDevicesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_move_builder(client, &args.name)?;
    prod_tt_sasportal_deployments_devices_move_execute(builder)
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_patch_execute()` to send, or `prod_tt_sasportal_deployments_devices_patch` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_deployments_devices_patch_execute()` or `prod_tt_sasportal_deployments_devices_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_deployments_devices_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_deployments_devices_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_deployments_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_deployments_devices_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_deployments_devices_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalDeploymentsDevicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_patch_builder()` + `prod_tt_sasportal_deployments_devices_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalDeploymentsDevicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_deployments_devices_patch_builder(client, &args.name, &args.updateMask)?;
    prod_tt_sasportal_deployments_devices_patch_execute(builder)
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_sign_device_execute()` to send, or `prod_tt_sasportal_deployments_devices_sign_device` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_sign_device_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{devicesId}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_deployments_devices_sign_device_execute()` or `prod_tt_sasportal_deployments_devices_sign_device`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_sign_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_sign_device_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_deployments_devices_sign_device_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_sign_device_task()`.
/// For the simplest API, use `prod_tt_sasportal_deployments_devices_sign_device()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_sign_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_deployments_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_deployments_devices_sign_device_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_deployments_devices_sign_device`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalDeploymentsDevicesSignDeviceArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_sign_device_builder()` + `prod_tt_sasportal_deployments_devices_sign_device_execute()`.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_sign_device_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_sign_device(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalDeploymentsDevicesSignDeviceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_sign_device_builder(client, &args.name)?;
    prod_tt_sasportal_deployments_devices_sign_device_execute(builder)
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_deployments_devices_update_signed_execute()` to send, or `prod_tt_sasportal_deployments_devices_update_signed` for simplest API.

pub fn prod_tt_sasportal_deployments_devices_update_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/deployments/{}/devices/{devicesId}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_deployments_devices_update_signed_execute()` or `prod_tt_sasportal_deployments_devices_update_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_update_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_update_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_deployments_devices_update_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_update_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_deployments_devices_update_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_deployments_devices_update_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_deployments_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_deployments_devices_update_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_deployments_devices_update_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalDeploymentsDevicesUpdateSignedArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1alpha1/deployments/{deploymentsId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_deployments_devices_update_signed_builder()` + `prod_tt_sasportal_deployments_devices_update_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_deployments_devices_update_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_deployments_devices_update_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalDeploymentsDevicesUpdateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_deployments_devices_update_signed_builder(client, &args.name)?;
    prod_tt_sasportal_deployments_devices_update_signed_execute(builder)
}

/// POST v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_installer_generate_secret_execute()` to send, or `prod_tt_sasportal_installer_generate_secret` for simplest API.

pub fn prod_tt_sasportal_installer_generate_secret_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/installer:generateSecret",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_installer_generate_secret_execute()` or `prod_tt_sasportal_installer_generate_secret`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_installer_generate_secret_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_generate_secret_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalGenerateSecretResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalGenerateSecretResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_installer_generate_secret_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_installer_generate_secret_task()`.
/// For the simplest API, use `prod_tt_sasportal_installer_generate_secret()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_installer_generate_secret_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_installer_generate_secret_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalGenerateSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_installer_generate_secret_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/installer:generateSecret
/// Generates a secret to be used with the ValidateInstaller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_installer_generate_secret_builder()` + `prod_tt_sasportal_installer_generate_secret_execute()`.
/// For task-level control, use `prod_tt_sasportal_installer_generate_secret_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_generate_secret(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalGenerateSecretResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_installer_generate_secret_builder(client)?;
    prod_tt_sasportal_installer_generate_secret_execute(builder)
}

/// POST v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_installer_validate_execute()` to send, or `prod_tt_sasportal_installer_validate` for simplest API.

pub fn prod_tt_sasportal_installer_validate_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/installer:validate",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_installer_validate_execute()` or `prod_tt_sasportal_installer_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_installer_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalValidateInstallerResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalValidateInstallerResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_installer_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_installer_validate_task()`.
/// For the simplest API, use `prod_tt_sasportal_installer_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_installer_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_installer_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalValidateInstallerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_installer_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/installer:validate
/// Validates the identity of a Certified Professional Installer (CPI).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_installer_validate_builder()` + `prod_tt_sasportal_installer_validate_execute()`.
/// For task-level control, use `prod_tt_sasportal_installer_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_installer_validate(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalValidateInstallerResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_installer_validate_builder(client)?;
    prod_tt_sasportal_installer_validate_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_get_execute()` to send, or `prod_tt_sasportal_nodes_get` for simplest API.

pub fn prod_tt_sasportal_nodes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_get_execute()` or `prod_tt_sasportal_nodes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_get_builder()` + `prod_tt_sasportal_nodes_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_get_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_get_execute(builder)
}

/// DELETE v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_delete_execute()` to send, or `prod_tt_sasportal_nodes_deployments_delete` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_delete_execute()` or `prod_tt_sasportal_nodes_deployments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_delete_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Deletes a deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_delete_builder()` + `prod_tt_sasportal_nodes_deployments_delete_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_delete(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_delete_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_deployments_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_get_execute()` to send, or `prod_tt_sasportal_nodes_deployments_get` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_get_execute()` or `prod_tt_sasportal_nodes_deployments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Returns a requested deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_get_builder()` + `prod_tt_sasportal_nodes_deployments_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_get_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_deployments_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_list_execute()` to send, or `prod_tt_sasportal_nodes_deployments_list` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_list_execute()` or `prod_tt_sasportal_nodes_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/nodes/{nodesId}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_list_builder()` + `prod_tt_sasportal_nodes_deployments_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_nodes_deployments_list_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_move_execute()` to send, or `prod_tt_sasportal_nodes_deployments_move` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{deploymentsId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_move_execute()` or `prod_tt_sasportal_nodes_deployments_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_move_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}:move
/// Moves a deployment under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_move_builder()` + `prod_tt_sasportal_nodes_deployments_move_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_move(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_move_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_deployments_move_execute(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_patch_execute()` to send, or `prod_tt_sasportal_nodes_deployments_patch` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_patch_execute()` or `prod_tt_sasportal_nodes_deployments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}
/// Updates an existing deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_patch_builder()` + `prod_tt_sasportal_nodes_deployments_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_deployments_patch_builder(client, &args.name, &args.updateMask)?;
    prod_tt_sasportal_nodes_deployments_patch_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_devices_create_execute()` to send, or `prod_tt_sasportal_nodes_deployments_devices_create` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{deploymentsId}/devices",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_devices_create_execute()` or `prod_tt_sasportal_nodes_deployments_devices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_devices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_devices_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_devices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_devices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_create_builder()` + `prod_tt_sasportal_nodes_deployments_devices_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_devices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_devices_create_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_deployments_devices_create_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_devices_create_signed_execute()` to send, or `prod_tt_sasportal_nodes_deployments_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{deploymentsId}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_devices_create_signed_execute()` or `prod_tt_sasportal_nodes_deployments_devices_create_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_devices_create_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_devices_create_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_devices_create_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_devices_create_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_create_signed_builder()` + `prod_tt_sasportal_nodes_deployments_devices_create_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_devices_create_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_create_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_deployments_devices_create_signed_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_deployments_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_deployments_devices_list_execute()` to send, or `prod_tt_sasportal_nodes_deployments_devices_list` for simplest API.

pub fn prod_tt_sasportal_nodes_deployments_devices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/deployments/{deploymentsId}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_deployments_devices_list_execute()` or `prod_tt_sasportal_nodes_deployments_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_deployments_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_devices_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_deployments_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_deployments_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_deployments_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_deployments_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_deployments_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDeploymentsDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_deployments_devices_list_builder()` + `prod_tt_sasportal_nodes_deployments_devices_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_deployments_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_deployments_devices_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDeploymentsDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_deployments_devices_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_nodes_deployments_devices_list_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_create_execute()` to send, or `prod_tt_sasportal_nodes_devices_create` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_create_execute()` or `prod_tt_sasportal_nodes_devices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_create_builder()` + `prod_tt_sasportal_nodes_devices_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_create_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_devices_create_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_create_signed_execute()` to send, or `prod_tt_sasportal_nodes_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_create_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_create_signed_execute()` or `prod_tt_sasportal_nodes_devices_create_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_create_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_create_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_create_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_create_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_create_signed_builder()` + `prod_tt_sasportal_nodes_devices_create_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_create_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_create_signed_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_devices_create_signed_execute(builder)
}

/// DELETE v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_delete_execute()` to send, or `prod_tt_sasportal_nodes_devices_delete` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_delete_execute()` or `prod_tt_sasportal_nodes_devices_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_delete_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Deletes a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_delete_builder()` + `prod_tt_sasportal_nodes_devices_delete_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_delete(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_delete_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_devices_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_get_execute()` to send, or `prod_tt_sasportal_nodes_devices_get` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_get_execute()` or `prod_tt_sasportal_nodes_devices_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Gets details about a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_get_builder()` + `prod_tt_sasportal_nodes_devices_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_get_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_devices_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_list_execute()` to send, or `prod_tt_sasportal_nodes_devices_list` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_list_execute()` or `prod_tt_sasportal_nodes_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/nodes/{nodesId}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_list_builder()` + `prod_tt_sasportal_nodes_devices_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_nodes_devices_list_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_move_execute()` to send, or `prod_tt_sasportal_nodes_devices_move` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{devicesId}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_move_execute()` or `prod_tt_sasportal_nodes_devices_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_move_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:move
/// Moves a device under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_move_builder()` + `prod_tt_sasportal_nodes_devices_move_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_move(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_move_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_devices_move_execute(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_patch_execute()` to send, or `prod_tt_sasportal_nodes_devices_patch` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{devicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_patch_execute()` or `prod_tt_sasportal_nodes_devices_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}
/// Updates a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_patch_builder()` + `prod_tt_sasportal_nodes_devices_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_devices_patch_builder(client, &args.name, &args.updateMask)?;
    prod_tt_sasportal_nodes_devices_patch_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_sign_device_execute()` to send, or `prod_tt_sasportal_nodes_devices_sign_device` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_sign_device_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{devicesId}:signDevice",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_sign_device_execute()` or `prod_tt_sasportal_nodes_devices_sign_device`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_sign_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_sign_device_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_sign_device_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_sign_device_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_sign_device()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_sign_device_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_sign_device_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_sign_device_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_sign_device`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesSignDeviceArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/nodes/{nodesId}/devices/{devicesId}:signDevice
/// Signs a device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_sign_device_builder()` + `prod_tt_sasportal_nodes_devices_sign_device_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_sign_device_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_sign_device(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesSignDeviceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_sign_device_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_devices_sign_device_execute(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_devices_update_signed_execute()` to send, or `prod_tt_sasportal_nodes_devices_update_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_devices_update_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/devices/{devicesId}:updateSigned",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_devices_update_signed_execute()` or `prod_tt_sasportal_nodes_devices_update_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_update_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_update_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_devices_update_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_update_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_devices_update_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_devices_update_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_devices_update_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_devices_update_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_devices_update_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesDevicesUpdateSignedArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1alpha1/nodes/{nodesId}/devices/{devicesId}:updateSigned
/// Updates a signed device.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_devices_update_signed_builder()` + `prod_tt_sasportal_nodes_devices_update_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_devices_update_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_devices_update_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesDevicesUpdateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_devices_update_signed_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_devices_update_signed_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_create_execute()` or `prod_tt_sasportal_nodes_nodes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_create_builder()` + `prod_tt_sasportal_nodes_nodes_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_create_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_nodes_create_execute(builder)
}

/// DELETE v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_delete_execute()` to send, or `prod_tt_sasportal_nodes_nodes_delete` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_delete_execute()` or `prod_tt_sasportal_nodes_nodes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_delete_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Deletes a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_delete_builder()` + `prod_tt_sasportal_nodes_nodes_delete_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_delete(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_delete_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_nodes_delete_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_get_execute()` to send, or `prod_tt_sasportal_nodes_nodes_get` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_get_execute()` or `prod_tt_sasportal_nodes_nodes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Returns a requested node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_get_builder()` + `prod_tt_sasportal_nodes_nodes_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_get(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_get_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_nodes_get_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_list_execute()` or `prod_tt_sasportal_nodes_nodes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_list_builder()` + `prod_tt_sasportal_nodes_nodes_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_nodes_nodes_list_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_move_execute()` to send, or `prod_tt_sasportal_nodes_nodes_move` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_move_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}:move",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_move_execute()` or `prod_tt_sasportal_nodes_nodes_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_move_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesMoveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}:move
/// Moves a node under another node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_move_builder()` + `prod_tt_sasportal_nodes_nodes_move_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_move(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_move_builder(client, &args.name)?;
    prod_tt_sasportal_nodes_nodes_move_execute(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_patch_execute()` to send, or `prod_tt_sasportal_nodes_nodes_patch` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_patch_execute()` or `prod_tt_sasportal_nodes_nodes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_patch_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1alpha1/nodes/{nodesId}/nodes/{nodesId1}
/// Updates an existing node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_patch_builder()` + `prod_tt_sasportal_nodes_nodes_patch_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_patch(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_nodes_patch_builder(client, &args.name, &args.updateMask)?;
    prod_tt_sasportal_nodes_nodes_patch_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_deployments_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_deployments_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_deployments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}/deployments",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_deployments_create_execute()` or `prod_tt_sasportal_nodes_nodes_deployments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_deployments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_deployments_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_deployments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_deployments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Creates a new deployment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_deployments_create_builder()` + `prod_tt_sasportal_nodes_nodes_deployments_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_deployments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_deployments_create_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_nodes_deployments_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_deployments_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_deployments_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_deployments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_deployments_list_execute()` or `prod_tt_sasportal_nodes_nodes_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_deployments_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/deployments
/// Lists deployments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_deployments_list_builder()` + `prod_tt_sasportal_nodes_nodes_deployments_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_deployments_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_deployments_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_nodes_nodes_deployments_list_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_devices_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_devices_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}/devices",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_devices_create_execute()` or `prod_tt_sasportal_nodes_nodes_devices_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_devices_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_devices_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_devices_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_devices_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_devices_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_devices_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesDevicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Creates a device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_create_builder()` + `prod_tt_sasportal_nodes_nodes_devices_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_devices_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesDevicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_devices_create_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_nodes_devices_create_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_devices_create_signed_execute()` to send, or `prod_tt_sasportal_nodes_nodes_devices_create_signed` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_signed_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}/devices:createSigned",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_devices_create_signed_execute()` or `prod_tt_sasportal_nodes_nodes_devices_create_signed`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_signed_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalDevice = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_devices_create_signed_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_devices_create_signed_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_devices_create_signed()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_devices_create_signed_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_signed_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_devices_create_signed_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_devices_create_signed`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesDevicesCreateSignedArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
/// Creates a signed device under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_create_signed_builder()` + `prod_tt_sasportal_nodes_nodes_devices_create_signed_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_devices_create_signed_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_create_signed(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesDevicesCreateSignedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalDevice>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        prod_tt_sasportal_nodes_nodes_devices_create_signed_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_nodes_devices_create_signed_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_devices_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_devices_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_devices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}/devices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_devices_list_execute()` or `prod_tt_sasportal_nodes_nodes_devices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListDevicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_devices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_devices_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_devices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_devices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_devices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_devices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_devices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesDevicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices
/// Lists devices under a node or customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_devices_list_builder()` + `prod_tt_sasportal_nodes_nodes_devices_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_devices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_devices_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesDevicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListDevicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_devices_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_nodes_nodes_devices_list_execute(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_nodes_create_execute()` to send, or `prod_tt_sasportal_nodes_nodes_nodes_create` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_nodes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}/nodes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_nodes_create_execute()` or `prod_tt_sasportal_nodes_nodes_nodes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalNode>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalNode = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_nodes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_nodes_create_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_nodes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_nodes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Creates a new node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_nodes_create_builder()` + `prod_tt_sasportal_nodes_nodes_nodes_create_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_nodes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_create(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalNode>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_nodes_create_builder(client, &args.parent)?;
    prod_tt_sasportal_nodes_nodes_nodes_create_execute(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_nodes_nodes_nodes_list_execute()` to send, or `prod_tt_sasportal_nodes_nodes_nodes_list` for simplest API.

pub fn prod_tt_sasportal_nodes_nodes_nodes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://prod-tt-sasportal.googleapis.com/v1alpha1/nodes/{}/nodes/{nodesId1}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_nodes_nodes_nodes_list_execute()` or `prod_tt_sasportal_nodes_nodes_nodes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_nodes_nodes_nodes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_nodes_list_task()`.
/// For the simplest API, use `prod_tt_sasportal_nodes_nodes_nodes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_nodes_nodes_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_nodes_nodes_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_nodes_nodes_nodes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`prod_tt_sasportal_nodes_nodes_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ProdTtSasportalNodesNodesNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_nodes_nodes_nodes_list_builder()` + `prod_tt_sasportal_nodes_nodes_nodes_list_execute()`.
/// For task-level control, use `prod_tt_sasportal_nodes_nodes_nodes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_nodes_nodes_nodes_list(
    client: &SimpleHttpClient,
    args: &ProdTtSasportalNodesNodesNodesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalListNodesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_nodes_nodes_nodes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    prod_tt_sasportal_nodes_nodes_nodes_list_execute(builder)
}

/// POST v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_policies_get_execute()` to send, or `prod_tt_sasportal_policies_get` for simplest API.

pub fn prod_tt_sasportal_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/policies:get",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_policies_get_execute()` or `prod_tt_sasportal_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_policies_get_task()`.
/// For the simplest API, use `prod_tt_sasportal_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/policies:get
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_policies_get_builder()` + `prod_tt_sasportal_policies_get_execute()`.
/// For task-level control, use `prod_tt_sasportal_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_policies_get_builder(client)?;
    prod_tt_sasportal_policies_get_execute(builder)
}

/// POST v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_policies_set_execute()` to send, or `prod_tt_sasportal_policies_set` for simplest API.

pub fn prod_tt_sasportal_policies_set_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/policies:set",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_policies_set_execute()` or `prod_tt_sasportal_policies_set`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_policies_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_set_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_policies_set_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_policies_set_task()`.
/// For the simplest API, use `prod_tt_sasportal_policies_set()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_policies_set_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_policies_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_policies_set_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/policies:set
/// Sets the access control policy on the specified resource. Replaces any existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_policies_set_builder()` + `prod_tt_sasportal_policies_set_execute()`.
/// For task-level control, use `prod_tt_sasportal_policies_set_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_set(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SasPortalPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_policies_set_builder(client)?;
    prod_tt_sasportal_policies_set_execute(builder)
}

/// POST v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `prod_tt_sasportal_policies_test_execute()` to send, or `prod_tt_sasportal_policies_test` for simplest API.

pub fn prod_tt_sasportal_policies_test_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://prod-tt-sasportal.googleapis.com/v1alpha1/policies:test",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `prod_tt_sasportal_policies_test_execute()` or `prod_tt_sasportal_policies_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_policies_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalTestPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SasPortalTestPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `prod_tt_sasportal_policies_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `prod_tt_sasportal_policies_test_task()`.
/// For the simplest API, use `prod_tt_sasportal_policies_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `prod_tt_sasportal_policies_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn prod_tt_sasportal_policies_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalTestPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = prod_tt_sasportal_policies_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha1/policies:test
/// Returns permissions that a caller has on the specified resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `prod_tt_sasportal_policies_test_builder()` + `prod_tt_sasportal_policies_test_execute()`.
/// For task-level control, use `prod_tt_sasportal_policies_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn prod_tt_sasportal_policies_test(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SasPortalTestPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = prod_tt_sasportal_policies_test_builder(client)?;
    prod_tt_sasportal_policies_test_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalCustomer
// =============================================================================

/// ResourceIdentifier implementation for SasPortalCustomer with ProdTtSasportalCustomersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersGetArgs> for SasPortalCustomer {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalCustomer/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalCustomer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListCustomersResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListCustomersResponse with ProdTtSasportalCustomersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersListArgs> for SasPortalListCustomersResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersListArgs) -> String {
        "gcp::prod_tt_sasportal::SasPortalListCustomersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListCustomersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListGcpProjectDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListGcpProjectDeploymentsResponse with ProdTtSasportalCustomersListGcpProjectDeploymentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersListGcpProjectDeploymentsArgs>
    for SasPortalListGcpProjectDeploymentsResponse
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersListGcpProjectDeploymentsArgs,
    ) -> String {
        "gcp::prod_tt_sasportal::SasPortalListGcpProjectDeploymentsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListGcpProjectDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListLegacyOrganizationsResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListLegacyOrganizationsResponse with ProdTtSasportalCustomersListLegacyOrganizationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersListLegacyOrganizationsArgs>
    for SasPortalListLegacyOrganizationsResponse
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersListLegacyOrganizationsArgs,
    ) -> String {
        "gcp::prod_tt_sasportal::SasPortalListLegacyOrganizationsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListLegacyOrganizationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalCustomersMigrateOrganizationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersMigrateOrganizationArgs> for SasPortalOperation {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersMigrateOrganizationArgs,
    ) -> String {
        "gcp::prod_tt_sasportal::SasPortalOperation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalCustomer
// =============================================================================

/// ResourceIdentifier implementation for SasPortalCustomer with ProdTtSasportalCustomersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersPatchArgs> for SasPortalCustomer {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalCustomer/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalCustomer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalProvisionDeploymentResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalProvisionDeploymentResponse with ProdTtSasportalCustomersProvisionDeploymentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersProvisionDeploymentArgs>
    for SasPortalProvisionDeploymentResponse
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersProvisionDeploymentArgs,
    ) -> String {
        "gcp::prod_tt_sasportal::SasPortalProvisionDeploymentResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalProvisionDeploymentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalCustomersSetupSasAnalyticsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersSetupSasAnalyticsArgs> for SasPortalOperation {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersSetupSasAnalyticsArgs,
    ) -> String {
        "gcp::prod_tt_sasportal::SasPortalOperation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalCustomersDeploymentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsCreateArgs> for SasPortalDeployment {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDeploymentsCreateArgs,
    ) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalDeployment/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalCustomersDeploymentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsDeleteArgs> for SasPortalEmpty {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDeploymentsDeleteArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalCustomersDeploymentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsGetArgs> for SasPortalDeployment {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDeploymentsGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDeploymentsResponse with ProdTtSasportalCustomersDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsListArgs>
    for SasPortalListDeploymentsResponse
{
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDeploymentsListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalCustomersDeploymentsMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsMoveArgs> for SasPortalOperation {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDeploymentsMoveArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalCustomersDeploymentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsPatchArgs> for SasPortalDeployment {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDeploymentsPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersDeploymentsDevicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsDevicesCreateArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDeploymentsDevicesCreateArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersDeploymentsDevicesCreateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsDevicesCreateSignedArgs>
    for SasPortalDevice
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDeploymentsDevicesCreateSignedArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDevicesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDevicesResponse with ProdTtSasportalCustomersDeploymentsDevicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDeploymentsDevicesListArgs>
    for SasPortalListDevicesResponse
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDeploymentsDevicesListArgs,
    ) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDevicesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDevicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersDevicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesCreateArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDevicesCreateArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersDevicesCreateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesCreateSignedArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDevicesCreateSignedArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalCustomersDevicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesDeleteArgs> for SasPortalEmpty {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDevicesDeleteArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersDevicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesGetArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDevicesGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDevicesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDevicesResponse with ProdTtSasportalCustomersDevicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesListArgs> for SasPortalListDevicesResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDevicesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDevicesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDevicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalCustomersDevicesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesMoveArgs> for SasPortalOperation {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDevicesMoveArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersDevicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesPatchArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersDevicesPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalCustomersDevicesSignDeviceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesSignDeviceArgs> for SasPortalEmpty {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDevicesSignDeviceArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersDevicesUpdateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersDevicesUpdateSignedArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersDevicesUpdateSignedArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalCustomersNodesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesCreateArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesCreateArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalCustomersNodesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesDeleteArgs> for SasPortalEmpty {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesDeleteArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalCustomersNodesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesGetArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListNodesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListNodesResponse with ProdTtSasportalCustomersNodesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesListArgs> for SasPortalListNodesResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListNodesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListNodesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalCustomersNodesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesMoveArgs> for SasPortalOperation {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesMoveArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalCustomersNodesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesPatchArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalCustomersNodesDeploymentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesDeploymentsCreateArgs>
    for SasPortalDeployment
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersNodesDeploymentsCreateArgs,
    ) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalDeployment/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDeploymentsResponse with ProdTtSasportalCustomersNodesDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesDeploymentsListArgs>
    for SasPortalListDeploymentsResponse
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersNodesDeploymentsListArgs,
    ) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersNodesDevicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesDevicesCreateArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersNodesDevicesCreateArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalCustomersNodesDevicesCreateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesDevicesCreateSignedArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalCustomersNodesDevicesCreateSignedArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDevicesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDevicesResponse with ProdTtSasportalCustomersNodesDevicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesDevicesListArgs>
    for SasPortalListDevicesResponse
{
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesDevicesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDevicesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDevicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalCustomersNodesNodesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesNodesCreateArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesNodesCreateArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListNodesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListNodesResponse with ProdTtSasportalCustomersNodesNodesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalCustomersNodesNodesListArgs> for SasPortalListNodesResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalCustomersNodesNodesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListNodesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListNodesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalDeploymentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalDeploymentsGetArgs> for SasPortalDeployment {
    fn generate_resource_id(&self, input: &ProdTtSasportalDeploymentsGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalDeploymentsDevicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalDeploymentsDevicesDeleteArgs> for SasPortalEmpty {
    fn generate_resource_id(&self, input: &ProdTtSasportalDeploymentsDevicesDeleteArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalDeploymentsDevicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalDeploymentsDevicesGetArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalDeploymentsDevicesGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalDeploymentsDevicesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalDeploymentsDevicesMoveArgs> for SasPortalOperation {
    fn generate_resource_id(&self, input: &ProdTtSasportalDeploymentsDevicesMoveArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalDeploymentsDevicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalDeploymentsDevicesPatchArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalDeploymentsDevicesPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalDeploymentsDevicesSignDeviceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalDeploymentsDevicesSignDeviceArgs> for SasPortalEmpty {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalDeploymentsDevicesSignDeviceArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalDeploymentsDevicesUpdateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalDeploymentsDevicesUpdateSignedArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalDeploymentsDevicesUpdateSignedArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalGenerateSecretResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalGenerateSecretResponse with ProdTtSasportalInstallerGenerateSecretArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalInstallerGenerateSecretArgs>
    for SasPortalGenerateSecretResponse
{
    fn generate_resource_id(&self, input: &ProdTtSasportalInstallerGenerateSecretArgs) -> String {
        "gcp::prod_tt_sasportal::SasPortalGenerateSecretResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalGenerateSecretResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalValidateInstallerResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalValidateInstallerResponse with ProdTtSasportalInstallerValidateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalInstallerValidateArgs>
    for SasPortalValidateInstallerResponse
{
    fn generate_resource_id(&self, input: &ProdTtSasportalInstallerValidateArgs) -> String {
        "gcp::prod_tt_sasportal::SasPortalValidateInstallerResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalValidateInstallerResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalNodesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesGetArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalNodesDeploymentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsDeleteArgs> for SasPortalEmpty {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDeploymentsDeleteArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalNodesDeploymentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsGetArgs> for SasPortalDeployment {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDeploymentsGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDeploymentsResponse with ProdTtSasportalNodesDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsListArgs>
    for SasPortalListDeploymentsResponse
{
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDeploymentsListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalNodesDeploymentsMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsMoveArgs> for SasPortalOperation {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDeploymentsMoveArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalNodesDeploymentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsPatchArgs> for SasPortalDeployment {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDeploymentsPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesDeploymentsDevicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsDevicesCreateArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalNodesDeploymentsDevicesCreateArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesDeploymentsDevicesCreateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsDevicesCreateSignedArgs>
    for SasPortalDevice
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalNodesDeploymentsDevicesCreateSignedArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDevicesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDevicesResponse with ProdTtSasportalNodesDeploymentsDevicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDeploymentsDevicesListArgs>
    for SasPortalListDevicesResponse
{
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalNodesDeploymentsDevicesListArgs,
    ) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDevicesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDevicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesDevicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesCreateArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesCreateArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesDevicesCreateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesCreateSignedArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesCreateSignedArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalNodesDevicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesDeleteArgs> for SasPortalEmpty {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesDeleteArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesDevicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesGetArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDevicesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDevicesResponse with ProdTtSasportalNodesDevicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesListArgs> for SasPortalListDevicesResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDevicesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDevicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalNodesDevicesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesMoveArgs> for SasPortalOperation {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesMoveArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesDevicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesPatchArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalNodesDevicesSignDeviceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesSignDeviceArgs> for SasPortalEmpty {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesSignDeviceArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesDevicesUpdateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesDevicesUpdateSignedArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesDevicesUpdateSignedArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalNodesNodesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesCreateArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesCreateArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalEmpty
// =============================================================================

/// ResourceIdentifier implementation for SasPortalEmpty with ProdTtSasportalNodesNodesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesDeleteArgs> for SasPortalEmpty {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesDeleteArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalNodesNodesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesGetArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesGetArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListNodesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListNodesResponse with ProdTtSasportalNodesNodesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesListArgs> for SasPortalListNodesResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListNodesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListNodesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalOperation
// =============================================================================

/// ResourceIdentifier implementation for SasPortalOperation with ProdTtSasportalNodesNodesMoveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesMoveArgs> for SasPortalOperation {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesMoveArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalNodesNodesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesPatchArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesPatchArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDeployment
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDeployment with ProdTtSasportalNodesNodesDeploymentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesDeploymentsCreateArgs> for SasPortalDeployment {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalNodesNodesDeploymentsCreateArgs,
    ) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalDeployment/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDeploymentsResponse with ProdTtSasportalNodesNodesDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesDeploymentsListArgs>
    for SasPortalListDeploymentsResponse
{
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesDeploymentsListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesNodesDevicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesDevicesCreateArgs> for SasPortalDevice {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesDevicesCreateArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalDevice
// =============================================================================

/// ResourceIdentifier implementation for SasPortalDevice with ProdTtSasportalNodesNodesDevicesCreateSignedArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesDevicesCreateSignedArgs> for SasPortalDevice {
    fn generate_resource_id(
        &self,
        input: &ProdTtSasportalNodesNodesDevicesCreateSignedArgs,
    ) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalDevice/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalDevice"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListDevicesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListDevicesResponse with ProdTtSasportalNodesNodesDevicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesDevicesListArgs> for SasPortalListDevicesResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesDevicesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListDevicesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListDevicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalNode
// =============================================================================

/// ResourceIdentifier implementation for SasPortalNode with ProdTtSasportalNodesNodesNodesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesNodesCreateArgs> for SasPortalNode {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesNodesCreateArgs) -> String {
        format!("gcp::prod_tt_sasportal::SasPortalNode/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalNode"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalListNodesResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalListNodesResponse with ProdTtSasportalNodesNodesNodesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalNodesNodesNodesListArgs> for SasPortalListNodesResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalNodesNodesNodesListArgs) -> String {
        format!(
            "gcp::prod_tt_sasportal::SasPortalListNodesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalListNodesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalPolicy
// =============================================================================

/// ResourceIdentifier implementation for SasPortalPolicy with ProdTtSasportalPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalPoliciesGetArgs> for SasPortalPolicy {
    fn generate_resource_id(&self, input: &ProdTtSasportalPoliciesGetArgs) -> String {
        "gcp::prod_tt_sasportal::SasPortalPolicy".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalPolicy
// =============================================================================

/// ResourceIdentifier implementation for SasPortalPolicy with ProdTtSasportalPoliciesSetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalPoliciesSetArgs> for SasPortalPolicy {
    fn generate_resource_id(&self, input: &ProdTtSasportalPoliciesSetArgs) -> String {
        "gcp::prod_tt_sasportal::SasPortalPolicy".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SasPortalTestPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for SasPortalTestPermissionsResponse with ProdTtSasportalPoliciesTestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ProdTtSasportalPoliciesTestArgs> for SasPortalTestPermissionsResponse {
    fn generate_resource_id(&self, input: &ProdTtSasportalPoliciesTestArgs) -> String {
        "gcp::prod_tt_sasportal::SasPortalTestPermissionsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::prod_tt_sasportal::SasPortalTestPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
