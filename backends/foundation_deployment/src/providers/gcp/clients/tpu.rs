//! Auto-generated API clients for gcp/tpu.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/projects/{projectsId}/locations/{locationsId}:generateServiceIdentity
/// Generates the Cloud TPU service identity for the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_generate_service_identity_execute()` to send, or `tpu_projects_locations_generate_service_identity` for simplest API.

pub fn tpu_projects_locations_generate_service_identity_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}:generateServiceIdentity",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}:generateServiceIdentity
/// Generates the Cloud TPU service identity for the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_generate_service_identity_execute()` or `tpu_projects_locations_generate_service_identity`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_generate_service_identity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_generate_service_identity_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateServiceIdentityResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateServiceIdentityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}:generateServiceIdentity
/// Generates the Cloud TPU service identity for the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_generate_service_identity_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_generate_service_identity_task()`.
/// For the simplest API, use `tpu_projects_locations_generate_service_identity()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_generate_service_identity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_generate_service_identity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateServiceIdentityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_generate_service_identity_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_generate_service_identity`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsGenerateServiceIdentityArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}:generateServiceIdentity
/// Generates the Cloud TPU service identity for the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_generate_service_identity_builder()` + `tpu_projects_locations_generate_service_identity_execute()`.
/// For task-level control, use `tpu_projects_locations_generate_service_identity_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_generate_service_identity(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsGenerateServiceIdentityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateServiceIdentityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_generate_service_identity_builder(client, &args.parent)?;
    tpu_projects_locations_generate_service_identity_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_get_execute()` to send, or `tpu_projects_locations_get` for simplest API.

pub fn tpu_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_get_execute()` or `tpu_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_get_task()`.
/// For the simplest API, use `tpu_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_get_builder()` + `tpu_projects_locations_get_execute()`.
/// For task-level control, use `tpu_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_get(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_get_builder(client, &args.name)?;
    tpu_projects_locations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_list_execute()` to send, or `tpu_projects_locations_list` for simplest API.

pub fn tpu_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://tpu.googleapis.com/v2/projects/{}/locations", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_list_execute()` or `tpu_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_list_task()`.
/// For the simplest API, use `tpu_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_list_builder()` + `tpu_projects_locations_list_execute()`.
/// For task-level control, use `tpu_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_list(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    tpu_projects_locations_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes/{acceleratorTypesId}
/// Gets AcceleratorType.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_accelerator_types_get_execute()` to send, or `tpu_projects_locations_accelerator_types_get` for simplest API.

pub fn tpu_projects_locations_accelerator_types_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/acceleratorTypes/{acceleratorTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes/{acceleratorTypesId}
/// Gets AcceleratorType.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_accelerator_types_get_execute()` or `tpu_projects_locations_accelerator_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_accelerator_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_accelerator_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AcceleratorType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AcceleratorType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes/{acceleratorTypesId}
/// Gets AcceleratorType.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_accelerator_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_accelerator_types_get_task()`.
/// For the simplest API, use `tpu_projects_locations_accelerator_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_accelerator_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_accelerator_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_accelerator_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_accelerator_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsAcceleratorTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes/{acceleratorTypesId}
/// Gets AcceleratorType.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_accelerator_types_get_builder()` + `tpu_projects_locations_accelerator_types_get_execute()`.
/// For task-level control, use `tpu_projects_locations_accelerator_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_accelerator_types_get(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsAcceleratorTypesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AcceleratorType>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_accelerator_types_get_builder(client, &args.name)?;
    tpu_projects_locations_accelerator_types_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes
/// Lists accelerator types supported by this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_accelerator_types_list_execute()` to send, or `tpu_projects_locations_accelerator_types_list` for simplest API.

pub fn tpu_projects_locations_accelerator_types_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/acceleratorTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes
/// Lists accelerator types supported by this API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_accelerator_types_list_execute()` or `tpu_projects_locations_accelerator_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_accelerator_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_accelerator_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAcceleratorTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAcceleratorTypesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes
/// Lists accelerator types supported by this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_accelerator_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_accelerator_types_list_task()`.
/// For the simplest API, use `tpu_projects_locations_accelerator_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_accelerator_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_accelerator_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAcceleratorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_accelerator_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_accelerator_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsAcceleratorTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/acceleratorTypes
/// Lists accelerator types supported by this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_accelerator_types_list_builder()` + `tpu_projects_locations_accelerator_types_list_execute()`.
/// For task-level control, use `tpu_projects_locations_accelerator_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_accelerator_types_list(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsAcceleratorTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAcceleratorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_accelerator_types_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    tpu_projects_locations_accelerator_types_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Creates a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_create_execute()` to send, or `tpu_projects_locations_nodes_create` for simplest API.

pub fn tpu_projects_locations_nodes_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    nodeId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = nodeId.as_ref() {
        query_parts.push(format!("nodeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Creates a node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_create_execute()` or `tpu_projects_locations_nodes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Creates a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_create_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: nodeId
    pub nodeId: Option<Option<String>>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Creates a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_create_builder()` + `tpu_projects_locations_nodes_create_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_create(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_create_builder(client, &args.parent, &args.nodeId)?;
    tpu_projects_locations_nodes_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Deletes a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_delete_execute()` to send, or `tpu_projects_locations_nodes_delete` for simplest API.

pub fn tpu_projects_locations_nodes_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes/{nodesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_delete_execute()` or `tpu_projects_locations_nodes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Deletes a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_delete_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Deletes a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_delete_builder()` + `tpu_projects_locations_nodes_delete_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_delete(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_delete_builder(client, &args.name)?;
    tpu_projects_locations_nodes_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Gets the details of a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_get_execute()` to send, or `tpu_projects_locations_nodes_get` for simplest API.

pub fn tpu_projects_locations_nodes_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes/{nodesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Gets the details of a node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_get_execute()` or `tpu_projects_locations_nodes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Node>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Node = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Gets the details of a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_get_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Node>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Gets the details of a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_get_builder()` + `tpu_projects_locations_nodes_get_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_get(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Node>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_get_builder(client, &args.name)?;
    tpu_projects_locations_nodes_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:getGuestAttributes
/// Retrieves the guest attributes for the node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_get_guest_attributes_execute()` to send, or `tpu_projects_locations_nodes_get_guest_attributes` for simplest API.

pub fn tpu_projects_locations_nodes_get_guest_attributes_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes/{nodesId}:getGuestAttributes",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:getGuestAttributes
/// Retrieves the guest attributes for the node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_get_guest_attributes_execute()` or `tpu_projects_locations_nodes_get_guest_attributes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_get_guest_attributes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_get_guest_attributes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetGuestAttributesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetGuestAttributesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:getGuestAttributes
/// Retrieves the guest attributes for the node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_get_guest_attributes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_get_guest_attributes_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_get_guest_attributes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_get_guest_attributes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_get_guest_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetGuestAttributesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_get_guest_attributes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_get_guest_attributes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesGetGuestAttributesArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:getGuestAttributes
/// Retrieves the guest attributes for the node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_get_guest_attributes_builder()` + `tpu_projects_locations_nodes_get_guest_attributes_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_get_guest_attributes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_get_guest_attributes(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesGetGuestAttributesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetGuestAttributesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_get_guest_attributes_builder(client, &args.name)?;
    tpu_projects_locations_nodes_get_guest_attributes_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Lists nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_list_execute()` to send, or `tpu_projects_locations_nodes_list` for simplest API.

pub fn tpu_projects_locations_nodes_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_list_execute()` or `tpu_projects_locations_nodes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNodesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNodesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Lists nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_list_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNodesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nodes
/// Lists nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_list_builder()` + `tpu_projects_locations_nodes_list_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_list(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNodesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    tpu_projects_locations_nodes_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Updates the configurations of a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_patch_execute()` to send, or `tpu_projects_locations_nodes_patch` for simplest API.

pub fn tpu_projects_locations_nodes_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes/{nodesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Updates the configurations of a node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_patch_execute()` or `tpu_projects_locations_nodes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Updates the configurations of a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_patch_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}
/// Updates the configurations of a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_patch_builder()` + `tpu_projects_locations_nodes_patch_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_patch(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_patch_builder(client, &args.name, &args.updateMask)?;
    tpu_projects_locations_nodes_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:start
/// Starts a node.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_start_execute()` to send, or `tpu_projects_locations_nodes_start` for simplest API.

pub fn tpu_projects_locations_nodes_start_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes/{nodesId}:start",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:start
/// Starts a node.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_start_execute()` or `tpu_projects_locations_nodes_start`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_start_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:start
/// Starts a node.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_start_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_start_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_start()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_start_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_start`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesStartArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:start
/// Starts a node.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_start_builder()` + `tpu_projects_locations_nodes_start_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_start_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_start(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesStartArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_start_builder(client, &args.name)?;
    tpu_projects_locations_nodes_start_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:stop
/// Stops a node. This operation is only available with single TPU nodes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_nodes_stop_execute()` to send, or `tpu_projects_locations_nodes_stop` for simplest API.

pub fn tpu_projects_locations_nodes_stop_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/nodes/{nodesId}:stop",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:stop
/// Stops a node. This operation is only available with single TPU nodes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_nodes_stop_execute()` or `tpu_projects_locations_nodes_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:stop
/// Stops a node. This operation is only available with single TPU nodes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_nodes_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_nodes_stop_task()`.
/// For the simplest API, use `tpu_projects_locations_nodes_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_nodes_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_nodes_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_nodes_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_nodes_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsNodesStopArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nodes/{nodesId}:stop
/// Stops a node. This operation is only available with single TPU nodes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_nodes_stop_builder()` + `tpu_projects_locations_nodes_stop_execute()`.
/// For task-level control, use `tpu_projects_locations_nodes_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_nodes_stop(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsNodesStopArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_nodes_stop_builder(client, &args.name)?;
    tpu_projects_locations_nodes_stop_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_operations_cancel_execute()` to send, or `tpu_projects_locations_operations_cancel` for simplest API.

pub fn tpu_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_operations_cancel_execute()` or `tpu_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `tpu_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_operations_cancel_builder()` + `tpu_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `tpu_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_operations_cancel_builder(client, &args.name)?;
    tpu_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_operations_delete_execute()` to send, or `tpu_projects_locations_operations_delete` for simplest API.

pub fn tpu_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_operations_delete_execute()` or `tpu_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_operations_delete_task()`.
/// For the simplest API, use `tpu_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_operations_delete_builder()` + `tpu_projects_locations_operations_delete_execute()`.
/// For task-level control, use `tpu_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_operations_delete_builder(client, &args.name)?;
    tpu_projects_locations_operations_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_operations_get_execute()` to send, or `tpu_projects_locations_operations_get` for simplest API.

pub fn tpu_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_operations_get_execute()` or `tpu_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_operations_get_task()`.
/// For the simplest API, use `tpu_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_operations_get_builder()` + `tpu_projects_locations_operations_get_execute()`.
/// For task-level control, use `tpu_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_operations_get_builder(client, &args.name)?;
    tpu_projects_locations_operations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_operations_list_execute()` to send, or `tpu_projects_locations_operations_list` for simplest API.

pub fn tpu_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_operations_list_execute()` or `tpu_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_operations_list_task()`.
/// For the simplest API, use `tpu_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_operations_list_builder()` + `tpu_projects_locations_operations_list_execute()`.
/// For task-level control, use `tpu_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    tpu_projects_locations_operations_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Creates a QueuedResource TPU instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_queued_resources_create_execute()` to send, or `tpu_projects_locations_queued_resources_create` for simplest API.

pub fn tpu_projects_locations_queued_resources_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    queuedResourceId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/queuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = queuedResourceId.as_ref() {
        query_parts.push(format!("queuedResourceId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Creates a QueuedResource TPU instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_queued_resources_create_execute()` or `tpu_projects_locations_queued_resources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Creates a QueuedResource TPU instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_queued_resources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_queued_resources_create_task()`.
/// For the simplest API, use `tpu_projects_locations_queued_resources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_queued_resources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_queued_resources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_queued_resources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsQueuedResourcesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: queuedResourceId
    pub queuedResourceId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Creates a QueuedResource TPU instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_queued_resources_create_builder()` + `tpu_projects_locations_queued_resources_create_execute()`.
/// For task-level control, use `tpu_projects_locations_queued_resources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_create(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsQueuedResourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_queued_resources_create_builder(
        client,
        &args.parent,
        &args.queuedResourceId,
        &args.requestId,
    )?;
    tpu_projects_locations_queued_resources_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Deletes a QueuedResource TPU instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_queued_resources_delete_execute()` to send, or `tpu_projects_locations_queued_resources_delete` for simplest API.

pub fn tpu_projects_locations_queued_resources_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    force: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/queuedResources/{queuedResourcesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Deletes a QueuedResource TPU instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_queued_resources_delete_execute()` or `tpu_projects_locations_queued_resources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Deletes a QueuedResource TPU instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_queued_resources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_queued_resources_delete_task()`.
/// For the simplest API, use `tpu_projects_locations_queued_resources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_queued_resources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_queued_resources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_queued_resources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsQueuedResourcesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Deletes a QueuedResource TPU instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_queued_resources_delete_builder()` + `tpu_projects_locations_queued_resources_delete_execute()`.
/// For task-level control, use `tpu_projects_locations_queued_resources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_delete(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsQueuedResourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_queued_resources_delete_builder(
        client,
        &args.name,
        &args.force,
        &args.requestId,
    )?;
    tpu_projects_locations_queued_resources_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Gets details of a queued resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_queued_resources_get_execute()` to send, or `tpu_projects_locations_queued_resources_get` for simplest API.

pub fn tpu_projects_locations_queued_resources_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/queuedResources/{queuedResourcesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Gets details of a queued resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_queued_resources_get_execute()` or `tpu_projects_locations_queued_resources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueuedResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueuedResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Gets details of a queued resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_queued_resources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_queued_resources_get_task()`.
/// For the simplest API, use `tpu_projects_locations_queued_resources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_queued_resources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueuedResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_queued_resources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_queued_resources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsQueuedResourcesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}
/// Gets details of a queued resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_queued_resources_get_builder()` + `tpu_projects_locations_queued_resources_get_execute()`.
/// For task-level control, use `tpu_projects_locations_queued_resources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_get(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsQueuedResourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueuedResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_queued_resources_get_builder(client, &args.name)?;
    tpu_projects_locations_queued_resources_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Lists queued resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_queued_resources_list_execute()` to send, or `tpu_projects_locations_queued_resources_list` for simplest API.

pub fn tpu_projects_locations_queued_resources_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/queuedResources",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Lists queued resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_queued_resources_list_execute()` or `tpu_projects_locations_queued_resources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListQueuedResourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListQueuedResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Lists queued resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_queued_resources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_queued_resources_list_task()`.
/// For the simplest API, use `tpu_projects_locations_queued_resources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_queued_resources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListQueuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_queued_resources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_queued_resources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsQueuedResourcesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/queuedResources
/// Lists queued resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_queued_resources_list_builder()` + `tpu_projects_locations_queued_resources_list_execute()`.
/// For task-level control, use `tpu_projects_locations_queued_resources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_list(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsQueuedResourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListQueuedResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_queued_resources_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    tpu_projects_locations_queued_resources_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}:reset
/// Resets a QueuedResource TPU instance
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_queued_resources_reset_execute()` to send, or `tpu_projects_locations_queued_resources_reset` for simplest API.

pub fn tpu_projects_locations_queued_resources_reset_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/queuedResources/{queuedResourcesId}:reset",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}:reset
/// Resets a QueuedResource TPU instance
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_queued_resources_reset_execute()` or `tpu_projects_locations_queued_resources_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}:reset
/// Resets a QueuedResource TPU instance
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_queued_resources_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_queued_resources_reset_task()`.
/// For the simplest API, use `tpu_projects_locations_queued_resources_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_queued_resources_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_queued_resources_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_queued_resources_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_queued_resources_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsQueuedResourcesResetArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/queuedResources/{queuedResourcesId}:reset
/// Resets a QueuedResource TPU instance
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_queued_resources_reset_builder()` + `tpu_projects_locations_queued_resources_reset_execute()`.
/// For task-level control, use `tpu_projects_locations_queued_resources_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_queued_resources_reset(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsQueuedResourcesResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_queued_resources_reset_builder(client, &args.name)?;
    tpu_projects_locations_queued_resources_reset_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions/{runtimeVersionsId}
/// Gets a runtime version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_runtime_versions_get_execute()` to send, or `tpu_projects_locations_runtime_versions_get` for simplest API.

pub fn tpu_projects_locations_runtime_versions_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/runtimeVersions/{runtimeVersionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions/{runtimeVersionsId}
/// Gets a runtime version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_runtime_versions_get_execute()` or `tpu_projects_locations_runtime_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_runtime_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_runtime_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RuntimeVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RuntimeVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions/{runtimeVersionsId}
/// Gets a runtime version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_runtime_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_runtime_versions_get_task()`.
/// For the simplest API, use `tpu_projects_locations_runtime_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_runtime_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_runtime_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RuntimeVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_runtime_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_runtime_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsRuntimeVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions/{runtimeVersionsId}
/// Gets a runtime version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_runtime_versions_get_builder()` + `tpu_projects_locations_runtime_versions_get_execute()`.
/// For task-level control, use `tpu_projects_locations_runtime_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_runtime_versions_get(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsRuntimeVersionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RuntimeVersion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_runtime_versions_get_builder(client, &args.name)?;
    tpu_projects_locations_runtime_versions_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions
/// Lists runtime versions supported by this API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `tpu_projects_locations_runtime_versions_list_execute()` to send, or `tpu_projects_locations_runtime_versions_list` for simplest API.

pub fn tpu_projects_locations_runtime_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://tpu.googleapis.com/v2/projects/{}/locations/{locationsId}/runtimeVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions
/// Lists runtime versions supported by this API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `tpu_projects_locations_runtime_versions_list_execute()` or `tpu_projects_locations_runtime_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_runtime_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_runtime_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRuntimeVersionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRuntimeVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions
/// Lists runtime versions supported by this API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `tpu_projects_locations_runtime_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `tpu_projects_locations_runtime_versions_list_task()`.
/// For the simplest API, use `tpu_projects_locations_runtime_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `tpu_projects_locations_runtime_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn tpu_projects_locations_runtime_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRuntimeVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = tpu_projects_locations_runtime_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`tpu_projects_locations_runtime_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TpuProjectsLocationsRuntimeVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/runtimeVersions
/// Lists runtime versions supported by this API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `tpu_projects_locations_runtime_versions_list_builder()` + `tpu_projects_locations_runtime_versions_list_execute()`.
/// For task-level control, use `tpu_projects_locations_runtime_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn tpu_projects_locations_runtime_versions_list(
    client: &SimpleHttpClient,
    args: &TpuProjectsLocationsRuntimeVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRuntimeVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = tpu_projects_locations_runtime_versions_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    tpu_projects_locations_runtime_versions_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GenerateServiceIdentityResponse
// =============================================================================

/// ResourceIdentifier implementation for GenerateServiceIdentityResponse with TpuProjectsLocationsGenerateServiceIdentityArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsGenerateServiceIdentityArgs>
    for GenerateServiceIdentityResponse
{
    fn generate_resource_id(
        &self,
        input: &TpuProjectsLocationsGenerateServiceIdentityArgs,
    ) -> String {
        format!("gcp::tpu::GenerateServiceIdentityResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::GenerateServiceIdentityResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with TpuProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsGetArgs) -> String {
        format!("gcp::tpu::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with TpuProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsListArgs) -> String {
        format!("gcp::tpu::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AcceleratorType
// =============================================================================

/// ResourceIdentifier implementation for AcceleratorType with TpuProjectsLocationsAcceleratorTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsAcceleratorTypesGetArgs> for AcceleratorType {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsAcceleratorTypesGetArgs) -> String {
        format!("gcp::tpu::AcceleratorType/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::AcceleratorType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAcceleratorTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAcceleratorTypesResponse with TpuProjectsLocationsAcceleratorTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsAcceleratorTypesListArgs>
    for ListAcceleratorTypesResponse
{
    fn generate_resource_id(&self, input: &TpuProjectsLocationsAcceleratorTypesListArgs) -> String {
        format!("gcp::tpu::ListAcceleratorTypesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::ListAcceleratorTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsNodesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsNodesCreateArgs) -> String {
        format!("gcp::tpu::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsNodesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsNodesDeleteArgs) -> String {
        format!("gcp::tpu::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Node
// =============================================================================

/// ResourceIdentifier implementation for Node with TpuProjectsLocationsNodesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesGetArgs> for Node {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsNodesGetArgs) -> String {
        format!("gcp::tpu::Node/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Node"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetGuestAttributesResponse
// =============================================================================

/// ResourceIdentifier implementation for GetGuestAttributesResponse with TpuProjectsLocationsNodesGetGuestAttributesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesGetGuestAttributesArgs>
    for GetGuestAttributesResponse
{
    fn generate_resource_id(
        &self,
        input: &TpuProjectsLocationsNodesGetGuestAttributesArgs,
    ) -> String {
        format!("gcp::tpu::GetGuestAttributesResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::GetGuestAttributesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNodesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNodesResponse with TpuProjectsLocationsNodesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesListArgs> for ListNodesResponse {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsNodesListArgs) -> String {
        format!("gcp::tpu::ListNodesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::ListNodesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsNodesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsNodesPatchArgs) -> String {
        format!("gcp::tpu::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsNodesStartArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesStartArgs> for Operation {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsNodesStartArgs) -> String {
        format!("gcp::tpu::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsNodesStopArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsNodesStopArgs> for Operation {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsNodesStopArgs) -> String {
        format!("gcp::tpu::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with TpuProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsOperationsCancelArgs) -> String {
        format!("gcp::tpu::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with TpuProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsOperationsDeleteArgs) -> String {
        format!("gcp::tpu::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::tpu::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with TpuProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsOperationsListArgs) -> String {
        format!("gcp::tpu::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsQueuedResourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsQueuedResourcesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &TpuProjectsLocationsQueuedResourcesCreateArgs,
    ) -> String {
        format!("gcp::tpu::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsQueuedResourcesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsQueuedResourcesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &TpuProjectsLocationsQueuedResourcesDeleteArgs,
    ) -> String {
        format!("gcp::tpu::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueuedResource
// =============================================================================

/// ResourceIdentifier implementation for QueuedResource with TpuProjectsLocationsQueuedResourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsQueuedResourcesGetArgs> for QueuedResource {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsQueuedResourcesGetArgs) -> String {
        format!("gcp::tpu::QueuedResource/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::QueuedResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListQueuedResourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListQueuedResourcesResponse with TpuProjectsLocationsQueuedResourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsQueuedResourcesListArgs>
    for ListQueuedResourcesResponse
{
    fn generate_resource_id(&self, input: &TpuProjectsLocationsQueuedResourcesListArgs) -> String {
        format!("gcp::tpu::ListQueuedResourcesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::ListQueuedResourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with TpuProjectsLocationsQueuedResourcesResetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsQueuedResourcesResetArgs> for Operation {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsQueuedResourcesResetArgs) -> String {
        format!("gcp::tpu::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RuntimeVersion
// =============================================================================

/// ResourceIdentifier implementation for RuntimeVersion with TpuProjectsLocationsRuntimeVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsRuntimeVersionsGetArgs> for RuntimeVersion {
    fn generate_resource_id(&self, input: &TpuProjectsLocationsRuntimeVersionsGetArgs) -> String {
        format!("gcp::tpu::RuntimeVersion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::RuntimeVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRuntimeVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRuntimeVersionsResponse with TpuProjectsLocationsRuntimeVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TpuProjectsLocationsRuntimeVersionsListArgs>
    for ListRuntimeVersionsResponse
{
    fn generate_resource_id(&self, input: &TpuProjectsLocationsRuntimeVersionsListArgs) -> String {
        format!("gcp::tpu::ListRuntimeVersionsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::tpu::ListRuntimeVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
