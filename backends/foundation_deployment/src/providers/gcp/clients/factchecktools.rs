//! Auto-generated API clients for gcp/factchecktools.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha1/claims:imageSearch
/// Search through fact-checked claims using an image as the query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_claims_image_search_execute()` to send, or `factchecktools_claims_image_search` for simplest API.

pub fn factchecktools_claims_image_search_builder(
    client: &SimpleHttpClient,
    imageUri: &Option<String>,
    languageCode: &Option<String>,
    offset: &Option<i32>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://factchecktools.googleapis.com/v1alpha1/claims:imageSearch",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = imageUri.as_ref() {
        query_parts.push(format!("imageUri={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = offset.as_ref() {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/claims:imageSearch
/// Search through fact-checked claims using an image as the query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `factchecktools_claims_image_search_execute()` or `factchecktools_claims_image_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_claims_image_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_image_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimImageSearchResponse,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimImageSearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/claims:imageSearch
/// Search through fact-checked claims using an image as the query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `factchecktools_claims_image_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `factchecktools_claims_image_search_task()`.
/// For the simplest API, use `factchecktools_claims_image_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_claims_image_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn factchecktools_claims_image_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimImageSearchResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = factchecktools_claims_image_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`factchecktools_claims_image_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FactchecktoolsClaimsImageSearchArgs {
    /// Query parameter: imageUri
    pub imageUri: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: offset
    pub offset: Option<i32>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha1/claims:imageSearch
/// Search through fact-checked claims using an image as the query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_claims_image_search_builder()` + `factchecktools_claims_image_search_execute()`.
/// For task-level control, use `factchecktools_claims_image_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_image_search(
    client: &SimpleHttpClient,
    args: &FactchecktoolsClaimsImageSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimImageSearchResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_claims_image_search_builder(
        client,
        &args.imageUri,
        &args.languageCode,
        &args.offset,
        &args.pageSize,
        &args.pageToken,
    )?;
    factchecktools_claims_image_search_execute(builder)
}

/// GET v1alpha1/claims:search
/// Search through fact-checked claims.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_claims_search_execute()` to send, or `factchecktools_claims_search` for simplest API.

pub fn factchecktools_claims_search_builder(
    client: &SimpleHttpClient,
    languageCode: &Option<String>,
    maxAgeDays: &Option<i32>,
    offset: &Option<i32>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    query: &Option<String>,
    reviewPublisherSiteFilter: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://factchecktools.googleapis.com/v1alpha1/claims:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = maxAgeDays.as_ref() {
        query_parts.push(format!("maxAgeDays={}", val));
    }
    if let Some(val) = offset.as_ref() {
        query_parts.push(format!("offset={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = reviewPublisherSiteFilter.as_ref() {
        query_parts.push(format!("reviewPublisherSiteFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/claims:search
/// Search through fact-checked claims.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `factchecktools_claims_search_execute()` or `factchecktools_claims_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_claims_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimSearchResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimSearchResponse =
                    serde_json::from_str(&body).map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/claims:search
/// Search through fact-checked claims.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `factchecktools_claims_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `factchecktools_claims_search_task()`.
/// For the simplest API, use `factchecktools_claims_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_claims_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn factchecktools_claims_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimSearchResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = factchecktools_claims_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`factchecktools_claims_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FactchecktoolsClaimsSearchArgs {
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: maxAgeDays
    pub maxAgeDays: Option<i32>,
    /// Query parameter: offset
    pub offset: Option<i32>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
    /// Query parameter: reviewPublisherSiteFilter
    pub reviewPublisherSiteFilter: Option<String>,
}

/// GET v1alpha1/claims:search
/// Search through fact-checked claims.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_claims_search_builder()` + `factchecktools_claims_search_execute()`.
/// For task-level control, use `factchecktools_claims_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_search(
    client: &SimpleHttpClient,
    args: &FactchecktoolsClaimsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimSearchResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_claims_search_builder(
        client,
        &args.languageCode,
        &args.maxAgeDays,
        &args.offset,
        &args.pageSize,
        &args.pageToken,
        &args.query,
        &args.reviewPublisherSiteFilter,
    )?;
    factchecktools_claims_search_execute(builder)
}

/// GET v1alpha1/pages
/// Create ClaimReview markup on a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_pages_create_execute()` to send, or `factchecktools_pages_create` for simplest API.

pub fn factchecktools_pages_create_builder(
    client: &SimpleHttpClient,
    body: &GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://factchecktools.googleapis.com/v1alpha1/pages",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/pages
/// Create ClaimReview markup on a page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `factchecktools_pages_create_execute()` or `factchecktools_pages_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_pages_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/pages
/// Create ClaimReview markup on a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `factchecktools_pages_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `factchecktools_pages_create_task()`.
/// For the simplest API, use `factchecktools_pages_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_pages_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn factchecktools_pages_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = factchecktools_pages_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`factchecktools_pages_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FactchecktoolsPagesCreateArgs {
    /// Request body.
    pub body: GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage,
}

/// GET v1alpha1/pages
/// Create ClaimReview markup on a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_pages_create_builder()` + `factchecktools_pages_create_execute()`.
/// For task-level control, use `factchecktools_pages_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_create(
    client: &SimpleHttpClient,
    args: &FactchecktoolsPagesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_pages_create_builder(client, &args.body)?;
    factchecktools_pages_create_execute(builder)
}

/// GET v1alpha1/pages/{pagesId}
/// Delete all ClaimReview markup on a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_pages_delete_execute()` to send, or `factchecktools_pages_delete` for simplest API.

pub fn factchecktools_pages_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://factchecktools.googleapis.com/v1alpha1/pages/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha1/pages/{pagesId}
/// Delete all ClaimReview markup on a page.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `factchecktools_pages_delete_execute()` or `factchecktools_pages_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_pages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha1/pages/{pagesId}
/// Delete all ClaimReview markup on a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `factchecktools_pages_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `factchecktools_pages_delete_task()`.
/// For the simplest API, use `factchecktools_pages_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `factchecktools_pages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn factchecktools_pages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = factchecktools_pages_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`factchecktools_pages_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FactchecktoolsPagesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha1/pages/{pagesId}
/// Delete all ClaimReview markup on a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_pages_delete_builder()` + `factchecktools_pages_delete_execute()`.
/// For task-level control, use `factchecktools_pages_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_delete(
    client: &SimpleHttpClient,
    args: &FactchecktoolsPagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_pages_delete_builder(client, &args.name)?;
    factchecktools_pages_delete_execute(builder)
}
