//! Auto-generated API clients for gcp/factchecktools.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1alpha1/claims:imageSearch
/// Search through fact-checked claims using an image as the query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_claims_image_search_execute()` to send, or `factchecktools_claims_image_search` for simplest API.

pub fn factchecktools_claims_image_search_builder(
    client: &SimpleHttpClient,
    imageUri: Option<&str>,
    languageCode: Option<&str>,
    offset: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1alpha1/claims:imageSearch",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "imageUri",
            imageUri
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "offset",
            offset
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1alpha1/claims:imageSearch
/// Search through fact-checked claims using an image as the query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `factchecktools_claims_image_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from factchecktools_claims_image_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_image_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimImageSearchResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimImageSearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/claims:imageSearch
/// Search through fact-checked claims using an image as the query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_claims_image_search_builder()` + `factchecktools_claims_image_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_image_search(
    client: &SimpleHttpClient,
    imageUri: Option<&str>,
    languageCode: Option<&str>,
    offset: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimImageSearchResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_claims_image_search_builder(
        client,
        imageUri,
        languageCode,
        offset,
        pageSize,
        pageToken,
    )?;
    factchecktools_claims_image_search_execute(builder)
}

/// GET v1alpha1/claims:search
/// Search through fact-checked claims.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_claims_search_execute()` to send, or `factchecktools_claims_search` for simplest API.

pub fn factchecktools_claims_search_builder(
    client: &SimpleHttpClient,
    languageCode: Option<&str>,
    maxAgeDays: Option<i32>,
    offset: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    reviewPublisherSiteFilter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1alpha1/claims:search",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxAgeDays",
            maxAgeDays
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "offset",
            offset
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "reviewPublisherSiteFilter",
            reviewPublisherSiteFilter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1alpha1/claims:search
/// Search through fact-checked claims.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `factchecktools_claims_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from factchecktools_claims_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimSearchResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimSearchResponse =
                    serde_json::from_str(&body).map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/claims:search
/// Search through fact-checked claims.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_claims_search_builder()` + `factchecktools_claims_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_claims_search(
    client: &SimpleHttpClient,
    languageCode: Option<&str>,
    maxAgeDays: Option<i32>,
    offset: Option<i32>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    reviewPublisherSiteFilter: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1FactCheckedClaimSearchResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_claims_search_builder(
        client,
        languageCode,
        maxAgeDays,
        offset,
        pageSize,
        pageToken,
        query,
        reviewPublisherSiteFilter,
    )?;
    factchecktools_claims_search_execute(builder)
}

/// GET v1alpha1/pages
/// Create ClaimReview markup on a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_pages_create_execute()` to send, or `factchecktools_pages_create` for simplest API.

pub fn factchecktools_pages_create_builder(
    client: &SimpleHttpClient,
    body: &GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1alpha1/pages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1alpha1/pages
/// Create ClaimReview markup on a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `factchecktools_pages_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from factchecktools_pages_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/pages
/// Create ClaimReview markup on a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_pages_create_builder()` + `factchecktools_pages_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_create(
    client: &SimpleHttpClient,
    body: &GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_pages_create_builder(client, body)?;
    factchecktools_pages_create_execute(builder)
}

/// GET v1alpha1/{+name}
/// Delete all ClaimReview markup on a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_pages_delete_execute()` to send, or `factchecktools_pages_delete` for simplest API.

pub fn factchecktools_pages_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1alpha1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/{+name}
/// Delete all ClaimReview markup on a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `factchecktools_pages_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from factchecktools_pages_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/{+name}
/// Delete all ClaimReview markup on a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_pages_delete_builder()` + `factchecktools_pages_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_pages_delete_builder(client, name)?;
    factchecktools_pages_delete_execute(builder)
}

/// GET v1alpha1/{+name}
/// Get all ClaimReview markup on a page.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_pages_get_execute()` to send, or `factchecktools_pages_get` for simplest API.

pub fn factchecktools_pages_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1alpha1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/{+name}
/// Get all ClaimReview markup on a page.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `factchecktools_pages_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from factchecktools_pages_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/{+name}
/// Get all ClaimReview markup on a page.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_pages_get_builder()` + `factchecktools_pages_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_pages_get_builder(client, name)?;
    factchecktools_pages_get_execute(builder)
}

/// GET v1alpha1/pages
/// List the ClaimReview markup pages for a specific URL or for an organization.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_pages_list_execute()` to send, or `factchecktools_pages_list` for simplest API.

pub fn factchecktools_pages_list_builder(
    client: &SimpleHttpClient,
    offset: Option<i32>,
    organization: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    url: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1alpha1/pages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "offset",
            offset
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "organization",
            organization
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "url",
            url.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1alpha1/pages
/// List the ClaimReview markup pages for a specific URL or for an organization.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `factchecktools_pages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from factchecktools_pages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleFactcheckingFactchecktoolsV1alpha1ListClaimReviewMarkupPagesResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1ListClaimReviewMarkupPagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/pages
/// List the ClaimReview markup pages for a specific URL or for an organization.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_pages_list_builder()` + `factchecktools_pages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_list(
    client: &SimpleHttpClient,
    offset: Option<i32>,
    organization: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    url: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleFactcheckingFactchecktoolsV1alpha1ListClaimReviewMarkupPagesResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        factchecktools_pages_list_builder(client, offset, organization, pageSize, pageToken, url)?;
    factchecktools_pages_list_execute(builder)
}

/// GET v1alpha1/{+name}
/// Update for all ClaimReview markup on a page Note that this is a full update. To retain the existing ClaimReview markup on a page, first perform a Get operation, then modify the returned markup, and finally call Update with the entire ClaimReview markup as the body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `factchecktools_pages_update_execute()` to send, or `factchecktools_pages_update` for simplest API.

pub fn factchecktools_pages_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1alpha1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1alpha1/{+name}
/// Update for all ClaimReview markup on a page Note that this is a full update. To retain the existing ClaimReview markup on a page, first perform a Get operation, then modify the returned markup, and finally call Update with the entire ClaimReview markup as the body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `factchecktools_pages_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from factchecktools_pages_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha1/{+name}
/// Update for all ClaimReview markup on a page Note that this is a full update. To retain the existing ClaimReview markup on a page, first perform a Get operation, then modify the returned markup, and finally call Update with the entire ClaimReview markup as the body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `factchecktools_pages_update_builder()` + `factchecktools_pages_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn factchecktools_pages_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFactcheckingFactchecktoolsV1alpha1ClaimReviewMarkupPage>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = factchecktools_pages_update_builder(client, name, body)?;
    factchecktools_pages_update_execute(builder)
}
