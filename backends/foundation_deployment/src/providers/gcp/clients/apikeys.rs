//! Auto-generated API clients for gcp/apikeys.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/keys:lookupKey
/// Find the parent project and resource name of the API key that matches the key string in the request. If the API key has been purged, resource name will not be set. The service account must have the apikeys.keys.lookup permission on the parent project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_keys_lookup_key_execute()` to send, or `apikeys_keys_lookup_key` for simplest API.

pub fn apikeys_keys_lookup_key_builder<R>(
    client: &SimpleHttpClient<R>,
    keyString: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://apikeys.googleapis.com/v2/keys:lookupKey",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyString.as_ref() {
        query_parts.push(format!("keyString={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/keys:lookupKey
/// Find the parent project and resource name of the API key that matches the key string in the request. If the API key has been purged, resource name will not be set. The service account must have the apikeys.keys.lookup permission on the parent project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_keys_lookup_key_execute()` or `apikeys_keys_lookup_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_keys_lookup_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_keys_lookup_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<V2LookupKeyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V2LookupKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/keys:lookupKey
/// Find the parent project and resource name of the API key that matches the key string in the request. If the API key has been purged, resource name will not be set. The service account must have the apikeys.keys.lookup permission on the parent project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_keys_lookup_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_keys_lookup_key_task()`.
/// For the simplest API, use `apikeys_keys_lookup_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_keys_lookup_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_keys_lookup_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2LookupKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apikeys_keys_lookup_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_keys_lookup_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysKeysLookupKeyArgs {
    /// Query parameter: keyString
    pub keyString: Option<Option<String>>,
}

/// GET v2/keys:lookupKey
/// Find the parent project and resource name of the API key that matches the key string in the request. If the API key has been purged, resource name will not be set. The service account must have the apikeys.keys.lookup permission on the parent project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_keys_lookup_key_builder()` + `apikeys_keys_lookup_key_execute()`.
/// For task-level control, use `apikeys_keys_lookup_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_keys_lookup_key(
    client: &SimpleHttpClient,
    args: &ApikeysKeysLookupKeyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2LookupKeyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apikeys_keys_lookup_key_builder(client, &args.keyString)?;
    apikeys_keys_lookup_key_execute(builder)
}

/// GET v2/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_operations_get_execute()` to send, or `apikeys_operations_get` for simplest API.

pub fn apikeys_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://apikeys.googleapis.com/v2/operations/{}", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_operations_get_execute()` or `apikeys_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_operations_get_task()`.
/// For the simplest API, use `apikeys_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apikeys_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_operations_get_builder()` + `apikeys_operations_get_execute()`.
/// For task-level control, use `apikeys_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_operations_get(
    client: &SimpleHttpClient,
    args: &ApikeysOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apikeys_operations_get_builder(client, &args.name)?;
    apikeys_operations_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys
/// Creates a new API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_projects_locations_keys_create_execute()` to send, or `apikeys_projects_locations_keys_create` for simplest API.

pub fn apikeys_projects_locations_keys_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    keyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://apikeys.googleapis.com/v2/projects/{}/locations/{locationsId}/keys",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyId.as_ref() {
        query_parts.push(format!("keyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys
/// Creates a new API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_projects_locations_keys_create_execute()` or `apikeys_projects_locations_keys_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys
/// Creates a new API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_projects_locations_keys_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_projects_locations_keys_create_task()`.
/// For the simplest API, use `apikeys_projects_locations_keys_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_projects_locations_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apikeys_projects_locations_keys_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_projects_locations_keys_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysProjectsLocationsKeysCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: keyId
    pub keyId: Option<Option<String>>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys
/// Creates a new API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_projects_locations_keys_create_builder()` + `apikeys_projects_locations_keys_create_execute()`.
/// For task-level control, use `apikeys_projects_locations_keys_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_create(
    client: &SimpleHttpClient,
    args: &ApikeysProjectsLocationsKeysCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apikeys_projects_locations_keys_create_builder(client, &args.parent, &args.keyId)?;
    apikeys_projects_locations_keys_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Deletes an API key. Deleted key can be retrieved within 30 days of deletion. Afterward, key will be purged from the project. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_projects_locations_keys_delete_execute()` to send, or `apikeys_projects_locations_keys_delete` for simplest API.

pub fn apikeys_projects_locations_keys_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://apikeys.googleapis.com/v2/projects/{}/locations/{locationsId}/keys/{keysId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Deletes an API key. Deleted key can be retrieved within 30 days of deletion. Afterward, key will be purged from the project. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_projects_locations_keys_delete_execute()` or `apikeys_projects_locations_keys_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Deletes an API key. Deleted key can be retrieved within 30 days of deletion. Afterward, key will be purged from the project. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_projects_locations_keys_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_projects_locations_keys_delete_task()`.
/// For the simplest API, use `apikeys_projects_locations_keys_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_projects_locations_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apikeys_projects_locations_keys_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_projects_locations_keys_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysProjectsLocationsKeysDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Deletes an API key. Deleted key can be retrieved within 30 days of deletion. Afterward, key will be purged from the project. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_projects_locations_keys_delete_builder()` + `apikeys_projects_locations_keys_delete_execute()`.
/// For task-level control, use `apikeys_projects_locations_keys_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_delete(
    client: &SimpleHttpClient,
    args: &ApikeysProjectsLocationsKeysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apikeys_projects_locations_keys_delete_builder(client, &args.name, &args.etag)?;
    apikeys_projects_locations_keys_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Gets the metadata for an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_projects_locations_keys_get_execute()` to send, or `apikeys_projects_locations_keys_get` for simplest API.

pub fn apikeys_projects_locations_keys_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://apikeys.googleapis.com/v2/projects/{}/locations/{locationsId}/keys/{keysId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Gets the metadata for an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_projects_locations_keys_get_execute()` or `apikeys_projects_locations_keys_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<V2Key>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V2Key = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Gets the metadata for an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_projects_locations_keys_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_projects_locations_keys_get_task()`.
/// For the simplest API, use `apikeys_projects_locations_keys_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_projects_locations_keys_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2Key>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apikeys_projects_locations_keys_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_projects_locations_keys_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysProjectsLocationsKeysGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Gets the metadata for an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_projects_locations_keys_get_builder()` + `apikeys_projects_locations_keys_get_execute()`.
/// For task-level control, use `apikeys_projects_locations_keys_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_get(
    client: &SimpleHttpClient,
    args: &ApikeysProjectsLocationsKeysGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2Key>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apikeys_projects_locations_keys_get_builder(client, &args.name)?;
    apikeys_projects_locations_keys_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}/keyString
/// Get the key string for an API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_projects_locations_keys_get_key_string_execute()` to send, or `apikeys_projects_locations_keys_get_key_string` for simplest API.

pub fn apikeys_projects_locations_keys_get_key_string_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://apikeys.googleapis.com/v2/projects/{}/locations/{locationsId}/keys/{keysId}/keyString",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}/keyString
/// Get the key string for an API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_projects_locations_keys_get_key_string_execute()` or `apikeys_projects_locations_keys_get_key_string`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_get_key_string_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_get_key_string_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<V2GetKeyStringResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V2GetKeyStringResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}/keyString
/// Get the key string for an API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_projects_locations_keys_get_key_string_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_projects_locations_keys_get_key_string_task()`.
/// For the simplest API, use `apikeys_projects_locations_keys_get_key_string()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_get_key_string_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_projects_locations_keys_get_key_string_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2GetKeyStringResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apikeys_projects_locations_keys_get_key_string_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_projects_locations_keys_get_key_string`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysProjectsLocationsKeysGetKeyStringArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}/keyString
/// Get the key string for an API key. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_projects_locations_keys_get_key_string_builder()` + `apikeys_projects_locations_keys_get_key_string_execute()`.
/// For task-level control, use `apikeys_projects_locations_keys_get_key_string_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_get_key_string(
    client: &SimpleHttpClient,
    args: &ApikeysProjectsLocationsKeysGetKeyStringArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2GetKeyStringResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apikeys_projects_locations_keys_get_key_string_builder(client, &args.name)?;
    apikeys_projects_locations_keys_get_key_string_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys
/// Lists the API keys owned by a project. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_projects_locations_keys_list_execute()` to send, or `apikeys_projects_locations_keys_list` for simplest API.

pub fn apikeys_projects_locations_keys_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    showDeleted: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://apikeys.googleapis.com/v2/projects/{}/locations/{locationsId}/keys",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys
/// Lists the API keys owned by a project. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_projects_locations_keys_list_execute()` or `apikeys_projects_locations_keys_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<V2ListKeysResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: V2ListKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys
/// Lists the API keys owned by a project. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_projects_locations_keys_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_projects_locations_keys_list_task()`.
/// For the simplest API, use `apikeys_projects_locations_keys_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_projects_locations_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2ListKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = apikeys_projects_locations_keys_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_projects_locations_keys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysProjectsLocationsKeysListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: showDeleted
    pub showDeleted: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/keys
/// Lists the API keys owned by a project. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_projects_locations_keys_list_builder()` + `apikeys_projects_locations_keys_list_execute()`.
/// For task-level control, use `apikeys_projects_locations_keys_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_list(
    client: &SimpleHttpClient,
    args: &ApikeysProjectsLocationsKeysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<V2ListKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = apikeys_projects_locations_keys_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.showDeleted,
    )?;
    apikeys_projects_locations_keys_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Patches the modifiable fields of an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_projects_locations_keys_patch_execute()` to send, or `apikeys_projects_locations_keys_patch` for simplest API.

pub fn apikeys_projects_locations_keys_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://apikeys.googleapis.com/v2/projects/{}/locations/{locationsId}/keys/{keysId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Patches the modifiable fields of an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_projects_locations_keys_patch_execute()` or `apikeys_projects_locations_keys_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Patches the modifiable fields of an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_projects_locations_keys_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_projects_locations_keys_patch_task()`.
/// For the simplest API, use `apikeys_projects_locations_keys_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_projects_locations_keys_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apikeys_projects_locations_keys_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_projects_locations_keys_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysProjectsLocationsKeysPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}
/// Patches the modifiable fields of an API key. The key string of the API key isn't included in the response. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_projects_locations_keys_patch_builder()` + `apikeys_projects_locations_keys_patch_execute()`.
/// For task-level control, use `apikeys_projects_locations_keys_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_patch(
    client: &SimpleHttpClient,
    args: &ApikeysProjectsLocationsKeysPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        apikeys_projects_locations_keys_patch_builder(client, &args.name, &args.updateMask)?;
    apikeys_projects_locations_keys_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}:undelete
/// Undeletes an API key which was deleted within 30 days. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `apikeys_projects_locations_keys_undelete_execute()` to send, or `apikeys_projects_locations_keys_undelete` for simplest API.

pub fn apikeys_projects_locations_keys_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://apikeys.googleapis.com/v2/projects/{}/locations/{locationsId}/keys/{keysId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}:undelete
/// Undeletes an API key which was deleted within 30 days. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `apikeys_projects_locations_keys_undelete_execute()` or `apikeys_projects_locations_keys_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}:undelete
/// Undeletes an API key which was deleted within 30 days. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `apikeys_projects_locations_keys_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `apikeys_projects_locations_keys_undelete_task()`.
/// For the simplest API, use `apikeys_projects_locations_keys_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `apikeys_projects_locations_keys_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn apikeys_projects_locations_keys_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = apikeys_projects_locations_keys_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`apikeys_projects_locations_keys_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApikeysProjectsLocationsKeysUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/keys/{keysId}:undelete
/// Undeletes an API key which was deleted within 30 days. NOTE: Key is a global resource; hence the only supported value for location is global.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `apikeys_projects_locations_keys_undelete_builder()` + `apikeys_projects_locations_keys_undelete_execute()`.
/// For task-level control, use `apikeys_projects_locations_keys_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn apikeys_projects_locations_keys_undelete(
    client: &SimpleHttpClient,
    args: &ApikeysProjectsLocationsKeysUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = apikeys_projects_locations_keys_undelete_builder(client, &args.name)?;
    apikeys_projects_locations_keys_undelete_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for V2LookupKeyResponse
// =============================================================================

/// ResourceIdentifier implementation for V2LookupKeyResponse with ApikeysKeysLookupKeyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysKeysLookupKeyArgs> for V2LookupKeyResponse {
    fn generate_resource_id(&self, input: &ApikeysKeysLookupKeyArgs) -> String {
        "gcp::apikeys::V2LookupKeyResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::V2LookupKeyResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApikeysOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ApikeysOperationsGetArgs) -> String {
        format!("gcp::apikeys::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApikeysProjectsLocationsKeysCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysProjectsLocationsKeysCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &ApikeysProjectsLocationsKeysCreateArgs) -> String {
        format!("gcp::apikeys::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApikeysProjectsLocationsKeysDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysProjectsLocationsKeysDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ApikeysProjectsLocationsKeysDeleteArgs) -> String {
        format!("gcp::apikeys::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V2Key
// =============================================================================

/// ResourceIdentifier implementation for V2Key with ApikeysProjectsLocationsKeysGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysProjectsLocationsKeysGetArgs> for V2Key {
    fn generate_resource_id(&self, input: &ApikeysProjectsLocationsKeysGetArgs) -> String {
        format!("gcp::apikeys::V2Key/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::V2Key"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V2GetKeyStringResponse
// =============================================================================

/// ResourceIdentifier implementation for V2GetKeyStringResponse with ApikeysProjectsLocationsKeysGetKeyStringArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysProjectsLocationsKeysGetKeyStringArgs> for V2GetKeyStringResponse {
    fn generate_resource_id(&self, input: &ApikeysProjectsLocationsKeysGetKeyStringArgs) -> String {
        format!("gcp::apikeys::V2GetKeyStringResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::V2GetKeyStringResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for V2ListKeysResponse
// =============================================================================

/// ResourceIdentifier implementation for V2ListKeysResponse with ApikeysProjectsLocationsKeysListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysProjectsLocationsKeysListArgs> for V2ListKeysResponse {
    fn generate_resource_id(&self, input: &ApikeysProjectsLocationsKeysListArgs) -> String {
        format!("gcp::apikeys::V2ListKeysResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::V2ListKeysResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApikeysProjectsLocationsKeysPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysProjectsLocationsKeysPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &ApikeysProjectsLocationsKeysPatchArgs) -> String {
        format!("gcp::apikeys::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ApikeysProjectsLocationsKeysUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ApikeysProjectsLocationsKeysUndeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &ApikeysProjectsLocationsKeysUndeleteArgs) -> String {
        format!("gcp::apikeys::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::apikeys::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
