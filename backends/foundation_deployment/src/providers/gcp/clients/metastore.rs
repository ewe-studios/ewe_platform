//! Auto-generated API clients for gcp/metastore.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_get_execute()` to send, or `metastore_projects_locations_get` for simplest API.

pub fn metastore_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_get_execute()` or `metastore_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_get_task()`.
/// For the simplest API, use `metastore_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_get_builder()` + `metastore_projects_locations_get_execute()`.
/// For task-level control, use `metastore_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_get(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_get_builder(client, &args.name)?;
    metastore_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_list_execute()` to send, or `metastore_projects_locations_list` for simplest API.

pub fn metastore_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_list_execute()` or `metastore_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_list_task()`.
/// For the simplest API, use `metastore_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_list_builder()` + `metastore_projects_locations_list_execute()`.
/// For task-level control, use `metastore_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_list(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    metastore_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations
/// Creates a metastore federation in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_create_execute()` to send, or `metastore_projects_locations_federations_create` for simplest API.

pub fn metastore_projects_locations_federations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    federationId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = federationId.as_ref() {
        query_parts.push(format!("federationId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations
/// Creates a metastore federation in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_create_execute()` or `metastore_projects_locations_federations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations
/// Creates a metastore federation in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_create_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: federationId
    pub federationId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations
/// Creates a metastore federation in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_create_builder()` + `metastore_projects_locations_federations_create_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_create(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_federations_create_builder(
        client,
        &args.parent,
        &args.federationId,
        &args.requestId,
    )?;
    metastore_projects_locations_federations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Deletes a single federation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_delete_execute()` to send, or `metastore_projects_locations_federations_delete` for simplest API.

pub fn metastore_projects_locations_federations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations/{federationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Deletes a single federation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_delete_execute()` or `metastore_projects_locations_federations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Deletes a single federation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_delete_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Deletes a single federation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_delete_builder()` + `metastore_projects_locations_federations_delete_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_delete(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_federations_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    metastore_projects_locations_federations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Gets the details of a single federation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_get_execute()` to send, or `metastore_projects_locations_federations_get` for simplest API.

pub fn metastore_projects_locations_federations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations/{federationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Gets the details of a single federation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_get_execute()` or `metastore_projects_locations_federations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Federation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Federation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Gets the details of a single federation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_get_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Federation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Gets the details of a single federation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_get_builder()` + `metastore_projects_locations_federations_get_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_get(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Federation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_federations_get_builder(client, &args.name)?;
    metastore_projects_locations_federations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_get_iam_policy_execute()` to send, or `metastore_projects_locations_federations_get_iam_policy` for simplest API.

pub fn metastore_projects_locations_federations_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations/{federationsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_get_iam_policy_execute()` or `metastore_projects_locations_federations_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_get_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_get_iam_policy_builder()` + `metastore_projects_locations_federations_get_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_get_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_federations_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    metastore_projects_locations_federations_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations
/// Lists federations in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_list_execute()` to send, or `metastore_projects_locations_federations_list` for simplest API.

pub fn metastore_projects_locations_federations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations
/// Lists federations in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_list_execute()` or `metastore_projects_locations_federations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFederationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFederationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations
/// Lists federations in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_list_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFederationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/federations
/// Lists federations in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_list_builder()` + `metastore_projects_locations_federations_list_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_list(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFederationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_federations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    metastore_projects_locations_federations_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Updates the fields of a federation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_patch_execute()` to send, or `metastore_projects_locations_federations_patch` for simplest API.

pub fn metastore_projects_locations_federations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations/{federationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Updates the fields of a federation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_patch_execute()` or `metastore_projects_locations_federations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Updates the fields of a federation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_patch_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}
/// Updates the fields of a federation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_patch_builder()` + `metastore_projects_locations_federations_patch_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_patch(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_federations_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    metastore_projects_locations_federations_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_set_iam_policy_execute()` to send, or `metastore_projects_locations_federations_set_iam_policy` for simplest API.

pub fn metastore_projects_locations_federations_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations/{federationsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_set_iam_policy_execute()` or `metastore_projects_locations_federations_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_set_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_set_iam_policy_builder()` + `metastore_projects_locations_federations_set_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_set_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_federations_set_iam_policy_builder(client, &args.resource)?;
    metastore_projects_locations_federations_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_federations_test_iam_permissions_execute()` to send, or `metastore_projects_locations_federations_test_iam_permissions` for simplest API.

pub fn metastore_projects_locations_federations_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/federations/{federationsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_federations_test_iam_permissions_execute()` or `metastore_projects_locations_federations_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_federations_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_federations_test_iam_permissions_task()`.
/// For the simplest API, use `metastore_projects_locations_federations_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_federations_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_federations_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_federations_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_federations_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsFederationsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/federations/{federationsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_federations_test_iam_permissions_builder()` + `metastore_projects_locations_federations_test_iam_permissions_execute()`.
/// For task-level control, use `metastore_projects_locations_federations_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_federations_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsFederationsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_federations_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    metastore_projects_locations_federations_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_operations_cancel_execute()` to send, or `metastore_projects_locations_operations_cancel` for simplest API.

pub fn metastore_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_operations_cancel_execute()` or `metastore_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `metastore_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_operations_cancel_builder()` + `metastore_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `metastore_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_operations_cancel_builder(client, &args.name)?;
    metastore_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_operations_delete_execute()` to send, or `metastore_projects_locations_operations_delete` for simplest API.

pub fn metastore_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_operations_delete_execute()` or `metastore_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_operations_delete_task()`.
/// For the simplest API, use `metastore_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_operations_delete_builder()` + `metastore_projects_locations_operations_delete_execute()`.
/// For task-level control, use `metastore_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_operations_delete_builder(client, &args.name)?;
    metastore_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_operations_get_execute()` to send, or `metastore_projects_locations_operations_get` for simplest API.

pub fn metastore_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_operations_get_execute()` or `metastore_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_operations_get_task()`.
/// For the simplest API, use `metastore_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_operations_get_builder()` + `metastore_projects_locations_operations_get_execute()`.
/// For task-level control, use `metastore_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_operations_get_builder(client, &args.name)?;
    metastore_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_operations_list_execute()` to send, or `metastore_projects_locations_operations_list` for simplest API.

pub fn metastore_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_operations_list_execute()` or `metastore_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_operations_list_task()`.
/// For the simplest API, use `metastore_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_operations_list_builder()` + `metastore_projects_locations_operations_list_execute()`.
/// For task-level control, use `metastore_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    metastore_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterLocation
/// Alter metadata resource location. The metadata resource can be a database, table, or partition. This functionality only updates the parent directory for the respective metadata resource and does not transfer any existing data to the new location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_alter_location_execute()` to send, or `metastore_projects_locations_services_alter_location` for simplest API.

pub fn metastore_projects_locations_services_alter_location_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:alterLocation",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterLocation
/// Alter metadata resource location. The metadata resource can be a database, table, or partition. This functionality only updates the parent directory for the respective metadata resource and does not transfer any existing data to the new location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_alter_location_execute()` or `metastore_projects_locations_services_alter_location`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_alter_location_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_alter_location_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterLocation
/// Alter metadata resource location. The metadata resource can be a database, table, or partition. This functionality only updates the parent directory for the respective metadata resource and does not transfer any existing data to the new location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_alter_location_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_alter_location_task()`.
/// For the simplest API, use `metastore_projects_locations_services_alter_location()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_alter_location_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_alter_location_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_alter_location_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_alter_location`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesAlterLocationArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterLocation
/// Alter metadata resource location. The metadata resource can be a database, table, or partition. This functionality only updates the parent directory for the respective metadata resource and does not transfer any existing data to the new location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_alter_location_builder()` + `metastore_projects_locations_services_alter_location_execute()`.
/// For task-level control, use `metastore_projects_locations_services_alter_location_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_alter_location(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesAlterLocationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_alter_location_builder(client, &args.service)?;
    metastore_projects_locations_services_alter_location_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterTableProperties
/// Alter metadata table properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_alter_table_properties_execute()` to send, or `metastore_projects_locations_services_alter_table_properties` for simplest API.

pub fn metastore_projects_locations_services_alter_table_properties_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:alterTableProperties",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterTableProperties
/// Alter metadata table properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_alter_table_properties_execute()` or `metastore_projects_locations_services_alter_table_properties`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_alter_table_properties_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_alter_table_properties_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterTableProperties
/// Alter metadata table properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_alter_table_properties_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_alter_table_properties_task()`.
/// For the simplest API, use `metastore_projects_locations_services_alter_table_properties()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_alter_table_properties_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_alter_table_properties_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_alter_table_properties_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_alter_table_properties`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesAlterTablePropertiesArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:alterTableProperties
/// Alter metadata table properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_alter_table_properties_builder()` + `metastore_projects_locations_services_alter_table_properties_execute()`.
/// For task-level control, use `metastore_projects_locations_services_alter_table_properties_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_alter_table_properties(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesAlterTablePropertiesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_alter_table_properties_builder(
        client,
        &args.service,
    )?;
    metastore_projects_locations_services_alter_table_properties_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:cancelMigration
/// Cancels the ongoing Managed Migration process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_cancel_migration_execute()` to send, or `metastore_projects_locations_services_cancel_migration` for simplest API.

pub fn metastore_projects_locations_services_cancel_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:cancelMigration",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:cancelMigration
/// Cancels the ongoing Managed Migration process.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_cancel_migration_execute()` or `metastore_projects_locations_services_cancel_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_cancel_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_cancel_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:cancelMigration
/// Cancels the ongoing Managed Migration process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_cancel_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_cancel_migration_task()`.
/// For the simplest API, use `metastore_projects_locations_services_cancel_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_cancel_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_cancel_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_cancel_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_cancel_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesCancelMigrationArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:cancelMigration
/// Cancels the ongoing Managed Migration process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_cancel_migration_builder()` + `metastore_projects_locations_services_cancel_migration_execute()`.
/// For task-level control, use `metastore_projects_locations_services_cancel_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_cancel_migration(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesCancelMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_cancel_migration_builder(client, &args.service)?;
    metastore_projects_locations_services_cancel_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:completeMigration
/// Completes the managed migration process. The Dataproc Metastore service will switch to using its own backend database after successful migration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_complete_migration_execute()` to send, or `metastore_projects_locations_services_complete_migration` for simplest API.

pub fn metastore_projects_locations_services_complete_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:completeMigration",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:completeMigration
/// Completes the managed migration process. The Dataproc Metastore service will switch to using its own backend database after successful migration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_complete_migration_execute()` or `metastore_projects_locations_services_complete_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_complete_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_complete_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:completeMigration
/// Completes the managed migration process. The Dataproc Metastore service will switch to using its own backend database after successful migration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_complete_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_complete_migration_task()`.
/// For the simplest API, use `metastore_projects_locations_services_complete_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_complete_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_complete_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_complete_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_complete_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesCompleteMigrationArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:completeMigration
/// Completes the managed migration process. The Dataproc Metastore service will switch to using its own backend database after successful migration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_complete_migration_builder()` + `metastore_projects_locations_services_complete_migration_execute()`.
/// For task-level control, use `metastore_projects_locations_services_complete_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_complete_migration(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesCompleteMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_complete_migration_builder(client, &args.service)?;
    metastore_projects_locations_services_complete_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a metastore service in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_create_execute()` to send, or `metastore_projects_locations_services_create` for simplest API.

pub fn metastore_projects_locations_services_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<String>,
    serviceId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = serviceId.as_ref() {
        query_parts.push(format!("serviceId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a metastore service in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_create_execute()` or `metastore_projects_locations_services_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a metastore service in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_create_task()`.
/// For the simplest API, use `metastore_projects_locations_services_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: serviceId
    pub serviceId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services
/// Creates a metastore service in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_create_builder()` + `metastore_projects_locations_services_create_execute()`.
/// For task-level control, use `metastore_projects_locations_services_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_create(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.serviceId,
    )?;
    metastore_projects_locations_services_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_delete_execute()` to send, or `metastore_projects_locations_services_delete` for simplest API.

pub fn metastore_projects_locations_services_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_delete_execute()` or `metastore_projects_locations_services_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_delete_task()`.
/// For the simplest API, use `metastore_projects_locations_services_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Deletes a single service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_delete_builder()` + `metastore_projects_locations_services_delete_execute()`.
/// For task-level control, use `metastore_projects_locations_services_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_delete(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_delete_builder(client, &args.name, &args.requestId)?;
    metastore_projects_locations_services_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:exportMetadata
/// Exports metadata from a service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_export_metadata_execute()` to send, or `metastore_projects_locations_services_export_metadata` for simplest API.

pub fn metastore_projects_locations_services_export_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:exportMetadata",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:exportMetadata
/// Exports metadata from a service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_export_metadata_execute()` or `metastore_projects_locations_services_export_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_export_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_export_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:exportMetadata
/// Exports metadata from a service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_export_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_export_metadata_task()`.
/// For the simplest API, use `metastore_projects_locations_services_export_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_export_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_export_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_export_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_export_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesExportMetadataArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:exportMetadata
/// Exports metadata from a service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_export_metadata_builder()` + `metastore_projects_locations_services_export_metadata_execute()`.
/// For task-level control, use `metastore_projects_locations_services_export_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_export_metadata(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesExportMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_export_metadata_builder(client, &args.service)?;
    metastore_projects_locations_services_export_metadata_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets the details of a single service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_get_execute()` to send, or `metastore_projects_locations_services_get` for simplest API.

pub fn metastore_projects_locations_services_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets the details of a single service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_get_execute()` or `metastore_projects_locations_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Service>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Service = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets the details of a single service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_get_task()`.
/// For the simplest API, use `metastore_projects_locations_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Gets the details of a single service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_get_builder()` + `metastore_projects_locations_services_get_execute()`.
/// For task-level control, use `metastore_projects_locations_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_get(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Service>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_get_builder(client, &args.name)?;
    metastore_projects_locations_services_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_get_iam_policy_execute()` to send, or `metastore_projects_locations_services_get_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_get_iam_policy_execute()` or `metastore_projects_locations_services_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_get_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_get_iam_policy_builder()` + `metastore_projects_locations_services_get_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_get_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    metastore_projects_locations_services_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists services in a project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_list_execute()` to send, or `metastore_projects_locations_services_list` for simplest API.

pub fn metastore_projects_locations_services_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists services in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_list_execute()` or `metastore_projects_locations_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists services in a project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_list_task()`.
/// For the simplest API, use `metastore_projects_locations_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services
/// Lists services in a project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_list_builder()` + `metastore_projects_locations_services_list_execute()`.
/// For task-level control, use `metastore_projects_locations_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_list(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListServicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    metastore_projects_locations_services_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:moveTableToDatabase
/// Move a table to another database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_move_table_to_database_execute()` to send, or `metastore_projects_locations_services_move_table_to_database` for simplest API.

pub fn metastore_projects_locations_services_move_table_to_database_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:moveTableToDatabase",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:moveTableToDatabase
/// Move a table to another database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_move_table_to_database_execute()` or `metastore_projects_locations_services_move_table_to_database`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_move_table_to_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_move_table_to_database_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:moveTableToDatabase
/// Move a table to another database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_move_table_to_database_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_move_table_to_database_task()`.
/// For the simplest API, use `metastore_projects_locations_services_move_table_to_database()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_move_table_to_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_move_table_to_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_move_table_to_database_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_move_table_to_database`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMoveTableToDatabaseArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:moveTableToDatabase
/// Move a table to another database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_move_table_to_database_builder()` + `metastore_projects_locations_services_move_table_to_database_execute()`.
/// For task-level control, use `metastore_projects_locations_services_move_table_to_database_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_move_table_to_database(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMoveTableToDatabaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_move_table_to_database_builder(
        client,
        &args.service,
    )?;
    metastore_projects_locations_services_move_table_to_database_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_patch_execute()` to send, or `metastore_projects_locations_services_patch` for simplest API.

pub fn metastore_projects_locations_services_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_patch_execute()` or `metastore_projects_locations_services_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_patch_task()`.
/// For the simplest API, use `metastore_projects_locations_services_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}
/// Updates the parameters of a single service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_patch_builder()` + `metastore_projects_locations_services_patch_execute()`.
/// For task-level control, use `metastore_projects_locations_services_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_patch(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    metastore_projects_locations_services_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:queryMetadata
/// Query Dataproc Metastore metadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_query_metadata_execute()` to send, or `metastore_projects_locations_services_query_metadata` for simplest API.

pub fn metastore_projects_locations_services_query_metadata_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:queryMetadata",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:queryMetadata
/// Query Dataproc Metastore metadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_query_metadata_execute()` or `metastore_projects_locations_services_query_metadata`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_query_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_query_metadata_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:queryMetadata
/// Query Dataproc Metastore metadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_query_metadata_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_query_metadata_task()`.
/// For the simplest API, use `metastore_projects_locations_services_query_metadata()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_query_metadata_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_query_metadata_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_query_metadata_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_query_metadata`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesQueryMetadataArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:queryMetadata
/// Query Dataproc Metastore metadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_query_metadata_builder()` + `metastore_projects_locations_services_query_metadata_execute()`.
/// For task-level control, use `metastore_projects_locations_services_query_metadata_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_query_metadata(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesQueryMetadataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_query_metadata_builder(client, &args.service)?;
    metastore_projects_locations_services_query_metadata_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:restore
/// Restores a service from a backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_restore_execute()` to send, or `metastore_projects_locations_services_restore` for simplest API.

pub fn metastore_projects_locations_services_restore_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:restore",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:restore
/// Restores a service from a backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_restore_execute()` or `metastore_projects_locations_services_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:restore
/// Restores a service from a backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_restore_task()`.
/// For the simplest API, use `metastore_projects_locations_services_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesRestoreArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:restore
/// Restores a service from a backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_restore_builder()` + `metastore_projects_locations_services_restore_execute()`.
/// For task-level control, use `metastore_projects_locations_services_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_restore(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesRestoreArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_restore_builder(client, &args.service)?;
    metastore_projects_locations_services_restore_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_set_iam_policy_execute()` to send, or `metastore_projects_locations_services_set_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_set_iam_policy_execute()` or `metastore_projects_locations_services_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_set_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_set_iam_policy_builder()` + `metastore_projects_locations_services_set_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_set_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_set_iam_policy_builder(client, &args.resource)?;
    metastore_projects_locations_services_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:startMigration
/// Starts the Managed Migration process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_start_migration_execute()` to send, or `metastore_projects_locations_services_start_migration` for simplest API.

pub fn metastore_projects_locations_services_start_migration_builder<R>(
    client: &SimpleHttpClient<R>,
    service: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:startMigration",
        service,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:startMigration
/// Starts the Managed Migration process.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_start_migration_execute()` or `metastore_projects_locations_services_start_migration`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_start_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_start_migration_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:startMigration
/// Starts the Managed Migration process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_start_migration_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_start_migration_task()`.
/// For the simplest API, use `metastore_projects_locations_services_start_migration()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_start_migration_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_start_migration_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_start_migration_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_start_migration`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesStartMigrationArgs {
    /// Path parameter: service
    pub service: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:startMigration
/// Starts the Managed Migration process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_start_migration_builder()` + `metastore_projects_locations_services_start_migration_execute()`.
/// For task-level control, use `metastore_projects_locations_services_start_migration_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_start_migration(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesStartMigrationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_start_migration_builder(client, &args.service)?;
    metastore_projects_locations_services_start_migration_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_test_iam_permissions_execute()` to send, or `metastore_projects_locations_services_test_iam_permissions` for simplest API.

pub fn metastore_projects_locations_services_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_test_iam_permissions_execute()` or `metastore_projects_locations_services_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_test_iam_permissions_task()`.
/// For the simplest API, use `metastore_projects_locations_services_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_test_iam_permissions_builder()` + `metastore_projects_locations_services_test_iam_permissions_execute()`.
/// For task-level control, use `metastore_projects_locations_services_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_test_iam_permissions_builder(client, &args.resource)?;
    metastore_projects_locations_services_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Creates a new backup in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_backups_create_execute()` to send, or `metastore_projects_locations_services_backups_create` for simplest API.

pub fn metastore_projects_locations_services_backups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    backupId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = backupId.as_ref() {
        query_parts.push(format!("backupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Creates a new backup in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_backups_create_execute()` or `metastore_projects_locations_services_backups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Creates a new backup in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_backups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_backups_create_task()`.
/// For the simplest API, use `metastore_projects_locations_services_backups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_backups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_backups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_backups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesBackupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: backupId
    pub backupId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Creates a new backup in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_backups_create_builder()` + `metastore_projects_locations_services_backups_create_execute()`.
/// For task-level control, use `metastore_projects_locations_services_backups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_create(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesBackupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_backups_create_builder(
        client,
        &args.parent,
        &args.backupId,
        &args.requestId,
    )?;
    metastore_projects_locations_services_backups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Deletes a single backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_backups_delete_execute()` to send, or `metastore_projects_locations_services_backups_delete` for simplest API.

pub fn metastore_projects_locations_services_backups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Deletes a single backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_backups_delete_execute()` or `metastore_projects_locations_services_backups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Deletes a single backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_backups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_backups_delete_task()`.
/// For the simplest API, use `metastore_projects_locations_services_backups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_backups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_backups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_backups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesBackupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Deletes a single backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_backups_delete_builder()` + `metastore_projects_locations_services_backups_delete_execute()`.
/// For task-level control, use `metastore_projects_locations_services_backups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_delete(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesBackupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_backups_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    metastore_projects_locations_services_backups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Gets details of a single backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_backups_get_execute()` to send, or `metastore_projects_locations_services_backups_get` for simplest API.

pub fn metastore_projects_locations_services_backups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Gets details of a single backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_backups_get_execute()` or `metastore_projects_locations_services_backups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Backup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Backup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Gets details of a single backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_backups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_backups_get_task()`.
/// For the simplest API, use `metastore_projects_locations_services_backups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_backups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_backups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_backups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesBackupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}
/// Gets details of a single backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_backups_get_builder()` + `metastore_projects_locations_services_backups_get_execute()`.
/// For task-level control, use `metastore_projects_locations_services_backups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_get(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesBackupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_backups_get_builder(client, &args.name)?;
    metastore_projects_locations_services_backups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_backups_get_iam_policy_execute()` to send, or `metastore_projects_locations_services_backups_get_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_backups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_backups_get_iam_policy_execute()` or `metastore_projects_locations_services_backups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_backups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_backups_get_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_backups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_backups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_backups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_backups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesBackupsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_backups_get_iam_policy_builder()` + `metastore_projects_locations_services_backups_get_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_backups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesBackupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_backups_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    metastore_projects_locations_services_backups_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Lists backups in a service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_backups_list_execute()` to send, or `metastore_projects_locations_services_backups_list` for simplest API.

pub fn metastore_projects_locations_services_backups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Lists backups in a service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_backups_list_execute()` or `metastore_projects_locations_services_backups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBackupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Lists backups in a service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_backups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_backups_list_task()`.
/// For the simplest API, use `metastore_projects_locations_services_backups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_backups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_backups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_backups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesBackupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups
/// Lists backups in a service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_backups_list_builder()` + `metastore_projects_locations_services_backups_list_execute()`.
/// For task-level control, use `metastore_projects_locations_services_backups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_list(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesBackupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_backups_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    metastore_projects_locations_services_backups_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_backups_set_iam_policy_execute()` to send, or `metastore_projects_locations_services_backups_set_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_backups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_backups_set_iam_policy_execute()` or `metastore_projects_locations_services_backups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_backups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_backups_set_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_backups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_backups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_backups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_backups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_backups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesBackupsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/backups/{backupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_backups_set_iam_policy_builder()` + `metastore_projects_locations_services_backups_set_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_backups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_backups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesBackupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_backups_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    metastore_projects_locations_services_backups_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_databases_get_iam_policy_execute()` to send, or `metastore_projects_locations_services_databases_get_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_databases_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_databases_get_iam_policy_execute()` or `metastore_projects_locations_services_databases_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_databases_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_databases_get_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_databases_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_databases_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_databases_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_databases_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesDatabasesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_databases_get_iam_policy_builder()` + `metastore_projects_locations_services_databases_get_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_databases_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_get_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesDatabasesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_databases_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    metastore_projects_locations_services_databases_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_databases_set_iam_policy_execute()` to send, or `metastore_projects_locations_services_databases_set_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_databases_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_databases_set_iam_policy_execute()` or `metastore_projects_locations_services_databases_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_databases_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_databases_set_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_databases_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_databases_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_databases_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_databases_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesDatabasesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_databases_set_iam_policy_builder()` + `metastore_projects_locations_services_databases_set_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_databases_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_set_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesDatabasesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_databases_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    metastore_projects_locations_services_databases_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_databases_tables_get_iam_policy_execute()` to send, or `metastore_projects_locations_services_databases_tables_get_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_databases_tables_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_databases_tables_get_iam_policy_execute()` or `metastore_projects_locations_services_databases_tables_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_tables_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_tables_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_databases_tables_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_databases_tables_get_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_databases_tables_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_tables_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_databases_tables_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_databases_tables_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_databases_tables_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesDatabasesTablesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_databases_tables_get_iam_policy_builder()` + `metastore_projects_locations_services_databases_tables_get_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_databases_tables_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_tables_get_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesDatabasesTablesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_databases_tables_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    metastore_projects_locations_services_databases_tables_get_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_databases_tables_set_iam_policy_execute()` to send, or `metastore_projects_locations_services_databases_tables_set_iam_policy` for simplest API.

pub fn metastore_projects_locations_services_databases_tables_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_databases_tables_set_iam_policy_execute()` or `metastore_projects_locations_services_databases_tables_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_tables_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_tables_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_databases_tables_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_databases_tables_set_iam_policy_task()`.
/// For the simplest API, use `metastore_projects_locations_services_databases_tables_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_databases_tables_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_databases_tables_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_databases_tables_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_databases_tables_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesDatabasesTablesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/databases/{databasesId}/tables/{tablesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_databases_tables_set_iam_policy_builder()` + `metastore_projects_locations_services_databases_tables_set_iam_policy_execute()`.
/// For task-level control, use `metastore_projects_locations_services_databases_tables_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_databases_tables_set_iam_policy(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesDatabasesTablesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_databases_tables_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    metastore_projects_locations_services_databases_tables_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Creates a new MetadataImport in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_metadata_imports_create_execute()` to send, or `metastore_projects_locations_services_metadata_imports_create` for simplest API.

pub fn metastore_projects_locations_services_metadata_imports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    metadataImportId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/metadataImports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = metadataImportId.as_ref() {
        query_parts.push(format!("metadataImportId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Creates a new MetadataImport in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_metadata_imports_create_execute()` or `metastore_projects_locations_services_metadata_imports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Creates a new MetadataImport in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_metadata_imports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_create_task()`.
/// For the simplest API, use `metastore_projects_locations_services_metadata_imports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_metadata_imports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_metadata_imports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_metadata_imports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMetadataImportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: metadataImportId
    pub metadataImportId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Creates a new MetadataImport in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_metadata_imports_create_builder()` + `metastore_projects_locations_services_metadata_imports_create_execute()`.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_create(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMetadataImportsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_metadata_imports_create_builder(
        client,
        &args.parent,
        &args.metadataImportId,
        &args.requestId,
    )?;
    metastore_projects_locations_services_metadata_imports_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Gets details of a single import.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_metadata_imports_get_execute()` to send, or `metastore_projects_locations_services_metadata_imports_get` for simplest API.

pub fn metastore_projects_locations_services_metadata_imports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Gets details of a single import.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_metadata_imports_get_execute()` or `metastore_projects_locations_services_metadata_imports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MetadataImport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetadataImport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Gets details of a single import.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_metadata_imports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_get_task()`.
/// For the simplest API, use `metastore_projects_locations_services_metadata_imports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_metadata_imports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetadataImport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_metadata_imports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_metadata_imports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMetadataImportsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Gets details of a single import.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_metadata_imports_get_builder()` + `metastore_projects_locations_services_metadata_imports_get_execute()`.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_get(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMetadataImportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetadataImport>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_metadata_imports_get_builder(client, &args.name)?;
    metastore_projects_locations_services_metadata_imports_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Lists imports in a service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_metadata_imports_list_execute()` to send, or `metastore_projects_locations_services_metadata_imports_list` for simplest API.

pub fn metastore_projects_locations_services_metadata_imports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/metadataImports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Lists imports in a service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_metadata_imports_list_execute()` or `metastore_projects_locations_services_metadata_imports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMetadataImportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMetadataImportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Lists imports in a service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_metadata_imports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_list_task()`.
/// For the simplest API, use `metastore_projects_locations_services_metadata_imports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_metadata_imports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMetadataImportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_metadata_imports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_metadata_imports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMetadataImportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports
/// Lists imports in a service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_metadata_imports_list_builder()` + `metastore_projects_locations_services_metadata_imports_list_execute()`.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_list(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMetadataImportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMetadataImportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_metadata_imports_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    metastore_projects_locations_services_metadata_imports_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Updates a single import. Only the description field of MetadataImport is supported to be updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_metadata_imports_patch_execute()` to send, or `metastore_projects_locations_services_metadata_imports_patch` for simplest API.

pub fn metastore_projects_locations_services_metadata_imports_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Updates a single import. Only the description field of MetadataImport is supported to be updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_metadata_imports_patch_execute()` or `metastore_projects_locations_services_metadata_imports_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Updates a single import. Only the description field of MetadataImport is supported to be updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_metadata_imports_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_patch_task()`.
/// For the simplest API, use `metastore_projects_locations_services_metadata_imports_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_metadata_imports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_metadata_imports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_metadata_imports_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_metadata_imports_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMetadataImportsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/metadataImports/{metadataImportsId}
/// Updates a single import. Only the description field of MetadataImport is supported to be updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_metadata_imports_patch_builder()` + `metastore_projects_locations_services_metadata_imports_patch_execute()`.
/// For task-level control, use `metastore_projects_locations_services_metadata_imports_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_metadata_imports_patch(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMetadataImportsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_metadata_imports_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    metastore_projects_locations_services_metadata_imports_patch_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Deletes a single migration execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_migration_executions_delete_execute()` to send, or `metastore_projects_locations_services_migration_executions_delete` for simplest API.

pub fn metastore_projects_locations_services_migration_executions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Deletes a single migration execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_migration_executions_delete_execute()` or `metastore_projects_locations_services_migration_executions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_migration_executions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_migration_executions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Deletes a single migration execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_migration_executions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_migration_executions_delete_task()`.
/// For the simplest API, use `metastore_projects_locations_services_migration_executions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_migration_executions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_migration_executions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_migration_executions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_migration_executions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMigrationExecutionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Deletes a single migration execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_migration_executions_delete_builder()` + `metastore_projects_locations_services_migration_executions_delete_execute()`.
/// For task-level control, use `metastore_projects_locations_services_migration_executions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_migration_executions_delete(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMigrationExecutionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_migration_executions_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    metastore_projects_locations_services_migration_executions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Gets details of a single migration execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_migration_executions_get_execute()` to send, or `metastore_projects_locations_services_migration_executions_get` for simplest API.

pub fn metastore_projects_locations_services_migration_executions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Gets details of a single migration execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_migration_executions_get_execute()` or `metastore_projects_locations_services_migration_executions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_migration_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_migration_executions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MigrationExecution>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigrationExecution = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Gets details of a single migration execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_migration_executions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_migration_executions_get_task()`.
/// For the simplest API, use `metastore_projects_locations_services_migration_executions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_migration_executions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_migration_executions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationExecution>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_migration_executions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_migration_executions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMigrationExecutionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions/{migrationExecutionsId}
/// Gets details of a single migration execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_migration_executions_get_builder()` + `metastore_projects_locations_services_migration_executions_get_execute()`.
/// For task-level control, use `metastore_projects_locations_services_migration_executions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_migration_executions_get(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMigrationExecutionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MigrationExecution>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        metastore_projects_locations_services_migration_executions_get_builder(client, &args.name)?;
    metastore_projects_locations_services_migration_executions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions
/// Lists migration executions on a service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `metastore_projects_locations_services_migration_executions_list_execute()` to send, or `metastore_projects_locations_services_migration_executions_list` for simplest API.

pub fn metastore_projects_locations_services_migration_executions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://metastore.googleapis.com/v1/projects/{}/locations/{locationsId}/services/{servicesId}/migrationExecutions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions
/// Lists migration executions on a service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `metastore_projects_locations_services_migration_executions_list_execute()` or `metastore_projects_locations_services_migration_executions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_migration_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_migration_executions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMigrationExecutionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMigrationExecutionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions
/// Lists migration executions on a service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `metastore_projects_locations_services_migration_executions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `metastore_projects_locations_services_migration_executions_list_task()`.
/// For the simplest API, use `metastore_projects_locations_services_migration_executions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `metastore_projects_locations_services_migration_executions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn metastore_projects_locations_services_migration_executions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMigrationExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = metastore_projects_locations_services_migration_executions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`metastore_projects_locations_services_migration_executions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MetastoreProjectsLocationsServicesMigrationExecutionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/services/{servicesId}/migrationExecutions
/// Lists migration executions on a service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `metastore_projects_locations_services_migration_executions_list_builder()` + `metastore_projects_locations_services_migration_executions_list_execute()`.
/// For task-level control, use `metastore_projects_locations_services_migration_executions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn metastore_projects_locations_services_migration_executions_list(
    client: &SimpleHttpClient,
    args: &MetastoreProjectsLocationsServicesMigrationExecutionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMigrationExecutionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = metastore_projects_locations_services_migration_executions_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    metastore_projects_locations_services_migration_executions_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with MetastoreProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsGetArgs) -> String {
        format!("gcp::metastore::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with MetastoreProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsListArgs) -> String {
        format!("gcp::metastore::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsFederationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsFederationsCreateArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsFederationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsFederationsDeleteArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Federation
// =============================================================================

/// ResourceIdentifier implementation for Federation with MetastoreProjectsLocationsFederationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsGetArgs> for Federation {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsFederationsGetArgs) -> String {
        format!("gcp::metastore::Federation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Federation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsFederationsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsFederationsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFederationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFederationsResponse with MetastoreProjectsLocationsFederationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsListArgs> for ListFederationsResponse {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsFederationsListArgs,
    ) -> String {
        format!("gcp::metastore::ListFederationsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::ListFederationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsFederationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsFederationsPatchArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsFederationsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsFederationsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with MetastoreProjectsLocationsFederationsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsFederationsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsFederationsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::metastore::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MetastoreProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::metastore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with MetastoreProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::metastore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with MetastoreProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsOperationsListArgs) -> String {
        format!("gcp::metastore::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesAlterLocationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesAlterLocationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesAlterLocationArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesAlterTablePropertiesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesAlterTablePropertiesArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesAlterTablePropertiesArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesCancelMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesCancelMigrationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesCancelMigrationArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesCompleteMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesCompleteMigrationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesCompleteMigrationArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsServicesCreateArgs) -> String {
        format!("gcp::metastore::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsServicesDeleteArgs) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesExportMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesExportMetadataArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesExportMetadataArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Service
// =============================================================================

/// ResourceIdentifier implementation for Service with MetastoreProjectsLocationsServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesGetArgs> for Service {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsServicesGetArgs) -> String {
        format!("gcp::metastore::Service/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Service"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServicesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServicesResponse with MetastoreProjectsLocationsServicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesListArgs> for ListServicesResponse {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsServicesListArgs) -> String {
        format!("gcp::metastore::ListServicesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::ListServicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesMoveTableToDatabaseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMoveTableToDatabaseArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMoveTableToDatabaseArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &MetastoreProjectsLocationsServicesPatchArgs) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesQueryMetadataArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesQueryMetadataArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesQueryMetadataArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesRestoreArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesRestoreArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesRestoreArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesStartMigrationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesStartMigrationArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesStartMigrationArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.service)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with MetastoreProjectsLocationsServicesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::metastore::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesBackupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesBackupsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesBackupsCreateArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesBackupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesBackupsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesBackupsDeleteArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Backup
// =============================================================================

/// ResourceIdentifier implementation for Backup with MetastoreProjectsLocationsServicesBackupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesBackupsGetArgs> for Backup {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesBackupsGetArgs,
    ) -> String {
        format!("gcp::metastore::Backup/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Backup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesBackupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesBackupsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesBackupsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBackupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBackupsResponse with MetastoreProjectsLocationsServicesBackupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesBackupsListArgs> for ListBackupsResponse {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesBackupsListArgs,
    ) -> String {
        format!("gcp::metastore::ListBackupsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::ListBackupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesBackupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesBackupsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesBackupsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesDatabasesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesDatabasesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesDatabasesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesDatabasesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesDatabasesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesDatabasesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesDatabasesTablesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesDatabasesTablesGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesDatabasesTablesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with MetastoreProjectsLocationsServicesDatabasesTablesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesDatabasesTablesSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesDatabasesTablesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::metastore::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesMetadataImportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMetadataImportsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMetadataImportsCreateArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MetadataImport
// =============================================================================

/// ResourceIdentifier implementation for MetadataImport with MetastoreProjectsLocationsServicesMetadataImportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMetadataImportsGetArgs>
    for MetadataImport
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMetadataImportsGetArgs,
    ) -> String {
        format!("gcp::metastore::MetadataImport/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::MetadataImport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMetadataImportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMetadataImportsResponse with MetastoreProjectsLocationsServicesMetadataImportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMetadataImportsListArgs>
    for ListMetadataImportsResponse
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMetadataImportsListArgs,
    ) -> String {
        format!(
            "gcp::metastore::ListMetadataImportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::ListMetadataImportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesMetadataImportsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMetadataImportsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMetadataImportsPatchArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with MetastoreProjectsLocationsServicesMigrationExecutionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMigrationExecutionsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMigrationExecutionsDeleteArgs,
    ) -> String {
        format!("gcp::metastore::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MigrationExecution
// =============================================================================

/// ResourceIdentifier implementation for MigrationExecution with MetastoreProjectsLocationsServicesMigrationExecutionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMigrationExecutionsGetArgs>
    for MigrationExecution
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMigrationExecutionsGetArgs,
    ) -> String {
        format!("gcp::metastore::MigrationExecution/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::MigrationExecution"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMigrationExecutionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMigrationExecutionsResponse with MetastoreProjectsLocationsServicesMigrationExecutionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<MetastoreProjectsLocationsServicesMigrationExecutionsListArgs>
    for ListMigrationExecutionsResponse
{
    fn generate_resource_id(
        &self,
        input: &MetastoreProjectsLocationsServicesMigrationExecutionsListArgs,
    ) -> String {
        format!(
            "gcp::metastore::ListMigrationExecutionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::metastore::ListMigrationExecutionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
