//! Auto-generated API clients for gcp/fcmdata.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/deliveryData
/// List aggregate delivery data for the given Android application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fcmdata_projects_android_apps_delivery_data_list_execute()` to send, or `fcmdata_projects_android_apps_delivery_data_list` for simplest API.

pub fn fcmdata_projects_android_apps_delivery_data_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fcmdata.googleapis.com/v1beta1/projects/{}/androidApps/{androidAppsId}/deliveryData",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/deliveryData
/// List aggregate delivery data for the given Android application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fcmdata_projects_android_apps_delivery_data_list_execute()` or `fcmdata_projects_android_apps_delivery_data_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fcmdata_projects_android_apps_delivery_data_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fcmdata_projects_android_apps_delivery_data_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/deliveryData
/// List aggregate delivery data for the given Android application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fcmdata_projects_android_apps_delivery_data_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fcmdata_projects_android_apps_delivery_data_list_task()`.
/// For the simplest API, use `fcmdata_projects_android_apps_delivery_data_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fcmdata_projects_android_apps_delivery_data_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fcmdata_projects_android_apps_delivery_data_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = fcmdata_projects_android_apps_delivery_data_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fcmdata_projects_android_apps_delivery_data_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FcmdataProjectsAndroidAppsDeliveryDataListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/androidApps/{androidAppsId}/deliveryData
/// List aggregate delivery data for the given Android application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fcmdata_projects_android_apps_delivery_data_list_builder()` + `fcmdata_projects_android_apps_delivery_data_list_execute()`.
/// For task-level control, use `fcmdata_projects_android_apps_delivery_data_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fcmdata_projects_android_apps_delivery_data_list(
    client: &SimpleHttpClient,
    args: &FcmdataProjectsAndroidAppsDeliveryDataListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = fcmdata_projects_android_apps_delivery_data_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    fcmdata_projects_android_apps_delivery_data_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse with FcmdataProjectsAndroidAppsDeliveryDataListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FcmdataProjectsAndroidAppsDeliveryDataListArgs>
    for GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse
{
    fn generate_resource_id(
        &self,
        input: &FcmdataProjectsAndroidAppsDeliveryDataListArgs,
    ) -> String {
        format!(
            "gcp::fcmdata::GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fcmdata::GoogleFirebaseFcmDataV1beta1ListAndroidDeliveryDataResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
