//! Auto-generated API clients for gcp/composer.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:checkUpgrade
/// Check if an upgrade operation on the environment will succeed. In case of problems detailed info can be found in the returned Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_check_upgrade_execute()` to send, or `composer_projects_locations_environments_check_upgrade` for simplest API.

pub fn composer_projects_locations_environments_check_upgrade_builder(
    client: &SimpleHttpClient,
    environment: &str,
    body: &CheckUpgradeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:checkUpgrade",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:checkUpgrade
/// Check if an upgrade operation on the environment will succeed. In case of problems detailed info can be found in the returned Operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_check_upgrade_execute()` or `composer_projects_locations_environments_check_upgrade`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_check_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_check_upgrade_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:checkUpgrade
/// Check if an upgrade operation on the environment will succeed. In case of problems detailed info can be found in the returned Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_check_upgrade_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_check_upgrade_task()`.
/// For the simplest API, use `composer_projects_locations_environments_check_upgrade()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_check_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_check_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_check_upgrade_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_check_upgrade`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsCheckUpgradeArgs {
    /// Path parameter: environment
    pub environment: String,
    /// Request body.
    pub body: CheckUpgradeRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:checkUpgrade
/// Check if an upgrade operation on the environment will succeed. In case of problems detailed info can be found in the returned Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_check_upgrade_builder()` + `composer_projects_locations_environments_check_upgrade_execute()`.
/// For task-level control, use `composer_projects_locations_environments_check_upgrade_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_check_upgrade(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsCheckUpgradeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_check_upgrade_builder(
        client,
        &args.environment,
        &args.body,
    )?;
    composer_projects_locations_environments_check_upgrade_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// Create a new environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_create_execute()` to send, or `composer_projects_locations_environments_create` for simplest API.

pub fn composer_projects_locations_environments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Environment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// Create a new environment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_create_execute()` or `composer_projects_locations_environments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// Create a new environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_create_task()`.
/// For the simplest API, use `composer_projects_locations_environments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Environment,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// Create a new environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_create_builder()` + `composer_projects_locations_environments_create_execute()`.
/// For task-level control, use `composer_projects_locations_environments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_create(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        composer_projects_locations_environments_create_builder(client, &args.parent, &args.body)?;
    composer_projects_locations_environments_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:databaseFailover
/// Triggers database failover (only for highly resilient environments).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_database_failover_execute()` to send, or `composer_projects_locations_environments_database_failover` for simplest API.

pub fn composer_projects_locations_environments_database_failover_builder(
    client: &SimpleHttpClient,
    environment: &str,
    body: &DatabaseFailoverRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:databaseFailover",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:databaseFailover
/// Triggers database failover (only for highly resilient environments).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_database_failover_execute()` or `composer_projects_locations_environments_database_failover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_database_failover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_database_failover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:databaseFailover
/// Triggers database failover (only for highly resilient environments).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_database_failover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_database_failover_task()`.
/// For the simplest API, use `composer_projects_locations_environments_database_failover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_database_failover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_database_failover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_database_failover_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_database_failover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsDatabaseFailoverArgs {
    /// Path parameter: environment
    pub environment: String,
    /// Request body.
    pub body: DatabaseFailoverRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:databaseFailover
/// Triggers database failover (only for highly resilient environments).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_database_failover_builder()` + `composer_projects_locations_environments_database_failover_execute()`.
/// For task-level control, use `composer_projects_locations_environments_database_failover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_database_failover(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsDatabaseFailoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_database_failover_builder(
        client,
        &args.environment,
        &args.body,
    )?;
    composer_projects_locations_environments_database_failover_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Delete an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_delete_execute()` to send, or `composer_projects_locations_environments_delete` for simplest API.

pub fn composer_projects_locations_environments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Delete an environment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_delete_execute()` or `composer_projects_locations_environments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Delete an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_delete_task()`.
/// For the simplest API, use `composer_projects_locations_environments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Delete an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_delete_builder()` + `composer_projects_locations_environments_delete_execute()`.
/// For task-level control, use `composer_projects_locations_environments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_delete(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_delete_builder(client, &args.name)?;
    composer_projects_locations_environments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:executeAirflowCommand
/// Executes Airflow CLI command.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_execute_airflow_command_execute()` to send, or `composer_projects_locations_environments_execute_airflow_command` for simplest API.

pub fn composer_projects_locations_environments_execute_airflow_command_builder(
    client: &SimpleHttpClient,
    environment: &str,
    body: &ExecuteAirflowCommandRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:executeAirflowCommand",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:executeAirflowCommand
/// Executes Airflow CLI command.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_execute_airflow_command_execute()` or `composer_projects_locations_environments_execute_airflow_command`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_execute_airflow_command_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_execute_airflow_command_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ExecuteAirflowCommandResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecuteAirflowCommandResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:executeAirflowCommand
/// Executes Airflow CLI command.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_execute_airflow_command_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_execute_airflow_command_task()`.
/// For the simplest API, use `composer_projects_locations_environments_execute_airflow_command()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_execute_airflow_command_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_execute_airflow_command_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExecuteAirflowCommandResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_execute_airflow_command_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_execute_airflow_command`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsExecuteAirflowCommandArgs {
    /// Path parameter: environment
    pub environment: String,
    /// Request body.
    pub body: ExecuteAirflowCommandRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:executeAirflowCommand
/// Executes Airflow CLI command.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_execute_airflow_command_builder()` + `composer_projects_locations_environments_execute_airflow_command_execute()`.
/// For task-level control, use `composer_projects_locations_environments_execute_airflow_command_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_execute_airflow_command(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsExecuteAirflowCommandArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExecuteAirflowCommandResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_execute_airflow_command_builder(
        client,
        &args.environment,
        &args.body,
    )?;
    composer_projects_locations_environments_execute_airflow_command_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:fetchDatabaseProperties
/// Fetches database properties.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_fetch_database_properties_execute()` to send, or `composer_projects_locations_environments_fetch_database_properties` for simplest API.

pub fn composer_projects_locations_environments_fetch_database_properties_builder(
    client: &SimpleHttpClient,
    environment: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:fetchDatabaseProperties",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:fetchDatabaseProperties
/// Fetches database properties.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_fetch_database_properties_execute()` or `composer_projects_locations_environments_fetch_database_properties`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_fetch_database_properties_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_fetch_database_properties_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<FetchDatabasePropertiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchDatabasePropertiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:fetchDatabaseProperties
/// Fetches database properties.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_fetch_database_properties_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_fetch_database_properties_task()`.
/// For the simplest API, use `composer_projects_locations_environments_fetch_database_properties()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_fetch_database_properties_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_fetch_database_properties_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchDatabasePropertiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_fetch_database_properties_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_fetch_database_properties`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsFetchDatabasePropertiesArgs {
    /// Path parameter: environment
    pub environment: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:fetchDatabaseProperties
/// Fetches database properties.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_fetch_database_properties_builder()` + `composer_projects_locations_environments_fetch_database_properties_execute()`.
/// For task-level control, use `composer_projects_locations_environments_fetch_database_properties_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_fetch_database_properties(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsFetchDatabasePropertiesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchDatabasePropertiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_fetch_database_properties_builder(
        client,
        &args.environment,
    )?;
    composer_projects_locations_environments_fetch_database_properties_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Get an existing environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_get_execute()` to send, or `composer_projects_locations_environments_get` for simplest API.

pub fn composer_projects_locations_environments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Get an existing environment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_get_execute()` or `composer_projects_locations_environments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Environment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Environment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Get an existing environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_get_task()`.
/// For the simplest API, use `composer_projects_locations_environments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Environment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Get an existing environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_get_builder()` + `composer_projects_locations_environments_get_execute()`.
/// For task-level control, use `composer_projects_locations_environments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_get(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Environment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_get_builder(client, &args.name)?;
    composer_projects_locations_environments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// List environments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_list_execute()` to send, or `composer_projects_locations_environments_list` for simplest API.

pub fn composer_projects_locations_environments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// List environments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_list_execute()` or `composer_projects_locations_environments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListEnvironmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEnvironmentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// List environments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_list_task()`.
/// For the simplest API, use `composer_projects_locations_environments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEnvironmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments
/// List environments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_list_builder()` + `composer_projects_locations_environments_list_execute()`.
/// For task-level control, use `composer_projects_locations_environments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_list(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEnvironmentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    composer_projects_locations_environments_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:loadSnapshot
/// Loads a snapshot of a Cloud Composer environment. As a result of this operation, a snapshot of environment's specified in LoadSnapshotRequest is loaded into the environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_load_snapshot_execute()` to send, or `composer_projects_locations_environments_load_snapshot` for simplest API.

pub fn composer_projects_locations_environments_load_snapshot_builder(
    client: &SimpleHttpClient,
    environment: &str,
    body: &LoadSnapshotRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:loadSnapshot",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:loadSnapshot
/// Loads a snapshot of a Cloud Composer environment. As a result of this operation, a snapshot of environment's specified in LoadSnapshotRequest is loaded into the environment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_load_snapshot_execute()` or `composer_projects_locations_environments_load_snapshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_load_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_load_snapshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:loadSnapshot
/// Loads a snapshot of a Cloud Composer environment. As a result of this operation, a snapshot of environment's specified in LoadSnapshotRequest is loaded into the environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_load_snapshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_load_snapshot_task()`.
/// For the simplest API, use `composer_projects_locations_environments_load_snapshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_load_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_load_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_load_snapshot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_load_snapshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsLoadSnapshotArgs {
    /// Path parameter: environment
    pub environment: String,
    /// Request body.
    pub body: LoadSnapshotRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:loadSnapshot
/// Loads a snapshot of a Cloud Composer environment. As a result of this operation, a snapshot of environment's specified in LoadSnapshotRequest is loaded into the environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_load_snapshot_builder()` + `composer_projects_locations_environments_load_snapshot_execute()`.
/// For task-level control, use `composer_projects_locations_environments_load_snapshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_load_snapshot(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsLoadSnapshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_load_snapshot_builder(
        client,
        &args.environment,
        &args.body,
    )?;
    composer_projects_locations_environments_load_snapshot_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Update an environment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_patch_execute()` to send, or `composer_projects_locations_environments_patch` for simplest API.

pub fn composer_projects_locations_environments_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Environment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Update an environment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_patch_execute()` or `composer_projects_locations_environments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Update an environment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_patch_task()`.
/// For the simplest API, use `composer_projects_locations_environments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Environment,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}
/// Update an environment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_patch_builder()` + `composer_projects_locations_environments_patch_execute()`.
/// For task-level control, use `composer_projects_locations_environments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_patch(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    composer_projects_locations_environments_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:pollAirflowCommand
/// Polls Airflow CLI command execution and fetches logs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_poll_airflow_command_execute()` to send, or `composer_projects_locations_environments_poll_airflow_command` for simplest API.

pub fn composer_projects_locations_environments_poll_airflow_command_builder(
    client: &SimpleHttpClient,
    environment: &str,
    body: &PollAirflowCommandRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:pollAirflowCommand",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:pollAirflowCommand
/// Polls Airflow CLI command execution and fetches logs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_poll_airflow_command_execute()` or `composer_projects_locations_environments_poll_airflow_command`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_poll_airflow_command_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_poll_airflow_command_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PollAirflowCommandResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PollAirflowCommandResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:pollAirflowCommand
/// Polls Airflow CLI command execution and fetches logs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_poll_airflow_command_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_poll_airflow_command_task()`.
/// For the simplest API, use `composer_projects_locations_environments_poll_airflow_command()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_poll_airflow_command_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_poll_airflow_command_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PollAirflowCommandResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_poll_airflow_command_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_poll_airflow_command`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsPollAirflowCommandArgs {
    /// Path parameter: environment
    pub environment: String,
    /// Request body.
    pub body: PollAirflowCommandRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:pollAirflowCommand
/// Polls Airflow CLI command execution and fetches logs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_poll_airflow_command_builder()` + `composer_projects_locations_environments_poll_airflow_command_execute()`.
/// For task-level control, use `composer_projects_locations_environments_poll_airflow_command_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_poll_airflow_command(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsPollAirflowCommandArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PollAirflowCommandResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_poll_airflow_command_builder(
        client,
        &args.environment,
        &args.body,
    )?;
    composer_projects_locations_environments_poll_airflow_command_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:restartWebServer
/// Restart Airflow web server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_restart_web_server_execute()` to send, or `composer_projects_locations_environments_restart_web_server` for simplest API.

pub fn composer_projects_locations_environments_restart_web_server_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RestartWebServerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:restartWebServer",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:restartWebServer
/// Restart Airflow web server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_restart_web_server_execute()` or `composer_projects_locations_environments_restart_web_server`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_restart_web_server_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_restart_web_server_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:restartWebServer
/// Restart Airflow web server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_restart_web_server_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_restart_web_server_task()`.
/// For the simplest API, use `composer_projects_locations_environments_restart_web_server()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_restart_web_server_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_restart_web_server_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_restart_web_server_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_restart_web_server`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsRestartWebServerArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RestartWebServerRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:restartWebServer
/// Restart Airflow web server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_restart_web_server_builder()` + `composer_projects_locations_environments_restart_web_server_execute()`.
/// For task-level control, use `composer_projects_locations_environments_restart_web_server_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_restart_web_server(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsRestartWebServerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_restart_web_server_builder(
        client, &args.name, &args.body,
    )?;
    composer_projects_locations_environments_restart_web_server_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:saveSnapshot
/// Creates a snapshots of a Cloud Composer environment. As a result of this operation, snapshot of environment's state is stored in a location specified in the SaveSnapshotRequest.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_save_snapshot_execute()` to send, or `composer_projects_locations_environments_save_snapshot` for simplest API.

pub fn composer_projects_locations_environments_save_snapshot_builder(
    client: &SimpleHttpClient,
    environment: &str,
    body: &SaveSnapshotRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:saveSnapshot",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:saveSnapshot
/// Creates a snapshots of a Cloud Composer environment. As a result of this operation, snapshot of environment's state is stored in a location specified in the SaveSnapshotRequest.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_save_snapshot_execute()` or `composer_projects_locations_environments_save_snapshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_save_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_save_snapshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:saveSnapshot
/// Creates a snapshots of a Cloud Composer environment. As a result of this operation, snapshot of environment's state is stored in a location specified in the SaveSnapshotRequest.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_save_snapshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_save_snapshot_task()`.
/// For the simplest API, use `composer_projects_locations_environments_save_snapshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_save_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_save_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_save_snapshot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_save_snapshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsSaveSnapshotArgs {
    /// Path parameter: environment
    pub environment: String,
    /// Request body.
    pub body: SaveSnapshotRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:saveSnapshot
/// Creates a snapshots of a Cloud Composer environment. As a result of this operation, snapshot of environment's state is stored in a location specified in the SaveSnapshotRequest.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_save_snapshot_builder()` + `composer_projects_locations_environments_save_snapshot_execute()`.
/// For task-level control, use `composer_projects_locations_environments_save_snapshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_save_snapshot(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsSaveSnapshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_save_snapshot_builder(
        client,
        &args.environment,
        &args.body,
    )?;
    composer_projects_locations_environments_save_snapshot_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:stopAirflowCommand
/// Stops Airflow CLI command execution.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_stop_airflow_command_execute()` to send, or `composer_projects_locations_environments_stop_airflow_command` for simplest API.

pub fn composer_projects_locations_environments_stop_airflow_command_builder(
    client: &SimpleHttpClient,
    environment: &str,
    body: &StopAirflowCommandRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}:stopAirflowCommand",
        environment,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:stopAirflowCommand
/// Stops Airflow CLI command execution.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_stop_airflow_command_execute()` or `composer_projects_locations_environments_stop_airflow_command`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_stop_airflow_command_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_stop_airflow_command_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<StopAirflowCommandResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StopAirflowCommandResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:stopAirflowCommand
/// Stops Airflow CLI command execution.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_stop_airflow_command_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_stop_airflow_command_task()`.
/// For the simplest API, use `composer_projects_locations_environments_stop_airflow_command()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_stop_airflow_command_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_stop_airflow_command_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StopAirflowCommandResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_stop_airflow_command_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_stop_airflow_command`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsStopAirflowCommandArgs {
    /// Path parameter: environment
    pub environment: String,
    /// Request body.
    pub body: StopAirflowCommandRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}:stopAirflowCommand
/// Stops Airflow CLI command execution.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_stop_airflow_command_builder()` + `composer_projects_locations_environments_stop_airflow_command_execute()`.
/// For task-level control, use `composer_projects_locations_environments_stop_airflow_command_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_stop_airflow_command(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsStopAirflowCommandArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<StopAirflowCommandResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_stop_airflow_command_builder(
        client,
        &args.environment,
        &args.body,
    )?;
    composer_projects_locations_environments_stop_airflow_command_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Creates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_config_maps_create_execute()` to send, or `composer_projects_locations_environments_user_workloads_config_maps_create` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_config_maps_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UserWorkloadsConfigMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsConfigMaps",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Creates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_config_maps_create_execute()` or `composer_projects_locations_environments_user_workloads_config_maps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserWorkloadsConfigMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Creates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_config_maps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_create_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_config_maps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_config_maps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_config_maps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_config_maps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: UserWorkloadsConfigMap,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Creates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_config_maps_create_builder()` + `composer_projects_locations_environments_user_workloads_config_maps_create_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_create(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        composer_projects_locations_environments_user_workloads_config_maps_create_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    composer_projects_locations_environments_user_workloads_config_maps_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Deletes a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_config_maps_delete_execute()` to send, or `composer_projects_locations_environments_user_workloads_config_maps_delete` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_config_maps_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsConfigMaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Deletes a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_config_maps_delete_execute()` or `composer_projects_locations_environments_user_workloads_config_maps_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Deletes a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_config_maps_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_delete_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_config_maps_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_config_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_config_maps_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_config_maps_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Deletes a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_config_maps_delete_builder()` + `composer_projects_locations_environments_user_workloads_config_maps_delete_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_delete(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        composer_projects_locations_environments_user_workloads_config_maps_delete_builder(
            client, &args.name,
        )?;
    composer_projects_locations_environments_user_workloads_config_maps_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Gets an existing user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_config_maps_get_execute()` to send, or `composer_projects_locations_environments_user_workloads_config_maps_get` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_config_maps_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsConfigMaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Gets an existing user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_config_maps_get_execute()` or `composer_projects_locations_environments_user_workloads_config_maps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserWorkloadsConfigMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Gets an existing user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_config_maps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_get_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_config_maps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_config_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_config_maps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_config_maps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Gets an existing user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_config_maps_get_builder()` + `composer_projects_locations_environments_user_workloads_config_maps_get_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_get(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_user_workloads_config_maps_get_builder(
        client, &args.name,
    )?;
    composer_projects_locations_environments_user_workloads_config_maps_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Lists user workloads ConfigMaps. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_config_maps_list_execute()` to send, or `composer_projects_locations_environments_user_workloads_config_maps_list` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_config_maps_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsConfigMaps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Lists user workloads ConfigMaps. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_config_maps_list_execute()` or `composer_projects_locations_environments_user_workloads_config_maps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListUserWorkloadsConfigMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUserWorkloadsConfigMapsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Lists user workloads ConfigMaps. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_config_maps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_list_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_config_maps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_config_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserWorkloadsConfigMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_config_maps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_config_maps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps
/// Lists user workloads ConfigMaps. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_config_maps_list_builder()` + `composer_projects_locations_environments_user_workloads_config_maps_list_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_list(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserWorkloadsConfigMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_user_workloads_config_maps_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    composer_projects_locations_environments_user_workloads_config_maps_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Updates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_config_maps_update_execute()` to send, or `composer_projects_locations_environments_user_workloads_config_maps_update` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_config_maps_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UserWorkloadsConfigMap,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsConfigMaps/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Updates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_config_maps_update_execute()` or `composer_projects_locations_environments_user_workloads_config_maps_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserWorkloadsConfigMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Updates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_config_maps_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_update_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_config_maps_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_config_maps_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_config_maps_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_config_maps_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_config_maps_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UserWorkloadsConfigMap,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsConfigMaps/{userWorkloadsConfigMapsId}
/// Updates a user workloads ConfigMap. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_config_maps_update_builder()` + `composer_projects_locations_environments_user_workloads_config_maps_update_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_config_maps_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_config_maps_update(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsConfigMapsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsConfigMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        composer_projects_locations_environments_user_workloads_config_maps_update_builder(
            client, &args.name, &args.body,
        )?;
    composer_projects_locations_environments_user_workloads_config_maps_update_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Creates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_secrets_create_execute()` to send, or `composer_projects_locations_environments_user_workloads_secrets_create` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_secrets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &UserWorkloadsSecret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsSecrets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Creates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_secrets_create_execute()` or `composer_projects_locations_environments_user_workloads_secrets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserWorkloadsSecret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Creates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_secrets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_create_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_secrets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_secrets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_secrets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_secrets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: UserWorkloadsSecret,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Creates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_secrets_create_builder()` + `composer_projects_locations_environments_user_workloads_secrets_create_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_create(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_user_workloads_secrets_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    composer_projects_locations_environments_user_workloads_secrets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Deletes a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_secrets_delete_execute()` to send, or `composer_projects_locations_environments_user_workloads_secrets_delete` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_secrets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsSecrets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Deletes a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_secrets_delete_execute()` or `composer_projects_locations_environments_user_workloads_secrets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Deletes a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_secrets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_delete_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_secrets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_secrets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_secrets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_secrets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Deletes a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_secrets_delete_builder()` + `composer_projects_locations_environments_user_workloads_secrets_delete_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_delete(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_user_workloads_secrets_delete_builder(
        client, &args.name,
    )?;
    composer_projects_locations_environments_user_workloads_secrets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Gets an existing user workloads Secret. Values of the "data" field in the response are cleared. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_secrets_get_execute()` to send, or `composer_projects_locations_environments_user_workloads_secrets_get` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_secrets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsSecrets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Gets an existing user workloads Secret. Values of the "data" field in the response are cleared. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_secrets_get_execute()` or `composer_projects_locations_environments_user_workloads_secrets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserWorkloadsSecret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Gets an existing user workloads Secret. Values of the "data" field in the response are cleared. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_secrets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_get_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_secrets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_secrets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_user_workloads_secrets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_secrets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Gets an existing user workloads Secret. Values of the "data" field in the response are cleared. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_secrets_get_builder()` + `composer_projects_locations_environments_user_workloads_secrets_get_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_get(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_user_workloads_secrets_get_builder(
        client, &args.name,
    )?;
    composer_projects_locations_environments_user_workloads_secrets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Lists user workloads Secrets. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_secrets_list_execute()` to send, or `composer_projects_locations_environments_user_workloads_secrets_list` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_secrets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsSecrets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Lists user workloads Secrets. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_secrets_list_execute()` or `composer_projects_locations_environments_user_workloads_secrets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListUserWorkloadsSecretsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUserWorkloadsSecretsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Lists user workloads Secrets. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_secrets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_list_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_secrets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_secrets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserWorkloadsSecretsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_user_workloads_secrets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_secrets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets
/// Lists user workloads Secrets. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_secrets_list_builder()` + `composer_projects_locations_environments_user_workloads_secrets_list_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_list(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUserWorkloadsSecretsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_user_workloads_secrets_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    composer_projects_locations_environments_user_workloads_secrets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Updates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_user_workloads_secrets_update_execute()` to send, or `composer_projects_locations_environments_user_workloads_secrets_update` for simplest API.

pub fn composer_projects_locations_environments_user_workloads_secrets_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UserWorkloadsSecret,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/userWorkloadsSecrets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Updates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_user_workloads_secrets_update_execute()` or `composer_projects_locations_environments_user_workloads_secrets_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserWorkloadsSecret = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Updates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_user_workloads_secrets_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_update_task()`.
/// For the simplest API, use `composer_projects_locations_environments_user_workloads_secrets_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_user_workloads_secrets_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_user_workloads_secrets_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        composer_projects_locations_environments_user_workloads_secrets_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_user_workloads_secrets_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UserWorkloadsSecret,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/userWorkloadsSecrets/{userWorkloadsSecretsId}
/// Updates a user workloads Secret. This method is supported for Cloud Composer environments in versions composer-3-airflow-*.*.*-build.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_user_workloads_secrets_update_builder()` + `composer_projects_locations_environments_user_workloads_secrets_update_execute()`.
/// For task-level control, use `composer_projects_locations_environments_user_workloads_secrets_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_user_workloads_secrets_update(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsUserWorkloadsSecretsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserWorkloadsSecret>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_user_workloads_secrets_update_builder(
        client, &args.name, &args.body,
    )?;
    composer_projects_locations_environments_user_workloads_secrets_update_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/workloads
/// Lists workloads in a Cloud Composer environment. Workload is a unit that runs a single Composer component. This method is supported for Cloud Composer environments in versions composer-2.*.*-airflow-*.*.* and newer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_environments_workloads_list_execute()` to send, or `composer_projects_locations_environments_workloads_list` for simplest API.

pub fn composer_projects_locations_environments_workloads_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/environments/{}/workloads",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/workloads
/// Lists workloads in a Cloud Composer environment. Workload is a unit that runs a single Composer component. This method is supported for Cloud Composer environments in versions composer-2.*.*-airflow-*.*.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_environments_workloads_list_execute()` or `composer_projects_locations_environments_workloads_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_workloads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_workloads_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListWorkloadsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkloadsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/workloads
/// Lists workloads in a Cloud Composer environment. Workload is a unit that runs a single Composer component. This method is supported for Cloud Composer environments in versions composer-2.*.*-airflow-*.*.* and newer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_environments_workloads_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_environments_workloads_list_task()`.
/// For the simplest API, use `composer_projects_locations_environments_workloads_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_environments_workloads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_environments_workloads_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWorkloadsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_environments_workloads_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_environments_workloads_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsEnvironmentsWorkloadsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/environments/{environmentsId}/workloads
/// Lists workloads in a Cloud Composer environment. Workload is a unit that runs a single Composer component. This method is supported for Cloud Composer environments in versions composer-2.*.*-airflow-*.*.* and newer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_environments_workloads_list_builder()` + `composer_projects_locations_environments_workloads_list_execute()`.
/// For task-level control, use `composer_projects_locations_environments_workloads_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_environments_workloads_list(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsEnvironmentsWorkloadsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWorkloadsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_environments_workloads_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    composer_projects_locations_environments_workloads_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageVersions
/// List ImageVersions for provided location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_image_versions_list_execute()` to send, or `composer_projects_locations_image_versions_list` for simplest API.

pub fn composer_projects_locations_image_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    includePastReleases: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/imageVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includePastReleases {
        query_parts.push(format!("includePastReleases={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageVersions
/// List ImageVersions for provided location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_image_versions_list_execute()` or `composer_projects_locations_image_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_image_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_image_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListImageVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListImageVersionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageVersions
/// List ImageVersions for provided location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_image_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_image_versions_list_task()`.
/// For the simplest API, use `composer_projects_locations_image_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_image_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_image_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImageVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_image_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_image_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsImageVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: includePastReleases
    pub includePastReleases: Option<bool>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/imageVersions
/// List ImageVersions for provided location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_image_versions_list_builder()` + `composer_projects_locations_image_versions_list_execute()`.
/// For task-level control, use `composer_projects_locations_image_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_image_versions_list(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsImageVersionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListImageVersionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_image_versions_list_builder(
        client,
        &args.parent,
        args.includePastReleases,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    composer_projects_locations_image_versions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_operations_delete_execute()` to send, or `composer_projects_locations_operations_delete` for simplest API.

pub fn composer_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_operations_delete_execute()` or `composer_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_operations_delete_task()`.
/// For the simplest API, use `composer_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_operations_delete_builder()` + `composer_projects_locations_operations_delete_execute()`.
/// For task-level control, use `composer_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_operations_delete_builder(client, &args.name)?;
    composer_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_operations_get_execute()` to send, or `composer_projects_locations_operations_get` for simplest API.

pub fn composer_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_operations_get_execute()` or `composer_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_operations_get_task()`.
/// For the simplest API, use `composer_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = composer_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_operations_get_builder()` + `composer_projects_locations_operations_get_execute()`.
/// For task-level control, use `composer_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_operations_get_builder(client, &args.name)?;
    composer_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `composer_projects_locations_operations_list_execute()` to send, or `composer_projects_locations_operations_list` for simplest API.

pub fn composer_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://composer.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `composer_projects_locations_operations_list_execute()` or `composer_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `composer_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `composer_projects_locations_operations_list_task()`.
/// For the simplest API, use `composer_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `composer_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn composer_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = composer_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`composer_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComposerProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `composer_projects_locations_operations_list_builder()` + `composer_projects_locations_operations_list_execute()`.
/// For task-level control, use `composer_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn composer_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &ComposerProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = composer_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    composer_projects_locations_operations_list_execute(builder)
}
